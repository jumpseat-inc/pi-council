---
description: Bump the version (major|minor|patch), commit, tag, and move `latest`
argument-hint: "<major|minor|patch>"
---
Read `.pi/skills/bump/SKILL.md` and follow it.

Bump level: $ARGUMENTS

If the level above is empty or is not exactly one of `major`, `minor`, or
`patch`, stop and ask which level to use — do not guess.