node_modules/

# macOS
.DS_Store

# pi harness + transient hub state
.pi/git/
.pi/council/.pids.json
# Smoke test run artifacts (dot-dir so bun test never discovers them)
smoke/.artifacts/*
!smoke/.artifacts/.gitkeep
