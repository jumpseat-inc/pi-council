---
id: EV-11
title: Bounded decomposition session
state: Deliberating
owner: null
epic: EPIC-3
goal: The features-new decomposition session ends within a fixed round cap at an explicit convergence or fallback outcome on every run, mirroring the bounded deliberation of the council command, instead of exchanging for an unbounded number of rounds
---

## Intent

/council bounds its deliberation — a fixed round cap, a convergence state,
and a deterministic stop so a session cannot exchange forever. Once EV-10
puts seats into the decomposition, that same bound is required here — the
maintainer asked for the session to be bounded similar to the /council
command, and an unbounded multi-seat exchange would make /features-new
cost- and time-unpredictable at exactly the moment it feeds
/features-deliver's unattended runs.

This card adds the bounds to the rewritten procedure text — a stated round
cap, a convergence criterion (what the facilitator treats as settled enough
to draft), and a defined fallback when the cap is hit without convergence
(the facilitator drafts anyway and carries the open disagreements into the
step-3 draft pass for the human to settle).

Open questions for the council — the cap value (council.md's own cap is the
natural precedent), whether the fallback draft is written by the
facilitator alone or by the most-advanced seat position, and whether
non-convergence should be surfaced to the human as a distinct warning.

User-visible surface — none beyond the draft pass; the bound shows up as
run behavior and, when hit, as a note in the presented draft. Depends on
EV-10.

## Acceptance

- The procedure text states a numeric round cap, a convergence criterion,
  and a fallback outcome for non-convergence.
- The stated bound matches the deliberation bounds of council.md in shape
  (fixed cap, explicit stop) rather than inventing a new scheme.
- python3 council/validate.py stays green; step 3's gate text unchanged.

## Phase 1 rulings (recorded 2026 by the human before the run, immutable for this run, binding on every seat including steward)

- **Ruling CAP-1 (round cap):** the decomposition session is bounded at
  **3 rounds, the same exchange cap /council uses** — one consistent bound
  across the council's loops, per the council.md precedent named in this
  card's Intent.
- **Ruling FALLBACK-1 (non-convergence):** when the cap is hit without
  convergence, the **facilitator drafts the decomposition anyway** and
  carries every open disagreement into the step-3 draft pass, **labeled as
  unresolved**, for the human to settle at the approval gate the command
  already has. No new gate is created.

## Run record

### Step 1 — read and gate (council-runner, 2026)

- state `Ready` confirmed; card earns a Council run.
- **Full council** — the goal admits more than one reasonable design: where
  the bound text lives in the procedure, how the 3-round cap counts relative
  to the three waves, what counts as convergence for a decomposition
  session, and how the fallback interacts with the wave structure and the
  two-part gate presentation. The change touches a shipped payload file
  every /features-new run flows through.
- **Surface-touching: yes** — the deliverable is procedure text the human
  reads, and when the cap is hit, a note in the presented draft at the
  step-3 approval gate. Per council.md step 1, `designer` is seated as a
  third generator in steps 2–3.
- Rulings applied from the card face (not re-asked): **CAP-1** — the
  decomposition session is bounded at 3 rounds, the same exchange cap
  /council uses; **FALLBACK-1** — on cap-hit without convergence the
  facilitator drafts the decomposition anyway and carries every open
  disagreement into the step-3 draft pass, labeled as unresolved, for the
  human to settle at the approval gate; no new gate is created.
- Scope guard: the card's `goal` covers the bound text only. Step 3's
  draft-then-confirm text is byte-identical (pinned by test); no engine
  changes; no seat body changes. The bound text inserts into step 2 between
  the wave dispatches and aggregation, per the EV-10 spec's seam ("the
  first pass is named wave 1; aggregation consumes 'all recorded
  contributions'; the disagreement path is a ruling dispatch, not an
  exchange round").

### Step 2 — independent first pass, round 1 (council-runner, 2026)

Round 1 dispatched `owner`, `principal`, `designer` in parallel, each with
only the card (isolation preserved; no seat saw another's position). The
prior container's round-1 positions were lost to a mid-turn stall before
commit; this round re-runs the pass honestly. Positions appended verbatim.

**Principal (round 1, verbatim):**

> ## Cross-seam reading
>
> The contract that EV-11's bound text inserts into is **not an exchange loop**. The merged `features-new.md` step 2 is a total three-wave DAG built from single dispatches:
>
> - **Wave 1** — `principal` dispatched once, authors the first artifact alone (`features-new.md` lines 41–59).
> - **Wave 2** — `skeptic` + `designer` dispatched in parallel, identical input, once each (lines 60–73).
> - **Wave 3** — `product-owner` dispatched **last, unconditionally, ruling-only** (lines 75–90).
> - **Aggregation** consumes "all recorded contributions" verbatim; the facilitator "authors nothing" (lines 92–101).
> - The only re-dispatch anywhere is the **stall-retry** in the dispatch discipline: "cancel + one re-dispatch … on double-fail, stop" (line 144). That is a *reliability* bound, not an *exchange* round.
>
> The EV-10 spec's scope guard encodes exactly this: "the first pass is named wave 1; … the disagreement path is a **ruling dispatch, not an exchange round**" (`2026-09-03-EV-10-design.md`, "EV-11 seam"). By contrast, `council.md` step 3's "at most 2–3 rounds … stop early if positions have stabilised" bounds a *real* exchange — re-dispatching the same generator set against each other's latest positions. That loop does not exist in `features-new`. The merged text's `grep` for `converg|round|stop early` returns nothing but the stall-retry line — I verified this.
>
> So the seam has a lopsided shape that the card's "mirror council.md" framing misses: **council.md's cap terminates an otherwise-unbounded exchange; features-new's waves already terminate on their own, every run, with no cap.** The bound EV-11 writes is therefore not a termination guard — nothing loops to terminate — but a *statement of the DAG's totality plus a labeling rule* for the one genuinely unbounded residue: open disagreements left after wave 3.
>
> ## Blind spots
>
> **The producing slice (wave/aggregation author — the "data pipeline")** sees a fixed three-stage pipeline and will conclude the round cap is vacuous ("there's no loop, just write 'three waves, never re-run'"). What it cannot see from inside the pipeline is the *downstream unattended consumer*: `features-deliver`'s merge check rejects any card with "no `Needs Human` state or outstanding ruling" (per EV-10's O1 answer). The fallback's "draft anyway, labeled unresolved" is only safe because that label is **presented, never written** — the human's approval at the existing gate *is* the resolution (EV-10's settled step-6 ruling: "persisting a pre-approval dispute snapshot to disk is stale metadata"). If the bound text lets "unresolved" become a persisted card state, the fallback defeats its own purpose and gets the card set rejected downstream. This slice must state presentation-only, not just assume it.
>
> **The rendering slice (step-3 gate presentation)** sees the ledger surface already renders `Disagreements:` + `Decision: unresolved — your call`, and will conclude the fallback already has a home ("just add a note"). What it cannot see from inside the render path is that **cap-hit is session-scoped, not per-card**. A *converged* run can still produce a per-card dissent in the ledger (a seat didn't endorse one card); that is routine. A *non-converged* run flags the entire decomposition. If these two states render identically, the human at the gate cannot tell a routine one-card dissent from a whole-session fallback — and the card's only conceded user-visible surface ("when hit, as a note in the presented draft") is then indistinguishable from noise. The note must be a single session-level flag in Part 2, distinct from the per-card disagreement rows.
>
> ## Reframe
>
> The proposed design needs one concrete correction: **do not write the bound as a cap on exchange rounds, and do not import council.md's "stop early if stabilized."**
>
> 1. **The cap counts waves, not exchanges.** The three waves *are* the three rounds. The bound text reads: "exactly three waves; no wave is re-run; no fourth dispatch." One clause must explicitly separate the stall-retry from the round count — "a stall re-dispatch is a dispatch retry, not a new round" — otherwise an owner will either count retries as rounds and falsely trip the cap, or fear the cap and skip a retry the dispatch discipline mandates.
>
> 2. **Convergence is a label at the fixed endpoint, not a stopping condition.** council.md's "stop early if stabilised" has **no valid analog** here, because wave 3 is unconditional — SEATS-1 binds "all four seats dispatched every run," and the card's own step 2 says product-owner rules "last, unconditionally." You cannot skip product-owner to stop early. So convergence must be defined as a *predicate evaluated after wave 3*, and the text must say so — otherwise an owner merging council.md's shape literally will "stop early" and break the unconditional wave-3 dispatch.
>
> 3. **Convergence = zero open disagreements — not unanimity.** "Open" = not ruled by product-owner (ratify/amend, dissent named) AND not settled by a runnable check. A named dissent that *received a ruling* is converged. This matters because product-owner's whole format is "dissent named"; if "any dissent" meant non-converged, then every run where skeptic objects forces the fallback, and the "convergence criterion" collapses into a tautology. State it so.
>
> 4. **The fallback author question is already answered by the wave structure — and FALLBACK-1's wording needs one reconciliation.** "Facilitator drafts anyway" must be read as *the facilitator completes the same mechanical verbatim aggregation and proceeds to the step-3 pass* — **not** "facilitator authors card text," which would collide with the hard "facilitator authors nothing" mandate and the attribution-free Part 1 pin. Symmetrically, "most-advanced seat drafts" is also wrong: product-owner already *is* the last ruling and already sits in the aggregation; giving it a second authorship pass converts ruling into generating, the exact boundary EV-10 drew. There is no third author — the aggregation is the draft.
>
> 5. **Where the text lives:** step 2, at the EV-10 scope-guard seam — after wave 3, at the head of (or immediately before) the **Aggregation** subsection. Never step 3 (byte-identity pin), never steps 0/1/4/5.
>
> 6. **Non-convergence as a distinct warning: yes, but only in Part 2, once, session-scoped, never written.** The note's sentence must be physically co-located with "presented, never written … does not survive onto the on-disk card," so it cannot leak into card frontmatter/`Intent` and trip the attribution-free pin.
>
> ## Testable claims
>
> Disagreements, as runnable assertions (mirroring the existing `prose.test.ts` pins):
>
> 1. **Wave-totality clause.** A whitespace-normalized `expect(flat).toContain("no wave is re-run — no fourth dispatch")` (or equivalent), plus a co-occurrence assertion that the stall-retry is declared "not a new round" — failing today, green only when the bound text lands.
> 2. **Convergence-at-fixed-endpoint clause.** `flat` contains a clause in the form "product-owner always runs last; convergence is recorded at the fixed endpoint, never by stopping early" — failing test pins the no-early-stop property against an owner importing council's "stop early if stabilised."
> 3. **Non-convergence = zero-open, not unanimity.** `flat` contains "a named dissent is not non-convergence" (or structurally equivalent), pinning that a ruled dissent is converged.
> 4. **Bound-siting.** `text.indexOf(convergence-clause) < text.indexOf("## 3. Draft-then-confirm")` and the existing `STEP3_FIXTURE` byte-pin stays green — proving the bound lives in step 2 and step 3 is untouched.
> 5. **Presentation-only note.** A negative assertion: the sentence that names the session-level "non-converged" note also contains "presented, never written" (same flat-normalized check as the existing attribution pin), closing the render-vs-persist seam the downstream merge check depends on.
>
> If probe 3 is not pinned, the likely implementation error is "any dissent ⇒ fallback," which turns every skeptical objection into a false non-convergence and re-opens the exact unbounded-behavior illusion the card exists to close.

**Designer (round 1, verbatim):**

> **Design position** — Add a `Bounded session and fallback` block after Wave 3 and before aggregation. The three-round cap is the three required waves: wave 1, wave 2 as one parallel round containing both dispatches, and wave 3; there is no valid fourth exchange and convergence is checked only after wave 3. Convergence does not require unanimity: every in-scope open judgment must have a recorded product-owner ruling or explicit escalation. On non-convergence, the facilitator remains the sole renderer of a mechanical fallback draft from all recorded contributions—never the selected "most advanced" seat—then continues through the existing approval gate with every disagreement carried as unresolved.
>
> **Gulf closed** — **Evaluation**, for the facilitator deciding when to stop and the human evaluating whether a presented decomposition is a settled result or a bounded fallback.
>
> **Principle and evidence** — The hard cap and stop declaration are a **constraint**; the parallel-wave count is a **conceptual model**; the explicit status line is a **signifier and feedback**; carrying disagreements separately is a **forcing function against laundering uncertainty into card text**. This applies to `council/procedures/features-new.md`, between Wave 3 and Aggregation, using the three-wave contract in `docs/superpowers/specs/2026-09-03-EV-10-design.md` and the bounded-exchange precedent in `council/procedures/council.md` steps 2–3. The wiki's [[facilitator]], [[council-loop]], and [[engineering-board]] pages establish the facilitator's non-deciding and counting roles, but do not cover this decomposition-specific EV-11 seam; the EV-10 specification is therefore controlling. The presentation must remain outside Part 1 so `test/prose.test.ts`'s byte-identity and attribution-free pins continue to hold.
>
> - Recommended preface copy:
>   > `Session status: Non-converged after 3 rounds — this is a fallback draft. Part 2 lists every disagreement as unresolved for your decision at the existing approval gate.`
> - This is a preface, not a third gate or a third presentation part. The status must not be inserted into any card's `Intent`; each Part 2 disagreement retains the existing `Decision: unresolved — your call` marker.
> - A timeout followed by its allowed re-dispatch remains one round. The existing double-fail stop remains a separate incomplete-run outcome; it cannot masquerade as the fallback because aggregation has no complete Wave 3 contribution record.
>
> **Falsifiable predictions** —
>
> - A pure-text fixture test can feed the procedure a settled Wave 3 ruling and assert that it declares convergence, invokes the ordinary aggregation path, and emits no fallback warning; a second fixture with an unruled in-scope judgment must emit exactly one fallback status and carry every dispute in Part 2. The test is falsified if a fourth wave/exchange is permitted, Part 1 gains deliberation attribution, or a Part 2 disagreement lacks `Decision: unresolved — your call`.
> - A CLI smoke using a forced non-convergence seed should show three completed rounds in the transcript, with the skeptic and designer dispatched together in round 2 and no post–Wave 3 seat dispatch. The existing board must remain untouched until approval; this is a procedural smoke, not a CDP assertion.
> - `test/prose.test.ts` should remain green unchanged, while mutation of the Step 3 block should continue to fail its byte-identity assertion. No visual smoke is warranted because this change alters Markdown procedure contracts, not rendered layout.
>
> **Preferences, ranked last** — The exact capitalization and punctuation of the status line are minor copy preference. The grounded requirement is that it unmistakably says "non-converged," "fallback draft," and "existing approval gate" while remaining outside the attribution-free Part 1 card text.

**Owner (round 1, verbatim):**

> I've read the four context files, the product-owner seat body, and confirmed `validate.py` parses only cards/board (grep: no `procedures`/`features-new` reference), so a step-2 prose insertion cannot break the validate gate. Position follows.
>
> ---
>
> ## Position — EV-11: bounding the decomposition session
>
> **Approach**
>
> The bound is structural, not a new loop. The EV-10 seam guard already settles the shape: "the disagreement path is a ruling dispatch, not an exchange round" (`2026-09-03-EV-10-design.md`). So EV-11 must **not** introduce an exchange loop with a cap on it — it must declare the three waves *to be* the three rounds, and define convergence/fallback as properties of that fixed pipeline. Concretely, two insertions into step 2 of `features-new.md`, both inside the EV-10 seam (between dispatch and aggregation), neither touching step 3:
>
> 1. **One sentence in the step-2 intro** (line ~40, after "in three waves. You route, wait, aggregate verbatim, and author nothing."): the session is bounded at three waves = three rounds, the same cap `/council`'s step 3 uses; disagreements are ruled in wave 3, never exchanged — no seat is re-dispatched to respond to another seat's position. This sentence is load-bearing and must precede wave 2, because the facilitator's strongest pattern-match temptation is council.md's step-3 re-dispatch loop, and it arises *between* wave 2 and wave 3 — a block placed after wave 3 would be read too late.
>
> 2. **A "Bounded session" block between Wave 3 (line 90) and Aggregation (line 106)** — the seam's literal insertion point — carrying the convergence criterion, the fallback, and the cap-hit note.
>
> **How the cap counts.** The round is the wave, never the dispatch and never an exchange iteration. Wave 1 = round 1, wave 2 = round 2 (its two parallel dispatches are *one* round — the text must say this, or a dispatch-counting facilitator miscounts at 4), wave 3 = round 3. A stalled dispatch's cancel + one re-dispatch (dispatch discipline, line 142) is not a round. The cap is therefore never "approached" — it is the structure; the fallback is about reaching the end of the structure without convergence, not about exceeding it. Deliberately *not* imported from council.md: its "stop early if positions have stabilised" — features-new has no early stop, because wave 3 "rules, last, unconditionally" (line 90). The shape match with council.md is fixed cap + explicit deterministic stop (the session always ends after wave 3, converged or not), not early-stop.
>
> **Convergence criterion.** The session has converged when wave 3 has ruled every open-judgment dispute the attackers surfaced — epic goal and each child's `state` ratified or amended, each dispute ruled with dissent named — so that the aggregation's disagreement ledger contains only **named dissents** (seats that did not endorse), never unruled disputes. This is mechanically checkable by the facilitator: any dispute in the ledger without a wave-3 ruling = non-converged. Non-convergence's concrete instance is product-owner escalation (its `<escalation>` forbids portfolio change, reversing a recorded human decision, or a defective goal; per SEATS-1 that escalation goes to the human). FALLBACK-1 then subsumes escalation: the facilitator drafts anyway, and the escalated item rides to the step-3 gate as an unresolved disagreement — no separate Needs-Human stop, because unlike council.md (whose human gate is step 11), features-new's step-3 approval gate *is* the human decision point. "No new gate is created" is exactly this reasoning, and it is consistent, not a weakening.
>
> **Fallback authorship.** The fallback draft is the **mechanical aggregation of all recorded contributions** — identical to the converged case, including wave 3's amendments — never a facilitator-authored synthesis and never a "most-advanced seat position" base. Picking product-owner's amended draft as the base would be the facilitator choosing a winner, which the facilitator is forbidden to do (council.md: "The facilitator decides nothing"; EV-10: "authors nothing at any step"). The wave-3-amended draft naturally dominates the aggregation anyway — but as a property of the aggregation, not a facilitator choice. The fallback changes only the ledger content, never the wave structure and never Part 1: the unresolved items reuse the existing `Decision: unresolved — your call` markers (line 114's ledger contract), and the cap-hit note is a Part 2 line, never Part 1 card text (the attribution-free mandate at line 127 is untouched).
>
> **Distinct warning — yes, one line.** The existing `Decision: unresolved — your call` markers cannot distinguish "converged with named dissents" from "cap hit without convergence", and the human's decision posture differs between the two (approving a settled draft vs. settling disputes the seats could not). The card's own user-visible surface says "when hit, as a note in the presented draft". So: one line at the top of the ledger surface — the session did not converge within its round cap; the listed disagreements are unresolved and require the human's decision. Presentation-only, never written to disk, never a new gate.
>
> **Tradeoffs accepted**
>
> - **No exchange loop at all** — a re-dispatch exchange could in principle sharpen the decomposition, but it is what the card's goal forbids (cost/time unpredictability feeding `/features-deliver`'s unattended runs) and what the EV-10 seam explicitly excludes. The bound is worth more than the marginal convergence quality.
> - **Escalation rides to the gate instead of stopping the run** — a facilitator might otherwise halt on product-owner escalation; FALLBACK-1's draft-anyway means the human sees a full draft plus the unresolved marker at the one gate they already own. The cost is that a defective-goal escalation produces a draft of a defective goal — but the human authors the goal's source (`$ARGUMENTS`) and can edit/reject at the gate, so the risk is contained.
> - **No early-stop** — the session always pays for wave 3 even when wave 2 surfaces nothing. That is the price of "unconditionally" and of a deterministic stop; it is one dispatch, and it buys the ruling that makes the ledger complete.
>
> **Testable claims**
>
> 1. **Cap-parity pin (TDD, red now, green after EV-11):** `features-new.md` step 2 contains "three waves" and "not an exchange round" (or the equivalent no-re-dispatch statement), and the numeric cap equals council.md's — both name three. Written against today's text it fails; it passes only when the bound text lands.
> 2. **Convergence/fallback pins:** the step-2 text contains a convergence criterion (every surfaced dispute ruled by wave 3) and a fallback (cap hit without convergence → facilitator drafts anyway, disagreements carried labeled unresolved). The fallback paragraph must contain "aggregate" and must not instruct the facilitator to resolve or pick a seat's position — pinning the mechanical-aggregation answer to the "most-advanced seat" question.
> 3. **No-new-gate pin:** the set of `## N.` headings in `features-new.md` is unchanged (steps 0–5); the existing step-3 byte-identity pin in `test/prose.test.ts` stays green; the attribution-free pin stays green (the cap-hit note is defined as a Part 2 ledger line).
> 4. **Behavioral bound (smoke, per SMOKE-1):** the scratch-copy smoke run's job-id record shows exactly the four named dispatches (principal; skeptic + designer; product-owner) across three waves and **no re-dispatch of any seat with another seat's position** — the runnable evidence that the session "ends within a fixed round cap".
> 5. **validate.py unaffected:** it parses `council/cards/` and `council/board.md` only (verified by grep — no `procedures` reference), so the step-2 insertion cannot regress the gate; the acceptance's "validate.py stays green" holds by construction.
