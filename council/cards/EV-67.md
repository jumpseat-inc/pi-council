---
id: EV-67
title: Gate verdict rendered as information at the step-3 approval gate
state: Deliberating
owner: null
epic: EPIC-13
goal: At /features-new step 3 the approval gate renders exactly one additional line per card naming the resolved mode and a one-line basis, and a test over the render function proves the approve, edit, and drop options and the printed card body are byte-identical to the pre-gate rendering.
---

## Intent

Rollout step 3 enables the gate for the mechanical class and surfaces its
decision inside the approval gate that already exists, in the command that
already produces the cards. The single decision gate stays the single decision
gate: the human still approves, edits, or drops each card exactly as before, and
the verdict arrives as information attached to the card they are already
looking at — one line per card, naming the mode and the one-line reason.

That one line is the whole surface, and it is where the honesty of the feature
lives for a human. A verdict that reads as an instruction converts the existing
approval gate into a second decision point by implication; a verdict that reads
as a fact about the card's shape lets a human overrule it by editing the card,
which is the intended affordance. The line must also be readable when the gate
had nothing to say — a failed call resolves to the full mode and says so —
because a missing line would read as "no routing" and an invented one would read
as confidence the system does not have.

The basis is not authored at render time. It is the string the decision function
recorded, derived deterministically from the answers and the override that fired,
so the line a person reads and the ledger record they can audit cannot drift
apart. The rendering is a pure function of the recorded decision and the card,
so the byte-identity test is cheap and is the load-bearing evidence that no
second gate was introduced.

## Acceptance

One line per card, containing the resolved mode and a one-line basis; the
approve/edit/drop prompt and the card body text are byte-identical to the
pre-gate rendering in a golden test; a card whose gate call failed renders the
fallback mode with the failure named rather than omitting the line; and no
additional prompt, keystroke, or state transition exists in the step. The line
renders as `Mode: <mode> — <basis>`, placed immediately below the card body and
above the approve/edit/drop prompt; `<basis>` is the string recorded by the
decision function, rendered verbatim, and a test proves the render function
receives it as input and produces no prose of its own; a card whose gate call
failed renders `Mode: Deliberate — gate call failed: <reason verbatim>`.

## Phase 1 Rulings (recorded before dispatch — binding for this run)

- **R1 — merge authorization, every card in this run.** The human authorized
  the run-scoped admin bypass: the merge is `gh pr merge <PR> --squash --admin
  --match-head-commit <X>`, where `<X>` is the exact head SHA the `gates`
  workflow `SUCCESS` (merge-check criterion 2) was read against. This
  authorization is for this run only and is not extended to any later run; a
  SHA mismatch is a HALT, not a retry.
- **R3 — packaged gate default.** The packaged `council/gate/policy.json` ships
  `mode: "off"` (no gate call, no ledger line by default); consumers opt in per
  repo via a repo-local `policy.json`. Tests that exercise the gate set the
  mode explicitly; the default is never relied on to be on.

## Deliberation record

### Round 1 — independent first pass (step 2)

#### owner (job-18.1)

Grounding is complete. Citing: `extensions/gate-ledger.ts` (the `basis` doc comment pinning `"Mode: " + resolvedMode + " — " + basis`), `extensions/gate-tool.ts` (verdict-free result; `callId: null` on unrecorded failure), `extensions/gate.ts` (`decide()` basis strings, fail-closed constants), `extensions/gate-run.ts:165-186` (`failRun` writes the failure basis into the one v2 line), `council/procedures/features-new.md` steps 3–4, the EV-66 card record (Synthesis: result-opacity + "the printed verdict line belongs to EV-67"; Outcome), wiki `presented-never-written`, `metered-deliberation-routing`, `gate-parity`, `procedures-vs-commands` (index), the canary at `test/ev66-advisory-intake.test.ts:366-375`, and the registration seam at `extensions/index.ts:616`.

**Approach**

**1. Where the recorded decision comes from at presentation time — a second, dedicated presentation-time read over the committed ledger, keyed on the callIds the gate call already returned. Reject extending the `council_gate` result.**

The card's own sentence decides this: the basis "is the string the decision function **recorded**." The only seam where that string is guaranteed byte-identical to what an auditor can read is the ledger itself, read at presentation time. Concretely:

- **New pure module `extensions/gate-render.ts`** with the pinned function:
  ```ts
  export function renderGateModeLine(decision: { resolvedMode: string; basis: string }): string
  ```
  whose entire body is `` `Mode: ${decision.resolvedMode} — ${decision.basis}` `` (em dash U+2014). Type-only import edge to `gate-ledger.ts`, mirroring the gate-run pattern. It deliberately takes **no card** — I reject the card-body-as-input framing (tension 1's "pure function of the recorded decision and the card"): a function that receives the card could in principle touch it; a function that never receives it is byte-identity by construction. The card's placement requirement (below body, above prompt) is realized in the procedure text, not in a function input.

- **New parent-session tool `council_gate_render`**, registered from `index.ts`'s parent path only (its own registration beside `registerGateTool` at line 616, never in `registerHubTools`), implemented in `gate-tool.ts`. Parameter: `cards: Array<{ id: string; callId: string | null }>` — the {id, callId} pairs **verbatim from the step-3 `council_gate` result's `cards[]`**. This is the whole reason EV-66's "mechanical facts" shape is perfect: the opaque randomUUID callId EV-66 deliberately kept uncorrelatable from the reading model becomes the exact join key for presentation, and the gate-call result's bytes do not change by one character. The tool calls `readGateLedger(repoRoot)`, matches records by `callId` in input order, and returns `{ cards: [{ id, modeLine }] }` where `modeLine` is the exact string to print. It **loads no policy** — the committed ledger is the single source of truth; it performs **zero writes**; it touches no widget.

- **Why not extend the gate-tool result (the weighed candidate):** it would amend EV-66's converged, falsifier-pinned opacity contract (the §5 steering-branch and result-opacity scans in `test/ev66-advisory-intake.test.ts` target the gate call's result specifically) and would put the verdict into the parent context at the dispatch seam, reopening a settled ruling. The separate read moves the verdict through a seam whose *declared purpose* is presentation, after all dispatches are done. The interference posture is preserved *at the seam it was ruled for*; EV-67's card is precisely the re-opening of the channel at the presentation seam, so no settled decision is contradicted — the human-facing verdict was always EV-67's to carry. Residual (same terms as every procedure step, `procedures-vs-commands`): the facilitator calling `council_gate_render` earlier than step 4 is LLM-obeyed prose discipline, not engine-enforced; there is no engine event for "presentation about to happen."

- **Canary reconciliation.** `test/ev66-advisory-intake.test.ts:366-375` scans `extensions/*.ts` (minus `gate-ledger.ts`) for `readGateLedger` / `gate-ledger.jsonl` and expects `[]`. EV-67's `gate-render.ts` is the first legitimate production reader and must be enumerated in the canary — the invariant narrows from "no module references the ledger" to "no module outside {gate-ledger.ts, gate-render.ts} references it; gate-render.ts contains no `appendGateCall`" (a read-only-posture assertion added in the same test). The canary's *force* survives: no module may act on the ledger to change engine behavior; EV-69/EV-70 read it later under their own cards. This is an honest narrowing recorded in the test comment, not a suppression.

**2. The procedure edit — step 4, additively; step-3 naming is stale and gets corrected.**

The gate **call** is step 3; the approval gate is **step 4**. The card title/goal's "step-3 approval gate" references are off by one — the implementation edits step 4, and the card record's title/goal wording should be corrected at implementation time (a record edit, flagged, not silently absorbed). The step-4 edit:

- A self-contained block, additive only: *when the step-3 gate call returned per-card results* (i.e., `policyMode` was not off), invoke `council_gate_render` ONCE with the per-card `{id, callId}` pairs from that result; for each card print the returned `modeLine` **verbatim**, **exactly once**, immediately below that card's body, above the approve/edit/drop prompt; add no words of your own — no framing sentence, no paraphrase, no reordering, no instruction reading; the human's approve/edit/drop options are unchanged and unprejudiced by the line.
- The mode line is **presented, never written** (the `presented-never-written` pattern, third member after Part-1 attribution-freedom and the Part-2 disagreement ledger): it never reaches the card file, the board, or any on-disk artifact. The card body written to disk is exactly the pre-gate body.
- Off / skipped call (R3): **no lines render at all** — the presentation is byte-identical to pre-gate. This is the correct answer to tension 4's "does a line render under off": under off there *is* no routing decision, so a missing line reads truthfully; rendering one would be the "invented confidence" the Intent forbids.
- If the human edits a card at the gate, the line stays as recorded — it is information about the draft *as gated*; the human's edit stands; EV-69 owns the text-changed-after-record consequence.
- Failed call: the returned line already carries it (below).

**3. The failure paths — three cases, two rendered, one skipped.**

- **Recorded failure** (ledger line exists, `basis === "gate call failed: " + <verbatim reason>`): the render is a pure template over the record, so it byte-joins EV-65's pinned string *automatically* — `Mode: Deliberate — gate call failed: <reason verbatim>`. The render adds no sanitization; the reason renders verbatim exactly as the ledger composes it (if a transport reason ever contained a newline, the ledger line and the presented line break identically — one drift surface, not two; accepted residual).
- **Unrecorded call** (`callId: null` — pre-POST guard throw, append failure; the gate tool deliberately surfaced only a generic message): no record exists, so no verbatim reason exists to render — but omitting the line would read as "no routing" and the fail-closed posture says the card is unmetered ⇒ full mode. Position: the render tool returns the fallback `Mode: Deliberate — gate call failed: no ledger line recorded` for any requested callId with no matching record (also covering torn-tail lines and an absent ledger file — the tolerant reader returns empty, the fallback line is honest). The exact fallback literal is a step-4/PO copy call; the mechanism (TS-rendered, names the failure, never omitted, resolvedMode Deliberate) is the position.
- **Recorded success**: `Mode: <resolvedMode> — <basis>` from the record; `outcome`-joined fields are ignored (at `/features-new` presentation no outcome lines exist; the render reads only the call record's `resolvedMode` + `basis`).

**4. The golden test — three files, each pinning a different byte surface.**

- **`test/gate-render.test.ts` (pure):** (a) fixed golden bytes, e.g. `renderGateModeLine({resolvedMode:"Verify", basis:"composite 1.20 ≥ verify threshold 1.10"})` === `"Mode: Verify — composite 1.20 ≥ verify threshold 1.10"`; the separator asserted `=== "\u2014"`. (b) **The no-prose proof**: for a battery of adversarial inputs (basis containing the words "approve"/"edit"/"drop"/newlines/unicode; all three modes), the output equals the manually composed `"Mode: " + mode + " — " + basis` — any authored prose, truncation, or sanitization breaks equality. This is how "receives it as input and produces no prose of its own" is mechanically proven: template equality over the exact input strings, plus the type signature that admits nothing else. (c) Failure golden: the failure-basis record renders the pinned fallback form byte-equal to `"Mode: " + "Deliberate" + " — " + "gate call failed: " + reason`.
- **`test/gate-tool.test.ts` additions (the render tool):** mkdtemp + `appendGateCall` fixtures → per-card `{id, modeLine}` byte-equal, input order preserved; unknown/`null` callId → fallback line; the result contains no card-body text and no prompt/option vocabulary; zero writes (no `appendGateCall` reachable — canary-style source assertion); result shape golden.
- **`test/prose.test.ts` additions (the LLM-presented surface — the only mechanical gold available for it, the EV-66 procedure-placement precedent):** step 4's pre-existing presentation instruction ("exactly as each would be written to disk") present byte-unchanged; the new block present with pinned phrases (verbatim-once, placement, "add no words", presented-never-written extension, off-skip rule); and — the "no second gate" evidence — the render tool's result schema carries no prompt text and the write path (step 5) gained no mode-line instruction. Honest scoping: TS-produced bytes get golden byte tests; LLM-presented bytes get procedure-text pins, because the presentation surface has never been TS (`procedures-vs-commands`).

**Tradeoffs accepted**

- **A second tool call and a second parent-tool registration** instead of one amended result — one extra LLM tool call per `/features-new` run, traded for leaving EV-66's settled opacity contract and its falsifier untouched, and for the ledger-as-source guarantee the card's intent sentence demands.
- **Canary narrowing** — the "no reader" invariant becomes an enumerated-allowlist "no actor" invariant. Worth it because EV-67 is exactly the card the ledger was built to serve; the read-only-posture assertion keeps the canary load-bearing.
- **Verbatim failure rendering, no sanitization** — a pathological multi-line transport reason would wrap in the presentation. Accepted: the ledger line has the same property; fixing it would mean authoring prose at render time, which the card forbids.
- **Residual: presentation-verbatim discipline is LLM-obeyed.** The TS byte pin plus the procedure-text pin is the strongest mechanically falsifiable surface without building a TS presentation renderer (out of scope, would require echoing card bodies through tool results — a larger, drift-prone surface for no gain).

**Testable claims**

1. `renderGateModeLine` output === `"Mode: " + resolvedMode + " — " + basis` (U+2014) for every battery input, including a record whose basis is the pinned EV-65 failure string — red today (function does not exist), green at head.
2. The `council_gate` tool result remains byte-shape-identical to EV-66's (`{policyMode, cards:[{id,callId,status}]}`, no verdict token) — the existing EV-66 opacity assertions pass **unmodified**; the verdict reaches the parent context only via `council_gate_render`.
3. `council_gate_render` performs zero writes and zero policy loads: source canary (no `appendGateCall`, no `loadGatePolicy` reference) + behavioral test on a mkdtemp repo (tree hash unchanged after a render call).
4. Canary after the change: only `gate-ledger.ts` and `gate-render.ts` reference the ledger; `gate-render.ts` contains no write-path symbol; the narrowed message names both modules.
5. Unrecorded callId → fallback line rendered, never omitted; off-mode run → the render tool is never invoked per procedure copy (prose-pinned) and an accidental invocation with the off-run's empty `cards[]` returns zero lines.
6. Prose test: step 4's pre-existing presentation sentence byte-unchanged; the new instruction block is additive and pins verbatim-once/placement/no-prose/never-written/off-skip.

#### principal (job-18.2)

**Grounding read:** `vault/wiki/index.md` → `metered-deliberation-routing.md` (still the Backlog intent page — describes only the three lanes, **not** the ledger/tool/canary), `presented-never-written.md`, `three-wave-decomposition.md` (both still call the approval gate "the step-3 gate"). The wiki does not cover the gate lineage; every mechanism claim below is read from code. Key files: `extensions/gate-tool.ts`, `gate-ledger.ts`, `gate-run.ts`, `gate-states.ts`/`gate-state.ts`, `gate.ts`, `council/procedures/features-new.md`, `test/gate-tool.test.ts`, `test/ev66-advisory-intake.test.ts`.

## Cross-seam reading

**The contract as it stands, both sides.**

- The producer side (`gate-tool.ts:57-72`, `:145-150`, `:180-190`) hands the parent exactly `{ policyMode, cards: [{ id, callId, status }] }`, `callId: null` on a thrown pre-POST/append failure with the generic message; off is `{ mode:"off", recorded:0 }` with **no per-card array at all**.
- The value store (`gate-ledger.ts:78-130`) carries `resolvedMode` and `basis`, and `:103-104` already pins `"Mode: " + resolvedMode + " — " + basis` as byte-equal to EV-67's render. **The record has no card id and no title.** Its only per-card correlate is `stateHash` — a hash over a packed state whose sections are `card, touchedFiles, wiki, rulings, tests` (`gate-state.ts:29`), i.e. a function of the *repo tree*, not of the card alone. The file is append-only and **committed by default**, so it accumulates across runs.
- The consumer side: `features-new.md:209-210` currently says *"do not print the recorded verdict — the human sees the card text at the step-4 gate, not the gate's verdict."* EV-67's entire deliverable is that sentence inverted. The procedure, the card title/goal ("step-3 approval gate"), `presented-never-written.md`, and `three-wave-decomposition.md` are all on the old numbering; `features-new.md:192` is the gate call, `:215` is the approval gate.

**So the seam is not "where does the verdict cross" — it is "what is the join key, and who is allowed to read".** Two mechanical fences already stand in the way, and they are not the same kind of thing:

1. `test/gate-tool.test.ts` test 4 (recursive key walk + `/\b(Deliberate|Verify|Direct|Mode:)\b/` over every string) and test 7 (exact generic message, no `chat/completions`, no verdict token) — **pinned EV-66 falsifiers**. Extending the tool result to carry `basis` breaks these by construction.
2. `test/ev66-advisory-intake.test.ts:363-385` — the canary: no `extensions/*.ts` outside `gate-ledger.ts` may contain `readGateLedger`/`gate-ledger.jsonl`, and `gate-ledger.ts`'s only runtime importer may be `gate-run.ts`. A presentation reader breaks *both* clauses.

**This is the finding worth stating plainly: the canary is already falsified by the epic's own later cards.** `EV-71.md` acceptance requires the reconciliation path to accept "decision generation ids sourced from the gate ledger" — that is a second production reader, contracted before EV-67 is even deliberated. So the canary is not a constitution EV-67 must work around; it is a test that encodes a claim the epic has since retracted, and EV-67 is simply the first card required to amend it. If EV-67 amends it as a one-card carve-out, EV-71 amends it again.

**Join key:** the robust key is `callId` — the only per-card correlate the parent context holds, and the thing `gate-tool.ts` deliberately makes opaque (`randomUUID`, never `opts.callId`). A `stateHash` join is unsound: identical draft text in two runs collides in a committed, append-only file, and the hash also moves if `wiki/` or `rulings/` change between the call and presentation. That makes EV-66's opacity *load-bearing for EV-67* rather than an obstacle to it: opacity is exactly what forces the join through an opaque id instead of letting the verdict ride the result.

**Blind spots**

- **Inside the pipeline (owner/data vantage):** sees `GateLedgerRecord` as a self-describing complete record. Cannot see that the presentation layer has no way to say *which card* a line belongs to; the natural instinct — "read the ledger, match the card" — produces a tree-dependent hash join that silently mismatches. Also cannot see the ledger is committed and multi-run.
- **Inside the render (frontend vantage):** sees a pure function and a golden test. Cannot see that the actual bytes the human reads are produced by the LLM facilitator from prose — so "byte-identical in a golden test" is a claim about a *function*, not about the surface. Nothing in the repo renders a card body today; the golden baseline does not exist to compare against.
- **Inside the acting path (EV-69/70/71 vantage):** treats the ledger as *its* input and reads the canary as protection. Cannot see that EV-67 puts a second reader on the same file, that the fence they rely on will be the thing amended, or that EV-70's Phase-3 "recorded execution mode beside its merge basis" is a second surface that will drift from EV-67's line if two format strings exist.

**Reframe**

The proposed seam is *not* sound as the card words it, and the smallest honest reframe is:

1. **Join by `callId`, read once, at presentation.** One new parent tool (`council_gate_present`) takes the *mechanical* step-3 result (`{id, callId, status}`) and returns one line per card. No `stateHash` re-packing, no extension of `council_gate`'s result.
2. **The byte composite moves to the record layer.** Export `decisionLine(record)` from `gate-ledger.ts` — where `:103-104` already asserts the byte equality — and have every surface (EV-67's line, EV-70's Phase-3 entry) call it. No surface owns a format string; the presentation module becomes a pure leaf with no unique knowledge.
3. **Amend the canary to a role-enumerated accessor allowlist**, shared with EV-71: `{gate-run.ts: writer, gate-ledger.ts: owner, gate-present.ts: presentation, <EV-71 module>: accounting}`, *plus* a separate directionality assertion that no module reachable from the routing/merge/execution import graph imports `gate-present.ts`. That is what keeps "the recorded line makes no policy change effective" true while admitting the readers the epic already requires.
4. **Replace the acceptance's unconditional "one line per card" with a pinned literal table**, because three of its four cells are unnamed today:
   - record found → `"Mode: " + resolvedMode + " — " + basis` (uniform; recorded failure is *not* a branch, since `gate-run.ts:170-172` writes `resolvedMode:"Deliberate"` + `basis:"gate call failed: …"` into the same two fields — the pinned failure string is a consequence of the rule, and a test can assert that).
   - `status:"failed"`, `callId:null` (the forbidden-path/noul-join/append-throw path, `gate-tool.ts:180-190`) → **no record exists and none can exist**: the verbatim reason was deliberately scrubbed. EV-67's pinned `Mode: Deliberate — gate call failed: <reason verbatim>` is **unsatisfiable here**; the card must pin a third literal and own it as render-time prose (an explicit, named exception).
   - recorded `callId` with no matching line (ledger reverted/truncated) → a fourth state, currently unnamed; conflating it with the above is dishonest.
   - off → see below.

**Q2 (timing).** Verified: `features-new.md` contains `council_dispatch`/`council_wait` only at `:187-188`, inside step 2; the `## 3.` heading is at `:192` and nothing after it dispatches. So presentation time is not merely *safe* — it is *after the last dispatch by construction*. But that ordering is prose-enforced; the engine has no guarantee, and any future step that dispatches after the verdict is rendered silently re-opens the channel. That deserves its own fence.

**Q3 (off).** Off must render **zero additional lines**, and the acceptance must be scoped to gate-enabled runs. Three grounds: (a) the basis must be the string the decision function recorded, and under off *nothing is recorded* — any line is render-authored prose; (b) off is the packaged default (`council/gate/policy.json` ships `mode:"off"`), so per-card off-lines change the default rendering of every consumer's `/features-new` for a feature that ships off, and the byte-identity baseline would encode them; (c) "no routing" is *true* under off, so the absence is not the misleading case the intent fears — the intent's fear applies to a failure where routing was expected, and that case keeps its line. Absence-of-line is decidable: the tool result mechanically says `{mode:"off"}`, and a *skipped* call produces no tool result at all. Rendering `Mode: Deliberate — gate off` would be the invented-confidence failure in the card's own words.

**Q4 (naming drift).** Card title, goal, and acceptance must move to "step-4 approval gate" (gate call = step 3), and the procedure's `:209-210` "do not print the recorded verdict" must be rewritten — but **`recorded, never acted on` at `:206` and the ordering `**Aggregation.**` < `## 3. Record the advisory gate call` < `## 4. Draft-then-confirm` must survive byte-for-byte**, or `test/ev66-advisory-intake.test.ts:346-357` goes red. `presented-never-written.md`, `three-wave-decomposition.md`, and the "step-3 presentation" comments in `gate-tool.ts:9-10` carry the same drift.

**Q5 (downstream).** Nothing that *acts* can read the rendered line — guard it by import direction, not intent: formatter at the record layer, presentation module a leaf, directionality canary. EV-69 reads the ledger by `stateHash`, EV-70 reads `RunManifest.mode`, EV-71 reads `generationId`/`usage`; all three are unaffected *provided* the presentation module exports no routing computation. One residual to watch: the line states a mode for the **drafted text**, which the very next affordance (human edit) invalidates — EV-69's re-check is the enforcement. That is not EV-67's bug, but it is EV-67's claim to make honest.

**Testable claims**

- **T1 (uniform rule).** `decisionLine` over a success fixture and a recorded-failure fixture both equal `"Mode: " + resolvedMode + " — " + basis`; the recorded-failure output byte-equals `Mode: Deliberate — gate call failed: <reason>` with no branch. Red if the failure path has its own format string.
- **T2 (join key, red for `stateHash`).** Two lines written with equal `stateHash` and different `callId`/`resolvedMode`; assert the renderer selects by `callId`. A `stateHash` join returns the older line → red.
- **T3 (unrecorded failure is representable).** Reuse `test/gate-tool.test.ts` test 7's fixture (forbidden-path endpoint): assert the tool yields `{callId: null, status:"failed"}` with no ledger line **and** that `renderCardLine` has a pinned, non-recorded output for that cell. Red at base: no such cell is named.
- **T4 (off, byte-identity).** With the packaged policy, presentation output for every card equals the pre-gate output byte-for-byte and the ledger file is absent.
- **T5 (canary, enumerated).** Amended canary asserts the runtime-importer set of `gate-ledger.ts` equals `{gate-run.ts}` plus declared roles, and that no execution-path module imports the presentation module. Red at base once the new module exists.
- **T6 (ordering fence).** `features-new.md` contains no `council_dispatch`/`council_wait` occurrence after the `## 3. Record the advisory gate call` heading — the property that makes presentation-time delivery safe.
- **T7 (prose preservation).** The amended procedure still contains `recorded, never acted on` and the three-way heading ordering; step 4's approve/edit/drop prompt string is golden-unchanged, and no `ask_user_question`/option-adding call is introduced into step 4 (that would be the added keystroke the acceptance forbids).

#### designer (job-18.3)

**Design position.** I argue the bare line `Mode: <mode> — <basis>` placed immediately below each card body and above that card's approve/edit/drop prompt is the smallest, most honest surface the feature can ship. The line reads as a fact about the card's shape — never an instruction — because its only verb-free shape names a category ("Mode"), separates it with a fixed dash, and then carries the recorded decide()-derived basis verbatim, which is itself a reasoning-trace string. The failed-call variant `Mode: Deliberate — gate call failed: <reason verbatim>` is the same shape with the failure category in the basis slot, which lands the named failure on the surface the human is already reading — the EV-66 PO ruling's Q(a) standing concern. The off-mode case ships nothing on the surface — the honest behavior is R3-as-zero — and the byte-identity test in the acceptance is the load-bearing evidence that no second decision point exists.

**Gulf closed.** Gulf of Evaluation, for the human at the existing step-3 approval gate, at the moment they are deciding what to do with a card. The gate records a verdict they cannot see; the line gives them the verdict as information attached to the card they are already reading, scoped per-card, so they can weigh it without crossing a second decision boundary. The fact is auditable against the ledger (basis is recorded verbatim), so a person who doubts it can verify.

**Principle and evidence.**

- *Signifier, not directive.* The line starts with a noun phrase "Mode:" (a category label of the card), separates with an em-dash, and ends with a decide()-derived string. No imperative verb, no question, no "Recommended:" prefix. The byte-shape itself precludes the instruction-reading — anything instruction-shaped ("Verify recommended — confirm?") would break EV-65's load-bearing byte-equality rule (gate-ledger.ts doc comment: `"Mode: " + resolvedMode + " — " + basis` byte-equals EV-67's pinned render). What I read in source: gate-run.ts's `failRun` writes `basis: 'gate call failed: ' + f.reason` verbatim; decide()'s basis strings are fixed-form composites like `reversible? no (one-way door)`, `composite 3.60 ≥ direct threshold 3.40`, `decidablyTestable: confidence 0.70 < choice floor 0.70`. The acceptance pins that basis is passed in (not authored at render) — the render function receives the basis string as input and produces no prose of its own.

- *Knowledge in the world.* The card id is on the line; the mode is on the line; the basis (with its question id, threshold, or one-way-door phrase) is on the line. A person can read it cold without having memorized EV-65's data shapes. The audit affordance (ledger carries the same bytes) is the trust signal: taste is replaced by traceability.

- *No second decision point.* The existing step-3 prompt (approve / edit / drop) is unchanged in shape and placement. The line attaches to the card body, sits between body and prompt, and rides the existing affordance. Editing the card overrules the verdict — that is the intended override affordance. The PO ruling on EV-66 Q(a) is the recorded human position: "after EV-67 the run ends in a named failure at the surface the person is already reading, not in silence." This card is the surface.

- *Constraint honored.* R3 (off is a no-op): the off-mode case renders nothing on the surface. No meta-signal ("Gate disabled", "Advisory mode off") — the honest behavior is no delta. The acceptance's "no additional prompt, keystroke, or state transition exists in the step" is the load-bearing constraint; I read it as binding.

**Falsifiable predictions.**

- **P1 — the bare shape reads as fact, not instruction.** Hypothesis: 5 cold readers shown the literal string `Mode: Deliberate — reversible? no (one-way door)` with no context, asked "is this asking you to do something?", all answer no. Falsifier: a CDP / pure-seam test that asserts the rendered line contains no imperative verb (`\b(confirm|approve|select|choose|verify|recommend)\b`) and no question mark.

- **P2 — the line attaches to its card, not to the set.** Hypothesis: with the epic + 4 child cards rendered in one pass, each line is visually scoped between its own body and its own prompt (5 lines, 5 separate boundaries). Falsifier: a pure-seam test asserting the render function emits the line exactly once per card and that the byte-identity golden file pins the per-card order; a CDP smoke would render two cards in a 24-row terminal and confirm no row of one card's line is adjacent to another card's body. I cannot run this smoke — `skeptic` or `owner` should.

- **P3 — the failed-call line reads as a named fact.** Hypothesis: shown `Mode: Deliberate — gate call failed: no OpenRouter API key resolved (OPENROUTER_API_KEY env or stored credential)`, a cold reader can state "the gate call for this card didn't succeed because no API key was configured" without prompting. Falsifier: the basis string is byte-equal to `gate-run.ts:172`'s pinned literal (assertion in the acceptance: "verbatim"); a render-function test asserts the verbatim reason is rendered as-is with no paraphrase. The PO ruling's "named failure at the surface" intent is the product-side assertion; this is the evidence.

- **P4 — the off-mode case ships no surface delta.** Hypothesis: with packaged-default policy.json (mode "off"), the step-3 approval gate renders byte-identical to its pre-EV-67 form. Falsifier: a golden test that runs the off-mode path through the render function and asserts zero additional lines beyond the pre-gate baseline; the test is the "exactly one line per card … a card whose gate call failed renders the fallback mode" inverted — the off-mode case falls below the line emission threshold entirely.

- **P5 — override affordance is editing the card.** Hypothesis: a person who reads the line and disagrees with the mode edits the card body, and the edit flows through the existing approve/edit/drop gate with no extra step. Falsifier: the acceptance's explicit constraint "no additional prompt, keystroke, or state transition exists in the step"; verified by a byte-identity golden test over the prompt region of the render output.

- **P6 — basis strings are byte-identical to the ledger.** Hypothesis: the line a person reads and the `record.basis` field on the gate-ledger.jsonl line are the same bytes, in the same order, with no trimming, paraphrasing, or HTML escape. Falsifier: a render-function test that receives a basis string with embedded unicode (e.g. the em-dash itself) and asserts the output contains the exact bytes the basis input contained; a ledger-reader test that asserts `appendGateCall`'s `basis` field is the `decision.basis` field passed in untransformed. The acceptance's "verbatim" word is the load-bearing claim.

- **P7 — the byte-identity test is the load-bearing evidence.** Hypothesis: a one-byte change to the line (e.g. prefix "Verdict: " instead of "Mode: ") breaks the acceptance test immediately. Falsifier: the test over the render function asserts the rendered output against a checked-in golden file (the card's acceptance: "byte-identical to the pre-gate rendering"). I cannot run this — `owner` owns the test; I am stating what it must catch.

**Residual cost I cannot close from this seat (named, not argued for).** The basis strings from `decide()` are reasoning traces (composites, thresholds, "one-way door" metaphor). A person who has never seen the question set may misread `reversible? no (one-way door)` as "the card is a one-way door" (it is not — the change is), and `composite 3.60 ≥ direct threshold 3.40` is opaque without the question vocabulary. We cannot paraphrase these strings — the byte-equality constraint forbids it. Closing the comprehension gap for first-time readers is a documentation / scaffold concern, not a line-format concern, and is the wiki's job (`metered-deliberation-routing` is still the Backlog intent page; it will need the question set explained). The honest move is to call this out, not to soften the line.

**Preferences, ranked last.** None of the seat's discretion survives the byte-identity constraint; every literal in the line shape is pinned. Two taste calls inside the pinned frame, ranked last: (1) em-dash separator (— U+2014) over a colon or pipe — matches the agent's existing prose habit in the same card text (`council/procedures/features-new.md` uses em dashes throughout) and reads as a soft separator, not a syntactic boundary. (2) No trailing period — periods convert reasoning traces into statements; the absence keeps the trace-shape. Both are taste and could be reversed without design consequence.
