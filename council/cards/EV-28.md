---
id: EV-28
title: Full usage tuple and cost provenance in the hub usage record
state: Deliberating
owner: null
epic: EPIC-7
goal: A seat dispatched through the hub whose session JSONL carries assistant messages with cacheRead, cacheWrite, reasoning, totalTokens and a per-component cost breakdown produces a run manifest and a council_wait report whose usage separately accumulates input, output, cacheRead, cacheWrite, reasoning and totalTokens plus the cost components cost.input, cost.output, cost.cacheRead and cost.cacheWrite, where the record carries a costBasis field valued catalogue-estimate and the council_wait line reports that token split instead of only turns and a dollar total.
---

## Intent

`Job.usage` currently keeps only input, output, a catalogue-estimated dollar
total and a turn count. The session JSONL the hub already parses carries
cacheRead, cacheWrite, cacheWrite1h, reasoning, totalTokens and a per-component
cost breakdown, but ingestion drops them, and the persisted `Usage` has no field
to receive them. Nothing downstream can be honest before the tuple exists. The
user-visible surface is the `council_wait` report head line — today
`cost=$X turns=N`, read by every facilitator and hub-enabled seat — and the
on-disk run manifest a person inspects under `/council-tree`. The head line must
report the token split (input, output, cacheRead, cacheWrite, reasoning, total)
and show the dollar value with its basis signified distinctly from a reported
charge; the manifest carries the same fields plus `costBasis` valued
`catalogue-estimate`. Zero usage renders as zeros; a dispatch that produced no
assistant message produces no head line.

## Acceptance

- A fixture assistant message carrying input 100, cacheRead 900, cacheWrite 0,
  output 10 and a per-component cost breakdown accumulates `cacheRead` 900
  (today it is dropped) and every token field separately.
- The persisted manifest and the `council_wait` report agree on the accumulated
  tuple and on `costBasis` = `catalogue-estimate`.
- The `council_wait` head line names the token split and the basis; it is not a
  scalar dollar total.
- `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` stay green.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-1 money signifier** — an estimated dollar figure renders
  `cost≈$0.1234 (catalogue)`; a provider-reported figure renders
  `cost=$0.1234 (reported)`. The `≈` is U+2248.
- **R-2 token split** — the `council_wait` head line carries the token
  split (input, output, cacheRead, cacheWrite, reasoning, total) alongside
  the labelled cost, replacing the bare `cost=$X turns=N` scalar.
- **R-3 manifest** — the run manifest carries the same fields plus
  `costBasis` valued `catalogue-estimate`.

## Run record

### Re-entry (facilitator, this run)

Prior container halted at step 2 with `Tool council_dispatch not found` — the
hub tool was not registered in that child. Environment repaired before this
dispatch (pi-council installed as a global local-path package; probe confirms
a `COUNCIL_SEAT=council-runner` child registers `council_dispatch` and the
runner allowlist). All seats re-verified resolvable at the branch-free main
(`owner, principal, designer, skeptic, consolidator, judge, product-owner,
steward, council-runner` all `OK` via `loadSeat`). Re-entered at step 1; the
classification below stands. No deliberation had occurred, so nothing is
reopened.

### Step 1 — classification (facilitator)

- **Path: full council.** Two independent triggers: (a) *cross-seam* — the
  usage record is consumed by the hub engine (`extensions/hub.ts`), the run
  substrate (`extensions/runs.ts`), the report formatter
  (`extensions/hub-tools.ts`), and the eval/leaderboard consumers
  (`extensions/eval-runner.ts`, `extensions/eval-rubric.ts`, `sumSubtree`),
  all of which currently treat `usage.cost` as a **number**; (b)
  *spec-ambiguity / design judgment* — the card's `goal` names nested cost
  components (`cost.input`, `cost.output`, `cost.cacheRead`,
  `cost.cacheWrite`) while `Usage.cost` today is a scalar total with ~25
  typed consumers and test literals, so more than one reasonable design
  satisfies the goal (reshape `cost` and migrate consumers, vs. add a
  parallel component structure and keep the scalar).
- **Surface-touching: yes.** The `council_wait` report head line is
  user-visible copy read by every facilitator and hub-enabled seat, and the
  run manifest is person-inspected under `/council-tree`.
- Seating: `designer` joins `owner` and `principal` as a third generator in
  steps 2–3 (surface-touching + full council).

### Deliberation record

#### HALTED at step 2 — no seat was dispatched

The step-2 dispatch was attempted and failed before any seat ran. Literal
harness result for a `council_dispatch` call (seat `owner`):

    Tool council_dispatch not found

This container's tool namespace is `read, bash, edit, write, grep, find, ls`.
`council_dispatch`, `council_wait` and `council_cancel` are all absent, so
council.md step 2 onward cannot execute: no generator, Skeptic, consolidator
or judge can be dispatched. `loadSeat()` resolves every seat the card needs
(`owner`, `principal`, `designer`, `skeptic`, `consolidator`, `judge`) from
disk, so this is not an `Unknown seat` failure — the hub tool itself is not
registered in the council-runner child process.

The card is returned to `Ready` because no deliberation occurred; leaving it
`Deliberating` would misstate a step that never ran and would block a fresh
runner at step 1's `state` gate.

