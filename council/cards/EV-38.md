---
id: EV-38
title: Retry policy section in .council.json with shipped defaults
state: In Progress
owner: null
epic: EPIC-9
goal: loadRetryConfig returns the documented defaults when .council.json has no retry section, returns the file's values when it has one, and throws an error naming the file on any invalid value.
---

## Intent

Follow `loadThemeConfig`'s validate-loudly shape — a falsy `enabled` is the off
switch, and a malformed value throws naming the file and the offending key.
The policy is a new top-level `retry` sibling of `council` and `theme`.
`council-config-writer.ts` preserves unknown top-level keys by construction,
so a `council.<seat>` write must leave the `retry` bytes untouched.

Defaults visibility is settled as concrete JSON in
`council/scaffold/.council.json` rather than a runtime announcement — visible
in place, no new code path, consistent with the `loadThemeConfig` precedent.

## Orchestrator rulings (Phase 1, binding)

Recorded human decisions for this run. Immutable and binding on every seat,
`steward` included; a runner that hits one applies it and cites the ruling
rather than re-asking.

- **R2 (shipped defaults).** The `retry` block, surfaced as concrete JSON in
  `council/scaffold/.council.json`:

```json
{
  "retry": {
    "enabled": true,
    "maxAttempts": 3,
    "baseDelayMs": 2000,
    "maxDelayMs": 30000,
    "jitter": true
  }
}
```

- **R3 (validation set).** An invalid value throws naming the file and the
  offending key. `maxAttempts` is an integer ≥ 1, `baseDelayMs` an integer
  ≥ 100, `maxDelayMs` an integer ≥ `baseDelayMs`, and `enabled` / `jitter`
  are booleans. Any other type or range is invalid.

## Run record (features-deliver / EV-38)

### Step 1 — classification (facilitator)

- **Path: mechanical.** The card is narrowly scoped to a single config-loader
  seam and its falsifiable clauses are fully stated before any code exists:
  the deliverable is one loader function (`loadRetryConfig`, named by the
  `goal`) in `extensions/seats.ts` — the same file that already carries its
  two siblings — plus the seed bytes in `council/scaffold/.council.json` and
  their tests. The `goal` states the whole truth table (defaults when the
  section is absent; the file's values when it is present; a throw naming the
  file on any invalid value), R2 fixes the defaults table literally, and R3
  fixes the invalid-value set literally (type + range for all five keys).
  Nothing in the card is cross-seam: it does not touch `extensions/retry.ts`
  (EV-37), `hub.ts`, dispatch, the run substrate, or any rendered surface. Per
  council.md step 1, mechanical cards skip steps 2–6 and go directly to
  step 7.
- **Surface-touching: yes (recorded).** The deliverable includes error text —
  the throw names the file and the offending key, and council.md's definition
  counts error text as copy. This does not change the path: a surface-touching
  **mechanical** card seats no `designer` (council.md step 1), so no design
  concern can reopen the card; any such finding is a step-13 follow-up, not a
  blocker. No UI is added or changed.
- **Rulings applied (Phase 1, binding): R2 (shipped defaults), R3 (validation
  set).** R2 is cited as the literal default block to surface in
  `council/scaffold/.council.json`; R3 is cited as the literal validation set
  (integer ≥ 1, integer ≥ 100, integer ≥ `baseDelayMs`, two booleans). Both are
  squarely this card's subject, applied and cited — not re-asked, not reweighed.
- **Grounded environment facts (recorded; the owner re-verifies them):**
  `extensions/seats.ts:175-196` (`loadThemeConfig`) is the named precedent —
  it is the validate-loudly shape (malformed JSON / non-object root /
  non-object section all throw naming the file; `parseThemeSection` merges over
  a base, so "presence implies enabled" and a partial section still returns a
  complete object); `council/scaffold/.council.json` today carries `council` +
  `theme` and **no** `retry` key;
  `extensions/council-config-writer.ts` is a byte-region splice and preserves
  unknown top-level keys by construction (its own docstring), so it needs no
  change for a top-level `retry` sibling and a `council.<seat>` write leaves
  the `retry` bytes untouched. `loadCouncilConfig` iterates only
  `parsed.council`, and `loadThemeConfig` reads only `parsed.theme`, so a new
  top-level `retry` is inert to both existing readers.
- **Goal-text fact recorded for step 9/10, not adjudicated here:** the `goal`'s
  clause "returns the file's values when it has one" requires a policy object
  for a present `retry` section (an absent section returns the R2 defaults);
  whether that means merging a partial section over R2's defaults, and what a
  present `enabled: false` returns, are readings the owner states in its report
  and the Skeptic attacks at step 9 against the `goal` text. The facilitator
  settles neither.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `skeptic`, and `judge`; all three are present in the packaged seat
  set (`council/agents/*.md`) and this checkout has no `.pi/agents/` repo-local
  override directory, so nothing shadows them. No `designer` dispatch
  (mechanical path). No `Unknown seat`.
- **Environment:** step 0 preflight is skipped per the autonomous-run
  substitution (Phase 0 cleared the environment for the epic). Main checkout
  clean at `02f8f6a` == `origin/main`; `gh` authenticated as `tistaharahap`
  (`repo` scope). EV-37 is merged (`6a375c4`) with its card `Done`; EV-38 has
  no code dependency on it (the policy surface is independent of the
  classifier). No `Needs Human` state and no outstanding ruling on this card
  beyond R2/R3 — deterministic merge check criterion 5 holds at the start of
  the card.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md` does
  not exist here):** `bash council/preflight.sh EV-38`, `bunx tsc --noEmit`,
  `bun test`, `python3 council/validate.py` (AGENTS.md §Commands +
  `.github/workflows/gates.yml`). This repo defines no database/import/server
  gate; `COUNCIL_INTEGRATION=1` stays gated and is not run.

### Step 7 — handoff to one owner

Mechanical path: no design spec is written (council.md step 7 — the card's
`Intent` and `goal` are the handoff). One `owner` dispatch against the card; it
implements in a dedicated `git worktree` (never `main`) and clears every gate
in full. The facilitator remains the single writer of `council/board.md` and
this card file for the duration of the card.
