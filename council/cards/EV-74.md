---
id: EV-74
title: `/council-gate` to enable or disable the decisions gate
state: Deliberating
owner: null
epic: EPIC-14
goal: A /council-gate TS command takes a mode (off|advisory|active) and toggles the decisions gate by writing only the mode key inside .council.json's top-level gate section, its no-arg form prints the mode resolved through loadGateConfig, it echoes the resolved write through that same resolver, it atomically merges so every other byte and top-level key of .council.json is preserved, it leaves the file byte-identical when the requested mode already holds, and the echo names the resolved mode and states that it applies to dispatches after the echo.
---

## Run record (features-deliver, EPIC-14 card 3/5)

- Step 1 — recorded execution mode: **Deliberate** (ROOT manifest job-5, run
  `2026-09-20T08-59-16-719Z-259999-vjf6un`, mode recorded on the manifest per
  EV-68; with the gate `off` no ledger routing record exists, so the recorded
  manifest mode is the route read; full-vs-mechanical judgment not made).
  Surface-touching bit: **true** — the deliverable is the `/council-gate`
  echo, user-visible operator copy in TUI and headless; `designer` seated as
  third generator (binding card context).
- Preflight: skipped per the autonomous substitutions — Phase 0 already
  cleared the environment for the epic.
- Phase 1 rulings applied and binding (EPIC-14 card face): R1 (orchestrator
  merges; card left `In Review` with PR open), R2 (record commits ride this
  branch), R3 (card 3/5, order EV-73 → EV-75 → EV-74 → EV-76 → EV-77), R5
  (operator copy says "gate", never "jev"; the echo names the resolved mode
  and states it applies to dispatches after the echo).
- Binding card-context rulings (EPIC-14 wave-3 PO ruling job-33, amended
  this card): `/council-gate` is a TS `registerCommand` command taking mode
  off|advisory|active; no-arg form prints the mode resolved through
  loadGateConfig (EV-73's single resolver); the write touches ONLY the mode
  key inside `.council.json`'s reserved top-level `gate` section; the echo
  resolves through that same resolver (echo-then-run, [[gate-parity]]);
  preservation generalised to EVERY other byte and top-level key (a future
  unknown key survives byte-for-byte); a redundant set leaves the file
  byte-identical; unknown arg fails loud; atomic EV-24 writer discipline —
  share `extensions/council-config-writer.ts`'s byte-region splice and
  preservation logic, never hand-roll a second merge; today's `.council.json`
  carries `council` (9 seat overrides) and `theme`, no `gate` section
  (resolves `off`); the no-arg form must print `off` for that state.

## Intent

The operator enables/disables jev-for-decisions with one command and sees what
changed; the slash command's echo is the copy surface (TUI and headless), and it
must share the loader so the printed value cannot drift from what dispatch
resolves. Operator-facing copy says "gate"; "jev" appears in no operator-facing
string.

## Acceptance

- on→off→on round-trip; echo == loader resolution (echo-then-run).
- Every other top-level key byte-preserved, including unknown future keys.
- No-arg form prints the resolved mode.
- Unknown arg fails loud; a redundant set leaves the file byte-identical.
- Atomic EV-24 writer discipline.
## Round 1 — independent first pass (owner job-5.1, principal job-5.2, designer job-5.4)

### Owner (job-5.1)

Writer reuse: add `writeGateMode(args: { repoRoot; mode: GateMode }): { ok: true; changed: boolean } | { ok: false; error: string }` inside `extensions/council-config-writer.ts`, reusing the module's private scanner machinery (`parseValue`, `skipSpace`, `scanString`, `lineIndentAt`, `detectIndentUnit`, `writeAtomic`, `existingMode`) as-is. Nothing existing changes signature — `writeSeatOverride`/`clearSeatOverride` untouched, their byte-pinned tests risk-free. Rejects exporting primitives to a second module (invites a future third writer to hand-roll). Gate splice simpler than seat splice in every branch: value is a scalar string, no emitSeatObject/field-merge/existingThinking; replace = one-span swap of `gate.mode`'s value; insert = (a) `gate` exists w/o mode → insert member after last (or re-emit `{}` span), (b) `gate` absent → insert `"gate": { "mode": ... }` after last root member (NOT before-theme — no established scaffold position; accepts a push toward before-theme), (c) file absent → greenfield 2-space + newline. Validation mirrors loader grammar exactly: mode ∈ GATE_MODES imported from gate.ts (no import cycle). Same failure asymmetry (malformed → ok:false write-nothing; only fs failures throw), same writeAtomic+existingMode.

Command: new `extensions/council-gate-cmd.ts` pure logic (council-models pattern: `runGateCommand(args, repoRoot, emit, write = writeGateMode)` with injectable write), thin `pi.registerCommand("council-gate", ...)` in index.ts. NO TUI/headless behavioral fork — no modal for a three-value toggle; both share runGateCommand, differing only in emit sink. Arg parsing: trim/split; zero tokens → print resolved; one → validate ∈ GATE_MODES; >1 → fail loud. Copy: no-arg `council-gate: gate mode is <mode>` (mode from loadGateConfig); set: `council-gate: gate mode set to <mode> in .council.json — applies to dispatches after this message.`; redundant: `council-gate: gate mode already <mode> — no change.`; error: `[council-gate] error: unknown mode "xyz" — expected one of off, advisory, active` + usage line.

Echo semantics: (1) pre-write resolve via loadGateConfig (no-op check + malformed fails loud BEFORE write); (2) write or skip; (3) post-write resolve via loadGateConfig — THAT value is echoed; requested token and echoed value diverge only on a concurrent write between 1 and 3. "Applies to dispatches after the echo" mechanically free: loadGateConfig lazy-at-call, no cache to invalidate; echo line is the ordering marker; pinned by a test mutating the file between two resolutions. Redundant rule: `loadGateConfig(repoRoot).mode === requested` — keyed on the one resolver, cannot drift from the no-arg print; on today's gate-less repo `/council-gate off` is a redundant set and does NOT persist an explicit gate section (strongest byte-preservation reading, the only reading that shares the loader). Unknown-arg: pure validation ordered first; loadGateConfig's own FAIL lines propagate via handler catch as `[council-gate] error: <FAIL line>`, single line, no stack.

Test plan (TDD, mkdtempSync): test/ev74-council-gate-command.test.ts (+ writer cases colocated; council-config-writer.test.ts stays EV-24-scoped). Writer: greenfield; gate absent among council+theme+unknown → section inserted, everything else byte-identical; gate w/o mode → member inserted; mode span replaced; redundant → changed:false byte-identical mtime unchanged; malformed/non-object root/non-object gate → ok:false untouched; perms preserved; no .tmp leftover. Command: no-arg prints off on gate-less fixture; round-trip legs; echo==loadGateConfig after each; unknown/extra args write nothing; lazy-resolver proof; registration asserted (ev72 pattern).

Open questions: (1) no-op rule reading — resolver-equality vs persist-explicit-mode (one-branch change if council disagrees; argues equality contradicts "shares the loader" less). (2) Insert position: append-after-last vs before-theme (cosmetic). (3) No TUI modal — three fixed values; pure split makes a picker cheap later but scope creep. (4) GATE_MODES import cycle-free — verifiable via tsc+test.

Testable claims: (1) gate-less `.council.json` + `/council-gate off` → byte-identical, mtime unchanged. (2) set preserves council (9 overrides), theme, injected unknown key byte-for-byte incl. tab-indented. (3) echoed mode == next loadGateConfig; gate read after echo observes new mode w/o restart. (4) unknown/extra args write nothing; malformed JSON fails loud with EV-73 FAIL line, writes nothing. All four TDD reds pre-mechanism, green at head.

### Principal (job-5.2)

Cross-seam reading: `.council.json` = one file, four independent readers (loadCouncilConfig reads only parsed.council; loadThemeConfig; loadRetryConfig; loadGateConfig) and now a SECOND writer of a different top-level key (writer module exports exactly writeSeatOverride + clearSeatOverride, both targeting council.<seat>). Routing seam: loadGateConfig lazy-at-call (gate.ts:86-88); callers loadGatePolicy ← gate-tool intake, gate-route.ts:194 resolveRoute, gate-route-tool recheck. `mode === "off"` ⇒ resolveRoute returns Deliberate — off is NOT "ungated"; off routes everything to the full panel; enabling the gate REDUCES seat count. Merge check never sees config: evaluateMergeCheck takes mode from readCardMode reading the ROOT RunManifest.mode written at dispatch time — a flip does NOT retroactively change an in-flight card's recorded mode or its merge-check input; it affects routing reads and intake calls after the echo. Sharpest true statement: "applies to gate calls and routing reads after this echo, which govern later dispatches." Theme-watcher seam: watchCouncilConfig watches the dir filtered by basename, debounces 250ms, on any change calls loadThemeConfig+activateTheme — every /council-gate write triggers a theme reload/repaint (pre-existing via council-models writer; benign; real side effect).

Blind spots: writer vantage can't see that its tests pin EMITTED BYTES (emitSeatObject, detectIndentUnit fallbacks) — a generic-member-writer refactor is the highest-regression-risk path. Command/loader vantage can't see the TOCTOU: no-op decision must be made on the writer's own fresh parse, not a pre-read. Copy vantage can't see that off = full panel ("disable = less scrutiny" is wrong). Merge-check vantage can't see that intake and routing DO read live config.

Writer-sharing options, descending: (A, recommended) EXPORT the shared primitives (scanner + lineIndentAt/detectIndentUnit + writeAtomic + existingMode) from council-config-writer.ts and build a gate-targeted writeGateMode beside gate.ts — shares the splice literally, leaves writeSeatOverride/clearSeatOverride bodies byte-for-byte untouched. (C) same-file writeGateMode importing GATE_MODES from gate.ts — no export widening, but couples the seat writer to gate vocabulary (no cycle). (B, avoid) refactor writeSeatOverride onto a generic member-writer — exactly what "without destabilizing the EV-24 writer's tested contract" warns against. A/C must decide where a newly inserted gate section lands: loader is order-blind; append-after-last is low-surprise; canonical scaffold order is a follow-up.

Echo: mirror council-models' post-write read-back — write, then loadGateConfig, echo THAT resolution; echo == loader resolution by construction, free in the redundant branch too. Non-assertive rule: distinct form for redundant set (resolved mode + "unchanged"); may name mode + timing, may not claim a write when the file is byte-identical. No-op decided INSIDE the writer on its own read/parse (return e.g. {ok:true, unchanged:true}), not in the command after a separate loadGateConfig; clearSeatOverride models the idempotent no-op (mtime unchanged).

Blast radius/ordering: malformed → writer refuses ok:false; bare-string gate → loader FAILs on resolve-first, command never reaches writer (resolve-first cleaner; if write-first the writer must refuse unknown gate keys); file absent → greenfield load-valid (loadCouncilConfig → {}, theme watcher doesn't arm); concurrency — writeAtomic tmp is last-rename-wins across two sessions (pre-existing single-writer hazard, follow-up); lazy-at-call: off→active mid-run makes the NEXT intake call live — intended run-config-stability class.

In card: command; mode write via shared splice; no-op byte-identity; fail-loud unknown arg; post-write echo through loadGateConfig; R5 copy. Follow-up: (a) seed `gate: {"mode":"off"}` in scaffold + canonical top-level order; (b) run-config-stability Phase-0 assertion now that gate mode is a second mid-run-flippable input; (c) single-writer/versioned-write discipline for concurrent sessions; (d) theme-watcher reloading on gate-only writes (pre-existing, out of scope).

Testable claims: (1) sha256 of everything outside the gate.mode span unchanged per mode incl. theme block + every council.<seat> block. (2) echo string contains exactly the post-command loadGateConfig mode; loadGatePolicy(repoRoot).mode equals it (routing-parity, not just no-arg parity). (3) redundant set: bytes + mtime unchanged. (4) no-arg on today's repo prints off, writes nothing. (5) bogus arg fail-loud no write. (6) malformed file and bare-string gate → command errors, no write, FAIL surfaced not swallowed. (7) greenfield + active: loads, loadCouncilConfig → {}, loadThemeConfig → undefined. (8) after a flip, effectiveModeForCard for a run recorded before the flip unchanged; after the flip follows the new mode. (9) test/council-config-writer.test.ts passes with zero edits.

Open questions: A vs C; does the redundant echo carry an explicit "unchanged" token; should /council-init seed gate off (absent default may suffice); does any operator string risk implying "off = less rigor" (echo names only the mode — honest; flag for designer/PO).

### Designer (job-5.4)

Position: three copy surfaces — post-write echo, no-arg status read, unknown-arg failure — are easier to make honest than to make clever; pushing clever into them is the largest standing hazard. Echo after successful write: one line, command-family prefix, names .council.json (so hand-edit and command agree on location — echo lines double as documentation of the write), ends with R5's timing claim: `[council-gate] gate mode is now active in .council.json — applies to dispatches after this echo.` The "in .council.json" clause does real work (operator verifying by editor needs the file name). The trailing timing clause is load-bearing: resolver is lazy-at-call; "dispatches after this echo" is the strongest claim the surface can back; do NOT transplant /council-models' "takes effect at the next dispatch" verbatim — a gate change applies on the next loadGateConfig call, different in kind from a seat-model change.

Redundant-set echo (the harder one): non-assertive echo rule bites — cannot say "wrote" when no write occurred; "when an echo quotes post-merge effective state, it must quote the effective value, never a blank." Literal names the no-op as a no-op, names the mode, stops: `[council-gate] gate mode is already active in .council.json — no change.` Test for copy honesty: glance-diff of change vs no-op echo reveals the difference is precisely "did we write," nothing else. Sister precedent: /council-models' "— thinking unchanged".

No-arg form: a status read, not an assertion. One line: `[council-gate] gate mode is off.` No usage block, no gloss footnote — those belong to EV-77's docs sweep. Same grammatical skeleton as the echoes (three forms, one skeleton) — makes the no-arg form feel like a read, not a write.

Unknown-arg failure: fail loud, single line, name the allowed set, offending arg in quotes: `[council-gate] error: unknown mode "actiev" — expected one of off, advisory, active`. Typing cases: actiev, Active, "off " (trailing ws — trim then reject), --off, 0 — all land on the same error line.

Conceptual-model hazards: (H1) advisory vs active are NOT symmetric (advisory makes a network call and writes a ledger line, never blocks; active may block) — but the explainer belongs to EV-77's docs surface, NOT inline parentheticals on every echo (repeats fifty times a day, smuggles glossary copy past the EV-77 surface). (H2) naming .council.json on every form — right and load-bearing. (H3) the timing claim — any variant claiming "now"/"this session"/"all future commands" is too strong; any variant omitting timing leaves the operator wondering.

Consistency: `[council-gate]` bracket tag for errors (family pattern); success line bare-name `council-gate:` mirroring `council-models: wrote ...` (defensible opposite; defer to owner); headless emit(line) per item, no blocks (single tri-state value; /council-models' block is multi-line because seven seats); require the analogous pure helper so TUI and headless share copy — echo-then-run not enforceable if one surface has bespoke copy.

Falsifiable predictions P1–P11: P1 loadGateConfig the only resolver on the echo path (stub falsifier). P2 redundant set byte-identical (hash compare with realistic indentation + unknown keys). P3 no-op echo never asserts a write (`not.toMatch(/\bwrote\b/)`; matches already/no change). P4 every other top-level key byte-preserved through one change (theme, retry, council.<seat>, unknown "foo":"bar"). P5 unknown arg: single line, quoted offender, allowed set enumerated. P6 no-arg goes through loadGateConfig (stub test — same resolver as P1). P7 malformed gate section (bare string) surfaces the verbatim FAIL line through the same channel, file byte-identical (not written). P8 "jev" appears in NO operator-facing string of the new module (grep/emit-path falsifier; typesafe/jev-1.13 stays code-internal). P9 every form names .council.json. P10 success echo contains a timing claim (`/after this echo/|/next dispatch/|/applies to dispatches/`) — pins presence, not literal. P11 absent file/absent section/gate:{}/empty → identical status echo. Sharp critique for the skeptic: the post-write echo does two things at once (what changed + when it applies); press whether a fast reader sees the timing clause as the gating constraint or a footnote (predicts trailing is right; leading verb is the operator's primary question).

Ranked preferences: (1) echo names .council.json on every form; (2) success and no-op echoes share a grammatical skeleton differing only on whether a write happened; (3) timing clause trails as "— applies to dispatches after this echo"; (4) `[council-gate]` prefix matches the family; (5) bare-name success line mirroring council-models (defer to owner); (6) advisory/active gloss lives in EV-77, not the echo; (7) no-arg one line, not a block. Owner-discretion flags: changed-vs-no-op visual mark (taste); usage hint on status read (redundant, second line); first-time-advisory warning (no signal to detect first time — skip).

### Round-1 state (facilitator)

Converged without exchange: TS command on the council-models pattern (pure helper + injectable write, emit-sink-only TUI/headless difference); post-write echo through loadGateConfig (echo == loader resolution by construction); no-op byte-identity with mtime unchanged; fail-loud unknown arg; R5 copy; timing clause trailing; .council.json named on every form; no advisory/active gloss on the command surface (EV-77's). STILL OPEN (for round 2): (a) writer-sharing shape — owner: writeGateMode same-file in council-config-writer.ts (C); principal: export primitives, writeGateMode beside gate.ts (A); both reject the generic refactor (B). (b) no-op decision site — owner: loadGateConfig-equality (keyed on the one resolver); principal: writer's own fresh parse (TOCTOU framing). (c) gate-section insert position — owner: append-after-last; principal: append-after-last low-surprise, canonical order a follow-up. (d) redundant-echo wording — designer offers skeleton, owner's literal differs slightly (already <mode> — no change vs already set). (e) principal's follow-up list (scaffold seed, run-config-stability Phase-0 assertion, concurrent-session write discipline, theme-watcher) — for step 13 triage.
