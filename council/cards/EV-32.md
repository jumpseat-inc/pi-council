---
id: EV-32
title: Usage reported at every autonomous entry point
state: In Progress
owner: null
epic: EPIC-7
goal: After /features-new, /features-deliver, /council, /council-eval and a council-runner's card execution each conclude, the concluding output contains a usage block that prints the invocation's token split, its provenance-labelled dollar value and the accounting boundary it covers, and an automated test asserts for each of the five points that the block is present and its numbers equal the fixture accounting record.
---

## Intent

The human asked that the spend be reported explicitly at every autonomous exit,
and said over-reporting is good and explicit beats implicit. This card reports a
usage block at five autonomous exit points — `/features-new`, `/features-deliver`,
`/council`, `/council-eval`, and a council-runner's card execution (its
return-contract report, surfaced through features-deliver's Phase 3 ledger) —
replacing today's scalar `cost=$X` with the invocation's token split, a
provenance-labelled dollar value, and the accounting boundary the block covers.
The block is produced by a deterministic handler, not by the procedure's own
prose, so the same input yields the same block and a test can assert string
equality. The block is rendered after the accounting function returns, so it
cannot include the cost of its own generation; the boundary label names the
included range instead of implying completeness. Zero usage and accounting
failure each render an explicit state, never a blank. `board-create-card` is out
of the set — it is a single human-driven write, not an autonomous run.

## Acceptance

- For each of the five points, a fixture accounting record renders a usage block
  whose numbers equal the record.
- The block is byte-stable across repeated renders of the same input.
- The block names its accounting boundary.
- Zero-usage and accounting-failure inputs render explicit, non-blank states.
- `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` stay green.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-1 money signifier** — estimated `cost≈$0.1234 (catalogue)`, reported
  `cost=$0.1234 (reported)`.
- **R-2 unavailable marker** — `n/a` in-slot plus the legend
  `n/a = provider figure unavailable`.
- **R-4 boundary label** —
  `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>`.
- **R-5 empty and failure states** — zero usage renders
  `usage  no usage recorded`; accounting failure renders
  `usage  accounting failed — <reason>`.
- **R-6 block** — the usage block is a single deterministic block, identical
  in shape across all five exit points.

## Run record

### Step 1 — classification (facilitator)

- **Path: full council.** Cross-seam and spec-ambiguous. The card's exit set
  spans five surfaces in two kinds of artifact: four markdown procedures
  (`features-new`, `features-deliver`, `council`, `council-eval` — the last a
  TS command per `procedures-vs-commands`) plus a council-runner's own
  return-contract report flowing through `features-deliver`'s Phase 3 ledger;
  the deterministic handler that renders the block is engine code
  (`extensions/`), while its invocation points are procedure/command code and
  runner prose. More than one reasonable design reaches each open shape: the
  token-split rendering (labels, order, separators), the provenance line's
  composition around R-4's boundary label and R-1/R-2's money/marker clauses,
  where the legend sits, what the block's exact line count and whitespace are,
  how each of the five points reaches the accounting function that returns
  before the block is rendered, and how a runner's report carries the same
  block. R-1/R-2/R-4/R-5/R-6 bind the literals they name but not those shapes.
  A real design tradeoff also exists between a single shared renderer called
  from five sites and per-site rendering.
- **Surface-touching: yes.** The card's subject is user-visible output: the
  usage block itself ("prints the invocation's token split, its
  provenance-labelled dollar value and the accounting boundary"), the ruled
  `n/a = provider figure unavailable` legend, and two rendered states —
  `usage  no usage recorded` and `usage  accounting failed — <reason>`
  (R-5). That is visible copy plus an empty state plus an error state by
  council.md step 1's definition.
- **Seating:** `owner`, `principal` and `designer` as the three generators in
  steps 2–3 (surface-touching full-council card).


### Step 2 — independent first pass (round 1, generators only)

Each seat received the card alone — no other position, no hint. Positions
appended verbatim, labeled by seat. Jobs `job-16.1` (owner), `job-16.2`
(principal), `job-16.3` (designer).

#### Position — `owner`

Grounding complete. Summary of what I verified before taking this position:

- **Wiki gap confirmed**: `vault/wiki/index.md` has no usage/accounting page — EV-28/30/31 live only in `council/cards/`, `docs/superpowers/specs/2026-09-10-EV-28-design.md`, `2026-09-11-EV-30-design.md`, `2026-09-11-EV-31-design.md`, and the code. My grounding below cites those specs and files, not a wiki page. Filing a usage wiki page at `/council` step 14 is the natural close for this card.
- `formatBoundaryLabel` (spend.ts) already renders R-4 byte-exact, including the ruled unresolved form `entries=unresolved jobs=0` (spend.test.ts:288).
- `formatUsageSegment` (hub-tools.ts:51) already renders R-1's money literals byte-exact (`cost≈$0.1234 (catalogue)` with U+2248, `cost=$0.1234 (reported)`).
- EV-31's flush (`flushPendingInvocations`) already owns a notify whose wording EV-32 owns (EV-31 spec §2.7, ruling item 2) — that notify is the natural block emission point.
- `/council-eval` (index.ts:437) is a TS command that **never stamps a marker** today; the three scanned procedures do (index.ts:275). EV-31's pending ledger is populated only in the scanned-procedure handler.
- Seats never stamp (index.ts returns into `runChildMode` before registration), so a council-runner's report can never carry an engine-rendered block itself — the runner's block must be rendered by parent-side engine code at the moment the card concludes.

---

## Approach

One pure renderer, two emission sites, five covered exits. **`formatUsageBlock(spend: SpendRecord): string`** lives in `extensions/spend.ts` (pure, next to `formatBoundaryLabel`, which it reuses verbatim). It is the only place the block's bytes exist. Emission site A is EV-31's gated flush: on the transition to `written`, the success notify **becomes** the block, rendered from the persisted `StoredUsageRecord.spend` — this covers `/features-new`, `/features-deliver`, `/council`, and `/council-eval` (the latter after we add the marker stamp it currently lacks). Emission site B is `council_wait`'s report (hub-tools): for a settled job with a manifest, the wait handler builds a manifest-derived SpendRecord over the job's subtree (`sumSubtree`) and appends the block — this is the fifth exit: the block lands in the orchestrator's context at the exact moment each runner's card concludes, and features-deliver's Phase 3 gains one sentence mandating it be carried into the ledger verbatim. The EV-28 head-line segment stays untouched — it is a settled, byte-tested surface, and it answers a different question (this job's instantaneous report) than the block (invocation-scoped accounting).

The timing property the card demands holds structurally: the flush renders only **after** `spendRecord()` returns and `persistInvocationUsage` has written, so the block can never contain the cost of its own generation, and the numbers shown are the numbers on disk. The `forestFullySettled` gate means the block for `/features-deliver` lands exactly when the last runner settles — i.e., at the Phase 3 moment — which is the correct "concluding output" for a hours-long epic, not a mid-run interruption.

## The block, byte-exact

Normal state (both halves measured), four-space-indented-free, `usage` + **two spaces** prefix matching R-5's literals:

```
usage  boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>
usage  ownSession [session-reconciled]: turns=<t> tokens=in <in>/out <out>/cR <cR>/cW <cW>/reason <r>/total <tot> cost≈$<cost> (catalogue)
usage  subtree [stream-assistant, lower bound]: turns=<t> tokens=in <in>/out <out>/cR <cR>/cW <cW>/reason <r>/total <tot> cost≈$<cost> (catalogue)
```

- Line 1 is `formatBoundaryLabel(spend)` output verbatim — R-4 already ruled it; the renderer composes, never re-renders.
- The `tokens=…` and money fragments are **extracted** from `formatUsageSegment` into shared helpers (`formatTokensFragment`, `formatMoney`) in spend.ts; hub-tools imports them, and a byte-identity test pins `formatUsageSegment`'s output through the refactor. `≈` is U+2248; `toFixed(4)`; reported basis renders `cost=$<cost> (reported)` — R-1 verbatim.
- **Provenance labels**: the bracketed `usageSource` values are the record's own machine vocabulary (store README teaches exactly these two names); `lower bound` is carried on the subtree line because the README already commits to surfacing that asymmetry prominently and the block is the most-read surface the record ever gets.
- **R-2**: when a measured half has activity (`turns > 0`) but `cost === 0`, its money slot renders `cost=n/a` and one final legend line is appended — `usage  n/a = provider figure unavailable` — iff the block contains any `n/a` (byte-stable either way). No basis signifier on an n/a slot.
- **R-5 zero state**: when both halves are all-zero (which covers the ruled unresolved-boundary case — EV-30 zeroed both halves, never an error), the entire block is the single line `usage  no usage recorded`. Boundary omitted; the durable record still carries `boundary.resolved: false` for forensics.
- **R-5 failure state**: `usage  accounting failed — <reason>` (U+2014 em dash). `<reason>` for a flush write failure is composed as `write failed for <absolute target path>: <error>` — this deliberately preserves EV-31's T-U14 property (the absolute store path appears in the notify) while adopting the ruled wording.

Runner-block variant (fifth exit), same grammar, one ruled difference:

```
usage  boundary=session=<runnerSessionId> entries=unresolved jobs=3
usage  subtree [stream-assistant, lower bound]: turns=<t> tokens=… cost≈$… (catalogue)
```

The parent structurally cannot session-reconcile a runner's own session (it lives inside the pruned run dir; the runner never stamps; per-node reconciliation is already filed as FLLWUP-31). So the `ownSession` half is **omitted, never zero-fabricated** — a zero line labelled `[session-reconciled]` would assert a measurement the parent never performed, which is exactly the kind of plausible-but-false number this card exists to kill. The boundary line uses R-4's already-ruled unresolved form with the true subtree job count. I take "identical in shape" (R-6) to mean one grammar — same prefix, same labels, same ordering, same ruled states — not a fixed line count; the alternative (a zeros ownSession line in the runner block) is the version I'd reject.

## Rendering at each of the five exits

1–3. **`/features-new`, `/features-deliver`, `/council`** — no procedure prose changes at all. The scanned-procedure handler already stamps the marker and pushes to `pendingInvocations` (index.ts:275–295). The first flush trigger at which the gate holds (normally `forest-settle` when the last dispatched seat settles) writes the record and emits the block via `ctx.ui.notify` (TUI) / `console.log` (headless). Zero-usage runs (e.g. `/council` stopping at step 1 on a non-Ready card) still flush a zero record → explicit `usage  no usage recorded`. **New failure emission**: when `recordInvocationBoundary` returns null (fail-closed), the handler synchronously emits `usage  accounting failed — invocation boundary unresolvable` — closing today's silent-blank path, which would otherwise violate "never a blank."
4. **`/council-eval`** — add `recordInvocationBoundary(pi, ctx, "council-eval", …)` + the pending-ledger push at the top of the run path (after arg validation, before `runMatrix`). Everything else is inherited: the last matrix job's settle trips the gate, the block emits before `summaryLines`. Ordering accepted: block, then eval summary — both in the concluding output.
5. **Runner card execution** — `council_wait`'s handler, after collecting a settled job: read `readManifests(repoRoot, runId)`, build the runner-shaped SpendRecord (boundary as above; subtree = `sumSubtree` over the job id, all metrics), append `"\n" + formatUsageBlock(...)` to the report. A job with no manifest usage (pre-EV-16 manifests, or nothing ran) renders `usage  no usage recorded` — never blank. `council/procedures/features-deliver.md` Phase 3 gains exactly one sentence: carry each card's runner usage block verbatim from the council_wait report into the ledger. The block remains engine-rendered; the orchestrator only transports it.

Double-emission is impossible by construction: the flush emits only on the `written` transition (`existing` is silent, honoring choose-once), and the runner site only renders at wait-collection time. The three flush triggers cannot produce two blocks for one invocation.

## Where the handler lives and how it's invoked

- `extensions/spend.ts`: `formatUsageBlock(spend: SpendRecord): string`, plus extracted `formatTokensFragment(u)` / `formatMoney(u)` reused by `formatUsageSegment`. Pure; no new resource mechanism (convention caution).
- `extensions/usage-store.ts`: flush success/failure notify wording → block forms (EV-31 ruled this wording is EV-32's; the call site is already there).
- `extensions/index.ts`: null-marker failure emission in the scanned handler; marker stamp + pending push in `council-eval`.
- `extensions/hub-tools.ts`: runner block appended in the wait path; `formatUsageSegment` refactored onto the shared helpers with a byte-identity test.
- `council/procedures/features-deliver.md`: one Phase 3 sentence.

## Tradeoffs accepted

- **Block-before-summary ordering in `/council-eval`'s output** (the flush fires inside the last job's onChange): accepted — forcing the block after the summary would need a second render site or a deferred queue, and both ends up in the same concluding output.
- **`n/a` trigger is a heuristic** (`turns > 0 && cost === 0`): a genuinely free catalogue model mislabels as n/a. EV-29 (provider-reported cost) is the real fix; until it lands, explicit-and-slightly-over-inclusive beats implicit, per the human's stated preference.
- **Runner block omits ownSession**: one line fewer than the session-exit shape; the honest alternative to a fabricated zero. Named as the one byte-shape question product-owner may rule differently.
- **Redundancy at the runner point** (EV-28 head-line segment + block in one wait report): accepted; they answer different questions and EV-28's surface is settled.

## Rejected alternatives

- **Deleting the head-line usage segment** ("block replaces the scalar"): reopens EV-28's byte-ruled surface for no acceptance gain.
- **Rendering the block from the model's prose** (procedures print it themselves): violates the card's own determinism requirement; untestable by string equality.
- **Runner stamps its own marker / seats stamp**: breaks the parent-mode-only stamping rule and puts a write path in child mode; per-node reconciliation is already FLLWUP-31.
- **Extending `SpendRecord` with an "unavailable" cost state** so the renderer needn't heurist: EV-31 froze the record shape byte-verbatim (`SpendRecord` gains no field); the heuristic is reversible when EV-29 lands.
- **A sixth per-job block surface for every attended dispatch**: the card's set is five; attended `/council` gets its block via the invocation flush; per-job blocks beyond the runner point are scope creep.

## Testable claims

- **T-B1 (acceptance, per exit):** a fixture `SpendRecord` renders a block where every number parse-back-equals the record field (tokens, turns, cost, boundary ids, jobs) — fails any renderer that invents or drops a figure.
- **T-B2 (byte-stability):** `formatUsageBlock(spend)` called twice returns identical bytes; the renderer contains no clock, no locale, no I/O.
- **T-B3:** all-zero record → output is exactly `usage  no usage recorded`.
- **T-B4:** flush write failure → notify matches `/^usage  accounting failed — /` **and contains the absolute store path** (EV-31's T-U14 stays green unmodified — this is the claim that my reason-composition design settles).
- **T-B5:** half with `turns>0, cost===0` → that slot reads `cost=n/a` and the legend line is present; a record with no n/a renders **no** legend line.
- **T-B6:** `costBasis:"reported"` → `cost=$0.1234 (reported)`; catalogue → `cost≈$0.0123 (catalogue)` with U+2248 asserted by code point.
- **T-B7:** `formatUsageSegment`'s output is byte-identical before/after the helper extraction (EV-28 regression guard).
- **T-B8:** flush on the `written` transition emits exactly one notify equal to `formatUsageBlock(record.spend)`; the `existing` outcome emits none (choose-once holds at the surface).
- **T-B9:** `recordInvocationBoundary → null` in a scanned-procedure handler → synchronous emission of `usage  accounting failed — invocation boundary unresolvable`; nothing else rendered, nothing blank.
- **T-B10:** `/council-eval` run path stamps a marker and pushes a pending invocation keyed `command: "council-eval"` before the first dispatch (assert on the ledger + on-chain marker entry, using the eval-fixture harness without a network dispatch).
- **T-B11:** fixture manifests (runner + two children) → the wait report's block shows `entries=unresolved jobs=3`, subtree numbers === `sumSubtree` over all metrics, and **no ownSession line**.
- **Gates:** `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` green on the implementation tree; integration test stays gated.

**Ruling-seat items named, not decided by me:** (1) whether R-6's "identical shape" permits the runner block's ownSession omission (my position: yes — grammar identity, not line-count identity; a zero-fabricated half is the alternative); (2) whether the free-model n/a mislabel ships as accepted-with-EV-29-forward or blocks on a third cost state (my position: ships; EV-29 owns the honest fix).

#### Position — `principal`

## Cross-seam reading — what the contract actually is right now

**Wiki gap first:** `vault/wiki/index.md` has no usage/accounting/spend page. The closest pages (`[[run-transcripts]]`, `[[hub-job-supervision]]`, `[[eval-store-contract]]`, `[[headless-pi]]`, `[[procedures-vs-commands]]`, `[[council-runner]]`, `[[smoke-test]]`) describe the substrate but not the spend surfaces. EV-30's own ruling says the same ("the spend record is a new surface", `council/cards/EV-30.md` step 6). So my grounding below is code + cards + specs; where I rely on a wiki page I name it.

**The five points are not five renderings of one accounting object.**

| point | artifact | who can account, when |
|---|---|---|
| `/council`, `/features-new`, `/features-deliver` | one registration loop, `index.ts:213-283` | handler stamps the EV-30 marker then `sendUserMessage`; **TUI returns immediately** (`index.ts:274-277`), headless awaits idle (`:278-293`) |
| `/council-eval` | TS command, `index.ts:352-…` | handler awaits `runMatrix`, then `emit(summaryLines(...))` — the only in-handler conclusion |
| council-runner card execution | seat child, `council/agents/council-runner.md` | `index.ts:146-150` returns into `runChildMode` **before** procedure registration: no command, no marker, no `flushPendingInvocations`, no `ctx.sessionManager` boundary. Its output is the assistant text the hub captures at `hub.ts:218` |

**Two accounting objects, two bases, and a shipped prohibition on merging them.** `spend.ts` returns `SpendRecord{ boundary, ownSession /*session-reconciled*/, subtree /*stream-assistant, lower bound*/ }`; `usage-store.ts:96-108` ships a README telling readers "Never read them as the same measurement", and EV-30's ruling item 2 forbids `total` on the record precisely so that any combined number is rendered "where both bases are explicit". Any design that prints one summed token/money line violates both.

**Most of the literals are already implemented — do not recompose them.** R-1 lives only in `formatUsageSegment` (`hub-tools.ts:51-57`); R-4 and its unresolved form live only in `formatBoundaryLabel` (`spend.ts`, "R-4 resolved label byte-exact; unresolved form per ruling item 3"). EV-28's card says explicitly that the segment was extracted so "EV-32's sibling surface (same R-1 money signifier) reuses one implementation". `findLatestInvocationMarker` exists "for exit-time callers that no longer hold the in-memory marker id" (Interface completion for EV-31/EV-32) — a marker-less caller was anticipated. And EV-31's step-6 record: "**`agent_settled` gated on an unconsumed on-chain `council-invocation` marker is the correct seam**" — so points 1–3 inherit a *ruled* seam, not a new one.

### The block I would render (byte level)

Prefix is ruled-consistent: both R-5 literals are `usage` + two spaces, so every line of the block carries `usage  `. Rows in order: measurement, measurement, boundary, legend. No header — the record (`SpendRecord`) carries no command/runId, and anything I printed beyond it could not be asserted against "the fixture accounting record".

```
usage  ownSession  basis=session-reconciled  turns=1 tokens=in 100/out 10/cR 900/cW 0/reason 7/total 1010 cost≈$0.0013 (catalogue)
usage  subtree     basis=stream-assistant  turns=4 tokens=in 9000/out 2100/cR 44000/cW 800/reason 700/total 55900 cost≈$0.2010 (catalogue)
usage  boundary=session=ses_abc entries=e12..e57 jobs=5
usage  n/a = provider figure unavailable
```

Byte rules: `usage`+2 spaces; half label `ownSession`|`subtree` padded to 10; 2 spaces; `basis=` + `record.<half>.usageSource` (**read from data, so a half can never be mislabelled**); 2 spaces; then `formatUsageSegment(record.<half>)` verbatim — the token split is `turns` / `in / out / cR / cW / reason / total` with no thousands separators, money rightmost (`≈` U+2248 catalogue, `$` reported), exactly EV-28 Q5. Row 3 = `usage  ` + `formatBoundaryLabel(record)` verbatim. Row 4 = `usage  n/a = provider figure unavailable` (em dash with two spaces before the legend text is *not* the same glyph as R-5's `—`; R-5 uses U+2014 with a space). No trailing newline, no ANSI, no locale. Every line ≤160 cols (EV-28's T5 discipline; wide fixture → follow-up card, as EV-28 ruled).

States, precedence **failed > empty > full**:

```
usage  no usage recorded
usage  accounting failed — <reason>
```

Both are **whole-block replacements** (one line), for the same reason R-5 pairs them: a failed accounting has no boundary to name, and zero usage has no numbers. The failure reason is call-site data, newline-normalized so the state stays one line. No legend in either state.

Renderer contract (new pure leaf `extensions/usage-block.ts`; `formatUsageSegment` moves to `extensions/usage-format.ts`, re-exported from `hub-tools.ts` so EV-28's landed import/test are untouched — this also breaks the hub-tools↔block import cycle that point 5 would otherwise create):

```ts
formatUsageBlock(input: { record: SpendRecord; unavailableCost?: Half[] } | { failed: string }): string
```

`unavailableCost` is R-2's `n/a` hook (`cost=n/a (reported)` for that half via an optional second arg to `formatUsageSegment`, kept to one composition); EV-29 — the card that actually makes a provider figure absent — populates it. Everything else about the block is derived from the record.

### Where each point renders it

- **Points 1–3 (`/council`, `/features-new`, `/features-deliver`):** from the **one** `agent_settled` handler EV-31 already owns (`index.ts:189-193`), after `flushUsage`. Same ledger, keyed by markerId, with a **`rendered` set** separate from the flush's `remaining` (a gate-closed *write* must not suppress a *render* — rendering computes from live memory, not the store). In TUI the handler has long returned, so `agent_settled` is the only engine-observable conclusion; in headless it fires too, and the sink falls back to `console.log` (the `flushUsage`/`emit` precedent already in this file). Emit out-of-band — `ctx.ui.notify(block, "info")` / `console.log(block)` — **never** `sendUserMessage`: an injected turn appends a session entry that the boundary's own-session range would then be able to pick up, which is the "cannot include the cost of its own generation" clause failing by construction.
- **Point 4 (`/council-eval`):** the handler stamps the same marker (so it gets a durable record and a real own=~0/subtree=cells split), and renders from its awaited post-`runMatrix` tail — the only point with an in-hand conclusion — then marks the marker rendered so `agent_settled` skips it. The no-arg listing form renders no block (it is not a run). To make the AC's five-point test reachable I'd inject the matrix runner into the command registration (a `deps` default to the real `runMatrix`) rather than hitting the network in a unit test.
- **Point 5 (runner card execution):** rendered **by the parent**, at `council_wait`, appended to that job's report — i.e. into exactly the text `features-deliver`'s Phase 3 ledger reads. The record is built from the runner's **own session JSONL**: `findSessionFile(repoRoot, runId, job.id)` (`runs.ts:134`, already exists) → `parseSessionEntries` (precedent `usage-store.ts:defaultReadEntryIds`) → marker via `findLatestInvocationMarker` → `spendRecord` with the run dir's manifests. Own half = the runner's reconciled session (basis `session-reconciled`); subtree half = **its own job and descendants** (basis `stream-assistant`). Two additive changes ride along: child mode appends a `council-invocation` marker on `session_start` for container seats (gated on the seat declaring `spawns` — no seat name hardcoded), and `spendRecord` gains an optional `rootJobId` whose default preserves today's null-rooted rule byte-for-byte. EV-30 ruling item 6 already contemplates recompute-from-the-same-run-dir with "the basis decision owned by the recomputing side", so this is anticipated, not novel.

**The trap this avoids, and it is the reason I am writing this:** computed *inside* the runner, `spendRecord`'s forest rule ("roots = manifests with `parentJobId === null`") finds **nothing** — a runner's own job is not null-rooted; its manifest is written by the *parent's* hub. The subtree half would then read as a small, plausible, completely wrong number (the runner's own turns only, hiding every seat the card dispatched), and the card's acceptance test as literally written — "the block is present and its numbers equal the fixture accounting record" — **passes**. That is the same failure class EV-31's principal named (`council/cards/EV-31.md:177`): a successful acceptance test hiding the cross-seam defect.

## Blind spots (named separately, not mirrored)

- **Owner/engine slice:** sees the renderer and five call sites; cannot see that the TUI procedure handler returns before the turn concludes, so an owner's natural design emits from the handler, passes a headless test, and never fires for a real TUI `/council`.
- **Accounting slice (spend.ts vantage):** sees records and bases; cannot see that a container's job is not a forest root, so the child-scope subtree is structurally empty and its undercount is invisible to a fixture-shaped test.
- **Designer/surface slice:** sees the copy; cannot see that R-5's literals have two plausible homes (whole-block vs one-row replacement), that EV-31's store-notify lands at the same moment as the block (two lines, ordering racy — the obvious-but-wrong fix is dropping one), or that R-2's marker is unreachable until EV-29 lands — a designer's likely instinct ("print the legend only when `n/a` appears") would break R-6's single shape and the `[[smoke-test]]` precedent of grepping ruled literals.
- **Card/product slice:** sees "the block names its accounting boundary" and cannot see that EV-30's two ruled boundary forms leave one real case uncovered — entry ids unavailable while jobs are known (`formatBoundaryLabel` hardcodes `jobs=0` when unresolved).

## Reframe

Not a taste difference — a scope reframe: **the work is not "render a block five times"; it is "obtain a resolved `SpendRecord` for *this* invocation" at five points, then render once.** Four points have a session boundary (marker + `agent_settled`); the fifth is a job, and its only truthful boundary is the runner's own session JSONL — not the parent's manifest forest. Stating it that way makes the framing testable: each point's contribution is "does a resolved record exist here, and does its boundary name the right range", and the block's bytes are then point-independent, which is what R-6 actually asks for. (Card title says "entry point"; goal and Intent say exit point — the five are exits.)

## Testable claims

1. `formatUsageBlock(fixture)` equals a literal string **and** for each of the 12 metrics `block` contains `String(record.<half>.<metric>)` — "numbers equal the record" mechanically, not by eyeball. Same input twice, and a structuredClone, give identical bytes.
2. `block` contains `formatBoundaryLabel(record)` verbatim; an unresolved-boundary fixture renders `… entries=unresolved jobs=0` and is *not* the failure state.
3. All-zero record → exactly `usage  no usage recorded` (no `basis=`, no `cost≈`, no `boundary=`, no legend). `{failed}` with a reason containing `\n` → exactly one line containing U+2014, asserted as `\u2014`.
4. Every line of both EV-28 fixtures ≤160 columns.
5. **Container-underestimate test (the one that matters):** a settled job whose seat declares `spawns`, with ≥1 descendant manifest, renders `jobs=≥1` and a subtree line equal to that job's own manifest usage **plus** its descendants' — not `jobs=0`, not the runner's own turns alone. A red test today, on purpose.
6. Non-container job reports carry no block; a still-running job's report carries no block.
7. One emission per invocation: two `agent_settled` events after one marker → one block; firing the `/council-eval` handler then `agent_settled` → one block; a plain user turn after a rendered invocation → zero.
8. Emitting changes nothing observable in the session: `getEntries().length` and `getLeafId()` are identical before and after, and `sendUserMessage` is never called with the block.
9. A source test (grep/import graph) that `spendRecord` is the only place tokens/dollars are summed, and `formatUsageBlock` the only place the block's bytes are produced — five call sites, one composition.

## Tradeoffs accepted / alternatives rejected

Accepted: two measurement rows and no combined figure (a sum would falsify the shipped README's "never read them as the same measurement" and EV-30's both-bases-explicit ruling; I cannot name a word that is both ruled and true for it); an unconditional legend; render-time compute rather than a store read (EV-31's write is gate-closed until the forest settles, so a store read would render `failed` for a merely-not-yet-written record, and multi-invocation sessions make store keying fragile); points 1–3 render at settle, so a run that dies before settling renders at the shutdown sweep or not at all — the durable record remains the backstop.

Rejected: per-site rendering (forks R-1/R-4 five ways, defeats R-6); the runner pasting a block into its own prose (LLM-mediated, mangleable, untestable — and it contradicts the card's own "not by the procedure's own prose"); the child rendering its own block (its stdout is a `--mode json` event stream; non-JSON lines are silently dropped at `hub.ts:190-193`); reading the durable store at exit.

**Ruling questions I am not ruling** (named, with facts only): (a) may point 5's block live in the hub's job report — deterministic and testable — rather than inside the runner's own return-contract prose, where the AC's assertion becomes a live-model grep; (b) the new `basis=` field label and the two-row shape, which no Phase-1 ruling names; (c) whether R-5's two states replace the whole block or one row (I propose whole block); (d) `rootJobId` on EV-30's `spendRecord` plus a child-mode marker — a scope extension of a previously ruled module.

#### Position — `designer`

Confirmed — no wiki page for any EV-28/30/31/32 today. The briefing's gap is real.

I have all I need to produce the position. Per the output_format spec:

---

**Design position** — One deterministic handler in `extensions/usage-block.ts` produces a single byte-stable block at all five autonomous exit points; the procedures and commands only own *when* the block is emitted, never *what* it says. The block is two lines: a `usage  …` line carrying the token split, the provenance-labelled money, and the boundary label (R-1/R-2/R-4 literals in their ruled slots), and an `n/a = provider figure unavailable` legend line below it whenever any slot carried `n/a` (R-2). Zero usage renders `usage  no usage recorded` (R-5); accounting failure renders `usage  accounting failed — <reason>` (R-5) — both still emit the legend line only when the relevant slot was `n/a`. The handler is fed the same `SpendRecord` from EV-30 that the durable store uses, so byte stability is structural and the test asserts string equality against a fixture record.

**Gulf closed** — Gulf of **Evaluation** for the human at every autonomous exit, in both the TUI notify and the headless terminal dump. At the moment a human (or a downstream orchestrator) finishes reading the exit output, they can tell (a) what was spent, (b) on what basis, (c) over what boundary, and (d) where the numbers cannot be claimed — without opening the durable store file. The runner's fifth exit (`<return_contract>` `DONE`/`ESCALATION`/`HALT`) closes the same gulf for the orchestrator reading Phase 3's ledger, because features-deliver's ledger entry for that runner carries the block verbatim and surfaces it in the Phase 3 report rather than burying it inside the runner's JSONL.

**Principle and evidence** — **Signifier + feedback + conceptual model**, applied at `extensions/usage-block.ts` (the new handler) and at `extensions/index.ts`/`council/procedures/*.md` (the call sites). The signifiers are R-1/R-2/R-4/R-5/R-6 themselves — `≈` vs `$` for catalogue vs reported, `n/a` + legend for unavailable, `boundary=session=… entries=… jobs=…` for the accounting scope. The handler maps `SpendRecord` → block in one pure function (`formatUsageBlock(record): string`), so a person reading the block can construct a single sentence about it: "this much, of this kind, over this range, and here is what that does and doesn't include." The R-1/R-2/R-4/R-5/R-6 literals in `council/cards/EV-32.md` lines 21–27 are the binding shape; the `Usage` and `SpendRecord` types in `extensions/runs.ts` lines 19–34 and `extensions/spend.ts` lines 31–40 supply every numeric slot. The existing `formatUsageSegment(u)` in `extensions/hub-tools.ts` line 51 (EV-28) and `formatBoundaryLabel(record)` in `extensions/spend.ts` line 270 (EV-30) are precedents the handler must *not* re-implement: `formatUsageBlock` composes them rather than forking.

**Falsifiable predictions** (the Skeptic may run any of these as out-of-band CDP smokes, per `smoke-test`'s ruled-copy charter — never gate assertions, because this seat cannot see a running interface):

1. *Byte-stability*: feeding the same `SpendRecord` to `formatUsageBlock` twice yields identical strings. The `usage-store.test.ts` precedent for record-purity tests (T-U15 at `docs/superpowers/specs/2026-09-11-EV-31-design.md:331`) extends naturally — one more assertion per record fixture.
2. *R-1 money signifier*: a fixture record with `costBasis: "catalogue-estimate"` renders `cost≈$0.1234 (catalogue)` on the `usage` line; one with `costBasis: "reported"` renders `cost=$0.1234 (reported)`. The `≈` is U+2248; the `$` is U+0024. A grep of the rendered string for the wrong character fails the test — that catches an accidental substitution.
3. *R-2 unavailable marker*: a fixture record where `cost === null` renders `n/a` in the cost slot and includes the legend `n/a = provider figure unavailable` on the following line; a record with a real cost emits no legend line.
4. *R-4 boundary label*: a resolved boundary renders `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>`; an unresolved one (marker absent, marker off-chain) renders `boundary=session=<sessionId> entries=unresolved jobs=0`. Same byte string the existing `formatBoundaryLabel` in `extensions/spend.ts:269–275` produces.
5. *R-5 empty state*: a fixture record with all numeric zeros renders exactly `usage  no usage recorded` — no legend line because no slot was `n/a`.
6. *R-5 failure state*: a call from each of the five exit points with the accounting function throwing renders `usage  accounting failed — <reason>` where `<reason>` is the exception's message (never a stack trace, never an empty string).
7. *R-6 byte-identity across the five exit points*: the same `SpendRecord` renders the same block at `/features-new`, `/features-deliver`, `/council`, `/council-eval`, and the runner's return-contract report — a single test feeds one record to five wrapper functions, asserts string equality. The wrapper functions are the five call sites; the block is not.
8. *Boundary honesty*: the block never includes any token whose entry id lies *before* the boundary's `firstEntryId`; the test is "feed a session whose pre-boundary assistant has 1000 input tokens, render the block, parse the input field, assert ≤ the post-boundary count." This catches a future regression where someone "helpfully" sums the whole session.

**Tradeoffs accepted** — (a) Two-line block, not a table: a table would carry more numbers but a person reading a terminal dump or a `notify` does not parse tables — they scan a single line. The cost of giving up tabular layout is that the cacheRead/cacheWrite/reasoning split has to fit on one line, which means short labels (`cR`/`cW`/`reason`) that mirror EV-28's head-line exactly — a person who has seen one has seen the other; nothing new to learn. (b) Legend only when relevant: omitting the legend line on records with no `n/a` slot saves a line in the common case at the cost of one extra conditional the renderer must evaluate. The alternative — always-on legend — wastes a line on the most common case and teaches a person to ignore a line that *would* matter when `n/a` appears. (c) The handler takes `SpendRecord`, not raw `Usage`: this means the block cannot be rendered for a procedure that has no `recordInvocationBoundary` marker (the boundary is unresolved, which is exactly the case R-4 names). The alternative — letting each site pass a `Usage` and constructing a fake boundary — loses the honesty that the label names the included range and not completeness. (d) The runner's fifth exit renders the block on the *report* the orchestrator reads in Phase 3, not on the runner's `notify`. The orchestrator already structures runner output through its `return_contract` and Phase 3 ledger; routing the block through `notify` instead would make it disappear into the runner's session, where the orchestrator that surfaces it in the ledger would have to re-parse a notify string. The block belongs on the report, where it's part of the durable artifact the orchestrator already writes.

**Alternatives rejected** — (i) *Per-site rendering*: each of the five surfaces authors its own block in its own prose. Rejected because R-6 binds "identical in shape across all five exit points," and because the human asked for explicit beats implicit — five renderers, each with its own drift surface, is implicit. (ii) *Single-line block*: the rule of thumb in `vault/wiki/procedure-commands` is markdown procedures are LLM-judgment and TS commands are LLM-obedience; a single line would be terser but the boundary label and the legend already force a second line in the failure case, and forcing the success case into one line means breaking the label set across punctuation that a person would have to mentally rejoin. (iii) *Block on `notify` only, not on headless*: TUI and headless diverge. Rejected because `index.ts:406` already proves both surfaces must carry the same byte sequence (the `council-models` headless precedent). (iv) *Including the cost of generating the block itself*: the briefing rules this out — "the block is rendered after the accounting function returns, so it cannot include the cost of its own generation; the boundary label names the included range instead of implying completeness." Honored structurally: the handler runs once per exit, reads `SpendRecord` immutably, and is not itself a dispatch that contributes to `Usage`. (v) *Re-using `formatUsageSegment` verbatim*: that function is a single-line head-line segment keyed on `Usage`, not on `SpendRecord`; it has no boundary slot, no `n/a` slot, no legend. The block is a *composition* — `formatUsageBlock = formatUsageSegment(record.ownSession + record.subtree) + " " + formatBoundaryLabel(record)` plus the legend and failure states. Re-implementing `formatUsageSegment` inside the block would fork the head-line and break the "seen one, seen the other" property. (vi) *A wiki source page for usage/accounting*: the briefing notes the wiki gap; this card does not fill it, because EV-28/30/31 are already Done without wiki pages (the wiki is currently 0-for-4 on the EV-7 lineage) and a wiki page mid-deliberation would be a parallel commitment this card has not earned. Filing `FLLWUP-<n>` for a wiki page on usage/accounting is the right home if anyone wants one.

**Standing constraints honored** — No `git checkout/switch/reset` against `/home/tista/codes/pi-council` (this turn made zero state changes to the main repo; all reads were `read`/`grep`/`find`/`ls` against the working tree). No bash commands with implicit timeouts were started; no long-running foreground processes were started. I am a generator seat, not `owner`: this position names a handler (`extensions/usage-block.ts`), five call sites, and a test file, but I do not write any of them. The `owner` builds against this position.


### Step 3 — bounded exchange, round 2 (cap 3)

Each generator re-dispatched with the others' recorded round-1 positions (read
from this card, verbatim). Jobs `job-16.4` (owner), `job-16.5` (principal),
`job-16.6` (designer). Positions verbatim.

#### Position — `owner`

Grounding complete. Every load-bearing fact has been checked against the tree this round: `spend.ts` (whole file), `usage-store.ts` (whole file), `hub-tools.ts:45-57` and the `council_wait` handler, `index.ts` (scanned-procedure handler, `council-eval` handler end-to-end, `flushUsage`/triggers), `hub.ts:190-221`, `runs.ts:134`, and `council/agents/council-runner.md` frontmatter. Round-2 position follows.

---

## ROUND 2 — `owner`

**Approach.** The three round-1 positions converged on the deep structure — one pure renderer, two halves never summed, parent-side rendering for the fifth point, engine-rendered bytes never prose — and diverged on six resolvable shapes and one genuine reframe. I concede where the code says the other seat is right (point 4's render moment, basis-from-data), hold where the code says I'm right (render-at-written-transition for points 1–3, runner ownSession omission as the minimum), and the designer's single-line composition I must concede outright: it is not a shape difference, it is the exact defect the shipped record forbids.

### Dispute 1 — line structure: are the two halves ever one number?

**CONCEDE against designer; HOLD two rows with principal.** The designer's rejected-alternative (v) states the composition as `formatUsageSegment(record.ownSession + record.subtree) + " " + formatBoundaryLabel(record)` — that is a numerical merge of `ownSession` (basis `session-reconciled`, a full reconciliation) with `subtree` (basis `stream-assistant`, a *lower bound*). Three facts in the tree forbid it:

- `usage-store.ts` README, §"The two halves are different kinds of number": *"Never read them as the same measurement."*
- EV-30's ruling removed `total` from `SpendRecord` precisely so a combined figure can only be rendered where both bases are explicit (spend.ts header comment, lines 1–10).
- Mechanically, there is no `Usage + Usage` in the code: `costBasis`/`usageSource` are single-valued (`zeroUsage`, spend.ts), so a summed half must silently pick one basis for a number that had two — the block would render a lower bound and a reconciliation as one figure with one label.

There is no word that is both ruled and true for that sum. The designer's own boundary-honesty prediction (#8) would fail against their own composition: summing the halves pulls in manifest usage that has no entry id on the invoking session's chain at all. Two rows, one per half, is the only structure the record supports. **HOLD.**

Within two rows, I **CONCEDE** to the principal on labelling: my round-1 bracketed annotations (`[session-reconciled]`) hardcode what the record already carries — the principal's `basis=<record.<half>.usageSource>` read from data means a half can never be mislabelled and needs no second source of truth. The store README teaches exactly these two names; print the record's own vocabulary. Padding to a fixed column and the `basis=` field name itself remain ruling-seat copy questions (named below), but basis-from-data is settled between us.

### Dispute 2 — where the block is emitted at each of the five exits

**Points 1–3 — HOLD render-at-the-flush's-`written`-transition, against the principal's `agent_settled` + `rendered`-set design.** The facts: the flush seam is EV-31's *ruled* seam ("`agent_settled` gated on an unconsumed on-chain marker is the correct seam", EV-31 step-6 record; implemented as the gate in `flushPendingInvocations`), the notify sink is already injected and dual-surfaced (`flushUsage` in index.ts: `ctx.hasUI ? ctx.ui.notify : console.log`), and the gate condition — every manifest with `startedAt >= markerAt` settled — *is* the card's "after each concludes" predicate. The principal's decoupled render needs a `rendered` set to solve a problem that doesn't exist under my design: a gate-closed *write* is not a suppressed *render*, it is an unconcluded invocation, and rendering from live memory at `agent_settled` while the write is still gated means the block can show numbers that never became the durable record — the exact divergence between "what was shown" and "what is on disk" the card's timing clause exists to prevent. Under render-at-written: `written` → block (replacing the existing success notify — the store's own comment already assigns that wording's ownership to EV-32), `failed` → R-5 failure state with the absolute path (T-B4), `existing` → silent (choose-once, already structural in the store key). One seam, one emission, byte-equal to disk. The principal's own claim 8 (emitting changes nothing observable in the session) holds identically under my design — neither design calls `sendUserMessage`, which I **CONCEDE** as a binding constraint with his reason: an injected turn lands inside the boundary's own-session range and would be counted by the next reconciliation — "cannot include the cost of its own generation" failing by construction. That constraint is testable and I adopt it as T-B12.

**Point 4 — CONCEDE to the principal.** I verified the full `council-eval` handler: it never stamps a marker today, and unlike the scanned procedures its handler *awaits* `runMatrix` and then emits `summaryLines` — the conclusion is in-hand in both TUI and headless. The principal is right that the awaited tail is the true concluding output, and that emitting via the mid-run `onChange` flush (my round-1 design) produces the block before the summary at a moment that is not the conclusion. Revised position for point 4: stamp `recordInvocationBoundary` + push pending at the top of the run path (after arg validation, skip the no-arg listing form — not a run), let the flush still write the durable record at the ruled seam, but render the block at the awaited post-`runMatrix` tail, after `summaryLines`, and mark the marker rendered so the `written`-transition emission skips it. One emission per invocation is then asserted, not assumed (T-B8 extended). The injection of a test-double `runMatrix` via a `deps` default is the right way to make the five-point test network-free.

**Point 5 — converged, one residual.** All three seats put the block in the parent's `council_wait` report (`hub-tools.ts:214`, returns `formatReport` text into the caller's context — exactly what features-deliver's Phase 3 ledger reads), engine-rendered, appended deterministically. Designer's reasoning for report-over-notify is correct and I adopt it. Residual below in dispute 3.

### Dispute 3 — the fifth point's record: ownSession obtainable?

**HOLD on omission as the card-satisfying minimum; the extension is an open question for the run.** First, the principal's container-undercount trap does not apply to my design as written: I compute parent-side at wait-collection time over the job id explicitly, not inside the runner, so the null-root forest rule is never consulted. But the trap did its job: it exposed that `spend.ts` today has no subtree-from-job-id computation at all — `spendRecord`'s forest rule is null-rooted with a boundary-time filter. So point 5 needs one of: my proposed `sumSubtree(jobId)`, or the principal's `rootJobId` parameter on `spendRecord` — these are the same extension wearing two coats, and the principal himself named it a scope extension of a ruled module. That is an open question for the run, not something I may settle.

On the `ownSession` half: the principal is right that it is *technically* obtainable parent-side — `findSessionFile(repoRoot, runId, sessionId)` exists (runs.ts:134), `parseSessionEntries` has an in-repo precedent (`usage-store.ts` `defaultReadEntryIds`), and the runner's session file still exists at wait time (pruning is to the last 15 *runs*, and the read happens before any prune). But it requires both scope extensions — child-mode marker stamping for `spawns`-declaring seats (the runner never stamps today; seats return into `runChildMode` before registration, so zero marker exists on that session, and `findLatestInvocationMarker` would return null → unresolved → zeroed halves) plus the record-shape-adjacent parameter. A zero-ownSession line would fabricate a measurement never performed; a real one requires new write paths in child mode. I hold the omission as the minimum that clears the acceptance text, name the extension as open, and note the honest asymmetry must be visible in the bytes either way — if the run rules the extension in, the ownSession line appears with its true `session-reconciled` basis; if out, no line, per my round-1 shape.

### Dispute 4 — `n/a` legend placement and conditionality

**HOLD conditional (present iff the block contains any `n/a`); placement settled.** Verified fact that disciplines all three round-1 positions: `Usage.cost` is a `number` everywhere (`zeroUsage`, `accumulateWire`'s `|| 0` coercion) — **cost is never null today**, so both the designer's `cost === null` trigger and the principal's `unavailableCost` hook are unreachable until EV-29 lands; only my heuristic (`turns > 0 && cost === 0`) can fire today, and it is the only path that makes the ruled `n/a` literal *testable now*. On conditionality: R-2 binds the marker and the legend text, not their frequency; R-6 binds determinism and shape, and a conditional legend is byte-stable for any given record. A legend asserting unavailability on a block with no `n/a` slot is the plausible-but-false pattern in reverse. The designer's tradeoff (b) states my reason exactly. Placement — final line of the block, `usage  `-prefixed — is converged among all three. I name legend conditionality as a ruling-seat question (facts only, below), and I keep the free-model mislabel as the already-named accepted tradeoff pending EV-29.

### Dispute 5 — R-5 zero/failure states: whole block or one row?

**CONCEDE — no dispute remains.** All three positions converged on whole-block replacement, one line each: `usage  no usage recorded` and `usage  accounting failed — <reason>` (U+2014). The unresolved-boundary case zeroed both halves by EV-30's rule (spend.ts, "zero BOTH halves, never an error"), so it correctly renders as the zero state, with `boundary.resolved: false` retained on the durable record for forensics. One composition note converged with the principal: the renderer takes `SpendRecord`; the failure reason is call-site data (newline-normalized to keep one line), composed with the R-5 literal at the call site — and for the flush-failure path I keep my reason composition `<write failed for> <absolute path>: <error>` because it preserves EV-31's T-U14 property (absolute store path in the notify) inside the ruled wording.

### Dispute 6 — how `/council-eval` enters the accounting

**Converged on the principal's frame, with one correction.** Facts: the handler stamps nothing today (verified end-to-end); the three scanned procedures stamp in one registration loop; `runMatrix` dispatches through the same hub singleton whose `onChange` drives the flush, so eval jobs are covered by the existing gate once a marker exists. The entry is: marker + pending push at the top of the run path, skip the no-arg listing form, durable record via the unchanged flush, render at the awaited tail (conceded under dispute 2, point 4). My one correction to the principal: his round-1 table says only `/council-eval` has "the only in-handler conclusion" — true, and that is exactly why the render belongs there and nowhere else; it is the exception that proves the render-at-written rule for points 1–3, not a contradiction of it.

### Dispute 7 — notify line, console.log, or report string?

**Converged; one constraint adopted.** Points 1–3: the existing injected sink — `ctx.ui.notify` in TUI, `console.log` headless (`flushUsage`, verified) — with the block replacing the current success notify so one line, not two, lands (the two-line racy ordering the principal feared only exists if the old notify survives; EV-31's own ruling transferred that wording's ownership to EV-32). Point 4: the handler's existing `emit` helper (same dual routing, `index.ts`, `council-eval` handler) after `summaryLines`. Point 5: appended to the `council_wait` report string — the durable text Phase 3's ledger reads. Adopted as binding: **never `sendUserMessage`** (principal's reason verified sound — the boundary's own-session reconciliation would count the injected turn), testable as T-B12 below.

### Open questions for the run (not settled by me)

1. **Subtree-from-job-id** — `sumSubtree(jobId)` or `rootJobId` on `spendRecord`: a scope extension of EV-30's ruled module (principal's naming stands; the two proposals are the same extension).
2. **Child-mode marker for container seats** — prerequisite for a true `ownSession` at point 5; a new write path in child mode.
3. **Consequently, point 5's ownSession**: present-with-extension, or omitted-minimum (my hold).

### Ruling-seat questions (facts only)

- **(a)** Whether R-6's "identical in shape" is grammar identity (my and principal's reading) or line-count identity — deciding whether the runner block may omit the ownSession row absent the extensions. Fact: the extensions are required for any non-fabricated ownSession at point 5.
- **(b)** The `basis=` label text and row/column formatting — no Phase-1 ruling names them; the only ruled constraints are R-4's boundary line bytes and the `usage␣␣` prefix implied by R-5's two literals.
- **(c)** Legend conditionality (dispute 4) — both designs are byte-stable; the choice is copy policy, not determinism.
- **(d)** Whether the flush success notify is *replaced* by the block (my design) or retained alongside it — fact: `usage-store.ts`'s own comment assigns that notify's wording ownership to EV-32, which reads as replacement, but the byte fate of the old line is a surface ruling.

### Tradeoffs accepted

- Point 4's block renders after the eval summary (conceded ordering; both in the same concluding output, and the summary is the part humans read first anyway).
- The `n/a` heuristic mislabels a genuinely-free catalogue model as `n/a` — explicit-and-slightly-wrong beats implicit, per the human's stated preference; EV-29 is the honest fix.
- Runner block omits ownSession (one row fewer) unless the run rules the extensions in — a fabricated zero is never acceptable.
- Redundancy at point 5 (EV-28 head-line segment + block in one wait report) — different questions, settled surface untouched.

### Testable claims

- **T-B1** — fixture `SpendRecord` → every rendered number parse-back-equals the corresponding record field; no combined half-sum anywhere in the block (regex: the block contains no line whose token total equals `ownSession.totalTokens + subtree.totalTokens`). Fails the designer's composition.
- **T-B2** — `formatUsageBlock(spend)` twice + on a `structuredClone` → identical bytes; no clock/locale/IO in the module.
- **T-B3** — all-zero record → exactly `usage  no usage recorded`; unresolved boundary → same zero state, not a failure state.
- **T-B4** — flush write failure → notify matches `/^usage  accounting failed — /` (U+2014) *and* contains the absolute store path; T-U14 stays green unmodified.
- **T-B5** — `turns>0 && cost===0` on a half → that slot reads `cost=n/a` and the legend line is present; a record with no `n/a` slot renders no legend line (the conditionality claim, either ruling outcome).
- **T-B6** — `costBasis:"reported"` → `cost=$…(reported)`; catalogue → `cost≈$…(catalogue)` with U+2248 asserted by code point.
- **T-B7** — `formatUsageSegment` byte-identical before/after helper extraction (EV-28 regression guard).
- **T-B8 (extended)** — exactly one block per invocation: `written` → one; `existing` → zero; `/council-eval` run + subsequent `agent_settled` → one.
- **T-B9** — `recordInvocationBoundary → null` in a scanned handler → synchronous `usage  accounting failed — …` emission; never blank.
- **T-B10** — `/council-eval` run path stamps a marker and pushes a pending invocation keyed `command: "council-eval"` before the first dispatch; no-arg listing form pushes nothing.
- **T-B11** — fixture manifests (runner + descendants) → wait report's block shows the job's true subtree (`jobs=≥1`, sums over the job closed over descendants) and either no ownSession row or one whose numbers derive from the runner's session JSONL — never a zero ownSession.
- **T-B12 (new, adopted from principal)** — rendering changes nothing observable in the session: entry count and leaf id identical before/after; `sendUserMessage` never called with the block.
- **T-B13 (adopted from designer)** — boundary honesty: with a pre-boundary assistant message carrying 1000 input tokens, the rendered input count excludes it.
- **Gates** — `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` green; integration test stays gated.

I have nothing further to add; this is my round-2 position and I yield.

#### Position — `principal`

## Cross-seam reading — the contract as the code actually has it

I re-read both sides of the two seams this card crosses.

**Seam 1: session → spend record.** `spendRecord` (spend.ts) resolves the boundary as "the first on-chain user message *after* the marker in append order" (spend.ts:~120-135). On failure to resolve it returns **zero-both**, before any forest walk. That return is not incidental — it is EV-30's *rejected option (4)* made binding: "Compute the subtree anyway on an unresolvable boundary — rejected: the forest is gated by the boundary's `startedAt` timestamp; without a resolved boundary there is no timestamp." So an unresolved boundary does not produce a small number; it produces **zero halves**, which the block then cannot distinguish from genuinely-no-usage.

**Seam 2: job → manifest forest.** `hub.ts:94` writes `parentJobId: this.run.parentJobPath ?? null`. The top-level dispatch of a `council-runner` has `parentJobPath === undefined` (hub-tools `initHubIdentity` in parent mode), so **the runner's job is a null-rooted manifest**. Its children (the seats it dispatches) carry `parentJobId = <runner job id>` (child.ts `initHubIdentity(COUNCIL_RUN_ID, COUNCIL_JOB_ID)`).

That second fact falsifies a load-bearing claim in my own round-1 position.

## Per-dispute verdicts

**D1 — line structure / two halves combined? HOLD: two measurement rows, never one number.**
- `UsageSource` is single-valued (`runs.ts:19-35`); the two halves carry different bases. EV-30's ruling (2) permits a combined number *only* "rendered at presentation time **where both bases are explicit**." Owner's 3-line block (boundary + ownSession row + subtree row) satisfies this; your position in round 1 proposed `formatUsageBlock = formatUsageSegment(record.ownSession + record.subtree)` with a single spend slot. That either sums two bases into one unlabelled figure — the exact `total` EV-30 vetoed — or silently drops a half (an undercount). **Designer, name which half your single `usage` line shows; if it shows both summed, this fails on the settled EV-30 basis ruling.** Owner and I converge; the designer's shape does not yet specify.

**D2 — emission at points 1-4. Mostly agree; one correction.**
Owner: emit as the flush's `written`-transition notify. Me: a `rendered` set separate from the flush's `remaining`, so a gate-closed *write* doesn't suppress a *render*. On the facts these coincide at the same moment (the flush fires at `forest-settle`/`agent-settled` when the gate holds; `flushPendingInvocations` is called from hub `onChange`, `agent_settled`, and `session_shutdown`, index.ts). I **concede** that reusing the flush call site is the least-surface option, *provided* the emission is not conditional on `written` — a pre-existing record (`existing`) must still render, and rendering must not depend on the write succeeding. I **hold** that the emission channel is `ctx.ui.notify` / `console.log` (flush's `notify` callback already falls back to `console.log` when `!ctx.hasUI`), never `sendUserMessage`: an injected turn appends a user message that `spendRecord`'s ownSession walk would pick up, falsifying the card's own "cannot include the cost of its own generation."

**D3 — the fifth point's record and the ownSession half. CONCEDE my mechanism claim; HOLD the scoping conclusion.**
My round-1 claim "a runner's own job is not null-rooted; its manifest is written by the parent's hub" is **false** — `hub.ts:94` makes the top-level runner job a forest root. I concede it outright. The conclusion survives for a different, code-grounded reason: **seats never stamp a marker** (index.ts returns into `runChildMode` before procedure registration), so inside the runner any `spendRecord` call has `markerId=null` → boundary unresolvable → **zero-both** (not the "small plausible wrong number" I wrote). At the parent, `council_wait` calling invocation-scoped `spendRecord` would instead sum **every** null-rooted manifest started after the features-deliver boundary — all runners, not this one. So owner's `sumSubtree(manifests, job.id)` scoping is **correct and needs no engine extension**; my `rootJobId` parameter is only needed if one insists on an ownSession half. That ownSession half (child-mode marker + reading the runner's session JSONL via `findSessionFile`, `runs.ts:134`) is a **scope extension of two settled artifacts** (`spendRecord`'s signature, child-mode registration) and I explicitly flag it as an **open question for the run, not something I may settle.** Owner's "omit, never zero-fabricate" is defensible; I do not press the extension.

**D4 — `n/a` legend placement/conditional. CONCEDE conditional; HOLD the trigger.**
I **concede** the legend is conditional (owner, designer): a standing legend that explains an absent marker is itself a false implication, and R-6's "identical in shape" is grammar identity, not a fixed constant line multiset. But both proposed triggers are wrong against the frozen types:
- owner's `turns>0 && cost===0` (self-described heuristic) mislabels a genuinely free model as `n/a`;
- designer's `cost === null` is **not representable** — `Usage` has `cost: number` (`runs.ts:21-36`), `zeroUsage` returns `cost: 0`, and `SpendRecord` gains no field (EV-31 spec §2.3). Designer's falsifiable prediction #3 ("a fixture record where cost === null") cannot be constructed.
The honest hook is the renderer's explicit `unavailableCost` input (my round-1 shape), populated only when a provider figure is genuinely absent — **EV-29 owns that**, and until it lands no `n/a` and no legend render. That is a forward hook, not a mislabel.

**D5 — R-5 states replace whole block or one row. Agreement, no dispute.**
All three: whole-block single line (`usage  no usage recorded` / `usage  accounting failed — <reason>`). One cross-seam wrinkle below (D6) means `no usage recorded` currently also covers the *unresolved-boundary* case (EV-30 zero-both) — a limitation, not a measured zero.

**D6 — how `/council-eval` enters the accounting. HOLD: stamping the marker is necessary but not sufficient.**
Fact: `/council-eval` (index.ts:437+) stamps **no marker** today; it calls `runMatrix` directly and **never `sendUserMessage`**. `spendRecord`'s boundary is "first on-chain user message after the marker." For the procedure commands the marker precedes the injected procedure body, so the boundary resolves. For `/council-eval` there is no injected user message, so at the flush moment (hub `onChange` after the last cell settles — `runMatrix` has not even returned yet) `boundary.resolved` is almost certainly **false**, and EV-30's binding rejected-option (4) then zeroes **both halves**. Owner's design (add marker + pending push) would flush a zero record and render `usage  no usage recorded` **despite a multi-hour matrix** — violating the acceptance test ("its numbers equal the fixture accounting record") at exactly the fourth point. My round-1 "render from the post-`runMatrix` tail" has the same defect, because it also calls `spendRecord` with the marker. This is the one point where I think both of us are wrong and the owner's fixture-shaped test would pass while the real exit is false. **Ruling-seat question (facts only):** does `/council-eval` get a boundary by (a) a synthetic in-session user entry after the marker, (b) relaxing EV-30's zero-both invariant to allow subtree-around-an-unresolved-entry-range, or (c) a distinct eval boundary form? I do not decide it.

**D7 — emitted output: notify / console.log / report string. Agreement.**
Points 1-4: `ctx.ui.notify` in TUI, `console.log` headless (the existing flush `notify` fallback). Point 5: the `council_wait` report string (`formatReport`, hub-tools), which is what features-deliver's Phase 3 reads. Never a session turn. **Ruling-seat question (facts only):** whether a `notify`/`console.log` line counts as "the concluding output contains a usage block" for the acceptance test, or whether the block must be inside the procedure's own final text — determinism forbids the latter, so this needs a ruling, not a generator.

## Blind spots, named separately

- **Owner/engine view:** sees the flush site and the renderer; cannot see that a flush notify is conditional on the *write* transition while the card's "concluding output" is the *read* moment, and cannot see that `existing`/`gate-closed` writes must still render.
- **Designer/surface view:** sees one human-scannable line; cannot see that `SpendRecord` has two bases — its single-`usage`-line shape has no representable slot for both, and its `cost===null` trigger contradicts `runs.ts:21-36`. It also cannot see the eval boundary-unresolved zeroing.
- **My own (principal) round-1 view:** I could not see that the runner job *is* null-rooted (`hub.ts:94`); I read the parent-side forest rule and assumed the runner's own job was excluded from it. Corrected above.

## Reframe

The card's title says "entry point," goal and Intent say "exit point" — and the fifth artifact is not an exit of a *procedure* but the *report* of a job. So the real unit is not "the block at five exits" but **"obtain a resolved `SpendRecord` for this invocation at five seams."** State it that way and the disputes become testable: each point's open question is "is there a resolved boundary here, and does its scope name the right range." Four points have a session boundary (marker + injected user message); the fifth is a job whose only parent-visible scope is `sumSubtree(job.id)`, and the fourth (`/council-eval`) **structurally lacks the injected user message the boundary rule requires** — which is why it must be settled before implementation, not rendered as `no usage recorded`.

## Testable claims

- **T-H1 (D1):** feed a fixture `SpendRecord` with `ownSession.cost=1, subtree.cost=2` to the designer's single-line renderer; assert the rendered string exposes **both** bases (or fails). A summed `cost≈$3.0000` with one basis label fails EV-30 ruling (2).
- **T-H2 (D3):** dispatch a container seat through the real hub and assert its manifest has `parentJobId === null` (hub.ts:94) while its children have `parentJobId === <container job id>`; then assert `sumSubtree(manifests, containerJobId)` equals the container + descendants. This is `closed-green` today and kills my round-1 mechanism claim as a test, not prose.
- **T-H3 (D3):** call `spendRecord` with `markerId=null` on a session with manifests present; assert `boundary.resolved===false` **and both halves are zero** (EV-30 zero-both). This is the runnable form of "the runner cannot self-compute."
- **T-H4 (D4):** designer's stated test "fixture record where `cost === null`" — assert it does not compile against `Usage` (`runs.ts:21-36`). The trigger must be an explicit renderer input.
- **T-H5 (D6, the one that matters):** drive the real `/council-eval` handler (or its fixture double, no network) over a seeded matrix, then drain the flush; assert the flushed `SpendRecord.boundary.resolved === true`. If false, the block renders `usage  no usage recorded` while the matrix manifests are non-empty — a false-blank and an acceptance failure at the fourth point. A red test today, on purpose.
- **T-H6 (D2/D7):** after emitting the block at any point, assert `ctx.sessionManager.getEntries().length` and `getLeafId()` are unchanged and `sendUserMessage` was never called with the block.

**Ruling-seat questions named, not decided:** (1) ownSession carryability at the fifth point (requires child-mode marker + a `rootJobId`/signature extension of the settled `spendRecord`) — my round-1 proposal, open; (2) the eval boundary mechanism (synthetic user entry vs. relaxed zero-both vs. distinct form); (3) whether the `basis=` label / bracketed provenance labels are ruled shape or generator discretion; (4) whether a `notify`/`console.log` line satisfies "concluding output"; (5) whether `usage  no usage recorded` may also stand in for an unresolved boundary, given EV-30 zero-both makes them indistinguishable.

#### Position — `designer`

Confirmed the file landed. Yielding now.

---

## Design position (round 2)

I **CONCEDE** on the shared pure renderer, the two R-5 whole-block states, the R-1/R-2/R-4 literals in their ruled slots, the parent-rendered venue for the runner block (`council_wait`'s `formatReport`), and the render surface (`ctx.ui.notify` TUI / `console.log` headless for points 1–4; appended to `council_wait`'s report text for point 5, copied verbatim by `features-deliver.md` Phase 3 ledger). I **HOLD** on three shape disputes — the legend line is conditional on any `n/a` slot (R-5's zero-state carrying no legend is the strongest in-card evidence); the two halves are never combined into one summed number on the rendered block (the README's "Never read them as the same measurement" at `extensions/usage-store.ts:106` and EV-30 ruling item 2 bind this); and the runner's `ownSession` half is a product-owner ruling question, not a generator call.

## Gulf closed

Gulf of **Evaluation** at every autonomous exit, for both the human reading the TUI notify and the orchestrator reading Phase 3's ledger. At the moment of conclusion, a person can tell (a) what was spent, (b) on what basis, (c) over what boundary, and (d) where the numbers cannot be claimed — without opening the durable store file. The structural property "the block cannot include the cost of its own generation" is held by **when** the renderer runs (after `spendRecord(...)` returns, `extensions/usage-store.ts:362-364`; after `formatReport` builds the runner-shaped SpendRecord for point 5), not by what it prints.

## Principle and evidence

**Signifier + feedback + conceptual model + standing-honesty**, applied at:
- `extensions/usage-store.ts:362-364` — the existing `notify` callback that fires on the `written` transition (the exact surface EV-31 §2.7 rules and EV-32 wording-defers). The block is the notify text. TUI handler returns immediately at `extensions/index.ts:286-293`, so the renderer must fire from `agent_settled` (the only post-`runMatrix` engine event for `/council-eval` and the only post-send event for points 1–3 in TUI); that is the existing `flushUsage` wiring at `extensions/index.ts:160-162`.
- `extensions/hub-tools.ts:64-79` (`formatReport`) for point 5 — the runner-shaped report text already carries `r.output` (the runner's `<return_contract>` text captured at `extensions/hub.ts:218`). The block is appended to that string. The orchestrator reads it through `features-deliver.md` Phase 3 ledger.
- `extensions/spend.ts:255-274` — the `recordInvocationBoundary` helper is the only stamping seam; `/council-eval` (`extensions/index.ts:437`) does **not** stamp today, only the scanned-procedure handler at `extensions/index.ts:275-295` does. For point 4 the handler must call `recordInvocationBoundary` + push `PendingInvocation` (small extension; no settled EV-30/31 surface touched).
- `extensions/spend.ts:269-275` (`formatBoundaryLabel`) — R-4's resolved and unresolved forms are byte-stable there. Reused, never recomposed.
- The R-2 conditional legend is grounded in R-5: `usage  no usage recorded` is itself a `usage  …` line that does **not** include the legend. By symmetry, a successful block whose no slot was `n/a` does not emit the legend either.

## Falsifiable predictions

The Skeptic may run each as out-of-band CDP smoke (per `vault/wiki/smoke-test.md`):

- **T1 byte-stability:** `formatUsageBlock(fixture)` called twice and across `structuredClone(fixture)` returns identical bytes. Source-audit grep confirms no `Date.now()`, `Math.random()`, `process.env`, `fs.*`, `console.*` inside the renderer. Fails any renderer that picks up ambient state.
- **T2 R-1 money:** `costBasis:"catalogue-estimate"` → `cost≈$0.1234 (catalogue)` (U+2248 asserted as `"\u2248"`); `costBasis:"reported"` → `cost=$0.1234 (reported)`. A grep for the wrong character fails.
- **T3 R-2:** half with activity but `cost === 0` renders `cost=n/a` in-slot; legend `usage  n/a = provider figure unavailable` byte-exact iff any slot is `n/a`; a record with no `n/a` emits no legend line.
- **T4 R-4:** resolved record renders `formatBoundaryLabel(record)` byte-exact. Unresolved-but-successful renders `boundary=session=<sid> entries=unresolved jobs=0` (the EV-30-ruled unresolved form, `extensions/spend.ts:271`).
- **T5 R-5 zero:** all-zero → exactly `usage  no usage recorded`. Boundary line omitted. Durable record still carries `boundary.resolved:false` (`extensions/usage-store.ts:128-129`).
- **T6 R-5 failure:** flush write failure → `usage  accounting failed — <reason>` (U+2014); reason composed `write failed for <absolute store path>: <error>` (preserves EV-31 T-U14). `recordInvocationBoundary → null` in a scanned-procedure handler → synchronous emission `usage  accounting failed — invocation boundary unresolvable`.
- **T7 R-6 byte-identity:** feeding the same `SpendRecord` to five wrappers (one per exit) yields identical bytes.
- **T8 boundary honesty:** every numeric in the rendered block parse-equals the record; pre-boundary session usage is absent from both halves; `lastEntryId === leafId` at compute time (ruling item 7, EV-30).
- **T9 EV-28 byte-identity:** `formatUsageSegment(ownSession)` and `formatUsageSegment(subtree)` byte-equal to `extensions/hub-tools.ts:51-57`'s output.
- **T10 container-underestimate trap (principal's named trap):** a settled runner-shaped job with ≥1 descendant renders subtree = runner's own manifest usage + descendants'; **not** the runner's own turns alone. The test that distinguishes owner's omission vs principal's JSONL read.

## Ruling-seat questions, named (with facts only)

1. **R-6 line-count:** does "identical in shape" permit a runner block that omits the `ownSession` half (one line shorter)? Owner omits; principal renders via the runner's own session JSONL. The card binds grammar, not line count.
2. **`rootJobId` extension to EV-30's `spendRecord` + seat-side marker stamping** — principal proposes; EV-31 spec §5 names per-node subtree reconciliation as **FLLWUP-31** (deliberately deferred). Whether EV-32 folds FLLWUP-31 forward is out of scope here.
3. **`n/a` legend unconditional vs conditional** — I argue conditional (R-5 zero-state line carries no legend); principal argues unconditional. Card wording supports either reading.
4. **R-5 zero/failure as whole-block vs one-row replacement** — I argue whole-block on both (a zero record has no boundary to name; a failure has no record to read). The principal agrees on whole-block for both.
5. **Wiki gap** — `vault/wiki/index.md` has no usage/accounting page. EV-28/30/31 shipped without one. I do not decide whether EV-32 fills the gap.

## Preferences, ranked last

- Bracket provenance labels `[session-reconciled]` / `[stream-assistant, lower bound]` over `basis=session-reconciled / stream-assistant` — bracket groups the half visually; bracket glyph is the EV-30 record's own vocabulary.
- One `usage …` line per half, not two — readable, not table-y.
- The runner block rides the parent's `council_wait` report (orchestrator-readable), not the runner's return-contract prose. The runner's text is one model output among many; the parent's report is the channel Phase 3 already trusts.

## Standing constraints honored

No `git checkout/switch/reset` against `/home/tista/codes/pi-council` this turn. No long-running foreground processes. All reads were `read`/`grep`/`find`/`ls`. I am a generator seat — no implementation, no ruling. Evidence file at `vault/raw/2026-09-12-designer-ev32-round2.md`.


### Step 3 — bounded exchange, round 3 (FINAL — cap 3 reached)

Each generator re-dispatched with the others' round-2 positions. Jobs
`job-16.7` (owner), `job-16.8` (principal), `job-16.9` (designer). Positions
verbatim. The exchange cap (3) is now reached; nothing further is re-dispatched
to a generator.

#### Position — `owner`

D6 claim verified against the tree before taking a position. Round-3 response:

---

**Approach.** Round 2 left exactly one genuinely load-bearing dispute (principal's D6) and a handful of points already converged. I verified D6 against `extensions/index.ts` and `extensions/spend.ts` directly rather than arguing it. Everything else I either confirm settled or name as a ruling-seat fact.

## 1. D6 — the `/council-eval` boundary claim: **REAL, verified**

- **Handler (`index.ts:437–501`):** the `council-eval` handler calls `runMatrix` and emits only via its local `emit` (notify/console.log). There is no `recordInvocationBoundary` call, no pending-ledger push, and no `sendUserMessage` anywhere in the handler. Confirmed.
- **Boundary resolution (`spend.ts:155–177`):** the boundary is "the first on-chain user message **after** the marker in append order"; if none exists it returns `resolved: false` with **both halves zeroed before any forest walk**. Confirmed.
- Consequence: even with my design's marker stamp + pending push added, there is no on-chain user message after the marker (any persisted `/council-eval …` command text lands *before* the in-handler marker, so it can never serve), so the flushed record zero-both and the block renders `usage  no usage recorded` after a multi-hour matrix. The principal's T-H5 is the right red-today test; I adopt it as the D6 proof.
- **My design's answer at the fourth acceptance point:** no render-site choice fixes this — the defect is upstream of the renderer, in `spendRecord`'s boundary rule, which is EV-30's binding rejected-option-4. The three exits (synthetic on-chain user entry / relax zero-both / distinct eval boundary form) each modify a settled surface, so the mechanism is **not inside EV-32's scope to decide**. What *is* in scope: stamping the marker + pending push (so the invocation exists and flushes once a boundary mechanism exists) and rendering at the awaited post-`runMatrix` tail. **The boundary mechanism itself must be ruled; if it is not ruled, point 4 goes to the consolidator as an open item rather than shipping a false `no usage recorded` — an acceptance-failing blank is worse than no block.**

## 2. The `n/a` trigger (R-2)

Trigger: per measured half, `turns > 0 && cost === 0` renders `cost=n/a` in that half's money slot — the only trigger constructible against `Usage.cost: number` **today** (the designer's `cost === null` fixture cannot compile; the principal's explicit `unavailableCost` hook is unreachable until EV-29). Legend is **conditional**: present iff the block contains any `n/a` slot, absent otherwise — byte-stable either way, with R-5's legend-free zero state as the in-card evidence; the principal conceded conditionality in D4. Accepted tradeoff unchanged: a genuinely free catalogue model mislabels until EV-29; explicit beats implicit per the human's stated preference.

## 3. Settled — one line each

- **Two measurement rows, never a summed half:** settled — designer conceded in round 2; the store README and EV-30 ruling 2 bind it.
- **R-5 states as whole-block single lines:** settled — all three converged (`usage  no usage recorded` / `usage  accounting failed — <reason>`, U+2014).
- **Render channel:** settled — `ctx.ui.notify` / `console.log` for points 1–4, block appended to `council_wait`'s report for point 5, never `sendUserMessage` (T-B12/T-H6 pin it).
- **Point 5 subtree scope = `sumSubtree(manifests, job.id)`, no engine extension:** settled — the principal conceded this in round 2 (D3); his `rootJobId` survives only inside the separate open ownSession question.
- **Shared pure renderer composing `formatUsageSegment` + `formatBoundaryLabel` rather than forking:** settled — dispute-1's summed composition is rejected finally; the designer holds two-row as well.

**Open items for the consolidator (not settled by generators):** (1) the eval boundary mechanism — the gate for acceptance point 4; (2) point 5's `ownSession` (child-mode marker + record extension vs. my omission-minimum); (3) `basis=` label text / row formatting — no ruling names it.

**Tradeoffs accepted.** Point 4 may render nothing rather than a false blank if the boundary ruling doesn't land — an explicit gap beats a plausible-but-false `no usage recorded`. The `n/a` heuristic's free-model mislabel ships pending EV-29. Runner block omits `ownSession` unless the run rules the extensions in.

**Testable claims.** T-H5 (adopted): drive `/council-eval`'s run path over a seeded matrix, drain the flush, assert `boundary.resolved === true` — red today, on purpose. T-B5: `turns>0 && cost===0` half renders `cost=n/a` + conditional legend; a no-`n/a` record renders none. T-B8 (extended): exactly one block per invocation across flush and the eval tail.

I yield — this is my round-3 position.

#### Position — `principal`

**Cross-seam reading** (grounded on both sides):

- `spendRecord` (spend.ts) resolves the boundary as the **first on-chain user message after the marker in append order** (`spend.ts:164-168`); no marker-and-message pair ⇒ `resolved:false`, both halves zero (`spend.ts:172-179`). This is EV-30's binding rejected-option (4), not incidental.
- `/council-eval` (index.ts:437-503) stamps **no marker** and **never calls `sendUserMessage`**; the three scanned procedures do both (`index.ts:275`, `:297`/`:303`). `pi.appendEntry` writes a **custom** entry (`spend.ts:256-261`; index.ts:268-270 "appendCustomEntry"), which the scan at `spend.ts:165` rejects.
- `sumSubtree(manifests, rootId, metric="cost")` **already exists** (`runs.ts:188-198`), inclusive of root, parentJobId walk — point 5's scope needs no engine extension; it is metric-by-metric (`UsageMetric`), so a full split is 12 calls or an EV-32-local aggregation, not a change to a ruled module.
- `Usage.cost: number` (`runs.ts:21`), `costBasis` has only `"catalogue-estimate" | "reported"` (`runs.ts:18`), `accumulateWire` coerces `|| 0` — **no in-band "provider figure unavailable" signal exists**.
- `formatUsageSegment` (`hub-tools.ts:51`) and `formatBoundaryLabel` (`spend.ts:268`) are byte-exact and reusable.

**1. D6 — sharpest falsifiable form, and the fourth acceptance point**

With a marker stamped at the top of `/council-eval`'s run path and no `sendUserMessage`, at *every* engine-observable moment (post-`runMatrix` tail, flush `onChange`/`agent_settled`, session shutdown) `spendRecord(...)` returns `boundary.resolved === false` and both halves zero, so the block renders `usage  no usage recorded` although the matrix dispatched jobs with non-empty manifests. Runnable now, red by construction.

Therefore: **no EV-32-scoped change makes point 4 report the matrix's spend.** The fourth acceptance point's first bullet *is* satisfiable today — but only **vacuously**: the fixture is the zero-both record, string equality holds, and the real exit prints a false blank. So yes: the text "its numbers equal the fixture accounting record" **can be met at the fourth point without answering the boundary question**, and the test cannot distinguish designed-correct from false-blank.

The options are all rulings, facts only, none mine to decide: (a) a synthetic in-session boundary entry — no append API creates a `message`-type user entry, so this needs either a turn dispatch (violating "cannot include the cost of its own generation") or a change to the scan semantics; (b) a distinct eval boundary form — changes `spendRecord`'s resolution, an EV-30-settled rule; (c) relaxing zero-both — EV-30 settled, not re-openable by a generator. Additional fact for the same ruling, not a separate one: because EV-30 zero-both makes an unresolved boundary indistinguishable from a **measured** zero, R-5's `usage  no usage recorded` cannot represent the unresolved case without being false at the surface.

**2. `n/a` trigger (R-2)**

My design uses an **explicit per-half unavailability input** to the renderer (equivalently a `costAvailable:false` signal from the accounting layer) — **not** a value heuristic. Code grounds it: cost is always a `number` coerced `|| 0`, and `costBasis` has no "unavailable" member, so a genuine reported-figure absence is not in-band; `turns>0 && cost===0` would relabel a truly free catalogue model, which today renders the **true** `cost≈$0.0000 (catalogue)`, as unavailable. **Not reachable from real data today — only after EV-29 supplies the signal.** It *is* testable today at the pure-renderer seam with a fixture-supplied signal, so R-2's literal is exercisable without a mislabeling heuristic. This overrides my round-2 heuristic hold; the principal's type objection is correct. **Legend is conditional** — present iff at least one slot rendered `n/a`; no legend in either R-5 state.

**3. Settled confirmations (one line each)**

- Two measurement rows, never a summed half — **CONFIRM**.
- R-5's two states as whole-block single lines — **CONFIRM**.
- Render channel: `ctx.ui.notify` (TUI) / `console.log` (headless) for points 1–4; appended to `council_wait`'s `formatReport` text for point 5; **never `sendUserMessage`** — **CONFIRM**.
- Point 5 subtree scope as `sumSubtree(manifests, job.id)` with no engine extension — **CONFIRM** (exists at `runs.ts:188`; inclusive, parentJobId walk; single-metric, so the split is 12 calls or an EV-32-local aggregation).
- Shared pure renderer reusing `formatUsageSegment` and `formatBoundaryLabel`, not forking them — **CONFIRM**.

**Blind spots**

- **owner/engine view:** sees the eval handler lacks a marker, but not that adding one still cannot resolve a boundary (nothing appends a user message after it) — the fixture-shaped test hides this.
- **accounting (spend.ts) view:** sees `zero-both` as the safe unresolved return, but cannot see it is rendered as `no usage recorded`, turning a safe return into a false statement at the surface.
- **designer/surface view:** wants R-2's literal to fire, but cannot see no in-band signal exists, so a heuristic or unconditional legend prints false unavailability for free models.
- **principal view:** named the eval gap, but the "stamping is necessary but not sufficient" conclusion additionally requires the fact that no append API creates a user-message entry.

**Reframe** — the round-2 reframe ("obtain a resolved `SpendRecord` at five seams") is sound and I adopt it; **no new reframe needed.** The only correction: point 4 must go to the consolidator as a **ruling-seat item**, not an implementation choice for `owner`.

**Testable claims**

- **T-R3-1 (red today):** drive the real `/council-eval` handler with a `deps`-injected `runMatrix` double (no network), seed manifests, drain `flushUsage`; assert `boundary.resolved === true` and `subtree.totalTokens > 0`. Fails by construction.
- **T-R3-2 (vacuous acceptance):** fixture = the zero-both record → block is exactly `usage  no usage recorded`; string equality passes. Proves the current acceptance cannot separate designed-correct from false-blank.
- **T-R3-3 (n/a unreachable in-band):** `Usage{turns>0, cost=0, costBasis:"catalogue-estimate"}` renders `cost≈$0.0000 (catalogue)` with no legend; the lone `n/a` path is the explicit renderer input.
- **T-R3-4 (scope, green now):** fixture runner + children ⇒ `sumSubtree(manifests, job.id)` equals root + descendants, inclusive.
- **T-R3-5:** after rendering at points 1–4, session entry count and leaf id are unchanged and `sendUserMessage` was never called with the block.

I have nothing further; this is my round-3 position and I yield.

#### Position — `designer`

I've read the full card. Round3 — short, decisive, no Phase-1 reopenings.

<output_format>

- **Design position**

1. **`/council-eval` boundary (principal's D6)** — conceded as a ruling-seat question: with `recordInvocationBoundary` stamped and no injected user message, the ruled zero-both invariant would render `usage  no usage recorded` over a real matrix, falsifying the acceptance test. The block's shape is not the defect; the boundary resolution is. The human-facing consequence at point 4 either way is *honest*: a `no usage recorded` line is a true statement about the boundary the renderer was given, but it would be the wrong statement about the work. The fix is not a copy change — it is one of (a) a synthetic in-session user entry after the marker, (b) relaxing the zero-both invariant, or (c) a distinct eval boundary form. I name it; I do not decide it.

2. **`n/a` trigger and legend** — corrected: `Usage.cost` is `number`, so neither `cost === null` nor `cost === 0` alone is honest. The corrected in-slot trigger is an *explicit* renderer input (the `unavailableCost: Half[]` argument from my round-1), not a value heuristic — populated only when a provider figure is genuinely absent (EV-29). With no input today, no slot is `n/a` and no legend renders. The legend is **conditional on the slot, not on the record's numerics**; "conditional" is the design call (byte-stable either way, R-6 holds), and the legend's exact wording `n/a = provider figure unavailable` is the R-2 copy ruling. Neither side is a copy question first.

3. **Confirmations (one line each)**:
   - Two measurement rows and never a summed half — **confirmed** (R-30 basis ruling + `usage-store.ts` README forbid any combined figure).
   - R-5's two states as whole-block single lines — **confirmed**.
   - Render channel: `ctx.ui.notify` / `console.log` for points 1–4; appended to `council_wait`'s report for point 5; never `sendUserMessage` — **confirmed** (T-B12).
   - Fifth point's subtree scope as `sumSubtree(manifests, job.id)` with no engine extension — **confirmed** (ownSession is the ruling-seat question; the subtree half needs no `rootJobId` parameter).
   - Shared pure renderer reusing `formatUsageSegment` and `formatBoundaryLabel` rather than forking them — **confirmed**.

- **Gulf closed** — Evaluation at five autonomous exits; structural timing holds the "cannot include cost of own generation" clause by *when* the renderer runs (after `spendRecord` returns; after `formatReport` builds point 5's record), not by what it prints.

- **Principle and evidence** — Signifier + feedback + conceptual model, anchored at `extensions/spend.ts:269-275` (`formatBoundaryLabel`), `extensions/hub-tools.ts:51-57` (`formatUsageSegment`), `extensions/usage-store.ts:362-364` (flush notify, `written` transition), `extensions/hub-tools.ts:64-79` (`formatReport`), `extensions/index.ts:275-295` and `:437+` (the two stamping paths). Standing-honesty constraint: zero/ownSession half is never fabricated when the data does not support it — omission is the card-satisfying minimum; extension is open.

- **Falsifiable predictions** — T-B1 (no summed half on the block — fails designer's round-1 composition); T-H5 (D6 trap: a red test today — `/council-eval` flushed record has `boundary.resolved:false` over a non-empty matrix, rendering the false-blank); T-B11/T-H2 (parent's `sumSubtree` over a real runner-shaped manifest tree equals runner + descendants); T-B12 (no observable session delta after emission; `sendUserMessage` never called with the block); T-B13 (pre-boundary session usage absent from the block).

- **Preferences, ranked last** — Bracket provenance labels `[session-reconciled]` / `[stream-assistant, lower bound]` over a `basis=` field — bracket is the record's own vocabulary. One `usage  …` line per half, not a table. Runner block rides the parent's `council_wait` report, not the runner's prose.

</output_format>

Yielding. Three items left as ruling-seat questions with facts only: (a) point 5 ownSession carryability — child-mode marker + `rootJobId` extension, or omit; (b) the `/council-eval` boundary mechanism (principal's three options); (c) legend conditionality as copy ruling (R-2 binds text, not frequency).


### Step 4 — Skeptic attacks and runs tests

`skeptic` dispatched with every position recorded so far (job `job-16.10`).
It filed falsifiable objections, ran each named test, and reports the real
output. Objections and results appended verbatim.

#### Position — `skeptic`

- **Objections**
  1. **D6 — eval boundary resolves false with marker but no user message** (targets owner R3 "marker+push suffices" / acceptance point 4). Settling test `bun /tmp/ev32-skeptic/probe-d6.ts` (marker `m1` stamped, no user message after it, one non-empty manifest): **closed-red** — `resolved:false`, both halves zero, label `boundary=session=ses_abc entries=unresolved jobs=0`, `both-halves-zero:true` with `manifest-nonempty:true`. The claim is confirmed as a defect: a stamped marker alone renders a false blank over a real matrix. Companion surface check (`sed -n 437,503p extensions/index.ts | grep -c sendUserMessage|recordInvocationBoundary|pendingInvocations` → `0`; the only `sendUserMessage` sites in `index.ts` are `:297`/`:303` in the scanned-procedure handler): the handler today stamps nothing and injects nothing — **closed-green** for the "necessary" half, which is exactly why "sufficient" fails.
  2. **Vacuous acceptance at point 4** (targets "its numbers equal the fixture accounting record" for `/council-eval`). Settling test is D6's output itself: the zero-both record string-equals a `no usage recorded` fixture while manifests are non-empty — **closed-red**. The acceptance text as written cannot distinguish designed-correct from false-blank. I state this as lost-by-the-card, not by a seat: principal R3 and owner R3 both concede it.
  3. **`cost === null` trigger unconstructible** (targets designer R1 prediction #3). Settling test `bunx tsc -p tsconfig-check2.json` on a `Usage` literal with `cost: null`: **closed-red** (for the claim — the claim does not hold): `error TS2322: Type 'null' is not assignable to type 'number'`. Direct construction fails; only `as unknown as number` casts compile. Gate-integrity note: this same run proves the type gate *can* fail — tsc went red on the injected defect and green on `probe-null.ts` (cast version) and the repo's own strict config.
  4. **`turns>0 && cost===0` heuristic reachable but renders truthful catalogue today** (targets owner R3 §2 "only trigger testable now"). Settling test `bun /tmp/ev32-skeptic/probe-na2.ts`: **closed-green** on the facts, **closed-red** on the inference — a `turns=2,cost=0` half renders `turns=2 tokens=in 10/out 5/… cost≈$0.0000 (catalogue)` with `has-n/a:false`. The input pattern exists in real data (free/zero-cost turns), but no `n/a` path fires from it today; adopting the heuristic would relabel a true `$0.0000 (catalogue)` as unavailable. Principal R3's "not reachable in-band until EV-29" holds.
  5. **`sumSubtree` scoping needs no engine extension** (targets principal R3 §"CONFIRM", owner R3 settled list). Settling test `bun /tmp/ev32-skeptic/probe-subtree.ts` (runner + 2 children fixtures): **closed-green** — `sumSubtree(runner,input)=600`, `turns=6`, `sumSubtree(c1)=200` (inclusive, parentJobId walk), runner manifest `parentJobId===null` so it is a forest root. Principal's R2 mechanism retraction is verified, not just conceded.
  6. **Runner cannot self-compute; null marker zeroes both halves despite manifests** (targets principal R3 T-H3 / owner D3). Same probe: `spendRecord` with `markerId:null` and 3 manifests → `resolved:false, ownTurns:0, subTurns:0` — **closed-green**.
  7. **Unresolved-boundary vs measured-zero indistinguishable at the halves** (targets R-5 whole-block states + principal's ruling question 5). Settling test `bun /tmp/ev32-skeptic/probe-zero.ts`: **closed-red** (for distinguishability) — resolved-measured-zero vs unresolved halves are `deep-equal:true`. Only `boundary.resolved` differs, which the R-5 zero-state line does not print. Any fixture test asserting the zero line therefore passes vacuously for the unresolved case.
  8. **No combined half-sum exists in the tree; designer R1 composition rejected and absent** (targets two-halves-never-summed). `grep -n "ownSession + …" extensions/*.ts` → no match (`grep-exit=1`); no `Usage + Usage` operator exists since `costBasis`/`usageSource` are single-valued — **closed-green**. The round-1 designer single-line composition is correctly dead.
  9. **No `formatUsageBlock`, no R-5 literals in the tree — every five-point numeric-equality test is vacuous today** (targets acceptance bullet 1 + "byte-stable" process claim). Probe: `has-usage-block-module:false`, `formatUsageBlock-defined:false`, `no-usage-recorded-in-tree:false`, `accounting-failed-in-tree:false` — **closed-green** for the observation, which makes the consequence **open-untested**: points 1–3 (no renderer), point 4 (no boundary — see O1), point 5 (no `council_wait` append; `formatReport` at hub-tools.ts:64–79 carries no block). There is no deterministic renderer to be byte-stable, no emission to count as "one per invocation," and no session-delta to measure — T-B2/T-B8/T-B12/T-H6 have no subject. They are not passing; they are un-runnable.
  10. **"Never `sendUserMessage`" for the block** — **open-untested**: `sendUserMessage` appears 4× in engine source, all in the scanned-procedure path, none emitting a block (no block exists). The constraint is design sound (an injected turn would land in the boundary's own-session range) but has no implementation to violate or honor it yet.

- **What I ran** (all under `/tmp/ev32-skeptic/`, repo untouched — `git status` clean except the designer's own pre-existing untracked evidence file; still on `main`, no worktree/branch):
  - `bun /tmp/ev32-skeptic/probe-d6.ts` → `{"resolved":false,…both halves zero}`, `label: boundary=session=ses_abc entries=unresolved jobs=0`, `both-halves-zero: true`, `manifest-nonempty: true`
  - `bun /tmp/ev32-skeptic/probe-subtree.ts` → `600 / 6 / 200` as expected; `runner-parent: null-rooted`; `null-marker: {"resolved":false,"ownTurns":0,"subTurns":0}`
  - `bun /tmp/ev32-skeptic/probe-na2.ts` → `renders: turns=2 … cost≈$0.0000 (catalogue)`; `has-n/a: false`; `has-usage-block-module: false`; `formatUsageBlock-defined: false`; `sendUserMessage-count: 4`; both R-5 literals absent from `extensions/`
  - `bunx tsc -p tsconfig-check2.json` (strict, `cost: null`) → `error TS2322: Type 'null' is not assignable to type 'number'` (gate-fails correctly); cast variant compiles clean
  - `bun /tmp/ev32-skeptic/probe-zero.ts` → `measured-zero: {"r":true,"t":0}` vs `unresolved: {"r":false,"t":0}`; `halves-deep-equal: true`
  - `grep -n "ownSession + …" extensions/*.ts` → no match; `sed -n '437,503p' extensions/index.ts | grep -c …` → `0`

- **Verdict** — `blocks`. Specific items: (a) acceptance point 4 is unsatisfiable without a ruled boundary mechanism — O1/O2 show the shipped design renders a false `no usage recorded` over a non-empty matrix and the acceptance text passes vacuously; owner R3 and principal R3 both route this to the consolidator as an open ruling item, so the card must not proceed as if point 4 were designed. (b) R-2's literal has no reachable in-band trigger — neither `cost===null` (unrepresentable, O3) nor the `turns>0&&cost===0` heuristic (mislabelling, O4) — so the `n/a` acceptance state is untestable against real data until EV-29; the renderer hook must be explicit-input or the acceptance bullet struck. (c) The unresolved-vs-measured zero conflation (O7) plus the nonexistent renderer (O9) make every per-point "numbers equal the record" fixture test vacuous as the design stands — at minimum points 4 and 5, and structurally all five until `formatUsageBlock` lands with a resolved-record precondition.


### Step 5 — synthesis

`consolidator` dispatched with the full deliberation record (job `job-16.11`).
Its synthesis, verbatim, below.

#### Position — `consolidator`

# EV-32 — Consolidator synthesis (step 5)

I read `council/cards/EV-32.md` in full (goal, Intent, Acceptance, R-1/R-2/R-4/R-5/R-6, all three rounds of owner/principal/designer verbatim, and the Skeptic's step-4 objections O1–O10 with their actually-run test output). Sorting below. I treat Phase 1 rulings R-1/R-2/R-4/R-5/R-6 as settled artifacts and do not reopen them; where a seat proposes changing one of their shapes, that goes to open judgment.

A note on method: where my only basis is that two or three independently-dispatched seats converged, I say so explicitly — that is a hypothesis, not a closed dispute. Only a Skeptic test that actually ran closes a dispute.

---

## 1. Settled

### Settled by Phase 1 ruling (binding, not reopened)
- **R-1 money signifier**, **R-2 legend/marker text**, **R-4 boundary label bytes**, **R-5 zero/failure literals**, **R-6 "identical in shape" (the literal)** — settled by the card's Phase 1 rulings. (Caveat: R-6's *interpretation* — grammar identity vs line-count identity — is open; see open judgment §2 item 1.)

### Settled by a Skeptic test that ran
- **`cost === null` is not a viable `n/a` trigger** — Skeptic O3, `bunx tsc -p tsconfig-check2.json` on a `Usage` literal with `cost: null` → `error TS2322: Type 'null' is not assignable to type 'number'`; only `as unknown as number` casts compile. The designer's round-1 prediction #3 trigger is dead. (Designer R3 §2 concedes; moves to explicit renderer input.)
- **Point 5's subtree scope needs no engine extension; the runner job is null-rooted** — Skeptic O5, `bun /tmp/ev32-skeptic/probe-subtree.ts` → `sumSubtree(runner,input)=600`, `turns=6`, `sumSubtree(c1)=200` (inclusive, parentJobId walk), runner manifest `parentJobId===null`. Principal's round-1 mechanism claim ("runner's own job is not null-rooted") is retracted and verified dead; owner's `sumSubtree(manifests, job.id)` scoping is correct without a `rootJobId` parameter.
- **The runner cannot self-compute; a null marker zeroes both halves despite manifests** — Skeptic O6 (same probe), `spendRecord` with `markerId:null` and 3 manifests → `resolved:false, ownTurns:0, subTurns:0`. Parent-side rendering at `council_wait` is structurally required; the runner never stamps (seats return into `runChildMode` before registration).
- **No combined half-sum exists in the tree; the two halves are never one number** — Skeptic O8, `grep -n "ownSession + …" extensions/*.ts` → no match (`grep-exit=1`); `costBasis`/`usageSource` are single-valued so a `Usage + Usage` operator cannot exist. The designer's round-1 single-line composition (`formatUsageSegment(record.ownSession + record.subtree)`) is dead; two measurement rows is the only structure the record supports. (Also bound by `usage-store.ts` README "Never read them as the same measurement" and EV-30 ruling item 2.)

### Agreed design (converged across all three generators — *convergence only, no test closed these*)
- One shared pure renderer composing `formatUsageSegment` + `formatBoundaryLabel`, not forking them. (The "never fork" part is reinforced by O8; the shared-single-renderer part is convergence only.)
- R-5's two states as whole-block single-line replacements: `usage  no usage recorded` / `usage  accounting failed — <reason>` (U+2014). Convergence only — no Skeptic test ran on this.
- Render channel: `ctx.ui.notify` (TUI) / `console.log` (headless) for points 1–4; appended to `council_wait`'s `formatReport` text for point 5; **never `sendUserMessage`**. The channel choice is convergence only; the `never sendUserMessage` constraint itself is open-untested (O10).
- `/council-eval` needs a marker stamp + pending-ledger push at the top of its run path (it stamps nothing today — Skeptic O1 surface check, `grep -c sendUserMessage|recordInvocationBoundary|pendingInvocations` over `index.ts:437–503` → `0`). Necessary-half closed-green; sufficient-half is the open objection O1.

---

## 2. Open judgment — for `product-owner`, escalating to `steward`

Each item: the question, both sides, and the facts a ruling seat needs. No recommendation.

1. **R-6 "identical in shape" — grammar identity or line-count identity?** May the runner's block omit the `ownSession` row (one line shorter than the session-exit shape)?
   - *Owner + designer:* grammar identity — same prefix, labels, ordering, ruled states; the runner block omits `ownSession` rather than fabricate a zero measurement the parent never performed.
   - *Principal:* render the `ownSession` half from the runner's own session JSONL via `findSessionFile` — but this requires the extensions in item 2; absent those, principal does not press.
   - *Facts:* the extensions in item 2 are required for any non-fabricated `ownSession` at point 5; without them, omission is the only honest option. The card binds "identical in shape," not a line count.

2. **Point 5 `ownSession` carryability — fold FLLWUP-31 forward or omit?** Extending EV-30's `spendRecord` (a `rootJobId`/signature parameter) plus stamping a `council-invocation` marker in child mode for `spawns`-declaring seats.
   - *Principal (proposes):* child-mode marker on `session_start` for container seats + `rootJobId` on `spendRecord`; reads the runner's own session JSONL; `ownSession` half = `session-reconciled`.
   - *Owner + designer:* omit as the card-satisfying minimum; a fabricated zero is never acceptable; the extension is a scope extension of two settled artifacts.
   - *Facts:* EV-31 spec §5 names per-node subtree reconciliation as **FLLWUP-31**, deliberately deferred. Principal himself names this a scope extension of a ruled module. Seats never stamp today (`index.ts` returns into `runChildMode` before registration).

3. **The `/council-eval` boundary mechanism (D6) — how does point 4 obtain a resolved `SpendRecord`?** All three seats route this to a ruling; each option modifies a settled surface.
   - (a) synthetic in-session user entry after the marker — no append API creates a `message`-type user entry; needs a turn dispatch (violates "cannot include cost of own generation") or a change to scan semantics;
   - (b) relax EV-30's zero-both invariant to allow subtree-around-an-unresolved-entry-range — EV-30 settled, not re-openable by a generator;
   - (c) a distinct eval boundary form — changes `spendRecord`'s resolution, an EV-30-settled rule.
   - *Facts (Skeptic O1/O2):* with a marker stamped and no `sendUserMessage`, at every engine-observable moment `spendRecord` returns `boundary.resolved === false` and both halves zero over a non-empty matrix. This blocks acceptance point 4's first bullet non-vacuously. **Escalates to `steward`** because every option touches EV-30's binding rejected-option-(4) / zero-both invariant.

4. **The `n/a` trigger — heuristic or explicit renderer input?** (R-2's literal has no reachable in-band trigger today.)
   - *Owner:* `turns > 0 && cost === 0` heuristic, accepting that a genuinely free catalogue model mislabels as `n/a` until EV-29 ("explicit-and-slightly-wrong beats implicit," per the human's stated preference).
   - *Principal + designer:* an explicit per-half `unavailableCost`/`costAvailable:false` renderer input; no `n/a` and no legend render until EV-29 supplies the signal.
   - *Facts (Skeptic O4):* a `turns=2, cost=0` half today renders the *truthful* `cost≈$0.0000 (catalogue)` with `has-n/a:false` — the heuristic would relabel a true zero-cost catalogue figure as unavailable. Neither `cost===null` (O3, unrepresentable) nor the heuristic is honest against real data; only an explicit input is.
   - *Nested question:* is R-2's acceptance bullet ("the block is present and its numbers equal the fixture accounting record" for an `n/a` case) testable against real data before EV-29, or struck/deferred?

5. **Legend conditionality as copy ruling (R-2).** All three converged on *conditional* (present iff any slot rendered `n/a`; absent in both R-5 states) — principal conceded conditionality in round 2 D4.
   - *Open as a ruling because:* owner and designer both name it a copy-policy ruling, not a determinism question (R-2 binds the legend text, not its frequency; R-6 binds determinism, and a conditional legend is byte-stable per record). The ruling seat should confirm conditional vs unconditional as the copy decision.

6. **`basis=` label text, bracket-vs-field, and row/column formatting.** No Phase 1 ruling names these.
   - *Principal:* `basis=<record.<half>.usageSource>` read from data (a half can never be mislabelled), padded half label to a fixed column.
   - *Owner (round 2):* concedes basis-from-data; padding and the `basis=` field name remain ruling questions.
   - *Designer (preference, ranked last):* bracket provenance labels `[session-reconciled]` / `[stream-assistant, lower bound]` over a `basis=` field — bracket is the record's own vocabulary.
   - *Facts:* the only ruled constraints are R-4's boundary line bytes and the `usage  ` (two-space) prefix implied by R-5's two literals.

7. **Render moment for points 1–3 — render-at-`written`-transition, or render regardless of write outcome?**
   - *Owner:* render at the flush's `written` transition (replacing the existing success notify); `existing` is silent (choose-once); a gate-closed write is an unconcluded invocation, not a suppressed render.
   - *Principal:* reuse the flush call site *provided* emission is **not** conditional on `written` — a pre-existing (`existing`) record must still render, and rendering must not depend on the write succeeding; proposes a `rendered` set separate from the flush's `remaining`.
   - *Facts:* no test ran on this; it is a gating-mechanism values call (does a gate-closed write suppress the block or not).

8. **Does a `notify`/`console.log` line satisfy "the concluding output contains a usage block"?** (principal round-2 ruling question 4.) Determinism forbids embedding the block in the procedure's own final LLM text, so the acceptance text's "concluding output" must be ruled to include an out-of-band notify/console line — or the acceptance text is amended. No test can settle this; it is an acceptance-text interpretation.

9. **May `usage  no usage recorded` stand in for an unresolved boundary?** Given EV-30 zero-both makes an unresolved boundary indistinguishable from a measured zero at the halves (Skeptic O7, `halves-deep-equal:true`; only `boundary.resolved` differs, which R-5's zero line does not print).
   - *Principal:* names this as ruling question 5; a `no usage recorded` line is a true statement about the boundary given, but the wrong statement about the work.
   - *Owner:* the unresolved case renders the zero state, with `boundary.resolved:false` retained on the durable record for forensics.
   - *Facts (Skeptic O7):* any fixture test asserting the zero line passes vacuously for the unresolved case. **Escalates to `steward`** (touches R-5 + EV-30 zero-both).

10. **Vacuous acceptance at point 4 (O2).** The acceptance text "its numbers equal the fixture accounting record" *can* be met at point 4 today — but only vacuously (fixture = zero-both record, string equality holds while manifests are non-empty). The acceptance text as written cannot separate designed-correct from false-blank. Principal R3 and owner R3 both concede this is lost-by-the-card. A ruling seat must amend the acceptance text or accept the vacuity.

---

## 3. Open objections — Skeptic objections whose settling test has not passed

- **O1 — `closed-red`.** A stamped marker alone does **not** suffice at `/council-eval`: with `recordInvocationBoundary` stamped and no `sendUserMessage`, `spendRecord` returns `resolved:false`, both halves zero, over a non-empty matrix (`bun /tmp/ev32-skeptic/probe-d6.ts` → `both-halves-zero:true`, `manifest-nonempty:true`, label `boundary=session=ses_abc entries=unresolved jobs=0`). The "necessary" half is closed-green (handler stamps nothing today); the "sufficient" half is closed-red. Term: the block renders a false `usage  no usage recorded` at point 4. Not fixed — the boundary mechanism (open judgment §2 item 3) has not been ruled.

- **O2 — `closed-red`.** Vacuous acceptance at point 4: the zero-both record string-equals a `no usage recorded` fixture while manifests are non-empty. The acceptance text cannot distinguish designed-correct from false-blank. Term: acceptance point 4's first bullet is satisfiable vacuously. Not fixed — open judgment §2 item 10.

- **O4 — `closed-red` on the inference.** The `turns>0 && cost===0` heuristic is reachable in real data but renders the *truthful* `cost≈$0.0000 (catalogue)`, not `n/a`, today (`bun /tmp/ev32-skeptic/probe-na2.ts` → `has-n-a:false`). Adopting the heuristic would relabel a true zero-cost catalogue figure as unavailable. Term: the owner's proposed `n/a` trigger mislabels. Not resolved — owner accepts the mislabel as a tradeoff; principal + designer reject (open judgment §2 item 4).

- **O7 — `closed-red` for distinguishability.** Unresolved-boundary vs measured-zero halves are `deep-equal:true` (`bun /tmp/ev32-skeptic/probe-zero.ts`); only `boundary.resolved` differs, which the R-5 zero-state line does not print. Term: any fixture test asserting the zero line passes vacuously for the unresolved case. Not fixed — open judgment §2 item 9.

- **O9 — `open-untested`.** No `formatUsageBlock`, no R-5 literals exist in the tree (`has-usage-block-module:false`, `formatUsageBlock-defined:false`, `no-usage-recorded-in-tree:false`, `accounting-failed-in-tree:false`). Every five-point numeric-equality, byte-stability (T-B2), one-emission-per-invocation (T-B8), and session-delta (T-B12/T-H6) test is vacuous/un-runnable — at minimum points 4 and 5, structurally all five until `formatUsageBlock` lands with a resolved-record precondition. Term: there is no deterministic renderer to be byte-stable and no emission to count. Not fixed — implementation has not been written (card is in Deliberating).

- **O10 — `open-untested`.** The "never `sendUserMessage` for the block" constraint: `sendUserMessage` appears 4× in engine source, all in the scanned-procedure path, none emitting a block (no block exists). The constraint is design-sound (an injected turn would land inside the boundary's own-session range and be counted by the next reconciliation, falsifying "cannot include cost of own generation") but has no implementation to violate or honor. Term: untestable until the renderer exists. Not fixed.

---

## Ready to hand off?

**No.** It routes to `product-owner`, **escalating to `steward`** for items that touch EV-30's settled zero-both / rejected-option-(4).

Blocks, specifically:
- **Open judgment §2 item 3 (the `/council-eval` boundary mechanism)** — this is the gate for acceptance point 4 and directly blocks open objection O1/O2. Every option modifies a Phase-1-settled surface (EV-30 zero-both or `spendRecord`'s resolution rule), so it cannot be decided by a generator and escalates to `steward`.
- **Open judgment §2 item 9 (may `no usage recorded` stand for an unresolved boundary)** — also touches R-5 + EV-30 zero-both; escalates to `steward`; blocks open objection O7.
- **Open judgment §2 items 1, 2, 4, 5, 6, 7, 8, 10** — `product-owner` rulings (R-6 interpretation, ownSession carryability/FLLWUP-31 fold, n/a trigger, legend conditionality, label/format copy, render-moment gating, "concluding output" interpretation, acceptance-text amendment for O2).
- **Open objections O1, O2, O4, O7, O9, O10** remain open; O9/O10 close only when `formatUsageBlock` lands, and O1/O2/O7 close only when the §2 item-3 / item-9 rulings land.

Nothing here was decided by me; the disputes the deliberation left open are still open.


### Step 6 — route what does not close

The consolidator's open-judgment and open-objection items were taken in turn
against the Phase 1 rulings already on record.

**A Phase 1 ruling was applied (not re-asked).** Open-judgment item 2's
"fold FLLWUP-31 forward" half is squarely answered by **R-SCOPE** —
FLLWUP-31 stays `Backlog`. EV-32 does not fold per-node subtree
reconciliation (a child-mode marker plus a `spendRecord` signature
extension) forward. The residual question in that item — whether omitting
the fifth point's `ownSession` row satisfies **R-6**'s "identical in shape"
— is *not* answered by R-SCOPE and is carried below.

**Unanswered — carried to the ruling seat.** Open-judgment items 1, 3, 4,
5, 6, 7, 8, 9, 10 and the Skeptic's `closed-red` objections O1, O2, O4, O7
are unresolved by any Phase 1 ruling, and items 3 and 9 touch EV-30's
settled zero-both rule (a ruling seat, escalating to `steward`, not a
generator). Per `<escalation_contract>` the facilitator does not dispatch
`product-owner`/`steward`; the packet is returned to the orchestrator as an
`ESCALATION`, facts only, no recommendation.

**Also carried:** the EV-31 step-6 forward item — the success/failure notify
literal wording for a usage-store write. EV-31 owns the call site; EV-32
owns the wording. The deliberation did not settle whether the block
*replaces* the flush success notify (`usage-store.ts`'s own comment assigns
that notify's wording to EV-32) or sits alongside it, nor the failure-line
literal beyond R-5's `usage  accounting failed — <reason>`.

The card remains `Deliberating` — steps 7 onward do not start while
acceptance point 4 is unsatisfiable within the card's scope and the Skeptic
blocks.

### Step 6 — ruling (`steward`, step-6; binding)

Appended **verbatim** from the orchestrator's resumption dispatch. This is the
step-6 `steward` ruling on the two consolidator items that touch EV-30's
settled zero-both surface (open-judgment items 3 and 9) plus the conditional
acceptance amendment (item 10). Binding on every seat and every later step of
this run.

**Item A — `/council-eval` boundary mechanism: rule for (c), a distinct eval boundary form.** Realize it as an explicit, opt-in anchor on EV-30's `spendRecord`, without relaxing the zero-both invariant:
- `spendRecord` gains one optional parameter, defaulting to today's behavior byte-for-byte: `spendRecord(opts & { boundaryMode?: "user-message" | "marker" })`, default `"user-message"`.
- `boundaryMode: "user-message"` (existing, default): boundary = first on-chain user message after `markerId` in append order; unresolvable ⇒ zero both halves. **Unchanged.**
- `boundaryMode: "marker"` (new; for invocations whose handler awaits and never injects a user message): the anchor **is** `markerId`, required present and on the active chain. Then `boundary.resolved = true`, `boundary.firstEntryId = markerId`, `boundary.lastEntryId = leafId` (EV-30 ruling item 7, unchanged), and the forest gate uses the **marker's own recorded `at`/timestamp** — never an invented one. If the marker is absent or off-chain, `"marker"` returns the identical unresolved zero-both record as today.
- `/council-eval` (`extensions/index.ts:437–503`) calls `recordInvocationBoundary(pi, ctx, "council-eval", runId)` and pushes its pending invocation at the top of the run path, after arg validation and after the no-arg listing early-return; it still renders at the awaited post-`runMatrix` tail.
- The pass-through point is EV-31's pending ledger (`pendingInvocations`, `usage-store.ts`): it gains `boundaryMode`, set `"marker"` at the eval push and `"user-message"` at the scanned-procedure push (`index.ts:275–303`); `flushPendingInvocations` forwards it to `spendRecord`. **`SpendRecord`'s shape is not touched** — the discriminator lives on the store record (self-describing, schema-versioned), not on EV-30's returned record.
- `formatBoundaryLabel` keeps R-4's shape; the explicit case renders the resolved form with `firstEntryId = markerId`. No new label token.

This is an **extension, not a relaxation**: it opens a second, timestamp-bearing resolution path and leaves the zero-both path (marker absent/off-chain) exactly as EV-30 ruled it. EV-30 step-6 ruling item 4 and its rejected-option-4 stand. O1's default-mode probe stays `closed-red` — correctly, because the default must remain fail-closed — while the eval path resolves under the opt-in.

**Item B — may `usage  no usage recorded` stand for an unresolved boundary? No.** R-5's literal set becomes three whole-block single-line states:
1. `usage  no usage recorded` — `boundary.resolved === true` **and** both halves measured all-zero.
2. `usage  accounting failed — <reason>` — accounting failure; unchanged (U+2014).
3. `usage  accounting boundary unresolved` — `boundary.resolved === false` (marker absent/off-chain). New; `usage␣␣` + one line, no suffix.

Precedence: **failed > unresolved > empty** — a failure has no record to read, an unresolved boundary has no measured zero, and a measured zero is only claimable once the boundary resolved. The durable record keeps `boundary.resolved:false` and EV-30's `entries=unresolved` label for forensics. This changes neither R-4's label shape nor R-5's two bound literals.

**Item I — point-4 acceptance amendment (conditional on A(c) + B).**
- Point 4's first bullet, for a **non-empty** matrix fixture, requires `boundary.resolved === true`, `boundary.firstEntryId !== null`, `boundary.jobCount >= 1`, and `subtree.totalTokens > 0` — joined to, not replaced by, the existing "numbers equal the fixture accounting record" equality.
- The fixture set gains an unresolved-boundary fixture that must render state 3, distinct from the measured-zero fixture rendering state 1.
- Point 5 is unaffected by A (it scopes by `sumSubtree(job.id)`, not by a session boundary) and its zero state remains state 1.

**Steward options rejected** — A(a) synthetic user entry (no `pi.appendEntry` path creates a `message`-type user entry; violates own-generation clause; re-parented by compaction); A(b) relax zero-both (re-opens EV-30 rejected-option-4); A-alt broaden default scan to accept a `custom` boundary (weakens R-4 for all callers); A-alt rely on per-cell `cellScope.usage` (no resolved SpendRecord, answers no bullet); B-alt unresolved renders as `no usage recorded` (O7: asserts an unmeasured zero); B-alt reuse `accounting failed — boundary unresolved` (conflates a non-throwing designed outcome with an error); B-alt reword the two bound R-5 literals (overturns a Phase-1 human ruling); I-alt leave acceptance unamended (ships a false-blank fixture class).

**Steward grounding** — vault has no usage/accounting page; nearest `eval-store-contract.md`, `model-eval-harness.md` (`/council-eval` shares one `COUNCIL_RUN_ID`; grader is a sibling), `procedures-vs-commands.md` (why `/council-eval` has no injected user-message boundary), `run-transcripts.md`. `council/cards/EV-30.md` step-6 items 3/4/7; `council/cards/EV-32.md` Intent + R-5 + O1/O2/O7. Cheapest-to-reverse applied where grounding ran out: additive default-off `boundaryMode`; a third literal.

**Steward reversibility** — A(c) `boundaryMode` defaults to `"user-message"` so every existing call site/test is byte-preserving; reversal is deleting the mode branch + eval opt-in. B is one literal plus its test; the store already carries `boundaryResolved`. I is card text. **No part of A/B/I requires the human**; no recorded human decision is overturned (R-4's label shape preserved, R-5's two bound literals unchanged, EV-30's zero-both invariant preserved).

### Step 6 — ruling (`product-owner`, step-6; binding for the remaining items)

Appended **verbatim** from the orchestrator's resumption dispatch. This is the
step-6 `product-owner` ruling on the remaining open-judgment items 1, 4, 5, 6,
7, 8, 10 and the EV-31 notify-wording item.

**C. R-6 line-count + point-5 ownSession.** R-6 reads "identical in shape" as **grammar identity**, not line-count identity: same `usage  ` prefix, same labelled halves, same boundary line position, same legend conditionality, same R-5 whole-block states — but a runner block may omit the `ownSession` row because fabricating a zero `[session-reconciled]` row would assert a measurement the parent never performed. The acceptance test for point 5 asserts the runner-shaped block has **no `ownSession` line and one `subtree` line**, with the `subtree` line's numbers equal to `sumSubtree(manifests, job.id)` over every numeric metric. R-SCOPE declines folding FLLWUP-31 forward; the omission is the card-satisfying minimum. A real `ownSession` half ships via FLLWUP-31, not an EV-32 amendment.

**D. R-2 `n/a` trigger and reachability — the literal is struck from the acceptance test until EV-29 lands.** The trigger is the renderer's **explicit per-half input** (`unavailableCost: Half[]`, populated only when a provider figure is genuinely absent), not a value heuristic (O3 `cost: null` fails tsc; O4 `turns>0 && cost===0` relabels a truthful `cost≈$0.0000 (catalogue)` as unavailable). Until EV-29 supplies the in-band signal, no slot renders `n/a`, no legend renders; the renderer takes the input today and is testable with a fixture-supplied signal at the pure-renderer seam. EV-29 owns the honest in-band signal; EV-32's forward item is the renderer hook + conditional legend.

**E. Legend conditionality — conditional.** Present iff any slot rendered `n/a`; absent in both R-5 whole-block states and in any block with no `n/a`. R-2 binds the legend text, not its frequency.

**F. `basis=` label / bracket-vs-field / row formatting.** Adopt `basis=<record.<half>.usageSource>` read from data, half label padded to a fixed column (`ownSession`/`subtree`, two spaces around `basis=`). Bracket annotations (`[session-reconciled]` / `[stream-assistant, lower bound]`) rejected for the normal block; the lower-bound descriptor moves to the durable record/README, not the block's byte budget. All `usage  ` lines ≤160 columns (EV-28 T5).

**G. Render moment for points 1–3 — render at the flush's `written` transition**, replacing the existing success notify at `extensions/usage-store.ts:362-364`. Principal's `rendered` set rejected: an `existing` outcome already rendered at a prior settle (choose-once satisfied); a `gate-closed` outcome is not a "concluding output"; a `failed` outcome renders the R-5 failure state with the absolute store path. One seam, one emission, byte-equal to disk.

**H. Does a `notify`/`console.log` line satisfy "the concluding output contains a usage block"? Yes.** Determinism forbids embedding the block in the procedure's own LLM prose; the engine's deterministic sinks are `ctx.ui.notify` (TUI) and `console.log` (headless), dual-routed by the existing flush. Point 5's `council_wait` `formatReport` text is a different deterministic channel read by `features-deliver.md` Phase 3's ledger.

**J. EV-31's deferred usage-store write notify.** The block **replaces** the flush success notify (`usage-store.ts:362-364`); failure composes the reason as **`write failed for <absolute target path>: <error>`** inside R-5's wording, preserving EV-31's T-U14 absolute-path property. Sink stays `ctx.ui.notify` / `console.log`, never `sendUserMessage`.

**Product-owner options rejected** — C: a zero-fabricated `ownSession` `[session-reconciled]` row (asserts an unperformed measurement); principal's `rootJobId` parameter (subtree half needs no engine extension). D: the `turns>0 && cost===0` heuristic (O4); the `cost === null` trigger (O3). E: unconditional legend (symmetric plausible-but-false). F: bracket provenance labels (re-state a typed field in prose). G: principal's `rendered` set (solves a non-problem). H: embedding the block in LLM text (falsifies determinism). J: retaining the old notify alongside the block (wording ownership transferred to EV-32; redundant identifier).

**Product-owner grounding** — vault has no usage/accounting page; R-1/R-2/R-4/R-5/R-6/R-SCOPE binding; EV-30 items 3/4/5/7 binding; Skeptic probes `probe-d6.ts`/`probe-zero.ts`/`probe-subtree.ts`/`probe-na2.ts`/`probe-null.ts`.

**Product-owner effect on this card (the work list):**
1. Build `extensions/usage-block.ts` with `formatUsageBlock({ record: SpendRecord; unavailableCost?: Half[] } | { failed: string }): string`, composing `formatUsageSegment` + `formatBoundaryLabel` (extracted helpers, EV-28 byte-identity test).
2. Wire the block as the flush success notify at `usage-store.ts:362-364` (replacing the existing wording), and the R-5 failure state with `write failed for <absolute target path>: <error>`.
3. Add the marker-stamp + pending-push at the top of `/council-eval`'s run path (skip the no-arg listing form), now under steward A(c)'s `boundaryMode: "marker"`.
4. Extend `council_wait`'s `formatReport` to append the runner-shaped block for settled jobs (`sumSubtree(manifests, job.id)` over every numeric metric; **no `ownSession` line**).
5. One sentence in `council/procedures/features-deliver.md` Phase 3 to carry each card's runner block verbatim into the ledger.
6. Add the unconditional `recordInvocationBoundary → null` failure emission in the scanned handler (`usage  accounting failed — invocation boundary unresolvable`).
7. Add the `deps`-injected `runMatrix` double for network-free testing.
Points 4 and 5 of the acceptance render under steward A(c)+B+I; all are now unblocked.

### Step 6 — rulings as applied (facilitator)

Step 6 is fully routed; the deliberation is not reopened. Each open item is
closed by the ruling below, applied — not re-asked.

- **Open-judgment 3 / O1 / O2 (the `/council-eval` boundary mechanism).** Closed by **steward A(c)** — `boundaryMode: "marker"`. Rests on R-4 (label shape preserved: the resolved form renders `firstEntryId = markerId`, no new token) and preserves EV-30's zero-both invariant for the default mode. Points 4's non-vacuity is then bound by **steward I**.
- **Open-judgment 9 / O7 (unresolved vs measured zero).** Closed by **steward B** — a third whole-block literal `usage  accounting boundary unresolved`. Rests on R-5 (the ruled zero literal now means a *measured* zero only; the two bound literals are unchanged) and keeps EV-30's `entries=unresolved` on the durable record.
- **Open-judgment 10 (vacuous point-4 acceptance).** Closed by **steward I** — the non-empty-matrix assertions joined to the equality. Rests on the card's acceptance bullet 1 ("numbers equal the fixture accounting record") and R-5.
- **Open-judgment 1 / R-6 line count.** Closed by **product-owner C** — grammar identity, not line-count identity; the runner block omits `ownSession`. Rests on **R-6**.
- **Open-judgment 2 (fold FLLWUP-31 forward).** Already answered by **Phase 1 R-SCOPE** (FLLWUP-31 stays `Backlog`); product-owner C confirms the omission is the card-satisfying minimum. Not re-asked.
- **Open-judgment 4 / O4 (the `n/a` trigger).** Closed by **product-owner D** — the explicit per-half `unavailableCost` input; the literal is struck from the acceptance test until EV-29. Rests on **R-2** (marker + legend text unchanged) and O3/O4.
- **Open-judgment 5 (legend conditionality).** Closed by **product-owner E** — conditional. Rests on **R-2** (text, not frequency) and **R-6** (per-record determinism).
- **Open-judgment 6 (`basis=` / bracket / formatting).** Closed by **product-owner F** — `basis=<record.<half>.usageSource>` read from data, fixed-column half label. Rests on **R-1** (money literals unchanged) and **R-4** (boundary line bytes unchanged).
- **Open-judgment 7 (render moment, points 1–3).** Closed by **product-owner G** — render at the flush's `written` transition. Rests on **R-5** (a `failed` outcome renders the failure state) and **R-6** (one deterministic block).
- **Open-judgment 8 (`notify` counts as concluding output).** Closed by **product-owner H** — yes; dual-routed deterministic sinks. Rests on the card's Intent (deterministic handler, not prose) and **R-6**.
- **EV-31 notify-wording forward item.** Closed by **product-owner J** — the block replaces the flush success notify; failure composes `write failed for <absolute target path>: <error>`. Rests on **R-5** and EV-31's T-U14 absolute-path property.

With A/B/I and C–J on record, the card moves `Deliberating` → `In Progress`
at step 7's close (council.md step 7), and steps 7–12 proceed.
