---
id: EV-36
title: Transcript legibility at the one-row progress floor
state: In Review
owner: null
epic: EPIC-8
goal: At every inline progress viewport from one row to a full panel the transcript's rendered line count never exceeds the granted viewport and is never empty, and the one-row case identifies the active block rather than a mid-body continuation line, proven by render assertions over the pinned termRows 7, 8, 9 and 40 layout vectors
---

## Intent

The user-visible surface is the inline progress transcript at the smallest
viewports, including the one-row case granted at termRows 7. Today the
view's follow mode scrolls to the last line of the whole flattened output,
so the single visible row is the tail of a wrapped body with no block
identity. This card makes the floor legible.

The one-row line reuses EV-34's composed tool-call head for the focused
block, with the failure token as a trailing suffix, rather than inventing a
parallel one-row rendering. The granted viewport is computed at the factory
and must not be exceeded; the count half is retained as a regression guard
alongside the content half.

Depends on EV-34 (composed head and failure token) and EV-35 (focus).

The one-row line identifies the **active block**: under follow mode it tracks
the live tail (the operator watching a running seat sees the latest unit's
composed head); under navigation it tracks the focused unit (the marker and
`e` coincide). At a fresh follow-on view with more than one visible unit the
marker is absent — the line is the live tail, not the cursor. Pressing any
navigation key flips follow off and the marker reappears on the cursor's
unit. The card ships this as the comprehension rule and acknowledges it as
the comprehension cost.

## Acceptance

- Over the pinned layout vectors for termRows 7, 8, 9 and 40, the rendered
  line count is at least one and never exceeds the granted viewport.
- At one row, the visible line identifies the active block — its composed
  head, not a wrapped body continuation.
- EV-7/EV-8/EV-9 suites stay green; render output adds no literal color.

## Phase 1 ruling (features-deliver, binding for this run)

**R-COPY (EPIC-8)** — the one-row line reuses EV-34's composed head
(`→ <Tool>  <primary-arg>`) for the focused block, with the `muted "✗"`
failure suffix when applicable; no parallel one-row surface. This is a
recorded human decision, immutable for the run; a runner that hits this
dispute applies this ruling and cites it rather than re-asking.

## Run record (features-deliver / EV-36)

### Step 1 — classification (facilitator)

- **Path: full council.** The card is spec-ambiguous and design-judgment
  inside the EPIC-8 seam. R-COPY pins the one-row line's *shape* (EV-34's
  composed head plus the `muted "✗"` suffix) but not *which* block the line
  shows: the goal says "the one-row case identifies the active block" and
  the Intent says `follow` "scrolls to the last line of the whole flattened
  output" — so the card must decide whether the one-row subject is the
  end-of-transcript block, the EV-35 focused block (`focused`, clamped in
  `render()`), or the block the user last expanded, and how that choice
  interacts with `follow`. R-COPY does not answer it; R-KEYMAP/R-MARKER do
  not either. Second, the goal's count half ("never exceeds the granted
  viewport and is never empty") does not say which layer owns the clamp —
  `computeProgressLayout` in `extensions/focus-nav.ts:103` (which already
  guarantees `progressLines >= 1` for termRows 7: `avail = 7 - 5 = 2`,
  `sepLines = 0`, `treeLines = 1`, `progressLines = 1`) or
  `TranscriptView.render` at `extensions/navigator.ts:813-817`, where the
  `all.slice(topLine, topLine + viewportRows)` window is exactly what
  produces the mid-body tail — nor which renderer
  (`CouncilTreeWidget.renderProgress`'s `slice(0, layout.progressLines)` at
  `:432` versus the view's own window) is the asserted subject. Two layers
  admitting two different clamps is the ambiguity class council.md step 1
  names. Design disputes are open-judgment by construction (council.md
  step 6).
- **Surface-touching: yes.** The delivered artifact is the inline progress
  transcript's rendered lines at the smallest viewports — every clause of
  the goal and Acceptance is about what a person sees on that surface, and
  the `TREE_ROW_MARKER` signifier plus the `muted "✗"` suffix are
  user-visible. Per council.md step 1 a surface-touching full-council card
  seats `designer` as a third generator in steps 2–3.
- **Rulings applied:** **R-ORDER** (Phase 1) — predecessor EV-35 is merged
  to local `main` as `85db7a6` (`git merge-base --is-ancestor 85db7a6 main`
  → ancestor; PR #49), and EV-36 is the last card in the pinned order, so
  it may start. **R-COPY** (Phase 1) — the one-row line reuses EV-34's
  composed head with the failure suffix; applied below, not re-asked.
  **R-MARKER** (Phase 1) — the focus signifier stays
  `TREE_ROW_MARKER` (U+258C); no new glyph. **R-MODAL** (Phase 1) — the
  full-screen modal transcript is out of scope; EV-36's subject is the
  inline progress expansion (`CouncilTreeWidget.renderProgress` →
  `ensureView` → `TranscriptView`, `extensions/navigator.ts`). **R-GATES**
  (Phase 1) — owner gates are `bash council/preflight.sh`,
  `bunx tsc --noEmit`, `bun test`, `python3 council/validate.py`, network
  integration test gated behind `COUNCIL_INTEGRATION=1`. **R-ESCALATE**
  (Phase 1) — judgment routes to `product-owner`; this runner dispatches
  no ruling seat.
- **Known context, not folded in:** FLLWUP-37 (keep the progress keymap
  header visible when follow-mode content overflows the viewport) is
  `Backlog`, filed by `steward` under R-FOLLOWUP after EV-35's Q2 ruling
  deliberately did *not* pin the header as row 0. EV-36's Acceptance is the
  one-row case, not the header-pinning case; if a generator's design for the
  one-row line genuinely requires the overflow behavior, that is a fold-in
  question routed to `product-owner` via `ESCALATION`, never absorbed here.
- **Seat resolution (2026 `<seat_resolution_check>`):** `owner`,
  `principal`, `designer`, `skeptic`, `consolidator`, and `judge` — every
  seat this card's full-council path dispatches — are present in the
  packaged seat set (`council/agents/*.md`; no repo-local override
  directory exists in this checkout, so nothing shadows them). Phase 0
  resolved the same names for the epic. No `Unknown seat` error.
- **Environment:** step 0 preflight is skipped per the autonomous-run
  substitution; Phase 0 cleared the environment for the epic. Main checkout
  is clean at `19ab0f5`, equal to `origin/main`
  (`git rev-list --left-right --count origin/main...main` → `0 0`); `gh`
  authenticated as `tistaharahap` with `repo` scope. Gates and merges will
  run against the main repository's files only via a dedicated
  `git worktree`; no `checkout`/`switch`/`reset` against this checkout.

### Step 2 — independent first pass (round 1, generators only)

Each seat received the card alone — no other seat's position, no hint.
Positions appended verbatim, labeled by seat.

- `job-16.1` (owner) — settled 3.7m / 9 turns; tokens in 66226 / out 12552 / cR 230720 / total 309498, cost≈$0.0231 (catalogue).
- `job-16.2` (principal) — settled 3.7m / 17 turns; tokens in 217893 / out 26078 / cR 785408 / total 1029379, cost≈$0.0507 (catalogue).
- `job-16.3` (designer) — **discarded, empty position**: settled 1.5m / 10 turns with `stopReason=stop` and a single preamble line ("Let me quickly confirm the EV-34/35 commits…"), no position content. Applied council.md's "stop one bad agent before scaling" guard: re-dispatched the identical input once (`job-16.4`, the dispatch-discipline single re-dispatch), which settled 1.5m / 7 turns, tokens in 92886 / out 18102 / cR 218246 / total 329234, cost≈$0.0627 (catalogue). No third dispatch.
- The designer's full position was written by the seat to
  `vault/raw/2026-09-15-design-ev36-round1.md` (its own convention —
  `vault/raw/*-design-*` is a tracked raw-source layer in this repo). It
  exceeded the facilitator's "do not write files" instruction but did not
  touch application code, `council/`, or branch state.

#### Position — `owner` (job-16.1)

**Wiki grounding:** `vault/wiki/council-job-tree-inline.md` (EV-9 binding rulings, tiny-regime floor at 7 rows, R-COPY surface), `vault/wiki/run-transcripts.md` (substrate, token-only drawing), `vault/wiki/index.md`. Code read in full: `extensions/focus-nav.ts`, `extensions/navigator.ts`, plus the EV-34/EV-35/EV-9 test suites.

---

## Approach

**What today's code actually does (verified, file:line):**

1. **The granted viewport is `computeProgressLayout(...).progressLines`** (`focus-nav.ts:106-131`), and the card's pinned vectors already exist as green assertions: `(7,5)→(tree 1, sep 0, progress 1)`, `(8,5)→(1,1,1)`, `(9,11)→(2,1,1)`, `(40,11)→(11,1,23)` (`test/ev9-progress.test.ts:128-137`). So "termRows 7/8/9" all grant **one** progress row.
2. **The count half is already true — but by two uncoordinated clamps.** `TranscriptView.render` slices to its constructor `viewportRows` (`navigator.ts:817`), and `renderProgress` slices again to the current grant (`navigator.ts:432`). The hazard: `ensureView` **ignores the grant for an existing view** (`navigator.ts:438` — `if (this.viewFor?.sessionId === sid) return this.viewFor.view`), so once a job lands mid-progress, tree rows grow, the grant shrinks, and the view's constructor `viewportRows` (set at `navigator.ts:445`) is stale-large. The stale tail-window's *first* `grant` lines survive the outer slice — so after tree growth, the one-row line is a mid-body line picked from the wrong window. This makes grant re-sync load-bearing, not cosmetic.
3. **The content half fails exactly as the card says.** In follow mode `topLine = all.length − viewportRows` (`navigator.ts:815`); at grant 1 the single visible line is the last flattened line — for a trailing user/assistant unit the body is *always* rendered (`navigator.ts:745-751`), and `bodyLines` wraps long text (`navigator.ts:703-708`), so the floor line is a wrapped continuation with no block identity.
4. **EV-34's composed head is directly reusable:** `unitLines` returns the head `→ <label>  <arg>` (warning) with the muted ` ✗` failure suffix as element 0 (`navigator.ts:708-724`), marker prepended *before* truncation so narrow widths can't eat the `▌`. EV-35's focus clamp already lives in `render` (`navigator.ts:796-797`) — render already reconciles `focused` as a side effect.

**The design I would write up:**

- **Which block, and `follow`:** the one-row line is *always the focused unit's composed head* (element 0 of `unitLines(u, i, width, isFocused=true)`), satisfying R-COPY verbatim with no parallel one-row copy. To make "focused" honest in follow mode, `render` pins `this.focused = vis.length − 1` **only when the effective viewport is 1 and follow is on** — follow means "watching the live tail", so at a one-row floor the newest unit *is* the active block. Precedent for render-side focused reconciliation is the existing clamp at `navigator.ts:796-797`. Non-follow shows the user's chosen unit (up/down/g/G all set `follow=false`, `navigator.ts:757-777`). Empty transcript at grant 1 returns the existing dim placeholder (`(waiting for output · idle)` / `(no transcript)`), never `[]`, never the header.
- **Which layer owns the clamp:** `computeProgressLayout` stays the sole pure grantor; `renderProgress` becomes the *grant applier* — it propagates the current grant into the live view each render (a `setViewportRows(n)` mutator; the view has no line cache, so this is one field), fixing the stale-viewport hazard from tree growth. `view.render(width).slice(0, layout.progressLines)` at `navigator.ts:432` is **retained** as the count regression guard. The modal path (`openTranscript`, viewport `termRows−4 ≥ 6`) never reaches 1 and is untouched.
- **Floor-path width safety:** the one-row line is `truncateToWidth`'d (mirroring the toolCall head's own truncation at `navigator.ts:723-724`), because single-unit heads are deliberately untruncated today (`navigator.ts:722`).

## Tradeoffs accepted

- **Follow state is invisible at the floor** — the header carrying the `f follow(on)` suffix is `all[0]` and a one-row viewport can never show it. Accepted: the visible block snapping on arrow keys is the feedback; fixing that would need floor-specific chrome, which is a new surface.
- **Grant 2 (termRows 9 with one job) keeps the tail-window defect** — two wrapped continuation lines, no head identity. The acceptance pins only one row; extending unit-anchoring to viewport ≥ 2 would churn the pinned `(12,8)→(3,1,3)`, `(24,8)→(8,1,10)`, `(40,11)→(11,1,23)` vectors and EV-7/8/9 suites for no acceptance gain.
- **Focused-pin in follow mode is scoped to viewport 1**, so at taller viewports the `▌` can still scroll out of sight in follow mode — a pre-existing EV-35 asymmetry I deliberately do not "fix" here.
- **What I would NOT do:** no parallel one-row renderer or new copy (R-COPY); no unit-anchored window at viewport ≥ 2; no EV-35 key/marker/keymap changes; no literal ANSI/hex (AGENTS.md 9.6 — the line comes from `unitLines`, tokens only); no `hub.ts`, `transcript.ts`, modal-path, or layout-constant changes; no lowering of `PROGRESS_*` floors to make a count assert easier.
- **Residual risks:** `termRowsCap` staleness on mid-session terminal resize is pre-existing EV-9 capture-once semantics, out of scope; tree-row count is monotone within a run (manifests are only pruned at `session_start`), which is why the stale-viewport hazard is a shrink-only, growth-triggered bug.

## Testable claims

All runnable against today's tree; the first three fail before the fix, pass after.

1. **Floor content, pinned vectors:** over the pinned vectors `(7,5)`, `(8,5)`, `(9,11)`, `(40,11)` with a fixture transcript whose last unit is a multi-line assistant body: rendered lines length `≥ 1` and `≤ layout.progressLines`, and at `progressLines === 1` the single line contains the composed-head tokens (`→ bash  <arg>`, `✗` when the paired `isError` result exists) and **not** the body's continuation string. Today this fails — `navigator.ts:815` returns the wrapped body tail.
2. **Tree-growth re-sync (the load-bearing claim):** build the widget at `termRows 24` so the initial grant is 3, open progress, render, then append a manifest (tree rows 1→2+, grant drops to 1, e.g. at `termRows 8` semantics), re-render: the single line is the focused head. Today this fails even though a from-scratch grant-1 view would pass, proving the `ensureView` staleness at `navigator.ts:438` must be fixed by the grant re-sync, not by the outer slice.
3. **Follow pin:** at grant 1 with follow on, append a new unit to the JSONL, poll, re-render — the line moves to the new unit's head and carries exactly one `▌` (R-MARKER parity, one test pattern from `test/ev35-transcript-interaction.test.ts:140+`).
4. **Non-regression (already green, retained):** the count half at all four vectors, the empty-transcript placeholder line at grant 1, and EV-7/EV-8/EV-9/ev34/ev35 suites — `bun test` per AGENTS.md; no literal color added (the line reuses `unitLines`' theme tokens exclusively).

#### Position — `principal` (job-16.2)

## Cross-seam reading

**What the contract actually is today (four owners, not one):**

| Layer | Owns | Evidence |
|---|---|---|
| `focus-nav.computeProgressLayout(termRows, treeContentLines)` | the *split*: `avail = termRows-5`, then `treeLines/sepLines/progressLines`, with `progressLines >= 1` | `extensions/focus-nav.ts:104-120`; pinned pairs at `test/ev9-progress.test.ts:127-137` |
| `CouncilTreeWidget` | the *capture* (`termRowsCap` from `tui.terminal.rows`, once, in the factory) and the *final slice* | `extensions/navigator.ts:561-570`, `:388`, `:428-433` |
| `TranscriptView` | the *window* (`viewportRows` constructor-only; `follow` → `all.slice(maxTop, maxTop+vp)`) | `navigator.ts:628`, `:799-817` |
| `TreeFocusState` + editor routing | the *focus subject* (`focused` in the view; `surface` in the controller) | `navigator.ts:618-620`, `:766-779`, `:796-798`; `focus-nav.ts:188-199` |

Three facts the card's framing does not have:

1. **The grant is recomputed every render but never re-granted to the cached view.** `renderProgress` computes `layout` fresh (`:428`) and passes `layout.progressLines` to `ensureView` (`:431`), but `ensureView` returns the cached view when the sessionId matches and *ignores the argument* (`:437-440`). `view.viewportRows` is frozen at first construction (`:445`, field at `:628`). The widget's `slice(0, layout.progressLines)` (`:432`) masks this for the **count** half and does nothing for the **window**.
2. **The grant changes without any resize**, via EV-9's own O5 path. `computeProgressLayout(9, 1) = {avail 4, tree 1, sep 1, progress 2}` (`focus-nav.ts:105-110`); a second job lands mid-progress → `refresh()` (`navigator.ts:448-455`) → `computeProgressLayout(9, 2) = {tree 2, sep 1, progress 1}`. The grant drops 2 → 1 on a **cached** view whose `viewportRows` is still 2, so the single visible line is `all.slice(maxTop, maxTop+2)[0]` (`:814-817`) — a mid-body continuation. This is exactly the card's red state, reachable on a termRows **9** vector, and invisible to a test that pins `(termRows, treeContentLines)` once at open.
3. **"The granted viewport" is two quantities.** `avail` is the widget's budget; `progressLines` is the transcript's grant. The card asserts "the transcript's rendered line count" against "the granted viewport" — the spec must name both and assert both (today both hold; D8 already recorded the count half as a tautology retained as a guard).

**Subject disambiguation.** `focused` starts at 0 and is only mutated in `handleInput` (`:766-779`) — `poll()`/`render()` never advance it. So with `follow === true` (the default, `:620`) the *focused* unit is unit 0 while the *active* unit is `vis.length-1`. The card says "active block" in the goal and "the focused block" in the Intent/R-COPY; D9 ruled the mechanism on the focused block, and the card's own step-1 record names this as the open question. It is the load-bearing ambiguity: taken verbatim, "focused" makes the one-row floor show the **oldest** unit's head.

## Blind spots, named separately

- **Data/import side (`transcript.ts`, EV-33).** Cannot see that `visible()` filters thinking (`navigator.ts:679-684`), so "the active block" changes under the `t` toggle; cannot see that `firstArgOf` is untruncated here while the tree row truncates to 30 (`navigator.ts:238`) — the same event renders two different lengths two rows apart.
- **Render path (`TranscriptView`).** From inside the view the grant *is* a constructor constant; it cannot see that its caller re-slices, nor that its caller's grant moved. This is precisely how the frozen-grant divergence hides — the view's `maxTop` is consistent with its own stale `viewportRows`.
- **Layout (`focus-nav`).** `computeProgressLayout` is pure in `(termRows, treeContentLines)`; it guarantees `progressLines >= 1` about the *budget* and is silent about the *window* that consumes it. A caller can honour the budget and still show a body tail.
- **Editor/routing (`focus-nav` kernel).** Sees `surface` and consumes `up/down/e/t/f/g/G` without knowing whether they change a rendered line; at a 1-row grant, `routeEditorFocus` reports `up/down` as meaningful today only because follow-off shows `all[0]` (the header) at `focused: 0` (`:813-816`).
- **Widget factory.** Sees `tui` once, so from inside it the terminal never resizes; the modal path in the same file re-reads `tui.terminal.rows` per render (`:827`). Two surfaces, two capture policies, one file.
- **The acceptance itself.** A per-vector test built at fixed `(termRows, treeContentLines)` with one render will pass while the frozen-grant path (§2 above) stays red, because the count half is satisfied either way.

## Reframe

**Two nouns, not the mechanism.** The design is sound; I agree with R-COPY and D9 — reuse the EV-34 composed head (`unitLines`' toolCall head, `navigator.ts:699-708`, `✗` at `:706`), no parallel one-row renderer, token-only. Do not relitigate it. The reframe is:

1. **Replace "the granted viewport is computed at the factory" with "the grant is a per-render parameter with one owner and two consumers."** The widget computes `layout` per render and passes `layout.progressLines` into the view's *render* (e.g. `render(width, viewportRows = this.viewportRows)` or a `setViewportRows` call in `ensureView`), so the window and the one-row projection key on the **current** grant. Keep `slice(0, layout.progressLines)` (`:432`) untouched as the redundant count guard. Without this, the projection's `vp === 1` trigger is wrong on the termRows-9 transition and on any resize.
2. **Replace "the focused block" with "the active unit" = `follow ? vis[vis.length-1] : vis[fv]`**, read `focused`-only, never written back (EV-35 Q5 forbids cursor-stability work). Then the one-row line tracks what the user is watching under follow and what `e`/`t` act on when follow is off, and the marker stays honest: `isFocused` remains the same `vi === fv` expression (`:709`), so a fresh follow-on view at one row shows no `▌`, and `G`/`down` make the identity line navigable at the floor.
3. **The projection belongs in `TranscriptView.render`, not in `renderProgress`** — one renderer, T11 parity by construction (`test/ev9-progress.test.ts:309-320`), and the `vis.length === 0` case falls through to the existing empty-state line (`:797-798`).
4. **"Pinned layout vectors" are `(termRows, treeContentLines)` pairs, asserted against `computeProgressLayout`, never against literals.** D7 already proved a bare termRows number is false: `(40,11) → 23` vs `(40,1) → 31` (`vault/raw/2026-09-15-po-epic8-ruling.md:113-126`). Reuse the existing pair convention at `test/ev9-progress.test.ts:127-137`.

**Scope boundary to record explicitly:** EV-36 does **not** pin the header; at a 1-row grant the header cannot be the single line. FLLWUP-37 stays `Backlog` (its retirement condition — "retires for free if EV-36 consumes the overflow case" — must be a deliberate human/steward call, not an incidental consequence of this card).

## Testable claims

- **T1 (count, green today — keep as guard).** For every pinned pair with `termRows >= 7`: `w.render(w).length <= max(1, termRows-5)`, and `lines.slice(layout.treeLines, layout.treeLines+layout.sepLines+layout.progressLines)` length `<= layout.progressLines`, where `layout = computeProgressLayout(termRows, jobCount)`. Evidence: `focus-nav.ts:104-120`, `navigator.ts:388,397,428-433`. Assert against the computed layout, never a literal.
- **T2 (content, red today).** Fixture ends in a multi-line body; grant 1. Today `render(80).length === 1` passes but the line is `all[maxTop]` (`:814-817`) with no head. After: the single line contains the active unit's composed head — for a failed paired call, `→ <Tool>  <arg> ✗` (`:699-708`). This is D8's settling test, already written down (`po-epic8-ruling.md:130-138`).
- **T3 (subject discriminator).** Three-unit fixture, follow on, `focused` untouched: the grant-1 line is unit 3's head. If the implementation shows `vis[fv]` instead, the same assertion yields unit 1's head — the two readings are distinguishable by one `expect`.
- **T4 (frozen grant, the vector the card's list misses).** Open progress at `termRows 9` with 1 job (`progressLines 2`), then add a manifest and call `w.refresh()` (the O5 path, `:448-455`), then render. Today the single transcript line is `all.slice(maxTop, maxTop+2)[0]` — a mid-body continuation, and the count half cannot see it. After: the active head. Discriminates "projection keyed on the current grant" from "keyed on the view's frozen `viewportRows`".
- **T5 (re-grant observable).** After T4's refresh, `w.activeTranscriptView`'s viewport equals `computeProgressLayout(9,2).progressLines === 1` (getter `:453-455`, field `:628`); and after raising the grant (9 → 40 with unchanged rows, 1 → 31) the view emits 31 lines, not 1. Today: red (stuck at the construction value; `ensureView` returns early at `:440`).
- **T6 (no parallel surface, falsifiable).** For each of `user`, `assistant`, `thinking`, unpaired `toolResult`, paired `toolCall`: the grant-1 line is byte-equal to the head line the multi-row renderer emits for the same unit at a large grant (locate it by its expected literal, as `test/ev34-tool-unit.test.ts:47-60` does). Forces the spec to define head semantics for the four non-toolCall kinds, which R-COPY does not.
- **T7 (marker semantics at one row).** Fresh view, follow on, grant 1 → 0 occurrences of `▌`; after `G` → exactly 1, on the last unit's head. Today both halves are red (follow-off window lands at `focusLine-2`, `:813-816`).
- **T8 (never empty).** Empty fixture at grant 1 → exactly 1 line containing `(waiting for output · idle)`; missing file → `(no transcript)`. Green today (`:797-798` + `maxTop=1`); keep so the new branch cannot return `[]` for `vis.length === 0`.
- **T9 (token-only + width).** `theme-compliance.test.ts:122-137` stays green (no `\x1b`, no `#hex` in `extensions/*.ts`), and `visibleWidth(line) <= width` for every returned line at `width 40` with a long MCP-style tool label. Note: the toolCall head is clamped (`:708`), the `single` head is **not** (`:723` `const out = [marker + head]`) — implementing the projection by reuse makes that clamp mandatory for `toolResult`/long-label singles. Per `docs/tui.md:25,330` an over-wide line is a component-contract violation whose terminal wrap consumes rows the budget assumes are free — the count half cannot detect it. Truncating at the single-head site (one clamp) rather than inside the projection (two clamps) preserves the single-source rule; FLLWUP-38 owns the header line, not this one.
- **T10 (parity, holding-only).** Inline transcript lines are byte-identical to a fresh standalone `TranscriptView` given the same JSONL, width and grant — including at grant 1. Honest note: this does not discriminate a widget-side from a view-side projection; it constrains the spec so T11 (`po-epic8-ruling.md` §3.1 item 6, `po-epic8-ruling.md:2.7`) keeps its meaning.
- **T11 (no regression on the shipped floor tests).** `test/ev9-progress.test.ts:176-190` (`term=7`, asserts `lines[0]` contains `owner`, `length <= 2`) and `:127-137` stay green; the projection mutates only the transcript slice, never the tree row.

**Risks:** writing anything back into `focused` breaks EV-35's "marker and `e` target the same unit" and reopens Q5; touching the header or adding a header row at grant 1 collides with FLLWUP-37/38 and is out of scope; clamping the `single` head changes the multi-row render beyond the one-row case (EV-34/35 suites must stay green); the modal `openTranscript` shares `TranscriptView` and will inherit the projection (harmless at `termRows-4 >= 6`, but T10 must cover it); and a projection that returns a head with no `truncateToWidth` silently overflows the grant in *physical* rows.

Wiki pages used: `vault/wiki/index.md`, `vault/wiki/council-job-tree-inline.md`, `vault/wiki/run-transcripts.md`; plus `council/cards/EV-36.md`, `council/cards/FLLWUP-37.md`, `council/cards/FLLWUP-38.md`, `vault/raw/2026-09-15-po-epic8-ruling.md`, `docs/superpowers/specs/2026-09-15-EV-35-design.md`, `docs/tui.md` (pi packaged docs). New code, not the wiki: `extensions/focus-nav.ts:83-120`, `:188-199`; `extensions/navigator.ts:388,427-455,561-570,618-620,699-708,723,766-779,796-817`; `extensions/transcript.ts:33-47,84-102`; `test/ev9-progress.test.ts`, `test/ev34-tool-unit.test.ts`, `test/theme-compliance.test.ts`.

#### Position — `designer` (job-16.4)

**Design position**

The one-row line must be the focused unit's composed head with the `▌` marker at column 0 and the `muted "✗"` failure suffix where applicable — and only that line. Concretely: `TranscriptView.render` gains a `viewportRows === 1` branch that anchors the slice on `focusLine = starts[focused]` (the focused unit's head line in `all`) and ignores `follow`, which is meaningless at this size. The count half is already satisfied by `computeProgressLayout` (`progressLines ≥ 1` at `termRows ≥ 7`) and the factory's existing outer `slice(0, layout.progressLines)` at `navigator.ts:431`. Two halves, two functions, no overlap. R-COPY (binding) is honored: the one-row line *is* the EV-34 composed head for the focused block; no parallel one-row surface. R-MARKER is honored: `▌` is `TREE_ROW_MARKER`, the same constant the tree rows and `unitLines` already read.

**Gulf closed**

**Evaluation gulf, at the moment of "which block am I on?" on a one-row terminal.** Today the slice lands on the last wrapped body line of the transcript (`topLine = maxTop` in follow mode); the operator sees a string of text that names no tool, no arg, no success/failure, no block identity. After EV-36, the operator sees `▌ → <Tool>  <primary-arg>` (with ` ✗` on failure) — the answer on-screen at the granted viewport's only row. Secondary **execution gulf** at "I want to see the body": the slip "press `e` repeatedly" is silent at one row (body is sliced out) but harmless (expand state survives a resize). The interface refuses to lie about what is visible — a forcing function. Cost: widening the terminal is the only way to see the body.

**Principle and evidence**

- **Mapping, single source.** Focused unit ↔ head line ↔ visible row at one line is one expression: `focused` (visible index) → `starts[focused]` (head line index in `all`) → `all[starts[focused]]`. All three are computed in the same `render` pass; no off-screen state can drift. Apply at `extensions/navigator.ts:813–817`.
- **Signifier reuse by construction.** `▌` is the same `TREE_ROW_MARKER = "\u258C"` constant the tree rows read (`navigator.ts:415`, `focus-nav.ts:38`) and `unitLines` reads (`navigator.ts:705`). R-MARKER binding.
- **Count is the layout function's contract.** `computeProgressLayout(7,·) === {avail:2, sepLines:0, treeLines:1, progressLines:1}` (`extensions/focus-nav.ts:103`); the factory's `slice(0, layout.progressLines)` is the runtime clamp. The card's acceptance bullet "never exceeds the granted viewport" is satisfied at this slice; the regression guard is a pure-seam assertion that `view.render(width).length ≤ viewportRows`.
- **Token-only.** Composed head uses `warning` (R-COPY); `✗` uses `muted`; marker uses `accent` — all on the documented palette. AGENTS.md 9.6 binding. `test/theme-compliance.test.ts` stays green.
- **Minimal-ui disposition.** One column per load-bearing fact: marker / verb / tool / arg / status. Five cells, no decoration. (`vault/raw/2026-09-15-design-ev36-round1.md` carries the full unitLines + render sketch.)

**Falsifiable predictions**

(I cannot see the running interface; each is a hypothesis the skeptic runs as a pure-seam test against `TranscriptView` + `CouncilTreeWidget.renderProgress`.)

- **P1.** `viewportRows === 1`, focused tool unit: `view.render(width).length === 1`; line starts with `\u258C`; contains `→ <label>  <primary-arg>`; no 2-space-indented body in the slice. *Falsifier:* paired-and-unpaired tool fixture; render at `viewportRows=1`; assert shape.
- **P2.** **The headline bug.** `viewportRows === 1` + `follow=true` (default) + focused unit expanded: today the slice is the body tail (RED on merged `85db7a6`); after EV-36 it is the head (GREEN). Run on current code first to confirm RED, then on the change.
- **P3.** Failed focused unit at one row: slice ends with ` ✗` (U+2717 with leading space), `muted` token. *Falsifier:* paired tool fixture with `isError: true`.
- **P4.** Focused unit whose head is mid-transcript (others after it): slice is the focused head, not the trailing unit's head, not body. *Falsifier:* three-tool fixture; `G` → assert last head; `↑` → assert middle head; current code is RED on this case too.
- **P5.** Empty transcript at one row: slice is `(waiting for output · idle)` (EV-34 empty state), no marker, no blank, length1. *Falsifier:* fresh view over empty file.
- **P6.** Focused user/assistant/thinking: slice is `▌ user` / `▌ assistant` / `▌ thinking`, no body. *Falsifier:* mixed fixture; navigate focus; assert.
- **P7.** Across `termRows ∈ {7, 8, 9, 40}`: factory line count equals `treeLines + sepLines + min(view.render(width).length, progressLines)`; never exceeds `treeLines + sepLines + progressLines`; never zero in the progress region. *Falsifier:* instantiate `CouncilTreeWidget` at each termRows; populated jsonl; assert.
- **P8.** `termRows === 7`: factory progress output is **exactly one line**, that line begins with `▌`, contains the composed head. *Falsifier:* factory at termRows=7; assert length and shape.
- **P9.** Empty transcript at `termRows === 7`: factory output is exactly one line (the empty state), not zero, not two. The factory's slice is the count-clamp site.
- **P10.** No new theme tokens; grep `theme.(fg|bg|bold)("...")` returns only the eight documented tokens. EV-7/EV-8/EV-9/EV-34/EV-35 suites stay green; `bunx tsc --noEmit` clean.
- **P11.** EV-34 O12 invariant: `firstArgOf` has exactly two consumers (tree row at `navigator.ts:237`, composed head at `:710`); single derivation survives.

**Comprehension checks** (judgment, settles only on a human read): **C1** a first-time operator answers (i) which seat (highlighted tree row above), (ii) which block (▌), (iii) which tool + arg (→ Tool arg), (iv) did it fail (✗) — all on the one row, no docs. **C2** the composed head's regular shape (`▌ → Tool arg[ ✗]`) is recognizable as a tool-call identifier in one read — not a wrapped body wall. **C3** `e` at one row is silent, honest, recoverable by widening — the forcing function. **C4** the empty state at one row is recognizable as "no blocks yet," not "hung."

**What this card should NOT do** (filed as follow-ups)

1. **DO NOT pin the keymap header as row 0 of the progress viewport.** EV-35 round-2 D2 already ruled against pinning because it contradicts EV-36's acceptance ("at one row, the visible line identifies the active block"). The natural home for header discoverability is `FLLWUP-37` (header visibility on viewport-overflow transcripts), already on the backlog per EV-35 round-2.
2. **DO NOT change `computeProgressLayout`.** It already pins `progressLines ≥ 1` at `termRows ≥ 7`. EV-36's change set is the view's slice anchor, not the layout function.
3. **DO NOT introduce a parallel one-row surface.** R-COPY (binding) forbids it. The one-row line IS the composed head of the focused block.
4. **DO NOT widen `DISPLAY_FLOOR`.** That's an EPIC-2 follow-up, not EV-8.
5. **DO NOT add a "current/total" indicator at one row.** The `▌` + composed head IS the indicator; a second crowds one row with redundant information.
6. **DO NOT add a follow-mode badge at one row.** Follow is irrelevant at one row; a badge suggests scrollability that doesn't exist. The forcing function at one row is "the slice is the slice."
7. **DO NOT change the routing kernel in `extensions/focus-nav.ts`.** EV-36 is a render-time change inside `TranscriptView`. `classifyProgressKey`/`routeEditorFocus` are settled by EV-35's Q1 ruling.
8. **DO NOT couple `follow` to `viewportRows`.** The `viewportRows === 1` branch sets `topLine` from `focusLine` directly and never reads `follow`; `follow` retains its existing meaning at `viewportRows >= 2`.
9. **DO NOT carry the EV-35 owner round-1 header-pin prediction into this card.** The *resolution* is "header is sliced out at one row" (this card), not "header is pinned" (EV-35 owner round-1).
10. **DO NOT couple to FLLWUP-36 (the `blockLines` cleanup from EV-34 step-13).** Both touch `navigator.ts`; both are EPIC-8. EV-36's fold-in question (if any) is its own; FLLWUP-36 is the `steward`-confirmed cleanup for the superseded `blockLines` private. They land separately or interleave by R-ORDER, not as one card.

Full position written to `vault/raw/2026-09-15-design-ev36-round1.md` (with the file/line citations, the concrete `render(width)` sketch, the routing table, and the preferences section). No application code touched. Turn ends here.

### Step 3 — bounded exchange, round 2 of ≤3 (facilitator digest + positions)

The three generators were re-dispatched with each other's round-1 positions (they
read the verbatim positions from this card) and six named divergences to
answer. Jobs `job-16.5` (owner, 9.7m / 6 turns, total 142840 tok, cost≈$0.0148),
`job-16.6` (principal, 9.7m / 10 turns, total 509155 tok, cost≈$0.0421),
`job-16.7` (designer, 9.7m / 3 turns, total 80918 tok, cost≈$0.0330).

**Facilitator note — the exchange is stopped at round 2 of 3.** Round 2
produced a clean swap rather than a drift: `owner` conceded divergence 1
outright in favour of `principal`'s round-1 reading, `principal` retracted
its T7 and conceded divergence 1 in favour of `owner`'s round-1 writeback
mechanism, and `designer` conceded its own round-1 anchor in favour of
`principal`'s reading. Two generators and one generator now sit on
opposite sides of a crux that neither side moved on in round 2; positions
have stabilised into a single named question, and a third round would
re-litigate it rather than settle it (council.md step 3: "Stop early if
positions have stabilised"). **The ≤3-round cap is not reached** — two
rounds ran. Agreement between seats is not recorded as evidence of
anything; the crux below is left open for step 4 (Skeptic tests) and step 5
(consolidator sort).

**The crux, as the round-2 positions leave it — which block the one-row
line shows when `follow` is on.** Three readings were on the table:
(R1) anchor on `starts[focused]`, ignore `follow` (designer round 1);
(R2) write `this.focused = vis.length − 1` in `render` when the effective
viewport is 1 and follow is on (owner round 1); (R3) a read-only effective
index `follow ? vis[vis.length−1] : vis[fv]`, `focused` never written in
`render` (principal round 1). After round 2: **`owner` and `designer` hold
R3; `principal` holds R2.** Two records bear on it and the facilitator
records them here as facts, not as a ruling applied:

- **Q5 (EV-35 step-6 product-owner ruling, recorded, binding on EV-35's
  change set)** — "keep `focused` as a visible-array index; no rename, no
  `focusedAbsolute`, no `focusedVisible()` getter, **no render-time
  recompute**", with empty-`vis` guards added per O6a
  (`council/cards/EV-35.md`, step 6, Q5); restated in EV-35's shipped spec
  anti-goals: "**No `focusedAbsolute` / recompute** (Q5) and no
  cursor-stability work" (`docs/superpowers/specs/2026-09-15-EV-35-design.md:272`).
  Today's `render` does write `focused`, but only as a range clamp
  (`navigator.ts:796-797`), which `principal` round 2 raised as evidence
  that a write is not categorically forbidden, and which `owner` round 2
distinguished as reconciliation-to-range rather than a recompute.
- **R-COPY (EPIC-8 Phase 1, recorded human decision, immutable for the run)
  + its card-face transcription + D9** — the epic card's R-COPY names the
  copy and the reuse ("EV-36's one-row line reuses this composed head plus
  the failure suffix", `council/cards/EPIC-8.md`); the EV-36 card face adds
  "for the focused block"; the card's own Intent says the one-row line
  "reuses EV-34's composed tool-call head **for the focused block**"; and
  D9's ruling (all three mechanisms it weighed presuppose the focused
  block) says "the one-row line is the focused block's composed head ...
  with the failure token as a trailing suffix"
  (`vault/raw/2026-09-15-po-epic8-ruling.md:142-151`). Under R1/R2 the
  displayed unit and the unit `e` expands are the same unit; under R3 (with
  `isFocused` left as `vi === fv`) a follow-on one-row view displays the
  tail's head while `e` acts on `vis[fv]`, and carries no `▌`.

No dispute is resolved here by the facilitator. Divergences 2–6 after round
2, recorded as they stand: **(2)** frozen grant — owner took grant re-sync
into scope, principal put T1+T4 in the acceptance and T5 as the mechanism
assertion, designer held that `computeProgressLayout` must not change (all
three agree the layout function is untouched; a narrow disagreement remains
on whether the projected line alone or the `single`-kind head site carries
the width clamp); **(3)** projection location — all three converged on
view-side `TranscriptView.render`, and owner/designer both named a
widget-side projection an R-COPY/T11 parity violation; **(4)** head
semantics for `user`/`assistant`/`thinking`/unpaired `toolResult` — all
three agreed: reuse `unitLines` element 0 per kind, no new copy, no
per-kind floor renderer; **(5)** width — all three agreed a clamp is needed
and disagreed only on the site (owner: `unitLines` single-head site
`navigator.ts:723`; principal: the projected line only; designer:
`unitLines` site, calling the alternative "two clamps"); **(6)** FLLWUP-37 —
all three agreed EV-36 does not consume the overflow case and FLLWUP-37
stays `Backlog`, i.e. no fold-in question arises.

#### Position — `owner` (job-16.5, round 2)

Round 2 — `owner`. I re-grounded against `extensions/navigator.ts`, `extensions/focus-nav.ts`, the EV-35 spec's anti-goals (`docs/superpowers/specs/2026-09-15-EV-35-design.md`), and D9 (`vault/raw/2026-09-15-po-epic8-ruling.md:142-151`). Two things in today's tree decide the biggest divergence, so I open with the concession.

---

## The verdict on Divergence 1 first: the principal is right, and I withdraw my mechanism

EV-35's anti-goals bind, in the spec's own words: **"No `focusedAbsolute` / recompute (Q5) and no cursor-stability work"** (`2026-09-15-EV-35-design.md:272`). My round-1 pin (`render` writes `this.focused = vis.length − 1` when effective vp is 1 and follow is on) is a render-side cursor repositioning of exactly the class Q5 ruled out. The precedent I cited — the clamp at `navigator.ts:796-797` — is reconciliation-to-valid-range after focus *keys* moved the list; it is not the same move, and citing it was wrong.

Adopted reading, then, is the principal's:

- **Projection target (read-only):** `follow ? vis[vis.length−1] : vis[fv]`, computed in `render`, `focused` never written there (`focused` is mutated only in `handleInput`, `navigator.ts:766-779`).
- **Marker:** `isFocused` stays the existing `vi === fv` expression (`navigator.ts:709`). Therefore **a fresh follow-on one-row view carries no ``** — the projected unit is `vis[length−1]` while `fv = 0`, and painting `▌` on a unit `e` would not act on would be the lie R-MARKER exists to prevent. `G`/`↓` set `follow=false` and move `focused`; the marker then appears on the navigated unit (principal's T7: 0 `▌` fresh-follow, exactly 1 after `G`).
- Why the designer's "anchor on `starts[focused]`, ignore follow" loses: on a fresh view `focused = 0`, so the default floor state shows the **oldest** unit's head — with a `▌` claiming focus on it. The card's goal line 7 and acceptance ("the one-row case identifies the active block") are written for exactly the fresh follow-on state; a reading that fails the card's own default state is not a reading of D9, it's a misreading of which noun D9 was ruling on. D9's "the focused block" (`po-epic8-ruling.md:142-151`) was ruling the *shape* — composed head + status suffix, single source — not adjudicating the focused-vs-active index, which the card itself flags as the open question. The shape is unchanged; only the index feeding it is follow-conditional.

## The six divergences, answered

**1. Which block — answered above.** Principal's reading, verbatim consequences: no `focused` write in `render`, no `▌` on a fresh follow-on one-row view. Where I hold: nothing. I concede outright.

**2. Frozen-grant hazard — in scope, and the mechanism framing is the principal's.** `ensureView` returns the cached view and ignores its argument (`navigator.ts:437-440`), so `viewportRows` is frozen at construction (`:445`, `:628`); the outer `slice(0, layout.progressLines)` (`:432`) masks the count half but not the window — at grant 1 after an O5 tree-growth the line shown is `all.slice(maxTop, maxTop+stale)[0]`, a mid-body continuation. This is not deferrable because the vp===1 projection *keys on the current grant*: without re-sync the branch never fires on the termRows-9 transition and the card's red state survives the fix. Mechanism: re-grant per render — either a `setViewportRows(n)` mutator called from `ensureView` on the cache hit, or `render(width, viewportRows = this.viewportRows)`; the view has no line cache (render recomputes from `blocks` each call), so either is one field/parameter. I won't spend a divergence on which form; **T5 is the gate**: after `refresh()` at termRows 9 with a new manifest, the view's viewport equals `computeProgressLayout(9,2).progressLines === 1`, and raising the grant emits the larger window. Where I hold: keep `slice(0, layout.progressLines)` at `:432` as the redundant count guard.

**3. Projection location — concede: view-side, in `TranscriptView.render`.** It matters materially: `unitLines`, `visible`, and the head derivations are private to the view (`navigator.ts:693+`); a widget-side projection would fork the composed-head construction, which is an R-COPY violation by construction and breaks T10/T11 parity. The modal path (`openTranscript`) shares the class and inherits the branch, but is dormant — its viewport is `termRows−4 ≥ 6`, never 1 — and T10 must include a modal-shaped render at its own grant to prove the inheritance is inert (the principal already scoped this; agreed).

**4. Head semantics — the principal holds; R-COPY generalizes rather than forks.** The one-row line is element 0 of `unitLines(u, i, width, isFocused)` for whatever kind the projected unit is: `toolCall` → `→ <label>  <arg>` warning + muted ` ✗` on failed pair, truncated (`:712-724`); `user` → accent-bold `user`; `assistant` → success-bold `assistant`; `thinking` → dim `thinking` (only when `t` has made it visible, `:679-684`); unpaired `toolResult` → muted `⎿ <label> · <N>b` + ` ✗` when `isError` (`:717-721`). No new copy, no per-kind floor renderer. T6's byte-equality-per-kind test is the right falsifier and I adopt it as binding.

**5. Width — in scope, not follow-up, and the clamp site is the principal's.** Verified: the toolCall head is truncated (`navigator.ts:724`) but single heads are emitted raw (`:723`, `const out = [marker + head]`). Of the four single kinds, only the unpaired `toolResult` head is realistically over-wide (` <label> · Nb` — MCP-style labels); the other three are fixed short words. An over-wide line terminal-wraps and consumes a physical row the budget counts free — the count half goes false in physical terms and no pure-seam test sees it. Clamp the single head at `:723` (one site, single-source; idempotent with the toolCall clamp) rather than inside the projection. The risk — that this changes multi-row render for singles at narrow widths — is real and is exactly what the ev34/ev35 suites gate.

**6. FLLWUP-37 — no fold-in; the designer is right.** My design consumes nothing from FLLWUP-37: the acceptance needs the one-row line to identify the block, and at grant 1 the header is sliced out — that is the honest state, not a gap EV-36 must paper over. FLLWUP-37 stays Backlog; its retirement condition remains a deliberate steward call, not an incidental consequence of this card.

---

## Approach

Concede the focused-pin (Q5 anti-goal, `EV-35-design.md:272`), adopt the principal's read-only active-unit projection with the no-`▌`-on-fresh-follow consequence, adopt view-side projection and T6 head semantics, take grant re-sync and the `:723` width clamp into EV-36's scope, and leave FLLWUP-37 untouched. The card's shape is otherwise unchanged from round 1: reuse `unitLines` element 0, `computeProgressLayout` untouched, outer slice retained, empty-state placeholder retained.

## Tradeoffs accepted

- The fresh follow-on floor shows the live tail's head with no `▌` — the operator loses the "this is the active one" cue until they press a nav key. Accepted: it is the only state where R-MARKER, Q5, and the goal are simultaneously true; a `▌` there would promise `e` targets a unit it doesn't.
- Clamping single heads at `:723` changes multi-row rendering for narrow widths beyond the one-row case. Accepted; ev34/ev35 suites are the gate and the change is one line.
- Follow state remains invisible at the floor (header carries the suffix; header is sliced out) — unchanged from round 1.

## Testable claims

1. **D1 discriminator (adopt principal's T3+T7 as binding):** fresh view, three-unit fixture, follow on, grant 1 → single line is unit 3's head and `count("▌") === 0`; after `G` → unit 3's head, `count("▌") === 1`. Red today (line is a body continuation); also red under my round-1 design in the first half and under the designer's reading in both halves (unit 0's head, `▌` present).
2. **Q5 non-violation:** after any sequence of `poll()` + `render()` (including new units arriving and `t` toggling) with no key pressed, `focused` is unchanged. Green today; would be red under my round-1 pin — this is the test that killed my own mechanism.
3. **Re-grant observable (T5):** open at termRows 9 / 1 job (grant 2), append a manifest, `refresh()`, render → the line is the active head, and the view's viewport equals `computeProgressLayout(9,2).progressLines === 1`; raising the grant to 31 emits 31 lines. Red today (`ensureView` early-return, `:440`).
4. **Width:** at width 40 with a long unpaired-`toolResult` label, every returned line satisfies `visibleWidth ≤ 40`. Red today (`:723` untruncated); green after the `:723` clamp, with `test/ev34-tool-unit.test.ts` and `test/ev35-transcript-interaction.test.ts` green unchanged.

#### Position — `principal` (job-16.6, round 2)

**Cross-seam reading**

The contract has four owners today, and I re-read both sides before speaking:

- **Grant.** `computeProgressLayout` is the sole grantor (`extensions/focus-nav.ts:104-120`); the widget calls it per render and passes `layout.progressLines` into `ensureView` (`extensions/navigator.ts:428-431`).
- **Window.** `TranscriptView.viewportRows` is a constructor field (`navigator.ts:628`, set at `:445`); the window is `all.slice(topLine, topLine + viewportRows)` with `maxTop = all.length - viewportRows` (`:814-817`).
- **The seam that leaks.** `ensureView(viewportRows)` **ignores its argument** when the session matches — `if (this.viewFor?.sessionId === sid) return this.viewFor.view` (`:440`). `renderProgress` then re-slices at `:432`, which masks the *count* and does nothing for the *window*. This is read, not executed (see divergence 6).
- **Focus.** `focused` is a visible index; it is mutated in `handleInput` **and** in `render` (`:796-797` clamp — so my round-1 "never write `focused`" was factually wrong as a description of today's code). `e` reads `vis[this.focused].i` and the marker flows from the same expression via `isFocused = vi === fv` (`:699`, `:796`).

**Blind spots, named separately**

- **Owner's slice.** Seeing the render-side clamp as precedent (`:796-797`) is correct, but owner's writeback condition — "effective viewport is 1" — is evaluated *inside* the view, on the very field whose staleness owner just identified (`:628`). The fix and the trigger are the same frozen field; the projection silently won't fire in the transition owner's own T4 constructs unless the grant is threaded into the render call.
- **Designer's slice.** From inside `render` the grant *is* the constructor constant, so "count half is already satisfied, don't touch the layout function" is true and still concludes "no other layer moves." Nothing inside the view can detect that its `viewportRows` is no longer the grant. Second blind spot: "ignore `follow` at one row" cannot see that under follow-on the active block *advances*, so the floor freezes on unit 0 for a running seat.
- **My own.** I read Q5 (`EV-35.md:880-889, 949-951`) as forbidding render-side focus writes; Q5 forbade the `focusedAbsolute`/recompute *scheme*, not a visible-index clamp — and render already writes the clamp. That over-read is the root of my T7.

**Reframe / concessions, divergence by divergence**

1. **Which block.** Concede to **owner**, retract **T7**. D9 says "the focused block"; EV-35's contract is marker ↔ `e` on one expression (`vis[this.focused]`). A read-only active projection shows a block `e` will not expand, breaking that. Owner's writeback makes active == focused under follow, so the goal's word ("active") and D9's word ("focused") coincide and the marker stays honest. Designer's ignore-follow branch satisfies D9 literally but fails the goal's "active" (under follow `focused` is 0, so the floor pins the *oldest* unit). T7's 0-marker prediction is the wrong assertion; the right one is A4 below. One hold against owner: the writeback must key on the **current** grant, not the frozen `viewportRows`, or it doesn't fire — and must stay scoped to `follow === true` so a user who navigated still sees `focused`. Also state openly that owner's writeback persists after the grant returns to ≥2 (marker then follows the tail at larger viewports) — a behavior delta beyond the one-row case, acceptable but not hidden.

2. **Frozen grant.** Owner is right that the re-sync is needed; designer is right that `computeProgressLayout` must not change. **In scope:** T1 (count guard, green today) and **T4** — termRows 9 is a named acceptance vector and `(9,2) → progressLines 1` (`focus-nav.ts:105-115`), reachable via the O5 job-landing path, so Acceptance bullet 2 is violated there today. **T5** is in scope as the widget→view seam guard: given T11 parity (`test/ev9-progress.test.ts`, "T11 parity"), a widget-side projection override breaks byte-identity, so the projection must live in the view and the view must know the current grant. So nothing files as a separate defect — T1+T4 are acceptance, T5 is the mechanism assertion.

3. **Projection location / single source.** Single source is `computeProgressLayout`; the grant must flow as a per-render parameter into `render(width, grant)` (or a `setViewportRows` immediately before `view.render`), and the projection keys on that parameter. Designer's `viewportRows === 1` on the frozen field is a second copy of the grant → loses T4. A widget-side projection (owner's framing) needs a new accessor into view internals → loses T11. Per-render parameter loses nothing; the widget already owns the grant lifecycle (`:428-431`).

4. **T6/T9.** **T6 is in scope** as proof with no design delta: the projection selects element 0 of the active unit's `unitLines` (`:723` singles, `:708` toolCall), so `user`/`assistant`/`thinking`/unpaired-`toolResult` heads come free. **T9 is in scope narrowly:** the *projected one-row line* gets one `truncateToWidth`, because an over-wide single head wraps in physical rows and the logical count half (`Acceptance` bullet 1) cannot see that. A general clamp of single heads at vp≥2 is a follow-up — it changes multi-row renders and risks EV-34/35 suites. Minimal set: projection on the live grant + one width clamp on the projected line + empty-`vis` fall-through (`:806`) + owner's follow-scoped focus advance.

5. **FLLWUP-37.** EV-36's goal says nothing about header visibility at vp≥2. FLLWUP-37's bullets 1-2 (header present, marked head present at overflow) are untouched by EV-36; only bullet 3's one-row carve-out is consumed, and that is already mandated by EV-36's own bullet 2. So EV-36 neither requires nor retires FLLWUP-37 — it stays Backlog, and the card's "retires for free" clause is conditional on EV-36 consuming the *overflow* case, which it does not. Deliberate call, not an incidental consequence; I agree with the designer's outcome, not its reasoning.

6. **Shared hazard.** **Untested hypothesis.** Evidence is two reads: `ensureView` early-return (`:440`) + frozen field (`:628`) + `renderProgress` slice (`:432`). The existing O5 test (`test/ev9-progress.test.ts`, "O5") only asserts row re-sync; no test asserts `activeTranscriptView`'s viewport after a grant change. Neither owner nor I ran it.

**Testable claims**

- **A1 (retracts T7).** Fresh follow-on view, grant 1, three tool units: exactly one `▌`, on the newest unit's head; `focused` is not written by the test. Falsifies T7 and designer's branch (which yields unit 0's head).
- **A2 (discriminator).** termRows 9, one job (grant 2); enter progress; write a second manifest; `w.refresh()`; render → the single transcript line is the active head. Red on today's main at two sites; still red after a re-grant alone; still red after a projection keyed on the frozen field. Only "live grant + projection" passes.
- **A3 (T11 under projection).** Same JSONL/width/current-grant → inline == standalone, at grant 1 and at the `(9,2)` transition. Falsifies a widget-side projection.
- **A4 (marker/e coherence).** After A1's render, `handleInput("e")` changes the body of the unit whose head is the displayed line, and the marker stays on that line. Falsifies any active projection that doesn't advance `focused`.
- **A5 (width).** Grant 1, unpaired `toolResult` with a label wider than width at width 40 → `visibleWidth(line) <= 40`.

The design is otherwise sound — R-COPY/D9 mechanism, token reuse, single `unitLines` derivation, layout function untouched, modal out of scope — and needs no further reframe beyond the trigger's source and the follow subject.

#### Position — `designer` (job-16.7, round 2)

# Round-2 designer position — EV-36

## Design position

The one-row line is the **effective-indexed unit's head**, read-only, never writing back to `focused`: `vis[follow ? vis.length-1 : fv]`. The marker `▌` is shown iff the line is `vis[fv]` (follow off, or `vis.length === 1`); at follow on + `vis.length > 1` + fresh view, the marker is absent. This is the principal reading. My round-1 anchor on `starts[focused]` produced a comprehension failure I did not name cleanly at the time (line frozen on the oldest head while the transcript streams past — a worse failure than I conceded in round 1), and the owner's reading collides with EV-35 Q5's prohibition on focused-writeback from `render`.

## Engagement with the six divergences

### 1. Which block the one-row line shows — comprehension failure of each

- **My reading (`starts[focused]`, ignore follow)** — at follow on + 1 row + fresh view, the line is frozen on the oldest unit's head while the JSONL streams new units in. The operator who opened progress to watch the live tail sees a static line. **Failure: conceptual model.** The operator concludes "the view is stuck" or "follow doesn't work." This is the gulf of evaluation widened, not narrowed — and my round-1 framing ("the operator is asking what the seat is doing right now") contradicted my own anchor ("ignore follow, show the cursor"). Conceded.
- **Owner's reading (mutate `focused` to `vis.length-1` at grant 1 + follow on)** — violates **EV-35 Q5** (recorded decision: no writeback to `focused` from `render`, because the marker and `e` must target the same unit). Setting that aside: at follow on + 1 row, `↑` moves `focused` from `vis.length-1` to `vis.length-2`, but the slice still shows `vis[vis.length-1]`'s head because the render-side mutation re-pins it. **Failure: mapping** — marker position and line position diverge; the operator sees the marker scroll up while the line stays put.
- **Principal's reading (effective index, read-only)** — at follow on + 1 row + fresh view, the line is the last unit's head, no marker. Operator who presses `↑` (which flips follow off in EV-35's keyboard handler at `navigator.ts:766-779`) sees the line snap from "last head, no marker" to "oldest head, with marker." **Failure: snap on follow-toggle.** Smallest of the three; honest because the marker is the cursor signifier and the cursor was at `vis[0]` before the keypress.

**Recommendation: principal's reading.** Only reading consistent with EV-35 Q5. The snap is a one-time event per follow-toggle and the operator's mental model ("marker = cursor") holds throughout. My round-1 reading widens the evaluation gulf; owner's reading violates a recorded decision.

### 2. P2 / P4 / P8 under the three readings

- **P2 (headline bug)** — RED today (`navigator.ts:815` returns the wrapped body tail at `topLine = maxTop` when follow is on). GREEN under all three readings after EV-36: the slice becomes a head. P2 does not discriminate.
- **P4 (focused head mid-transcript)** — under principal's reading, restated: at `viewportRows === 1` + follow off (the only state in which the marker navigates), `↑`/`↓`/`g`/`G` move the marker with the line. At follow on, the line tracks `vis[vis.length-1]` regardless of `↑`. **P4 holds under principal's reading.** Under owner's reading, the `↑` half fails (line stays on `vis[vis.length-1]`'s head because render re-pins `focused`). **P4 discriminates owner's from principal's.** Falsifier: three-unit fixture; `G` → assert last head with marker; `↑` → assert middle head with marker; toggle `f` on → assert last head no marker.
- **P8 (termRows=7 line begins with `▌`)** — round-1 prediction is wrong under principal's reading at follow on + `vis.length > 1`. **Restated**: at termRows=7, the factory's progress output is exactly one line; that line is (a) the last unit's head, no marker, when follow on + `vis.length > 1` + never moved cursor; (b) the focused unit's head, with marker, when follow off; (c) the empty-state line, no marker, when `vis.length === 0`. Falsifier: instantiate at termRows=7 with a three-unit transcript, follow on default; assert factory output length === 1, line contains the third unit's composed-head tokens, `line[0] !== '\u258C'`. Then `G`, re-render, assert `line[0] === '\u258C'`. Under my reading, P8 is wrong at fresh view + follow on (line is unit 1's head with marker). Under owner's reading, P8 holds (line is unit 3's head with marker because `focused` is mutated to `vis.length-1`).

**The principal reading makes the one-row surface most comprehensible**: the marker is always honest — present when the line IS the cursor, absent when the line is the live tail and the cursor is elsewhere.

### 3. Follow-invisible floor — acceptable?

**Yes**, with one caveat. Both owner and principal agree the header cannot be pinned at one row; I agree. The forcing function: at one row, the operator can see WHAT follow does (the line scrolls to the latest unit as new units stream in) but not THAT it's labeled "follow." The label is the keymap hint; the behavior is observable without it. **Knowledge in the world** — the row's motion is the only signal at one row, and it IS on the world. The keymap hint is unnecessary at this size because the moving row is its own explanation.

**Caveat**: the transition from 1 row to 2+ rows must be coherent. At 1 row, the line tracks the live tail (under follow on); at 2+ rows, the line tracks the focused unit with body prefix (`navigator.ts:813-816`). The operator who widens once sees the transition and learns the rule. Acceptable cost — the rule is consistent ("the line is what you're watching"), only the referent of "watching" changes with viewport. **Where this breaks**: if the operator's first exposure is at one row + follow off (so the line is the oldest head), the transition to multi-row shows the body of the oldest head, which is not what the operator was watching moments earlier. Pre-existing EV-35 behavior; not EV-36's debt.

### 4. Unpaired / non-toolCall units at one row

R-COPY pins only the `toolCall` head; the four other kinds need a definition at one row. Restated under principal's reading:

- **`user` / `assistant`** — line is the EV-34 single-kind label (`navigator.ts:723`). Marker iff `vi === fv`. Body sliced out. The label is recognizable as a chat-block identifier.
- **`thinking`** — same: `thinking` label, marker iff focused, body sliced out. The `t` toggle filters thinking from `vis()` (`navigator.ts:679-684`); if filtered, the unit is not in `vis()` and cannot be the active unit. The operator who toggles `t` to show thinking sees the line change to the thinking label if the live tail is a thinking unit.
- **Unpaired `toolResult`** — today, `unitLines` for `single`-kind returns `[marker + head]` (`navigator.ts:723`). **Need to verify** whether the single-kind marker is gated by `isFocused` the way the `toolCall` marker is (`navigator.ts:705`). **Hypothesis**: if the single-kind marker is unconditional, that is a pre-existing multi-row bug too (a non-focused unpaired toolResult would show `▌ toolResult` when not focused). **Design position**: the `single`-kind marker MUST be conditional on `isFocused`, consistent with `toolCall`, fixed at the `unitLines` site (single source), not at the projection. At one row, unpaired `toolResult` shows the label, marker iff focused. **Distinct one-row treatment? No** — R-COPY forbids a parallel surface. The label is the head; the head is the line.

### 5. Width — clamping at one row

**Part of this card.** The acceptance bullet "never exceeds the granted viewport" is violated by a wrapped single-kind head at one row — line count is 1, physical rows consumed is 2. The factory's outer `slice(0, layout.progressLines)` cannot detect this (it slices lines, not physical rows). Per `docs/tui.md:25,330` (pi packaged docs), an over-wide line is a component-contract violation whose terminal wrap consumes rows the budget assumes are free.

**Where to clamp**: at `unitLines` (single source), NOT at the projection (two clamps). The tool-call head is already clamped at `navigator.ts:708`; the single-kind head is not (`navigator.ts:723`). **Clamp the single-kind head** with the same `truncateToWidth` helper, conditional on `width`. Reasoning: at one row, a wrapped label is fatal; at multi-row, a wrapped label is still ugly but the count half survives (the factory's slice absorbs the extra lines up to `progressLines`). Clamping at `unitLines` makes the rule apply at both viewport sizes — and at the multi-row case, it costs nothing observable.

### 6. FLLWUP-37 — fold-in?

**No.** EV-36's acceptance is the one-row case. FLLWUP-37 is "header visibility when follow-mode content overflows the viewport" — a multi-row case (viewport ≥ 2, follow on, content overflows the granted region). The two cards solve inverse problems: EV-36 hides the header at one row because there is no room; FLLWUP-37 pins the header at multi-row when content overflows because there is no other honest place for the keymap hint. Complementary, not absorbing.

**State**: FLLWUP-37 stays Backlog. EV-36 does NOT consume the overflow case. Recording this explicitly as a non-fold-in so the run record carries it forward to the consolidator.

## Gulf closed (revised)

**Evaluation gulf, at the moment of "which block am I on?" on a one-row terminal.** Under the principal reading, the operator sees the live tail's composed head (or the focused unit's head, depending on follow state) — naming tool, arg, and failure status, with the marker honestly indicating whether the line is the cursor. The marker-absence-under-follow is the smallest honesty cost: the operator learns "marker = cursor" in one read, and the absence teaches "I'm watching, not navigating." The "press `e` to see the body" forcing function holds under all three readings; not EV-36's distinguishing contribution.

## Falsifiable predictions (revised set)

- **P2 (unchanged)**: `viewportRows=1` + follow on + expanded focused unit → slice is a head, not a body tail. RED on merged `85db7a6`; GREEN after EV-36. Pure-seam test against `TranscriptView`; `navigator.ts:815` is the failing site.
- **P4 (revised)**: at `viewportRows=1` + follow off, `↑`/`↓`/`g`/`G` move the marker with the line; at follow on, the line tracks `vis[vis.length-1]` regardless of `↑`. Discriminates principal from owner. Falsifier above.
- **P8 (revised)**: at termRows=7, factory progress output is exactly one line; line content per state per the table in divergence 2. Falsifier above.
- **P-new (follow-toggle snap)**: at `viewportRows=1` + follow on + `vis.length > 1`, pressing `f` flips follow off; the line snaps from "last head, no marker" to "vis[fv] head, with marker." Falsifier: three-unit fixture, fresh view + follow on; render; assert no marker; press `f`; re-render; assert marker on first unit's head (because `fv = 0` default).
- **P-new (single-kind width clamp)**: at `viewportRows=1` + `width=40` + single-kind unit with a label longer than 40 cells (e.g., a long MCP tool name), `view.render(width)` returns one line whose `visibleWidth(line) <= 40`. Falsifier: fixture with a single-kind unit carrying a 60-char label; render at width=40; assert `visibleWidth(line) <= 40`.
- **P-new (single-kind marker gating, multi-row parity)**: at `viewportRows=2` + non-focused single-kind unit (e.g., a `toolResult` whose preceding `toolCall` is the focused unit), the slice contains the single-kind label WITHOUT a `▌` prefix. RED if the marker is unconditional in the single-kind branch today; GREEN after gating it on `isFocused` at `unitLines`. Falsifier: two-unit fixture, toolCall + unpaired toolResult, focus on toolCall; render at `viewportRows=2`; assert the second line does not start with `\u258C`. This is a multi-row falsifier — fixes a pre-existing bug at multi-row, not only at one row.
- **P-new (effective-index frozen-grant)** (carries principal's T4): open progress at `termRows=9` with 1 job (`progressLines=2`), then add a manifest and call `w.refresh()` (O5 path, `navigator.ts:448-455`), then render at `viewportRows=1`. Today RED (slice is `all.slice(maxTop, maxTop+2)[0]` — a mid-body continuation). After: the last unit's head. Falsifier: instantiate widget at termRows=9 with one manifest and one transcript unit; render at viewportRows=1 (the post-refresh grant); assert single line contains the unit's composed head.

## Comprehension checks (open judgment — settle only on a human read)

- **C-revised-1**: a first-time operator at one row + follow on sees the line move as new units stream in and forms the mental model "this line is the live tail." Pressing `↑` snaps the line to the cursor (oldest head, with marker) and the operator learns "marker = cursor, no marker = watching the tail." The model transfers to multi-row without contradiction: at multi-row, the marker is always present because the focused unit is always on screen.
- **C-revised-2**: the snap on follow-toggle is comprehensible because it is exactly when the operator took an action (`f`); the line was showing the tail, the action made it show the cursor, and the marker appeared. Not a random flicker — a labeled response to a labeled key.
- **C-revised-3**: `e` at one row is silent under all readings; the operator widens to see the body. The forcing function holds.
- **C-revised-4**: a long-label single-kind unit at one row is shown truncated, recognizable as a tool-result identifier (begins with the kind or its label), not wrapped into a wall of text. The marker-absence-under-follow case is the only state in which the label appears without `▌`, and the operator has just learned (in C-revised-1) that no marker means "watching, not navigating."

## Principle and evidence (consolidated)

- **Mapping (effective index, read-only)**: three referents collapse to one expression. `vis` is computed in `render()`; `fv` is `this.focused` (never written from the projection); `follow` is `this.follow` (written only by `handleInput` at `navigator.ts:766-779`). The slice at `viewportRows=1` is `all[starts[effective(follow, fv, vis.length)]]` — every input is observable in the same function pass. Apply at `extensions/navigator.ts:813-817`.
- **Signifier honesty**: `` is the cursor signifier (`TREE_ROW_MARKER`, R-MARKER binding). Shown when the line IS the cursor; absent when the line is the live tail. Honest by construction; the marker never lies about cursor position.
- **Conceptual model**: "marker = cursor, no marker = tail" is the rule. The operator learns it in one read at one row and it transfers to multi-row (where the marker is always present because the focused unit is always on screen).
- **Constraint: count is the layout function's contract** (unchanged from round 1). `computeProgressLayout` pins `progressLines >= 1` (`focus-nav.ts:103`); the factory's `slice(0, layout.progressLines)` (`navigator.ts:431`) caps line count; the `single`-kind width clamp (NEW, this round) caps physical rows.
- **Forcing function against `e`**: at one row, `e` toggles `expanded` but the body is sliced out. The line does not change; the operator widens to see the body. Same under all three readings.
- **EV-35 Q5 binding**: no writeback to `focused` from `render`. The principal reading is the only one that respects this.

## Preferences, ranked last

- **Whether the empty-state line at one row + follow on + `vis.length > 1` with the live tail as a `user` block should differ from the multi-row empty state**. I prefer: the empty-state line is for `vis.length === 0` only. At `vis.length > 1`, the line is always a head. Taste, not load-bearing.
- **Whether the snap on follow-toggle should be animated or instant**. I prefer instant — the operator took an action, the line is the response. Animation would suggest the line "moved through" intermediate states, which it didn't. Equally defensible: a brief fade to teach the transition. Taste, not in card scope.
- **Where the marker goes when the focused unit is the live tail AND `vis.length === 1`**. Under the principal reading, marker shown (because `vi === fv` when `vis.length === 1`). This is consistent — the cursor and the live tail are the same unit, so marker and "active" coincide. No preference, just naming.

## Yield

Round-2 position concedes the principal reading on divergences 1, 2 (P4/P8), 3, and 4 (with the marker-gating fix at `unitLines` as a multi-row parity issue). It folds width clamping into the card (divergence 5). It declines to absorb FLLWUP-37 (divergence 6). No application code touched; no `vault/raw/` document written this round. The principal reading is the only one consistent with EV-35 Q5; my round-1 reading widens the evaluation gulf; owner's reading violates a recorded decision. The single load-bearing change is the projection at `extensions/navigator.ts:813-817`: `effective = follow ? vis.length-1 : fv; topLine = clamp(starts[effective], 0, maxTop)` at `viewportRows=1`, plus the `single`-kind width clamp at `navigator.ts:723`, plus the marker-gating fix on `single`-kind units (verifiable, multi-row parity).

Turn ends.

### Step 4 — Skeptic attacks and runs tests (job-16.8)

Dispatched with every position recorded so far (rounds 1 and 2) and with the
frame stated: no branch and no PR exist yet, so the subject is the delivered
application tree at this checkout (EV-33/34/35 merged); step 9 verifies a
real head later. Job settled 2.5m / 11 turns, tokens in 109214 / out 15576 /
cR 669787 / total 794577, cost≈$0.3032 (catalogue). It ran three throwaway
probe scripts under `/tmp/ev36/` (no repo file written) plus the existing
suites (`bun test test/ev9-progress.test.ts test/ev34-tool-unit.test.ts
test/ev35-transcript-interaction.test.ts` → **39 pass, 0 fail**). Its output
is appended verbatim below.

Attribution note: the Skeptic's O11 credits the facilitator's crux note for
recording Q5 as binding on EV-35's change set rather than as a decided
ruling on EV-36; the facilitator made no such determination — it recorded
Q5's text and principal round 2's scope objection side by side without
adjudicating either.

#### Skeptic output, verbatim

# Skeptic attack — EV-36 (deliberation-time, no branch; subject = checkout @ `740e02d`, EV-33/34/35 merged)

Wiki grounding used: `vault/wiki/council-job-tree-inline.md` (inline expansion, 7-row floor, Enter/Esc semantics), `vault/wiki/run-transcripts.md` (substrate + token-only drawing), `vault/wiki/council-theme.md` (AGENTS.md 9.6 token-only rule). No repo file was written; all probes ran as throwaway scripts under `/tmp/ev36/` or against existing tests. Every `bash` call carried a timeout.

## Objections

- **O1 — "The one-row line identifies the focused block" (card Intent, R1-read). Settling test** (`/tmp/ev36/probe1.ts` F7): single `user` / `assistant` / unpaired-`toolResult` units rendered at `viewportRows=1`, width 80. **Status: `closed-red`.** Output: `user oneRow= ["many words yes"]` (body tail, no `user` head, no `▌`); `assistant oneRow= ["here"]`; `toolResult oneRow= ["  odata"]` (preview line, not the `⎿` head). With a trailing long assistant body (F2): `tail40= "word" hasHead= false hasMarker= false`. Today's follow window (`topLine = maxTop`, `navigator.ts:814-815`) shows a wrapped continuation with no block identity — the card's red state is real, and no reading under which today shows "the focused block" survives. (Corollary: designer R1, `starts[focused]` ignoring follow, is not descriptive of today either — fresh-view `focused=0` would show the *oldest* head; conceded by designer round 2, confirmed unnecessary to re-run.)
- **O2 — R2's mechanism ("render pins `focused = vis.length−1` at vp 1 + follow") describes today. Settling test** (F3/G4): fresh view, append unit, `poll()`+`render()`; read `(view as any).focused`; render-only stability check. **Status: `closed-red` (as description — no such write exists).** Output: `F3 focusedAfterFirstRender= 0`, `focusedAfterNewUnit= 0`, `G4 renderStable= true`. Render never advances the cursor; R2 is a proposal, not a fact. What render *does* write is only the range clamp — see O11.
- **O3 — R3 (read-only `follow ? tail : vis[fv]`) keeps marker/`e` coherent. Settling test** (H4): 3 collapsed tool units, fresh follow-on `vp=1`; press `e`; compare the vp=1 line against a vp=24 twin showing marker+body. **Status: `closed-red` (divergence is real).** Output: `vp1 before-e: ["→ bash  echo three"]`, `vp1 after-e: ["→ bash  echo three"] focused= 0`, while the vp=24 twin shows `marked= ["▌ → bash  echo one"] bodyUnderMarked= "  one"`. The displayed head (unit 3) is not the unit `e` expands (unit 1), and no `▌` appears. Principal round 2's *factual premise* (A4) is therefore `closed-green` — the divergence exists — but whether it blocks R3 is **comprehension judgment, no test settles it** (owner/designer round 2 openly accept the no-marker cost; principal round 2 does not).
- **O4 — Frozen-grant hazard (principal round 2: explicitly "untested hypothesis"). Settling test** (G1): widget at `termRows 9`, 1 job (grant 2) → `enterProgress` → render → land second manifest → `refresh()` → render; read `activeTranscriptView.viewportRows` and the transcript line. **Status: `closed-red` — hypothesis confirmed as defect.** Output: `layout912= {... "progressLines":2}`, `view.viewportRows(before)= 2`; after: `layout922= {... "progressLines":1}`, `sameView= true view.viewportRows(after)= 2 expectedGrant= 1`, and the single transcript line is `"  streaming output line with many words …"` (untruncated mid-body continuation, no head). The grant shrinks with no resize; the cached view keeps the stale window; the outer `slice(0,1)` masks the count but serves the wrong line. Owner round 2 was right to take re-sync into scope; any projection keyed on the frozen field misses this transition.
- **O5 — Count half (D8: "tautology retained as guard"). Settling tests** (F8 view-level at `(7,5),(8,5),(9,11),(40,11)`; H1 widget budgets at 7/8/9). **Status: `closed-green` in logical lines.** Output: `grant=1 rendered=1 okCount=true` (×3), `grant=23 rendered=4`; widget totals within `tree+sep+progress` at 7/8/9. (H1-tr40 `ok=false` is my probe's slicing artifact — with 1 job only 1 tree row renders against `treeLines=3`; transcript lines 2 ≤ grant 31. Not a code finding.) **Physical-rows half: `closed-red`** — see O6.
- **O6 — Width: `single`-kind head untruncated (`navigator.ts:723`). Settling test** (G3): unpaired `toolResult` with 60-char label, width 40. **Status: `closed-red`.** Output: `row1 vwidth=69 line= "▌ ⎿ aaaa… · 2b"` — 69 cells in a 40-cell viewport: wraps to 2 physical rows, silently spending budget the count half cannot see. Contrast fact **`closed-green`**: toolCall head at width 40 truncates exactly (`G6 row1 vwidth=40`, marker survives). A clamp is needed; site (projection vs `unitLines`) is design judgment, but note G3's over-wide line is row 1 of a multi-row render too — the `unitLines`-site clamp changes multi-row output, the projection-site clamp does not.
- **O7 — Designer round 2's marker-gating hypothesis ("verify whether single-kind `▌` is unconditional — if so, pre-existing multi-row bug"). Settling test** (F5): toolCall + unpaired toolResult, focus on toolCall, vp=24. **Status: `closed-green` — already gated, no bug.** Output: `row1= "▌ → bash  echo one"`, `row2= "⎿ bash · 11b"` (no `▌`), `row3= "  orphan-body"`. The `marker` prefix is `isFocused`-gated for both kinds; the hypothesis is falsified in the good direction.
- **O8 — Head semantics per kind ("reuse of `unitLines` element 0 gives a defined head"). Settling test** (F7 multi-row renders + G3/G6). **Status: split — head definitions `closed-green`, reuse byte-equality `open-untested`.** Multi-row heads exist and are well-defined per kind (`▌ user`, `▌ assistant`, `thinking`, `▌ ⎿ bash · 5b`, `→ <Tool>  <arg>[ ]`). But no projection exists today, so "the one-row line is byte-equal to the multi-row head" names a test against unimplemented code — falsifiable, not yet runnable.
- **O9 — T11 parity at grant 1 + modal inheritance. Settling tests** (G2: inline vs standalone `TranscriptView`, same JSONL/width/grant 1; G5: modal viewport arithmetic). **Status: premise `closed-green`, prediction `open-untested`.** Output: `G2 … equal= true`; `G5 termRows=7 modalVp=6 reaches1=false` (10/24/40 → 6/20/36). Parity holds today; the modal can never reach grant 1 — so "a view-side projection preserves T11 / inherits inertly in the modal" is a sound prediction about future code, not a run result.
- **O10 — Never-empty at grant 1. Settling test** (H2): empty file, vp=1. **Status: `closed-green`.** Output: `count= 1 lines= ["  (waiting for output · idle)"]`.
- **O11 — Q5 scope ("no render-time recompute" vs the existing clamp). Settling test** (H3): vis=[T,A,B], `G` (focused=2, follow off), `t` (vis shrinks to [A,B]), render; read `focused`. **Status: `closed-green` fact, open judgment.** Output: `focusedBeforeShrink= 2`, `focusedAfterT-noRender= 2` (keys don't clamp), `focusedAfterRender= 1 marked= ["▌ → bash  echo B"]`. Render *does* write `focused` today — reconciliation-to-range. Principal round 1's "never written" was factually wrong (conceded round 2); owner round 1's "precedent" cites a real write. Whether R2's follow-writeback is the same class Q5 forbade is **judgment**: Q5's text (`EV-35-design.md:125,272`) bans the never-built `focusedAbsolute`/recompute scheme, and the facilitator correctly records it as binding on EV-35's change set, not as a decided ruling on EV-36. No test settles a scope question.
- **O12 — Pinned vectors. Settling test** (F8 + `computeProgressLayout`): **Status: `closed-green`.** `(7,5)→1, (8,5)→1, (9,11)→1, (40,11)→23` reproduced exactly.

## What I ran
- `/tmp/ev36/probe1.ts` (F1–F8): crux facts, tail-vs-head, focused stability, marker gating, width, per-kind heads, count vectors.
- `/tmp/ev36/probe2.ts` (G1–G6): frozen-grant widget transition, T11-at-1 parity, single-head width (69>40), DOWN/marker movement, modal arithmetic, toolCall truncation.
- `/tmp/ev36/probe3.ts` (H1–H5): widget budgets, empty state, clamp-write, e-vs-display divergence, failed-pair tail accident (`H5 … ["▌ → bash  echo boom ✗"]` — collapsed-tail heads look right by luck, masking the bug for tool-only fixtures).
- `bun test test/ev9-progress.test.ts test/ev34-tool-unit.test.ts test/ev35-transcript-interaction.test.ts` → **39 pass, 0 fail**.

## Verdict
**`blocks`** — not on the card's existence, but on five premises the implementation must not inherit unexamined: (1) the floor shows a mid-body continuation today (O1); (2) the frozen-grant path serves the wrong line at the card's own termRows-9 vector (O4); (3) an over-wide single head breaks the grant in physical rows (O6); (4) under R3 the displayed unit is not the `e` target with no `` (O3/H4) — acceptable only if the card says so explicitly; (5) Q5's scope over an R2-style writeback is undecided judgment, not settled text (O11). Closed-green: count half (logical), empty state, marker gating, per-kind head definitions, T11-at-1 today, modal-never-1, pinned vectors, and the 39-test baseline. Open-untested: projection byte-equality (O8), parity-under-projection and modal-inertness as predictions (O9), and the crux choice itself — R2 vs R3 is a comprehension ruling for product-owner, which my runs constrain but do not settle.

### Step 5 — synthesis (consolidator, job-16.9; verbatim)

Job settled 0.9m / 5 turns, tokens in 19445 / out 4081 / cR 127625 / total 151151, cost≈$0.0630 (catalogue). Verbatim:

## Synthesis — EV-36 step 5

Wiki grounding: `vault/wiki/index.md` → `council-job-tree-inline.md`, `run-transcripts.md`, `council-theme.md`; plus `council/cards/EV-36.md`, `council/cards/EPIC-8.md`, `council/cards/EV-35.md` (step 6 Q5), `vault/raw/2026-09-15-po-epic8-ruling.md` (D9), and the Skeptic's verbatim step-4 output. Code sites cited as recorded by the seats; not independently re-run (read-only).

### Agreed design (settled — by seat convergence *and* a Skeptic result where one exists)

- **R-COPY shape.** The one-row line is EV-34's composed head (`→ <Tool>  <primary-arg>` + `muted "✗"` on failure), reusing `unitLines` element 0; no parallel one-row surface. R-COPY (EPIC-8 Phase 1) is binding and no seat disputed it.
- **Projection location — view-side, in `TranscriptView.render`.** All three generators converged round 2; owner and designer both named a widget-side projection an R-COPY/T11 parity violation.
- **Head semantics per kind.** `user`/`assistant`/`thinking`/unpaired `toolResult` reuse `unitLines` element 0 per kind; no new copy, no per-kind floor renderer (all three agree; Skeptic O8 head-definitions `closed-green`).
- **`computeProgressLayout` untouched.** All three agree; Skeptic O12 `closed-green` reproduces the pinned vectors `(7,5)→1, (8,5)→1, (9,11)→1, (40,11)→23`.
- **Frozen-grant hazard is real and in scope.** Skeptic O4 `closed-red` confirmed the defect: at the card's own `(9,2)` transition the cached view keeps a stale `viewportRows=2` and serves a mid-body continuation; the outer `slice(0,1)` masks the count but not the window. Re-grant per render (a `setViewportRows` mutator or a `render(width, grant)` parameter) is the mechanism; `slice(0, layout.progressLines)` is retained as the redundant count guard.
- **FLLWUP-37 not consumed; stays `Backlog`.** All three agree EV-36 does not consume the overflow case; no fold-in question arises.
- **Logical count half retained as guard** — Skeptic O5 `closed-green`.
- **Never-empty at grant 1** — Skeptic O10 `closed-green` (`(waiting for output · idle)`).
- **Single-kind marker already `isFocused`-gated** — Skeptic O7 `closed-green` (designer round-2 hypothesis falsified in the good direction; no pre-existing multi-row bug).
- **T11 parity holds today at grant 1; modal never reaches grant 1** — Skeptic O9 premise `closed-green`.
- **Baseline suites green** — Skeptic ran `ev9-progress` / `ev34-tool-unit` / `ev35-transcript-interaction` → 39 pass, 0 fail.

### Settled disputes (each with the test that settled it)

- **"Today's floor shows the focused block" (R1-descriptive / status quo)** — Skeptic O1 `closed-red`: at `viewportRows=1` today the line is a wrapped body continuation with no head and no marker. The card's red state is real; no reading under which today is correct survives.
- **"R2 describes today" (render already pins `focused = vis.length−1`)** — Skeptic O2 `closed-red` (as description): render never advances the cursor; `focused` stays 0 across `poll()`/`render()` with new units arriving. R2 is a proposal, not a fact.
- **"Render never writes `focused`" (principal round-1 factual premise)** — Skeptic O11 `closed-green` *fact, against the premise*: render *does* write `focused` today, but only as a reconciliation-to-range clamp (`navigator.ts:796-797`). Principal retracted this round 2; owner's "precedent" cites a real write.
- **Frozen-grant hazard (principal round-2 "untested hypothesis")** — Skeptic O4 `closed-red`: hypothesis confirmed as defect, reachable at the termRows-9 acceptance vector.
- **Single-kind head over-wide** — Skeptic O6 `closed-red`: a 60-char unpaired-`toolResult` label renders 69 cells in a 40-cell viewport, wrapping to 2 physical rows the count half cannot see. A clamp is needed (the toolCall head already truncates correctly, `closed-green` in G6).
- **Pinned vectors** — Skeptic O12 `closed-green`.

### Open judgment — for `product-owner`, escalating to `steward`

1. **The crux — which block the one-row line identifies when `follow` is on: R2 vs R3.** No test can settle this; it is comprehension/contract judgment. After round 2: **owner + designer hold R3** (read-only effective index `follow ? vis[vis.length−1] : vis[fv]`, `focused` never written by the projection, no `▌` on a fresh follow-on one-row view); **principal holds R2** (write `this.focused = vis.length−1` in `render` at the one-row grant under `follow`, so the displayed unit and the `e` target coincide and the marker stays honest). R1 (`starts[focused]`, ignore follow) was conceded by designer round 2. Relevant test results that *constrain but do not settle* it: Skeptic O3/H4 `closed-red` (the R3 divergence is real — displayed head is unit 3 while `e` expands unit 1, no `▌`); Skeptic O2 `closed-red` (R2 is not today's behavior, so it is a real behavior delta, not a no-op); Skeptic O11 `closed-green` fact (render already writes a range clamp, so a write is not categorically forbidden).
2. **Whether Q5's "no render-time recompute" reaches an R2-style writeback (Skeptic O11).** Q5 (EV-35 step 6, `council/cards/EV-35.md`; restated at `docs/superpowers/specs/2026-09-15-EV-35-design.md:272`) is recorded as binding on **EV-35's change set** — it forbade the never-built `focusedAbsolute`/recompute scheme and cursor-stability work. The facilitator recorded Q5's text and principal round-2's scope objection side by side **without adjudicating**. Whether Q5's prohibition extends to R2's follow-scoped `focused` writeback at the one-row grant is open judgment; no test settles a scope question. Principal round 2 argues the existing clamp is precedent that a write is not categorically forbidden; owner/designer round 2 argue R2 is the same class Q5 forbade.
3. **Whether R3's displayed-unit-vs-`e`-target divergence (no `▌` on the fresh follow-on floor) is acceptable (Skeptic O3/H4).** Owner and designer round 2 openly accept the no-marker cost (the marker would lie about the `e` target); principal round 2 does not accept it and holds that the marker must stay honest by making active == focused. Comprehension judgment — settle only on a human read.
4. **Width-clamp site (Skeptic O6).** All three agree a clamp is needed; they disagree on the site. **Owner + designer:** clamp at the `unitLines` single-head site (`navigator.ts:723`) — one source, idempotent with the toolCall clamp, but **changes multi-row output for singles at narrow widths** (gated by ev34/ev35 suites). **Principal:** clamp on the projected one-row line only — does **not** change multi-row output, but is a second clamp. Skeptic O6 records the distinction explicitly: G3's over-wide line is row 1 of a multi-row render too, so the `unitLines`-site clamp changes multi-row output, the projection-site clamp does not. Tradeoff, not fact.

### Open objections (Skeptic, no settling result)

- **O8 — one-row line byte-equal to the multi-row head per kind.** `open-untested`: head definitions are `closed-green`, but no projection exists today, so the byte-equality reuse test names unimplemented code — falsifiable, not yet runnable. Becomes runnable once a projection lands.
- **O9 — T11 parity under the projection and modal-inertness as predictions.** `open-untested`: parity holds today (`closed-green` premise) and the modal never reaches grant 1 (`closed-green`), but "a view-side projection preserves T11 / inherits inertly in the modal" is a prediction about future code, not a run result. Becomes runnable once a projection lands.

### Ready to hand off?

**No.** The facilitator must route the four open-judgment items to **`product-owner`** (escalating to `steward`), without resolving the substance:

1. The crux — R2 vs R3 (which block the one-row line identifies under `follow`).
2. Q5's scope over an R2-style `focused` writeback (Skeptic O11).
3. Acceptability of R3's displayed-unit-vs-`e`-target divergence with no `▌` on the fresh follow-on floor (Skeptic O3/H4).
4. Width-clamp site — `unitLines` single-head site (`:723`, changes multi-row) vs projected line only (does not) (Skeptic O6).

The two open objections (O8, O9) are implementation-gated rather than blocking this routing: they become runnable only after a projection exists, and they should be carried forward as step-9 falsifiers, not as blockers of the ruling. I do not recommend a winner on any of the four; the deliberation left each open, and they leave open here.

### Step 6 — routing: four open-judgment items, no ruling available inside this container

- **Phase 1 rulings checked before routing (per `<escalation_contract>` step 1).**
  **R-COPY** names the one-row line's copy and names "the focused block" as its
  subject, **R-MARKER** pins the glyph, **R-ORDER**/**R-GATES**/
  **R-MODAL**/**R-FOLLOWUP**/**R-ESCALATE** bear on build order, gates, the
  modal, follow-up authority, and the routing rule — none of them decides
  *which* unit is the subject when `follow` is on and the focused index is 0,
  which is the dispute in front of the run. The deliberation put the question
  to the record directly: `principal` round 2 argued D9/R-COPY ruled only the
  *shape* (composed head + status suffix) and not the focus-versus-active
  index; `owner` round 2 argued the opposite and then conceded R3 anyway;
  `designer` round 2 conceded its own R1. The consolidator sorted the crux,
  Q5's reach, the R3 acceptability question, and the clamp site into "open
  judgment — for `product-owner`, escalating to `steward`", and answered
  "Ready to hand off? **No.**"
- **Q5 is a ruling on record but not one that *squarely covers* this dispute.**
  Its text bans a `focusedAbsolute`/recompute scheme and cursor-stability
  work for EV-35's change set; whether it reaches a follow-scoped `focused`
  writeback at the one-row grant in EV-36 is exactly the question
  `principal` round 2 raised and no seat closed. `council/cards/EV-35.md`
  step 6 records Q5 as answering EV-35's residual, and the escalation
  contract forbids extending a ruling to a question it did not actually
  answer. **No ruling is applied here; nothing is decided by inference.**
- **The frozen-grant defect (Skeptic O4 `closed-red`) is not routed** — the
  consolidator placed it in "settled … in scope" because the card's goal
  itself pins the `termRows 9` vector and the "one-row case" and O4 is that
  case at that vector; the mechanism (re-grant per render) is recorded as
  settled along with the retained outer slice.
- **The two `open-untested` objections (O8, O9) are not routed as blockers** —
  per the consolidator they become runnable only once a projection exists,
  and they are carried forward as step-9 falsifiers.
- **Card state: stays `Deliberating`.** No ruling seat may be dispatched from
  this container (`<escalation_contract>`; `R-ESCALATE`), so the run ends this
  turn with an `ESCALATION` report carrying the four questions as facts. Steps
  7 onward do not start on a card with an open-judgment item outstanding.

### Step 6 — ruling (product-owner, job-17), appended verbatim; binding

Resumption: this is a fresh `council-runner` container for EV-36. The prior
runner turn (job-16) completed steps 1–5 and stopped at step 6 with an
`ESCALATION` (record commit `bc7690a`); a settled runner job cannot be
re-entered, so the orchestrator dispatched job-17 (`product-owner`) and
resumed the card with the ruling below. It is appended verbatim, is binding
on every seat, and its amendments are applied as recorded under "Amendments
applied" beneath it.

**Q1 — which unit the one-row line identifies when `follow` is on, and whether the projection may write `focused`.** **R3.** The one-row line is the **effective-indexed unit's composed head** in `TranscriptView.render`, computed **read-only** as `follow ? vis[vis.length − 1] : vis[fv]`; `focused` is never written by the projection; `isFocused` remains the existing `vi === fv` expression (`extensions/navigator.ts:709`), so a fresh follow-on one-row view at `vis.length > 1` carries no `▌`. The marker reappears on the next navigation keypress — nav keys (`up`, `down`, `g`, `G`) already set `follow = false` at `navigator.ts:766-779`, so the divergence lasts exactly until the operator signals they are no longer watching.

Intent amendment text (append to EV-36.md `Intent` verbatim):
> The one-row line identifies the **active block**: under follow mode it tracks the live tail (the operator watching a running seat sees the latest unit's composed head); under navigation it tracks the focused unit (the marker and `e` coincide). At a fresh follow-on view with more than one visible unit the marker is absent — the line is the live tail, not the cursor. Pressing any navigation key flips follow off and the marker reappears on the cursor's unit. The card ships this as the comprehension rule and acknowledges it as the comprehension cost.

Acceptance bullet 2 stands as written; the Intent amendment makes the reading of "active block" the design owns explicit. No goal amendment.

**Q2 — does Q5 reach R2's follow-scoped writeback.** **Yes, it does.** This clarifies Q5 (EV-35 step-6 ruling), not reverses it. The four-item list ("no rename, no `focusedAbsolute`, no `focusedVisible()` getter, no render-time recompute") names the designer's round-1 scheme specifically, but Q5's broader anti-goal — `docs/superpowers/specs/2026-09-15-EV-35-design.md:272`: "No `focusedAbsolute` / recompute (Q5) and no cursor-stability work" — is the binding part for new writes. R2's `this.focused = vis.length − 1` at vp 1 + follow is **cursor-stability work**: it makes `focused` mean different things based on viewport and follow state. Q5 forbids it. R3 (Q1) writes nothing to `focused`; the existing top-only range clamp at `navigator.ts:796-797` (which Q5 explicitly carved out: "the existing render() clamp (top-only, plus the empty-vis guard the Skeptic named) is the only clamp added") is unchanged.

**Q3 — acceptability of R3's divergence (no `▌` on a fresh follow-on floor).** **Acceptable.** This is the comprehension rule the card owns (Intent amendment above). The single-source mapping is *marker = cursor, no marker = watching the live tail*; the operator learns it in one read and it transfers to multi-row (where the marker is always present because the focused unit is always on screen). The cost is one `e` press at fresh view (silent because the body is sliced out by the one-row grant), recoverable by widening. The principal's alternative (R2) is foreclosed by Q2.

**Q4 — width-clamp site.** **`unitLines` single-head site, `extensions/navigator.ts:723`.** One clamp at the single source, idempotent with the toolCall head's existing `truncateToWidth` at `:708`. The rule ("a head line that overflows the granted viewport is wrong at any size") applies at both viewport sizes; the multi-row change is real but is the same rule everywhere, gated by `test/ev34-tool-unit.test.ts` and `test/ev35-transcript-interaction.test.ts`. Skeptic O6 (closed-red) names the defect at both viewport sizes; Skeptic O7 (closed-green) confirms the `single`-kind `` is already `isFocused`-gated, so the clamp site needs no companion marker change.

**Options rejected (Q1):** R1 (`starts[focused]`, ignore follow) — the floor pins the oldest unit while the JSONL streams past; worst comprehension cost. R2 (write `this.focused = vis.length − 1` in `render` at vp 1 + follow) — reach of Q5's anti-goal (cursor-stability work). **(Q2):** Q5 does not reach R2 — treating the four-item list as exhaustive reanimates the class of cursor-stability work Q5 was chosen to avoid. **(Q3):** rejecting the divergence forces R2, foreclosed by Q2. **(Q4):** clamp the projected line only — ships a second clamp and leaves the multi-row single-kind over-wide bug unfixed; worse single-source discipline.

**Grounding:** D9 (`vault/raw/2026-09-15-po-epic8-ruling.md:142-151`) ruled the one-row line's shape, not the index; EPIC-8 R-COPY rules shape/reuse, not the index; EV-36 goal/Acceptance bullet 2 say "identifies the **active block**"; EV-35 Q5 (`council/cards/EV-35.md:880-889, :924-926, :949-951`) and spec `:124-126, :272`; `extensions/navigator.ts:709, :723, :796-797, :766-779`; Skeptic record O1/O3/H4/O6 closed-red, O11 closed-green-fact/open-judgment. No source speaks to whether the fresh follow-on no-marker state is better/worse than R2's always-marker state (a values call); cheapest-to-reverse is R3, and the deciding principle is Q5's binding anti-goal.

**Reversibility:** Q1 flipping to R2 is one render-time write but requires a `steward` reversal of Q5's anti-goal; Q2 narrowing requires amending the EV-35 spec anti-goal (steward escalation); Q3/Q4 trivial.

No Phase 1 ruling is reversed; no other card's acceptance touched; no card declined; no permanent residual accepted (Q3 is a shipped comprehension rule); no `steward` escalation required.

### Step 6 — amendments applied (facilitator)

- **Q1:** the Intent amendment text was appended verbatim to this card's
  `Intent` section above. Acceptance bullet 2 is unchanged; no goal amendment.
- **Q2:** binding — Q5 reaches R2's follow-scoped writeback; R3 writes nothing
  to `focused`; the existing top-only range clamp at `navigator.ts:796-797` is
  unchanged; R2 is out of the design.
- **Q3:** binding — R3's no-`▌` fresh follow-on floor is the shipped
  comprehension rule and the card's stated limitation; it is not an
  `open-untested` residual requiring acceptance.
- **Q4:** binding — the single width clamp lands at the `unitLines` single-head
  site, `extensions/navigator.ts:723`.
- **Settled items from step 5 carry forward unchanged:** view-side projection
  in `TranscriptView.render`; the frozen-grant re-sync in scope; R-COPY
  composed head + `muted "✗"`; per-kind `unitLines` element 0; the outer
  `slice(0, layout.progressLines)` retained as count guard;
  `computeProgressLayout` untouched; the empty-state placeholder retained;
  FLLWUP-37 not consumed and left `Backlog`.
- **Step-9 falsifiers carried from steps 4–5:** O8 (projection byte-equality
  per kind) and O9 (parity-under-projection and modal inertness), both
  `open-untested` until a projection exists; O1 (body-tail red), O3/H4
  (divergence), O4 (frozen grant), O6 (width clamp) as constraints as the
  task directs.
- No Phase 1 ruling is reversed; no `steward` escalation is required. The card
  proceeds to step 7.

### Step 7 — spec

Settled design written to `docs/superpowers/specs/2026-09-15-EV-36-design.md`
(commit `4a62771`), self-reviewed for placeholders, internal consistency,
scope, and ambiguity. It applies the step-6 ruling's Q1–Q4: R3's read-only
effective-index projection (no `focused` write, no `▌` on a fresh follow-on
one-row view, marker returns on the next nav key); re-grant per render
(`setViewportRows`) to fix the frozen-grant defect at the termRows-9 vector;
the single width clamp at `unitLines`' `single`-head site
(`navigator.ts:723`); R-COPY's composed head plus `muted "✗"`; per-kind
`unitLines` element 0; the retained outer `slice(0, layout.progressLines)` as
the count guard; `computeProgressLayout` untouched; the empty-state
placeholder retained; FLLWUP-37 not consumed and left `Backlog`; no new theme
tokens. Handed to a single `owner`. Card and board set `In Progress`;
`validate.py` clean before step 8.

### Step 8 — owner

Single owner dispatched (`job-18.1`, settled 11.7m / 53 turns, total
5137075 tok, cost≈$0.1911 catalogue). Worktree
`/home/tista/codes/pi-council-wt-ev36`, branch `feat/ev-36-one-row-floor`,
plan `docs/superpowers/plans/2026-09-15-EV-36-plan.md`. TDD: the spec's
T1–T9 test file was authored first and the red spectrum observed (7 fail /
3 pass, each failure the predicted mode — body continuation, wrong unit
after `G`, frozen `viewportRows=2`, 82-cell head, inline≠standalone at grant
1), then three implementation commits, each landing green. PR **#50** open
against `main`, head `abe2712bc4617464fa39eb49ece0dfe1495ffe63`. Observed
artifact verified directly: `gh pr view 50` → `state OPEN`, `headRefOid
abe2712…`, base `main`, `mergeable MERGEABLE`; `git ls-remote origin
feat/ev-36-one-row-floor` matches the same SHA. Owner's local gates at the
head: `bash council/preflight.sh EV-36` → `PASS: preflight clean` (no
FLLWUP-27 artifact), `bunx tsc --noEmit` → exit 0, `bun test` → **704 pass /
2 skip / 0 fail** (706 tests, 65 files; integration stays gated), `python3
council/validate.py` → `All council artifacts valid`. No `package.json`
version bump (EPIC-8 close owns it); FLLWUP-37 untouched. Card and board set
`In Review` the moment the PR was observed open; the owner's report of green
gates was **not** used as a precondition for the transition.

### Step 9 — verify by acting

