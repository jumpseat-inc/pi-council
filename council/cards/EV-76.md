---
id: EV-76
title: Run-start preflight fails loud when the gate is on and no OpenRouter credential resolves
state: Done
owner: null
epic: EPIC-14
goal: When a repository's resolved gate mode is not off, the run-start preflight that both /features-deliver Phase 0 and /council step 0 execute fails with a single-line FAIL: naming the decisions gate as the consumer and both remediations unless an OpenRouter credential resolves by the same env-or-stored-credential rule resolveOpenRouterApiKey() applies, the check runs on a packaged, override-resolved path that every consumer reaches without refreshing council/preflight.sh, the mode is obtained by invoking loadGateConfig rather than re-parsing configuration in shell, and the existing unconditional OpenRouter check and the off-mode output stay byte-identical; a test proves the enabled-no-credential run-start FAIL: and the off-mode add-nothing behavior.
---

## Intent

The preflight that matters is the one `/features-deliver` runs at **every run
start** — Phase 0's `bash council/preflight.sh` — and `/council` step 0 runs the
same script. `council/preflight.sh` is data-class and never refreshed by
`/council-update`, so a check that lives only in the scaffold script reaches
fresh consumers and silently misses every repo already `/council-init`-ed.
The check therefore belongs on a **packaged, override-resolved path** the
run-start procedures invoke, so existing consumers get it immediately.

The safety property already partially holds: the existing OpenRouter check is
unconditional and already halts a run when no credential resolves. The delta is
to name the **decisions gate** as the consumer and name **both** remediations
(set `OPENROUTER_API_KEY`, or use the stored `openrouter` `api_key` the runtime
also accepts) — never a symptom-only message, and never a check stricter than
the runtime ([[gate-parity]]).

## Acceptance

- Run-start (`/features-deliver` Phase 0 and `/council` step 0) fails with the
  named single-line `FAIL:` when the resolved mode is not `off` and no credential
  resolves.
- The check reaches **existing** consumers without a `council/preflight.sh`
  refresh (packaged/override-resolved path; it does not depend on the scaffold
  script being updated).
- The mode comes from invoking `loadGateConfig`, not a shell re-parse.
- A stored `openrouter` `api_key` satisfies the check (runtime parity, not
  env-only).
- The existing unconditional OpenRouter check and the off-mode output stay
  byte-identical; `off` adds no new `FAIL:` line.
- If the packaged path and the scaffold script both report, exactly one is
  authoritative — no double `FAIL:`.
## Deliberation record

### Round 1 — owner (job-6.1)

Mechanism: candidate (a) — a packaged bun-runnable check script invoked from the packaged procedure text through a NEW package-root placeholder.

Grounding: `renderProcedure` substitutes only `$COUNCIL_PROCEDURES`/`$ARGUMENTS` (index.ts:146-150); `proceduresDir` returns the override dir whenever it exists (seats.ts:557-560), so `$COUNCIL_PROCEDURES` is NOT a stable package locator; `loadGateConfig` (gate.ts) and `resolveOpenRouterApiKey` (provider-cost.ts:477-483, `PI_CODING_AGENT_DIR` honored at call time — test seam exists at test/provider-cost.test.ts:28-34); `council/preflight.sh` is DATA_FILES (never refreshed); procedures are never scaffolded.

Proposal:
1. New packaged file `council/gate-preflight.ts` (package root, OUTSIDE `council/scaffold/` — never enters the scaffold walk or the TOOLING/DATA set-equality guard). Takes repoRoot (arg or cwd); calls `loadGateConfig(repoRoot)`: mode `off` → exit 0, print nothing; mode non-off and `resolveOpenRouterApiKey() === null` → print ONE line, exit 1: `FAIL: the decisions gate is enabled (gate.mode: <mode> in .council.json) but no OpenRouter credential resolves — set OPENROUTER_API_KEY or store an openrouter api_key credential (pi /login openrouter), then re-run preflight`. Credential resolves → exit 0, print nothing. A `loadGateConfig` throw is caught and printed verbatim (gateFail already single-line). Runtime parity by construction — the check IS `resolveOpenRouterApiKey()`. Bun guaranteed by preflight's existing bun gate; node_modules resolves from the package clone.
2. New placeholder `$COUNCIL_PKG_ROOT` → `PKG_ROOT` in `renderProcedure` (one `.replace`). Candidate (c) defective: `$COUNCIL_PROCEDURES/../` resolves inside the consumer repo for override-dir consumers.
3. Invocation: packaged `council.md` step 0 + `features-deliver.md` Phase 0 gain one line, ordered BEFORE `bash council/preflight.sh`: run the gate check, stop on any `FAIL:` (the stop rule both procedures already state).
4. Exactly one authoritative FAIL is STRUCTURAL by ordering: gate-on + no-cred → gate check FAILs, procedure halts before preflight.sh runs; gate-off + no-cred → gate check silent, existing unconditional check fires byte-identical FAIL; gate-on + cred → silent, preflight normal. Neither preflight script is touched — byte-identity proven by empty diff. Neither scaffold script gains the check (one source of truth for the message).

Rejects candidate (b): session_start is not a halting FAIL at the run-start moment, and misses the first run after a mid-session `/council-gate` enable.

Tradeoffs: FAIL rides LLM-followed procedure prose (same trust class as the existing preflight.sh invocation); gate-on runs lose preflight's other checks when the credential check halts first (fail-fast as designed); `features-new.md` has no preflight step → candidate follow-up, out of scope.

Tests: (1) enabled-no-credential → exit 1, exactly one FAIL line naming decisions gate + both remediations (mkdtemp repoRoot, gate.mode advisory, PI_CODING_AGENT_DIR→empty tmp, env unset); (2) off/absent/`{}` → exit 0, stdout empty; (3) stored-credential parity → exit 0 silent; (4) malformed gate config fails loud single-line; (5) procedure-text pin: invocation present and BEFORE `bash council/preflight.sh` in both packaged procedures + `$COUNCIL_PKG_ROOT` substitution unit incl. override-dir case; (6) byte-identity by empty diff, digest test on offer.

Open (owner): exact FAIL wording (R5 constrains, taste doesn't) — designer/PO to bless; `bash -c` vs direct `bun run` in rendered text; whether scaffold preflight should eventually gain the check (engineering position: no).

### Round 1 — principal (job-6.2)

Reframe: the seam is that the run-start moment is a shell command in LLM-executed procedure text while the authoritative predicates are TS engine functions; the gate-on × no-credential case is exactly where the existing unconditional shell check also fires, so "exactly one authoritative FAIL" is a PROCESS-ORDERING property, not a copy property.

Refutations: (a) as "packaged SHELL script" cannot run loadGateConfig without a TS runtime (notes the scaffold deliberately refuses a language/runtime requirement); (b) session_start cannot halt and creates a second reporting surface (FLLWUP-50 drift notice precedent is non-fatal); (c) `$COUNCIL_PROCEDURES` flips to the override dir whenever it exists.

Proposal — the packaged TS authority becomes the run-start entry point, with the scaffold script as its CALLEE:
1. New `extensions/preflight.ts`, pure core `runStartGatePreflight(repoRoot, { apiKey? }): string | null` — mode ← `loadGateConfig`; off ⇒ null (proceed); else credential ← `apiKey ?? resolveOpenRouterApiKey()`; null ⇒ return single-line literal; else null (proceed).
2. Literal (R5-constrained, newline-sanitized like gateFail): `FAIL: the decisions gate is enabled (mode "<mode>") but no OpenRouter credential resolves — set OPENROUTER_API_KEY, or run /login openrouter so a stored openrouter api_key exists, then re-run preflight`.
3. New parent-only tool `council_preflight` (never in registerHubTools; pattern: registerGateTool/registerRouteTool; structural pin precedent test/gate-render.test.ts:330): on non-null fail, return it and NEVER spawn the script; else spawn `bash council/preflight.sh [cardId]` cwd=repoRoot, streaming via onUpdate.
4. `council.md` step 0 and `features-deliver.md` Phase 0 call the tool instead of the raw bash line.

Exactly one FAIL BY CONSTRUCTION (script never spawned on gate FAIL), not by LLM obedience. Named fallback if wrapped-tool transport is judged to break off-mode byte-identity at the transcript level: tool returns only the gate FAIL, procedure then runs the script (ordering-only guarantee).

Blind spots named: owner blind to `$COUNCIL_PROCEDURES` override-dir flip and to smoke's `cmp` pin on the fixture preflight.sh; "reaches an already-initialized consumer" has only a procedure-text pin, no e2e falsifier; smoke container always holds a credential so gate-on-no-cred is not smoke-reachable.

Tests: truth table on the pure core (active/advisory + apiKey:null → one FAIL line, no \n, no "jev"; off → null; any mode + apiKey → null); runner-spy never-called-on-FAIL (single authority mechanical); byte-identity pins on both preflight.sh OpenRouter blocks; reach pins (procedures name council_preflight; index.ts parent block calls it; hub-tools/child.ts never mention); parity pin (resolver call site is resolveOpenRouterApiKey).

Open (principal): (i) exact literal wording (PO/copy); (ii) whether "off-mode output byte-identical" means the script's lines (satisfied) or the transcript framing (changed by the tool transport) — decides wrapped-tool vs ordering-only fallback; (iii) whether a credential-free smoke arm is owed; (iv) procedure-override repos opt out by convention #5 — "every consumer" = every consumer without a procedure override.

### Round 1 — designer (job-6.4)

Copy proposal (exact literal): `FAIL: decisions gate is enabled but no OpenRouter credential resolved — set OPENROUTER_API_KEY or run /login openrouter in pi, then re-run preflight`. Mirrors the existing preflight OpenRouter FAIL (preflight.sh:114) with the consumer noun swapped to "decisions gate" per R5; house style from gateFail (single line, em-dash, newline-sanitized) and gate-run.ts:216's reason vocabulary ("no OpenRouter API key resolved (OPENROUTER_API_KEY env or stored credential)").

Pass path: SILENT — no new OK line (avoids a redundant second OpenRouter OK; off-mode byte-identity trivially held).

Sequencing (as designer framed it): check sits between the MCP gate and the existing OpenRouter check, with authority to suppress the existing OpenRouter FAIL when mode≠off — i.e., inside the preflight sequence, so the gate-specific message is what the operator sees, not the generic one.

Predictions: P1 exact-literal byte test on the FAIL; P2 off-mode byte-identity (no new line); P3 on-pass output identical to off-pass except the existing OK line; P4 cold-read comprehension probe (reader names "decisions gate" as consumer); P5 first-attempt remediation probe (env path first, slash-command alternative recognized); P6 existing unconditional check byte-preserved; P7 FAIL-count == 1 on enabled-no-credential; P8 literal "jev" absent (R5).

Preferences ranked last: (1) whether to add a third remediation `/council-gate off` — leans no, PO/owner to rule; (2) "credential" vs "API key" — chose "credential" for grammar; (3) sequence position as above; (4) no OK line on pass (taste call); (5) `/login openrouter` (action) vs naming the stored `api_key` artifact — chose the action, notes R5's literal artifact-naming is also defensible.

### Round 2 — owner (job-6.5)

Concedes: its bun-run TS transport (runtime/deps at consumer sites unguaranteed — "the parent pi process is the only TS runtime whose presence at the run-start moment is guaranteed — the principal's in-process direction is correct on transport"); the `$COUNCIL_PKG_ROOT` placeholder; that the principal's stated refutation targeted a strawman (bun-run TS, not a shell re-parse) but the seam concern lands on transport anyway. Holds: single-FAIL by "construction" is conditional on invocation obedience either way — the tool is itself invoked by procedure text, so both designs share the obedience point; the card's byte-identity clause reads LINE-LEVEL (printed output lines, not transcript framing); framing deviation must be recorded, not smuggled.

Merged design endorsed: (1) principal's pure core `runStartGatePreflight` as single authority; (2) parent-only tool `council_preflight` with the principal's own FALLBACK promoted to primary — runs the core, returns FAIL or pass, NEVER spawns the script; procedures keep the existing `bash council/preflight.sh` line unchanged, after the tool; (3) copy with mode inline; (4) designer's in-script sequencing rejected (data-class preflight.sh never reaches existing consumers); (5) no credential-free smoke arm owed; override-repo opt-out recorded.

Tests: truth table; parent-only registration pin; ordering pins (tool line before the unchanged bash line in both procedures); byte-identity by empty diff on scaffold + smoke fixture preflight.sh; non-spawn by construction (no spawn/exec in tool code path).

### Round 2 — designer (job-6.7)

Concedes: mechanism relocation (in-script → pre-script); "re-run preflight" wrong under pre-script placement; artifact-naming over action-naming per R5. Holds: silent pass; single-line em-dash; no "jev"; consumer noun "decisions gate"; gate-first ordering when both MCP and credential broken (credential is cheaper and structurally prior — no key → no model dispatch possible).

FINAL COPY: `FAIL: the decisions gate is enabled but no OpenRouter credential resolved — set OPENROUTER_API_KEY or store an openrouter api_key credential (pi /login openrouter), then re-run`

Predictions revised: P1 byte-exact literal; P2 off-mode empty + preflight.sh byte-identical; P3 on-pass silent; P4 cold-read comprehension; P5 both remediation paths named (action parenthetical); P6 existing check byte-preserved; P7 FAIL-count==1 by construction; P8 no "jev"; P9 transcript contains no preflight.sh OK lines on gate-FAIL; P10 doubly-broken probe (sequential single-line FAILs, never stacked).

Escalation-class notes: (1) no automated e2e falsifier that an already-initialized consumer with stale preflight.sh reaches the gate FAIL — pinned only by procedure-text inspection; (2) trust class is LLM-executed procedure prose — parity with existing preflight.sh invocation accepted, tool-wrap mechanically stronger but not pushed; (3) mode inline — leans no, owner/PO call.

### Round 2 — principal (job-6.8, re-dispatch after job-6.6 stalled at turn 15)

Concedes: round-1 "single FAIL BY CONSTRUCTION, not by LLM obedience" overclaimed — tool invocation remains an obedience point; owning the spawn removes only obedience point (ii) (stop-on-FAIL before the bash line), and the acceptance never names spawning preflight.sh from the tool; larger tool surface (streaming, exit codes, card args) not worth it. Accepts line-level byte-identity reading: with transcript-framing in scope the card's own "reaches existing consumers without refreshing preflight.sh" clause would be unsatisfiable for ANY mechanism — line-level is the only coherent reading; off-mode transcript gains one `council_preflight` tool unit at every run-start, recorded, never called byte-identity.

MERGED DESIGN ENDORSED (all three seats now converge): pure core `runStartGatePreflight(repoRoot, {apiKey?})` + parent-only tool `council_preflight` that never spawns + unchanged `bash council/preflight.sh` line after it + ordering/reach pins.

COPY (principal's merged literal, endorsed): `FAIL: decisions gate is enabled (mode "<mode>") but no OpenRouter credential resolved — set OPENROUTER_API_KEY, or run /login openrouter in pi to store an openrouter api_key credential, then re-run preflight` — names the decisions gate, both remediations, and R5's stored `openrouter` `api_key` artifact; mode inline is a controlled enum, diagnostic, zero injection surface. Byte test: the literal must CONTAIN `openrouter api_key` (owner's literal fails this; designer's drops mode and pi).

Test set (minimal sufficient): truth table on the pure core (byte `===` on the literal; exactly one `FAIL:`; no `\n`; no "jev"; malformed config → single-line verbatim); source-level non-spawn pin (the "spawn never called on FAIL" spy is vacuous once the tool never spawns — the vacuity search is the falsifier); ordering/reach pins (both procedures invoke `council_preflight` before the bash line; parent-block-only registration; absent from hub-tools/child.ts); byte-identity cmp pin on scaffold preflight.sh; FAIL-count covered by the truth table, "script never ran on FAIL" is a procedure-text pin recorded as such.

### Round 2 outcome — positions stabilised

All three seats converge on the merged mechanism: pure TS core (`loadGateConfig` + `resolveOpenRouterApiKey`) exposed via a parent-only non-spawning `council_preflight` tool, invoked by packaged procedure text BEFORE the unchanged `bash council/preflight.sh` line; line-level byte-identity; single-FAIL by ordering conditional on invocation obedience (honestly recorded). Remaining variance is copy taste (mode inline) — R5 delegates exact literal wording to the seat; the principal's merged literal satisfies every R5 constraint and is carried forward as the endorsed literal.

### Step 4 — skeptic attack (job-6.9): no open objections; every load-bearing fact closed-green

- **O1 closed-green** — loadGateConfig truth table ×12: absent/`{}`/no-key/`off` → off; advisory/active resolve; all 6 malformed classes throw exactly one `FAIL:` line, zero newlines (mode enum → `<mode>` inline has zero injection surface).
- **O2 closed-green** — resolveOpenRouterApiKey ×9: env > typed stored api_key > null; oauth-typed/keyless/empty/wrong-provider/no-file → null; PI_CODING_AGENT_DIR honored at call time (dir-switch probe).
- **O3 closed-green** — the scaffold preflight's shell grep is NOT the runtime rule: a concrete auth.json (oauth-typed openrouter + api_key-typed google) passes the shell predicate while the resolver returns null. Resolver-based parity is the only correct predicate; the mechanism's `apiKey ?? resolveOpenRouterApiKey()` is right.
- **O4 closed-green** — scaffold tree is exactly 8 files, no procedures; command scan walks [override, packaged] with PER-FILENAME dedupe, so an override dir holding unrelated files still serves the packaged council.md/features-deliver.md text. renderProcedure substitutes exactly `$COUNCIL_PROCEDURES`/`$ARGUMENTS`.
- **O5 closed-green (precision refinement)** — the recorded opt-out limitation is coarser than the code: only consumers that shadow council.md or features-deliver.md BY FILENAME opt out; an override dir with any other file does not. Neither procedure text uses `$COUNCIL_PROCEDURES`, so the proceduresDir flip is inert for these two texts. Implementer must pin the per-filename semantics.
- **O6 closed-green** — exactly-one-FAIL enumeration over every path: no non-obedience double exists (script's fail() is echo+exit1; gate-on+cred and gate-off paths can't stack; malformed config halts with one line). The recorded ordering-conditional-on-invocation-obedience caveat is honest and complete.
- **O7 closed-green** — smoke cmp pins the fixture-seeded consumer copy only; live/scaffold/fixture preflight drift is pre-existing and intended; mechanism touches neither file → byte-identity vacuous and pin stable; smoke fixture carries the same OpenRouter block (lines 48-54) as a future pin point.
- **O8 closed-green** — import-graph safety: gate.ts/provider-cost.ts/seats.ts pull no hub, no child_process, no network at module level; probes imported them in bare bun processes side-effect-free. A new extensions/preflight.ts importing both is safe (convention 7).
- **O9 closed-green** — council-runner explicitly skips step 0 preflight (runner instruction); enforcement points are parent-side. Parent-only registration precedent (registerGateTool/registerRouteTool; child.ts calls only registerHubTools) is structurally sufficient; parent-only pin test precedent passes (test/gate-render.test.ts:330-337).
- **O10 (probe-class warning, not an objection)** — this machine's shell env carries a live OPENROUTER_API_KEY; every future "enabled-no-credential" test must delete the var (`env -u`) or it silently tests a credentialed world.

Skeptic's implementation-time obligations handed forward: pin per-filename override semantics (O5); failure-injection into the repo's own suite is a branch-time obligation, not a deliberation-time one; the card's test set (truth table, registration pins, ordering/reach pins, byte cmp) is the standing settling test.

### Step 5 — consolidator synthesis (job-6.10)

SETTLED: merged mechanism (pure core `runStartGatePreflight(repoRoot,{apiKey?})` in new `extensions/preflight.ts`; parent-only tool `council_preflight` that never spawns the script; one new procedure line before the unchanged `bash council/preflight.sh` line in council.md step 0 + features-deliver.md Phase 0; silent pass path); in-process transport (parent pi is the only guaranteed TS runtime); line-level byte-identity reading (off-mode transcript gains one tool unit — recorded, never called byte-identity); skeptic O1–O9 all closed-green (test-settled facts); no credential-free smoke arm owed; override opt-out recorded (with O5's per-filename precision).

OPEN JUDGMENT (consolidator routed to product-owner): exact FAIL literal copy — four sub-choices (mode inline, article, trailing phrase, action phrasing) between the principal's and designer's finals. OPEN OBJECTIONS: none.

### Step 6 — routing: open judgment closed by Phase-1 ruling R5 (facilitator, citing R5)

The consolidator's single open-judgment item (exact FAIL literal) is squarely covered by a recorded Phase-1 ruling: **R5 — "the run-start preflight `FAIL:` names the decisions gate as the consumer and both remediations (`OPENROUTER_API_KEY` or the stored `openrouter` `api_key`). Exact literal wording is the seat's within these constraints."** R5 explicitly delegates literal wording to the seat; the seats converged on the principal's merged literal, which the skeptic byte-verified against every R5 constraint (contains `decisions gate`, `OPENROUTER_API_KEY`, `openrouter api_key`; zero `jev`; zero embedded newlines; exactly one `FAIL:`). Ruling applied; endorsed literal adopted:

```
FAIL: decisions gate is enabled (mode "<mode>") but no OpenRouter credential resolved — set OPENROUTER_API_KEY, or run /login openrouter in pi to store an openrouter api_key credential, then re-run preflight
```

No open objections; no `Needs Human` route. Card proceeds to step 7 (spec).

### Step 8 — owner implementation (job-6.11)

PR #95 opened (`feat/ev-76-gate-preflight`), deliverable commit `a9744dc` (plan commit `9faabeb` beneath the spec commit `27eda83`). Files: `extensions/preflight.ts` (new), `extensions/index.ts` (import + parent-block registration), `council/procedures/council.md` + `features-deliver.md` (one step each, before the unchanged script line), `test/preflight.test.ts` (new, 14 tests incl. both card-named tests), plan doc. TDD; O10 hygiene structural (beforeEach deletes OPENROUTER_API_KEY, temp PI_CODING_AGENT_DIR). Owner gates observed at head: preflight PASS exit 0; tsc clean; bun test 1280 pass / 5 skip / 0 fail (111.33s); validate.py clean. Facilitator observed the PR open (gh pr view: OPEN, head a9744dc…) before setting In Review. Record commit `e1a8ef9` pushed (head moves with records per R2).

### Step 9 — skeptic verification (job-6.12): no blocking objections, all green

Re-ran at head worktree e1a8ef9: preflight PASS exit 0; tsc exit 0; bun test 1280/5/0 (109.82s); validate clean. Byte-identity: deliverable diff touches exactly 6 files; all three preflight.sh copies + hub.ts + scaffold/** zero-diff (digests recomputed from base, match test pins). Source: loadGateConfig + resolveOpenRouterApiKey invoked (no re-parse); literal byte-equal to spec for advisory+active; parent-block-only registration; zero spawn/exec surface; procedures order the tool BEFORE the script line with stop-on-FAIL connected. Live probes 15/15: active+no-cred → exact literal; off shapes → null; stored api_key → pass; oauth-typed → FAIL (parity); malformed JSON → single-line loud FAIL; override dir → reach unaffected; empty env → FAIL. Gate-integrity injections (in /tmp, restored): literal typo → 3 red; type error → tsc red; removed superpowers pin → preflight red. Non-blocking notes: (a) stale untracked council/EV-76.md at worktree root (cleaned by facilitator); (b) proceduresDir whole-dir shadowing phrasing nuance; (c) {apiKey:""} seam nit → FLLWUP-92.

### Step 10 — judge verdict (job-6.13): PASS

Basis: fresh verification at e1a8ef9 confirms every goal sub-clause — resolvers invoked not re-parsed; single-line literal naming the decisions gate + both remediations, zero "jev"; no shell execution of preflight.sh anywhere in the tool path; parent-block registration; both procedures integrate the tool before the unchanged script line; all three preflight.sh paths untouched; both card-named tests present and passing; full suite green.

### Steps 11–12 — merge gate and sync

R1 applied: the ORCHESTRATOR performs every merge (`gh pr merge 95 --squash --admin --match-head-commit <X>` against the head SHA its deterministic check read); the card remains In Review with the PR open; step-12 Done + record push are the orchestrator's post-merge acts.

### Step 13 — follow-ups drafted (autonomous draft-then-confirm: orchestrator confirms before the PR merges)

Surfaced-but-not-done, each its own card (drafted on this branch):
- **FLLWUP-89** — /features-new has no run-start preflight step; its seat dispatches share the gate-credential hole (owner round-1 finding, consolidator-carried).
- **FLLWUP-90** — e2e falsifier: stale-preflight consumer reaches the run-start gate-credential FAIL (designer escalation note 1; principal blind-spot; smoke can't reach gate-on-no-cred).
- **FLLWUP-91** — wrapped-tool council_preflight (spawn-on-pass) — the mechanically-stronger single-FAIL design, rejected here as card creep, adjudicated on its own merits (principal round-1; designer/principal round-2 notes).
- **FLLWUP-92** — runStartGatePreflight {apiKey:""} seam nit vs the resolver's empty-means-absent rule (skeptic step-9 note c).
Not carded (converged, recorded): credential-free smoke arm (not owed); O10 env hygiene (implemented structurally in the suite); override opt-out (recorded limitation, R5-adjacent convention #5).

## Run closure (orchestrator, step 11–12)

- **Merged** as `d53db842fe60029e9566df06e139db8ee12dd856` (PR #95, squash).
- Merge check: mode **Deliberate** (`council_route op:authority`, run
  `2026-09-20T08-59-16-719Z-259999-vjf6un`, ROOT `job-6`), all five criteria
  satisfied — criterion 1 owner gates independently re-run by the orchestrator
  on the head worktree `06eaa38e`: preflight PASS, `tsc` exit 0, `bun test`
  1280 pass / 5 skip / 0 fail, `validate.py` clean; criterion 2 `gates`
  workflow `SUCCESS` on `06eaa38e` and again on the merged SHA `d53db842`;
  criterion 3 no blocking skeptic objection (step 9, all acceptance clauses
  green); criterion 4 judge PASS; criterion 5 no `Needs Human`, no outstanding
  ruling.
- Admin merge authorized by EPIC-14 R1, pinned with `--match-head-commit`.
- FLLWUP-89, FLLWUP-90, FLLWUP-91, FLLWUP-92 confirmed by the orchestrator at
  the step-13 gate and merged with the PR.
