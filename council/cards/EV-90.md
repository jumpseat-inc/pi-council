---
id: EV-90
title: Pass renderProcedure-substituted council.md and features-deliver.md into the council-runner dispatch
state: In Review
owner: null
epic: EPIC-23
goal: A `council-runner` dispatch input contains the `renderProcedure`-substituted bodies of `council.md` and `features-deliver.md`, and the `council-runner` seat's `<procedure>` block no longer instructs the runner to read those two files from the procedures directory.
---

## Intent

`buildSystemPrompt` appends only the procedures *directory path* to a seat, so every `council-runner` opens its turn by reading `council.md` and `features-deliver.md` from disk before doing anything — a cost paid on every cold start, including every escalation resumption. The engine already renders procedures with substitution for the parent slash-command path; this card routes the same rendered bodies into the runner's dispatch input and updates the seat block to affirm that its input already carries them. The operator-observable surface is the transcript, not the copy: `/council-tree` today opens a runner with `Read council.md` / `Read features-deliver.md`, and after this change those reads are gone — the first visible actions are the deliberation dispatches. The seat block should say what the input carries rather than prohibit a read, so a later editor does not drift around a negative rule. Nothing else about the dispatch changes.

## Acceptance

1. One exported function composes the runner dispatch input from the resolved procedure bodies under `renderProcedure` substitution (the same `procDir` and `$ARGUMENTS` the parent slash-command path uses — both procedures carry `$ARGUMENTS` but not `$COUNCIL_PROCEDURES`).
2. A test asserts the composed input contains both substituted bodies byte-identical to `renderProcedure`'s output for the same args.
3. The packaged `council/agents/council-runner.md` `<procedure>` block no longer mandates reading `council.md`/`features-deliver.md` in full; a prose pin asserts the read-in-full instruction is gone and names the in-input text instead.
4. `renderProcedure`'s substitution set and `buildSystemPrompt`'s block order are unchanged — no new token, no new block.
5. Everything else about the dispatch is byte-identical: same seat, model, tools, timeouts.
6. `bun test` and `bunx tsc --noEmit` pass.


## Deliberation record

### Round 1 — independent first pass (step 2)

*Dispatches: principal job-8.2 (complete, below); designer job-8.3 returned corrupted output (minimax-m3 stream emitted malformed tool-call text, no position) — re-dispatched once as job-8.5 per dispatch discipline (complete, below); owner job-8.1 elapsed its window mid-work with no position — cancelled and re-dispatched once as job-8.4 per dispatch discipline, which also produced no position (36 turns, zero assistant text blocks). Per dispatch discipline no third owner dispatch; the run HALTed pending owner-seat repair.*

*HALT resolution (orchestrator-recorded, 2026-09-24): owner dispatched a third time as job-9.1 with the same card-only input under an orchestrator-granted 30-minute window — completed (22.3m, 24 turns, in 61419/out 60742/cR 822976/total 945137, cost≈$0.0807 catalogue). Position below, verbatim.*

#### owner (job-9.1)

I have all the grounding I need. Writing the position.

---

## Owner position — EV-90 (first pass, design)

**Wiki pages used:** [[procedure-commands]], [[override-resolution]], [[council-runner]], [[seats]], [[run-transcripts]], [[eval-store-contract]], [[test-suite-budget]].

### Approach

The card's goal is implementable only if the engine can name two things at dispatch time that today exist nowhere in engine code: the **call site** (the runner's input is `params.input`, typed free-text by the orchestrator LLM at `extensions/hub-tools.ts:254`, and *no code composes it today*) and the **substitution values**. My design: one exported pure composer `composeRunnerInput(repoRoot, cardId, taskInput)` in `extensions/seats.ts` (that file, not `index.ts` — `hub-tools.ts` already imports `seats.ts` at line 8; importing `index.ts` from `hub-tools.ts` would create a cycle, so the designer's mild preference for `index.ts` is overruled by the import graph, not taste). It resolves both bodies **per-file, override-first** (walk `[repoOverride, packaged]` by filename, same frontmatter strip as the scan at `index.ts:855` — never `path.join(proceduresDir(repoRoot), name)`, which is directory-level first-hit per `seats.ts:557` and reads the wrong file in any repo with a partial override), renders each with `renderProcedure(body, procDir, args)` where `procDir = proceduresDir(repoRoot)` (byte-for-byte what the parent path uses at `index.ts:833/865`), frames them as `<council-procedure>` / `<features-deliver-overlay>` blocks, and appends the parent's `params.input` **verbatim as a `<task>` tail** — concatenated, never passed through `.replace` (its `$&`/`$n` replacement metacharacters would corrupt arbitrary task text). The call site is one ternary in `hub-tools.ts` guarded on `seat.name === "council-runner"`, with the composed string computed once and reused in **both** `buildChildArgv` sites (initial spawn at line 254 and the retry supervisor's `attemptSpec` at line 278) so every attempt carries identical bytes.

**The one real contract decision (D1): the args are not one value.** `council.md`'s `$ARGUMENTS` is always the card id (`council.md:6,19,71,82,363`); `features-deliver.md`'s is always the epic key (`features-deliver.md:6,68`). AC1's "the same `$ARGUMENTS` the parent slash-command path uses" resolves because there are *two* parent paths, each with its own args: `/council` takes the card id, `/features-deliver` takes the epic. Binding either single value to both bodies writes falsehoods into operative context: card id renders `features-deliver.md` into "You are the orchestrator delivering EV-90" / "every card in EV-90's scope" (false, and it contradicts the runner's own `<role>` block); epic key renders `council.md` into "Open `council/cards/EPIC-23.md`" and "preflight.sh EPIC-23" — lies in the document the runner actually executes. So: `council.md` renders with a new **optional `card_id` param on `council_dispatch`** (fail-loud refusal when absent, same house pattern as the `allowedSeats` refusal at `hub-tools.ts:139`); the epic key is **derived from the card face's `epic:` field** (`council/cards/<cardId>.md`), so a mismatched (card, epic) pair is impossible by construction — the orchestrator can't drift the pair, which is the [[eval-store-contract]] lesson applied to keys. `epic: null` cards are outside any features-deliver scope (per [[card-id-allocation]]/the 2026-09-23 curation consequence), so that derivation failing is a loud error naming the card, never a silent fallback. The orchestrator learns the param from the tool-parameter description (the same way it learned `mode`, `cellId`, `timeout_minutes` — features-deliver.md documents no dispatch params today, so no procedure edit is *required*).

The seat-block rewrite (AC3) is **affirmation, not prohibition**, per the card's Intent: "Your dispatch input opens with two blocks the engine attached — `<council-procedure>` (council.md rendered with your card id in its `$ARGUMENTS` slots) and `<features-deliver-overlay>` (features-deliver.md rendered with the epic key; it addresses the orchestrator — read it as the run's standing substitutions, not your role). The `<task>` block carries your dispatch's task text. Execute the in-input text." The "Skip step 0", environment-facts, and "everything else in council.md applies" prose stays byte-anchored; the escalation step-1 class-list read (`council/procedures/features-deliver.md`'s Phase 1) stays — it's a rare-path pointer to a file still legitimately readable, and AC3 scopes only the `<procedure>` block.

### Tradeoffs accepted

- **Two new optional tool params worth of contract surface** (`card_id` only, plus the derived epic) — accepted because the only alternatives are fragile prose-parsing of `params.input` (a `[A-Z]+-\d+` regex can't distinguish `EV-90` from `EPIC-23` in free text) or LLM-mediated 40KB byte transfer (lossy, untestable). AC5 lists seat/model/tools/timeouts; the param touches none of them.
- **Token savings are honest but small**: the same ~2 bodies enter context either way (Read results today, input tomorrow); the win is two tool round-trips per cold start and per escalation resume, plus the `/council-tree` surface ([[run-transcripts]]) losing the two `Read` units. The card's Intent already says exactly this.
- **Eval path deliberately untouched** (and unpinned): `eval-runner.ts` dispatches the runner via `spawnSeatJob`/`dispatch.ts`, not `council_dispatch` — composing there would change eval-cell inputs and invalidate [[eval-store-contract]] version comparability. Isolation is by reviewed diff (three files: `seats.ts`, `hub-tools.ts`, `council-runner.md` + tests), not an automated pin.
- **No scan refactor**: the scan keeps its own walk; strip-equivalence is test-pinned instead, keeping this card at minimum change.
- **FLLWUP-112 (open) interplay**: the composer is a pure consumer of `renderProcedure` — if 112 widens the substitution set, composed bytes follow automatically and the byte-identity test (which calls the same function) stays green. No coordination needed; no new token (AC4).

### Testable claims

1. **Per-file override (principal's falsifier, adopted):** tmp repo with `$CONFIG_DIR_NAME/council/procedures/` containing only `council.md` (sentinel content) → composed input contains the *override* `council.md` rendering, contains the *packaged* `features-deliver.md` rendering, and does *not* contain the packaged `council.md` sentinel. A `proceduresDir`-join implementation reds.
2. **Args-binding non-tautology (closes AC2's tautology hole):** compose for card `EV-90`, face carrying `epic: EPIC-23`: contains `council/cards/EV-90.md`; does **not** contain `council/cards/EPIC-23.md`; contains "delivering EPIC-23 autonomously"; does **not** contain "delivering EV-90 autonomously". Any single-args implementation reds one clause. Byte-identity against `renderProcedure(...)` for the respective args is asserted *in addition*, not instead.
3. **Verbatim task tail:** `taskInput` containing `$&` and the card id appears exactly once, unmodified, after both bodies; no `argument-hint:`/`description:` frontmatter lines anywhere; no unresolved `$ARGUMENTS` inside the two body blocks (task text excluded).
4. **Epic derivation:** face with `epic: EPIC-9` → composed contains `EPIC-9`; face missing/`epic: null` → throws, message names the card.
5. **AC3 prose pins** (EV-89 precedent, `test/prose.test.ts`): packaged `council-runner.md` no longer contains "read `council.md` and `features-deliver.md` from the procedures directory"; contains the marker names; FLLWUP-107's pin in `test/render.test.ts` still green untouched (AC4).
6. **Retry byte-identity:** composed string computed once, reused by `attemptSpec` — implementation invariant; existing retry tests guard argv shape.
7. **Integration falsifier:** smoke's epic phase ([[smoke-test]]) exercises a real orchestrator→runner dispatch; a stale orchestrator omitting `card_id` gets the loud refusal, retries with it — observable in the smoke transcript.

Placement: composer + tests in `test/render.test.ts`-adjacent new file (pure, ms-scale — [[test-suite-budget]]); gates per AGENTS.md (`bunx tsc --noEmit`, `bun test`, preflight) when implementation follows.

### Round 2 — bounded exchange (step 3)

*Dispatches: owner job-9.2 (complete, below); principal job-9.3 (complete, below); designer job-9.4 (complete, below). Convergence: per-file override-first body resolution, the `hub-tools.ts` call-site guard on `seat.name === "council-runner"`, verbatim `<task>` append, the two-args binding (`council.md` → card id via a new optional `card_id` param on `council_dispatch`; `features-deliver.md` → epic key derived from the card face), AC2's non-tautology fix, and relocating `renderProcedure` into `seats.ts` with an `index.ts` re-export (designer withdrew its `index.ts` preference; owner revised its own `seats.ts` placement after the principal confirmed both original homes cycle). One open dispute: **D1-epic-null** — owner holds fail-loud (throw when the card face's `epic:` is null, message naming the card); principal holds no-throw (append the overlay un-substituted or omit it, so a standalone-card runner dispatch still works, citing AC5). Principal's argument reached owner and designer only via the facilitator's round-2 inputs (parallel dispatch), so neither has responded to it; the dispute is carried to the skeptic and consolidator rather than settled by the exchange.*

#### owner (job-9.2)

Grounding re-checks done — all line citations held (`hub-tools.ts:8` imports `seats.ts`, never `index.ts`; `council.md` `$ARGUMENTS` = card id at lines 6/19/71/82/363; `features-deliver.md` = epic at 6/68; runner block at ~58–64). One re-check corrected *my own* round-1 design; see D3 below.

## Owner position — EV-90, round 2

### Approach

Convergence is high: all three seats agree on per-file override-first resolution, a guarded call site in `hub-tools.ts`, verbatim task append, and AC2's non-tautology fix. What remains is three contract decisions, and one of them exposes a flaw in my own round-1 placement that I'm revising.

**Where I agree.**

- **Principal:** adopted nearly everything in round 1 — per-file override falsifier (their usages.md variant and my council.md sentinel variant are the same test), AC2 non-tautology, verbatim append, frontmatter-strip pin, call-site guard. Their claim 3's negative half (`spawnSeatJob` dispatches carry no bodies) is achieved structurally by the hub-tools ternary; I keep it as a grep/guard pin. We still differ on exactly one thing: their step 3's `readProcedureBody` scan refactor. I still decline — the divergent-regex risk their refactor guards against is already pinned by my claim 3's strip assertion, and touching the live scan path is a bigger diff than this card needs. Settle: nothing — either passes the same tests; it's a diff-size call, and I own diff size.
- **Designer:** the gulf framing and the transcript-level predictions are the best skeptic material in the round — predictions 1 (no procedure-read `toolCall` in the runner's first turn, via `parseTranscript`) and 5 (no re-read) are exactly the out-of-band verification my pure-seam tests can't give, and I adopt both as the skeptic's smoke checks. Their AC3 rewrite philosophy (affirmation, byte-anchored "Skip step 0"/environment prose) matches mine. But their **prediction 3 bakes in the single-args assumption** — `renderProcedure(features-deliver.md-body, procDir, args)` with *the same* args renders "You are the orchestrator delivering EV-90" into the runner's input, a falsehood, per their own D2 reading. Their prediction 4 ("behaviorally identical deliberation output") is also not checkable as stated — runner output is model-generated, not deterministic; I'd drop it in favor of their 1/2/5.

**Disagreements, each on a named contract decision.**

- **D1 — args binding (unresolved; needs an orchestrator ruling, nothing else settles it).** The designer never confronts it: their signature `composeRunnerInput(repoRoot, runnerArgs, taskInput)` has one args value and their prediction 3 applies it to both bodies. My position stands: `council.md` renders with the **card id** (new optional `card_id` param on `council_dispatch`, fail-loud when absent for runner dispatches, per the `allowedSeats` refusal pattern), `features-deliver.md` renders with the **epic key derived from the card face's `epic:` field** (loud error naming the card when `epic:` is null). The designer's signature is also underspecified at the call site: `hub-tools.ts` receives only free-text `params.input`, so without the param, the card id can only come from regex-on-prose — the fragile path my round-1 tradeoffs section rejected. This is a ruling, not a test: claim 2 pins whichever binding wins, but the card should record the decision.
- **D2 — the fallback if the ruling refuses the second arg.** Both the principal and (implicitly) the designer offer "append `features-deliver.md` un-substituted." I hold the fallback is worse than my epic-keyed rendering: unresolved `$ARGUMENTS` slots in operative context violate the card's own goal ("`renderProcedure`-substituted bodies"), and epic-in-council.md falsehoods vs. unrendered-$ARGUMENTS-in-features-deliver is not symmetric — a rendered-with-epic overlay is true prose addressed to the orchestrator (framed as such by the AC3 rewrite), while `$ARGUMENTS` placeholders are simply broken instructions. Settle: testable — my claim 3's "no unresolved `$ARGUMENTS` inside the two body blocks" reds the fallback; if the ruling picks the fallback, that clause must be consciously amended, not silently dropped.
- **D3 — composer home (revising my own round-1 design).** The designer prefers `index.ts`; I overruled that because `hub-tools.ts → index.ts` cycles (`index.ts` line 6 imports hub-tools). But the re-check shows my `seats.ts` placement has the *mirror* problem: `index.ts` line 13 imports `seats.ts`, so a composer in `seats.ts` cannot import `renderProcedure` from `index.ts` without the reverse cycle. Revision: move `renderProcedure` (a 6-line pure function) from `index.ts` into `seats.ts` beside the composer, with `index.ts` re-exporting it so the parent scan path and every existing importer stays green. AC4 is untouched — the substitution set and behavior are byte-identical; only the module home moves. Settle: testable — `bunx tsc --noEmit` green plus a grep pin that `renderProcedure` is defined in exactly one module and re-exported, and the existing `test/render.test.ts` byte-pins unchanged.

### Tradeoffs accepted

- Same as round 1 (one new optional tool param; small honest token win; eval path untouched by reviewed-diff isolation; no scan refactor), plus one new one: **relocating `renderProcedure`** — accepted because the alternative (import cycle, either direction) is worse, and a re-export makes the move invisible to consumers.
- Designer's prediction 4 dropped as unverifiable; the transcript checks 1/2/5 carry the observable surface.

### Testable claims

1. **Override falsifier (unchanged, adopted from principal):** tmp repo, override dir containing only `council.md` sentinel → composed contains override council body + packaged features-deliver body, not the packaged council sentinel. A `proceduresDir`-join implementation reds.
2. **Args-binding non-tautology (unchanged):** card `EV-90` / epic `EPIC-23` → contains `council/cards/EV-90.md`; does not contain `council/cards/EPIC-23.md`; contains "delivering EPIC-23 autonomously"; does not contain "delivering EV-90 autonomously". Byte-identity asserted in addition, per body with its own args. The designer's round-1 design reds the last clause.
3. **Verbatim tail + strip:** `$&`-bearing task text appears exactly once, unmodified; no `argument-hint:`/`description:` lines; no unresolved `$ARGUMENTS` inside either body block (this clause is what reds D2's fallback).
4. **Epic derivation:** `epic: EPIC-9` → composed contains `EPIC-9`; missing/null → throws naming the card.
5. **Placement (new):** `renderProcedure` defined once (in `seats.ts`), re-exported from `index.ts`; `bunx tsc --noEmit` green; existing `test/render.test.ts` pins untouched and passing.
6. **AC3 prose pins + no-double-read:** packaged `council-runner.md` no longer carries the read-in-full instruction; names the in-input blocks; FLLWUP-107 pin green. Skeptic adds the designer's transcript checks 1 and 5 against a smoke run.
7. **Call-site + retry:** guard on `seat.name === "council-runner"` in `hub-tools.ts` only; composed string computed once and reused by both `buildChildArgv` sites (initial + `attemptSpec`); non-runner dispatches and `spawnSeatJob` paths carry no bodies.

Standing by for the orchestrator's ruling on D1 (args binding) — it is the only decision in this card that a test cannot settle by itself.

#### principal (job-9.3)

**Cross-seam reading** — I re-verified the two claims that turn on code, and both are confirmed:

- `index.ts:6` imports `registerHubTools` from `hub-tools.ts`, and `hub-tools.ts:8` imports `seats.ts`; `index.ts:13` imports from `seats.ts`, and `seats.ts` currently imports nothing from `index.ts`. So **both** proposed homes create a new cycle: composer-in-`seats.ts` must import `renderProcedure` from `index.ts` → `seats ↔ index`; composer-in-`index.ts` called from `hub-tools.ts` → `hub-tools ↔ index`. Neither owner nor designer named this; owner's "overruled by the import graph" is right in direction but incomplete, and designer's preference re-introduces the same problem from the other side.
- `council.md` does not dispatch `council-runner` at all (grep: no match); `features-deliver.md` is the only dispatcher (lines 9, 30, 158, 172). `epic: null` exists only on `EPIC-*` faces. So owner's composer guard `seat.name === "council-runner"` fires, in practice, only inside a features-deliver run — where every delivered card carries an `epic:`.

**Blind spots**

- **Owner's vantage** sees the card face's `epic:` field as a safe derivation key but cannot see that its fail-loud `epic: null` throw is *broader than the features-deliver flow*: the guard is on the seat name, so `council_dispatch` of `council-runner` for any epic-less/standalone card now throws where it previously dispatched. That contradicts AC5's "Everything else about the dispatch is byte-identical."
- **Designer's vantage** has the exact blind spot I named in round 1, unchanged: designer's claim 3 asserts both bodies are rendered "for a given `(procDir, args)`" — one args value for both. That is the tautology; the test cannot fail. Designer's claim 4 ("no behavioral change") is smoke-only and not a CI gate.
- **Both** miss the import-graph consequence of where `renderProcedure` lives.

**Reframe** — no new reframe needed; owner's D1 is the correct resolution of AC1's latent defect (the card's single "`$ARGUMENTS`" is actually two: `council.md`→card id, `features-deliver.md`→epic). I concede my round-1 `composeRunnerInput(..., runnerArgs, ...)` under-specified the *source* of the card id; owner's `card_id` param is the only non-fragile source, and I accept it as the contract decision to record. Two boundings, though:

1. **Do not throw on `epic: null`.** Fail loud only on a missing `card_id`; when the face carries no epic, append the `features-deliver.md` overlay un-substituted (or omit it) so dispatching `council-runner` for a standalone card still behaves as before. The "impossible pair" benefit survives; the AC5 regression does not.
2. **Relocate `renderProcedure` to `seats.ts`** (or a leaf `render.ts`), and have `index.ts` import it, so the composer calls it without `seats → index`. AC4's "substitution set unchanged" is preserved (it is a move, not an edit). This makes owner's placement cycle-free with one extra line.

Designer should drop its own claim 3 in favour of the two-args formulation, or it ships a test that cannot red.

**Testable claims**

1. **Epic-null dispatch (settles the one real disagreement):** a `council_dispatch` of `council-runner` for a card whose face has `epic: null` (or a standalone card) — (a) does not throw and still dispatches, (b) carries the `council.md` body rendered with the card id, (c) either omits the overlay or carries it with literal `$ARGUMENTS`. An implementation that throws on epic-null reds.
2. **Guard precision:** a `council_dispatch` of any non-`council-runner` seat, with no `card_id`, succeeds with the un-composed input (byte-identical to today); the same dispatch of `council-runner` without `card_id` throws a message naming the parameter.
3. **Per-file override (owner's claim 1, retained):** tmp repo with a `$CONFIG_DIR_NAME/council/procedures/` containing only `council.md` (sentinel) → composed input has the override `council.md`, the packaged `features-deliver.md`, and not the packaged `council.md`; a `proceduresDir`-join implementation reds.
4. **Two-arg non-tautology:** for card `EV-90` / epic `EPIC-23`, composed contains `council/cards/EV-90.md`, does not contain `council/cards/EPIC-23.md`, and contains "delivering EPIC-23"; a same-args-for-both implementation reds.
5. **No new import cycle (runnable invariant):** `extensions/seats.ts` (or wherever the composer lands) must not contain an import of `./index.ts`, and `hub-tools.ts` must not either. A file-content assertion, cheap to add to the pure test file.

#### designer (job-9.4)

**Design position (revised).** Round 1 missed two real contract seams: `$ARGUMENTS` is *two* values (card id for `council.md`, epic key for `features-deliver.md`), and procedure resolution is *per-file* override-first, not directory-level. Both are now in scope. The seats converged on `composeRunnerInput(repoRoot, cardId, taskInput)` in `extensions/seats.ts` (not `extensions/index.ts` — the import graph is binding, see preferences), called from one ternary in `hub-tools.ts` guarded on `seat.name === "council-runner"`. The seat-block rewrite (AC3) is affirmation with **named affordances** (`<council-procedure>`, `<features-deliver-overlay>`, `<task>`) and an **interpretation instruction** for the orchestrator-voice block — the latter is the design-seat pin that prevents the conceptual-model slip the runner would otherwise make on orchestrator prose.

**Gulf closed.** Evaluation, for the operator opening `/council-tree` (or the in-line `CouncilTreeWidget`) on a fresh runner or an escalation resumption. At the moment "the runner is in flight," the activity row stops reading `ran Read council/procedures/council.md` and reads whatever the runner actually does first — seat-resolution or `validate.py`, then the first deliberation `council_dispatch`. The runner also gains an evaluation gain at cold-start: it sees a *named* in-input layout so it can find and address each block by name without pattern-matching prose.

**Principle and evidence.**

- **Conceptual model (forcing function, addressed to the runner-as-person).** `features-deliver.md` opens "You are the orchestrator delivering `<epic>` autonomously." Rendered into the runner's input with the epic substituted, that line reads as if the runner *is* the orchestrator — contradicting its own `<role>` block and inviting a **mistake** (wrong intention from a wrong model). Owner's seat-block draft handles this with the parenthetical *"addresses the orchestrator — read it as the run's standing substitutions, not your role"* — an **interpretation instruction**, not a redundant reminder. Without it, positive affirmation alone fails on the orchestrator-voice block, and the card's evaluation-gulf closure becomes a comprehension-gulf widening. Apply to the rewritten `<procedure>` block in `council/agents/council-runner.md` (around lines 58–64 per principal's reading).
- **Signifier (named affordances).** `<council-procedure>`, `<features-deliver-overlay>`, `<task>` as labeled seams give the runner discoverability — it can find and address each block by name without parsing prose. Apply to the composer output framing in `composeRunnerInput`.
- **Mapping (input order = working order).** Playbook (`<council-procedure>`), standing substitutions (`<features-deliver-overlay>`), specific work (`<task>`). Top-down reading matches the runner's working order; reversing it would reopen the evaluation gulf at cold-start by meeting the task text before the playbook. Apply to the composer output order.
- **Knowledge in the world beats knowledge in the head (round-1 instrument, retained).** The runner pays two disk round-trips on cold-start to re-load bodies its dispatcher is about to hand it. After: those bodies enter as the dispatch input, computed once and reused by both `buildChildArgv` call sites (`hub-tools.ts:254` initial, `:278` retry supervisor). The runner never needs to know the on-disk paths for `council.md` or `features-deliver.md` at startup — only `<council_runtime>procedures directory: ${procDir}</council_runtime>` remains in the system prompt for any other file the runner may still legitimately read.
- **Constraint (the two-args contract).** Card id and epic key bind *each their own* body. The card id is a new optional `card_id` param on `council_dispatch` (fail-loud when absent, per `hub-tools.ts:139` refusal pattern); the epic key is derived from the card face's `epic:` field, so a mismatched (card, epic) pair is impossible by construction. `epic: null` cards fail loud, never silent. Apply to the composer signature.
- **Constraint (per-file override resolution).** `proceduresDir(repoRoot)` is directory-level first-hit (`seats.ts:557-560`); the scan is per-file (`index.ts:847-886`). The composer must walk `[repoOverride, packaged]` per filename with the same frontmatter strip (`index.ts:855`) — never `path.join(proceduresDir(repoRoot), name)`. Apply to the composer's body-resolution helper.

**Falsifiable predictions** (revised where round 1 was loose):

1. *No procedure-read tool calls at runner startup.* In a fresh `council-runner` dispatch (any card), the transcript's `toolCall` blocks for the first ~10s contain no entry whose `firstArgOf(detail)` ends in `council/procedures/council.md` or `council/procedures/features-deliver.md`. Smoke: `parseTranscript` on the runner session JSONL (`extensions/transcript.ts:54-104`), filter `kind === "toolCall"` with `label === "Read"` and a `council/procedures/<filename>.md` first arg, assert empty before the first dispatch block.
2. *First visible action is seat-resolution or validate.py.* The first `toolCall` block in the runner transcript is one of `Read`/`Glob`/`Bash` against `council/agents/<seat>.md` or `council/agents/`, or `Bash python3 council/validate.py`. Smoke: assert `firstArgOf(block.detail)` is *not* under `council/procedures/` for the runner's first `toolCall` block.
3. *Dispatch input is byte-identical to `renderProcedure`'s output, per body, with the correct args.* Round-1 #3 was tautological; revised: the test asserts the composed string contains `renderProcedure(councilBody, procDir, cardId)` *and* `renderProcedure(featuresBody, procDir, epicKey)` as contiguous substrings in that order, *and* that the load-bearing substituted substrings are present (`council/cards/<cardId>.md` for the procedure body, "delivering <epicKey> autonomously" for the overlay body) and absent for the cross-binding. (Owner's claim 2; closes AC2's tautology hole.)
4. *Per-file override resolution.* Tmp repo with `$CONFIG_DIR_NAME/council/procedures/council.md` (sentinel content) and packaged `features-deliver.md`: composed input contains the override `council.md` rendering, the packaged `features-deliver.md` rendering, and does *not* contain the packaged `council.md` sentinel. A directory-level read reds. (Owner's claim 1.)
5. *No double-read on retry; byte-identical attempts.* Composer computed once, reused by `attemptSpec` (`hub-tools.ts:278`). Both attempts see byte-equal first user-message blocks. Smoke: parse both attempts' user-message blocks; assert byte-equal.
6. *Operator learns `card_id` from the tool schema.* A fixture calling `council_dispatch` for `council-runner` *without* `card_id` is refused with a message naming the missing param and the seat; a fixture *with* `card_id` succeeds.
7. *No orchestrator-voice bleed in the runner's first action.* After receiving the composed input, the runner's first `toolCall` block is not a self-orchestration action (e.g. not `Bash python3 council/orchestrator.py`). This is the interpretation-instruction pin — if the parenthetical were dropped, this would be a probable failure mode in early-use evidence. Out-of-band smoke on a real-card dispatch.

**Preferences, ranked last.**

- *Taste (low weight).* Block names `<council-procedure>`, `<features-deliver-overlay>`, `<task>` name *what the block is*, not *who reads it*. Endorse as drafted — the right axis for discoverability.
- *Taste (low weight).* Fail-loud message for missing `card_id` should name the card being dispatched (per the `hub-tools.ts:139` refusal pattern). Endorse as drafted.
- *Withdrawn.* Round-1 preference for the composer in `extensions/index.ts` over `extensions/seats.ts`. The import graph is binding: `hub-tools.ts:8` already imports `seats.ts`; importing `index.ts` would cycle. Owner's argument is correct.

End of round 2.

#### principal (job-8.2)

Grounding complete. I read both sides of the seam: the renderer and its parent-command call site, the dispatch path that would carry the runner's input, the seat block that mandates the reads, the two procedure bodies, and the override resolver.

## Cross-seam reading

**What the contract is right now, on both sides:**

- `renderProcedure` is `extensions/index.ts:148–153`: `.replace(/\$COUNCIL_PROCEDURES/g, procDir).replace(/\$ARGUMENTS/g, (args ?? "").trim())`. Substitution is per-body; there is no body registry.
- The parent slash-command path (`extensions/index.ts:847–886`) reads procedures as `[repoOverride, packaged]`, **dedupes per filename**, strips frontmatter with `raw.replace(/^---\n[\s\S]*?\n---\n/, "")`, then calls `renderProcedure(body, procDir, args)`. So the parent's *body* is per-file override-first, while `procDir` is `proceduresDir(repoRoot)` (`seats.ts:557–560`) — a **directory-level first hit**.
- The runner's input is `params.input`, passed straight into `buildChildArgv(seat, params.input, …)` at `hub-tools.ts:254` (and `:278` for retries). **No code composes it today** — the parent LLM writes it from prose in `features-deliver.md` Phase 2. `buildChildArgv`/`buildSystemPrompt` must be left alone (AC4/AC5).
- The eval path is separate: `eval-runner.ts:557–565` dispatches `council-runner` via `dispatch.ts`'s `spawnSeatJob`, not `council_dispatch`. Any composition wired into `buildChildArgv` or `buildSystemPrompt` leaks into eval cells; it must live at the `council_dispatch` call site only.
- The runner's `<procedure>` block (`council/agents/council-runner.md`, ~lines 58–64) mandates reading `council.md` and `features-deliver.md` **from the procedures directory named in `<council_runtime>`** — exactly the block AC3 rewrites. `buildSystemPrompt` still appends that dir (`seats.ts:598–604`).
- **`$ARGUMENTS` is not one value across the two bodies.** In `council.md` every use is the **card id** (`council/cards/$ARGUMENTS.md` ×2, `preflight.sh $ARGUMENTS`, "run on card `$ARGUMENTS`"). In `features-deliver.md` every use is the **epic key** (`orchestrator delivering $ARGUMENTS`, "`$ARGUMENTS`'s scope — the epic and every child"). Neither body carries `$COUNCIL_PROCEDURES`, so `procDir` does not affect rendered bytes — it only affects where a naive implementation *looks*.

## Blind spots

- **The engine/owner vantage** sees `params.input` as "the task text" and does not see that the *parent is an LLM*. "The dispatch input contains the bodies" therefore requires a **named call site**; an exported function with a unit test but no caller satisfies AC1/AC2 and fails the goal. The card names neither the call site nor the arg source.
- **The procedure-scan vantage** sees `proceduresDir` as "the procedures dir" and cannot see that it is directory-level while the scan is per-file. `path.join(proceduresDir(repoRoot), "council.md")` reads the right file in this repo (no `.pi/council/procedures`) and **throws or reads wrong in any consumer repo that overrides even one procedure**, because the override dir then wins outright and may not contain `council.md`.
- **The seat-prose vantage** sees `features-deliver.md` as "the autonomous overlay" but cannot see it is written **to the orchestrator**, not the runner. Rendering it with the runner's card id turns "delivering EV-90" / "every card in EV-90's scope — the epic and every child" into a false statement inside the runner's own input.
- **The test-author vantage** cannot see that AC2's byte-identity assertion is **tautological**: if the test picks the args and passes the same args to `renderProcedure`, `composed.includes(render(councilBody, dir, args))` passes even when the implementation bound the *wrong* args to `council.md`. The test as specified has no falsifier for the load-bearing substitution.

## Reframe

The card's shape is right, but AC1's "the same … `$ARGUMENTS` the parent slash-command path uses" is not implementable as a single value, and the implementation seam is unnamed. Concrete design, all inside EV-90:

1. **One helper, called from one place.** Add `composeRunnerInput(repoRoot, runnerArgs, taskInput): string` (pure, exported) in `seats.ts` (near `proceduresDir`), and call it in `hub-tools.ts` immediately before `buildChildArgv` **only when `seat.name === "council-runner"`**, producing the string that becomes `params.input` to the child. Touch neither `buildSystemPrompt`, `buildChildArgv`, nor `dispatch.ts` (AC4/AC5; keeps eval cells untouched).
2. **Bind each body its own args.** `council.md` renders with the **card id**; `features-deliver.md` renders with the **epic key**. If the card truly refuses a second arg, then render only `council.md` and append the `features-deliver.md` overlay **un-substituted** (its `$ARGUMENTS` slots are non-load-bearing prose) — do not render the overlay with a card id. State this in the deliberation record; it is the one real contract decision here.
3. **Resolve bodies per-file, override-first, exactly like the scan.** Extract a `readProcedureBody(repoRoot, name)` helper that walks `[repo-local, packaged]` and applies the same frontmatter strip, and have the command scan use it too — one source for the strip/override seam. Do **not** use `path.join(proceduresDir(repoRoot), name)`.
4. **Append the parent's task text verbatim, do not substitute it into the bodies.** On escalation resume the ruling must still arrive as `taskInput`. (Also avoids feeding free text through `.replace`’s replacement-string `$&`/`$n` metacharacters.)
5. **Rewrite `<procedure>` (AC3)** to say the input carries both bodies (naming the two in-input blocks) and drop the read-in-full instruction; the block may still note `council.md` is the procedure and `features-deliver.md` the overlay, so the runner isn't confused that features-deliver addresses an orchestrator.

## Testable claims

1. **Override falsifier (runnable):** with a temp repo containing `.pi/council/procedures/usages.md` and nothing else, `composeRunnerInput` must contain the **packaged** `council.md` body. A `proceduresDir`-join implementation throws / emits the override-dir body — red.
2. **Args-binding falsifier (runnable):** for card `EV-90`, epic `EPIC-23`, assert the composed input contains `council/cards/EV-90.md`, does **not** contain `council/cards/EPIC-23.md`, and contains an epic-keyed features-deliver sentence. Any single-args implementation reds one clause.
3. **Call-site claim (runnable):** `composeRunnerInput`'s only caller is `hub-tools.ts` guarded on `seat.name === "council-runner"` — a dispatch of a different seat, or of a `council-runner` via `spawnSeatJob`, must not carry the bodies. A test that imports the function without exercising `council_dispatch` leaves the card goal unmet.
4. **AC2 non-tautology:** the byte-identity test must additionally assert the load-bearing substituted substrings (claim 2), or it cannot fail.
5. **Frontmatter strip:** the composed input contains no `description:`/`argument-hint:` frontmatter line — byte-compare the strip against `index.ts`’s scan strip; a divergent regex reds.
6. **No double-inclusion:** the parent's `taskInput` is appended exactly once and the prose in `features-deliver.md`/`council-runner.md` does not instruct the parent to also paste the bodies (`grep` pin).

Wiki pages used: `vault/wiki/index.md`, [[override-resolution]], [[procedure-commands]], [[council-runner]], [[seats]]. Source used: `extensions/index.ts`, `extensions/seats.ts`, `extensions/hub-tools.ts`, `extensions/dispatch.ts`, `extensions/eval-runner.ts`, `council/agents/council-runner.md`, `council/procedures/council.md`, `council/procedures/features-deliver.md`, `council/cards/EV-90.md`, `council/cards/EV-89.md`, `council/cards/EPIC-23.md`, `council/cards/FLLWUP-112.md`, `test/render.test.ts`.

#### designer (job-8.5)

I've read the card, the relevant code (`buildSystemPrompt` in `extensions/seats.ts:598`, `renderProcedure` in `extensions/index.ts:149-153`, the `council-runner.md` `<procedure>` block, the navigator's `activityCopy` in `extensions/navigator.ts:244-260`, the FLLWUP-107/112 boundary, and the transcript surface in `extensions/transcript.ts:33-38`). Writing the position.

---

**Design position.** This card closes the Gulf of Evaluation that today sits between "runner dispatched" and "runner deliberating" — the two `Read council.md` / `Read features-deliver.md` tool calls the runner emits on every cold start are pure noise in `/council-tree`'s verb-first activity row, and they cost a tool round-trip plus two file parses per runner invocation (and again per escalation resumption). The card routes the same `renderProcedure`-substituted bodies the parent slash-command path already uses into the dispatch input, asserts the input is byte-identical to that rendered form, and rewrites the seat's `<procedure>` block to *affirm* what its input carries rather than prohibit a disk read. The smallest change that closes the named gulf.

**Gulf closed.** Evaluation, for the operator opening `/council-tree` (or the in-line `CouncilTreeWidget`) on a fresh runner or an escalation resumption. At the moment "the runner is in flight," the activity row stops reading `ran Read council/procedures/council.md` and reads whatever the runner actually does first — a seat-resolution `Bash` / `Read` of `council/agents/<seat>.md`, then `Bash python3 council/validate.py`, then the first deliberation `council_dispatch`. The "spinning up on procedure files" beat disappears.

**Principle and evidence.**

- **Knowledge in the world beats knowledge in the head.** The runner is currently paying two disk round-trips on cold start to re-load ~hundred-line procedure bodies its dispatcher is already about to hand it. Today: `council/agents/council-runner.md:48-51` (the `<procedure>` opening line: "Before doing anything else, read `council.md` and `features-deliver.md` from the procedures directory named in your `<council_runtime>` system-prompt block, in full"). After: the dispatch input carries both bodies already rendered by `renderProcedure` (`extensions/index.ts:149-153`) with the same `procDir` and `$ARGUMENTS` the parent slash-command path used — and `buildSystemPrompt` (`extensions/seats.ts:598-604`) stays untouched, so `<council_runtime>procedures directory: ${procDir}</council_runtime>` remains the directory pointer for any *other* file the runner may still legitimately read (none currently does at startup, but the pin matters for override-shaped edge cases).
- **Affirmation over prohibition in the seat block.** The card's Intent is explicit and correct: name what the input carries, do not write "do not read X." A negative rule reads as redundant on review and gets deleted by an editor trying to tighten the file; a positive rule ("your dispatch input carries the rendered bodies of `council.md` and `features-deliver.md` — execute its steps under its substitutions") drifts by amendment, not by deletion. The rewrite keeps the substantive prose that follows (skip step 0, two environment facts) byte-anchored and only swaps the first paragraph.
- **No `buildSystemPrompt` change, no new block, no new token.** Acceptance 4 is the seam pin and is the right call: the dispatch input is per-dispatch and per-seat (only `council-runner` needs these bodies); the system prompt is shared and would over-deliver to every `council-runner`-shaped dispatch and to any future seat that aliases it. FLLWUP-112 (open, EPIC-20) owns the substitution-set widening; EV-90 consumes the same `renderProcedure` and adds nothing.

**Falsifiable predictions** (state as hypotheses; each names the smoke or pure-seam assertion a Skeptic could run out-of-band — not a gate):

1. *No procedure-read tool calls at runner startup.* In a fresh `council-runner` dispatch (any card), the transcript's `toolCall` blocks for the first ~10s contain no entry whose `firstArgOf(detail)` ends in `council/procedures/council.md` or `council/procedures/features-deliver.md`. Smoke: load the runner's session JSONL via `parseTranscript` (the pure seam in `extensions/transcript.ts:54-104`), filter to `kind === "toolCall"` with `label === "Read"` and a `council/procedures/<filename>.md` first arg, assert empty for the first N blocks before the first `council_dispatch` block.
2. *First visible action is seat resolution or validate.py, not a procedure read.* The first `toolCall` block in the runner transcript is one of: `Read`/`Glob`/`Bash` against `council/agents/<seat>.md` or `council/agents/` (per `<seat_resolution_check>`, `council-runner.md:99-114`), or `Bash python3 council/validate.py` (per `<board_discipline>`). Smoke: assert `firstArgOf(block.detail)` is *not* a path under `council/procedures/` for the runner's first `toolCall` block.
3. *Dispatch input is byte-identical to `renderProcedure`'s output.* For a given `(procDir, args)`, the new exported composer function returns a string that contains the `renderProcedure(council.md-body, procDir, args)` result and the `renderProcedure(features-deliver.md-body, procDir, args)` result as contiguous substrings, in that order, with whatever framing the function chooses between them. Pure-seam test on the composer; no transcript required. (Acceptance 2.)
4. *No behavioral change to the runner's deliberation output.* For a fixed card and a fixed epic, the runner's `<return_contract>` tag and gate/result content is functionally identical pre/post — the substituted bodies carry the same procedure content as the on-disk reads did. Smoke: rerun a captured fixture's runner and diff the report copy above the card-id line.
5. *No double-read.* The runner does not redundantly `Read council.md` or `Read features-deliver.md` from disk after this change on the same cold start. The seat block's positive affirmation should make this self-enforcing; the falsification is "any runner transcript ever re-reads those two files within its first turn" — i.e. an editor weakened the affirmation to something the runner interpreted as "verify on disk."

**Preferences, ranked last.** *I have no grounded preference against any element of this card.* It is tight, scoped, and the seat-block rewrite philosophy is the right one. If forced to rank taste:

- *Taste (low weight).* The new `<procedure>` opening should probably name the two procedures with their filenames (`council.md`, `features-deliver.md`) rather than with prose — but a prose affirmation that names the *in-input text* (per the card's "names the in-input text instead") is also fine and is what acceptance 3 explicitly asks for. I would not push back on either.
- *Taste (lowest weight).* I mildly prefer the new exported composer to live next to `renderProcedure` in `extensions/index.ts` rather than in `extensions/seats.ts` — it is a renderer-shape helper, not a seat schema concern — but this is naming; either file is defensible.

### Skeptic attack (step 4)

*Dispatch: skeptic job-9.5 (complete, 15.0m, 25 turns). Objections and results below, verbatim. Six structural claims `closed-green`; the AC2-as-written tautology confirmed `closed-green` as a defect in the card as written (all three seats' round-2 positions already adopt the non-tautology fix, so no position is overthrown); D1-epic-null left open as a judgment call with facts attached. One factual error in the deliberation corrected (non-load-bearing).*

#### skeptic (job-9.5)

All probes run, all gates baseline. Compiling the record.

---

## Skeptic report — EV-90, attack round (step 4)

**Grounding:** `vault/wiki/index.md` (procedure-commands, override-resolution, council-runner, seats, eval-store-contract, run-transcripts, card-id-allocation, 2026-09-23 curation) + the card file + actual sources. Every claim below was settled by command output, not source-reading alone.

### Objections

**1. Import-graph constraint (owner/principal claim) — `closed-green`.**
Claim: `hub-tools.ts → seats.ts`, `index.ts → hub-tools.ts` + `index.ts → seats.ts`, `seats.ts` imports nothing local; both naive composer homes cycle; owner D3 (move `renderProcedure` into `seats.ts`, re-export from `index.ts`) is cycle-free.
Settling test: `/tmp/ev90-probe-cycle.ts` — static graph of `extensions/*.ts` imports plus the proposed edges.
Result: all five current edges exactly as claimed; `seats.ts → index.ts` edge and `hub-tools.ts → index.ts` edge each close a cycle (`index` reaches both). D3 adds no edge. Honest caveat: ESM tolerates static cycles when references are used at call time, so "cycle" is a static-graph fact, not a proven runtime crash — the claim as made (static cycle) holds. Also verified: `test/render.test.ts` imports `renderProcedure` from `../extensions/index.ts`, so the re-export is load-bearing for that test.

**2. Two-args claim — `closed-green`.**
Settling test: grep `$ARGUMENTS`/`$COUNCIL_PROCEDURES` in both procedure bodies + frontmatter `argument-hint`.
Result: `council.md` uses at lines 6/19/71/82/363, every one card-scoped (`card \`$ARGUMENTS\``, `council/cards/$ARGUMENTS.md` ×3, `preflight.sh $ARGUMENTS`; `argument-hint: [card-id]`). `features-deliver.md` uses at lines 6/68, both epic-scoped ("delivering \`$ARGUMENTS\`", "every card in \`$ARGUMENTS\`'s scope"; `argument-hint: [EPIC-KEY]`). Neither carries `$COUNCIL_PROCEDURES`. Owner's line citations exact.

**3. Per-file override-first vs directory-level — `closed-green`.**
Settling test: `/tmp/ev90-probe-override.ts` — tmp repo with only `.pi/council/procedures/council.md` (sentinel).
Result: scan-way (per-file `[override, packaged]`, dedupe, strip — `index.ts:837-844`) resolves override sentinel + packaged `features-deliver.md`; `path.join(proceduresDir(repoRoot), name)` resolves the raw **unstripped** override `council.md` and **throws ENOENT** for `features-deliver.md`. The owner/principal warning is accurate: a `proceduresDir`-join composer reds in any partial-override repo. (`proceduresDir` at `seats.ts:557-560` is directory-level first-hit — confirmed.) Minor citation drift, non-substantive: owner cites the strip at `index.ts:855` (actual 844) and the `allowedSeats` refusal at `hub-tools.ts:139` (actual 156-161) — both patterns exist as described.

**4. Call-site claim — `closed-green`.**
Settling test: read `hub-tools.ts` execute body + grep `params.input`; read `dispatch.ts` `spawnSeatJob` + `eval-runner.ts` driver path.
Result: `buildChildArgv(seat, params.input, …)` at exact lines 254 (initial) and 278 (retry `attemptSpec`), identical expression, both using the same `promptFile`/MCP allowlist; grep shows no other use or transformation of `params.input` — nothing composes it today. Eval path: `eval-runner.ts:32` imports `spawnSeatJob` from `./dispatch.ts` (calls at :426/:488); `resolveDriver` (:555) gives `driverSeat: "council-runner"` with fixture command text (`/council EV-1`, `/features-deliver EPIC-1` verified from fixture.json). `spawnSeatJob` (`dispatch.ts:107`) forms its own argv — a hub-tools ternary cannot touch it.

**5. D1-epic-null — facts settled; compliance is a judgment call, not mine to close.**
Settling tests (grep + read):
- Who dispatches `council-runner` today: only `features-deliver.md` prose (`:9, :30, :158` "Dispatch one `council-runner` per card", `:172` escalation resume). `council.md` never mentions it. `:68-69` scopes delivery to "the epic and every child whose `epic:` field names it" — epic-null cards are **excluded from the documented flow** by definition.
- Can an epic-null card reach a runner dispatch today? **Yes, via the tool**: `council_dispatch`'s params are `seat/input/timeout_minutes/stall_minutes/model/thinking/cellId/mode` — no `card_id`, no epic; the execute body (read in full) never touches a card file. A runner dispatch for an epic-null card succeeds today. So the throw posture removes a currently-working (if undocumented) dispatch; no-throw preserves it.
- **Principal's premise is factually false**: "`epic: null` exists only on `EPIC-*` faces" — `EV-45`, `EV-46`, `EV-47` carry `epic: null` in frontmatter (3 non-epic faces). Their conclusion (in the delivery flow, every dispatched card carries an epic) survives independently, so the false premise is non-load-bearing.
- AC5 plain text: "Everything else about the dispatch is byte-identical: same seat, model, tools, timeouts." Neither posture changes seat/model/tools/timeouts. Throw changes whether a dispatch *occurs* for epic-null cards; no-throw changes input bytes (the card's own mandate). Whether "byte-identical" governs occurrence or only the enumerated fields is a reading dispute — **explicitly a judgment call I do not close**. The one fact each side should keep: the throw is a behavior change on a tool capability that exists and works today.

**6. AC2 tautology — `closed-green` (claim confirmed against the real implementation).**
Settling test: `/tmp/ev90-probe-tautology.ts` + `-2` — real packaged bodies, real `renderProcedure` semantics (`index.ts:148-153`), composed with a wrong binding.
Result: composed with card-id bound into `features-deliver.md` → the AC2-as-written assertions (`includes(renderProcedure(body, dir, args))`, "for the same args" per the card's own text) **pass in both binding directions**; the falsehood "delivering \`EV-90\` autonomously" and "every card in \`EV-90\`'s scope" lands in the composed input undetected. Only load-bearing substring assertions (owner claim 2 / designer prediction 3) red it. The card **as written** ships a tautological AC2; the round-2 non-tautology fix is required, not optional.

**7. `<council_runtime>` pointer claim — `closed-green`.**
Settling test: grep + read.
Result: `buildSystemPrompt` at `seats.ts:598-604` (block at :601) appends `` `<council_runtime>\nprocedures directory: ${procDir}\n</council_runtime>` ``; `procDir = proceduresDir(repoRoot)` at both call sites (`hub-tools.ts` prompt build, `dispatch.ts`). The runner seat block references exactly that block (`council-runner.md:56`). The owner's claim and the principal's "still appends that dir (`seats.ts:598-604`)" citation are exact.

### What I ran
- `grep` imports: all `extensions/*.ts` edges (1 command); `$ARGUMENTS`/`$COUNCIL_PROCEDURES` in both procedures; `council-runner` across procedures/extensions/tests; `epic: null` across `council/cards/` (31 files, incl. `EV-45/46/47` frontmatter — principal's premise falsified); `params.input` at 254/278; frontmatter of runner seat; fixture targets via python.
- `bun /tmp/ev90-probe-cycle.ts` → current graph holds; both naive homes cycle; D3 cycle-free.
- `bun /tmp/ev90-probe-tautology.ts` / `-2` → AC2-as-written passes with wrong bindings both directions; falsehood lands in composed input.
- `bun /tmp/ev90-probe-override.ts` → scan-way correct, join-way throws ENOENT / reads unstripped override.
- Baseline gates on the current (Deliberating) tree: `bunx tsc --noEmit` → exit 0; `bun test test/render.test.ts` → 4 pass / 0 fail; `test/prose.test.ts test/child.test.ts` → 37 pass / 0 fail. (`render.test.ts` importing from `index.ts` confirms the D3 re-export is mandatory for AC4-pin continuity.)

### Verdict
**No open objections.** Every focus-area claim was settled by a runnable test: all six structural claims hold (`closed-green`), the AC2 tautology is real and confirmed as a defect in the card as written, and the one factual error in the deliberation is the principal's "`epic: null` exists only on `EPIC-*` faces" — non-load-bearing for its conclusion. What remains on D1-epic-null is a genuine judgment call (does AC5's "byte-identical" govern dispatch occurrence or only the enumerated fields?) that no test can close; the deliberation should record it as a ruling, with the settled fact attached that the throw posture changes a tool capability that works today.

### Synthesis (step 5)

*Dispatch: consolidator job-9.6 (complete, 2.3m, 4 turns). Synthesis below, verbatim.*

#### consolidator (job-9.6)

# EV-90 — Consolidation (step 5)

Grounding: card record read in full; wiki `vault/wiki/index.md` plus the pages the seats cited — [[procedure-commands]], [[override-resolution]], [[council-runner]], [[seats]], [[run-transcripts]], [[eval-store-contract]], [[test-suite-budget]], [[smoke-test]]. This synthesis sorts the record; it adds no position.

## Agreed design

Every seat converged on the following. This is the design, not a dispute.

- **One exported pure composer** `composeRunnerInput(repoRoot, cardId, taskInput)`, living in `extensions/seats.ts` beside `proceduresDir`.
- **One call site**: a ternary in `hub-tools.ts` guarded on `seat.name === "council-runner"`, with the composed string computed once and reused at **both** `buildChildArgv` sites — initial (`hub-tools.ts:254`) and the retry supervisor's `attemptSpec` (`:278`) — so every attempt carries identical bytes.
- **Per-file override-first body resolution**: walk `[repoOverride, packaged]` by filename with the same frontmatter strip as the command scan (`index.ts` scan; owner cited :855, actual :844). Never `path.join(proceduresDir(repoRoot), name)` (`proceduresDir` is directory-level first-hit, `seats.ts:557–560`).
- **Two-args binding**: `council.md` renders with the **card id**, sourced from a new optional `card_id` param on `council_dispatch`; `features-deliver.md` renders with the **epic key**, derived from the card face's `epic:` field so a mismatched (card, epic) pair is impossible by construction. `epic: null` handling is the one open dispute (below).
- **Framing and order**: `<council-procedure>` (playbook), `<features-deliver-overlay>` (standing substitutions), `<task>` (specific work) — designer's named affordances; owner/distributor framing adopted by all.
- **Verbatim task tail**: parent `params.input` appended as the `<task>` tail, concatenated, never passed through `.replace` (avoids `$&`/`$n` replacement metacharacters).
- **`renderProcedure` relocation**: move the 6-line pure function from `index.ts` into `seats.ts`, `index.ts` re-exports it; AC4 substitution set and behavior byte-identical, only the module home moves.
- **AC3 rewrite**: affirmation + interpretation instruction — the overlay block "addresses the orchestrator — read it as the run's standing substitutions, not your role." Names the in-input blocks; byte-anchored "Skip step 0"/environment facts preserved.
- **AC2 non-tautology fix**: byte-identity against `renderProcedure` asserted **in addition to** load-bearing substituted-substring assertions.
- **Eval-path isolation, no systemic-prompt change**: `eval-runner.ts` dispatches via `spawnSeatJob`/`dispatch.ts`, untouched; `buildSystemPrompt` keeps the `<council_runtime>` procedures-directory pointer; no new token/block.

## Settled disputes

Each closed by a skeptic test that actually ran (`closed-green`), or by all-seat agreement where no dispute remained.

1. **Import graph / composer home (`renderProcedure` → `seats.ts`)** — `closed-green`. Test: `/tmp/ev90-probe-cycle.ts`. Result: the five current `extensions/*.ts` edges are exactly as claimed; `seats → index` and `hub-tools → index` each close a cycle; D3 adds no edge; `test/render.test.ts` imports `renderProcedure` from `../extensions/index.ts`, so the re-export is load-bearing. Skeptic caveat on record: this is a static-graph fact; ESM tolerates static cycles referenced at call time, so no runtime crash is proven.
2. **Two-args claim** — `closed-green`. Test: grep `$ARGUMENTS`/`$COUNCIL_PROCEDURES` in both bodies. Result: `council.md` lines 6/19/71/82/363 all card-scoped (`argument-hint: [card-id]`); `features-deliver.md` lines 6/68 both epic-scoped (`argument-hint: [EPIC-KEY]`); neither carries `$COUNCIL_PROCEDURES`. Owner's line citations exact.
3. **Per-file override-first vs directory-level join** — `closed-green`. Test: `/tmp/ev90-probe-override.ts` (tmp repo with only `.pi/council/procedures/council.md` sentinel). Result: scan-way resolves override `council.md` + packaged `features-deliver.md`; `proceduresDir`-join resolves the raw **unstripped** override and **throws ENOENT** for `features-deliver.md`. A join implementation reds in any partial-override repo.
4. **Call-site claim** — `closed-green`. Test: read `hub-tools.ts` execute body + grep `params.input`; read `dispatch.ts`/`eval-runner.ts`. Result: `buildChildArgv(seat, params.input, …)` at exactly :254 and :278, no other transformation of `params.input`; eval path imports `spawnSeatJob` and forms its own argv, so the hub-tools ternary cannot touch eval cells.
5. **AC2 as written is tautological** — `closed-green` (defect confirmed against real bodies/`renderProcedure`). Test: `/tmp/ev90-probe-tautology.ts` and `-2`. Result: with the wrong binding in **either direction**, the AC2-as-written `includes(renderProcedure(body, dir, args))` assertions **pass**; the falsehood "delivering `EV-90` autonomously" lands undetected. Only load-bearing substring assertions red it. The non-tautology fix is required; all three round-2 positions already adopt it.
6. **`<council_runtime>` pointer / AC4 preservation** — `closed-green`. Test: grep + read. Result: `buildSystemPrompt` at `seats.ts:598–604` appends `` procedures directory: ${procDir} ``; runner block references exactly that block. Unchanged by the move.
7. **Epic-null facts (settled portion)** — `closed-green` by grep + read. Result: only `features-deliver.md` dispatches `council-runner` (lines 9/30/158/172); `council.md` never mentions it; epic-null cards are excluded from the documented delivery flow. **But** `council_dispatch` for an epic-null card succeeds today (params `seat/input/timeout_minutes/stall_minutes/model/thinking/cellId/mode` carry no card_id/epic; the execute body never touches a card file), so a fail-loud throw removes a currently-working dispatch. The principal's premise "`epic: null` exists only on `EPIC-*` faces" is **false** (`EV-45`, `EV-46`, `EV-47` carry `epic: null`); the falsity is non-load-bearing for the principal's conclusion.
8. **Designer prediction 4 dropped** — agreed by designer and owner: "behaviorally identical deliberation output" is not checkable (model output is not deterministic).
9. **Baseline gates on the Deliberating tree** — `closed-green`: `bunx tsc --noEmit` exit 0; `test/render.test.ts` 4 pass/0 fail; `prose.test.ts child.test.ts` 37 pass/0 fail.
10. **Minor citation drift corrected, non-substantive** — owner's strip citation `index.ts:855` is actually :844; `allowedSeats` refusal cited at `hub-tools.ts:139` is actually :156–161. Both patterns exist as described.

## Open judgment — for `product-owner`, escalating to `steward`

**D1-epic-null posture — the one dispute no test can settle.** AC5's "Everything else about the dispatch is byte-identical: same seat, model, tools, timeouts" is read two ways. Both sides, equal weight:

- **Owner + designer: fail-loud throw.** When the card face's `epic:` is null, refuse with a message naming the card (the `council_dispatch`/`allowedSeats` refusal-house pattern). Rationale on record (owner D2): un-substituted `$ARGUMENTS` in operative context violates the card's own goal ("`renderProcedure`-substituted bodies"), and owner's claim-3 clause "no unresolved `$ARGUMENTS` inside either body block" would red the alternative; owner holds an epic-rendered overlay would be false prose while unrendered placeholders are simply "broken instructions."
- **Principal: no-throw.** Fail loud only on a missing `card_id`; when the face carries no epic, append the `features-deliver.md` overlay un-substituted (or omit it), so dispatching `council-runner` for a standalone card still behaves as before. Rationale on record: the guard is on seat name, so a throw is **broader than the features-deliver flow** and contradicts AC5's "everything else byte-identical"; the impossible-pair benefit survives without the regression.

Settled facts that bear on it (not a tiebreak): the throw changes a tool capability that works today (item 7 above); AC5's text does not state whether "byte-identical" governs dispatch **occurrence** or only the enumerated fields — the skeptic explicitly declined to close this reading.

## Open objections

**None.** Verified against the record: the skeptic's objections 1–4, 6, 7 are `closed-green`; objection 5 (D1-epic-null) is recorded as a judgment call with facts attached, not as an open objection; the verdict reads "No open objections." The unrun designer smoke predictions (below) are verification material, not skeptic objections — they are not counted here.

## Implementing owner's diff-size discretion

Not routed to any ruling seat; owner holds them.

- **Principal's proposed `readProcedureBody` scan refactor** — owner declined; both forms pass the same tests. Owner: "it's a diff-size call, and I own diff size."
- **Test file placement** (new pure test file adjacent to `test/render.test.ts`).
- **Deferred verification to the verify pass** (owner adopted as skeptic material; not run yet, not blockers): designer predictions **1** (no `council/procedures/<file>.md` `Read` toolCall in the runner's startup transcript), **2** (first visible `toolCall` is not under `council/procedures/`), **5** (no double-read; both retry attempts' user-message blocks byte-equal). These must run in the later verify pass; prediction **4** is dropped (item 8).

## Ready to hand off?

**Yes — to `product-owner`, escalating to `steward` per its criteria**, for the single D1-epic-null ruling. Nothing else blocks: all structural disputes are test-closed, AC2's defect is confirmed and adopted, and there are zero open skeptic objections. The ruling gates only the composer's epic-null/overlay branch and the AC5 compliance reading; the rest of the agreed design can proceed within the implementing owner's diff-size discretion. If the ruling selects the no-throw fallback, owner test-claim 3's "no unresolved `$ARGUMENTS` inside either body block" clause must be consciously amended, not silently dropped.

### Routing (step 6) — ESCALATED

*The consolidator's synthesis routes exactly one open-judgment item: D1-epic-null (owner+designer fail-loud throw vs. principal no-throw/overlay-un-substituted-or-omitted). The facilitator checked the run's Phase 1 rulings (P1-1 run-scoped admin authorization; P1-3 build order) — neither covers this dispute, and no recorded ruling answers it. Per the escalation contract, the ruling is sought from the orchestrator (product-owner, escalating to steward per its criteria): the in-container dispatch of the ruling seat is not permitted. The card remains `Deliberating` pending the ruling; steps 7+ have not started.*

### Ruling (step 6 resumed) — product-owner, D1-epic-null

*The ruling arrived with the resumption dispatch of 2026-09-24 and is appended verbatim, binding on every seat. Phase 1 rulings P1-1 and P1-3 were re-checked against it: neither speaks to this dispute; the ruling is new, not an application of an existing one. The card proceeds to step 7 with the consolidator's synthesis (job-9.6) as the settled design plus this ruling.*

---

- **Ruling** — **D1: fail-loud throw.** When `composeRunnerInput` resolves the card face's `epic:` field and finds it null or absent, the dispatch is refused with an error naming the card, per the house refusal pattern (the `allowedSeats` refusal in `hub-tools.ts:156–161`, the `Unknown seat` hard error). No-throw in either variant (un-substituted overlay or omitted overlay) is rejected. The owner's test-claim 3 clause ("no unresolved `$ARGUMENTS` inside either body block") and test-claim 4 (null/missing epic → throw naming the card) stand **unamended** — the conditional is moot because the ruling selects throw, not no-throw.

- **Options rejected** —
  - *Principal's no-throw, un-substituted overlay:* writes literal `$ARGUMENTS` placeholders into operative context, which fails the card's immutable goal ("`renderProcedure`-substituted bodies") for exactly the dispatches it purports to enable. Meeting the goal would require amending the goal — not mine to do.
  - *Principal's no-throw, omitted overlay:* worse than the first variant. `features-deliver.md` is not decorative context — its authority map is the complete re-homing of the human's reserved powers for the run. A runner dispatched *without* it is an autonomy-shaped dispatch stripped of its authorization context; that is not a capability worth preserving, it is a governance hazard wearing one's clothes. The "currently-working capability" the throw removes is, on the skeptic's own facts, exercised by nothing in the documented flow, nothing in the eval path (`spawnSeatJob` → fixture `/features-deliver EPIC-1`), and nothing in smoke — and its output would be a degraded run in every variant.
  - *Splitting scope (throw only for epic-null, no-throw for standalone):* not offered coherently by either side and functionally identical to the throw — epic-null *is* the standalone case.

- **Grounding** —
  - `council/cards/EV-90.md` — the goal's "renderProcedure-substituted bodies" (immutable, and satisfiable only under throw for occurring dispatches); AC1/AC2 as the card's mandate; the deliberation record (owner D2 rationale, principal's falsified premise, skeptic job-9.5 item 5, consolidator synthesis).
  - `council/procedures/features-deliver.md` Phase 0 — "a seat that does not resolve is a **hard error, not a degraded result**"; this is the house norm the throw follows. Same file, lines 68–69 and 158/172: the documented flow excludes epic-null cards by definition, so the throw breaks no documented path.
  - `vault/wiki/follow-up-backlog-curation.md` (Phase 2) — `epic: null` is a curation side-effect and grouping re-homes orphans; epic-null cards are orphans outside any `/features-deliver` scope, confirming owner's derivation-key reading. The principal's contrary premise ("epic-null only on EPIC-* faces") was falsified by the skeptic (EV-45/46/47), and its conclusion survived only via the flow-exclusion fact — which cuts for throw, not against.
  - `council/cards/EPIC-23.md` Phase 1 rulings — P1-1 and P1-3 checked; neither speaks to this. No vault page addresses derived-key refusal posture; the call rests on the hard-error norm above plus the goal's own text. This card is EPIC-23's own mechanism: the overlay *is* the run's authority map, so preserving an overlay-less runner dispatch serves the tool, not the operator watching `/council-tree`.
  - **AC5 scope (ruled):** "Everything else about the dispatch is byte-identical: same seat, model, tools, timeouts" governs **the enumerated fields only**. "Byte-identical" is a data claim — a dispatch's *occurrence* has no bytes — and the colon construction makes the list appositive: the enumeration defines "everything else." AC5 must also be read against the card's own mandate: the input bytes change for *every* runner dispatch by design (AC1/AC2), so AC5 cannot be the no-change clause the no-throw reading needs. The throw therefore does not violate AC5. The Intent's "Nothing else about the dispatch changes" is prose, not an acceptance bullet, and the acceptance bullets govern.
  - The already-adopted AC2 non-tautology fix is confirmed **required** (skeptic job-9.5 item 6); it stands as part of the agreed design.

- **Reversibility** — Low. Flipping to no-throw later is one composer branch plus one test delta, and this ruling is recorded on a `Deliberating` card, not a merged tree — the cost of being wrong is a re-ruling before step 7 lands, not a shipped regression. The asymmetry favors throw: if throw is wrong, the symptom is a loud, named refusal that an operator reports immediately; if no-throw is wrong, the symptom is a silently degraded autonomous run — the expensive direction to discover.

No steward escalation: this is card-level contract posture; it touches no recorded human decision, does not change the portfolio, and the goal as written is satisfiable under the chosen design. Card EV-90 is clear to proceed to step 7 with the consolidated design as synthesized by job-9.6, with this ruling recorded on the card.

---

*Step 6 closes: zero open objections, the single open-judgment item ruled, zero `Needs Human` items. Proceeding to step 7.*
