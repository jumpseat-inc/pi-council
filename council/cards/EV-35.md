---
id: EV-35
title: Transcript interaction model — visible focus, honest keymap, visible toggles
state: Deliberating
owner: null
epic: EPIC-8
goal: Every key the inline progress transcript advertises in its header has well-defined behavior and the keys that change a rendered line do so, a single-row signifier marks the block that e will expand, and the follow and thinking states are visible in the rendered output, proven by handleInput-driven render assertions
---

## Intent

The user-visible surface is the inline progress transcript's header line and
its focus/expand affordances. Today the header advertises keys the view does
not honor: the arrow cursor is never drawn as a signifier, and the follow
state is named in the header string but not drawn anywhere. The keymap must
be honest — every advertised key has a defined behavior, and the keys that
change a rendered line are asserted to. Keys that clamp correctly at a
boundary (up at the top, down at the bottom) are defined no-ops, not
failures. The `g` and `G` jump keys already work in handleInput and must be
advertised in the header rather than removed.

The focus signifier reuses the council's existing selection marker
(`TREE_ROW_MARKER`, U+258C) so the transcript does not invent a second
idiom. The visible-index versus absolute-index split between the focus
cursor and the expanded set must be resolved so the signifier marks the
block `e` will actually expand.

The full-screen modal transcript is out of scope. Depends on EV-34 for the
composed line shapes.

## Acceptance

- For each key the header advertises, a handleInput-driven test asserts a
  rendered line changes, except boundary clamps, which are asserted no-ops.
- `g` and `G` are advertised in the header and continue to jump.
- The focused block carries the U+258C signifier and it tracks the block
  `e` expands.
- Follow and thinking state changes are visible in the rendered output.
- EV-8's navigation suite stays green.

## Phase 1 ruling (features-deliver, binding for this run)

**R-KEYMAP (EPIC-8)** — the header copy is
`<title> — ↑↓ move · e expand · t thinking · f follow(on) · g/G jump · esc back`;
`g`/`G` are advertised, not removed. **R-MARKER (EPIC-8)** — the focus
signifier is `TREE_ROW_MARKER` (U+258C); no new glyph. These are recorded
human decisions, immutable for the run; a runner that hits this dispute
applies this ruling and cites it rather than re-asking.

## Run record (features-deliver / EV-35)

### Step 1 — classification (facilitator)

- **Path: full council.** The card is design-judgment and spec-ambiguous:
  its goal pins honesty (every advertised key defined; the signifier tracks
  the block `e` expands) but the card's own Intent names an unresolved
  design question — "the visible-index versus absolute-index split between
  the focus cursor and the expanded set must be resolved" — and the
  positions below show three materially different resolutions of it, plus a
  dispute over whether the keymap's honesty must hold through the
  production key gate and through the scroll viewport. R-KEYMAP and
  R-MARKER pin two copy/glyph points; they do not decide the rest. Design
  disputes are open-judgment by construction (council.md step 6), which is
  the definition of a full-council card.
- **Surface-touching: yes.** The delivered artifact is the inline progress
  transcript's header line and focus/expand affordances — every clause of
  the goal is about what a person sees on that surface. Per council.md
  step 1 a surface-touching full-council card seats `designer` as a third
  generator in steps 2–3.
- **Rulings applied:** **R-ORDER** (Phase 1) — predecessor EV-34 is merged
  to local `main` as `7235178` (PR #48, ancestor of `main` at `8f51550`),
  so EV-35 may start, and no later card may start before EV-35's merge SHA
  is on `main`. **R-KEYMAP** / **R-MARKER** (Phase 1) — recorded human
  decisions; applied below, not re-asked. **R-MODAL** (Phase 1) — the
  full-screen modal transcript path is out of scope; EV-35's subject is the
  inline progress transcript hosted by `CouncilTreeWidget.renderProgress` →
  `ensureView` (`extensions/navigator.ts`). **R-GATES** (Phase 1) — the
  owner gate set is `bash council/preflight.sh`, `bunx tsc --noEmit`,
  `bun test`, `python3 council/validate.py`, network integration test gated
  behind `COUNCIL_INTEGRATION=1`.
- **Seat resolution (2026 `<seat_resolution_check>`):** `owner`,
  `principal`, `designer`, `skeptic`, `consolidator`, and `judge` — every
  seat this card's full-council path dispatches — resolve by name from the
  packaged seat set (`council/agents/*.md`; no repo-local override
  directory exists here). No `Unknown seat` error. `product-owner` and
  `steward` are ruling seats the runner never dispatches.
- **Environment note:** step 0 preflight is skipped per the autonomous-run
  substitution; Phase 0 cleared the environment for the epic. The main
  checkout is clean at `8f51550` and equal to `origin/main`
  (`git rev-list --left-right --count origin/main...main` → `0 0`).

### Step 2 — independent first pass (round 1, generators only)

Each seat received the card alone — no other position, no hint. Positions
appended verbatim, labeled by seat. Jobs `job-11.1` (owner, settled 5.9m /
12 turns), `job-11.2` (principal, settled 5.9m / 27 turns), `job-11.3`
(designer, settled 5.9m / 7 turns).

#### Position — `owner` (job-11.1)

**Approach**

The card is three coupled fixes to `TranscriptView` (navigator.ts:610–805) that must land as one render contract, and the current code confirms every claim in the brief: the header at navigator.ts:788 is `${this.title} — ↑↓ move · e expand · t thinking · f follow${this.follow ? "(on)" : ""} · esc back` — no `g/G` advertisement despite `g`/`G` being fully implemented in `handleInput` (navigator.ts:770–774); `focused` is a visible-array index (navigator.ts:618, driven against `vis` at 757/760/761) while `expanded` is keyed by absolute unit index `i` (navigator.ts:616, written at 762 from `vis[this.focused].i`); and `focusLine = starts[this.focused]` (navigator.ts:796) is used only as a scroll anchor, never drawn.

**1. Header copy — apply R-KEYMAP verbatim, keep the dynamic `(on)`.** The ruled copy `…f follow(on) · g/G jump · esc back` matches the on-state rendering; the honest reading is `${this.follow ? "(on)" : ""}` survives and `· g/G jump` is inserted between follow and esc. `g`/`G` need zero behavior change — they already work and set `follow=false` (navigator.ts:770–774).

**2. The one structural change: pin the header as row 0 of the rendered viewport.** Falsifiable prediction about today's code: the header is element 0 of `all` (navigator.ts:787–789) and the viewport slice is `all.slice(topLine, …)` (navigator.ts:800) with `topLine = maxTop = all.length − viewportRows` whenever follow is on (navigator.ts:798) — so **in follow mode with content exceeding the viewport, the entire keymap line, including the `(on)` suffix, is scrolled off-screen.** The follow indicator is invisible in exactly the state it describes, and after `G` (jump to bottom, follow off, navigator.ts:773–774) the header is equally gone. A keymap that scrolls away cannot satisfy "every advertised key has well-defined behavior" as a user-visible contract, and "follow state visible in rendered output" would only be testable on transcripts short enough that the header happens to still be on screen. Pinning makes the invariant trivial: rendered row 0 is always the header; the body window is the remaining `viewportRows − 1` rows, with the existing focus-anchor math (`focusLine − 2`, navigator.ts:799) applied to the body sub-slice. This is the only place I'd spend the card's structural budget; the alternative (a `↓ following` badge in the body) competes with the ruled header copy and still leaves the keymap invisible.

**3. Index/signifier resolution — the focus cursor lives entirely in visible-index space.** Do not key the marker by absolute index. The rule: `focused` is the visible index; `expanded` stays keyed by absolute `i` (that's a feature — toggling `t` re-filters `visible()` (navigator.ts:677–683) and must not collapse the user's expanded set); the signifier is drawn from `starts[this.focused]` — the *same coordinate* `e` reads at navigator.ts:761–762. So "the marker marks the block `e` will expand" holds by construction, not by a mapping that can drift. The failure mode to design against is the opposite choice: a marker keyed by absolute `i` desynchronizes the moment `showThinking` filters units (visible index ≠ absolute index). Render already clamps `focused` into range when `vis` shrinks (navigator.ts:786), which keeps the marker well-defined as blocks stream in.

**4. Marker rendering.** `theme.fg("accent", TREE_ROW_MARKER) + " "` prefixing the focused unit's head line only (body lines never carry it), reusing focus-nav.ts:37 exactly as the tree rows do (navigator.ts renderTree, `▌ ${base}` idiom). Token-only — the theme-compliance grep-audit stays green because we add no ANSI. R-COPY is prefix-safe: ev34's assertions use `.includes("→ bash  echo one")` (test/ev34-tool-unit.test.ts:30–38), which a 2-column prefix doesn't break.

**5. State visibility on a token-only surface.** Follow: the `(on)` suffix in the pinned header (now always visible) plus the scroll-anchor change. Thinking: the ruled copy deliberately gives `t` no suffix because thinking's state *is the body* — thinking units render or vanish (navigator.ts:677–683 filter). Visibility for `t` is a body assertion, not a header assertion. No new glyphs, no new colors.

**6. A real latent bug the signifier exposes.** Down on an empty transcript sets `focused = Math.min(0 − 1, 1) = −1` (navigator.ts:760 with `vis.length === 0`). Today it's unobservable (`starts[-1] ?? 0`, navigator.ts:796), but a marker drawn from `starts[focused]` must not draw from index −1. Fix: when `vis.length === 0`, up/down/e/g/G are consumed no-ops. Falsifiable: fresh view on an empty file, `handleInput(DOWN)`, then draw — with a naive marker this is undefined behavior; with the guard, no marker, no crash.

**7. Known test amendment (forced, not a dodge): T9.** test/ev9-progress.test.ts:262 asserts the joined progress render contains **no** U+258C anywhere — written when progress drew no marker. The transcript's own focused-unit `▌` breaks that join-level assertion while preserving T9's intent (tree rows stay unmarked during progress). Amend T9 to scope the no-marker assertion to the tree-row lines (`lines.slice(0, layout.treeLines)`), and keep :264 (marker restored on tree) untouched. EV-8's own suite (test/ev8-focus-navigation.test.ts) has no joined-progress ▌ assertion — T8 checks tree-surface renders only — so "EV-8's navigation suite stays green" holds literally, no edits there.

**Tradeoffs accepted**

- **One content row lost on overflow.** With the header pinned, an overflowing transcript shows `viewportRows − 1` body rows instead of `viewportRows`. Worth it: the alternative is a keymap/state indicator that is invisible precisely when relevant. computeProgressLayout pins (test/ev9-progress.test.ts:75–83) are untouched — they govern the tree/sep/progress split, not the view's internal slice.
- **The modal path inherits the pin.** `TranscriptView` is shared by the legacy modal (navigator.ts:507 area, openTranscript at the file bottom) and the inline view; R-MODAL keeps the modal *guard* out of scope, but render parity through the shared class is the existing contract (T11, test/ev9-progress.test.ts:206–228 asserts byte-identity inline vs standalone) — both sides pin identically, T11 stays green by construction.
- **Clamp no-ops are not byte-no-ops while follow is on.** Up/down set `follow=false` unconditionally (navigator.ts:756, 759), so "up at the top" flips the header's `(on)` off even as the cursor clamps. I define the clamp no-op as *cursor position unchanged*; the strict byte-identical-render assertion applies only once follow is already off. Stating this now so the runner doesn't write an assertion that's false by design.
- **`e` has defined no-ops the header doesn't enumerate.** `e` on a user/assistant unit (always-expanded bodies, navigator.ts:712–717) or on an unpaired toolCall unit (no result body to reveal, navigator.ts:702–704) is a consumed no-op. Honest keymap ≠ zero no-ops; it means every key's behavior is defined and asserted, including its no-ops.

**Testable claims**

All handleInput-driven against a real `TranscriptView` over fixture JSONL, identity theme, assertions on rendered lines. The suite I'd hold the deliverable to:

1. **Header copy:** `render(80)[0]` contains `g/G jump` and, on a fresh view (follow defaults true), `f follow(on)`. After `f`, `[0]` contains `f follow` and not `follow(on)`.
2. **Header pinned:** 30 blocks, viewportRows 10 → `render(80)[0]` is the header line (falsifies today's behavior, where it is a body line).
3. **Signifier tracks `e`:** three collapsed tool units; render → exactly one line starts with `▌`, and it is unit 0's head; press `e` → the indented body appears directly beneath the *marked* head line, and its content matches unit 0's result. Press DOWN, render → the marker moved to unit 1's head (the single marked line is now unit 1's).
4. **↑/↓ move + change a rendered line:** after `G` (focus last), `↑` → marker moves from unit n to unit n−1's head line (marked-line identity asserted both sides).
5. **Boundary clamps as defined no-ops:** with follow already off (press `f` once first), snapshot the full render at focused=0, press `↑`, render again → **byte-identical**. Mirror at the bottom for `↓`. (With follow on, assert instead: marker stays on unit 0's head while the header legitimately drops `(on)` — the definition from Tradeoffs.)
6. **Empty transcript no-ops:** empty file; DOWN, UP, `e`, `g`, `G` each consumed; render never crashes, never draws a marker, and stays `header + (waiting for output · idle)` (R-COPY empty state intact).
7. **t is body-visible:** fixture with a thinking block; render → no line contains the thinking head; `t` → a rendered line contains it; `t` again → gone. (Header unsuffixed for `t`, per ruled copy — assert `[0]` still says `t thinking`.)
8. **f is doubly visible:** 30 blocks, viewportRows 10, follow on → body pinned to bottom (last rendered body line is the newest block's text); `f` → `(on)` gone from row 0 AND the body window moves to the focus anchor (first body row is no longer the tail).
9. **g/G jump:** from top, `G` → marker on the last unit's head, follow off; then `g` → marker on unit 0's head. Both asserted through rendered marker position, proving the newly advertised keys actually work.
10. **esc back:** navigator.test.ts already asserts close-on-esc (:93–96 region); keep, plus assert the header advertises `esc back` in row 0.
11. **Non-regression:** test/ev34-tool-unit.test.ts, test/ev9-progress.test.ts (T9 as amended per §7), test/navigator.test.ts, test/theme-compliance.test.ts, test/ev8-focus-navigation.test.ts all green; `bunx tsc --noEmit` clean; T11 byte-parity untouched and passing.

No genuine disputes beyond the three flagged (header pinning, T9 amendment, clamp-no-op semantics); each has a concrete resolution above and none requires a new ruling — the header pin is a design consequence of R-KEYMAP's honesty requirement, not a contradiction of it.

#### Position — `principal` (job-11.2)

## Cross-seam reading — what the contract actually is

The advertised keymap and the honored keymap are **three lists in two files**, with no test tying them together:

- the header string, authored in the renderer at `extensions/navigator.ts:788` (pushed raw via `t.bold(...)` — never width-clamped);
- the behavior set, `TranscriptView.handleInput` at `extensions/navigator.ts:753-783`;
- the **production gate**, `classifyProgressKey` at `extensions/focus-nav.ts:62-72`, consumed by `CustomTreeEditor.handleInput` at `focus-nav.ts:286-307`, which forwards only `e/t/f/up/down/g/G` to the view through `viewHost` (`focus-nav.ts:296-297`, wired at `navigator.ts:454`). `esc` and `enter` are consumed by the router and never reach the view (`focus-nav.ts:214-234`).

The signatures right now: the marker exists only for tree rows (`TREE_ROW_MARKER` used once, `navigator.ts:415`, and only when `surface === "tree"`); `g`/`G` are bound (`navigator.ts:772-777`) but unadvertised; `e` targets `vis[this.focused].i` (`navigator.ts:762`); the focus line is `starts[this.focused]` (`navigator.ts:796`) — so **`e` and a correctly-derived marker already share one pointer**. `render` mutates `focused` at `navigator.ts:786`; `follow` gates the slice at `navigator.ts:798-800`, and `up/down/g/G` clear it as a side effect (`:756, :759, :773, :776`).

Two facts about the neighboring cards fix the boundary. `openTranscript` (`navigator.ts:806`) is only reachable through `onActivate`, which the editor never calls (`focus-nav.ts:313-315` calls `enterProgress` instead) — the modal is not merely out of scope (R-MODAL/D10(d)), it is unreachable, so `TranscriptView`'s only live path is `ensureView` (`navigator.ts:437-456`). And EV-36 already owns the follow-vs-active-block reconciliation: its Intent says so in as many words ("follow mode scrolls to the last line… the single visible row is the tail of a wrapped body with no block identity").

## Blind spots, named separately

- **owner (inside `navigator.ts`)**: cannot see that the key it writes a branch for was already classified by a *different predicate* in `focus-nav.ts`. Every unit test feeds literal `"e"`; on a kitty-protocol terminal pi requests flags 7 (`pi-tui/dist/terminal.js:13`), enables on response (`:192-198`), and the TUI hands components the **raw** sequence (`pi-tui/dist/tui.js:723`). `classifyProgressKey` compares `data === "e"` (`focus-nav.ts:67-70`) — that fails for `\x1b[101u`, while `matchesKey(data,"e")` succeeds (`keys.js`, `matchesKittySequence` branch). The gate drops the key, the router forwards it, and the character is typed into the draft buffer behind the progress surface.
- **designer (from the surface)**: designs the keymap for the states where it works and cannot see that `e` is **render-dead on `user`/`assistant` units** — `showBody` is unconditionally true for them (`navigator.ts:717`), so toggling `expanded` changes no line. Nor that the header it is lengthening by 11 columns is unclamped at `navigator.ts:788`.
- **skeptic/judge (from falsifiers)**: green tests here prove nothing about the encoding or the viewport. A short fixture makes the marker visible; a literal-`"e"` fixture makes the gate pass. Both are false comforts.
- **product-owner/steward (from the copy rulings)**: R-KEYMAP pins `t thinking` with no state suffix, so "thinking state visible in the rendered output" has **no channel when a fixture contains no thinking block** — an advert whose only feedback is the absence of lines that were never there.

## Reframe

The card names the risk as the visible-index/absolute-index split; that is the smaller half, and the code already has one pointer for it. The real seam is **the gate's predicate and the glyph's viewport**:

1. **One keymap, one predicate.** The card's honesty claim is only true if classification and consumption use the same matcher. `classifyProgressKey`'s raw equality is the live defect; fix it to `matchesKey` inside this card and assert the kitty-encoded form. Leaving it to a follow-up ships a card whose central claim ("honest keymap") is false on the terminals the repo already wrote a pty smoke harness for.
2. **The signifier is only honest if it is in the granted viewport.** With `follow` on, `topLine = maxTop` (`navigator.ts:798-800`), so on any transcript longer than the viewport the marker line is sliced out of `render()`, and the first `e` expands an off-screen block. EV-35 must therefore assert the marker against the **returned line array** (its contract) and leave the follow-vs-active-block reconciliation to EV-36, whose D9 ruling already picks the mechanism. Do not fix it here: EV-35 pre-empting it would interleave two writers in the same render path under R-ORDER.

Where the design is sound: `▌` on the focused unit's head line derived from `vis[this.focused]`, plus `g/G` added to the ruled header, needs no reframe. The index split resolves the cheap way — keep `focused` as the visible position and derive the marker from the same expression `e` uses; re-keying `focused` to a unit index changes `t`-toggle semantics and is not required by the goal.

## Testable claims

1. `classifyProgressKey("\x1b[101u") === "other"` while `matchesKey("\x1b[101u","e") === true` → the advertised `e` is dropped by the production gate on a kitty terminal.
2. Fresh view, `render(80)`, `handleInput("\x1b[A")`, `render(80)` → outputs **differ** (header loses `(on)`). So the Acceptance bullet "a rendered line changes" is satisfied by the follow suffix alone even at the top clamp — it does not prove focus movement. This also falsifies owner §5's stated settling test ("outputs are equal").
3. Fixture `[thinking, toolCall(result)]` with default `showThinking=false`: `vis=[{i:1}]`, `focused=0`. A marker derived from the unit index lands on the wrong line; one derived from `vis[focused]` lands on the composed head. Press `t` then `e`: the same pointer must still govern both.
4. `e` on a focused `user` or `assistant` unit: rendered lines are byte-identical before/after (`navigator.ts:717`) → `e` is a defined no-op for a large class of blocks; the acceptance must say so, or "e expand" is a false advert.
5. `t` with zero thinking blocks: rendered lines identical → no feedback channel exists (R-KEYMAP cannot grow a suffix). The proof fixture must contain a thinking block or the claim is vacuous.
6. `test/ev9-progress.test.ts:262` asserts the whole progress render contains no `U+258C`; it passes today only because that fixture writes no session file. Add one block and a focused-unit marker → red. Its named intent (no *tree-row* marker off the tree surface) is preserved; the assertion must be narrowed, and that edit is a judgment call on an "EV-8/EV-9 stay green" clause (Acceptance names only EV-8, EPIC-8 names both).
7. `test/ev9-progress.test.ts:267` (T11 parity) compares the inline view to a standalone `TranscriptView` byte-for-byte → the marker must not depend on `controller.surface` or an inline-only constructor flag, or parity breaks.
8. Header width: with a long `title` and the ruled copy, `visibleWidth(render(80)[0]) > 80` (`navigator.ts:788` is unclamped). No card asserts header width; adding 11 columns can push a wrapped header into the row budget EV-36 counts.

Adjacent, not this card's: `ensureView` replaces `viewFor` without disposing the prior view (`navigator.ts:437-456`), leaking a 1s timer and a stale `onChange` per seat switch; and the unreferenced `blockLines` (`navigator.ts:727-750`) is a second head renderer in the same class that EV-35/EV-36 edit — FLLWUP-36, deliberately left `Backlog` by the EV-34 steward ruling.

#### Position — `designer` (job-11.3)

## Design position

EV-35's surface is a small, named-signifier problem on a one-row header and on the head of every block in a 1–31-row inline transcript: four signifiers (the focus marker on the head, the header keymap, the header's follow-suffix state, and the existence of thinking blocks in the body) must each be the **single source of truth** for one piece of state, with no duplication, no silent flip, and no cursor-jump surprise when toggling. The smallest change that satisfies the card is: add a `▌` glyph at column 0 of the head of the **focused unit only**, keep the head itself marker-only (no extra accent/background — token scarcity), render the U+258C **always on the focused unit regardless of scroll position**, recompute `focused` against the absolute-unit index after a `t` toggle so the marker never desyncs from what `e` will expand, and change the header's follow-suffix from a ternary `f follow` / `f follow(on)` to the ruled literal `f follow(on)` rendered with the trailing `(on)` styled as a separate token (`accent` when on, `dim` when off) so the state is honestly visible at one glance without the header copy mutating.

## Gulf closed

- **Evaluation gulf, at the moment of "what will `e` do?"** Today the transcript has no on-screen marker for the unit a keypress will target; the cursor exists only as an invisible `focused` integer. Add `▌` to the head and the question answers itself by reading. (closes)
- **Evaluation gulf, at the moment of "is follow on?"** Today the answer is in the header (`f follow` ↔ `f follow(on)`) but the literal copy changes between two strings; the ruled copy `f follow(on)` is pinned, so the state must be carried by the suffix token, not by copy mutation. (closes)
- **Execution gulf, at the moment of "I just pressed `t`."** Today the focused index doesn't move with the visible list; toggling thinking can leave the marker on a different block than `e` will expand. Track focus by absolute-unit index and the slip disappears. (closes)
- **Execution gulf, at the moment of "what if follow is on and a new block lands?"** Today follow-on is honored by the scroll window, but `t`'s visibility recomputation isn't. (closes)

## Principle and evidence

- **Single source of truth = one signifier per state.** The card lists four state-bits (focus, expand, follow, thinking). Each already has a state slot: `focused` (visible-index today), `expanded` (Set keyed by absolute `i`), `this.follow`, `this.showThinking`. The marker, the header suffix, and the body filter must each read from exactly one of these — no computed flag duplicated across two. Apply at `extensions/navigator.ts:783-805` (the `render` and `unitLines` site) and `extensions/navigator.ts:761-768` (the toggle site). Specifically:
  - `▌` reads `this.focusedAbsolute` (renamed from `focused`).
  - header follow suffix reads `this.follow` via `theme.fg(this.follow ? "accent" : "dim", this.follow ? "(on)" : "(off)")`.
  - `visible()` reads `this.showThinking`.
  - `expanded.has(i)` continues to key by absolute `i` (do not change).

- **Knowledge in the world beats knowledge in the head.** The header pin copy per R-KEYMAP is `<title> — ↑↓ move · e expand · t thinking · f follow(on) · g/G jump · esc back`. The `(on)` literal is part of the published contract. The state-bit must therefore ride the **token** of the suffix, not the presence/absence of parentheses. Apply by replacing the ternary `this.follow ? "(on)" : ""` with a conditional-token styled suffix; the visible cells are always "(on)" or "(off)" so the published copy stays byte-stable.

- **Mapping: head line ↔ cursor position.** The marker must live on the head line of the focused unit only — never on a body line (the body mutates with `e`; the head is the stable identifier). The current `unitLines` already produces `[head, ...body]` for every unit (extensions/navigator.ts:694-733); adding the marker as the first emitted cell of `unitLines` when `u === vis[focused].u` keeps the row → unit mapping one-to-one and avoids tracking a separate "focus line offset" alongside `starts[]`.

- **Constraint: marker on the focused unit, not on the focused visible-slot.** The card calls out the visible-index/absolute-index split explicitly. The fix is one rename plus a recompute hook:
  1. Rename `focused` → `focusedAbsolute: number` (stores the absolute unit index, consistent with `expanded.has(i)`).
  2. Add `focusedVisible(): number` that returns `this.visible().findIndex(v => v.i === this.focusedAbsolute)`, returning 0 if -1 (clamped).
  3. In `render()`, after computing `vis`, call `this.focusedAbsolute = vis[Math.min(this.focusedVisible(), vis.length - 1)]?.i ?? vis[0]?.i ?? 0` (the canonical recompute).
  4. `up`/`down` mutate `focusedAbsolute` to `vis[next].i` (the absolute index of the next visible unit). `g`/`G` mutate to `vis[0].i` and `vis[vis.length - 1].i` respectively.
  5. `t` toggles `showThinking`; the next `render()` recomputes `focusedAbsolute` via step 3 against the new visible list — the focused OBJECT stays the same (e.g., a tool-call unit) even though its visible-slot may shift because thinking blocks were inserted/removed.
  6. `e` toggles `expanded` on `focusedAbsolute` (matches what the marker points at, by construction).

- **Signifier, not affordance, for state bits that are token-rendered.** The marker is rendered with `theme.fg("accent", TREE_ROW_MARKER)` — same call site, same token, same glyph as `extensions/navigator.ts:382` (tree row marker). Reusing the call site is reuse by construction; the marker reads identically wherever the accent token is bound. No new tokens, no new glyphs (per R-MARKER).

- **Forward-unhandled discipline carries.** EV-8 ruling 2 (OJ-2) and the `progress` arm in `extensions/focus-nav.ts:120-140` are binding. EV-35 changes nothing in the routing kernel; the only handleInput change is renaming `focused` to `focusedAbsolute` and threading `i` correctly. The `t` toggle stays a one-line `this.showThinking = !this.showThinking`; the recompute is a render-time invariant, not a handleInput side effect.

- **No new tokens; no literal ANSI.** The follow-suffix token (`accent`/`dim`) and the marker token (`accent`) are existing palette entries, and all emitted color is via `theme.fg(...)` / `theme.bold(...)`. AGENTS.md 9.6 binding holds.

## Concrete shape (the contract owner writes against)

```
// extensions/navigator.ts TranscriptView (after EV-35)

private focusedAbsolute = 0;           // was: focused
private expanded = new Set<number>();  // unchanged: keys are absolute unit indices
private showThinking = false;
private follow = true;

// NEW: recompute the visible-slot lazily; this is the single resolver.
private focusedVisible(): number {
  const vis = this.visible();
  const i = vis.findIndex(v => v.i === this.focusedAbsolute);
  return i < 0 ? Math.max(0, vis.length - 1) : i;
}

// CHANGED handleInput branches:
//   up:   const vis = this.visible();
//         const v = Math.max(0, this.focusedVisible() - 1);
//         this.focusedAbsolute = vis[v]?.i ?? this.focusedAbsolute;
//   down: const vis = this.visible();
//         const v = Math.min(vis.length - 1, this.focusedVisible() + 1);
//         this.focusedAbsolute = vis[v]?.i ?? this.focusedAbsolute;
//   e:    if (vis.length > 0) this.expanded.has(vis[this.focusedVisible()].i)
//           ? this.expanded.delete(vis[this.focusedVisible()].i)
//           : this.expanded.add(vis[this.focusedVisible()].i);
//   t:    this.showThinking = !this.showThinking;
//         (no focusedAbsolute change — render() recomputes)
//   f:    this.follow = !this.follow;
//   g:    this.focusedAbsolute = vis[0]?.i ?? 0; this.follow = false;
//   G:    this.focusedAbsolute = vis[vis.length - 1]?.i ?? 0; this.follow = false;

// NEW in unitLines signature: pass `isFocused: boolean` (no i-based inference).
private unitLines(u: ToolUnit, i: number, width: number, isFocused: boolean): string[] {
  // ... existing head + body computation ...
  // If isFocused: prepend "▌ " styled with theme.fg("accent", TREE_ROW_MARKER) + " "
  // to the head cell (one visible column plus a single space separator).
  // DO NOT alter the head's existing token styling — marker-only.
  // For expanded body lines: do NOT carry the marker down; the marker is a head-only sign.
}

// CHANGED render():
  const vis = this.visible();
  // canonical recompute against the visible list — clamps + stays on the same object
  this.focusedAbsolute = vis[Math.min(this.focusedVisible(), vis.length - 1)]?.i ?? vis[0]?.i ?? 0;
  const fv = this.focusedVisible();
  const followSuffix = this.follow ? "(on)" : "(off)";
  const followToken: ThemeColor = this.follow ? "accent" : "dim";
  const all: string[] = [
    t.bold(`${this.title} — ↑↓ move · e expand · t thinking · f follow`) +
      t.fg(followToken, followSuffix) +
      t.bold(` · g/G jump · esc back`),
  ];
  // ... existing empty-state handling, starts[], all[] accumulation ...
  // Pass isFocused down to unitLines so the head can prepend the marker.
  for (let vi = 0; vi < vis.length; vi++) {
    const { i, u } = vis[vi];
    starts.push(all.length);
    all.push(...this.unitLines(u, i, width, vi === fv));
  }
```

The header is now assembled as three styled segments: the bold keymap (literal copy from R-KEYMAP, minus the follow-suffix), the token-styled suffix, and the trailing bold continuation. The published string is byte-stable: a person reading it sees `…f follow(on)…` when on and `…f follow(off)…` when off — the cells move; the surrounding copy is fixed.

## Falsifiable predictions

I cannot see the running interface. Each claim is a hypothesis the skeptic can run as a pure-seam test.

**Rendering (testable):**

- **R1.** When the transcript renders, exactly one head line carries the U+258C cell at column 0 (styled `theme.fg("accent", TREE_ROW_MARKER)`). No body line carries the marker. *Falsifier:* construct a `TranscriptView` with a fixture containing 3 tool-call units, render at width 80; `findIndex(l => l.startsWith("\u258C"))` returns the line index of the focused unit's head, and `filter(l => l.includes("\u258C")).length === 1`.
- **R2.** After `t` toggles, the marker remains on the **same object** (the unit the user was focused on before the toggle), even if its visible position shifts because a thinking block was inserted or removed. *Falsifier:* construct a fixture with [thinking, toolCall, thinking]; render; press `down` once (focused = toolCall, visible-index 1); press `t` (thinking hidden); assert the marker is still on the toolCall head.
- **R3.** `e` expands the unit the marker points to, by construction. *Falsifier:* same fixture; press `down` twice; press `e`; assert the unit's body lines now appear (count of lines increased by the body's line count) and the marker is still on that unit's head.
- **R4.** The header reads `f follow` plus a token-styled suffix `accent "(on)"` when follow is on, `dim "(off)"` when off. The surrounding keymap copy (the literal from R-KEYMAP) is byte-identical across both states. *Falsifier:* record the bold-styled segments and the suffix segment separately across both `f` states; assert the bold segments are byte-equal and the suffix tokens differ.
- **R5.** At narrow widths (e.g., width=40), the title truncates first, then `g/G jump`, then `esc back` — never the keys that change a rendered line (`e`, `t`, `f`, `↑`, `↓`). *Falsifier:* construct a transcript with title length 60; render at width 40; assert `e expand`, `t thinking`, `f follow`, `↑↓ move`, `esc back` substrings are all present and `g/G jump` may be truncated.
- **R6.** Boundary clamps (`up` at top, `down` at bottom) are defined no-ops: `focusedAbsolute` is unchanged, the marker stays on the same head line, the rendered output is byte-identical before and after. *Falsifier:* render at focused=0; press `up`; assert render output equals pre-press render output. Same for `down` at the last visible index.
- **R7.** `g` and `G` jump to the first and last **visible** unit respectively (not the first/last absolute unit, because thinking-hidden would otherwise leak). After `g`, `focusedAbsolute = vis[0].i`; after `G`, `focusedAbsolute = vis[vis.length - 1].i`. *Falsifier:* fixture with [thinking, toolCall]; with `showThinking=false`; press `G`; assert focused is the toolCall, not a thinking unit.
- **R8.** The marker tracks the focused unit across the scroll window. When `follow` is off and the user scrolls, the focused unit's marker remains on the line where its head sits; the `topLine` computation keeps `focusLine - 2` ≤ `topLine` ≤ `focusLine` so the marker is always within the viewport. *Falsifier:* 200-block fixture; scroll up 5 units; press `down` once; assert the focused head line is within `[topLine, topLine + viewportRows)`.
- **R9.** No new theme tokens. The marker uses `accent`; the follow suffix uses `accent` and `dim`. *Falsifier:* a grep of `theme.(fg|bg|bold)("…")` call sites in `extensions/navigator.ts` after the change lists only tokens from the documented palette (`accent`, `border`, `customMessageBg`, `dim`, `success`, `warning`, `muted`, `bold`).
- **R10.** The marker is rendered with `TREE_ROW_MARKER` (U+258C) by name — same constant as `extensions/focus-nav.ts:37`. No new glyph constant is introduced. *Falsifier:* a grep for `TREE_ROW_MARKER` in `extensions/navigator.ts` shows at least one new use inside `TranscriptView.unitLines`, and there is no literal `"\u258C"` outside the constant.
- **R11.** EV-8 / EV-9 suites stay green: `test/ev8-*.test.ts` and `test/ev9-progress.test.ts` all pass after the rename.

**Comprehension (judgment — settles only on a human read):**

- **C1.** A first-time visitor who has just opened inline progress can tell (a) what their keys target (the `▌` head), (b) whether follow is on (the `accent "(on)"` cell on the header line, visually brighter than the surrounding keymap), and (c) whether thinking is shown (count of head shapes in the body). All three are on-screen without reading any docs.
- **C2.** When the user toggles `t`, the marker does not jump; the unit they were watching stays watched. When they press `e`, the unit the marker points to expands. The mapping is stable.
- **C3.** The header's three-segment styling (bold / token-suffix / bold) reads as one sentence to the eye — the suffix is part of the keymap, not an annotation.

## Index-split failure modes (explicit, ranked)

If the visible-index/absolute-index split is left as-is (current code), the following slips occur — each grounded in the existing code:

1. **`t` adds a thinking block BEFORE the focused unit.** Visible indices shift; the marker (when added) lands on the unit that moved into the focused slot, but `e` still expands the unit at the absolute index stored in `expanded`. Marker and `e` desync. Severity: high — the marker lies about what `e` does.
2. **`t` hides the focused unit itself.** `focused` points at a now-hidden unit; the render clamps to `vis.length - 1`; the marker jumps to a different unit than the one the user was watching. Severity: high — visible cursor jump.
3. **`g`/`G` jump to absolute index 0 / last.** When thinking is hidden, that's a unit the user can't see. Severity: medium — silent out-of-sight.
4. **`e` toggles `expanded` on the absolute `i` of the focused VISIBLE unit.** If `focused` was renumbered to absolute index but `expanded` is keyed absolute, the toggle works. If `focused` stays visible-index, `e` reads `vis[focused].i` and toggles the correct absolute — by accident, because both indexes meet at the call site. The desync only happens after `t` shifts the visible list. Severity: latent — works today, breaks the next time someone touches the toggle.

The single-rename to `focusedAbsolute` (plus the recompute hook in `render`) eliminates all four by construction.

## What this card should NOT do (filed as follow-ups)

- **DO NOT** add a background highlight or bold variant on the focused head. The marker is sufficient.
- **DO NOT** introduce a second marker glyph (a square, a caret, a chevron) for the transcript. R-MARKER is binding; U+258C is reused.
- **DO NOT** change the routing kernel in `extensions/focus-nav.ts`. EV-35 is a render-time + handleInput-correctness change inside `TranscriptView`.
- **DO NOT** widen `DISPLAY_FLOOR` (the EV-9 tiny-regime floor at `extensions/focus-nav.ts:84`). One-row legibility is EV-36's territory.
- **DO NOT** touch the modal `openTranscript` path at `extensions/navigator.ts:810+`. R-MODAL is binding.
- **DO NOT** add an animation, fade, or transient marker.

## Preferences, ranked last

- I prefer the suffix wording `(off)` (symmetric with `(on)`) over dropping the suffix entirely. The `(off)` reads at the same eye-height as `(on)`; the absence of a suffix reads as "feature is absent" rather than "feature is off" — which is the wrong frame. But either is defensible; what matters is the suffix is token-styled and visible.
- I prefer the marker cell as `▌` + one space (two visible columns) over `▌` alone so the marker doesn't visually fuse with the head glyph (`→`, `⎿`, `user`, etc.). The two-column rhythm mirrors the tree-row marker at `extensions/navigator.ts:382`. But one column is also defensible.
- I prefer the recompute hook to live in `render()` (one place) over spreading it across `handleInput` (every branch). The render-time recompute means `t` doesn't need a handler; it just toggles and lets the next render resolve.

### Step 3 — bounded exchange, round 2 of ≤3 (facilitator digest)

All three generators were re-dispatched (jobs `job-11.4` owner 12.6m/20 turns,
`job-11.5` principal 12.6m/16 turns, `job-11.6` designer 12.6m/6 turns) with
each other's round-1 positions on the card and the five divergence points
put to them explicitly. Verdicts, with each seat's load-bearing reasons:

**D1 — the follow-off header cell.**
- `owner`: **CONCEDE to designer** — render `f follow(off)` (`dim`) when off.
  Evidence-driven concession: an absent suffix reads as "feature absent",
  and the card is titled "visible toggles". Ownership of the ruling flagged:
  "R-KEYMAP's ruled string names the header copy in the follow-on state; no
  ruling names the off-state bytes, so this is an open design point the
  seats resolve — no new ruling needed, provided the on-state renders the
  ruled bytes exactly"; a strict reader could call `(off)` a deviation from
  "exactly X".
- `principal`: **CONCEDE on content, HOLD on authority** — off state renders
  `f follow(off)` (`dim`); R-KEYMAP "supplies exactly one string, rendered
  with follow *on*. It has no off-state rule, so a positive off-token is a
  design inference, not a ruled contract, and only this seat can make it
  one." Unless recorded as an addendum, the owner cites the card's Intent
  (which names the current conditional as "named in the header string but
  not drawn anywhere") directly.
- `designer`: **HOLD for the conditional suffix** (owner's round-1 reading,
  dropping its own round-1 `(off)` preference). Reason: state visibility by
  *presence* is the strongest non-color signal; `accent "(on)"` vs
  `dim "(off)"` is invisible on any rendering where the two tokens shade
  alike, including the non-color pty surface FLLWUP-14's kitty smoke uses
  (".copy is interface"). Prefers the smaller change where the argument is
  also stronger.
- State after round 2: two seats for `(off)`, one for presence/absence, and
  no seat claims R-KEYMAP decides the off state. **Unconverged on
  user-visible copy.**

**D2 — does the header pin as row 0 of the viewport?**
- `owner`: **CONCEDE to principal** — verified its round-1 prediction is
  true (header is element 0 of `all`; `topLine = maxTop` in follow mode, so
  the keymap line scrolls away), then withdrew the remedy: (a) the goal's
  prose is provable against the returned line array with fixtures sized so
  `maxTop === 0`; (b) EV-36's Intent owns the follow-vs-viewport
  reconciliation and the row budget a pin changes; (c) R-ORDER's sequential
  merges do not prevent semantic interleaving in the same render path. EV-35
  must assert the header copy at `render(width)[0]` only where
  `all.length <= viewportRows`, and **no** deep-scroll header-visibility
  claim. "The pin must be explicitly recorded in the run record as
  EV-36-relevant (or a FLLWUP) so it doesn't evaporate."
- `principal`: **HOLD — do not pin in EV-35.** The pin collides with a ruled
  contract: at `termRows 7` the progress viewport is exactly one row
  (`computeProgressLayout(7,·).progressLines === 1`, pin at
  `test/ev9-progress.test.ts:108`), so a pinned header's returned array is
  `[header]` and can never contain the active block's head — contradicting
  EV-36's Acceptance ("At one row, the visible line identifies the active
  block") and EV-36's Intent, which owns the follow-scroll rewrite. Also:
  the marker is written into `all` and then sliced, so no render-time choice
  can put back a line the slice removed; EV-35 must not assert header or
  marker presence under follow-on overflow. "If a persistent header is
  wanted, that is an FLLWUP."
- `designer`: **HOLD — the pin is in this card.** Signatory to owner's
  structural argument as a design matter ("the card's own acceptance clause
  'follow and thinking state changes are visible in the rendered output'
  cannot be true on a viewport-overflowing transcript without the pin"), and
  refutes the scope claim: EV-36 owns the **scroll target**, the pin is
  **viewport allocation** — different lines of one `render()` that do not
  collide; the pin decides `viewportRows − 1` body rows, EV-36 decides where
  the bottom of that window sits. Cost accepted: the oldest visible row is
  paid while tail-following.
- State after round 2: two seats for leaving the pin out (one of them naming
  a ruled EV-36 acceptance clause it would contradict), one for pinning as
  this card's user-visible contract. **Unconverged on a design/scope
  question with a plausible test.**

**D3 — how focus is represented.**
- `owner`: **CONCEDE to designer** — both schemes keep marker and `e` on one
  pointer, but they do differ observably in exactly one sequence class, which
  owner constructed: `[thinking, toolCallA, toolCallB]`,
  `showThinking=false` so `vis=[A(i=1),B(i=2)]`; DOWN once (focus B); press
  `t`. Owner scheme: `focused` stays 1 → marker and `e` move to **A**.
  Designer scheme: `focusedAbsolute = i(B)` → both stay on **B** ("pressing
  `t` to peek at thinking silently changes which block `e` expands"). Cost is
  contained to `TranscriptView`; designer R2 becomes acceptance-grade.
- `principal`: **HOLD owner's visible-index design.** Confirms the two differ
  (constructs the mirror sequence `[thinking(0), toolCall(1)]`, `t` then
  render: owner's marker moves to the thinking head, designer's stays on the
  toolCall) and that **neither desyncs marker from `e`** — "the card's named
  defect is solved by either". Designer's `t`-stability "is a genuine UX
  gain, but it is not a clause of EV-35's goal; it buys a second coordinate
  plus a render-time recompute"; the minimal route if wanted is to re-aim
  the slot on `t`, not re-key focus.
- `designer`: **CONCEDE to owner.** Its own absolute-index variant is worse
  in the only divergent case (focus on a thinking block, then `t`: owner
  preserves the user's visual *position*, the absolute scheme falls back to
  the first visible unit); the extra rename/getter/recompute is not bought.
  Marker stays derived from the same `vis[this.focused].i` that `e` reads.
- State after round 2: two seats (principal, designer) for the visible-index
  scheme, one (owner) for the absolute-index scheme; **all three agree marker
  and `e` share one expression and cannot drift**, and that the residual
  difference is outside the card's goal clauses.

**D4 — the production key gate.**
- `owner`: **CONCEDE to principal — the claim is true, verified by
  execution, and the fix is in scope.** Ran the real modules:
  `classifyProgressKey("\x1b[101u") === "other"` while
  `matchesKey("\x1b[101u","e") === true`; likewise `\x1b[116u`/`t`,
  `\x1b[102u`/`f`, `\x1b[103u`/`g` are all `"other"`, while
  `\x1b[71;2u`/`G`, arrows, and literal `"e"` are correct — so the gate's
  broken set is worse than principal stated. Grounds the terminal class in
  the repo's own harness: FLLWUP-14's kitty search-smoke
  (`smoke/search-smoke/driver.py:50-60`) models it and passes *by using
  `matchesKey`*. One narrowing: the strict kitty spec with pi's requested
  flags 7 sends plain letters as text and key-release events are filtered at
  `tui.js:721`, so the leak is not universal across kitty terminals. Fix ≈ 4
  lines at `focus-nav.ts:67-70`; rebuts the designer's "do not change
  focus-nav.ts" line with "`classifyProgressKey` is not the routing kernel —
  `routeEditorFocus` is untouched — it is the surface's key predicate".
- `principal`: **HOLD — in scope, fix in `classifyProgressKey`.** All four
  raw comparisons are broken, not just `e`; `G`/arrows/enter/escape are
  already correct. Regression surface: on kitty/modifyOtherKeys terminals
  the four keys become consumed by progress instead of forwarded to the
  editor — that is the fix; legacy raw input is unchanged, so
  `test/ev9-progress.test.ts:48-58` stays green. "If a ruling instead makes
  this a follow-up, the card's goal and title must be amended, because the
  honesty claim is false on the terminals this repo already smoke-tests."
- `designer`: **HOLD — in scope.** "This is scope creep relative to the
  card's INTENT … but it is not scope creep relative to the card's GOAL. The
  goal cannot be satisfied without it." Prefers the in-card fix (4 lines +
  2 assertions) to a follow-up, and would escalate the two-sentence dispute
  if owner pushed back.
- State after round 2: **all three seats agree** the defect is real and the
  fix belongs inside EV-35's change set. The seats are not a ruling seat;
  whether it is a fold-in is a ruling-seat question under R-FOLLOWUP.

**D5 — narrowing `test/ev9-progress.test.ts:262`.**
- `owner`: **HOLD, with a self-correction** — narrowing is legitimate
  (preserves T9's named intent: the *tree-row* marker is `surface`-gated),
  but **not forced**: T9's fixture calls `writeManifest` and never
  `writeSession`, so the transcript region draws no `▌` and T9 passes
  unamended. "The run record must not claim it was required to unblock a
  red." Other assertions audited: T11 parity holds if the marker does not
  read `controller.surface`; the EV-8 suite has no joined-progress `▌`
  assertion; no test pins today's header string.
- `principal`: **HOLD — narrowing legitimate and required**, and adds the
  complementary requirement: assert the progress render *does* contain `▌`
  once a unit is focused, "otherwise the narrowing could hide a regression
  to 'marker never draws'". T11 is the one assertion at risk and holds iff
  the marker is not gated on `surface` or a constructor flag.
- `designer`: **CONCEDE** — narrowing is intent-preserving and "the same
  shape of edit EV-9 itself made when TranscriptView started rendering
  bodies".
- State after round 2: all three agree a narrowing (if done) is legitimate
  and intent-preserving; owner's correction that it is hygiene rather than a
  red-unblock is unanswered by the other two and stands as fact to verify.

**Round-2 endorsements (what each seat says the owner should build):**
`owner` — one change set in `TranscriptView` (`focusedAbsolute` + single
resolver, marker via `isFocused`, ruled header with token-styled follow
suffix, empty-visible guards) + `classifyProgressKey` via `matchesKey` + two
test amendments; assertions only where `maxTop === 0`. `principal` — keep
`focused` as visible index; marker from the same `vis[this.focused]`; empty
`vis` guards; ruled header with token-styled `(on)`/`(off)`; `matchesKey` for
`e/t/f/g`; narrow T9 and add the positive marker assertion; record the
`(off)` token as a card-Intent inference on an R-KEYMAP-silent state and the
follow-vs-viewport header persistence as explicitly deferred. `designer` —
ruled header with the **conditional** suffix; **pin the header as row 0**;
marker on the focused head via `vis[this.focused].u`; advertise `g/G`; narrow
T9; fix the gate; skip any rename, new glyph/token, modal change, or routing
change beyond the gate predicate.

**Positions stabilised** (each side holding with reasons; no seat moved a
verdict without new evidence), so the exchange is closed at two rounds —
below the ≤3 cap — and no third round is spent.

### Step 3 — facilitator status

The following are **not** settled by the exchange and are carried forward:
user-visible off-state follow copy (D1), the header pin (D2), and the
residual focus-scheme choice (D3). D4 (the production key gate) and D5 (the
T9 narrowing) are technically agreed but their authority is a ruling-seat
question (fold-in under R-FOLLOWUP; an existing-assertion edit under the
card's "suites stay green" clause). Nothing here is decided by the
facilitator; steps 4–6 run next.

### Step 4 — Skeptic attacks and runs tests

Skeptic dispatch `job-11.7` (20-min ceiling, settled 3.5m / 14 turns) at the
main checkout `05b7089` (read-only; every probe under `/tmp/ev35-skeptic/`,
repo untouched). Pre-implementation: no EV-35 branch exists, so its probes
settle the deliberation's factual claims against current code and mark
design objections `open-untested` explicitly.

**Objections and their actual terms (real runs quoted):**

| # | Claim attacked | Term | Basis (skeptic's real run) |
|---|---|---|---|
| O1 | "honest keymap" holds without a gate fix (D4) | factual sub-claims `closed-green`; the fix is undelivered | `classifyProgressKey("\x1b[101u")` → `"other"` while `matchesKey("\x1b[101u","e")` → `true`; same for `\x1b[116u`/t, `\x1b[102u`/f, `\x1b[103u`/g. `G` (via `Key.shift("g")`, which matches both literal `G` and `\x1b[71;2u`), arrows, `\r`, `\x1b` all classify correctly. `routeEditorFocus("other")` → `forward` vs `"e"` → `consumed`, so the kitty bytes reach the editor draft. `smoke/search-smoke/driver.py:49-60` uses exactly this flag-1 CSI-u class. Fix-turning test (`classifyProgressKey("\x1b[101u")==="e"` + editor delivery) run red pre-fix. |
| O2 | header scrolls off in follow mode; one-row pin contradiction (D2) | both `closed-green`; the pin-vs-EV-36 choice `open-untested` | 30 blocks, `viewportRows=10`, follow on → returned array length 10, row 0 is `"user"` and the header substring appears **nowhere**; short fixture → row 0 is the header. `computeProgressLayout(7,·)` → `{1,0,1}`; `TranscriptView` with `viewportRows=1` returns one body row, so a pinned header there returns `[header]` only — principal's EV-36-acceptance collision is mechanically real. |
| O3 | T9's no-`▌` assertion is vacuous not fragile (D5) | both `closed-green`; narrowing legitimacy a judgment | T9 calls `writeManifest` only, never `writeSession` (`test/ev9-progress.test.ts:250-258`); as-written progress renders 4 lines ending `(no transcript)`. Rebuilt with a session file, the progress region renders the tool head with no `▌` today — so a future head marker **would** enter the joined assertion. Narrowing is hygiene-today, red-prevention-tomorrow. |
| O4 | focus-scheme claims (D3) | same-expression + empty-DOWN claims `closed-green`; designer-half `open-untested` | `e` reads `vis[this.focused].i` (`navigator.ts:761-762`), so any marker derived from that expression tracks it by construction; DOWN+`t`+`e` expands `outA` (the unit now in slot 1). `focusedAbsolute` has no code to run. All seats agree neither scheme desyncs marker from `e`; the residual is a UX judgment. |
| O5 | R-COPY / theme non-regression | today's-code half `closed-green`; branch half `open-untested` | `test/ev34-tool-unit.test.ts:30-38` uses `.includes(...)`/`startsWith("  ")`/`trim()` — a 2-column head prefix breaks none by reading; T11 parity holds **iff** the marker does not read `controller.surface` or a constructor flag; `accent`/`dim`/`TREE_ROW_MARKER` all exist. Five suites as they stand: **47 pass, 0 fail** (364 expects). |
| O6a | owner's "render already clamps `focused`" | `closed-green` (correction) | the render-time clamp is top-only (`focused >= vis.length`, `navigator.ts:786`); there is no lower clamp, so `focused = -1` persists after empty-DOWN and is harmless only via `starts[-1] ?? 0`. The empty-`vis` guard is required, and the "already clamps" premise is false. |
| O6b | principal's "`e` on user/assistant is a no-op" | `closed-green` | `showBody` unconditionally true (`navigator.ts:712-717`); `e`-before/after renders byte-identical. |
| O6c | modal unreachable from the production editor route | `closed-green` (stronger than principal stated) | `onActivate` is stored (`focus-nav.ts:278`) and never invoked; tree ENTER calls `enterProgress` only (`:314`); the modal `open()` path is referenced only by a comment. |
| O6d | owner's clamp-no-op semantics | `closed-green` (independently reproduced) | UP-at-top with follow on differs (header drops `(on)`); strict byte-identity holds only with follow already off (UP-at-top and DOWN-at-bottom both byte-identical). |
| O6e | designer's R8 ("marker always within viewport") | mechanism `closed-green`; prediction `open-untested` | follow-on forces `topLine = maxTop` regardless of focus, so a marked line above the tail window is sliced like any other. |
| O6f | the record's focused/expanded description | `closed-green` | `focused` visible-index (`:618`, `:757/:760/:761`), `expanded` absolute-`i` (`:616`, `:762`), `starts[focused]` scroll-anchor-only (`:796`), `visible()` filters only thinking (`:677-683`). |

**Blocking verdict: `blocks`.** Not on sloppiness — nearly every executable
factual claim was confirmed, including owner's own self-corrections — but
because the closable items are not closed on any branch: (1) the gate defect
O1 is red on `main` and turns green only on a branch carrying the
`matchesKey` fix, whose fold-in authority is a ruling-seat question under
R-FOLLOWUP; (2) D2's pin-vs-EV-36 collision, D1's off-state copy under
R-KEYMAP silence, and D3's residual `t`-toggle scheme choice are
`open-untested` with named step-9 tests but no branch; D2 needs a scope
ruling and D1 arguably a copy addendum; (3) the T9 narrowing is verified
hygiene rather than a red-unblock, and editing the assertion is
authority-sensitive.

Step-9 minimum kit the skeptic named: O1's two assertions, O6a's empty-file
guards, O6d's follow-off clamp equalities, O2's `maxTop===0`-scoped
header/marker assertions, O3's with-session positive-`▌` case, unchanged T11,
and the five suites green.

### Step 5 — synthesis (consolidator, job-11.8; verbatim)

Consolidator dispatch `job-11.8` (15-min ceiling, settled 1.2m / 4 turns). It
independently confirmed the load-bearing code facts (`classifyProgressKey`
raw equality at `focus-nav.ts:67-70`; `focused` a visible-array index at
`navigator.ts:618`) before synthesizing.

---

# EV-35 step 5 — synthesis

## Agreed design

Every seat converged on the following as the EV-35 change set, independent of
the open items below:

- **Apply R-KEYMAP verbatim** to the on-state header: `<title> — ↑↓ move · e expand · t thinking · f follow(on) · g/G jump · esc back`. `g`/`G` are advertised, not removed; they already work in `handleInput` and clear `follow`.
- **Apply R-MARKER**: the focus signifier is `TREE_ROW_MARKER` (U+258C), rendered via `theme.fg("accent", …)`, reusing the existing tree-row call site idiom. No new glyph, no new token, no literal ANSI (AGENTS.md 9.6).
- **Marker is head-only, on the focused unit only**, never on body lines.
- **The marker and `e` must share one expression** — both read `vis[this.focused].i` — so marker↔`e` cannot drift by construction. *(convergence only, not evidence — no branch exists)*
- **Empty-`vis` guards are required**: when `vis.length === 0`, `up`/`down`/`e`/`g`/`G` are consumed no-ops; no marker is drawn; no crash. *(This is a correction: owner's round-1 "render already clamps `focused`" premise is false — the clamp is top-only, no lower clamp, so `focused = -1` persists after empty-DOWN.)*
- **Clamp no-op semantics**: strict byte-identical-render for boundary clamps holds **only with follow already off**. With follow on, up/down flip the header's `(on)` to off as a side effect even as the cursor clamps; the clamp no-op is defined as *cursor position unchanged*, not byte-no-op. *(convergence only, not evidence)*
- **`e` on `user`/`assistant` units (always-expanded bodies) and on unpaired toolCall units is a defined consumed no-op** — honest keymap ≠ zero no-ops.
- **`t` visibility is a body assertion, not a header assertion** — thinking units render or vanish via `visible()`; the ruled header gives `t` no suffix, so the proof fixture must contain a thinking block or the claim is vacuous.
- **The modal `openTranscript` path is out of scope (R-MODAL)** and is in fact unreachable from the production editor route (`onActivate` stored but never invoked; tree ENTER calls `enterProgress` only).
- **T11 inline-vs-standalone byte-parity holds iff the marker does not read `controller.surface` or a constructor flag.**
- **The five EV/EV-8/EV-9/navigator/theme-compliance suites are green on `main` today** (skeptic: 47 pass / 0 fail, 364 expects) — no current red from the EV-35 change set.

## Settled disputes

Each closed by a named basis. I do not dress agreement as a test result; where no test ran on a branch, I say so.

- **O1 — the production key gate is broken on `main`.** *Settled by Skeptic test, closed-red.* `classifyProgressKey("\x1b[101u")` → `"other"` while `matchesKey("\x1b[101u","e")` → `true`; same pattern for `\x1b[116u`/t, `\x1b[102u`/f, `\x1b[103u`/g. `G` (`Key.shift("g")`), arrows, `\r`, `\x1b` classify correctly. `routeEditorFocus("other")` → `forward`, so the kitty bytes reach the editor draft. I independently confirmed the source: `classifyProgressKey` uses raw `data === "e"/"t"/"f"/"g"` at `focus-nav.ts:67–70` while every other key uses `matchesKey`. The repo's own `smoke/search-smoke/driver.py:49–60` models this flag-1 CSI-u class. Fix-turning test run red pre-fix. **The defect is real; the *fix landing inside EV-35* is not settled — see Open judgment D4.**
- **O2a — the header scrolls off in follow mode.** *Settled by Skeptic test, closed-green.* 30 blocks, `viewportRows=10`, follow on → returned array length 10, row 0 is `"user"` and the header substring appears nowhere; short fixture → row 0 is the header. Owner's round-1 prediction is verified true.
- **O2b — a one-row pinned header collides with EV-36's acceptance.** *Settled by Skeptic test, closed-green.* `computeProgressLayout(7,·)` → `{1,0,1}`; `TranscriptView` with `viewportRows=1` returns one body row, so a pinned header there returns `[header]` only and cannot contain the active block's head. Principal's EV-36-acceptance collision is mechanically real. **Whether to pin is not settled — see Open judgment D2.**
- **O3 — T9's no-`▌` assertion is vacuous today, not fragile.** *Settled by Skeptic test, closed-green.* T9 calls `writeManifest` only, never `writeSession` (`test/ev9-progress.test.ts:250–258`); as-written progress renders 4 lines ending `(no transcript)`. Rebuilt with a session file, the progress region renders the tool head with no `▌` today — so a future head marker *would* enter the joined assertion. Narrowing is hygiene-today, red-prevention-tomorrow. **Whether/when to narrow is authority-sensitive — see Open judgment D5.**
- **O4a — marker↔`e` share one expression.** *Settled by Skeptic test, closed-green.* `e` reads `vis[this.focused].i` (`navigator.ts:761–762`); any marker derived from that expression tracks it by construction. DOWN+`t`+`e` expands the unit now in slot 1. All seats agree neither candidate scheme desyncs marker from `e`.
- **O4b — the `focusedAbsolute` (designer) scheme is unimplemented.** *Settled as open-untested — no code to run.* The residual D3 difference is a UX judgment outside the card's goal clauses.
- **O5a — R-COPY / theme non-regression on today's code.** *Settled by Skeptic test, closed-green.* `test/ev34-tool-unit.test.ts:30–38` uses `.includes(...)`/`startsWith("  ")`/`trim()`; a 2-column head prefix breaks none by reading. `accent`/`dim`/`TREE_ROW_MARKER` all exist in the palette.
- **O6a — owner's "render already clamps `focused`" is false.** *Settled by Skeptic test, closed-green (correction).* The render-time clamp is top-only (`focused >= vis.length`, `navigator.ts:786`); no lower clamp, so `focused = -1` persists after empty-DOWN and is harmless only via `starts[-1] ?? 0`. The empty-`vis` guard is required.
- **O6b — `e` on user/assistant is a no-op.** *Settled by Skeptic test, closed-green.* `showBody` unconditionally true (`navigator.ts:712–717`); before/after renders byte-identical.
- **O6c — modal unreachable from the production route.** *Settled by Skeptic test, closed-green (stronger than principal stated).* `onActivate` stored (`focus-nav.ts:278`) and never invoked; tree ENTER calls `enterProgress` only (`:314`); the modal `open()` path is referenced only by a comment.
- **O6d — clamp-no-op byte-identity holds only with follow off.** *Settled by Skeptic test, closed-green (independently reproduced).* UP-at-top with follow on differs (header drops `(on)`); strict byte-identity holds for UP-at-top and DOWN-at-bottom both, with follow already off.
- **O6e — designer's R8 ("marker always within viewport") mechanism.** *Settled as open-untested on the prediction.* Mechanism closed-green: follow-on forces `topLine = maxTop` regardless of focus, so a marked line above the tail window is sliced like any other. The prediction that the marker stays in the viewport under follow-on overflow is false on today's slice; under a pin it is untested.
- **O6f — the record's `focused`/`expanded` description.** *Settled by Skeptic test, closed-green.* `focused` visible-index (`:618`, `:757/:760/:761`), `expanded` absolute-`i` (`:616`, `:762`), `starts[focused]` scroll-anchor-only (`:796`), `visible()` filters only thinking (`:677–683`).

## Open judgment — for `product-owner`, escalating to `steward`

No test can settle these; each is a values/tradeoff/authority question. Both sides stated at equal weight.

### D1 — the follow-off header cell (user-visible copy)

- **For `f follow(off)` rendered `dim` when off (owner conceded to designer; principal concedes on content):** an absent suffix reads as "feature absent" rather than "feature off"; the card is titled "visible toggles"; symmetry with `(on)` reads at the same eye-height.
- **For presence/absence — `f follow` when off, `f follow(on)` when on (designer, round-2 hold):** state visibility by *presence* is the strongest non-color signal; `accent "(on)"` vs `dim "(off)"` is invisible on any rendering where the two tokens shade alike, including the non-color pty surface FLLWUP-14's kitty smoke uses (".copy is interface"); smaller change, stronger argument.
- **Authority question:** R-KEYMAP supplies exactly one string, rendered with follow *on*; it has no off-state rule. No seat claims R-KEYMAP decides the off state. Principal holds that a positive off-token is "a design inference, not a ruled contract, and only this seat can make it one" — i.e. recording it as an R-KEYMAP addendum vs. an EV-35 design inference is itself the authority call. Owner flagged that "a strict reader could call `(off)` a deviation from 'exactly X'."
- **What a ruling must decide:** (a) the off-state bytes — `f follow(off)` (`dim`) vs. `f follow` (no suffix); and (b) whether that decision must be recorded as an R-KEYMAP addendum or may stand as an EV-35 design inference under the card's Intent.

### D2 — does the header pin as row 0 of the viewport?

- **For leaving the pin out of EV-35 (owner conceded to principal; principal hold):** owner withdrew after verifying its own prediction is true, on three grounds — (a) the goal's prose is provable against the returned line array with fixtures sized so `maxTop === 0`; (b) EV-36's Intent owns the follow-vs-viewport reconciliation and the row budget a pin changes; (c) R-ORDER's sequential merges do not prevent semantic interleaving in the same render path. Principal adds a ruled-contract collision: at `termRows 7` the progress viewport is exactly one row, so a pinned header returns `[header]` only and contradicts EV-36's Acceptance ("At one row, the visible line identifies the active block") and EV-36's Intent, which owns the follow-scroll rewrite. Principal: "If a persistent header is wanted, that is an FLLWUP."
- **For pinning in EV-35 (designer hold):** the card's own acceptance clause "follow and thinking state changes are visible in the rendered output" cannot hold on a viewport-overflowing transcript without the pin. Refutes the scope claim: EV-36 owns the *scroll target*; the pin is *viewport allocation* — different lines of one `render()` that do not collide; the pin decides `viewportRows − 1` body rows, EV-36 decides where the bottom of that window sits. Cost accepted: the oldest visible row is paid while tail-following.
- **What a ruling must decide:** whether EV-35's "state changes are visible in the rendered output" acceptance clause *requires* a pinned header on overflowing transcripts, or whether EV-35 may scope its header/marker assertions to `maxTop === 0` (short fixtures) and defer the follow-on-overflow header-visibility problem to EV-36 / a FLLWUP. This is a scope ruling under R-ORDER/R-FOLLOWUP, and the run cannot dispatch it.

### D3 — focus representation, residual (`t`-toggle stability)

- **For the absolute-unit-index scheme `focusedAbsolute` + render-time recompute (owner, post-concede):** in the one divergent sequence class owner constructed (`[thinking, toolCallA, toolCallB]`, `showThinking=false`, DOWN to B, then `t`), the visible-index scheme silently moves marker and `e` to A; the absolute scheme keeps both on B ("pressing `t` to peek at thinking silently changes which block `e` expands").
- **For the visible-index scheme, marker from `vis[this.focused].i` (principal hold; designer concede):** both schemes keep marker and `e` on one pointer — "the card's named defect is solved by either." Designer concedes its own absolute variant is worse in the mirror case (focus on a thinking block, then `t`: owner preserves the user's visual *position*; the absolute scheme falls back to the first visible unit). Principal: designer's `t`-stability "is a genuine UX gain, but it is not a clause of EV-35's goal; it buys a second coordinate plus a render-time recompute"; the minimal route if wanted is to re-aim the slot on `t`, not re-key focus.
- **What a ruling must decide:** whether `t`-toggle cursor stability is in scope for EV-35's goal ("the signifier marks the block `e` will expand") or is a UX improvement outside the goal clauses. All three agree the residual is outside the card's goal clauses; the question is whether to ship the improvement anyway. This is a values call, not a fact.

### D4 — the production key gate fix as a fold-in (authority)

- All three generators agree the defect is real (O1, closed-red) and the fix belongs inside EV-35's change set (~4 lines at `focus-nav.ts:67–70`, replacing raw `===` with `matchesKey` for `e/t/f/g`). Designer: "scope creep relative to the card's INTENT … but not relative to the card's GOAL. The goal cannot be satisfied without it." Principal: "If a ruling instead makes this a follow-up, the card's goal and title must be amended, because the honesty claim is false on the terminals this repo already smoke-tests."
- **Authority question:** the card's Intent does not name `focus-nav.ts`. Under R-FOLLOWUP, a fold-in is ruled by `product-owner`; a genuinely new follow-up card is confirmed by `steward`. **The runner dispatches neither ruling seat.** Owner conceded the file change is outside the named Intent.
- **What a ruling must decide:** whether the `classifyProgressKey` fix is a fold-in (ruled by `product-owner`, applied inside EV-35) or a new follow-up card (confirmed by `steward`, written mid-run as `FLLWUP`). Until that ruling lands, the gate is red on `main` and EV-35's honesty claim is false on kitty/modifyOtherKeys terminals.

### D5 — narrowing `test/ev9-progress.test.ts:262` (authority-sensitive assertion edit)

- All three agree a narrowing to the tree-row slice is intent-preserving if done (preserves T9's named intent: the *tree-row* marker is `surface`-gated). Designer concedes.
- **Owner's verified correction (unanswered, stands as fact):** T9's fixture calls `writeManifest` and never `writeSession`, so the transcript region draws no `▌` and T9 passes unamended — the narrowing is hygiene rather than a required red-unblock. "The run record must not claim it was required to unblock a red."
- **Principal's complementary requirement:** the narrowing must ship with a positive assertion that the progress region *does* contain `▌` once a unit is focused with a session present, "otherwise the narrowing could hide a regression to 'marker never draws'."
- **Authority question:** editing an existing "suites stay green" assertion is authority-sensitive — the card's Acceptance names only EV-8; EPIC-8 names both EV-8 and EV-9. Whether an EV-9 assertion edit is within EV-35's authority is a ruling-seat question.
- **What a ruling must decide:** (a) whether EV-35 may edit `test/ev9-progress.test.ts:262` at all (scope under the Acceptance/EPIC-8 clauses); and (b) if yes, whether principal's complementary positive-`▌` assertion is required to ship with the narrowing.

## Open objections — Skeptic, standing

The Skeptic's overall verdict is **`blocks`**, not on sloppiness but because the closable items are not closed on any branch. Listing each by its close status:

- **O1 — `closed-red`.** The gate defect is real and red on `main`; the fix-turning test (`classifyProgressKey("\x1b[101u")==="e"` + editor delivery) was run red pre-fix. **Closes only when a branch carries the `matchesKey` fix and the two named assertions pass — and that branch's fold-in authority is D4, still open.**
- **O2 — header-scroll `closed-green`; pin-vs-EV-36 `open-untested`.** The pin choice has named step-9 tests but no branch. Closes when D2 is ruled and the corresponding `maxTop===0`-scoped (or pinned) header/marker assertions run on a branch.
- **O3 — vacuous-today `closed-green`; narrowing legitimacy `open-untested` (judgment).** Closes when D5 is ruled and, if narrowing is approved, the with-session positive-`▌` case runs green.
- **O4 — same-expression + empty-DOWN `closed-green`; designer `focusedAbsolute` half `open-untested`.** Closes when D3 is ruled and the chosen scheme's tests run on a branch.
- **O5 — today's-code half `closed-green`; branch half `open-untested`.** Closes when the branch renders and T11 parity + theme-compliance grep run green (iff marker does not read `controller.surface`/constructor flag).
- **O6a — `closed-green` (correction to owner).** The empty-`vis` guard is required. Closes as an objection when the guard ships and O6a's empty-file no-op tests run green.
- **O6b — `closed-green`.** No-op confirmed; closes when the `e`-no-op assertion ships.
- **O6c — `closed-green`.** Modal unreachable; no objection to close beyond recording.
- **O6d — `closed-green`.** Clamp byte-identity holds only follow-off; closes when the follow-off clamp-equality assertions ship.
- **O6e — mechanism `closed-green`; prediction `open-untested`.** Designer's R8 prediction is false under today's slice; under a pin it is untested. Closes with D2.
- **O6f — `closed-green`.** Record description confirmed; nothing to close.

The Skeptic's named step-9 minimum kit (to unblock): O1's two assertions, O6a's empty-file guards, O6d's follow-off clamp equalities, O2's `maxTop===0`-scoped header/marker assertions, O3's with-session positive-`▌` case, unchanged T11, and the five suites green.

## Ready to hand off?

**No.** Step 7 (spec and owner handoff) cannot begin until the following are routed to a ruling seat, because the runner dispatches neither `product-owner` nor `steward` and none of these is decidable by a test:

1. **D4 (authority — R-FOLLOWUP)** → `product-owner`, escalating to `steward`: is the `classifyProgressKey` fix a fold-in inside EV-35, or a new `FLLWUP` card? This is the single hardest block — O1 is closed-red on `main` and EV-35's honesty claim is false on kitty/modifyOtherKeys terminals until this lands.
2. **D2 (scope — R-ORDER/R-FOLLOWUP)** → `product-owner`, escalating to `steward`: does EV-35's "state changes visible in rendered output" acceptance require a pinned header on overflow, or may EV-35 scope header/marker assertions to `maxTop===0` and defer overflow-header-visibility to EV-36 / a FLLWUP?
3. **D1 (copy + authority — R-KEYMAP addendum vs. EV-35 inference)** → `product-owner`, escalating to `steward`: the off-state bytes (`f follow(off)`/`dim` vs. `f follow` no suffix), and whether that decision must be recorded as an R-KEYMAP addendum or may stand as an EV-35 design inference.
4. **D5 (authority — existing-assertion edit)** → `product-owner`, escalating to `steward`: whether EV-35 may edit `test/ev9-progress.test.ts:262` at all, and whether principal's complementary positive-`▌` assertion is required.
5. **D3 (values — outside goal clauses)** → `product-owner`, escalating to `steward`: whether `t`-toggle cursor stability is in scope for EV-35's goal or is an out-of-scope UX improvement. All three seats agree it is outside the goal clauses; a ruling decides whether to ship it anyway.

**Already decided by a Phase 1 ruling the runner should simply apply and cite** (no routing needed):

- **R-KEYMAP** — the on-state header copy is pinned; render `f follow(on)` with follow on. (Does not decide D1's off-state.)
- **R-MARKER** — `TREE_ROW_MARKER` (U+258C), no new glyph.
- **R-MODAL** — the modal path is out of scope (and O6c confirms it is unreachable anyway).
- **R-ORDER** — EV-35 starts after EV-34's merge SHA `7235178` on `main` (verified at `8f51550`); no later card starts before EV-35's merge SHA is on `main`.
- **R-GATES** — the owner gate set is fixed.
- **R-MERGE / R-ESCALATE** — the merge check and the authority map are binding.

No bucket is empty.

### Step 6 — routing: five open items, all ruling-seat questions

The consolidator's five open-judgment items were taken in turn. Per
`<escalation_contract>` step 1 the Phase 1 rulings were checked first: none of
the five is squarely covered — **R-KEYMAP** pins only the follow-*on* header
bytes (D1's off state is unanswered), **R-ORDER/R-MODAL/R-COPY** do not speak
to the header pin (D2), the focus scheme (D3), a `focus-nav.ts` fold-in (D4),
or an EV-9 assertion edit (D5), and **R-FOLLOWUP** assigns exactly D4's
authority and D5's assertion-edit surface to `product-owner`/`steward`, which
this container may not dispatch. Per `<escalation_contract>` step 2 the turn
ends with an `ESCALATION` report carrying the five disputes as facts, their
positions, and the Skeptic's real results — no recommendation.

The card stays `Deliberating` (steps 2–6 are the deliberation phase and step 6
is paused on rulings); it is **not** set to `Needs Human`, because the
authority map re-homes the `Needs Human` route to `product-owner`/`steward`
for this run and each item is routable there. Steps 7 onward do not start
until the rulings land. On resumption the runner appends the ruling verbatim,
re-reads the card and board, and continues from step 6/7.