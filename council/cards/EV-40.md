---
id: EV-40
title: Automatic continuation of a parent turn that ends in a provider error
state: Deliberating
owner: null
epic: EPIC-9
goal: A parent turn that ends with stopReason error is continued automatically after the configured backoff, and once the budget is exhausted the session stops with a named terminal message rather than a bare Continue.
---

## Intent

This is the mechanism that actually addresses the intake. No retry API exists
for the parent's own provider call, so it is built from the `agent_settled`
event plus a continuation send, and it must hold in both the TUI and the
headless branches.

The intake's terminal word is `Continue`, the only affordance between the error
and the person's next action, so the surface is named by ruling rather than
left to the implementer. This card carries the epic's real risk; the Council
should expect it to be split if the continuation path turns out not to be
reachable from a handler.

## Orchestrator rulings (Phase 1, binding)

Recorded human decisions for this run. Immutable and binding on every seat,
`steward` included; a runner that hits one applies it and cites the ruling
rather than re-asking.

- **R5 (retry surface copy).** During backoff the input bar reads
  `Retrying in 2s (attempt 2 of 3) — Esc to abort`, and `Esc` aborts the
  remaining retries. When the budget is exhausted the session stops with
  `Retries exhausted after 3 attempts. The provider kept failing. Press Enter
  to try again.` The per-branch observable for the goal is the input bar's
  text delta in the TUI and the equivalent stdout line in the headless branch.

## Run record (features-deliver / EV-40)

### Step 1 — classification (facilitator)

- **Path: full council.** The card is cross-seam (`extensions/index.ts`'s
  `agent_settled` registration and the parent-turn lifecycle, the EV-38
  `retry` policy in `.council.json`, and the TUI/headless mode split), and it
  is spec-ambiguous: the `goal` fixes the outcome (automatic continuation +
  a named terminal message) but the mechanism is open — which event carries
  the continuation, how the backoff is scheduled inside/around a turn, how
  abort is expressed, and whether the terminal copy is emitted by the
  extension or by pi. That is design judgment, not one implementation
  choice. Per council.md step 1 this card runs steps 2–6 and then 7–14.
- **Surface-touching: yes (recorded).** R5 is a recorded human decision that
  fixes user-visible copy in the input bar (`Retrying in 2s (attempt 2 of 3)
  — Esc to abort`) and a terminal message (`Retries exhausted after 3
  attempts. The provider kept failing. Press Enter to try again.`), with a
  named per-branch observable (input-bar text delta in TUI; equivalent
  stdout line headless). A full-council, surface-touching card seats
  `designer` as a third generator in steps 2–3. R5 bounds that seat's
  subject matter (the copy is ruled, not open) but does not unseat it: the
  surface state machine around the ruled strings, the abort affordance's
  discoverability, and the terminal-state hand-off remain design questions.
- **Rulings applied (Phase 1, binding): R5 applies to this card as written
  and is quoted verbatim above; it is applied, not reweighed.** R1
  (classifier superset — `extensions/retry.ts`), R2/R3 (retry defaults +
  validation — `extensions/seats.ts` `loadRetryConfig`, mirrored in
  `council/scaffold/.council.json`) are the mechanism's inputs and are cited
  where they bind. R4 (hub attempt-label visibility) is EV-39's surface, not
  this card's. No ruling is extended to a question it did not answer.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `principal`, `designer`, `skeptic`, `consolidator`, and `judge`.
  All six resolve from the packaged seat set (`council/agents/*.md`, each
  file's `name:` matching its filename); this checkout has **no
  `.pi/agents/`** directory, so nothing shadows them and no repo-local
  override exists. `product-owner` / `steward` are not dispatched by this
  container (escalation-only). No `Unknown seat` is possible on the seats
  named here.
- **Environment (facilitator-read, first-hand):** main checkout clean at
  `00039d1fe3800a4f797bb681c28f18b7880b9b93` == `origin/main`; `gh`
  authenticated as `tistaharahap`; EV-37 (`6a375c4`), EV-38 (`3e39e66`) and
  EV-43 (`8853712`) are ancestors of `HEAD` with their cards `Done`. This
  card is `Ready`; no `Needs Human` state and no outstanding ruling —
  deterministic merge check criterion 5 holds at the start of the card.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md`
  does not exist here):** `bash council/preflight.sh EV-40`,
  `bunx tsc --noEmit`, `bun test`, `python3 council/validate.py` (AGENTS.md
  §Commands + `.github/workflows/gates.yml`). This repo defines no
  database/import/server gate; `COUNCIL_INTEGRATION=1` stays gated and is
  not run. The `preflight.sh` branch-freshness line (FLLWUP-27) fires by
  construction once a facilitator record commit advances `origin/main` past
  a branch cut — recorded verbatim, never reclassified, and never used to
  weaken a criterion.
- **Grounded facts carried into deliberation (inputs, not a settled design;
  the Council verifies them at this tree):**
  - The intake class is `Provider finish_reason error`, classified retryable
    for council's purposes by `classifyRetry` (EV-37, `extensions/retry.ts`)
    while remaining **non-retryable by pi's own**
    `isRetryableAssistantError` (EV-43's live contrast probe). The
    "no hook can re-issue" premise is EV-41's forcing class.
  - EV-43's merged falsifier (`ev43/ev43-falsifier-extension.ts`, PR #53)
    established empirically: an `agent_settled` handler calling
    `pi.sendUserMessage` **does** produce a second assistant message in both
    the TUI and `-p` branches, attributable to the handler (control arm
    closed attribution); the `agent_settled` **event context does not carry
    `waitForIdle`** (that is a command-context affordance —
    `extensions/index.ts:448-454` cannot be transplanted verbatim), so the
    headless pattern is fire-then-poll-`ctx.isIdle()` plus a guarded
    `waitForIdle`; the nested run is covered by `_runAgentPrompt`'s `finally`
    awaiting `_emitAgentSettled`. Scratch dir on `main` is `ev43/`.
  - `extensions/index.ts:379-385` currently registers the only
    `agent_settled` handler (`await flushUsage("agent-settled", ctx)`);
    `:442`/`:448-454` carry the established TUI fire-and-forget vs headless
    poll-then-`waitForIdle` branch pattern.
  - `extensions/index.ts:66-81` registers `before_provider_request` and
    raises `max_completion_tokens`/`max_tokens` to the
    `council/model-floors.json` floor for pinned models; a continuation send
    re-enters this hook, so a continuation cannot silently escape the floor.
  - `loadRetryConfig(repoRoot)` (EV-38) always returns a full policy
    (`{enabled, maxAttempts, baseDelayMs, maxDelayMs, jitter}`, defaults
    `true/3/2000/30000/true`), throwing on malformed or unknown keys; this
    repo's own `.council.json` carries **no** `retry` section, so the
    defaults apply here.
- **Watch item for the deliberation (roster interaction):** EV-39 (hub-level
  seat retry, R4) is scheduled *after* this card in the steward `job-6`
  build order. Anything this card's mechanism does to the parent turn must
  not pre-empt or duplicate the hub's per-attempt identity work; the two
  backoff loops key on different subjects (a parent provider call vs a
  settled child job report).
- **`brainstorming` step (council.md step 2's first pass) is satisfied by
  this dispatch.** No separate brainstorming session is opened: the
  deliberation below is the brainstorming discipline, and the human's
  intake is the card's `Intent`.

### Step 2 — independent first pass (round 1)

Recorded from a fresh run: the earlier container for this card dispatched
round-1 generators but its model route failed before any position was
recorded, so this section was empty and is re-run fresh. No invented
positions; the three below are the actual round-1 returns, given the card
alone (no seat saw another's position).

Jobs: `owner` `job-16.1` (3.6m, 11 turns), `principal` `job-16.2` (3.6m, 19
turns), `designer` `job-16.3` (3.6m, 8 turns). All settled `done`.

#### Owner position (`job-16.1`) — verbatim

## **Position** (the design)

**1. Continuation mechanism.** `agent_settled` is the only correct carrier — not because EV-43 proved reachability (it did), but because its contract is the only settle point pi *guarantees* no further automatic work will follow (types.d.ts:561-563: "after an agent run has fully settled and no automatic retry, compaction, or queued continuation will run"). `agent_end` (types.d.ts:556-559) fires *before* pi's own auto-retry (`auto_retry_start`, agent-session.d.ts:64-67) can interleave, so scheduling there would race pi's retry machinery. The backoff timer lives **outside the turn lifecycle, in closure state in the parent-mode section of `extensions/index.ts`** (a sibling of `flushChain`, ~line 285): the `agent_settled` handler (index.ts:379) arms a `setTimeout` and returns immediately. The send is `pi.sendUserMessage` using the exact branch pattern at index.ts:439-454 — TUI fire-and-forget; headless fire-then-poll-`ctx.isIdle()` then a guarded `waitForIdle` (the falsifier proved the event context lacks `waitForIdle`; types.d.ts:258 shows it exists only on `ExtensionCommandContext`).

**2. Policy.** `loadRetryConfig(repoRoot)` (seats.ts:267) is read **once at parent-mode init** (wrapped try/catch; a throw disables retry and notifies — the malformed-config session must not crash at init), cached beside the timer state. Delay = `min(baseDelayMs * 2^(attempt-1), maxDelayMs)`, halved/doubled when `jitter` (scheme must be pure and testable; I'd put a `computeBackoffDelay(policy, attempt, rand)` pure fn in `extensions/retry.ts` next to `classifyRetry`). Attempt counting: the failed turn is attempt 1; retries are attempts 2..maxAttempts — matching R5's copy literally ("attempt 2 of 3"). Note this repo's `.council.json` has **no `retry` key**, so defaults `true/3/2000/30000/true` (seats.ts:229-235) govern; R5's example "2s" matches the default baseDelay exactly — the copy must interpolate the real delay, not hardcode 2s.

**3. Esc abort.** This is the weakest seam. `ctx.abort()` aborts "the current agent operation" (types.d.ts) — while idle (backoff is between runs) I cannot confirm it does anything useful. The only proven key-capture mechanism in this repo is the EV-8 custom-editor overlay (`focus-nav.ts`/`navigator.ts`, editor interception at navigator.ts:775+). My design: during backoff, the TUI branch installs a passthrough custom-editor wrapper whose key handler watches Escape → sets a closure abort flag, clears the countdown surface, and disarms the timer. Headless: Esc doesn't exist; R5's "equivalent stdout line" prints the ruled string as-is, and SIGINT is the de-facto abort — I'd record this as a designer/skeptic question, not silently decide it.

**4. Terminal message.** TUI: after exhausting the budget, clear the countdown widget and `ctx.ui.notify("Retries exhausted after 3 attempts. The provider kept failing. Press Enter to try again.", "warning")` — the notify is the input-bar-adjacent surface council already uses. Headless: `console.log(...)` (the index.ts:439/448 dual-route pattern). "Press Enter to try again" is left inert copy — the session simply stops, and Enter re-submits whatever the user types; making Enter *itself* re-trigger the retry would require editor capture I wouldn't build.

**5. Floor re-entry.** Yes, and that's fine: `before_provider_request` (index.ts:66-81) patches any outgoing payload by model id, so the continuation re-enters the `max_completion_tokens` floor. No special handling; a test can assert a continuation payload for a floored model carries the floor.

**6. Error detection.** `agent_settled` has **zero payload** (types.d.ts:561-563), so the signal must be cached from `agent_end`, whose extension event carries `messages: AgentMessage[]` — inspect the last assistant message for `stopReason === "error"` and classify `errorMessage` with the R1 predicate. `classifyRetry` takes a `JobReport` (retry.ts:80-101), so I'd extract the pattern test into a message-side pure predicate in `retry.ts` shared by both; the parent-turn adapter treats the turn as settled-with-error. `stopReason === "aborted"` must never schedule. Edge case to rule on: if the error *does* match pi's own retryable list, pi already retried before settle — council's loop stacks on top of pi's. I'd engage anyway (the epic wants survival, not dedup) but flag it.

**7. Split judgment.** Split into two cards: **(A)** continuation + policy + detection + terminal message, both branches, **without** interactive Esc (backoff abortable only headless-via-SIGINT; TUI Esc not yet wired); **(B)** the Esc-abort editor overlay. A is EV-43-proven reachable end to end; B is unproven surface work in the hardest part of the TUI (editor interception), and is designer/judge territory. Shipping A alone still satisfies the epic goal minus abortability.

## **Evidence**

- `agent_settled` = `{ type: "agent_settled" }`, no payload, and its no-more-work contract — `node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts:561-563`; `waitForIdle` only on `ExtensionCommandContext` (types.d.ts:254-258) — **confirms the EV-43 finding first-hand**.
- Falsifier file read in full (`ev43/ev43-falsifier-extension.ts`): treatment arm produced a second assistant message in both branches; headless used poll-`isIdle()` + guarded `waitForIdle`; send-inside-handler is what was proven — **not** a send deferred past handler return.
- index.ts:66-81 (`before_provider_request` floor patch, applies per-payload regardless of originating run), :379-385 (sole `agent_settled` handler), :439-454 (TUI/headless send branch).
- retry.ts:57-59 (`PROVIDER_FINISH_REASON_ERROR` — **with colon**; note the falsifier's injected message omitted the colon, so EV-40's parent-turn classifier must match pi's real emitted literal, and test/retry.test.ts's bundle-derived assertion is the guard).
- seats.ts:229-267 (`DEFAULT_RETRY_POLICY`, `loadRetryConfig` full-return/throw semantics); `.council.json` carries no `retry` key (read first-hand); model-floors.json has exactly the deepseek entry.
- agent-session.d.ts:64-67: `auto_retry_start/end` events exist — pi's own retry machinery is real and runs *before* settle.

## **Risks / unknowns**

1. **Headless teardown vs deferred send (highest risk).** The falsifier sent inside the handler. If the send is deferred by the backoff timer until *after* the handler returns, print mode may tear the runtime down before the nested run starts. Mitigation if confirmed: in headless, await the backoff + send inside the handler (blocking settle is harmless headless and is exactly the delay we want); in TUI, arm-and-return.
2. Whether the TUI stays interactive while the awaited settle chain blocks (only if mitigation (1) is needed in TUI too) — unverified.
3. Esc capture while the user's editor owns focus — only the EV-8 overlay pattern is proven; cost is real, hence the split.
4. Stacking with pi's `auto_retry_start` for errors pi *does* retry — needs a judge ruling, not a silent choice.
5. Uncertainty, stated plainly: I did not verify `ctx.abort()`'s behavior while idle, nor whether a widget (non-focused) can receive Escape — both are checkable in implementation.

## **Predictions**

- **P1:** A continuation sent from an `agent_settled` handler via timer-deferral in `-p` mode produces no assistant message if the handler returns first (teardown) — falsifiable with a two-arm variant of the EV-43 falsifier (send-after-1s-timer vs send-immediately). This test should be EV-40's first commit.
- **P2:** A continuation for `deepseek/deepseek-v4-pro-0813` re-enters `before_provider_request` and carries `max_completion_tokens = 131072` (index.ts:66-81).
- **P3:** A turn aborted by the user (`stopReason: "aborted"`) schedules no retry under the design above.
- **P4:** `loadRetryConfig` read inside the settled handler on a malformed `.council.json` would throw inside the settle chain — supporting my read-once-at-init choice (seats.ts:269-282 throw semantics).

#### Principal position (`job-16.2`) — verbatim

# EV-40 — principal (first pass, deliberation only)

## Framing critique

The card's load-bearing premise — "No retry API exists for the parent's own provider call, so it is built from `agent_settled` plus a continuation send" — is **falsified by the installed pi**. Verified first-hand: pi ships a first-class parent-turn auto-retry loop.

- `core/agent-session.js:2286` `_prepareRetry(message)`: `_retryAttempt++`, `delayMs = baseDelayMs * 2 ** (attempt-1)`, abortable `sleep(delayMs, this._retryAbortController.signal)`; `:2335 abortRetry()`; `:2342 autoRetryEnabled`; `:2339 isRetrying`.
- Settings are a real policy surface: `core/settings-manager.d.ts:17-22` `RetrySettings { enabled?, maxRetries?, baseDelayMs?, provider? }`, read at `agent-session.js:2287`, also honoured by compaction/branch-summary summarisation (`:1453`, `:2552`).
- The gate is a **pure predicate**, not a hook: `_isRetryableError` (`:2248-2253`) → `isRetryableAssistantError(message)` (`node_modules/@earendil-works/pi-ai/dist/utils/retry.js:167`) which reads only `{stopReason, errorMessage}` against `RETRYABLE_PROVIDER_ERROR_PATTERN` (`retry.js:20-60`). `Provider finish_reason: error` is absent from that list — that, and only that, is why the intake doesn't retry.
- The loop already drops the errored assistant message before re-issuing (`agent-session.js:2306-2310`, `messages.slice(0,-1)`) and already renders an Esc-abortable indicator (`modes/interactive/interactive-mode.js:2802-2816`).

So the seam is not "who re-issues the turn". It is **where the retryable verdict enters pi's loop**. The card builds a parallel loop one layer up, and therefore has to re-earn four semantics pi already has: budget/reset (`:410-417`), error-message drop, abort, and surface. A continuation send does *not* drop the errored assistant turn — it appends a user message after it. That is a different conversation shape and my highest-risk prediction (below).

Second premise mismatch: `AgentSettledEvent` is `{type:"agent_settled"}` with no payload (`core/extensions/types.d.ts:559-562`). The card's signal is not *on* that event; it must be recovered from session state. `AgentEndEvent.messages` (`:554-558`) and `MessageEndEvent.message` (`:603-606`) carry it directly, and `message_end` additionally allows replacement (`MessageEndEventResult{message?}` `:841-844`; applied in place at `agent-session.js:515-525`).

## Recommended mechanism (and rejected alternatives)

**Push the verdict down, don't stack a loop on top.** Ranked:

1. *(Preferred, needs pi)* Widen the predicate pi itself consults. No such API exists today — I found no retry/verdict hook in `ExtensionAPI.on` (`types.d.ts:918-937`), none in `ExtensionContextActions` (`:1266-1279`), and `isRetryableAssistantError` is not in `pi-coding-agent`'s public exports (only re-exported internally via `@earendil-works/pi-ai/compat`, `agent-session.js:18`). This is a small upstream ask (a retryable-verdict hook or configurable extra patterns) that would make EPIC-9's entire parent half pi-owned.

2. *(Rejected)* The `message_end` rewrite hack — return `{message: {...m, errorMessage: "overloaded"}}` so pi retries. It **provably works** (`:384` emit awaited, then `:404 _lastAssistantMessage = event.message`, then `:793` → `_prepareRetry`), but it persists a falsified `errorMessage` into the session JSONL (`:388-400 appendMessage`), corrupting EV-42's per-attempt identity, usage attribution and `runs/` transcripts (AGENTS.md §12). Reject.

3. *(Fallback, if council owns it)* Reuse pi's *semantics* without lying:
   - **Detect** at `message_end` (payload present; fires before `agent_end` and settle).
   - **Drop, don't append**: filter the errored assistant message out of the next request via the `context` hook (`ContextEvent{messages}` `:514-517` / `ContextEventResult{messages?}` `:814-816`) — this reproduces pi's `slice(0,-1)` without mutating persisted history. Do **not** issue the retry from `message_end`/`agent_end`: pi's own `_willRetryAfterAgentEnd` (`:429-441`) may be about to retry and you would double-issue.
   - **Re-issue** at `agent_settled` only — the one event documented as "no automatic retry, compaction, or queued continuation will run" (`types.d.ts:559`), consistent with EV-43's measured falsifier.
   - **Timer**: `setTimeout(...).unref()`, cleared in `session_shutdown` (pattern already in `extensions/index.ts:388-405`). A new prompt during backoff must **abort** the pending retry (R5's Esc semantics), else a stale "Continue" is injected after the human's own message. Never let the pending timer touch `ctx` unguarded — wiki [[headless-pi]]: captured ctx getters `assertActive()` and throw after session replacement.

## Evidence

Verified first-hand: `agent-session.js` lines cited above; `types.d.ts:554-562, 603-606, 814-816, 841-844, 918-937, 1266-1279`; `settings-manager.d.ts:17-22`; `pi-ai/dist/utils/retry.js:1-2, 20-60, 167-175`; `interactive-mode.js:2802-2824`; `extensions/index.ts:66-81` (max-tokens floor on `before_provider_request` — a continuation re-enters it, so the floor holds), `:379-385` (`agent_settled`), `:442-455` (TUI fire-and-forget vs poll-`isIdle()`+`waitForIdle`); `extensions/hub.ts:275-311` (no attempt notion today); wiki [[headless-pi]], [[principal]].

Inferred, not verified: that an OpenAI-compatible provider rejects a conversation ending in an empty errored assistant message followed by a user message; that no `context`-hook reset hazard exists across the retry boundary.

## Interfaces/risks

- **R5's surface is not implementable as written.** `ExtensionUIContext` has no input-bar text API: `notify` (`:76`), `setStatus(key, text)` footer (`:79-80`), `setWidget` above/below editor (`:96-100`), `setEditorText`/`getEditorText` (`:131-133`). "Input bar reads …" must be re-pinned to the status row or a widget row now — and `setEditorText` must be forbidden (clobbers typing). If option 1 lands, R5's surface is pi's `RetryStatusIndicator` for free.
- **Shared interface with EV-39/EV-42**: exactly one thing needs fixing now — an **attempt-identity shape** (attempt index + parent prompt-run id) and one attempt counter per subject. Parent loop keys on a provider call; hub loop keys on a settled child report (`hub.ts:275-311` has no attempt concept). Neither should read the other's counter; both should stamp the same field names. That is a ruling, not code — and it must be decided before either card implements.
- **Backoff policy mismatch**: `.council.json retry` has `maxDelayMs` + `jitter` (`seats.ts:230-235, 267-309`); pi's `RetrySettings` has neither (unbounded `2**n`, no jitter). Option 1 cannot honour R2/R3 without an upstream change; the fallback loop can.

## Split judgment

Split — but not along the line the card predicts. EV-43 already proved the continuation path **is** reachable, so reachability is not the split criterion. Split by **who owns the affordance**: (a) resolve pi-owned-retry vs council-owned-retry (a ruling with option 1's upstream ask as its output), then (b) the parent-turn loop + attempt identity, (c) the R5 surface/copy/abort. If (a) resolves to option 1, (c) collapses to copy text only.

**Falsifiable predictions**
1. A handler that re-issues after `stopReason:"error"` **without** filtering the errored assistant message via `context` produces a second `message_end` with a *different* `errorMessage` (payload validation), not the same provider error. Control arm: same test with the `context` filter.
2. With pi's `retry` settings at `{maxRetries:3, baseDelayMs:2000}` and an `errorMessage` containing `overloaded`, the session emits `auto_retry_start` with `delayMs===2000` then `4000`, and Esc calls `abortRetry()` — proving the pi-owned path is reachable from config alone.
3. An `agent_settled` handler that writes the R5 string via `setEditorText` changes `ctx.ui.getEditorText()` while a human is typing (test asserts the deltas differ); the same write via `setStatus`/`setWidget` does not.

#### Designer position (`job-16.3`) — verbatim

# Designer Position — EV-40 Surface State Machine Around R5

## Surface state machine

A person who hits this surface traverses six states, four of them visible:

| # | State | Surface they see | Source / mechanism |
|---|---|---|---|
| 1 | **idle** | empty input bar (or pi default placeholder) | pre-state |
| 2 | **turn in flight** | pi's own streaming render; council surfaces (active-jobs widget, inline tree) unaffected | `widgetLines` ticker at `extensions/index.ts:336`, 5s |
| 3 | **provider error** | **invisible as a state** — `agent_settled` fires (`extensions/index.ts:379`) and the next visible thing is the countdown. Partial streamed assistant text is *already in the transcript above* the input bar (pi writes it during streaming; council never intervenes). | `agent_settled` event |
| 4 | **backoff (countdown)** | ruled string in input bar / headless stdout | R5 |
| 5 | **attempt N** | `sendUserMessage` fires (`extensions/index.ts:442` TUI fire-and-forget; `:448–454` headless pattern). Returns to state 2. | EV-43 falsifier, closed-green |
| 6 | **exhaustion** | ruled terminal copy; session halts | R5 |

**The transition #3→#4 is the largest design gap:** no surface ever says "the provider errored because…" — only the countdown appears. The error reason is the literal `Provider finish_reason: error` (R1, `extensions/retry.ts:54`), and a person who sees the countdown with no error context cannot form a correct conceptual model of *why* they're waiting. State 3 is invisible by design; that is the ruling. The Design position holds it should at minimum leave a transient toast/log line naming the failure, distinct from the countdown itself.

**EV-43's skeptic falsifier (card EV-43 §9, observation 7)** proved that **no input-bar text delta exists today in any arm** — the only visible delta is in conversation-tail rows above the input bar. R5 names "input bar" as the location; the implementation will have to *create* an input-bar text surface where today none exists (likely via `setEditorComponent` in the `installTreeEditor` pattern at `navigator.ts:348–356`). This is the largest open seam and the seat flags it as a ruling tension, not a settled fact.

## Discoverability / failure tolerance

The affordance `Esc to abort` is **one string-embedded hint inside a passive countdown.** No styled glyph (no ⎋, no styled span), no separate hint line, no second-row affordance list. A person who didn't read the R5 ruling cold may read "Retrying in 2s (attempt 2 of 3)" as informational, miss the dash-and-verb pattern, and never realise they can cancel. The 1-second tick is itself the only corroborating cue that the system is still working.

If they ignore Esc, the countdown completes, the retry fires, and the budget ticks. After 3 attempts the conceptual-model payoff lands: they are *forced* into the terminal state because nothing else is offered. This is acceptable for a transient error but punishing for a *permanent* provider outage — three ~30s waits then a stop, with no "give up early" surface other than Esc.

## Terminal state model — the load-bearing ambiguity

R5: `Retries exhausted after 3 attempts. The provider kept failing. Press Enter to try again.`

The copy says "try again" — but "again" is genuinely ambiguous along three axes, and R5 doesn't pick:

1. **What does Enter do?** Re-send the *same* user prompt that just failed (the continuation behavior R5's mechanism describes), or return to a fresh idle input bar? The word "again" reads either way.
2. **What survives in the transcript?** The failed-turn's partial assistant tokens are *already committed to the JSONL* before `agent_settled` fires. Enter does not roll them back. The person will see `…partial assistant text… / countdown / exhaustion copy / Press Enter / (next attempt) → new assistant entry` — a stacked history, not a clean retry.
3. **Is the attempt budget reset?** Enter does what: one more attempt with the budget already exhausted, or a fresh `1 of 3`?

Conceptual model the ruling hands the person: **a paused, resumable state** — *not* an error, *not* idle. The affordance name "Press Enter" implies the same surface already has Enter-bindable behavior (it does — submit), so the affordance is at least signposted by the host widget. The Design position: rule these three axes explicitly before implementation; the current copy alone does not close the Gulf of Evaluation.

## Headless parity

EV-43 closed-green established that the headless pattern requires fire-then-poll-`ctx.isIdle()` then `await ctx.waitForIdle()` (`extensions/index.ts:448–454`), because `agent_settled`'s event context lacks `waitForIdle`. For a backoff loop the same pattern must wrap **each retry's** continuation — otherwise the parent process exits between agent_settled and the next attempt's sendUserMessage, and the countdown stdout line is observed by no one.

What the headless stdout line must carry to preserve TUI parity:
- attempt `n/N` + remaining `s` (or trigger for 1s re-emit)
- an *abort* affordance — but headless has no Esc. The only honest equivalents are SIGINT to the parent or a JSONL marker a wrapper watches. R5 does not name one; this is open.
- on exhaustion, the terminal copy + an exit code that *signals retryable exhaustion* distinctly from generic non-zero.

What is lost: editability, the per-second tick (headless is line-buffered), and the `Press Enter` affordance. The ruled copy's last clause has no headless equivalent — `Enter` in a `-p` shell means nothing. This is a **headless design question**, not a copy edit.

## Falsifiable predictions (labeled)

All below are hypotheses for Skeptic / owner to test as out-of-band CDP/TUI smokes. None are settled design points.

1. **P1 (hypothesis):** On `agent_settled` with stopReason=error + literal `Provider finish_reason: error`, the input bar's rendered text changes from empty/placeholder to `Retrying in 2s (attempt 2 of 3) — Esc to abort` at t=0 and re-emits at 1s ticks. **Smoke:** inject provider error; capture input-bar frame at t=0, 1, 2, 2.01; assert text delta sequence.
2. **P2 (hypothesis):** `Esc` during countdown aborts remaining retries and emits the **same** terminal copy as exhaustion, *not* a distinct "cancelled" copy. **Smoke:** countdown visible; press Esc; assert terminal string and that no further `agent_settled` fires. The alternative (Esc → distinct copy) is the ruling choice to test.
3. **P3 (hypothesis):** Already-streamed partial assistant tokens remain in the transcript above the countdown line; the next attempt's response is appended below. **Smoke:** force a mid-stream provider error; assert transcript JSONL contains the partial assistant entry followed by the next assistant entry.
4. **P4 (hypothesis):** In `-p` mode, the parent process does NOT exit between `agent_settled` and the next retry's `agent_settled` — it holds via the waitForIdle pattern. **Smoke:** pi -p with injected error; assert the parent process is alive at t=baseDelayMs+500ms and that stdout lines for each tick arrive.
5. **P5 (hypothesis):** If the user types into the input bar during countdown, the typed text is preserved in the input-bar buffer and submitted on the next user-Enter *after* the retry turn settles — it is **not** lost, **not** sent as part of the continuation. **Smoke:** countdown visible; type "hello"; wait for retry to settle; press Enter; assert the next assistant message replies to "hello", not to the original failed prompt.
6. **P6 (hypothesis):** The countdown text lives in the input-bar region (per R5) and does not visually conflict with the inline tree widget (`setWidget(COUNCIL_TREE_WIDGET_KEY, …, { placement: "belowEditor" })` at `navigator.ts:565`) or the active-jobs `council` widget at `extensions/index.ts:325`, because the three occupy distinct TUI planes. **Smoke:** countdown ticking with both widgets active; assert no occlusion in a 24-row terminal.
7. **P7 (hypothesis, the ruling tension):** An input-bar text delta is *not* producible by the current extension API — the only available writes are `setEditorComponent` (used at `navigator.ts:350`), `setWidget(..., { placement: "belowEditor" })`, `notify`. **Smoke:** verify the implementation route; if `setEditorComponent` is the chosen seam, the input bar's text reads from a council-owned editor subclass, not from the host's editor — a *new* affordance surface, not a delta of the existing one. This contradicts R5's "input bar reads …" wording if the host input bar is not addressable.

## Open design questions

1. **Input-bar write route** — `setEditorComponent` (parallel to `installTreeEditor`), or relocate R5's countdown to the `council` widget area, or extend pi's API? EV-43's empirical finding says option (a) is the only available route; R5's wording says the host input bar. These are in tension.
2. **What "Press Enter to try again" does** — same prompt re-sent? fresh idle? budget reset?  R5 leaves three axes open; rule them.
3. **Headless abort affordance** — SIGINT, JSONL marker, or some other signal?  R5 names no headless equivalent of `Esc`.
4. **Headless `Press Enter`** — no equivalent in `-p`/json/rpc; is the terminal state reached via process exit code only, with the named copy as a final stdout line?
5. **User typing during backoff** — preserved, queued, or discarded by the continuation `sendUserMessage`?  No ruling names the interaction; the input bar's edit semantics during a council-controlled countdown are unspecified.
6. **Per-second tick source** — current timers in the codebase are 5s (council widget), 2s (tree), 1s (transcript). The countdown needs a 1s timer that starts at `agent_settled` and stops at `agent_settled` (next) or Esc. Where does it live — `extensions/index.ts` parent handler, or a new module? Cross-seam concern for principal.

