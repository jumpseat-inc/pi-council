---
id: EV-42
title: Per-attempt identity for a retried dispatch in the run substrate
state: Deliberating
owner: null
epic: EPIC-9
goal: Every attempt of a retried dispatch is recoverable from that dispatch's manifest alone — its session transcript and its spend — and the dispatch's provider-reported figure sums every attempt.
---

## Note

Re-scoped in place (same id, same slot) before dispatch, under the EV-39
step-6 steward ruling, because the settled cardinality — one job id, one
manifest, and one job-tree row per dispatch, with an `attempt` field and
carried cumulative usage — made this card's original premise false. `attempt`
is fixed by the EV-40 design agreement and may not be renamed; `attemptGroupId`
is dropped, because under that cardinality the dispatch's job id is already
the group key.

## Intent

A retried dispatch keeps one job id, one manifest, and one row. What is still
missing is per-attempt provenance: each attempt's session transcript and spend
must be recoverable from the manifest alone, and the dispatch's
provider-reported figure must sum every attempt rather than reflecting only
the final one.

EV-39 discloses the interim partial reported figure machine-readably (a
record-only reason on the provider sibling, rendered through the usage
block's conditional legend); this card closes that disclosure by persisting
the per-attempt session pointers on the manifest and walking that list in
`usage-store`, so the reported figure becomes whole.

## Run record (features-deliver / EV-42)

### Step 1 — classification (facilitator)

- **Path: full council.** The card is cross-seam and spec-ambiguous. It
  touches the run substrate (`extensions/runs.ts` `RunManifest`), the hub's
  manifest writer and re-spawn path (`extensions/hub.ts`
  `writeJobManifest`/`spawnJob`/`respawn` and the `Job` record), the dispatch
  layer that mints per-attempt session ids (`extensions/hub-tools.ts`
  `attemptSpec`, `extensions/job-retry.ts` `RetryAttemptSpec`), the durable
  usage write path (`extensions/usage-store.ts` provider-job harvest), the
  provider sibling's reason vocabulary (`extensions/provider-cost.ts`), and
  the user-visible usage block (`extensions/usage-block.ts` conditional
  legend). That is four-plus seams, not one area. It is also
  spec-ambiguous: the `goal` fixes the outcome (every attempt's transcript
  and spend recoverable from the manifest alone; the provider-reported figure
  sums every attempt) but the mechanism is open — what the manifest field is
  named and shaped, whether per-attempt spend is persisted as tuples or
  derived from the per-attempt session JSONLs, how the one-manifest-to-N-jobs
  mapping attributes generations, what happens to the provider report's
  worst-of `status` when one attempt errored before producing a generation,
  and whether the `partial` reason retires or survives in a new form. Those
  are design judgment, not one implementation choice. Per council.md step 1
  this card runs steps 2–6 and then 7–14.
- **Surface-touching: yes (recorded).** The card changes what a person reads
  in the durable usage block: today a retried dispatch's persisted record
  carries `provider.partial = "final-attempt-only"` and
  `extensions/usage-block.ts:32,118-120` renders the legend
  `usage  partial = reported figure is final-attempt-only`; closing the
  disclosure means that string stops rendering for a whole report (and may be
  replaced by different copy when some attempt's session is unaccounted for).
  That is user-visible copy on a real surface. A full-council,
  surface-touching card seats `designer` as a third generator in steps 2–3;
  its subject matter is the usage block's legend/partial copy after
  wholeness, and the honest display of a still-partial figure.
- **Rulings applied (Phase 1, binding):** **R1** (classifier superset —
  `extensions/retry.ts` `classifyRetry`), **R2/R3** (retry defaults
  `enabled:true, maxAttempts:3, baseDelayMs:2000, maxDelayMs:30000,
  jitter:true` + validation — `extensions/seats.ts` `loadRetryConfig`,
  mirrored in `council/scaffold/.council.json`): this card consumes the
  policy/classifier as settled inputs and changes neither. **R4** (one
  job-tree row per dispatch, labeled `attempt N/M` while `N > 1`) fixed the
  manifest cardinality this card must build on — one job id / one manifest /
  one row (`attempt` on the manifest, carried cumulative usage); it is
  applied, not reopened. **R5** (parent-turn retry copy) is EV-40's surface
  and does not reach this card's usage-block copy; no ruling is extended to a
  question it did not answer.
- **Steward ruling carried (EV-39 step 6, Ruling 2 — binding):** *(Escalation
  1)* EV-42's premise text was re-scoped in place (same id, same slot) before
  dispatch — the cardinality (A) the EV-39 run adopted made the original
  "fresh id/manifest/session per attempt" premise false; **`attemptGroupId`
  is dropped** and `attempt` is fixed by the EV-40 §2.8 agreement and may not
  be renamed. *(Escalation 2, Q4)* the final-attempt-only provider figure was
  disclosed in EV-39 and **the wholeness fix is this card's** — "not a fix in
  EV-39, not a separate card, not a permanent residual". G4's wholeness
  assertion moved to EV-42. Both rulings are recorded verbatim at
  `council/cards/EV-39.md` §Step 6 and are applied here as the card's
  authority.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `principal`, `designer`, `skeptic`, `consolidator`, and `judge`.
  All six resolve by name: packaged seats at
  `/home/tista/.pi/agent/git/github.com/jumpseat-inc/pi-council/council/agents/*.md`,
  each file's `name:` line matching its filename, verified first-hand this
  run. This checkout has **no `.pi/agents/`** directory (verified:
  `ls .pi/agents/` → no such directory), so nothing shadows them and no
  repo-local override exists. `product-owner` / `steward` are not dispatched
  by this container (escalation-only). No `Unknown seat` is possible on the
  seats named here.
- **Environment (facilitator-read, first-hand):** main checkout clean
  (`git status --porcelain` empty) at
  `5ac8b7760478eaeefd7d4ae86e266c85052d1717` == `origin/main`; `gh`
  authenticated as `tistaharahap`; no open PRs (`gh pr list` → `[]`). EV-37,
  EV-38, EV-43, EV-40 and EV-39 are ancestors of `HEAD` with their cards
  `Done` (EV-39 merged as `79c0573`). This card was `Ready`; no `Needs Human`
  state and no outstanding ruling — deterministic merge check criterion 5
  holds at the start of the card. No `ev-42` worktree exists (`git worktree
  list`), so no prior container left artifacts.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md`
  does not exist here):** `bash council/preflight.sh EV-42`,
  `bunx tsc --noEmit`, `bun test`, `python3 council/validate.py` (AGENTS.md
  §Commands + `.github/workflows/gates.yml`). This repo defines no
  database/import/server gate; `COUNCIL_INTEGRATION=1` stays gated and is not
  run by the default suite. The `preflight.sh` branch-freshness line
  (FLLWUP-27) fires by construction once a facilitator record commit advances
  `origin/main` past a branch cut — recorded verbatim, never reclassified,
  and never used to weaken a criterion.
- **Grounded facts carried into deliberation (inputs, not a settled design;
  the Council verifies them at this tree):**
  - **Cardinality (A) shipped.** One job id, one `<id>.json` manifest, one
    job-tree row per dispatch. `Hub.respawn` (`extensions/hub.ts:186-221`)
    reuses the id, carries `prev.usage` cumulative, keeps `startedAt`, and
    re-bases `attemptStartedAt`; `writeJobManifest`
    (`extensions/hub.ts:111-128`) writes `sessionId: job.sessionId ?? job.id`
    (the **current** attempt's session id only), `attempt` (present only when
    `> 1`), and `nextAttemptAt`.
  - **Per-attempt session ids exist on disk but are unlinked from the
    manifest.** `council_dispatch` arms `createRetrySupervisor` with
    `attemptSpec: (n) => { sessionId = \`${jobId}-attempt${n}\`; … }`
    (`extensions/hub-tools.ts:269-276`); each attempt spawns
    `pi -p --session-id <id>-attemptN` into the same run dir. The run dir
    therefore already holds attempt 1's and attempt 2's JSONLs, but
    `RunManifest` (`extensions/runs.ts:44-62`) carries no list of them — this
    is the gap.
  - **The provider figure is built from one session path per manifest.**
    `extensions/usage-store.ts:421-427` builds
    `jobs = manifests.filter(openrouter ∧ startedAt ≥ markerAt).map(m => ({
    jobId: m.id, model: m.model, sessionPath: findSessionFile(repoRoot,
    runId, m.sessionId) ?? null }))` — `m.sessionId` is the final attempt's,
    so only the final attempt's generations are fetched. Line 443-448 then
    marks `partial: "final-attempt-only"` when any eligible manifest is at
    `attempt > 1`. **This is the exact line EV-42 replaces.**
  - **The reason vocabulary is record-only, rendered once.**
    `ProviderPartialReason = "final-attempt-only"`
    (`extensions/provider-cost.ts:40`), optional on `ProviderCostReport`
    (`:73`); `extensions/usage-block.ts:32` holds the legend literal and
    `:118-120` pushes it iff `provider.partial !== undefined`. `partial` has
    no place in the goal's rendered copy — it is a durable, machine-readable
    qualifier.
  - **`findSessionFile` resolves by scanning the run dir for a JSONL whose
    first line's `id` matches** (`extensions/runs.ts:139-160`) and returns the
    first hit, `undefined` when absent. `ProviderGeneration.jobId` is the R-7
    attribution key (`extensions/provider-cost.ts:52-56`); the transport is
    injected and the production one is the only network code
    (`provider-cost.ts`).
  - **Worst-of `status` is existing semantics.**
    `fetchProviderReport` sets `status: failures.length > 0 ?
    "unavailable" : "reported"` (`extensions/provider-cost.ts`), with a C3
    reason precedence (`no-api-key` > `session-missing` > `no-generation-id`
    > `timeout` > `fetch-failed:`). Under a multi-attempt job list, an attempt
    that produced no assistant generation (the ordinary shape of a
    provider-errored attempt) contributes a `no-generation-id` failure and
    can flip an otherwise summable report `unavailable`.
  - **The run dir is pruned to the last 15 runs** (`pruneRuns`,
    `extensions/runs.ts`); the durable store at
    `getAgentDir()/council/usage/` is what outlives it (wiki
    [[usage-store]]). "Recoverable from the manifest alone" is therefore
    bounded by run-dir pruning — the manifest can index the transcripts, it
    cannot outlive their deletion.
  - **The flush gate keeps the write whole.** `forestFullySettled` refuses to
    write while any manifest in the invocation window has `exitCode === null`
    (`extensions/usage-store.ts`), and a mid-backoff manifest is
    `state: retrying` with `exitCode: null` — so a retried dispatch is only
    ever persisted after its final attempt settles (EV-39 G3).
  - **Tests that pin today's behavior:** `test/usage-store.test.ts:845`
    ("a retried openrouter/ manifest persists
    provider.partial='final-attempt-only' … a non-retried one does not"),
    `test/usage-block.test.ts:612` (legend renders iff `partial` present),
    `test/job-retry.test.ts:248-270` (one manifest, `attempt: 2`,
    `sessionId == <id>-attempt2`), `test/runs.test.ts:197` (`attempt`
    round-trip; plain manifests carry neither key).
- **Watch items for the deliberation:**
  - **G4 (wholeness assertion) is this card's** per the steward ruling — its
    live form is network-gated (`COUNCIL_INTEGRATION=1`) and therefore not
    part of the default suite; the default-suite form must still assert
    wholeness off injected transports.
  - **Byte-identity for a non-retried dispatch** (EV-39 G1/P4): a single
    attempt's manifest and the pre-EV-42 usage record must stay
    byte-identical. Any new field must be conditional.
  - **AGENTS.md §7** binds: `hub.ts`'s stall/timeout/kill semantics are
    battle-tested — additive, individually-tested deltas only.
  - **The session-id suffix `<id>-attemptN` is provisional** and this card
    finalizes it (EV-39 record, steward Escalation 1).

### Step 2 — independent first pass (round 1)

`Deliberating` set on the card frontmatter and `council/board.md` (EV-42 moved
from `Ready` to `Deliberating`), `python3 council/validate.py` clean, then
`owner`, `principal`, and `designer` dispatched **in parallel** (surface-touching
full-council card) — each given **only the card** (its frontmatter, `## Note`,
and `## Intent`), never another seat's position. Job IDs and positions are
recorded below as they settle.

Jobs: `owner` `job-23.1` (13 turns, 7.0 m, 653,497 tokens), `principal` `job-23.2` (24 turns, 7.0 m, 2,128,196 tokens), `designer` `job-23.3` (9 turns, 7.0 m, 647,479 tokens). All three settled `done`. Positions below are extracted verbatim from the seat session JSONL under `.pi/council/runs/` (the hub's trailing usage footer is omitted).

#### Owner position (`job-23.1`) — verbatim

# EV-42 — owner first-pass position

## Approach

**The substrate is one field away from done; the store's walk is the second half.** At this tree the manifest already carries `attempt` (runs.ts:58) and cumulative usage (carried by `respawn`, hub.ts:183 `...prev`), and every attempt already writes its own session JSONL under a distinct session id (`${jobId}-attempt${n}`, hub-tools.ts:270, O-4 naming — I would finalize it exactly as-is). What is missing is the *pointer list* on the manifest and the *walk* in the flush. Four seams, in dependency order:

**1. `extensions/runs.ts` — the manifest field.** `RunManifest` gains:

```ts
/** EV-42 — per-attempt provenance. Present iff attempt > 1 (same gate as
 * `attempt`); complete across attempts 1..N, ordered by n. */
attempts?: { n: number; sessionId: string }[];
```

Deliberately **minimal**: `n` + `sessionId`. No `startedAt`, no per-attempt exitCode, no per-attempt usage (see tradeoffs). Invariant, test-pinned: `attempts[attempts.length-1].sessionId === manifest.sessionId` (the current-attempt `sessionId`, hub.ts:115, keeps its existing meaning for `findSessionFile` consumers — navigator.ts:341/464 tail the live attempt through it).

**2. `extensions/hub.ts` — bookkeeping only, no behavior change.** `Job` gains `attempts?: {n, sessionId}[]`; `spawnJob` seeds `[{n:1, sessionId: opts.sessionId ?? id}]`; `respawn` appends `{n: prev.attempt ?? 1 + ...}` — concretely, `respawn` (hub.ts:164-215) appends the new attempt's record from `spec.sessionId` when it re-bases the clock; `writeJobManifest` (hub.ts:96-118) emits `attempts` under the same `attempt !== undefined && attempt > 1` gate as `attempt` itself. This is additive inside a §7-stable file, and each piece is reachable by a failing test first. No retry-semantics line changes.

**3. `extensions/usage-store.ts` — the one-manifest-to-N-jobs mapping.** Today the flush resolves **one** session path per manifest (usage-store.ts:421-427: `findSessionFile(..., m.sessionId) ?? null`) — which is why the figure is final-attempt-only. Replace with an expansion: for each invocation-window OpenRouter manifest, if `attempts` is present, emit **one job entry per attempt** `{jobId: m.id, model: m.model, attempt: a.n, sessionPath: findSessionFile(repoRoot, runId, a.sessionId) ?? null, zeroSpendOk: true}`; else the legacy single entry (no `attempt`, `zeroSpendOk` false) with byte-identical behavior. Then **delete the `retried` heuristic** (usage-store.ts:437-448) — but with one backward-compat carve-out below.

**4. `extensions/provider-cost.ts` — per-attempt entry semantics + attribution stamp.** `FetchProviderReportInput.jobs` entry gains optional `attempt?: number` and `zeroSpendOk?: boolean`; `ProviderGeneration` gains optional `attempt?: number` stamped by `toGeneration` (provider-cost.ts:231-260). Attribution stays R-7: **every generation of every attempt carries the same `jobId` — the dispatch id**; the attempt ordinal disambiguates without forking the id space, which is exactly why `attemptGroupId` could be dropped. The per-entry rule in the fetch loop (provider-cost.ts:282-300) becomes:

- `sessionPath === null` → `session-missing` failure (unchanged);
- readable, zero ids, `zeroSpendOk` → **contributes zero, not a failure** (new — see justification below);
- readable, zero ids, legacy entry → `no-generation-id` (unchanged, byte-identity);
- unreadable/corrupt file on a `zeroSpendOk` entry → `session-missing` (indistinguishable from missing once you own a pointer to it; legacy stays `no-generation-id`).
- `totalCost` = Σ over reported generations across all attempts — the existing reduce (provider-cost.ts:345-352) needs no change beyond the entry expansion.

**Per-attempt spend: derived, never persisted.** The goal says "transcript **and spend** recoverable from the manifest alone" — the manifest alone plus the session file it names. Each attempt owns a private session id, so attempt N's spend is the assistant-usage sum over *its* JSONL (the read side already exists: spend.ts:214 accumulates `e.message.usage` from session entries). Persisting per-attempt usage tuples would require the Hub to snapshot its accumulator per attempt (new state), would duplicate the cumulative tuple's numbers, and buys nothing: the manifest and the JSONLs live in the same run dir and are pruned together — after pruning, neither survives, and the durable usage-store record (with the now-whole provider figure) is the only survivor by design ([[usage-store]]: `pointerSurvivable`/`ResolveOutcome`). The basis honesty is preserved too: derived spend is `session-reconciled`, the manifest tuple stays `stream-assistant` — two persisted numbers with one provenance each, never re-collapsed.

**`provider.partial` and the legend once whole.** The write path stops setting `partial` whenever the attempts list was walked completely — a reported report is whole *by construction*, so `partial: "final-attempt-only"` has no remaining true producer. But **do not delete the literal or the legend**: stored records are choose-once immutable (usage-store.ts:388-392), records persisted pre-EV-42 carry `partial` on disk (pinned by test/usage-store.test.ts:845-884), and `formatUsageBlock` must keep rendering `PARTIAL_LEGEND` for them (usage-block.ts:32, 120). The carve-out that keeps the fallback honest: an EV-39-era manifest (`attempt > 1`, no `attempts` list — the window between EV-39 and EV-42, or a provenance-less shape) still walks only the final attempt, so the flush **keeps marking `partial` for exactly that shape** (`attempt > 1 && attempts === undefined`). Without it, deleting the heuristic silently upgrades a still-partial figure to whole — the exact falsification EV-39's Q4 ruling exists to prevent.

**Missing attempt session file.** The figure is **whole or absent, never short-and-labeled**: a missing/unreadable attempt JSONL yields `session-missing` → report `unavailable` → the existing worst-of machinery (usage-block's C4 path, T-B4 semantics) renders the reported row for what *was* fetched plus `cost=n/a` on subtree and the n/a legend. I reject the alternative (sum resolvable attempts under a new `partial` reason): it re-opens the EV-39 falsification class with new copy and a new literal, for a case (one file of one run dir missing while its siblings survive) that `ResolveOutcome` already names a **defect signal** (`missing-unexpected`), not a by-design state. Nothing throws; no path is ever fabricated.

**Byte-identity / backward requirements.** (1) A single-attempt dispatch: no `attempts` key (snapshot-test the manifest bytes), legacy jobs entry, identical provider sibling, identical block. (2) `USAGE_RECORD_SCHEMA_VERSION` stays **2** — the record shape gains nothing new beyond optional `attempt` on generations; v1/v2 read tolerance untouched. (3) Old stored records with `partial` render identically. (4) Old manifests without `attempt`: legacy path. (5) `formatReport`/`formatWaitReport` untouched.

## Tradeoffs accepted

- **Minimal `attempts` entries (`{n, sessionId}`) omit per-attempt timing/exit state.** If someone later wants per-attempt timelines, that's an additive extension; I'm not pre-purchasing it. What I *am* deliberately giving up: a persisted per-attempt spend (argued above — divergence risk with zero post-pruning recoverability gain).
- **The unreadable-vs-empty asymmetry** (legacy `no-generation-id` vs `zeroSpendOk` `session-missing`) is two subtly different failure classes for single- vs multi-attempt manifests. I accept it because the legacy literal set and behavior are pinned by EV-29's C3 tests, and the new semantics are only *provable* when the manifest attests the attempt existed. If round-2 prefers one unified rule, the cost is changing legacy semantics for readable-empty single-attempt sessions from `unavailable/no-generation-id` to a zero-contributing report — a visible byte change to non-retried records I won't make silently.
- **`hub.ts` grows three small deltas** (seed/append/emit) inside a §7-stable file. The alternative — sidecar files written by the retry supervisor — fragments the substrate and violates the settled cardinality (one manifest per dispatch). The deltas are individually test-able and touch no kill/stall semantics.
- **Navigator browsing of *past* attempts' transcripts is explicitly out of scope** — the tree tails the current attempt via `manifest.sessionId`, which stays correct. Per-attempt transcript browsing in the navigator is a natural follow-up card, not this one.

## Testable claims

All failing-first; the harnesses exist (`test/hub.test.ts:419-441` fail-once stub; `test/usage-store.test.ts:845+` G4 retried-flush harness; injected `FetchGeneration` doubles per `test/provider-cost.test.ts`).

1. **T1 (manifest shape):** final manifest of a fail-once retry carries `attempt:2` **and** `attempts:[{n:1,sessionId:id},{n:2,sessionId:id+"-attempt2"}]`; a single-attempt manifest has no `attempts` key (byte snapshot). *Extends test/hub.test.ts:436.*
2. **T2 (invariant):** after budget exhaustion (`attempt:3`), `attempts` is complete 1..3 and `attempts[last].sessionId === manifest.sessionId` (currently `${id}-attempt3`).
3. **T3 (whole figure):** retried dispatch, two attempt JSONLs each carrying a distinct generation id with costs c1, c2 → persisted `provider.totalCost === c1+c2`, **no** `partial`, generations carry `attempt:1|2` and the same `jobId`. *Fails today: today's code resolves only attempt 2's file and sets `partial`.*
4. **T4 (zero-spend attempt):** attempt-1 file present, readable, zero assistant entries → report `reported`, `totalCost` = attempt 2's, no failure reason, no `partial`.
5. **T5 (missing file):** delete attempt-1's JSONL → `status:"unavailable"`, `reason:"session-missing"`, block renders reported row (attempt 2's) + subtree `cost=n/a` + the n/a legend — and **no** `partial` legend, no short-sum-labeled-whole record.
6. **T6 (fallback disclosure):** manifest shaped `attempt:2` with **no** `attempts` list → still persists `partial:"final-attempt-only"` (the EV-39 G4 assertion survives for that shape only).
7. **T7 (legacy render):** a stored record with `partial` renders the partial legend verbatim (usage-block test on a record read from disk).
8. **T8 (byte identity):** single-attempt flush → persisted record byte-identical to pre-EV-42 (snapshot), including the unreadable-file→`no-generation-id` path.

**Falsifiable predictions:**
- **P1:** Post-EV-42, a fully-retried dispatch's persisted provider figure equals the sum of its per-attempt generation costs — settled by T3; a green T3 falsifies the current tree, where the same fixture yields `partial:"final-attempt-only"` and c2 alone (test/usage-store.test.ts:877).
- **P2:** Derived per-attempt spend sums to the manifest's cumulative tuple within the stream-projection caveat: Σ assistant usage over both attempts' JSONLs ≈ final `manifest.usage`. Settle with a stub that emits usage in both attempts (the ACC_FIXTURE pattern, test/hub.test.ts:167-181).
- **P3:** Zero-generation-id attempts are *common*, not exotic — any attempt that dies on spawn error before the first assistant message produces one. T4's semantics (zero, not failure) therefore decide whether most real retried dispatches render `reported` or `n/a`.
- **P4:** No record is ever persisted mid-retry with a timing-partial figure: `forestFullySettled` (usage-store.ts:408-411) stays closed through the backoff window because `respawn`/`onSettle` retract `exitCode` to null (hub.ts D2). Test: drive the flush between attempts, assert `gate-closed`.

**Split judgment: keep the card whole.** The attempts list has exactly one consumer — the flush walk — and the walk has exactly one source: the list. Split, either half ships dead: a pointer field no reader resolves, or a reader of an unpublished shape. The invariant being delivered ("whole or absent, never short") lives across both seams and is only testable end-to-end (T3/T5). The one legitimate follow-up split is navigator-side per-attempt transcript browsing — surface work, zero coupling to the accounting invariant, and it should be named as out-of-scope rather than split out now.

**Inferred, flagged:** that pi's child session JSONL carries `message.usage` on assistant entries is verified on the *read* side (spend.ts:214, provider-cost.ts:110-113) and by ev40-live-gates P3 reading assistant entries; I did not run a live child to confirm the write side, and T2/P2's harness settles it cheaply. The `zeroSpendOk`/legacy asymmetry is my design proposal, not a settled pattern.

#### Principal position (`job-23.2`) — verbatim

## Cross-seam reading — what the contract is right now

Both sides opened first-hand at HEAD `5ac8b77`.

**Producer side (hub → manifest).** `respawn` reuses the job id, carries `usage` cumulatively, and sets the *next* attempt's session id (`hub.ts:186-221`); the manifest writer emits exactly one `sessionId` = the **current** attempt's, plus `attempt` only when `>1` and `nextAttemptAt` only while `retrying` (`hub.ts:111-128`, `runs.ts:44-62`). `council_dispatch` mints `${jobId}-attempt${n}` (`hub-tools.ts:269-278`), attempt 1 being the bare job id (`hub-tools.ts:249-254`, `hub.spawnJob` default `hub.ts:172`). `job.usage` accumulates in place (`hub.ts:255-275`) and `respawn`'s `...prev` spread (`hub.ts:197-201`) means the new Job **shares the same usage object** as the old one. Attempt-1 JSONL and attempt-2 JSONL both land in one run dir; `findSessionFile` matches on the header `id` (`runs.ts:140-159`).

Per-attempt spend is therefore **not retained anywhere**: only the cumulative tuple survives, and per-attempt identity is discarded at respawn.

**Consumer side (manifest → store → block).** The harvest maps *one job per manifest*, resolving `m.sessionId` — the final attempt's — to one session path (`usage-store.ts:421-427`), then post-hoc copies `partial:"final-attempt-only"` onto the report whenever any eligible manifest has `attempt>1` (`:443-448`). The module itself sets `status = failures.length>0 ? "unavailable" : "reported"`, where a per-job `sessionPath===null` contributes `session-missing` (`provider-cost.ts:285`) and zero ids contributes `no-generation-id` (`:297`) — so **a retried attempt that produced no generation can flip an otherwise fully-reported dispatch to `n/a`**. `ProviderGeneration.jobId` is the R-7 row key (`provider-cost.ts:52-56`); no attempt granularity exists on generations. The block renders the partial legend iff `provider.partial !== undefined` (`usage-block.ts:120`) from a single literal (`:32`).

## Blind spots, per slice

- **Hub/retry slice** cannot see that the store keys one session path per manifest, so it cannot know that "cumulative usage is enough" is false; and it cannot see that at the retrying settle the ordinal has *already advanced* (`job-retry.ts:75-80` mutates `job.attempt` before the hub's `writeJobManifest` at `hub.ts:341-347`) — inside its slice `job.attempt` reads as "the attempt that just ran".
- **Run-substrate slice** sees the manifest as the record; it cannot see the render legend or the report's worst-of, and so cannot see that "recoverable from the manifest alone" is time-bounded — `pruneRuns` deletes the run dir (`runs.ts:182-193`), and the manifest is *inside* it.
- **usage-store slice** sees the harvest and the choose-once write; it cannot see the hub's delta arithmetic (nothing in its view says whether a persisted per-attempt tuple sums to the top-level), nor the *pending* vs *completed* ordinal skew during backoff.
- **provider-cost slice** discovers the "no ids" fact but does **not own** the disclosure — `partial` is stamped one layer up (`usage-store.ts:443-448`), so the module cannot express "this attempt is unaccounted, that one is not".
- **Render slice** sees a `partial` literal; it cannot tell *which* attempts are missing, and it cannot see that durable records on disk carry literals a later vocabulary may not know — with `partial !== undefined` as the trigger, an unknown literal silently drops the qualifier (fail-open).
- **Navigator slice** resolves the same field two ways: the inline tail/view keys on `manifest.sessionId` (`navigator.ts:335,341,464`), while the modal `openTranscript` resolves `manifest.id` (`navigator.ts:869`) — which is attempt 1's session, not the current one. (Inferred, but grep-backed: `onActivate` is stored in `CustomTreeEditor` (`focus-nav.ts:282`) and has no call site in `focus-nav.ts`; Enter routes to `enterProgress` (`focus-nav.ts:318`), so the `manifest.id` path is latent, not live.)

**Wiki grounding:** `vault/wiki/index.md` → [[run-transcripts]], [[usage-accounting]], [[usage-store]], [[usage-block]], [[spend-record]], [[cost-provenance]], [[hub-job-supervision]]. The wiki has **no page for hub-level retry, the retry policy, or per-attempt identity** — the last ingest is EPIC-8 (2026-09-15, `vault/wiki/log.md`); EPIC-9's EV-37…EV-43 landed without an ingest. So the substrate authority is the code plus `council/cards/EV-39.md` §Step 6 (steward Ruling 2: the disclosure must be "machine-readable and durable … a stored figure that looks whole a month later is the defect") — which is itself the argument against treating `partial` as retiring.

## Reframe

The card's mechanism is right; two of its claims are wrong, and it misses that the real seam is a *field with three meanings*.

1. **"the dispatch's provider-reported figure sums every attempt" — not unconditionally.** An attempt that was stall-killed or died before a `responseId` was persisted may have been charged with no recoverable generation; `no-generation-id` is not evidence of zero. So wholeness must be *falsifiable*: `partial` does not retire, it **narrows**. It becomes an observable of the *unaccounted* attempt rather than an unconditional consequence of `attempt>1`.
2. **"recoverable from the manifest alone" is an index claim, not a durability claim.** The manifest can name the attempts while the run lives; only the store record outlives pruning. Per-attempt *provider* attribution must therefore also land on `ProviderGeneration` (record-only), or the provenance dies exactly where it is needed a month later.
3. **The seam is `RunManifest.sessionId`'s three meanings:** the store wants *all* attempts, the tail wants the *current* one, the legacy transcript path takes `manifest.id` (attempt 1), and during backoff `attempt` is the **pending** ordinal while `sessionId` is the **completed** attempt's (`hub.ts:126-127` + `:119`). One accessor in the substrate — not two derivations per consumer.

Concretely:

- **Manifest field:** `attempts?: AttemptRecord[]`, present only when `attempt > 1` (non-retried manifest byte-identical, EV-39 G1), each `{ attempt, sessionId, usage }` where `usage` is **this attempt's delta** over the stream tuple (`usageSource:"stream-assistant"`), so `Σ attempts[].usage == manifest.usage` metric-wise for all twelve numeric fields. `manifest.usage` **stays cumulative** — if it becomes the final attempt's, `spend.ts:250`/`sumSubtreeUsage` silently halve every retried dispatch's subtree half. "Settled attempts, in order" is the documented meaning; the pending attempt is `attempt` with `state==="retrying"`.
- **Substrate accessor:** `attemptEntries(m)` in `runs.ts` (returns `m.attempts ?? [{attempt: m.attempt ?? 1, sessionId: m.sessionId, usage: m.usage}]`) — the single derivation both the store and the UI consume.
- **Harvest:** flatten to one job entry per attempt, `{jobId: m.id, model: m.model, attempt, sessionPath: findSessionFile(…, a.sessionId) ?? null}`; prices nothing, changes no key. `delete` the `retried` block at `usage-store.ts:443-448` — the harvest no longer *computes* any disclosure.
- **`ProviderGeneration`:** `attempt?: number` additive (absent for a single-attempt job ⇒ old records byte-identical); `jobId` **stays the tree-row id** — do not mint `${jobId}-attempt2` as a job key, that is a key pointing at no row. `attemptGroupId` being dropped is only correct because this field now carries the attribution.
- **`status`/`reason` for an attempt with no generation:** the report is `reported` iff at least one entry reported; an entry contributing zero ids is **unaccounted**, not a failure flip. `status:"unavailable"` is reserved for (a) `no-api-key`, (b) a harvested generation that could not be fetched (`fetch-failed:`/`timeout`), (c) **every** entry unaccounted (which reproduces today's single-attempt shape byte-for-byte, so T-P8 and `provider-cost.test.ts:361-389` stay green). A **gap** (max ordinal > entry count for a jobId, i.e. a pre-EV-42 manifest) is fail-closed to the old literal.
- **`partial` and the legend:** `ProviderPartialReason = "final-attempt-only" | "attempts-unaccounted"`, plus record-only `unaccountedAttempts?: number[]`; legend lookup becomes a **total** map (unknown literal → a generic "figure is not whole" line), because choose-once means old records are never rewritten. The legend lives in the module's output, not in the caller's post-hoc copy, so `usage-block.ts:32,120` reads the same conditional as today with two literals; `partial`'s *absence* becomes meaningful for the first time — it now asserts "every attempt's generations were fetched".

## Testable claims

1. **Delta, not reference** (`hub.ts:197` aliasing): two attempts with distinct assistant usage → `manifest.attempts[0].usage.output === attempt1Output` and `Σ attempts[].usage[X] === manifest.usage[X]` ∀ numeric metrics. An implementation storing `prev.usage` by reference fails immediately.
2. **Ordinal at settle:** after a retryable attempt-1 settle, the `retrying` manifest reads `attempt: 2`, `nextAttemptAt` set, `attempts: [{attempt: 1, …}]` — never `attempts:[{attempt: 2}]`. (Requires the hub to snapshot the ordinal *before* calling `job.retry?.onSettle`.)
3. **Byte-identity:** a non-retried dispatch's manifest has neither `attempts` nor `attempt`; its persisted record equals today's fixture bytes (no generation `attempt`, no `partial`).
4. **Module-level disclosure:** two entries, attempt 1 `sessionPath:null`, attempt 2 with a generation → `status:"reported"`, `partial:"attempts-unaccounted"`, `unaccountedAttempts:[1]`, `totalCost` = attempt 2's, and no `n/a` legend. Single entry with `attempt:2` and a reporting session → `partial:"final-attempt-only"` (reproduces `usage-store.test.ts:845-885` at module level).
5. **All-unaccounted:** both entries null/empty → `status:"unavailable"`, C3 worst reason, `totalCost:null`, `n/a` legend — byte-identical to the pre-card path.
6. **Read-compat:** the block emits a legend for a report whose `partial` is the *old* literal — the fail-open guard.
7. **No double count:** 2-attempt manifest → `sumSubtreeUsage(...).subtree.cost === manifest.usage.cost` and `spendRecord`'s subtree half equals it.
8. **Attempt-1 reachability:** after a 2-attempt run with two JSONLs (`id` `job-1`, `id` `job-1-attempt2`), the harvest's transport call list contains **both** generation ids.
9. **G4 live (`COUNCIL_INTEGRATION=1`):** real 2-attempt openrouter dispatch → `reported` with generations from both attempts and `Σ = both`; negative arm (stall-killed attempt 1) → `partial` present.

## Split judgment

**Keep whole.** The goal's two clauses share one input, and the legend copy is a *claim about the harvest*: changing the copy without the harvest yields copy that is false or uncomputable, and shipping the field without the harvest ships a substrate nothing reads — EV-39's own test for splitting. The single item that must not be deferred even though it looks like polish: the legend map's totality over old literals (test 6), because that is the one place the change can make an existing durable record read as whole.

## Hazards the card does not name

- **Off-by-one at the retrying settle** (ordinal advanced before the write) — §above, test 2.
- **`partial` lost on old records** if the renderer's condition stays "presence of a literal" while the vocabulary changes.
- **Pre-EV-42 manifests inside the 15-run window** after a package upgrade carry `attempt>1` and no list: the harvest must fail closed to `final-attempt-only`, not assert wholeness.
- **Pruning bound, restated precisely:** the manifest list is ephemeral; per-attempt *token* spend is not durable at all — `SpendRecord` is frozen (`usage-store.ts:62-64`, EV-31) and the store's `subtree` half is a whole-forest sum (`spend.ts:250`), so durable per-attempt *spend* would require a wrapper-level field, a scope expansion the Note does not authorize. If the team reads "recoverable" as durable, this is a steward goal-wording question, not an implementation choice. (Also note `pid:null` during backoff makes the run look non-live to `pruneRuns`'s check at `runs.ts:186`.)
- **Flush cost:** the harvest becomes Σ attempts × generations on a serial loop with a 5 s per-generation `AbortSignal.timeout` (`provider-cost.ts:305`), inside awaited drains (`index.ts:751,765`); bounded, but linear in attempts in the teardown path.
- **Navigator divergence** (`navigator.ts:869` vs `:341/464`) — latent only; recommend a named follow-up rather than folding a navigator fix into this card.

#### Designer position (`job-23.3`) — verbatim

# Designer position — EV-42 (first-pass independent)

I've taken a first-pass independent position on EV-42's copy question. Full argument at `vault/raw/2026-09-16-design-ev42-partial-legend.md`. Summary in the output format below.

---

**Reconnaissance.** First-hand read of `extensions/usage-block.ts`, `extensions/provider-cost.ts`, `extensions/usage-store.ts:421-448` (the line EV-42 replaces — `jobs = manifests.filter(...).map(m => ({ sessionPath: findSessionFile(... m.sessionId) ?? null }))` plus the unconditional `partial: "final-attempt-only"` mark at `:443-448`), `extensions/runs.ts:43-62` (manifest schema — the slot for the per-attempt list), `extensions/job-retry.ts:36` (session-id suffix `${jobId}-attempt${n}`, O-4 provisional pending finalization), `vault/wiki/usage-block.md` (conditional-legend discipline), `vault/wiki/cost-provenance.md` (`(reported)` / `n/a` honesty contract), and the existing tests at `test/usage-block.test.ts:608-628` (EV-39 Q4 partial legend), `test/usage-store.test.ts:845+` (partial persistence), `test/provider-cost.test.ts`, `test/usage-block.test.ts:T-B3/T-B4` (unavailable vs reported-with-n/a). The wiki's `vault/wiki/index.md` `usage-block`, `usage-store`, `cost-provenance`, `usage-accounting` pages were the standing-concern entries.

---

<output_format>

**Design position.** Retire the literal `final-attempt-only` from the block's *display* vocabulary (the on-disk literal is preserved as audit history); introduce a single new record-only literal `attempt-unaccounted` for the post-EV-42 still-partial case; keep the legend key `partial` and update the wording to `usage  partial = reported figure excludes unaccounted attempts`; migrate legacy records on read so the surface divergence closes; the existing `n/a = provider figure unavailable` legend and predicate are unchanged. The block's grammar-identity constraint binds the proposal: no new rows, no new keys, no new ordering — only an updated literal and a wider aggregate (the routed multiset now sums across attempts under the existing rule). The full reported figure for a whole retried dispatch renders with no partial legend, in the same shape as a single-attempt's block (modulo the multiset's content).

**Gulf closed.** Gulf of Evaluation, for the person reading the durable usage block at any of the five autonomous exits (`/features-new`, `/features-deliver`, `/council`, `/council-eval`, `council-runner`). At the moment the person looks at the block and wonders "is this number my dispatch's whole figure?", the answer is now visibly honest: the partial legend is absent for whole reports and present with new wording for still-partial ones; the partial state is distinguished from the `n/a` state by its own key (`partial` vs `n/a`), answering different questions ("is the figure we have incomplete?" vs "do we have a figure at all?"). For a non-retried dispatch, the byte-identity invariant from EV-39 G1/P4 is preserved (the block shape is unchanged).

**Principle and evidence.**

- **Signifier (closed gap: figure integrity).** A absent-`partial` legend *signifies* wholeness; a present `usage  partial = reported figure excludes unaccounted attempts` *signifies* the partial state. Wording lands at `extensions/usage-block.ts:32` (replacing the existing `PARTIAL_LEGEND` constant) and `:118-120` (the conditional predicate, unchanged).
- **Distinction (forced by the constraint, not preference).** The two states — `unavailable` (slot absent, `n/a` legend) and `partial` (slot present but incomplete, `partial` legend) — are orthogonal: a record can carry both, neither, or one. The block grammar stacks them in a fixed order so a person reading top-to-bottom encounters the qualifier closest to what it qualifies. Evidence: today's `T-B4` (`test/usage-block.test.ts:572+`) already proves they coexist on a single render. The post-EV-42 widening makes the coexistence the *normal* retried case, not the edge.
- **Conceptual model (whole vs partial is a property of the figure, not of the slot).** Closing the EV-39 Q4 disclosure does not collapse the distinction — it makes the *default* whole and the *edge* partial, both named honestly. The mechanism always tries to fetch every attempt (`extensions/usage-store.ts:421-427` rebuilt to walk the manifest's per-attempt list, EV-42 deliverable); the new literal names what happens when that walk fails for some attempts. The migration-on-read means the surface wording is uniform across record vintages, closing the "permanent residual" the EV-39 steward ruling forbade.
- **Knowledge in the world.** The wording `"reported figure excludes unaccounted attempts"` puts the missing piece on the screen — the person does not need to remember what an "attempt" is or how per-attempt session harvesting works. The legend key `partial` is the same word the existing disclosure used, so a person who saw the EV-39 legend recognizes the slot.
- **Composition discipline (binding constraint).** PO C/D/E/F grammar-identity discipline at `extensions/usage-block.ts:29-32, 88-89, 116-119`: one literal per legend, fixed `usage  ` prefix, `key = short definition`, conditional on a record-only input. The proposal respects every clause: the `partial` key is preserved, the legend stack position is preserved, the conditional predicate (`provider.partial !== undefined`) is preserved.

**Falsifiable predictions** (each is a string-equality assertion the Skeptic can run; all five pin specific byte sequences in `extensions/usage-block.ts`):

1. **Whole retried dispatch — no partial legend.** A `providerReport` carrying `generations: [genAttempt1, genAttempt2]` with `partial` undefined renders as four rows (ownSession, subtree, boundary, reported) plus no `partial` substring anywhere. Assert: `expect(out).not.toContain("usage  partial")` and `expect(out.split("\n")).toHaveLength(4)` and `expect(out).not.toContain("final-attempt-only")`.
2. **Still-partial retried dispatch — new wording, byte-exact.** A `providerReport` with `status: "unavailable"`, `reason: "no-generation-id"`, `totalCost: 0.0042`, `partial: "attempt-unaccounted"` renders the legend byte-equal to `usage  partial = reported figure excludes unaccounted attempts`. Assert: `expect(out).toContain("usage  partial = reported figure excludes unaccounted attempts")` and the reported row still carries `cost=$0.0042 (reported)` and the `n/a` legend still renders as the last line.
3. **Legacy record migration — surface uniformity.** A `providerReport` carrying the legacy literal `partial: "final-attempt-only"` renders without the substring `final-attempt-only` anywhere in the output, and renders the new wording in its place. Assert: `expect(out).not.toContain("final-attempt-only")` and `expect(out).toContain("usage  partial = reported figure excludes unaccounted attempts")`. The on-disk literal stays in the record (the read predicate at `extensions/usage-store.ts:240-249` already tolerates extra fields; no schema change needed).
4. **Non-retried dispatch byte-identity.** A single-attempt dispatch renders byte-equal to pre-EV-42: no `partial` substring, no `excludes unaccounted attempts` substring, no `attempt-unaccounted` substring, four rows. Assert: `expect(out).not.toContain("partial")` and `expect(out).not.toContain("excludes unaccounted attempts")` and `expect(out).not.toContain("attempt-unaccounted")`.
5. **Conditional predicate unchanged.** The presence/absence of the partial legend is solely a function of `provider.partial !== undefined`, regardless of which literal value sits behind it. Assert: `expect(formatUsageBlock({ ...provider: { ...providerReport(), partial: undefined } })).toBe(formatUsageBlock({ ...provider: providerReport() }))` and `expect(formatUsageBlock({ ...provider: { ...providerReport(), partial: "attempt-unaccounted" } })).toContain("usage  partial = reported figure excludes unaccounted attempts")`.

I cannot run these myself (no browser, no `bun test` in design-seat mode, and the implementation is not yet on disk — these are predictions *of* the rendering after EV-42 lands, not observations of today). The Skeptic's role is to assert against the post-merge bytes.

**Preferences, ranked last.** I have no grounded preference on (a) the exact wording of the new legend (alternatives like `reported figure excludes unaccounted attempts` vs `some attempts were unaccounted for` are roughly equivalent in honesty and brevity — I lean the former for naming the consequence on the figure, not the failure mode); (b) the field shape for the per-attempt session list on `RunManifest` (`attemptSessions: string[]` vs a richer record per attempt — owner territory); (c) whether the legacy literal `final-attempt-only` is retired on disk or only on the surface (I lean migrate-on-read for audit history, but the type-shrink variant is defensible). I am grounded on (a) the `partial` legend key preservation (grammar-identity constraint), (b) the literal-set evolution (record-only, two values: legacy + new), (c) the conditional predicate (`provider.partial !== undefined`), and (d) the routed multiset aggregation across attempts under the existing ordering rule (the only honest way to surface upstream-routing variance).

</output_format>

Note: the designer's full first-pass argument was written by the seat to
`vault/raw/2026-09-16-design-ev42-partial-legend.md` (the established
designer-raw pattern; committed with this record).

### Step 3 — bounded exchange, round 2

Jobs: `owner` `job-23.4` (6 turns, 6.5 m), `principal` `job-23.5` (14 turns, 6.5 m), `designer` `job-23.6` (14 turns, 6.5 m). All settled `done`. Each seat received all three round-1 positions (by reference to this card's Step 2) and the enumerated live disagreements. Positions below are verbatim from the seat session JSONL under `.pi/council/runs/` (hub usage footer omitted).

#### Owner round-2 position (`job-23.4`) — verbatim

# EV-42 — owner round-2 position

## Approach

**I concede two of my round-1 central claims, keep the byte-identity spine, and adopt from both seats — with one amendment to each.** Verified facts driving the revisions:

- **Aliasing is real (principal is right).** `respawn`'s `...prev` spread (`hub.ts:196-201`) copies the `usage` accumulator **by reference**, and `processLine` mutates it in place with `+=` (`hub.ts:264-279`). An implementation that stores the accumulator (or a per-attempt entry pointing at it) by reference produces two entries that both read as the final cumulative tuple. But the hazard is fully avoidable: every `Usage` field except `costBasis`/`usageSource` is a primitive (`runs.ts:21-36`), so a numeric baseline snapshot at spawn/respawn (`usageBaseline: {...prev.usage}`) is independent, and the delta at settle is `current − baseline` per `UsageMetric` (`runs.ts:38-39` already names exactly this metric set). Delta capture is therefore **cleanly capturable** — the hazard constrains the implementation, not the design.
- **The delta tuple is necessary under the goal's strict text, and I abandon "derived, never persisted."** The transcript is only *named* by the manifest, so "recoverable from the manifest alone" must mean "the manifest is the complete index." But spend has a stricter available reading — the number itself on the manifest — and the delta tuple satisfies it at ~15 additive lines while buying a real integrity gate principal's test 1 makes precise: `Σ attempts[].usage[X] === manifest.usage[X] ∀ UsageMetric` catches double-count and attribution bugs no derived-sum test can (a derived sum can't distinguish "attempt 1 was empty" from "attempt 1 was skipped"). My round-1 objection ("duplicates the cumulative tuple, buys nothing") was wrong on the second half: it buys the Σ invariant, and it survives single-JSONL corruption pre-pruning. Both durability boundary facts stand as I stated them ([[usage-store]]: manifest and JSONLs are pruned together; the durable store record is the sole survivor) — the delta is *not* the durable answer either, and principal's own hazard list concedes that. We converge: **delta in the manifest for the ephemeral boundary, `attempt?: number` on `ProviderGeneration` for the durable one.**
- **The ordinal hazard is real and my round-1 bookkeeping site was wrong.** `onSettle` mutates `job.attempt` to the pending ordinal before the hub's retrying write (`job-retry.ts:75-80`, then `hub.ts:341-347`), and `respawn` receives `attemptSpec(job.attempt ?? 2)` — so inside `respawn`, `prev.attempt` is already the *new* ordinal. **I abandon "respawn appends the new attempt's record."** The correct single rule: `settle` appends the just-completed attempt `{n: job.attempt ?? 1, sessionId: job.sessionId}` **before** calling `onSettle` — the only point where the completed ordinal/sessionId pairing is unambiguous — and `respawn` touches only `sessionId`. Walk-through at the tree: retrying write → `attempt:2` (pending), `attempts:[{attempt:1,…}]` (exactly principal's test 2); respawn write → `sessionId` now attempt-2's, list unchanged (settled prefix); final settle → append `{attempt:2, sessionId:<id>-attempt2}`, then write. In consequence I abandon my round-1 invariant `attempts[last].sessionId === manifest.sessionId` **as an every-write claim**; it holds at every *settled* write, and mid-flight manifests carry the settled prefix with the current attempt recoverable from `attempt` + `sessionId` (existing fields). This is safe for every current consumer: `tree.ts:77-78` reads only `m.attempt` for the row label, and the flush reads manifests only post-`forestFullySettled` — the final write, where the invariant holds.
- **Zero-generation attempts: I concede to principal, and it hurts to.** My round-1 `session-missing → unavailable` rule makes the *ordinary* retried dispatch render `n/a`: any attempt that dies before its first assistant message (spawn error, instant fail — P3, which I still assert as probable) yields either no JSONL or an empty one, so a fail-once-then-succeed dispatch — the most common real shape — would persist `unavailable` and throw away a perfectly fetched, correct attempt-2 figure. And symmetric to EV-39's own defect principle ("a stored figure that looks whole a month later is the defect"): a stored figure that looks *absent* when it was mostly present and attributable is also a disclosure loss. Principal's `unaccounted` semantics — `reported` iff ≥1 entry reported; `unavailable` reserved for `no-api-key`/`fetch-failed`/`timeout`/all-unaccounted; legacy single-attempt readable-empty stays `no-generation-id` (byte-identity, EV-29 C3) — is more honest and more informative. I abandon my T5 as stated and my "whole or absent, never short-and-labeled" rule; the label is precisely what makes short-and-partial honest here.
- **`partial` reconciles cleanly.** Converged design: key `partial` preserved (designer's grammar-identity constraint, `usage-block.ts:32` slot and `:118-120` predicate untouched in *form*); vocabulary = `"final-attempt-only"` + `"attempts-unaccounted"` (principal), plus record-only `unaccountedAttempts?: number[]`; the legacy literal **remains produced** — but only for the window shape `attempt > 1 && attempts === undefined` (my round-1 carve-out, which principal independently endorses as fail-closed); display adopts designer's migrate-on-read wording `usage  partial = reported figure excludes unaccounted attempts`, which is honest for *both* literals (a legacy final-attempt-only figure does exclude unaccounted attempts) and makes the legend map trivially total. One amendment to designer: their prediction-2 fixture pairs `status:"unavailable"` with `totalCost: 0.0042` and a partial literal — under principal's module semantics that exact combination (unavailable *with* a figure) shouldn't co-occur; see the non-concession below for the coexistence case that *does*.
- **Accessor: adopt `attemptEntries(m)`** in `runs.ts`, with one honesty amendment: for the window shape, the synthesized entry's `usage` must be `undefined`, not `m.usage` — a cumulative tuple labeled as a per-attempt delta is a lie, and single-attempt (`attempt === 1 || undefined`) is the only synthesized case where `m.usage` *is* that attempt's delta. One accessor, not per-consumer derivation: it centralizes the window-shape fail-closed semantics so no second consumer can re-derive them wrong — exactly the class of divergence principal's three-meanings-of-`sessionId` analysis predicts.

## Tradeoffs accepted

- **I give up the minimal manifest entry.** The delta tuple adds twelve numeric fields per attempt and one hidden `usageBaseline` field on `Job`. Worth it for the Σ integrity gate and the strict goal reading; the alternative (my round-1 `{n, sessionId}`) fails principal's test 1 by construction.
- **I give up "whole or absent."** The unaccounted disclosure re-introduces a rendered still-partial state, which I argued against in round 1. I was wrong for the ordinary case; my remaining defense is only the corner below.
- **Hub deltas grow from three to four** (baseline init, settle-append, respawn baseline snapshot, manifest emit) inside a §7-stable file — still additive, each individually failing-test-first, zero kill/stall semantics touched (AGENTS.md §7 discipline held).
- **Abandoned from designer:** nothing load-bearing; their display migration is adopted as-is. Their prediction 2's fixture needs reshaping (above).
- **Inferred (flagged):** that `unavailable + final-attempt-only` is a *reachable* persisted shape today rests on reading `usage-store.ts:443-448` (the stamp is applied to the copy regardless of `provider.status`) — I did not find a test pinning that exact combination; test 845's fixture is `reported`+`partial` (verified at `test/usage-store.test.ts:872-884`). The claim below is written so it fails on today's code only in its post-EV-42 form.

## Testable claims

1. **T1′ (delta arithmetic + no aliasing):** two attempts with distinct assistant usage → `attempts[0].usage.output === attempt1Output`, `attempts[1].usage.output === attempt2Output`, and `Σ attempts[].usage[X] === manifest.usage[X] ∀ UsageMetric` (principal's claim 1, now mine). A reference-storing implementation fails immediately (verified hazard: `hub.ts:197-201` + in-place `+=` at `hub.ts:264-279`).
2. **T2′ (ordinal-at-settle):** retrying manifest reads `attempt:2`, `nextAttemptAt` set, `attempts:[{attempt:1, sessionId:job-1, usage:<delta>}]` — never `{attempt:2}` paired with attempt-1's session id (principal's claim 2; the append must precede `onSettle`).
3. **T3′ (whole figure):** two attempt JSONLs, both with generations → `reported`, `totalCost === c1+c2`, no `partial`, generations carry `attempt:1|2`, same `jobId` (unchanged from my round-1 T3; fails today at `usage-store.test.ts:877`).
4. **T4′ (zero/missing attempt → unaccounted, not n/a):** attempt-1 file missing *or* present-empty → `status:"reported"`, `partial:"attempts-unaccounted"`, `unaccountedAttempts:[1]`, `totalCost` = attempt-2's, no n/a legend. This **replaces** my round-1 T4/T5.
5. **T5′ (single-attempt byte-identity):** readable-empty single-attempt session → `no-generation-id` failure → `unavailable` (today's bytes; principal's all-unaccounted carve-out). My round-1 asymmetry argument survives as a *compatibility* rule, not a semantics claim.
6. **T6′ (window fail-closed):** manifest `attempt:2`, no `attempts` list → `partial:"final-attempt-only"` persists (EV-39 G4 survives for that shape only; `attemptEntries` returns the single synthesized entry, usage `undefined`).
7. **T7′ (display migration):** legacy record with `partial:"final-attempt-only"` renders `usage  partial = reported figure excludes unaccounted attempts` and never the substring `final-attempt-only` (designer's prediction 3).
8. **T8 (byte identity, unchanged):** single-attempt dispatch → manifest without `attempts`/`attempt` (byte snapshot), persisted record byte-identical to pre-EV-42.

**The single point on which I will not concede:** the all-unaccounted retried shape must **not** lose its partial qualifier. Principal's claim 5 asserts both-entries-unaccounted → `unavailable`, n/a legend, "byte-identical to the pre-card path" — but today's code stamps `partial` on the copy **unconditionally** for any `attempt > 1` manifest (`usage-store.ts:443-448`), so the shape-matched pre-card record for a retried dispatch with a dead fetch is `unavailable` **plus** `partial`. Dropping the qualifier for that shape is a disclosure regression on a choose-once record: the durable file would stop saying "this dispatch's provider accounting never closed with a named gap," and EV-39's whole point was that such residuals are machine-readable and durable. **Test:** a two-attempt manifest, both entries unaccounted → persisted record carries `status:"unavailable"` **and** `partial:"attempts-unaccounted"` (+ `unaccountedAttempts`), and `formatUsageBlock` renders the n/a legend *and* the partial legend together — designer's orthogonality (her `partial`/`n/a` distinction, T-B4 coexistence), not principal's claim-5 reading. If principal means only the *single-attempt* all-unaccounted shape by "byte-identical" (where today no partial exists), we already agree and this costs nothing.

**Split judgment: unchanged — keep the card whole.** Both other seats agree and my round-1 reasoning stands: the Σ invariant (T1′), the walk (T3′/T4′), and the legend copy (T7′) share the manifest field as their only input; splitting ships dead halves. Navigator per-attempt transcript browsing and the latent `navigator.ts:869` divergence (principal's finding, inferred-latent per his own flag) stay named out-of-scope follow-ups.

#### Principal round-2 position (`job-23.5`) — verbatim

## Cross-seam reading — the contract right now

Producer side (`hub.ts` → `runs.ts`): `respawn` reuses the id, spreads `...prev` (`hub.ts:198`) **without overriding `usage`**, so the new `Job` shares one `usage` object that `processLine` then mutates in place (`hub.ts:276,286+`). `writeJobManifest` emits one `sessionId` = the *current* attempt's (`hub.ts:115`) and `attempt` only when `>1` (`hub.ts:126`). Attempt N's own transcript lands at `${jobId}-attemptN` (`hub-tools.ts:269-278`), but `RunManifest` (`runs.ts:44-62`) indexes none of them. `settle()` runs `report()` (`hub.ts:341`) → `onSettle` (`:342`, which advances `job.attempt` at `job-retry.ts:76`) → `writeJobManifest` (`:348`), so at the `retrying` write `attempt` is the **pending** ordinal while `sessionId` is the **completed** attempt's.

Consumer side (`usage-store.ts` → `provider-cost.ts` → `usage-block.ts`): the harvest maps **one job per manifest** off `m.sessionId` (`usage-store.ts:421-427`) and post-hoc stamps `partial:"final-attempt-only"` when any eligible manifest is `attempt>1` (`:443-448`). Per-entry, `sessionPath===null` → `session-missing` (`provider-cost.ts:284-285`), zero ids → `no-generation-id` (`:296-297`), and `status = failures.length>0 ? "unavailable" : "reported"` (`:322`) with C3 precedence (`:205-211`). The legend is keyed on *presence* of `partial` (`usage-block.ts:32,118-120`). `manifest.usage` is cumulative and feeds `spend.ts:250`, so it must never become the final attempt's.

`pruneRuns` `rmSync`s the whole run dir (`runs.ts:171-178`), manifests included — so manifest-level per-attempt data and the JSONLs die together. `findSessionFile` matches the JSONL header `id` (`runs.ts:140-160`). Wiki grounding is thin by design: [[usage-block]], [[usage-store]], [[spend-record]], [[cost-provenance]], [[usage-accounting]], [[run-transcripts]], and index.md's summaries; **no wiki page covers hub retry or per-attempt identity** (last ingest is EPIC-8, `vault/wiki/log.md`), so `council/cards/EV-39.md` §Step 6 plus the code are the authority. *(That gap is real; I read the code directly for every claim below.)*

## Ledger — abandoned / kept / adopted

**Abandoned (my round-1 errors):**
1. **Persisted per-attempt `usage` deltas.** I held `attempts?: {attempt, sessionId, usage}[]` with `Σ usage == manifest.usage`. The Intent's own mechanism sentence kills it: *"persisting the per-attempt session pointers on the manifest and walking that list in usage-store."* "Session pointers" is `{n, sessionId}`; spend is derivable from the pointed-at transcript. And `pruneRuns` (`runs.ts:171-178`) deletes manifest and JSONL together, so persisted deltas add zero post-pruning recoverability — owner's argument is simply correct. The aliasing I raised (`hub.ts:198` shares one `usage` object) is not evidence *for* persistence; it is evidence *against* it: a persisted-delta design needs a `{...job.usage}` snapshot at every attempt boundary and a subtraction, and is one spread away from silently double-counting. **Adopt owner's `{n, sessionId}`.**
2. **"One accessor for both store and UI."** The navigator tail reads `m.sessionId` directly as a string (`navigator.ts:335,341`); `attemptEntries(m)` returns *entries* and does not serve it. I overclaimed a unification that does not exist.
3. **`sessionPath === null` ⇒ unaccounted.** I now concede case (a) to owner (below).

**Kept (with sharpened grounding):**
4. The total legend map (`usage-block.ts:32,118-120` renders iff `partial !== undefined`; choose-once records at `usage-store.ts:388-392` are never rewritten).
5. `final-attempt-only` stays a *producer* for the pre-EV-42 window shape — this is owner's carve-out and I adopt it.
6. The ordinal-at-settle hazard (`hub.ts:341-348` after `job-retry.ts:76`).
7. `manifest.usage` stays cumulative (`spend.ts:250`).

**Adopted from the other seats:** owner's minimal entry shape and the window carve-out; owner's case-(a) fail-closed; designer's keep-the-`partial`-key and record-only new literal; designer's display ownership for wording.

## Blind spots, named separately

- **Owner (hub/retry slice)** cannot see that the store aggregates per *manifest*, not per *entry* — so "cumulative usage is enough" reads true inside the hub and false at the harvest. He also cannot see that a readable-but-empty attempt transcript is the *ordinary shape of a spawn-failed attempt* (his own P3), which makes his `zeroSpendOk`-contributes-zero path the common path, not the exotic one.
- **Designer (render slice)** sees the literal, not the producer. She can require surface uniformity, but cannot see that `usage-store.ts:443-448` — not `usage-block.ts` — owns *whether* a legend appears, so "migrate legacy on read" is a renderer promise about a producer she does not control.
- **My own seat's blind spot:** I can see the field-with-multiple-consumers, but not which consumer is live. I asserted the navigator `manifest.id` divergence from a grep without walking the call graph; verifying it changed the answer (below).

## The six points

**1. Per-attempt spend — owner is right; the aliasing forces the *derivation*, not persistence.** "Recoverable from the manifest alone" is an *index* claim: the manifest alone names the transcript, and the spend is recovered from it. The Intent says "session pointers." The aliasing at `hub.ts:198` forces exactly what I said it forces — a snapshot per attempt boundary *if* you persist deltas — which is an argument that you should not. Nothing forces it on owner's design. My round-1 test-1 ("storing `prev.usage` by reference fails") is a test of the design I am abandoning.

**2. Zero-generation attempt — (a) owner, (b) me, (c) agreed.** The honest rule distinguishes *evidence*:
- **(a) file absent/unreadable** → keep `session-missing` (`provider-cost.ts:284-285`) → `unavailable`. We cannot look; absence is not evidence of zero. This is owner's, and it preserves the existing C3 rank-1 literal. I concede it.
- **(b) file present and readable, zero ids** → **unaccounted**, not zero. `collectGenerationIds` returning nothing means *no fetchable generation*, not *no charge* — and a truncated partial write reads identically. The report stays `reported` **iff** ≥1 entry reported, with `partial` set. Owner's `zeroSpendOk`-contributes-zero sets no `partial`, so the persisted figure reads whole — that is the EV-39 falsification class, re-opened by the design that claimed to refuse it. **This is the disagreement I keep.**
- **(c) id present, fetch failed** → failure → `unavailable` (`:307,310,322`). Agreed, unchanged.
- **Unifier that protects `T-P8`:** if **every** entry is unaccounted, the report is `unavailable` with the C3 reason (`no-generation-id`), byte-identical to `test/provider-cost.test.ts:361-389`. So single-attempt present-empty is untouched, and the `partial` path exists only when a *sibling* attempt actually reported.

**3. `partial` — two literals, total map, owner's producer carve-out.** `ProviderPartialReason = "final-attempt-only" | "attempts-unaccounted"`, plus record-only `unaccountedAttempts?: number[]`; `final-attempt-only` remains produced *only* for `attempt>1 && attempts===undefined` (owner's carve-out). Designer's `attempt-unaccounted` singular vs my plural is cosmetic; I defer to designer on **copy** and keep the **literal/schema** in owner/principal territory. The decisive question — *which arrangement makes an existing durable record read whole* — is not whose field shape it is: it is any renderer whose predicate becomes "the literal ∈ a recognized set" without totality, because the old literal then renders nothing. Owner's single-literal design is trivially safe for *old* records; my two-literal + total map is safe for old records and does not regress new ones; designer's migrate-on-read is safe **only** if the predicate stays presence-based (her prediction 5) or the map is total. The one arrangement that also makes a *new* record read whole is owner's `zeroSpendOk` (point 2b).

**4. `attemptEntries(m)` — runs.ts is the right home, but my justification was wrong; the navigator divergence is latent.** Home: `runs.ts` owns `RunManifest` and already exports `findSessionFile`/`readManifests`; a pure accessor belongs there and centralizes the legacy fallback (`m.attempts ?? [{attempt, sessionId}]`) so store and any future UI cannot disagree about the fallback. But the tail does not consume it (point above), so owner's per-consumer derivation is defensible with one live consumer. `manifest.sessionId`'s three meanings: store wants all attempts (via the new list), the tail wants the current one (`navigator.ts:335,341`), and the legacy transcript path takes `manifest.id` (`navigator.ts:868-869`). That third is **latent, and for a stronger reason than I gave**: `openTranscript` is currently unreachable — the modal `open` (`navigator.ts:506-520`) has no caller, and `activateSession` (`:564-571`) is only passed as `onActivate` at `:588`, while `CustomTreeEditor.onActivate` (`focus-nav.ts:282`) is never invoked (Enter routes to `enterProgress`, `focus-nav.ts:318`; `handleInput` calls only `enterProgress`). So no attempt's transcript is openable today; do not fold a navigator fix in. **What is live instead:** during backoff the tree keys and tails `manifest.sessionId` = the *completed* attempt while the label reads `attempt N/M` = the *pending* ordinal (`navigator.ts:335,405,430` + `hub.ts:115,126`) — a transient, real mismatch, out of scope here.

**5. Ordinal-at-settle — owner's scheme satisfies it, stated exactly.** Because `onSettle` advances `job.attempt` (`job-retry.ts:76`) *before* the hub writes (`hub.ts:342→348`), the required rule is: **the attempt entry is appended at `respawn`, from `spec.sessionId`, and `writeJobManifest` must never synthesize an entry from `job.attempt`.** Owner's `spawnJob`-seed + `respawn`-append does exactly this; his `retrying` manifest correctly reads `attempt:2, attempts:[{1}]` (pending ordinal + completed list). A snapshot-before-`onSettle` is *a* way to satisfy the hazard but not the only one — do not write an entry inside the retrying write, or you record `{n:2, sessionId:<attempt 1's>}`. The harvest never sees the window anyway: `floorFullySettled`/the `exitCode===null` gate (`usage-store.ts` `flushPendingInvocations`) keeps the flush closed through backoff (EV-39 G3).

**6. Goal-wording — resolvable by design as the card stands; a ruling only if durable *dollar* spend is wanted.** The Intent's words ("session pointers") and the Note's cardinality settle the index reading, under which the goal is satisfiable without any ruling. It becomes a genuine steward question only if the product reads "recoverable" as *durable per-attempt spend in the store*. The cheapest partial satisfaction without a scope expansion — and the one piece of that reading I keep — is **`ProviderGeneration.attempt?: number`** (additive, absent for single-attempt jobs ⇒ old records byte-identical) so the durable record can decompose its own sum by attempt after the run dir is pruned. That is *not* a new scope: it is what makes clause 2 auditable.

**Split judgment: keep whole.** Both clauses consume one input (the attempt list) and the partial disclosure is a claim *about the harvest*; shipping the pointer field with no walk ships substrate nothing reads, and changing the legend with no producer ships copy that is false. The only legitimate follow-up is navigator-side per-attempt transcript browsing — which, per point 4, is currently unreachable anyway.

## Testable claims

1. **Present-empty must not read whole (the hill).** Entries `[{attempt:1, present-readable-empty}, {attempt:2, one generation c2}]` → persisted report satisfies `report.status === "unavailable" || report.partial !== undefined`, **never** `status==="reported" && partial===undefined`, with `totalCost === c2`. Under owner's `zeroSpendOk` this fails on the second clause. Settle with an injected transport, no network.
2. **`T-P8` preserved.** One entry, present-but-no-`responseId` → `status:"unavailable"`, `reason:"no-generation-id"`, `generations:[]`, transport calls `0` (`test/provider-cost.test.ts:361-389` must stay green, byte-identical).
3. **Case (a) fail-closed.** Entries `[{attempt:1, sessionPath:null}, {attempt:2, one generation}]` → `status:"unavailable"`, `reason:"session-missing"`.
4. **Fetch failure.** `[{attempt:1, one generation, transport throws}, {attempt:2, one generation}]` → `status:"unavailable"`, reason `fetch-failed:*`; `reportedRow` still renders the fetched part.
5. **Window carve-out.** Manifest `{attempt:2}` with **no** `attempts` → persisted `provider.partial === "final-attempt-only"` (`test/usage-store.test.ts:845-884` stays green); same with `attempts:[{1},{2}]` → no `partial`.
6. **Legend totality.** A record whose `provider.partial` is an *unknown* literal still renders a legend line (`expect(out).toContain("usage  partial")`), i.e. the renderer never treats an unrecognized literal as wholeness.
7. **Ordinal snapshot.** After an attempt-1 retryable settle, the on-disk manifest is `{attempt:2, nextAttemptAt:<set>, attempts:[{attempt:1}]}`, `sessionId === <id>`; `attempts` never contains `{attempt:2}` before respawn. Extends `test/job-retry.test.ts:248-270`.
8. **Delta invariant only if persisted.** If any attempt-level usage is persisted, `Σ attempts[].usage[X] === manifest.usage[X]` ∀ `UsageMetric` and attempt 1's tuple equals its own settle snapshot — a `...prev`-by-reference implementation fails immediately (`hub.ts:198`). *(Moot under the adopted design; keep as a tripwire.)*
9. **Cumulative preserved.** `sumSubtreeUsage(manifests, id).subtree.cost === manifest.usage.cost` for a retried manifest (`spend.ts:250`).
10. **Byte-identity.** A non-retried dispatch's manifest has neither `attempt` nor `attempts` (`test/runs.test.ts:197`); its persisted record carries no generation `attempt` and no `partial`.

**Single remaining point I will not concede:** an attempt whose transcript was *read* and yielded no generation id must never leave the durable figure reading whole — the persisted provider sibling must carry `status:"unavailable"` or a non-undefined `partial`. Evidence: `usage-block.ts:118-120` renders the qualifier on `partial !== undefined` alone, and choose-once records (`usage-store.ts:388-392`) are never rewritten, so any write that omits it is permanent. Claim 1 is its runnable form.

**Inferred, flagged:** that the production child honors `--session-id` and thus `findSessionFile` resolves `${jobId}-attemptN` on the *write* side is verified only on the read side (`runs.ts:140-160`, `hub-tools.ts:270`); neither owner's round-1 nor mine ran a live child. The navigator-unreachability finding is grep- and call-graph-derived, not executed. The wiki has no retry/per-attempt page, so nothing in `vault/` either confirms or denies the attempt-write side.

#### Designer round-2 position (`job-23.6`) — verbatim

I've read the card and the relevant source files (`extensions/usage-block.ts`, `extensions/usage-store.ts:388-465`, `extensions/provider-cost.ts:38-43, 270-330`, `vault/wiki/usage-block.md`, `vault/wiki/cost-provenance.md`), and the designer's round-1 raw document. Below is my round-2 position, structured per the four questions you raised.

---

##1. If `owner`'s "whole or absent" ships — does the usage block need any copy change?

**Yes, but less than I argued in round 1.** Three consequences I want to separate:

**a) The literal `final-attempt-only` stays live on the surface — not as historical, but as the description of a current mechanism for a current shape.** Owner is right that an EV-39-era manifest inside the 15-run window (`attempt > 1`, no `attempts` list — `extensions/runs.ts:58` confirms the field is conditional, so this shape is real and producible after a package upgrade) walks only the final attempt. The literal is then not "retired" — it is the truthful description of what that mechanism did. My round-1 framing of "retire from display" was wrong for this shape; I concede it. The block continues to render `usage  partial = reported figure is final-attempt-only` for that shape, with no migration needed.

**b) The comprehension cost on durable pre-EV-42 records is small.** A pre-EV-42 record carries `partial: "final-attempt-only"` on disk (`extensions/usage-store.ts:388-392` is choose-once, the file exists, the flush is a no-op). The block renders the legend verbatim. A post-EV-42 *whole* retried dispatch has no `partial` on disk and renders no legend. A post-EV-42 dispatch hitting the carve-out shape renders the same legend. So the surface shows **two renderings** depending on the record's mechanism: a partial legend for legacy-and-carve-out shapes, none for whole shapes. A person reading either can tell which kind of record they're looking at by the legend's presence — that is a real signifier. **But** the wording `final-attempt-only` is now twice-true (legacy + current carve-out), and a person who learned "this is the old disclosure" will misread it as historical-only on a current carve-out record. Honest but ambiguous. I'd accept this as a cost of owner's design.

**c) The legend wording itself does not need to change under owner's design.** The literal's description is correct for both legacy and carve-out shapes: only the final attempt was walked. The vocabulary doesn't evolve; the renderer doesn't widen. This is the smallest possible change. I rate this as **a real design win** for owner's position — and a reason I no longer hold "migrate-on-read is mandatory" as my primary position (see §3).

**d) Comprehension question I cannot settle:** whether a person seeing no partial legend on a whole retried dispatch can confidently infer the figure is whole, or whether they'll keep looking for the old disclosure and treat its absence as a bug. This is reading-pedagogy, not testable. I name it as open.

## 2. If `principal`'s "unaccounted" ships — wording against grammar discipline, and the `n/a` distinction

**Yes to the three-state distinction; yes to plural; record-only count stays record-only.**

**a) The two legends remain orthogonal, and they answer different questions.** Under principal's design, the four corners of `(partial present?, totalCost non-null?)`:

| `partial` | `totalCost` | Reported row | Partial legend | `n/a` legend | Slot values |
|---|---|---|---|---|---|
| undefined | `!=null` | renders | absent | absent if no slot is n/a | normal |
| undefined | `null` | absent | absent | absent if no slot is n/a (e.g. no-api-key with no rows) | normal |
| set | `!=null` | renders | renders | absent | normal — this is principal's "partial" |
| set | `null` | absent | absent | renders (subtree `cost=n/a`) | subtree n/a |

A person can distinguish "the figure we have excludes N attempts" from "we have no figure at all" by **the presence of a dollar figure** plus **the qualifier**. The reported row's `(reported)` token is the signifier; the partial legend names *why*; the n/a legend names *that there is no figure*. Two different questions, two different signifiers, both readable. **The block grammar does not widen** — same rows, same keys, same order; only the literal set grows and the worst-of semantics change inside `provider-cost.ts`. PO C/D/E/F (`extensions/usage-block.ts:88-89, 116-119`) holds.

**b) I adopt principal's plural.** My round-1 literal `attempt-unaccounted` was singular; principal's `attempts-unaccounted` is right because the state is plural — one *or more* attempts can be unaccounted, and the wording names the figure's property ("excludes unaccounted attempts") rather than the singular attempt. The plural in the literal matches the plural in the legend.

**c) The record-only count.** `unaccountedAttempts: number[]` should stay record-only. The legend grammar cannot carry a count without widening the `usage  ` prefix convention (the count would have to live either as a parenthetical on the legend — grammar change — or as a separate row — even worse). The auditor has the count; the person reading the block has the qualifier. Job tree row `attempt N/M` already tells the person how many attempts the dispatch ran; combining that with the qualifier, they can infer the gap without a count on the surface. **Record-only is the right place for the count.** I held this in round 1; principal holds it; we agree.

**d) Honest distinction, testable:** "the figure we have" vs "no figure here" maps cleanly to (reported row present, partial legend) vs (no reported row, n/a legend). A first-time reader can tell them apart by the dollar figure's presence — the most natural comprehension cue. I think this is better than owner's "whole or absent" because it preserves the actual *partial* state on the surface rather than collapsing it under unavailable.

**e) Open judgment I cannot settle:** whether the wording `reported figure excludes unaccounted attempts` is more or less honest than alternatives like `reported figure excludes some attempts' spend` or `some attempts were unaccounted for`. The first names the figure's property (what I want); the second names the figure's computation (auditor-style); the third names the failure mode (mechanism-style). The first wins on the principle **knowledge in the world beats knowledge in the head** — it tells the person what the dollar figure means, not what failed. I lean the first but cannot rule.

## 3. Migrate-on-read: does it survive principal's totality argument and choose-once immutability?

**Conditional answer.** It survives **only if the literal set evolves**. I retreat from my round-1 framing that it is the primary mechanism.

**a) Choose-once immutability is binding.** `extensions/usage-store.ts:388-392` short-circuits the entire flush when the file exists — `status: "existing", file` and `continue`. A pre-EV-42 record's on-disk literal `partial: "final-attempt-only"` is frozen at write time. The literal will never be rewritten. This is the substrate fact that makes the question about migrate-on-read the right question.

**b) Under owner's design, migrate-on-read is a no-op.** The literal set is invariant — `final-attempt-only` only, both for legacy records and for the carve-out shape. The renderer's legend map has one entry. Re-wording a legacy record's stored literal at render time would change the legend's text on the surface, which contradicts the literal's truth for the legacy mechanism (which only fetched the final attempt — *not* "excluded unaccounted attempts" in the post-EV-42 sense). I would now hold: under owner's design, do **not** migrate — render the stored literal against the stored mechanism.

**c) Under principal's design, migrate-on-read is mandatory.** The literal set evolves: `final-attempt-only` retires (no producer in the post-EV-42 mechanism), `attempts-unaccounted` is the new live literal. Old records carry old literals forever. The renderer's legend map must be **total** over all literals — old, new, future. An unknown literal must not fail-open (silently drop the qualifier). This is the totality argument principal made at `EV-42.md:314`. It is the discipline I now hold: migrate-on-read is required when the literal set evolves, optional otherwise.

**d) Is read-time re-wording honest?** I argued in round 1 that yes — the on-disk literal is audit, the surface is disclosure. I refine this: re-wording is honest **iff** the new wording truthfully describes the figure's state, not the mechanism that produced it. For principal's design, `reported figure excludes unaccounted attempts` describes a post-EV-42 record's state truthfully (some attempts were unaccounted). For a pre-EV-42 record rendered under migrate-on-read, the wording also describes the state truthfully — the figure excludes the unaccounted attempts (i.e., attempts 1 and 2 are absent from the sum because the mechanism only fetched attempt 3). The wording is mechanism-agnostic; it names the figure's property. So re-wording is honest under principal's design. **But** under owner's design, re-wording to `excludes unaccounted attempts` would be dishonest for the legacy/window shape — the mechanism literally didn't *try* to fetch every attempt; it was only ever final-attempt. So under owner's design, the wording `final-attempt-only` is the honest surface. This is the asymmetry: migrate-on-read is honest for principal's wording, dishonest for owner's wording.

**e) The two-engineer split is therefore also a design split on whether to migrate.** I cannot bind this without binding the engineering shape. I split the judgment: **migrate if and only if the literal set evolves.**

## 4. Falsifiable rendering predictions

Each is a string-equality assertion the Skeptic can run against `extensions/usage-block.ts` after the implementation lands. Predictions are organized by which engineering position ships.

### 4.1 If `owner`'s "whole or absent" ships

**P-O1 (whole retried dispatch):**
```ts
const report = providerReport({ generations: [genAttempt1, genAttempt2] }); // partial omitted
const out = formatUsageBlock({ record, provider: report });
expect(out).not.toContain("usage  partial"); // (1) no partial legend
expect(out).not.toContain("final-attempt-only");          // (2) legacy literal not on surface
expect(out.split("\n")).toHaveLength(4);                  // (3) same shape as single-attempt
expect(out).not.toContain("usage  n/a");                  // (4) no n/a legend
```

**P-O2 (one attempt's JSONL missing — figure is `unavailable`):**
```ts
const report = providerReport({ status: "unavailable", reason: "session-missing", totalCost: null, generations: [] });
const out = formatUsageBlock({ record, provider: report });
expect(out).not.toContain("usage  partial");              // (1) NO partial legend under owner's whole-or-absent
expect(out).toContain("cost=n/a");                        // (2) subtree slot renders n/a
expect(out.split("\n").at(-1)).toBe("usage  n/a = provider figure unavailable"); // (3) n/a legend last
expect(out).not.toContain("excludes unaccounted attempts"); // (4) no new wording
```

**P-O3 (legacy pre-EV-42 record, on-disk literal `final-attempt-only`):**
```ts
const legacy = providerReport({ partial: "final-attempt-only" });
const out = formatUsageBlock({ record, provider: legacy });
expect(out).toContain("usage  partial = reported figure is final-attempt-only"); // (1) literal verbatim
expect(out).not.toContain("excludes unaccounted attempts"); // (2) no migrate```

**P-O4 (EV-39-era shape — `attempt>1`, no `attempts` list):**
```ts
const out = formatUsageBlock({ record, provider: providerReport({ partial: "final-attempt-only" }) });
expect(out).toContain("usage  partial = reported figure is final-attempt-only"); // (1) carve-out fires
expect(out.split("\n").at(-1)).toBe("usage  partial = reported figure is final-attempt-only"); // (2) no n/a in this corner
```

**P-O5 (non-retried dispatch byte-identity — binding):**
```ts
const out = formatUsageBlock({ record, provider: providerReport() });
expect(out).not.toContain("partial");
expect(out.split("\n")).toHaveLength(4);
expect(out).not.toContain("excludes unaccounted attempts");
expect(out).not.toContain("attempt-unaccounted");
```

### 4.2 If `principal`'s "unaccounted" ships

**P-P1 (whole retried dispatch):** identical to P-O1 — both engineers agree this case renders no partial legend, no n/a legend, 4 rows.

**P-P2 (one attempt unaccounted — figure is `reported` with `partial` set):**
```ts
const report = providerReport({
  status: "reported",
  totalCost: 0.0042, // Σ over accounted attempts (attempt 2 only)
  generations: [genAttempt2],  // only the accounted attempt
  partial: "attempts-unaccounted",
  unaccountedAttempts: [1],
});
const out = formatUsageBlock({ record, provider: report });
expect(out).toContain("usage  reported  cost=$0.0042 (reported)"); // (1) reported row renders
expect(out).toContain("usage  partial = reported figure excludes unaccounted attempts"); // (2) partial legend renders
expect(out).not.toContain("usage  n/a");                  // (3) NO n/a legend (no slot is n/a)
expect(out).not.toContain("final-attempt-only");          // (4) no legacy wording```

**P-P3 (all attempts unaccounted — figure is `unavailable`):**
```ts
const report = providerReport({
  status: "unavailable",
  reason: "no-generation-id",
  totalCost: null,
  generations: [],
  partial: undefined,         // principal: totalCost null ⇒ partial not set
});
const out = formatUsageBlock({ record, provider: report });
expect(out).not.toContain("usage  partial");              // (1) no partial legend (partial undefined)
expect(out).toContain("cost=n/a");                        // (2) subtree n/a
expect(out.split("\n").at(-1)).toBe("usage  n/a = provider figure unavailable");
```

**P-P4 (legacy pre-EV-42 record, on-disk literal `final-attempt-only` — migrate-on-read fires via total legend map):**
```ts
const legacy = providerReport({ partial: "final-attempt-only" });
const out = formatUsageBlock({ record, provider: legacy });
expect(out).not.toContain("final-attempt-only");          // (1) legacy literal not on surface
expect(out).toContain("usage  partial = reported figure excludes unaccounted attempts"); // (2) new wording via map
expect(legacy.partial).toBe("final-attempt-only");        // (3) on-disk literal preserved
```

**P-P5 (EV-39-era shape — `attempt>1`, no `attempts` list):**
```ts
const out = formatUsageBlock({ record, provider: providerReport({ partial: "final-attempt-only" }) });
expect(out).toContain("usage  partial = reported figure excludes unaccounted attempts"); // (1) total map renders
expect(out).not.toContain("final-attempt-only");
```

**P-P6 (unknown literal — fail-closed guard):**
```ts
const future = providerReport({ partial: "future-literal" as any });
const out = formatUsageBlock({ record, provider: future });
expect(out).toContain("usage  partial"); // (1) qualifier present, NOT fail-open
expect(out).not.toContain("future-literal");              // (2) unknown literal not surfaced
// (3) generic wording expected, exact text is principal's callexpect(out).toMatch(/usage  partial = figure is not whole/);
```

### 4.3 Comprehension judgments I cannot settle with strings

- **C-1 (owner's design).** Whether a person seeing no partial legend on a whole retried dispatch correctly infers "whole" vs "old code path, broken rendering". I cannot falsify this without observing real use.
- **C-2 (principal's design).** Whether the wording `reported figure excludes unaccounted attempts` is more readable than alternatives. Comprehension pedagogy.
- **C-3 (both).** Whether the count of unaccounted attempts belongs on the visible surface. I hold record-only; principal holds record-only; the decision is a call about persona (operator vs auditor), not testable.
- **C-4 (both).** Whether the partial legend's stack position (after reported row, before n/a legend) is the right order. I think yes — partial qualifies the figure, n/a qualifies the absence — but this is reading-order preference.

## 5. Revised, ranked design position

**What I keep from round 1:**
1. The `partial` legend key — names the figure-integrity state. (`extensions/usage-block.ts:32`)
2. The two-legends-coexist discipline (`partial` and `n/a` are orthogonal; can render together, alone, or neither).
3. The conditional predicate `provider.partial !== undefined` (`extensions/usage-block.ts:120`).
4. The on-disk literal is audit history — never deleted, never rewritten (choose-once at `extensions/usage-store.ts:388-392`).
5. The routed multiset aggregates across attempts under the existing ordering rule (`extensions/usage-block.ts:65-78`).
6. The wording `usage  partial = reported figure excludes unaccounted attempts` for principal's design only.

**What I abandon from round 1:**
1. The literal name `attempt-unaccounted` (singular). Plural is right under principal; under owner, no new literal.
2. **Migrate-on-read as the primary mechanism.** Now conditional on whether the literal set evolves.
3. The framing "retire `final-attempt-only` from display". Under owner, the literal stays live for a current shape. I was wrong to call it historical-only.

**What I adopt from `owner`:**
1. **The carve-out is honest.** EV-39-era shapes inside the 15-run window after upgrade are real, and the literal truthfully describes what their mechanism did.
2. **"Whole or absent" is a defensible framing** for the figure's state — a two-state model avoids the comprehension load of "how incomplete is this number?".
3. **The literal set can stay invariant** under his design; no legend-map widening needed.
4. **Byte-identity for non-retried dispatches** (T8) is binding — `extensions/usage-block.ts:120` does not change.

**What I adopt from `principal`:**
1. **The three-state model is comprehension-honest.** "Partial with a sum" is a distinct surface state from "no figure here". Collapsing them widens the meaning of `n/a`.
2. **Total legend map is mandatory** under any design where the literal set evolves — and is a sound forward-safety discipline even under owner's design.
3. **Module-level disclosure** in `provider-cost.ts` is more honest than post-hoc at `usage-store.ts:443-448` — the disclosure is the producer's claim, not the caller's.
4. **`unaccountedAttempts: number[]` is record-only** — exactly the right place for auditor detail.
5. **A zero-generation attempt is recoverable, not fatal** — the worst-of `failures.length > 0 ⇒ unavailable` rule at `provider-cost.ts:305` is too coarse when one of N attempts is unaccounted but others reported.

**The single design point I will not concede:**

**The `partial` legend key must have at least one live producer path post-EV-42 on the surface, and the rendering must be honest about each record's mechanism.**

This rejects two designs I have seen argued:

- *Reject A:* a design where `partial` is set ONLY for pre-EV-42 records and never for new ones, with the new code path silently dropping the qualifier on the surface. That makes `partial` vestigial — the key is in the type, the legend is in the renderer, but no post-EV-42 record ever triggers it. **This is the "permanent residual" the EV-39 steward ruling forbade.** Owner's carve-out avoids it by keeping `partial` live for a current shape; principal's new literal keeps it live for the normal partial case. Either way, `partial` has work to do.

- *Reject B:* a design where migrate-on-read is applied regardless of literal-set evolution, re-wording a legacy record's `final-attempt-only` to "excludes unaccounted attempts" even when the legacy mechanism only ever fetched the final attempt. That is dishonest about the mechanism. Migrate is honest for principal's wording (mechanism-agnostic, names the figure's property); dishonest for owner's wording (which names the mechanism). The two engineers' positions cannot both be migrated-to.

The carve-out (owner's live shape) and the new literal (principal's live state) both honor my non-concession. The choice between them is the engineering call. I split the judgment: **migrate-on-read is conditional on the literal set evolving**, and **the `partial` key remains live in either case**.

I do not rule.