---
id: EV-10
title: features-new decomposition dispatches the council
state: In Progress
owner: null
epic: EPIC-3
goal: The features-new procedure dispatches council seats to deliberate on the epic and child decomposition and the draft presented to the human records each contributing seat and any unresolved disagreement instead of the facilitator decomposing alone
---

## Intent

Today steps 1 and 2 of council/procedures/features-new.md are executed by
the facilitator solo — one agent reads the template and board, slices the
children, drafts every goal, and presents the result. This card rewrites
those steps so the decomposition is deliberated — dispatched seats
(council_dispatch + council_wait, already available to the parent session,
no engine change) contribute to the slicing, the goal sentences, the state
assignments, and the designer-surface flags in each child's Intent, and the
facilitator aggregates their input into the single draft pass step 3
presents.

"Interested agents" is deliberately left as the deliberation's first open
question — the seats should rule on which seats participate (all nine, a
default subset, or a content-driven selection rule) and how a seat's
disagreement is carried into the draft pass rather than silently resolved
by the facilitator.

User-visible surface — the human's draft pass itself; the set of cards
presented at step 3 now carries seat attributions and named disagreements.
No TUI, no copy strings; whether this warrants a designer seat is the
council's call, flagged here explicitly.

The draft-then-confirm gate is not part of this card's surface — step 3's
text (write nothing to disk until the human approves) must survive this
rewrite verbatim.

## Acceptance

- The rewritten procedure text mandates seat dispatch for the decomposition
  steps and states how contributions and disagreements reach the step-3
  draft pass.
- A smoke run of /features-new on a toy feature shows the seats actually
  dispatched (job ids on record) and the presented draft naming them.
- Step 3's draft-then-confirm text is byte-identical to before the change;
  python3 council/validate.py stays green.

## Phase 1 rulings (recorded 2026 by the human before the run, immutable for this run, binding on every seat including steward)

- **Ruling SEATS-1 (interested agents):** the seats that participate in a
  /features-new decomposition are **product-owner, designer, principal, and
  skeptic**. If the product-owner requires it, escalation goes to the CEO
  (the human), reached through the orchestrator. No other seats deliberate
  by default; the procedure must name this set explicitly.
- **Ruling SMOKE-1 (smoke-run hygiene):** the acceptance smoke run executes
  /features-new against a **scratch copy of council/** (temp directory),
  never the real board. Evidence for the dispatched-job-ids criterion is
  the job ids captured from that scratch run. The real council/board.md is
  not modified by any smoke run.

### Step 6 resolution — product-owner ruling (verbatim, binding on all seats including steward)

**Ruling (wave-1 authorship):** The first decomposition artifact is authored
by `principal` alone, dispatched once in wave 1. Its output is the child
decomposition (slicing, per-child `goal`, `state`, surface flag) in
principal's native `Reframe` format, plus the epic goal as a one-line
transcription of the human's intake. Wave 2 dispatches `skeptic` +
`designer` in parallel against that artifact (identical input, their native
formats, the completeness charter). Wave 3 dispatches `product-owner` last,
unconditionally, ruling-only — ratify/amend the epic goal and each child's
`state`, rule disputes with dissent named, escalate per its `<escalation>`
to the human per SEATS-1. The facilitator authors nothing, at any step.
Attribution and the disagreement ledger live at the gate presentation and
the `runs/` transcript, never in the card file.

**Options rejected:** (1) `principal` + `product-owner` in parallel wave 1
(Side B) — produces a self-review loop the human cannot see at the gate (PO
writes its own product statement in wave 1 and then ratifies it in wave 3
with byte-identity), and the two-author merge forces the facilitator to
combine two independently-sliced decompositions, which is adjudication
wearing aggregation's clothes. (2) Facilitator first pass — forbidden by
`council.md` "The facilitator decides nothing" and by this card's own goal.
(3) All four seats parallel-generating — collides with each seat's own
output format; conceded in round 2.

**Grounding:** principal's charter and `Reframe` format fit "first
decomposition artifact"; product-owner's role text forbids designing the
solution and its established pattern is ruling-only (council.md steps 2 and
6); the epic goal as a one-line transcription of the human's intake is the
smallest possible charter stretch for principal — the human is the author
of what the product is for. The round-cap argument (EV-11 CAP-1) cuts the
same way for both sides and is load-bearing for neither. Skeptic O3 (leans
Side B) and O4 (leans Side A) were both contingent on O1/O2 closing first
and settle nothing here; O1/O2 remain open-untested and bind the spec write.

**Reversibility:** cheap — one procedure paragraph plus a smoke-harness
re-run; no engine code, no card files on disk.

## Run record

### Step 1 — read and gate (council-runner, 2026)

- state `Ready` confirmed; card earns a Council run.
- **Full council** — the goal admits more than one reasonable design for how
  the procedure mandates dispatch and aggregation, and the change touches a
  shipped payload file every /features-deliver run flows through.
- **Surface-touching: yes** — the deliverable is the draft the human reads at
  the step-3 approval gate, and the card flags the designer-surface question
  explicitly. Per council.md step 1, `designer` is seated as a third
  generator in steps 2–3.
- Rulings applied from the card face (not re-asked): **SEATS-1** — the
  rewritten procedure names product-owner, designer, principal, skeptic as
  the decomposition's participating seats; **SMOKE-1** — acceptance smoke
  evidence is job ids captured from a scratch-copy /features-new run.
- Scope guard: the card's `goal` covers dispatch, aggregation, and draft
  attribution only. Round-cap/convergence/fallback text is the surface of
  sibling card EV-11 (same epic) and is not drafted here; the rewrite must
  leave room for it.

### Step 2 — independent first pass

Dispatched `owner`, `principal`, `designer` in parallel (surface-touching
full council), each with only this card. Rounds below appended verbatim.

#### Round 1 — owner (job-1.1)

VERBATIM:

I've read the artifact (`council/procedures/features-new.md`), the sibling procedures (`council.md`, `features-deliver.md`, `board-create-card.md`), all four seat bodies, the hub tool returns (`extensions/hub-tools.ts`), `validate.py`'s actual scope, the tests that already scan procedure prose (`test/prose.test.ts`), the full EV-10/EPIC-3 faces, and the wiki pages `facilitator`, `council-loop`, `run-transcripts`, `engineering-board`. Here is my position.

**Approach**

The rewrite keeps the mechanics and moves all *content authorship* into seats. Step 1 keeps its mechanics (read `_template.md`, read `board.md`, assign `EPIC-<n>`) but stops drafting the epic goal solo — the epic goal is a seat contribution like everything else. The rewritten step 2 becomes: **(a)** dispatch `product-owner`, `designer`, `principal`, `skeptic` in parallel, one round, identical input — the feature (`$ARGUMENTS`), the template, the board, the assigned epic id, the procedural bars (goal falsifiability, no `: `, state rules, Intent-surface rule, em-dash/board rules), and a **mandated output shape** (children list with per-child scope/goal/state/surface flag + the epic goal) — with *no facilitator draft and no other seat's position* in any input (council.md step 2's independence precedent, `council/procedures/council.md:47-52`); **(b)** `council_wait` all four job ids, jobs bounded per the house dispatch discipline with each job id noted (`council.md:24-27`); **(c)** aggregate by **mechanical concordance** — children aligned by stated scope, agreeing elements drafted from the agreement and attributed to the seats whose text produced them, single-source elements attributed to their proposer, and every *conflicting* position on the same element (exists-vs-not, different goal, different state, different surface flag) recorded as a **named disagreement** with both sides and their job ids — never resolved by paraphrase, because *the facilitator paraphrasing a seat's line is silent resolution wearing punctuation*; **(d)** route named disagreements: what `product-owner` may rule is ruled via a bounded **ruling dispatch** (dissent + options-rejected recorded, per `product-owner.md`'s own format), what its escalation criteria forbid (portfolio change, wrong goal, reversing a recorded human decision) escalates to the human per SEATS-1 ("reached through the orchestrator" = the facilitator of this command stops and surfaces, the `council.md` Needs-Human shape); **(e)** assemble the step-3 draft: cards exactly as-written-to-disk + per-card attribution lines + a named-disagreement ledger (dispute, both sides named, ruling-or-human-open status). The attribution mandate lives in the rewritten step 2's aggregation language — **step 3's text is byte-identical**, and attribution is *presentation at the gate, never card-file content* (writing "contributed by X" into an Intent would corrupt exactly the `Intent` surface-reading `/features-deliver` depends on, `features-new.md:29-35`).

**Decisions on the four open questions**

1. **Dispatch structure / job ids on record.** Text inlines the house bounded-dispatch discipline (council.md precedent of restating, not cross-referencing — `council-runner.md:216-219` does the same): `council_dispatch` → note the returned job id → `council_wait` with a window → cancel + one re-dispatch on stall → stop and surface on double-fail. Job ids are recoverable artifacts regardless — each dispatch writes a `RunManifest` (`runs/<runId>/<jobId>.json`, seat field) under the run substrate (`vault/wiki/run-transcripts.md`), and the existing smoke harness already greps manifests as dispatch evidence, so SMOKE-1's "job ids captured from that scratch run" needs no new mechanism. The draft names *seats* (the human-visible surface); job ids ride in the facilitator's record and the manifests.
2. **Contributions → draft pass.** Every element in the presented draft names its contributing seats; the input's mandated output shape is what makes the contributions *comparable* — without a fixed shape, aggregation is the facilitator inventing structure, which is the defect this card removes.
3. **Disagreements.** Two-tier: `product-owner` rules what it may rule (a ruling dispatch, explicitly *not* an exchange round — the EV-11 seam), dissent always named; what it may not rule escalates to the human per SEATS-1 and is presented as an open item at the gate, where the human's edit/approve power settles it. No facilitator middle.
4. **What the facilitator aggregates.** Agreeing text (verbatim, attributed) + mechanical assembly (ids, card list, state mechanics) + a concordance diff that *produces* the disagreements. The facilitator writes no goal, state, or surface sentence of its own.

**Seam for EV-11 and step-3 integrity.** The first pass is named "round 1"; aggregation consumes "all recorded contributions;" the disagreement path is a ruling dispatch, not an exchange round — so EV-11's bounded-exchange text inserts between dispatch and aggregation without touching this card's written surfaces, and this card writes no cap/convergence/fallback language (EPIC-3 scope guard). `validate.py` parses cards and board only, never procedures, so the step-3 byte-identity is the real gate and must be pinned by test, not by eyeball.

**Tradeoffs accepted**

- **Cost**: four full independent decompositions + an occasional ruling dispatch is an order of magnitude more expensive than the solo decomposition it replaces, and independent granularity will diverge on fuzzy features — the step-3 gate becomes partial triage. Worth it: solo-facilitator resolution is the exact defect the card removes, and named disagreement is the product.
- **Attribution not persisted in cards**: who contributed what survives only in run transcripts (pruned to 15 runs) and the gate presentation, so post-hoc audit of a decomposition needs the transcript. Accepted deliberately — card files are the loop's durable state and must stay clean for `/features-deliver` and `validate.py`.
- **Verbatim, not summarized**: I accept that the draft may be longer and rougher (four seats' raw sentences) than a polished solo draft. Polish is the facilitator's favorite silent-resolution vector.

**Testable claims**

1. **Step-3 byte-identity pin (TDD red/green).** Add to `test/prose.test.ts` (or a sibling `test/features-new-gate.test.ts` — it runs under `bun test`): a fixture string equal to today's step-3 block (the section between `## 3. Draft-then-confirm` and `## 4.`) and `expect(shippedBlock).toEqual(fixture)`. Written against today's text it's green now, red the moment any byte of step 3 moves, green again only when restored.
2. **Card-content hygiene (smoke assertion).** After the scratch-copy run per SMOKE-1, assert every written card file contains no seat-attribution or disagreement line in frontmatter or Intent, and `python3 council/validate.py` on the scratch copy prints `All council artifacts valid` (state values, `: `-free goals, em-dash board lines — the real gate on seat-produced content).
3. **Dispatch-evidence (smoke assertion).** The scratch run's `runs/<runId>/*.json` manifests contain exactly ≥1 job per seat in `{product-owner, designer, principal, skeptic}` with state `done`, and the presented draft (the parent session's output between step 2 and the gate) names all four seats — `product-owner`, `designer`, `principal`, `skeptic` — as attribution.
4. **Independence (smoke assertion).** The four `council_dispatch` tool calls in the run carry identical `input` strings, and no input contains any text that could be another seat's position (no seat name's contribution, no facilitator decomposition) — only feature, template, board, epic id, shape spec. This pins the no-strawman/no-leak rule to an observable artifact.
5. **Concordance non-resolution.** In the scratch run's presented draft, no disagreement ledger entry has status "resolved by facilitator" — every entry is either `ruled by product-owner` (dissent named) or `open for the human`. I'll write this as a harness assertion if any seat argues that silent facilitator resolution is acceptable shorthand; it is the one claim I expect a disagreement on.

**Grounding**: `council/procedures/features-new.md` (artifact, steps 1–2), `council/procedures/council.md:22-27` (dispatch discipline), `council.md:47-52` (independence precedent), `council.md` step 6 (routing shape), `extensions/hub-tools.ts` (`details.jobId`, `Dispatched ... as <job.id>`), `vault/wiki/run-transcripts.md` (manifest substrate + harness precedent), `python3 council/validate.py` (cards/board only), `test/prose.test.ts` (existing prose-scan gate), `council/cards/EV-10.md` + `council/cards/EPIC-3.md` (rulings + scope guard). Wiki gap to note: no wiki page describes EV-10/EPIC-3 yet, and nothing in the vault speaks to decomposition-by-council — so the mechanism claims above rest on the procedures and engine, which I read directly.

#### Round 1 — principal (job-1.2)

VERBATIM:

## Principal seat — EV-10 position

### Cross-seam reading

The seam here is prose-to-prose, not prose-to-API. The engine half of this contract is thin and does not change:

- `council_dispatch` returns a job id + seat name (`Dispatched ${seat.name} as ${job.id}`; `details: { jobId, seat }`) and `council_wait` returns a `formatReport` whose payload is free text under `--- output ---` (`extensions/hub-tools.ts`). There is **no structured field** distinguishing "contribution" from "disagreement" from "ruling." The only thing that ever carries that distinction is prose the facilitator writes.
- None of the four seats SEATS-1 names holds a hub grant: `product-owner`/`designer` have `Read, Grep, Glob, Write`, `principal` has `Read, Grep, Glob`, `skeptic` has `Read, Grep, Glob, Bash` — no `task`/`hub`, no `spawns` (`council/agents/*.md`; `grantsFor` in `extensions/seats.ts`). So the facilitator is the *sole* dispatcher, exactly as `council.md`'s "the facilitator routes" and `[[facilitator]]` state. That is fine and should stay.
- The current `features-new.md` steps 1–2 contain **zero dispatch** — the facilitator alone reads template/board, assigns the `EPIC` id, writes the epic goal, and decomposes children. Step 3's gate block ("Write nothing to disk until the human approves…") is the text that must survive byte-identical, so **every new requirement must be a property of the draft's content specified in steps 1–2, never an edit to step 3.**
- The output contract that flows downstream is load-bearing: a child's `state` is consumed by `council.md` step 1 (`state == Ready` hard-gates a Council run) and by `features-deliver.md`'s unattended loop, and the surface flag lives in `Intent` prose consumed by `council.md` step 1's surface-touching test. So `state` and the `Intent` surface naming are **seam-decisions made at decomposition and consumed later with no re-check** — they are not local bookkeeping. `validate.py` enforces shape (`REQUIRED_KEYS` = id/title/state/owner/epic/goal, `ID_RE`, `STATE_COLUMNS`, no `: ` in goal, board em-dash) but validates **cards/board only** — it cannot see a malformed `features-new.md`.

### Blind spots

- **Procedure-text author (human-facing contract slice).** From inside the markdown, "dispatch seats to contribute" reads as a single call whose result arrives shaped. It cannot see that `council_wait` returns free text and that three of the four seat bodies are written for *existing-card* deliberation — `product-owner` "does not design the solution," `skeptic` needs a claim to attack, `designer` needs an artifact to critique (`council/agents/*.md`). Naive parallel-generation would hand those seats a task their bodies disclaim and force the facilitator to *reconcile* four incompatible drafts — which is resolution, not aggregation.
- **Engine slice (hub-tools.ts / seats.ts).** Sees dispatch/wait/cancel as generic spawn-collect; cannot see that the semantic contract (attribution, disagreement-preservation) is entirely in procedure prose it never reads. It will faithfully return seat text and have no notion whether the facilitator merged it or silently dropped it.
- **Facilitator slice (the parent agent executing the procedure).** It is the single writer and the same model-class that would have decomposed alone. Nothing in the engine stops it from narrating "I dispatched the seats" without calling `council_dispatch`, or from paraphrasing-and-merging seat output instead of preserving it. Only the procedure text constrains it, so only the smoke harness's *job-records-on-disk* check can catch a narrated-but-not-executed dispatch.

### Reframe

The card's own wording — "the facilitator aggregates their input into the single draft pass step 3 presents" — already forbids the naive reading of four parallel decomposition generators. Make that explicit: **generate-then-attack, not parallel-generate.**

1. Facilitator produces a **first-pass decomposition** (epic + candidate children with goals, states, surface flags) — preserving the exact output shape steps 1–2 already define, so step 3's gate is untouched.
2. Dispatch `principal`, `designer`, `skeptic` **in parallel against that same first pass**, each in its own charter: principal reframes the seam cuts (does any child's `goal` bake in another slice's assumption; is the epic `goal` the whole feature and not one child's slice), designer flags which children are surface-touching and argues the `Intent` must name the screen/copy/state (the existing step-2 bullet, now seated), skeptic attacks each `goal` for falsifiability/stub/colon-space and each `state` against the Ready-vs-Backlog bar with **runnable checks against the draft text itself** (colon-space grep, `REQUIRED_KEYS` presence, `ID_RE`).
3. Dispatch `product-owner` **last**, with the aggregated disputes only — it rules, it doesn't generate (its charter: "you do not design the solution"; `when_invoked` #4 already covers `Backlog → Ready` ratification).
4. Facilitator aggregates **verbatim, labeled by seat** — reuse `council.md` step 2's exact convention ("append each returned position… verbatim, labeled by seat") — and carries anything `product-owner` does not rule as a **named, attributed open item into the step-3 draft**. That is precisely the SEATS-1 escalation path: the facilitator *is* the orchestrator proxy in `/features-new`, so "escalation to the CEO (the human), reached through the orchestrator" = the open item reaches the human at the draft gate, where the human's edit/drop/approve authority already lives.

This structure respects SEATS-1 (same four seats, named explicitly), matches the seat charters rather than fighting them, and is round-minimal by construction — a single first-pass → seat-input → ruling → draft pass — so the sibling card can insert an explicit round bound without rewriting the attribution contract.

### Testable claims

1. **Dispatch is real, not narrated.** Assert the smoke run's job ids come from the hub's on-disk records (`$CONFIG_DIR_NAME/council/runs/` manifests, or the transcript's `council_dispatch` tool events), *not* from facilitator prose. A procedure where "dispatch" is only narrated passes a prose grep and fails this.
2. **Seat-body mismatch.** Run `council_dispatch` for each of the four seats with the input "decompose $FEATURE into children" and confirm `product-owner`/`skeptic` output contradicts their bodies (product-owner refuses to design; skeptic has no claim to attack). Red result = parallel-generate is the wrong structure; green (they coherently produce decomposition artifacts) = my reframe's premise is false.
3. **Byte-identical gate.** Diff the step-3 block of `features-new.md` against the pre-change text; must be zero bytes. (New attribution language lives in steps 1–2 only.)
4. **Attribution reaches the draft.** After the smoke run, the presented draft must contain, per disputed child/field, a seat label and verbatim-or-quoted seat text; a draft where seat input is paraphrased into the facilitator's voice fails.
5. **Disagreement is named, not resolved.** Where `principal` and `designer` disagree on whether a child is surface-touching (or `skeptic` blocks a `goal`), and `product-owner` does not rule it, the draft must contain an explicit "unresolved" item naming both seats; absence of any such item when the seat transcripts show a disagreement fails.
6. **`validate.py` cannot catch this card.** A broken `features-new.md` rewrite (malformed procedure prose) leaves `python3 council/validate.py` green because it validates cards/board only — so the acceptance's validate-green criterion is a regression guard, not evidence the rewrite is correct; the smoke run is the only check that exercises the rewrite.

Grounding: `council/procedures/features-new.md`, `council/procedures/council.md`, `council/procedures/board-create-card.md`, `council/procedures/features-deliver.md`, `extensions/hub-tools.ts`, `extensions/seats.ts`, `council/agents/{product-owner,designer,principal,skeptic}.md`, `council/validate.py`; wiki `[[facilitator]]`, `[[hub-job-supervision]]`, `[[seats]]`, `[[engineering-board]]` via `vault/wiki/index.md`.

#### Round 1 — designer (job-1.3)

VERBATIM:

The person path through the step-3 draft pass:

1. **Arrival.** Human just typed `/features-new` with a feature description. They expect a list of cards they need to approve, edit, or reject — not a transcript of who deliberated.
2. **First glance.** They scan the set: epic at top, children grouped under it. They want to immediately see scope (how many cards), the goal of each, and any red flags.
3. **First action.** They read the epic's goal sentence. Move to the first child. Compare goals against the original feature description.
4. **Feedback moment.** They hit a child where the goal is contested. They want to know: contested by whom, on what dimension (scope? testability? surface?), and what their options are.
5. **Approval moment.** They need to know what "approve" means on a contested card — does approval acknowledge the disagreement, ignore it, or override it?

The Gulf of Execution is wide today: the human cannot tell what approving a card commits them to, because the card doesn't reveal its deliberation history. The Gulf of Evaluation is also wide: there's no signal that an item is contested, so the human approves the set without noticing.

**Design position**

Each card in the step-3 draft pass must carry three things, surfaced as a small structured block at the top of the card's `Intent` section so they appear before the goal sentence is read: **(1) a `Contributors` line naming every seat whose dispatch produced a substantive contribution to the card** (the seats named by ruling SEATS-1: product-owner, designer, principal, skeptic — a seat that did not contribute is omitted, not listed as silent), **(2) a `Disagreements` block listing any seat that did not endorse the card as drafted**, where each disagreement is a one- or two-line note naming the seat and the dimension (scope, testability, surface, state-assignment, etc.) — never a long paragraph, never silently folded into the goal, and **(3) an explicit `Decision: unresolved — your call` marker on every line of the disagreement block** so the human can see at a glance that the facilitator carried the disagreement forward rather than resolving it. The facilitator aggregates by **placing** these blocks, never by **adjudicating** them — the procedure text must forbid the facilitator from rewriting, paraphrasing, or omitting a seat's stated disagreement, and must require that the seat's own wording (or a faithful one-line restatement) is what appears in the block. Per-seat labels appear only inside these two blocks, never on goal sentences or state assignments, so a card reads as one unit and the trace is opt-in to read.

**Gulf closed**

Evaluation, for the human at the moment of approving the draft pass. Specifically: closing the gulf between *"the facilitator showed me a set of cards"* and *"I can tell which of these cards had a seat that did not agree with the draft, and what they disagreed about"*. A secondary execution gulf closes for the same person at the same moment: between *"I approve this set"* and *"I know whether my approval resolves, ignores, or is asked to rule on each disagreement"* — the `Decision: unresolved — your call` marker makes the act of approving a contested card an explicit acknowledgement that the disagreement is being handed up rather than smoothed over.

**Principle and evidence**

- **Signifier + feedback (the principle).** A `Contributors` line and a `Disagreements` block are visible signifiers of deliberation state. They turn the cards from opaque drafts into a document where the human can read the trace before deciding.
- **Knowledge in the world (the principle).** "A seat disagreed" must not be a thing the human has to infer by reading carefully — it is a thing the surface must say.
- **Constraint by procedure text (the principle).** The smallest fix for "the facilitator silently resolves disagreement" is a procedure-level rule that forbids the facilitator from paraphrasing or omitting a seat's disagreement. The card carries what the seat said; the facilitator cannot blur it.
- **Slip vs. mistake (the principle).** Today's failure is a mistake class: the human approves a set believing all seats agreed because the surface does not say otherwise. The fix is making the conceptual model visible at the moment of decision.
- **Evidence (the artifact).** `council/procedures/features-new.md` step 3 ("Present the full draft of the epic and every child — complete frontmatter and `Intent` section, exactly as each would be written to disk — to the human in one pass") does not require any seat attribution or disagreement surface today; step 2's prose ("a single testable `goal`", "`state` set per `/board-create-card` step 4") defines the per-card shape that the draft pass inherits. The card's `Intent` section is the right insertion point because step 3 names `Intent` explicitly as part of the presented draft and `council.md` step 1 already reads `Intent` to decide whether `designer` is seated — so an `Intent` block that names contributors also strengthens that downstream decision.

**Falsifiable predictions**

Each is a hypothesis I cannot verify from this seat; the smoke run that would falsify each is named so `owner`/`skeptic` can confirm.

- **P1 (presented shape).** *Hypothesis:* after the rewrite, the step-3 draft pass of `/features-new` against the scratch copy in SMOKE-1 produces each card with a `Contributors:` line and, where applicable, a `Disagreements:` block above the `Intent` prose. *Falsifier:* a CDP smoke that asserts (a) every card in the presented set carries a `Contributors:` line naming at least one of {product-owner, designer, principal, skeptic} when that seat's dispatch produced substantive input, and (b) any card where a dispatched seat's `stopReason=end_turn` included a disagreement carries a `Disagreements:` block with that seat named — failing either assertion falsifies P1.
- **P2 (facilitator non-omission).** *Hypothesis:* when a dispatched seat returns a disagreement, the disagreement wording in the `Disagreements:` block is the seat's own (or a faithful ≤2-line restatement), not the facilitator's paraphrase. *Falsifier:* a pure-seam test on the procedure text that scans for the substring "must not paraphrase" or "verbatim" near the disagreement-routing instruction — and a smoke assertion that compares the seat's last assistant message against the presented `Disagreements:` block string-equal (allowing whitespace and the ≤2-line restatement allowed by the rule). Failure falsifies P2.
- **P3 (unresolved marker visible).** *Hypothesis:* every `Disagreements:` block contains the literal marker `Decision: unresolved — your call` (or a clearly equivalent fixed phrase committed by the procedure). *Falsifier:* a CDP smoke that greps the presented draft for that exact phrase and asserts at least one occurrence per contested card. Failure falsifies P3.
- **P4 (approval does not silently resolve).** *Hypothesis:* when the human approves a card with a `Disagreements:` block, the disagreement remains visible in the on-disk card after the write — i.e., the procedure does not strip the block at write time. *Falsifier:* after the smoke-run approval path, a grep on the written `council/cards/<id>.md` file for the seat name and the disagreement's distinguishing noun — the seat name should still appear. Falsifies P4 if absent.
- **P5 (step-3 byte-identity preserved).** *Hypothesis:* the prose "Write nothing to disk until the human approves…" survives the rewrite byte-identical and `python3 council/validate.py` stays green on the scratch copy. *Falsifier:* `diff <(git show HEAD:.../features-new.md) <(sed -n '/Write nothing to disk/,/assumption that silence means yes/p' features-new.md)` produces empty output, and `python3 council/validate.py` prints `All council artifacts valid`. The acceptance criterion in the card names this explicitly.

**Preferences, ranked last**

- **P-ranked-1 (taste).** I'd put the `Contributors:` line and `Disagreements:` block at the very top of `Intent`, above the prose, rather than as a subsection — but whether the procedure text puts them before or after the goal sentence in the rendered `Intent` is taste as long as both are visible before the human approves.
- **P-ranked-2 (ungrounded).** A separator glyph (e.g., `---` or a boxed marker) between contributors and disagreements would help scan, but I have not verified how the human reads the step-3 pass in practice; this is taste.
- **P-ranked-3 (escalation, not preference).** I am uncertain whether the procedure should require product-owner to also see and explicitly hand off disagreements it raised (under ruling SEATS-1 it participates but product-owner escalation to CEO is the human's route). If the disagreement involves a product-shape question, the surfaced marker should arguably *suggest* escalation rather than only mark it unresolved — but that bleeds into a product ruling the product-owner owns. I flag it; I do not decide it from this seat.

#### Round 2 — owner (job-1.4)

VERBATIM:

I've re-read the artifact, both seat bodies, the engine, the smoke harness, and the wiki pages both seats cite. Grounding: `council/procedures/features-new.md` (steps 1–3), `council/procedures/council.md` (step 1 surface-touching reads `Intent`; step 2 independence + "verbatim, labeled by seat"; step 6 routing), `council/agents/{product-owner,skeptic,designer,principal}.md`, `extensions/hub-tools.ts` (`council_wait` returns free text under `--- output ---`; `details: { jobId, seat }`), `extensions/seats.ts` (`grantsFor` — none of the four holds a hub grant), `council/validate.py` (cards/board only), `council/cards/EV-10.md` + `EV-11.md`, `vault/wiki/{facilitator,run-transcripts,smoke-test}.md`, AGENTS.md convention 12.

## What principal changed my mind on — I concede parallel-generate

Principal's claim 2 is the decisive test, and I now predict it comes out red. I read the four bodies against a bare "decompose $FEATURE into children" dispatch:

- `product-owner.md`: "You decide — you do not design the solution." A decomposition *is* the solution at card level. A bare decompose dispatch asks it to do the one thing its role forbids.
- `skeptic.md`: "An objection has standing only when it is falsifiable: it must name the specific test or observation that would settle it." With no claims in front of it, skeptic has nothing to attack; it would have to *generate* a decomposition to attack, which is not its charter.
- `designer.md`: "You cannot see the running interface… a critique of a screen you have not opened is a critique of your memory of screens." It needs an artifact to critique.
- `principal.md`: "Propose a reframe only when the card earns one." It reframes seams; it does not generate them.

Even if the models comply off-charter (LLMs are compliant), the output is off-charter — which is the point. And my round-1's "mechanical concordance" cannot survive contact with the real problem: four independent decompositions of a fuzzy feature produce four *different slicings*, and aligning "children by stated scope" forces the facilitator to choose a slicing — a design decision. That is adjudication wearing aggregation's clothes, the exact defect the card removes. Principal's generate-then-attack avoids it: one slicing (the first pass), seats critique it, aggregation is keep-what-survived + apply-rulings + carry-disagreements. I accept the reframe, and I accept principal's claims 1, 3, 5, 6 (dispatch-not-narrated via `runs/` manifests; byte-identical gate; disagreement named not resolved; `validate.py` blind to procedure prose) — all three of us already agree on those.

## What I hold against principal — three amendments

1. **product-owner is dispatched unconditionally, last, with the amended draft + the ledger — not "disputes only."** Two reasons. First, the epic goal is a product statement ("names what the whole feature delivers"), and "decide what the product should be when no test can decide it" is product-owner's core charter; under disputes-only, a clean run leaves the epic goal with zero product-owner scrutiny. Second, the card's own acceptance says "the seats actually dispatched (job ids on record) and the presented draft naming them" — under disputes-only, a clean toy-feature run may never dispatch product-owner and the acceptance fails deterministically. Its charter is ruling-only: rule each dispute it may rule (dissent named, per its `Ruling/Options rejected/Grounding/Reversibility` format), ratify or amend the epic goal and each child's `state` (its `when_invoked` #4 already covers `Backlog → Ready`), escalate what it may not rule per its `<escalation>` (portfolio change, wrong goal, reversing a recorded human decision) to the human per SEATS-1. It never re-slices children and never rewrites undisputed child goals — that boundary is what keeps it ruling, not generating.
2. **Independence within the attack round.** The three attacking seats are dispatched in parallel, each with the first pass only, never with another seat's critique — council.md step 2's isolation rule, preserved. That is what keeps the attack round independent rather than a bounded exchange; the exchange surface is EV-11's, and this card must not draft it.
3. **The first pass is a proposal, not a constraint.** The anchoring risk in generate-then-attack is real: seats critique what's there, not what's missing. The procedure text must charter each seat to attack *completeness* as well as elements — a missing child is "what is wrong" (skeptic), "what no single-vantage position is positioned to see" (principal), "what the person needs" (designer) — and a wholesale rejection of the slicing is a named disagreement, not a patch request. This is the mitigation for the one thing parallel-generate did better (surfacing missing children), and it costs no new mechanism.

## What designer changed my mind on — I accept P1, P2, P3, P5

Designer's P1–P3 are a better-specified version of my round-1 "per-card attribution lines + named-disagreement ledger": a `Contributors:` line and a `Disagreements:` block at the top of each card's `Intent` in the *presented* draft, with the `Decision: unresolved — your call` marker, seat labels only inside the blocks, and the procedure text forbidding paraphrase/omission (P2 — reuse council.md step 2's "verbatim, labeled by seat" convention). I accept all of it for the presentation surface. P5 (byte-identity + validate.py green) is already my claim 1. Designer's P-ranked-3 is resolved by my amendment: product-owner sees the *whole* ledger, not just its own disagreements, and the `unresolved — your call` marker *is* the escalation surface — no separate "suggest escalation" mechanism needed.

## What I hold against designer — P4, precisely

The Disagreements block does **not** survive onto the on-disk card. Four grounds:

1. **The human's approval at the gate IS the resolution.** SEATS-1's escalation target is the CEO (the human), reached through the orchestrator — the facilitator of `/features-new` *is* that orchestrator, and the step-3 gate is where the human's edit/drop/approve authority already lives. When the human approves a card with a visible disagreement, the human has ruled on it. Persisting a pre-approval dispute snapshot to disk is stale metadata about a settled question.
2. **The repo already separates deliberation record from work items.** AGENTS.md convention 12 and `vault/wiki/run-transcripts.md`: `runs/` is the deliberation record (manifests + transcripts, pruned to 15), and cards are the durable work items `validate.py`'s `REQUIRED_KEYS` and the board discipline govern. P4 blurs that separation. The human's ruling survives in the card *text* (the approved goal, the approved state); only the contest history is pruned, and future runs don't need it — the goal text binds them.
3. **The downstream `Intent` surface-reading is a corruption vector.** council.md step 1 reads `Intent` to decide surface-touching and designer seating; features-deliver's council-runner reads the card as design intent. A written `Disagreements:` block mentioning "screen"/"copy"/"state" — including one arguing a child is *not* surface-touching — can flip that decision. Deliberation metadata read as design intent is exactly the class of seam bug this card exists to prevent.
4. **The card's own acceptance says "reach the step-3 draft pass."** Disagreements reach the *draft pass*, not the disk. P4 goes beyond the card.

The gate contract ("complete frontmatter and `Intent` section, exactly as each would be written to disk") is preserved by presenting card text exactly as written *plus* a clearly-separate ledger surface; the procedure text must state that the ledger is presented, never written, so the facilitator cannot blur the two.

## Precise remaining disputes

- **With principal:** product-owner's dispatch — unconditional, with the amended draft + ledger, ruling-only (mine) vs. disputes-only (principal's). Settled by a smoke assertion: all four seats have a `done` job in the scratch run's manifests on *every* run; under disputes-only, a clean run lacks product-owner's job and fails the card's own acceptance.
- **With designer:** P4 — persistence to disk (designer) vs. presentation-only (mine). Settled by the downstream-invariance test below plus the card-content hygiene assertion.

## Testable claims

1. **Seat-body mismatch (principal's claim 2, now shared; one-time probe, not a permanent gate).** Dispatch each of the four seats with "decompose $FEATURE into children" (no first pass); assert off-charter output: product-owner designs the solution, skeptic files no falsifiable objection with a settling test, designer critiques no artifact. Red → generate-then-attack confirmed; green → my concession is wrong.
2. **Independence.** The three attack dispatches carry identical `input` (the first pass); no input contains another seat's critique or a facilitator decomposition beyond the first pass.
3. **All-four-dispatched.** Scratch-run `runs/<runId>/*.json` manifests contain exactly one `done` job per seat in {product-owner, designer, principal, skeptic} — pins my amendment and the card's acceptance.
4. **Step-3 byte-identity pin.** Fixture string equal to today's step-3 block; `expect(shippedBlock).toEqual(fixture)` — green now, red the moment any byte moves.
5. **Card-content hygiene (settles P4).** After the scratch-run approval path, every written `council/cards/<id>.md` contains no seat-attribution or disagreement text in frontmatter or `Intent`, and `python3 council/validate.py` on the scratch copy prints `All council artifacts valid`. Designer's P4 falsifier (seat name present in the written card) fails by design.
6. **Downstream invariance (settles P4's cost).** Surface-touching classification of a card's `Intent` (council.md step 1's mechanism) is unchanged by the presence or absence of a `Disagreements:` block; a block that flips the classification demonstrates the corruption vector that P4 would ship.

- **Approach** — I concede parallel-generate and adopt principal's generate-then-attack: facilitator first pass (exact step-1/2 output shape) → principal/designer/skeptic attack it in parallel, each in its own body's output format, chartered on completeness as well as elements → product-owner dispatched last, unconditionally, with the amended draft + ledger, ruling-only (ratify/amend epic goal and states, rule disputes, escalate per SEATS-1) → facilitator aggregates verbatim, labeled by seat, carrying every unruled disagreement as a named open item into the step-3 presentation. Attribution and disagreements are presentation at the gate, never card-file content.
- **Tradeoffs accepted** — The facilitator retains first-pass authorship (a proposal, subject to wholesale seat rejection); I give up parallel-generate's independent-child-surfacing in exchange for charter-consistent seats and an aggregation that is mechanical rather than adjudicative, with the completeness charter as the mitigation. Four dispatches per run (one more than principal's disputes-only) buys deterministic SEATS-1 compliance and the smoke acceptance. Attribution survives only in the gate presentation and the pruned `runs/` transcript — the written card is the settled artifact, and the human's approval at the gate is the resolution.
- **Testable claims** — Six above; the two that settle the live disputes are #3 (all-four-dispatched, vs. principal) and #5/#6 (card-content hygiene + downstream invariance, vs. designer's P4).

#### Round 2 — principal (job-1.5)

VERBATIM:

I've read the card, both rulings, all four seat bodies, both counters, the engine (`hub-tools.ts`, `seats.ts`), `validate.py`, `prose.test.ts`, and the three procedures that consume card files (`council.md`, `features-deliver.md`, `board-create-card.md`). Wiki pages used: `[[facilitator]]`, `[[hub-job-supervision]]`, `[[seats]]`, `[[engineering-board]]`, `[[run-transcripts]]`, `[[council-loop]]`, `[[procedure-commands]]` via `vault/wiki/index.md`. Same wiki gap owner flagged: nothing in the vault describes decomposition-by-council; the mechanism claims rest on procedures + engine, which I read directly.

## Engagement — owner

**What changes my mind.** Owner's "no facilitator draft" is right and I was wrong in round 1. EV-10's `goal` says the draft records seats "**instead of** the facilitator decomposing alone" — my round-1 "facilitator produces a first-pass decomposition" relocates the solo defect one step earlier and violates the goal's letter. The first decomposition artifact must be seat-authored, not facilitator-authored. I retract that part of generate-then-attack.

**What I adopt.** Three things, all correct and under-specified in my round 1: (1) the mandated output shape as the *aggregation contract* — it's what makes concordance mechanical instead of invented; (2) "aggregate by concordance, never by paraphrase"; (3) the two-tier disagreement routing (product-owner rules what its escalation criteria permit; the rest escalates to the human at the gate). These are compatible with my structure, and I fold them in.

**What I hold, and the precise dispute.** Owner's *parallel-generate of all four* is not coherent, and the reason is a file owner never opened: each seat already has a mandated output shape — its own `<output_format>` — and **none of the four is a children-list**:

- `principal` → Cross-seam reading / Blind spots / Reframe / Testable claims
- `designer` → Design position / Gulf closed / Principle and evidence / Falsifiable predictions
- `skeptic` → Objections / What I ran / Verdict
- `product-owner` → Ruling / Options rejected / Grounding / Reversibility

Owner's "mandated output shape (children list with per-child scope/goal/state/surface + the epic goal)" is a *second, conflicting* output contract layered on top of the seat's own. The engine builds the child prompt from the seat body verbatim (`buildSystemPrompt` → `seat.body`; `buildChildArgv` appends `input` after it) — nothing rewrites `<output_format>`. So owner's dispatch forces a two-way contradiction: either the seat obeys its body (emits Objections/Verdict, or a Ruling, not a children-list) and there is **no mechanical input for the concordance to concordance**; or the seat obeys the imposed shape (emits a children-list) and the procedure has authored a new seat that the seat file's charter disclaims — skeptic "finds what is wrong," it does not author; product-owner "does not design the solution"; designer "makes no edits… your output is a position or a critique." There is no third option. The shape fixes comparability of *what the human sees*; it does not fix that three of four seats are being asked to emit a document their own yield contract refuses.

That is the narrow remaining dispute, and it's now a partition question, not a taste question: **which seats author, which attack.** My position: `principal` (seam-framing is its charter; its `Reframe` output *is* a decomposition) and `product-owner` (the epic goal is the "what," its charter) author; `skeptic` and `designer` attack that artifact in their native formats; `product-owner` then rules what the attackers surface. Same four seats, same independence, no facilitator-authored content — but each seat works in the format its body already commits it to.

## Engagement — designer

**What I adopt, unqualified.** P1, P2, P3 as properties of the **gate presentation**: a `Contributors` line, verbatim-or-faithful-restatement disagreements, and the literal `Decision: unresolved — your call` marker in the step-3 draft the human reads. The signifier-at-approval argument is correct and it *is* the SEATS-1 escalation surface made explicit at the moment of decision. My round-1 "named, attributed open items in the draft" was the same idea stated weakly; designer's form is the right form, and I take it.

**What I hold against P4.** Persisting `Contributors`/`Disagreements`/`Decision:` into the card's `Intent` or frontmatter is wrong, and it fails at the seam designer's render-path vantage cannot see: **the card file is read by unattended consumers, not just by the human at the gate.** Three grounded reasons:

1. `council.md` step 1 reads `Intent` to run the surface-touching test (seats `designer`). A `Disagreements:` block whose note contains the word "screen" or "copy" would fabricate a surface flag; a block that doesn't would still dilute the read the test depends on. The `Intent`'s job is to name the user-visible surface — `features-new.md` step 2 says exactly this — and deliberation metadata displaces that signifier.
2. `council.md` step 10 gives the judge "the card's goal and nothing else" deliberately, for uncontaminated freshness. That protects the *goal*, but the principle generalizes: `features-deliver.md` and `council-runner` run the card unattended against its frontmatter and `Intent`, and the deterministic merge check's criterion 5 requires "no `Needs Human` state or outstanding ruling." A card that still carries "`Decision: unresolved — your call`" **after** the human approved it is a stale signifier that asserts "unresolved" about a dispute the human just resolved. The human's approval is the resolution (SEATS-1: escalation reaches the CEO/human through the orchestrator); persisting "unresolved" past that moment lies to every downstream reader.
3. `validate.py` cannot catch this pollution: `parse_frontmatter` accepts any `key: value` line (no unknown-key rejection) and stops reading at the first non-conforming line, so Intent prose is invisible to it. P4 would be *unvalidated* durable state — precisely the kind that silently drifts and that `validate.py`'s whole existence is meant to prevent.

So: attribution and disagreement survive in **the gate presentation and the run transcript** (`runs/<runId>/…`, the substrate `[[run-transcripts]]` describes) — never in the card. The card is the decision; the transcript is the trace. Designer's P1/P2/P3 stand fully; only their *location* changes, from card-file to presentation+transcript.

On designer's P-ranked-3 (should PO hand off its own disagreements): the seam answers it, and I'll state it — product-owner's `<escalation>` is a closed list (portfolio change, reversing a recorded human decision, the goal itself is the defect). An ordinary product-shape dispute is `when_invoked` #1: product-owner rules it, dissent named. The marker says "unresolved — your call" only for what product-owner *may not* rule; for what it *did* rule, the ledger says "ruled by product-owner (dissent: <seat>)". That is the two-tier, and it keeps P-ranked-3 from bleeding into EV-11's surface.

## Principal output format

- **Cross-seam reading** — The contract has three sides nobody reads together: (a) `council_wait` returns free text (`--- output ---`, `hub-tools.ts`) with no contribution/disagreement distinction — only prose the facilitator writes carries that; (b) each seat's `<output_format>` is itself an output contract, and the four don't align to "children-list + epic goal"; (c) card files are consumed unattended downstream (`council.md` step 1 surface test on `Intent`, step 10 judge on `goal` alone, `features-deliver` merge check criterion 5), so what the rewrite puts in `Intent`/frontmatter changes what those consumers read — with no re-check, since `validate.py` sees cards/board only, not procedures and not `Intent` prose.

- **Blind spots** — **Owner (aggregation slice):** sees the facilitator's bookkeeping and misses that the seats' own `<output_format>` blocks are the contract a mandated children-shape collides with — "mechanical concordance" presupposes mechanically-comparable seat outputs that the seats' bodies won't produce. **Designer (gate/render slice):** sees the human at approval and misses the unattended readers of the same file — persisting dissent into `Intent` changes what `council.md`'s surface test and `features-deliver` inherit. **My own round-1 blind spot (named honestly):** principal is a generator-flavored seat and I handed the facilitator a decomposition, which is the solo defect the card's `goal` says to remove; owner caught it and I concede it.

- **Reframe** — Not "all four parallel-generate" (owner) and not "facilitator generates, all attack" (my round 1). **Generate-with-the-generators, attack-with-the-adversaries:** wave 1 dispatches `principal` + `product-owner` in parallel, independent, no facilitator draft — principal authors the cross-seam decomposition (its `Reframe` output *is* one), product-owner authors the epic goal and the what-vs-how on each child (its `Ruling` output). Wave 2 dispatches `skeptic` + `designer` in parallel against that artifact — skeptic attacks goals/states with runnable checks, designer flags surface-touching children and argues `Intent` (their native Objections/Verdict and Design-position formats). Wave 3, conditional, `product-owner` rules what the attackers left open; what it may not rule carries to the gate as `Decision: unresolved — your call`. Aggregation is owner's mechanical concordance over *those* native outputs, verbatim, attributed; attribution and the disagreement ledger live in the gate presentation and the transcript, never the card file. This is SEATS-1's four seats, respects every seat's `<output_format>`, removes facilitator authorship (the card's literal goal), and leaves EV-11's bounded-exchange seam between generation and aggregation untouched.

- **Testable claims**
  1. **Seat-format collision (settles owner's counter).** Dispatch each of the four with "decompose `$FEATURE`" + owner's mandated children-shape, no other position. Assert skeptic's output matches its body (`Objections`/`Verdict`) and contains no children-list; assert product-owner's matches `Ruling`. *Red* (either emits a children-list) ⇒ the imposed shape overrode the seat body — procedure-authored-a-new-seat. *Green* (native formats only) ⇒ no children-list exists to concordance — owner's mechanical aggregation has no mechanical input. Both branches falsify "parallel-generate with a mandated shape is coherent."
  2. **No facilitator authorship.** Step-2 rewrite text contains no instruction for the facilitator to draft a goal/state/surface sentence, and the smoke run's generator wave is `principal`+`product-owner` dispatches (job ids on record), not facilitator prose. Red if any facilitator-authored goal survives un-attacked into the draft.
  3. **Persistence location (settles P4).** Two-sided smoke assertion: the pre-approval presented draft contains `Contributors:`, `Disagreements:`, and `Decision: unresolved — your call` per contested card (designer P1/P3, adopted); the post-approval written `council/cards/<id>.md` contains none of those strings, and `python3 council/validate.py` prints `All council artifacts valid` on the scratch copy. Red in either direction.
  4. **`validate.py` blindness.** A rewrite that injects attribution into cards leaves `validate.py` green (it parses frontmatter `key: value`, rejects no unknown key, never reads `Intent` prose) — so validate-green is a regression guard, not evidence the rewrite is correct; only the smoke + the step-3 byte-pin exercise it. I agree with owner's claim 1 and designer's P5 on the byte-pin, and dispute nothing there.

**Remaining disputed, precisely:** (i) whether skeptic and designer are generators or adversaries — settled by claim 1, which I predict lands on "adversaries"; (ii) whether attribution/disagreement persists to the card file (designer P4) or stops at presentation + transcript (owner + me). Everything else has converged.

#### Round 2 — designer (job-1.6)

VERBATIM:

**Design position (revised).** The `Contributors:` line + `Disagreements:` block + `Decision: unresolved — your call` marker belong on the **step-3 draft presentation** (the prose the facilitator puts in front of the human between step 2 and step 3's "write nothing to disk"), **not** in `council/cards/<id>.md`. The surface I argued for survives, but its location moves from card-file content to gate-presentation only — and the structure that produces it shifts from `owner`'s parallel-generate-with-mandated-output-shape to `principal`'s generate-then-attack with charter-fit dispatch (principal/designer/skeptic attack a facilitator first pass; product-owner rules last). The `Contributors:` line is only a true signifier if the seat had the charter to author what it is attributed with — `product-owner`'s body explicitly forbids designing the solution, so a `Contributors: product-owner` line on a goal sentence would be a **false signifier**.

**Gulf closed.** Evaluation, for the human at the step-3 approval gate: closing the gulf between *"the facilitator showed me a set of cards"* and *"I can tell which cards had a seat that did not agree with the draft, on what dimension, and whether approving carries it forward or silently resolves it."* A secondary execution gulf closes at the same moment: the human can now act on a contested card with explicit knowledge that approval is an acknowledgement, not a quiet override. Both gulfs close on the **gate presentation**, not on the durable card.

**Principle and evidence.**
- **Signifier + feedback.** A `Contributors:` line and `Disagreements:` block are visible signifiers of deliberation state at the gate.
- **Knowledge in the world beats knowledge in the head.** "A seat disagreed" must not be a thing the human infers by reading carefully — the surface must say it.
- **Human error is design error** (the false-signifier corollary). When the data cannot support the strong claim the surface makes, the gap is the design defect. `product-owner`'s `<role>` (`council/agents/product-owner.md`: "you do not design the solution") makes a `Contributors: product-owner` line on a goal sentence a claim the seat has no charter to back — closing that gap is *why* the rewrite must charter-fit, not just parallel-dispatch.
- **Evidence (the artifacts).**
  - `council/procedures/features-new.md:29–35` — step 2's `Intent` is the surface `council.md` step 1 reads for the surface-touching test; deliberation artifacts in `Intent` masquerade as surface-naming prose.
  - `python3 council/validate.py` — validates `id/title/state/owner/epic/goal` and the board em-dash, not `Intent`; deliberation traces in `Intent` are neither enforced nor parsed.
  - `council/agents/product-owner.md` (`<role>`, `<when_invoked>` #1 + #4) and `council/agents/skeptic.md` (`<stance>`) and `council/agents/designer.md` (`<deliberation_mode>`) — the charter claims that make parallel-generate a false-attribution hazard.
  - `council.md` step 2 ("append each returned position… verbatim, labeled by seat") and `council.md` step 6 (ruling-dispatch shape) — the precedent for principal's reframe's verbatim + ruling steps.
  - `extensions/hub-tools.ts` (`details: { jobId, seat }`, `Dispatched ${seat.name} as ${job.id}`) — confirms the engine return contract carries only seat name and job id, never content-type labels; the semantic distinction (contribution vs. disagreement vs. ruling) lives in prose the facilitator writes.

**Falsifiable predictions.**
- *P-rev-1 (charter-fit dispatch).* Hypothesis: under principal's structure, the four seats produce output consistent with their bodies — `product-owner` outputs a ruling, not a decomposition; `skeptic` outputs runnable objections against the first-pass draft; `designer` outputs surface-flag arguments; `principal` outputs seam-cut observations. Falsifier: smoke run's seat transcripts (`runs/<runId>/<jobId>.json`) show `product-owner` producing goal sentences.
- *P-rev-2 (gate-presentation attribution).* Hypothesis: step-3 presented draft contains per-card `Contributors:` + (where applicable) `Disagreements:` with the `Decision: unresolved — your call` marker; **on-disk card files after approval contain neither**. Falsifier: pure-seam test grepping `council/cards/<id>.md` for `Contributors:` and `Disagreements:` returns ≥1 match, or the presented draft is missing either.
- *P-rev-3 (no false signifier).* Hypothesis: no `Contributors:` line names a seat outside the scope its charter permits. Falsifier: smoke-run inspection finds a `Contributors: product-owner` line on a goal sentence.
- *P-rev-4 (step-3 byte-identity).* Hypothesis: gate block survives byte-identical and `validate.py` stays green on the scratch copy. Falsifier: non-empty diff, or `validate.py` prints anything other than `All council artifacts valid`. (Same as round-1 P5; pinning.)

**Remaining dispute.** Surface (gate-presentation, not card-file content) is now agreed across all three seats. **The dispute that remains is the generator set:** owner wants all four seats parallel-generating with a mandated output shape; principal wants `principal`/`designer`/`skeptic` attacking a facilitator first pass with `product-owner` ruling last; I am on principal's side on charter-fit grounds, with the addition that the rewrite should *name each seat's charter in the dispatch block* (one paragraph), not just the seats. This is small text, large consequence: it makes the `Contributors:` line honest.

**Preferences, ranked last.** Gate-presentation block ordering (above vs. below the goal sentence in the human-visible draft) — taste, not grounded. A boxed marker / `---` separator between `Contributors:` and `Disagreements:` — taste. Whether the unresolved marker should *suggest* escalation through `product-owner` for product-shape disputes — flagged as escalation territory the product-owner owns, not decided from this seat.

**Ground-noting correction.** Owner's citation `council-runner.md:216-219` points at `council/agents/council-runner.md` (a seat, not the procedure); grep for the literal "restating, not cross-referencing" finds no matches in the procedure corpus. The "restate, don't cross-reference" precedent is real — `council.md` step 1's dispatch-discipline block is itself the restatement — but the cited line is the wrong one. Not load-bearing for my position; flagging for the rewrite's grounding.

#### Round 3 — owner (job-1.7)

VERBATIM:

I've read the card, all four seat bodies, both procedures, the engine, `validate.py`, and the wiki pages (`facilitator`, `smoke-test`, `run-transcripts` via index). Grounding for this round: `council/cards/EV-10.md` (goal, Intent, acceptance, SEATS-1/SMOKE-1), `council/procedures/council.md` (step 2 generator set, step 6 routing), `council/agents/{principal,product-owner,designer,skeptic}.md`, `extensions/seats.ts:406-433` (`buildSystemPrompt` → `seat.body` verbatim; `buildChildArgv` appends `input`), `extensions/hub-tools.ts` (`council_wait` returns free text under `--- output ---`), `council/validate.py` (cards/board only), `vault/wiki/smoke-test.md` (harness asserts dispatch evidence in `runs/`).

## Engagement

**Principal — I concede the facilitator first pass, and I hold against the two-author wave 1.**

The goal's letter ("instead of the facilitator decomposing alone") and the Intent's "dispatched seats **contribute to** the slicing, the goal sentences, the state assignments, and the designer-surface flags" both point the same way: the decomposition elements must be seat-authored, and the facilitator aggregates. My round-2's facilitator first pass was the last piece of facilitator authorship, and your reading of the card text is right — I revise. But your specific partition (principal + product-owner author in parallel) fails on two grounds I can ground:

1. **Self-review, and it breaks the council's own pattern.** `council.md` step 2's generators are `owner`, `principal` (+ `designer` when surface-touching); `product-owner` appears only at step 6, to rule. The council's established design is product-owner-rules-not-generates. Your wave 1 has product-owner authoring the epic goal and the what-vs-how, then wave 3 has it ruling on what the attackers left open — including attacks on its own authorship. `product-owner.md`'s `<role>` ("you do not design the solution") and `<humility>` ("read every position handed to you to be changed by it") are written for a ruling seat, not an author-judging-its-own-work seat.
2. **The merge is adjudication, not aggregation.** Two independent wave-1 authors, dispatched in parallel with identical input, produce two decompositions. Your own claim 1 establishes the seats won't produce mechanically-comparable outputs — so the facilitator's combination of principal's slicing with product-owner's per-child what-vs-how (keyed to children product-owner imagined, never having seen principal's) is a *choice* between conflicting slicings. That is the exact defect this card removes, wearing aggregation's clothes.

**Designer — your round-2 is partially stale, and your charter-fit concern is satisfied by my revision.** Your "I am on principal's side" was written against principal's *round-1* (facilitator first pass + charter-fit dispatch) — the structure principal retracted in round 2. So on the author question your round-2 position (facilitator) is now orphaned: neither principal nor I hold it. What survives is your charter-fit principle — no `Contributors:` line may name a seat outside its charter — and my revision satisfies it: principal authors in its native `Reframe` format (its own claim: "its Reframe output *is* a decomposition"), skeptic attacks in `Objections`/`Verdict`, designer critiques in `Design position`, product-owner rules in `Ruling`. No seat emits a document its yield contract refuses, and no `Contributors: product-owner` line on a goal sentence exists because product-owner never authors goal sentences. I also accept your addition — the dispatch block names each seat's charter in one paragraph — and your ground-noting correction (`council-runner.md:216-219` points at a seat, not the procedure; the restate-don't-cross-reference precedent lives in `council.md` step 1's dispatch-discipline block).

## Final position on the single question

**Principal authors the first decomposition artifact — alone, wave 1, in its native `Reframe` format, carrying the slicing plus draft goals, draft states, and draft surface flags, which the facilitator extracts onto the mandated output shape (the aggregation contract).** Wave 2: `skeptic` + `designer` attack that artifact in parallel, chartered on completeness as well as elements (my round-2 amendment 3, kept). Wave 3: `product-owner` dispatched **last, unconditionally, ruling-only** — ratify or amend the epic goal and each child's state (its `when_invoked` #4), rule each open-judgment dispute the attackers surfaced (dissent named), escalate what its `<escalation>` forbids to the human per SEATS-1. The epic goal's final form is product-owner's ruling, so the "what" gets product-owner's judgment every run — just not its first draft. Attribution and the disagreement ledger live in the gate presentation and the run transcript, never the card file (converged).

- **Approach** — Concede facilitator-first-pass to principal's reading of the goal's letter; hold single-author (principal) against principal's two-author wave 1 on self-review and merge grounds; hold product-owner ruling-only, unconditional, last. The structure is principal's; the author set is mine.
- **Tradeoffs accepted** — Principal drafting goals/states/surfaces is partially off-charter (its charter is seam-cuts); the input charters the full proposal and the other three seats correct the elements in native formats, with the epic goal and states ending as product-owner's rulings. I give up product-owner authoring the epic goal (charter-fit: the what) to avoid self-review; the final form is identical either way. I give up the facilitator-first-pass's guaranteed single artifact; principal's proposal is the artifact, and the anchoring risk is mitigated by the completeness charter.
- **Testable claims** — 1. **No facilitator authorship** (principal's claim 2, shared): step-2 rewrite text contains no instruction for the facilitator to draft a goal/state/surface sentence; the smoke run's generator wave is principal's dispatch (job id on record), not facilitator prose. 2. **Product-owner ruling-only** (settles author-vs-ruler): smoke transcript for product-owner matches its `Ruling` format and contains no authored goal sentences — only ratification/amendment of principal's draft and rulings (designer's P-rev-1 falsifier applied). 3. **All-four-dispatched every run**: scratch-run manifests contain exactly one `done` job per seat in {product-owner, designer, principal, skeptic} — principal (wave 1), skeptic + designer (wave 2), product-owner (wave 3, unconditional). 4. **Merge test** (settles two-author wave 1): under principal's structure, dispatch principal + product-owner in wave 1 with identical input; assert their child sets are identical — red (they differ) means the facilitator's combination is a choice between conflicting slicings, i.e. adjudication; I predict red on any non-toy feature. 5. **Self-review test**: no product-owner ruling concerns an element product-owner authored — trivially green under my structure, red under principal's whenever product-owner rules on its own epic goal. 6. **Attribution location** (converged): pre-approval presented draft contains `Contributors:`/`Disagreements:`/`Decision: unresolved — your call`; post-approval `council/cards/<id>.md` contains none; `python3 council/validate.py` prints `All council artifacts valid` on the scratch copy.

#### Round 3 — principal (job-1.8)

VERBATIM:

**Decision: hold on the one open question; accept owner's amendments 1–2 and the completeness charter of amendment 3, and tighten my round-2 partition in response to designer's false-signifier point.** Everything else has converged and I won't re-argue it.

### Cross-seam reading

The seam is authorship, and it has three sides no single slice reads together:

1. **The goal's letter.** EV-10's goal ends "**instead of the facilitator decomposing alone**"; EPIC-3's goal says "rather than a solo facilitator draft." A facilitator first pass is the facilitator decomposing alone — the fact that seats subsequently attack it does not un-author it. "Proposal, not constraint" (owner's amendment 3) changes how binding the facilitator's draft is; it does not change *who wrote it*. The card's defect is authorship, not finality.
2. **The facilitator's charter.** `council.md` opens the facilitator block with "**The facilitator decides nothing.** You route work, fan seats out, count rounds, and write the board." A first-pass decomposition is a bundle of design decisions — which children exist, what each goal says, what state each gets. That is "picking a design," the exact act the same paragraph says "has destroyed the entire output." Owner correctly ran this charter test against `product-owner` in round 2 (claim 2: a decompose dispatch asks PO to do what its role forbids) but never ran it against the one role the test fails hardest on: the facilitator.
3. **The facilitator has no body.** There is no `facilitator.md` under `council/agents/` — the facilitator is a procedure role, not a seat. It has no charter body, no `Reframe`/`Ruling` output format. Seat-authored first pass is the *only* structure where the author of the decomposition has a charter and an output contract to hold them to. A facilitator first pass is authorship by a role that by definition cannot be attributed — which is the ultimate false signifier (see designer, below).

And the round budget cuts the same way: EV-11's CAP-1 bounds the session at 3 rounds. A facilitator first pass is an **uncounted round-0** — the decomposition, the costliest and most consequential step, is produced *before* the bounded session begins, so the cap bounds the critique but not the decomposing. A seat-authored wave 1 puts the decomposition inside the bound, which is what EV-11's goal ("the decomposition session ends within a fixed round cap") actually names.

### Blind spots

- **Owner's slice (authoring/aggregation mechanics).** Sees the facilitator first pass as a cheap, neutral bootstrap into a mechanical concordance, and sees that parallel-generate collapses on incompatible slicings (correct). What it cannot see from inside that slice: the facilitator is the *one* role `council.md` forbids from deciding, so "facilitator drafts a proposal" is the same charter violation it just used to kill parallel-generate — applied to the role with no charter at all. Its amendment 3 ("proposal, not constraint") is a mitigation for a structure that shouldn't exist; the completeness charter in that amendment is correct and I adopt it, but it patches anchoring, not authorship.
- **Designer's slice (gate/render).** Its round-2 text was written against my round-1 ("attack a facilitator first pass") and never saw my round-2 retraction of facilitator authorship. Its false-signifier argument is right and it cuts *against* the facilitator first pass harder than it knows: a `Contributors:` line names seats, but under owner's structure the actual author of every first-pass goal sentence is the facilitator — a role that can never be named. The human would read `Contributors: principal` and believe the seat wrote a sentence the facilitator wrote. That is the false signifier designer's own principle condemns, one step upstream of the PO-on-a-goal case it flagged. Its P-rev-3 is correct for *child* goals; it does not reach the *epic* goal, which is a what-statement PO is chartered to author.
- **My own round-2 slice (named honestly).** I wrote "product-owner authors the epic goal **and the what-vs-how on each child**." That second half is exactly the false signifier designer flags — PO deciding what-vs-how per child is designing the solution. I retract it and tighten the partition: PO authors the epic goal *only*; its contact with children is wave-3 ruling (ratify/amend state, rule disputes), never child-goal authorship.

### Reframe

The first decomposition artifact is two artifacts with two authors, each the seat whose output format *is* that artifact:

- **`principal` authors the child decomposition** — slicing, child goals, states, provisional surface flags. Its `Reframe` output is literally a decomposition, and its role is "read across the seams" and "propose a reframe when the card earns one." A feature decomposed into an epic + children is a reframe of the feature into its seams. This is the one seat chartered to propose the frame.
- **`product-owner` authors the epic goal** — "what the whole feature delivers." PO's charter is "decide what the product should be when no test can decide it"; the epic goal is a what-statement, and PO deciding it is a `Ruling`, not solution design.

Wave 1 dispatches these two **in parallel, no facilitator draft, no cross-seat input**. Wave 2 dispatches `skeptic` + `designer` in parallel against that seat-authored artifact, each in its native format, chartered on **completeness as well as elements** (owner's amendment 3's correct half: a missing child is attackable; a wholesale rejection of the slicing is a named disagreement, not a patch request). Wave 3 dispatches `product-owner` **last, unconditionally**, ruling-only — ratify/amend the epic goal and each child's `state`, rule each dispute it may rule with dissent named, escalate per its `<escalation>` (owner's amendment 1, which I accept: it removes my round-2 "conditional" wave 3 and fixes the clean-run-skips-PO failure deterministically). The facilitator writes nothing; it aggregates verbatim, labeled by seat, and carries every unruled disagreement into the step-3 presentation. Attribution/disagreement live at the gate and in `runs/`, never in the card file (already converged).

This is the only structure where (a) the goal's letter holds — no facilitator decomposition, first or final; (b) every author has a charter and an output contract; (c) the 3-round cap covers the decomposition itself, not just the post-hoc critique; and (d) the `Contributors:` line is honest — the author of every sentence is a named seat whose charter permits that sentence.

### Testable claims

1. **Facilitator-charter probe (settles authorship).** Assert `council.md`'s facilitator block contains "decides nothing" and the verb set "route / fan out / count rounds / write the board"; assert no `facilitator.md` exists under `council/agents/`. Then the rewritten `features-new.md` step 2 must contain **no instruction for the facilitator to draft a goal, state, or surface sentence** — grep for any such instruction; presence = red. Under owner's structure this fails; under mine it passes.
2. **Authorship provenance (smoke).** Wave-1 `council_dispatch` calls are exactly `{principal, product-owner}`; no wave-1 input contains a decomposition authored outside a seat dispatch. `principal`'s job transcript contains a `Reframe`-shaped child decomposition; `product-owner`'s contains a `Ruling`-shaped epic goal. Facilitator-authored decomposition in any wave-1 input = red.
3. **All-four-dispatched (owner's amendment 1, adopted).** Scratch-run `runs/<runId>/*.json` manifests contain exactly one `done` job per seat in `{principal, product-owner, designer, skeptic}` — PO unconditional last, every run.
4. **Contributors honesty (designer P-rev-3, tightened).** In the presented draft, no `Contributors:` line names `product-owner` on a child goal or `principal` on the epic goal; PO appears on the epic goal only, principal on child decomposition only, designer on surface flags only. A PO-on-child-goal or principal-on-epic-goal line = red.
5. **Round-cap coverage (EV-11 seam).** The decomposition is produced in a *counted* wave-1 round, so CAP-1's 3 rounds bound the decomposition, not merely the critique of a round-0 artifact. The rewrite's step 2 text must contain no pre-session decomposition step that CAP-1 does not count.

**Single question, answered precisely for the consolidator:** the first decomposition artifact is authored by **`principal` (child decomposition) and `product-owner` (epic goal), in parallel, wave 1** — no facilitator draft of any kind. The facilitator authors nothing, at any step; it routes and aggregates verbatim only.

#### Round 3 — designer (job-1.9)

VERBATIM:

## What I revise, what I hold

I revise. The key new fact for my decision: principal's wave-1 has product-owner *authoring* the epic goal in parallel with principal — not just ruling last, as my round-2 position assumed. That introduces a self-review problem my round-2 position did not face.

I hold the rest: charter-fit on skeptic and designer (they are adversaries, not generators); the gate-presentation-only location for P1/P2/P3 (`Contributors`, `Disagreements`, `Decision: unresolved — your call`); rejection of facilitator-first-pass.

## Engagement — owner

I accept owner's amendment 3 (the completeness charter — attacking seats must attack *what's missing* as well as elements) as a real refinement and require it in the procedure text. I reject facilitator-first-pass for the same reason principal does: EV-10's `goal` says "instead of the facilitator decomposing alone." The completeness charter bounds the anchoring risk but does not remove it; the artifact already has structure that may not survive scrutiny. The fix is to remove the pre-decision, not bound the attack on it. The human's intake stays — as the source of the product statement (transcribed into the epic goal field by principal), not as a decomposition.

## Engagement — principal

I accept principal's claim 1 (seat-format collision) as the test that settles skeptic and designer as adversaries. I accept the rejection of P4 — both owner and principal are right, and `validate.py`'s blindness to `Intent` prose plus the downstream-invariance test settle it. P1/P2/P3 stand at the gate presentation and the `runs/` transcript.

I refine principal's wave 1 from "principal + product-owner author in parallel" to "**principal alone authors the first decomposition artifact**." Product-owner is the ruler, not the author of the artifact it rules on. The self-review loop in principal's structure collapses a vantage: the human at the gate sees "product-owner ruled" but cannot see product-owner in doubt, because product-owner authored the epic goal as a fait accompli in wave 1 and then ratified it in wave 3. The Gulf of Evaluation widens on the goal. Splitting authoring from ruling keeps both vantages distinct.

## Final position — the single question

**The first decomposition artifact is authored by principal alone, dispatched once in wave 1.** The artifact is:

- **Epic goal**: a one-line product statement drafted by principal as a transcription of the human's intake feature description.
- **Children**: principal's cross-seam decomposition — children list with per-child `scope`, `goal`, `state` (proposed `Backlog` or `Ready`), `surface` flag.
- **Per-child what-vs-how**: ruled by product-owner in wave 3, not authored in wave 1.

Then:

- **Wave 2**: `skeptic` and `designer` attack the artifact in parallel, each with identical `input` (principal's first pass), each in its own `<output_format>` (skeptic: Objections/Verdict with runnable checks; designer: Design position with surface flags and Gulf of Evaluation concerns). Independence preserved.
- **Wave 3**: `product-owner` rules, dispatched unconditionally with the amended draft + the disagreement ledger (per owner's amendment 1), ruling-only — ratifies or amends the epic goal and each child's `state`, rules each dispute it may rule (dissent named per `Ruling/Options rejected/Grounding/Reversibility`), escalates what it may not rule per `<escalation>`.
- **Aggregation**: mechanical concordance over the four seat outputs in their native formats, verbatim, attributed.
- **Gate presentation**: principal's draft + `Contributors:` line + `Disagreements:` block (verbatim-or-faithful-restatement, labeled by seat) + `Decision: unresolved — your call` markers. The human ratifies or amends at the step-3 gate; their approval is the resolution.

## Convergence

I converge with principal on everything except wave 1's second author. I converge with owner on the completeness charter, all-four-dispatched, byte-identity, and the gate-presentation-only location for attribution. I diverge from owner on facilitator-first-pass and from principal on product-owner-as-co-author. The single open question the consolidator carries: **wave 1 = principal alone, or principal + product-owner?**

### Output Format

- **Design position** — Principal alone authors the first decomposition artifact (wave 1); product-owner rules last (wave 3) on the artifact it did not author; skeptic and designer attack in wave 2 in their native formats. This refines principal's wave 1 to remove the self-review loop that product-owner-as-co-author would create, and removes owner's facilitator-first-pass to comply with EV-10's goal. Both refinements are charter-fit improvements grounded in the seat bodies — principal: "Propose a reframe only when the card earns one"; product-owner: "decide — do not design the solution"; skeptic: objection standing only with falsifiable test; designer: position or critique, not authorship.
- **Gulf closed** — Gulf of Evaluation, for the human at the step-3 gate. The human sees (a) their own intake translated into a one-line epic goal field by principal — distinguishing what they authored from what the seats ruled; (b) children with scope/goal/state/surface they can scan and amend; (c) contributors, disagreements, and decision markers in the gate presentation. The human can tell what they decided (intake), what the seats decided (rule/ratify), and what's contested (ledger). Gulf of Execution is closed for completeness via owner's amendment 3 (charter the seats to attack what's missing).
- **Principle and evidence** —
  - *Signifier — the epic goal field*. A one-line product statement is a signifier of the product decision. When product-owner authors it in wave 1 and rules on it in wave 3, the signifier collapses — the human cannot tell whether product-owner was ever in doubt. Distinguish authoring from ruling by seat. Evidence: `principal.md` ("Propose a reframe only when the card earns one" — Reframe *is* a decomposition when the card earns one); `product-owner.md` ("decide — do not design the solution" — a decomposition *is* the solution at card level, exactly what product-owner must not author).
  - *Conceptual model — authoring vs. ruling as distinct vantages*. The four-seat model is four vantages. A vantage that proposes and ratifies its own proposal is one vantage, not two. Evidence: SEATS-1 escalation reaches the CEO through the orchestrator; the four seat bodies' `<output_format>` blocks commit each to a non-overlapping role (principal: Cross-seam/Reframe; product-owner: Ruling/Options rejected; skeptic: Objections/Verdict; designer: Design position/Principle and evidence).
  - *Constraint — the completeness charter*. Attacking seats must attack *what's missing*, not just *what's there*; a wholesale rejection of principal's slicing is a named disagreement, not a patch request. Evidence: owner's amendment 3, which I require in the procedure text.
  - *Knowledge in the world — the gate presentation, not the card*. Attribution and disagreement are surface-grade (gate), not durable (card). Evidence: `council.md` step 1 (surface test reads `Intent`), step 10 (judge sees goal alone), `features-deliver.md` merge check criterion 5 (no `Needs Human` after approval), `validate.py` blindness to `Intent` prose. All four argue for P1/P2/P3 at the gate presentation only — never in the card file.
- **Falsifiable predictions** —
  1. *Self-review falsifier (settles my refinement of principal)*. Two scratch runs on the same feature. Run A: principal's structure (principal + product-owner in wave 1). Run B: my refinement (principal alone in wave 1). Assert: in Run A, the epic goal field is byte-identical between wave-1 and wave-3 (no amendment — the loop). In Run B, wave-3 product-owner produces a non-trivial amendment or a named dissent on the epic goal. My position predicts Run B's non-trivial amendment; principal's predicts Run A's byte-identity.
  2. *Charter-fit (settles principal's claim 1)*. Dispatch each of the four seats with "decompose $FEATURE into children" + a mandated children-shape. Assert skeptic emits Objections/Verdict, designer emits Design position, product-owner emits Ruling — none emits a children-list. If true, parallel-generate-with-mandated-shape is incoherent, and principal-alone wave 1 is the only coherent generator structure.
  3. *Independence preserved*. The two attack dispatches carry identical `input` (principal's first pass); no input contains another seat's critique. Asserted by inspecting `runs/<runId>/*.json` manifests — each attack job's `input` equals principal's wave-1 output byte-for-byte.
  4. *All-four-dispatched (per owner's claim 3)*. Scratch-run manifests contain exactly one `done` job per seat in {product-owner, designer, principal, skeptic}.
  5. *Step-3 byte-identity (per P5)*. Fixture string equal to today's step-3 block; `expect(shippedBlock).toEqual(fixture)` — green now, red the moment any byte moves.
  6. *Card-content hygiene (settles P4 against me; owner + principal are right)*. After the scratch-run approval path, every written `council/cards/<id>.md` contains no seat-attribution or disagreement text in frontmatter or `Intent`, and `python3 council/validate.py` on the scratch copy prints `All council artifacts valid`.
  7. *Downstream invariance (settles P4's cost)*. Surface-touching classification of a card's `Intent` (council.md step 1's mechanism) is unchanged by the presence or absence of a `Disagreements:` block.
- **Preferences, ranked last** —
  - *Cost*. Principal alone = 2 dispatches (waves 1 + 3); principal's structure = 3 dispatches (waves 1 + 3). I prefer fewer. Cost grounds this — every dispatch is model spend — not aesthetic.
  - *Human intake as source*. I prefer the human's intake feature description to be the source of the epic goal field (transcribed by principal) rather than authored by a seat. The grounding is the Gulf of Evaluation: the human can distinguish their own authorship from the seats' deliberation. Without that grounding, the preference would be lower-ranked.

### Step 3 — exchange closed at 3 rounds (cap)

Converged: no facilitator authorship; generate-then-attack structure; wave 2 = skeptic + designer attack in parallel, native formats, completeness charter; wave 3 = product-owner last, unconditional, ruling-only; attribution/disagreements at gate presentation + run transcript, never card files; `Contributors:`/`Disagreements:`/`Decision: unresolved — your call` blocks; byte-identity + validate green; dispatch discipline with job ids on record.

Open judgment carried to step 5/6: **who authors the first decomposition artifact — `principal` alone (owner + designer) or `principal` + `product-owner` (principal)?**

### Step 4 — skeptic attacks (job-1.10)

VERBATIM:

### Objection 1: The step-3 byte-identity pin does not guard the semantic contract between step 2's attribution and step 3's "exactly as written"

**Claim targeted:** Designer's P5 / owner's claim 1 / principal's claim 3 — that byte-identity of step 3's prose text is a sufficient regression guard for the gate's contract.

**Settling test (can run NOW):** Read the exact text of step 3. Extract "Present the full draft of the epic and every child — complete frontmatter and `Intent` section, **exactly as each would be written to disk**" as a fixture. Then read the converged design's requirement: attribution blocks (`Contributors:`, `Disagreements:`, `Decision: unresolved — your call`) go in the step-3 presentation **but never in the on-disk card file** (owner's claim 5, principal's claim 3, designer's P-rev-2 — all three converge on this).

**Analysis:** If the presentation includes attribution blocks that will NOT be written to disk, the statement "exactly as each would be written to disk" is **false for those blocks**. The human sees card text with attribution, approves it, and the facilitator writes a DIFFERENT text (without attribution). This is not "exactly as would be written." A newline, a space, a word that appears in the gate presentation but not in the final file — any of those breaks the contract that text literally says.

The byte-identity pin only checks that the procedural *instruction* paragraph is unchanged. It does not check whether the *output* of that instruction (the presented card) matches what gets written. The deliberation aggregated byte-identity and semantic contract as one thing. They are two things.

**Status:** `open-untested` — the settling test is: the rewritten procedure's step-3 block (byte-identical) is read by a parser that extracts what "the full draft" means. If the rewrite's step 2 defines card format as including attribution blocks, but step 3 says "exactly as would be written to disk" and attribution is NOT written, the semantic contract is broken even though step 3's prose is byte-identical. The determination requires the actual rewritten text.

### Objection 2: The completeness charter is incompatible with skeptic's body

**Claim targeted:** All three seats converged on "attacking seats must attack completeness as well as elements" (owner's amendment 3, adopted by principal and designer). The charter applies to skeptic in wave 2.

**Settling test:** Read skeptic's own `<how_an_objection_counts>` block: "An objection has standing only when it is falsifiable: it must name the specific test or observation that would settle it, stated in a form that can actually be run **— a test file and assertion, a typecheck that should fail, an import run and an expected count, a request and an expected response** — not a description of one."

Now map "this decomposition is missing a child about X" onto that list. There is no test file to run. No typecheck that should fail. No import or expected count. No request or expected response. The decomposition is a markdown text, not an executable pipeline. A "missing child" objection is inherently observational — it's an argument about what the feature *should* contain — which is exactly the class of objection skeptic's body explicitly excludes: "not a description of one."

**Can test NOW (closed-green):** Read skeptic's body — confirmed, `<how_an_objection_counts>` lists only runnable-test-form objections. No category covers "this artifact has an omission." `closed-green` — the body does forbid non-falsifiable complaints.

**Can test NOW (closed-red):** There exists no mechanism for skeptic to file a standing objection about a missing decomposition child under its own charter rules. The completeness charter demands output that skeptic's body forbids. **This is a defect in the converged design.** The correct prediction: the scratch-copy smoke run's skeptic transcript will either (a) fail to address completeness (charter violated), or (b) produce an objection with no runnable settling test (own body violated). No third option.

**Status:** `open-untested` — requires the smoke run to observe skeptic's wave-2 output on a real feature to confirm either (a) or (b).

### Objection 3: Principal-alone authorship of the epic goal is charter-inconsistent (settles the open judgment against owner+designer)

**Claim targeted:** The open judgment question — who authors the first decomposition artifact, principal alone or principal+PO. Under the owner+designer position (principal alone), principal authors the epic goal.

**Settling test:** Read principal's `<deliberation_mode>` block: "Propose a reframe only when the card earns one." Read principal's `<output_format>`: Cross-seam reading / Blind spots / Reframe / Testable claims. Principal's output format contains no field for an epic goal. Principal's role is "read across the seams — data → API → serving → frontend... and say what no single-vantage position is positioned to see." The epic goal "names what the whole feature delivers" (features-new.md step 1) — that is a product statement, not a seam-cut.

Now read product-owner's charter: "You are the seat that decides **what the product should be** when no test can decide it." "You decide — you do not design the solution." The epic goal is a "what" statement, the exact thing PO is chartered to decide.

My prediction: Dispatch principal with a feature description and "produce the first decomposition artifact including the epic goal." Principal's output will be in its `<output_format>` — Cross-seam/Blind spots/Reframe/Testable claims — and the epic goal will either be absent (no output for it) or smuggled into the Reframe block (where it doesn't belong). Under principal+PO (principal's own position), PO authors the epic goal, which is charter-consistent: PO's "Ruling" format IS the right format for "the what."

**Can test NOW (closed-green):** Principal's body confirmed — output format has no epic-goal field. PO's body confirmed — "decide what the product should be" maps directly to epic goal authorship. `closed-green`: the claim that principal-alone-elic-goal violates principal's charter is supported by the seat body.

**Status:** `open-untested` — requires dispatch evidence from the smoke run to confirm what principal produces when asked for an epic goal.

### Objection 4: The self-review problem on the epic goal (settles the open judgment against principal+PO)

**Claim targeted:** Under principal's structure (principal+PO wave 1), PO authors the epic goal in wave 1, then rules on it in wave 3. Designer's self-review falsifier (designer's prediction 1, round 3) is correct.

**Settling test:** Two scratch runs on the same feature. Run A: principal's structure (principal+PO). Run B: owner+designer's structure (principal alone). Hypothesis for Run A: PO's wave-1 epic goal and PO's wave-3 ruling on that same goal are byte-identical (no amendment = self-review loop). Hypothesis for Run B: PO's wave-3 ruling on principal's proposed epic goal shows a non-trivial amendment or dissent.

My prediction: Run A shows byte-identity (PO does not change its own product statement) — confirming the self-review loop. Run B shows amendment or dissent — confirming that separation of authorship from ruling is productive.

**Status:** `open-untested` — requires two scratch runs on the same feature with different dispatch structures, which the smoke harness does not currently design for. The existing SMOKE-1 runs one /features-new against a scratch copy — it does not parameterize by authorship structure. This objection requires the harness to be extended before it can be settled.

### Objection 5: The facilitator-charter probe passes

**Claim targeted:** Principal's claim 1 (facilitator-charter probe) — that council.md's facilitator block says "decides nothing" and no facilitator.md exists under council/agents/. This was tested to confirm that the rewritten procedure must contain no instruction for the facilitator to draft a goal, state, or surface sentence.

**Settling test (ran NOW, closed-green):** grep council.md line 8 for "The facilitator decides nothing" — confirmed. Test for `council/agents/facilitator.md` — confirmed absent.

**Status:** `closed-green` — the charter constraint exists. The rewritten procedure text must be checked against it separately (cannot run until rewrite exists).

### What I ran

```
# validate.py
$ python3 council/validate.py
All council artifacts valid

# facilitator charter probe
$ grep -n "decides nothing" council/procedures/council.md
8: **The facilitator decides nothing.**

$ test -f council/agents/facilitator.md && echo "EXISTS" || echo "NO facilitator.md exists"
NO facilitator.md exists

# product-owner charter probe
$ grep -n "does not design the solution" council/agents/product-owner.md
13: — you do not design the solution

# skeptic falsifiability charter
$ grep -n "falsifiable" council/agents/skeptic.md
58: An objection has standing only when it is falsifiable

# principal's output format has no epic-goal field
$ grep -n "Reframe\|Cross-seam\|Testable claims" council/agents/principal.md
92: - **Reframe** — the reframe, stated concretely

# seat tool grants confirmed — no hub/task/spawns
$ grep -n "tools:" council/agents/*.md | grep -E "(product-owner|principal|designer|skeptic)"
product-owner.md:5:tools: Read, Grep, Glob, Write
principal.md:5:tools: Read, Grep, Glob
designer.md:5:tools: Read, Grep, Glob, Write
skeptic.md:5:tools: Read, Grep, Glob, Bash

$ grep -n "spawns:" council/agents/*.md   # empty — none have it

# existing gates
$ bun test    # 272 pass, 2 skip, 0 fail
$ bunx tsc --noEmit   # (clean, no output)

# step-3 byte-identity fixture extraction
$ python3 -c "
text = open('council/procedures/features-new.md').read()
import re
m = re.search(r'^## 3\. Draft-then-confirm.*?^(?=## 4\.)', text, re.MULTILINE | re.DOTALL)
block = m.group(0) if m else 'NOT FOUND'
print(f'Step-3 block length: {len(block)} bytes')
# key sentence for the semantic-contract objection
print('exactly as each would be written to disk' in text)
"
Step-3 block length: 583 bytes
True
```

### Verdict

**blocks** — Two objections name concrete defects the deliberated design has not closed:

1. **Objection 2 (completeness charter / skeptic incompatibility)** — open-untested. The converged design added a requirement (attack completeness) that skeptic's own charter forbids (objections must have runnable settling tests; a missing child has no settling test). This is not a matter of "the rewrite will handle it" — the charter is in skeptic.md, not in the procedure. Either skeptic.md must be rewritten to allow non-falsifiable completeness objections (touches a seat body, which EV-10's scope does not cover), or the completeness charter must be removed from the converged design. Neither path was explored in the deliberation.

2. **Objection 1 (step-3 semantic contract)** — open-untested. The byte-identity pin checks the prose but not the contract it describes. The existing text says the presented card is "exactly as each would be written to disk." Attribution blocks in the presentation but not on disk breaks that contract. The rewrite must either (a) modify step 3's language (losing byte-identity), (b) accept that attribution blocks go on disk (losing P4 convergence), or (c) accept that the gate presentation is NOT "exactly as written" (changing the gate's meaning without changing its prose — the byte-pin misses it). This trilemma was not addressed.

Both block the card until resolved. The principal-alone vs principal+PO judgment (objections 3 and 4) is contingent on those two defects being closed first — arguing about who authors the epic goal is premature when the mechanism to present it without writing it is unaddressed.

### Step 5 — synthesis (job-1.11)

VERBATIM:

## EV-10 consolidator synthesis

Source read in full: `council/cards/EV-10.md` (card face, Intent, Acceptance, SEATS-1/SMOKE-1, rounds 1–3 verbatim for owner/principal/designer, step-3 closing note, step-4 skeptic report O1–O5). I did not re-read anything beyond this card; the wiki grounding the seats cite was not re-opened (this turn only synthesizes the record as written).

### Settled design (every seat converged; or a Skeptic test closed it)

- **No facilitator authorship, at any step.** All three seats converged in round 3 against the facilitator-first-pass; the card's `goal` letter ("instead of the facilitator decomposing alone") and `council.md`'s "The facilitator decides nothing" both forbid it. Skeptic **O5 (facilitator-charter probe)** closed this green — `grep` confirmed `council.md:8 "**The facilitator decides nothing.**"` and `test -f council/agents/facilitator.md` returned absent.
- **Generate-then-attack structure** (owner conceded parallel-generate in round 2; principal retracted its round-1 facilitator first pass in round 2; designer rejected facilitator-first-pass in round 3).
- **Wave 2 = skeptic + designer attack in parallel**, identical input (principal's first-pass artifact), each in its own `<output_format>`, independence preserved (`council.md` step 2 isolation rule).
- **Wave 3 = product-owner last, unconditional, ruling-only** (owner amendment 1, adopted by principal and designer) — ratify/amend epic goal and each child's `state`, rule disputes with dissent named, escalate per `<escalation>` to the human per SEATS-1.
- **Skeptic and designer are adversaries, not generators.** Principal's claim 1 (seat-format collision) — skeptic's body requires runnable falsifiable objections, designer's requires an artifact to critique; a mandated children-shape collides with each seat's own `<output_format>`. Designer adopted this; owner conceded parallel-generate on this basis.
- **Attribution and disagreement ledger live at the gate presentation and the `runs/` transcript, never in card files** (P4 rejected: owner claims 5/6, principal claim 3, designer P-rev-2 converged). Grounding: `council.md` step 1 reads `Intent` for the surface test; step 10 gives the judge the goal alone; `features-deliver` merge check criterion 5; `validate.py` is blind to `Intent` prose.
- **`Contributors:` / `Disagreements:` / `Decision: unresolved — your call` blocks at the gate** (designer P1/P2/P3 adopted by all three).
- **Step-3 byte-identity + `validate.py` green** as acceptance criteria (designer P5, owner claim 1, principal claim 3).
- **Dispatch discipline with job ids on record** per SMOKE-1 (scratch copy of `council/`, manifests under `runs/<runId>/*.json`).
- **All four seats dispatched every run** (deterministic SEATS-1 compliance + the card's own "draft naming them" acceptance).
- **Completeness charter** (owner amendment 3, adopted): attacking seats attack what's missing as well as elements; a wholesale rejection of the slicing is a named disagreement, not a patch request. *(Note: this converged item is itself the target of open objection O2 below.)*

### Settled disputes (test + result)

- **Facilitator may not author the decomposition.** Settled by Skeptic **O5**, run now, `closed-green`: `grep` and `test -f` confirmed the charter constraint and the absent seat body. (The constraint exists; the *rewrite* must still be checked against it — that check is folded into the open objections below.)
- **Principal's output format has no epic-goal field; PO's charter maps to epic-goal authorship.** Settled by Skeptic **O3 (closed-green portion)**: `grep` of `principal.md` (Reframe/Cross-seam/Testable claims, no goal field) and `product-owner.md` ("does not design the solution"; "decide what the product should be"). This *supports* principal's position on the open judgment but does **not** settle it — the smoke dispatch evidence is still outstanding (O3's open-untested portion).

### Open judgment — for `product-owner`, escalating to `steward`

**Q: Who authors the first decomposition artifact — `principal` alone (owner + designer) or `principal` + `product-owner` in parallel wave 1 (principal)?**

- **Side A — `principal` alone (owner round 3 + designer round 3).** Grounding: (1) self-review loop — if PO authors the epic goal in wave 1 and rules on it in wave 3, the human at the gate cannot see PO "in doubt"; designer's self-review falsifier predicts wave-1 and wave-3 epic goals are byte-identical under PO-as-author. (2) PO's `<role>`: "you do not design the solution" — a decomposition is the solution at card level. (3) `council.md` step 2's generators are owner/principal/designer; PO appears only at step 6 to rule — the council's established pattern is PO-rules-not-generates. (4) Cost: fewer dispatches.
- **Side B — `principal` + `product-owner` in parallel wave 1 (principal round 3).** Grounding: (1) EV-10 `goal` letter requires seat-authored, not facilitator-authored — but PO is a seat, so PO authoring the epic goal satisfies the letter. (2) PO's charter "decide what the product should be when no test can decide it" maps directly to the epic goal (a what-statement); PO's `Ruling` format is the right format for it. (3) Principal's `<output_format>` has no epic-goal field (Skeptic O3 closed-green) — principal-alone forces principal to author outside its format. (4) Contributors honesty — the author of every sentence is a named seat whose charter permits that sentence; under principal-alone, the epic goal's author is unclear. (5) Round-cap coverage (EV-11 CAP-1): the decomposition must be produced inside a counted round, not a round-0.

The Skeptic's O3 (closed-green) leans toward Side B; O4 (open-untested) leans toward Side A. Neither settles it. The Skeptic itself ruled this judgment **contingent** on O1/O2 being closed first, so it cannot be routed until those objections clear.

### Open objections (Skeptic; settling test not passed)

- **O1 — step-3 semantic contract ("exactly as each would be written to disk" vs presentation-only attribution).** Status `open-untested`. Settling test: read the rewritten step-3 block (byte-identical per the pin) and determine whether attribution blocks in the presentation but not on disk makes "exactly as each would be written to disk" false. Requires the actual rewrite + the SMOKE-1 scratch smoke run (facilitator holds the dispatch tools; smoke evidence = job ids from a scratch copy of `council/` in a temp dir). Trilemma the deliberation did not address: (a) modify step-3 language → loses byte-identity; (b) write attribution to disk → loses P4 convergence; (c) accept presentation ≠ "exactly as written" → the byte-pin misses the semantic break. **Skeptic verdict: blocks.**
- **O2 — completeness charter incompatible with skeptic's falsifiability charter.** Status `open-untested`. Settling test: the scratch-copy smoke run observing skeptic's wave-2 output on a real feature — predict either (a) skeptic fails to address completeness (completeness charter violated) or (b) skeptic files an objection with no runnable settling test (skeptic's own `<how_an_objection_counts>` violated). Partial closed-green (skeptic body confirmed to forbid non-falsifiable complaints) and partial closed-red (no mechanism exists for skeptic to file a standing completeness objection under its own charter) — but the smoke observation is outstanding. Requires SMOKE-1 scratch run. **Skeptic verdict: blocks.** Neither path (rewrite skeptic.md — outside EV-10 scope — nor drop the completeness charter) was explored in the deliberation.
- **O3 — principal-alone epic-goal authorship is charter-inconsistent.** Status `open-untested` (closed-green on the body reading; open on the dispatch evidence). Settling test: smoke dispatch of principal with "produce the first decomposition artifact including the epic goal" — predict the epic goal is absent from principal's `<output_format>` or smuggled into Reframe. Supports Side B of the open judgment but does not settle it. **Skeptic verdict: does not block** (contingent on O1/O2).
- **O4 — self-review loop under principal+PO wave 1.** Status `open-untested`. Settling test: two scratch runs on the same feature — Run A (principal+PO) vs Run B (principal alone) — asserting wave-1 vs wave-3 epic-goal byte-identity. The existing SMOKE-1 harness does **not** parameterize by authorship structure, so this requires a harness extension before it can run. Supports Side A of the open judgment but does not settle it. **Skeptic verdict: does not block** (contingent on O1/O2).

### Ready to hand off?

**No.** Two open objections block:

1. **O1** (step-3 semantic contract) — open-untested; settling test requires the rewrite + the SMOKE-1 scratch smoke run (facilitator-executed).
2. **O2** (completeness charter vs skeptic falsifiability) — open-untested; settling test requires the SMOKE-1 scratch smoke run (facilitator-executed).

The **open judgment** (principal-alone vs principal+PO authorship) is routed to `product-owner`, escalating to `steward`, but the Skeptic ruled it **contingent on O1/O2 closing first** — so it cannot be product-owner's input yet. The immediate next step is the facilitator executing the rewrite and the SMOKE-1 scratch smoke run so O1 and O2 (and the open-untested portions of O3/O4) can be tested; the open judgment routes only after those objections clear.

### Step 6 — routing (council-runner)

- **Open judgment (wave-1 author: `principal` alone vs `principal` + `product-owner`)** — no test settles it (O3 closed-green leans one way, O4 open-untested leans the other; the seats split 2–1). Not covered by any Phase 1 ruling (SEATS-1 names the participating seats, not the authorship structure). Routed to `product-owner` per council.md step 6 — the facilitator never dispatches ruling seats, so this is escalated to the orchestrator per the escalation contract.
- **O1 (step-3 semantic contract)** — open-untested; settling test runs against the rewrite + SMOKE-1 smoke at step 9. Carries into the spec as a requirement: the gate presentation must present card text exactly as written plus a clearly-separate, never-written ledger surface (the design's answer to the trilemma, consistent with the byte-identity acceptance).
- **O2 (completeness charter vs skeptic falsifiability)** — open-untested; settling test runs at step 9. Carries into the spec as a requirement: the completeness charter is scoped per-seat — skeptic attacks completeness only in falsifiable form (e.g. a goal satisfiable by a stub), principal and designer carry the observational missing-child arguments in their native formats.
- **O3/O4** — open-untested portions verified at step 9 against the smoke evidence.

VERBATIM:

## Skeptic seat — EV-10 deliberation attack

**Position:** The converged design has three structural vulnerabilities the deliberation did not test. I raise five objections. One I can close-green now (the facilitator-charter probe). Four are open-untested, awaiting the smoke run or the rewrite text to settle.

### Steps 7–8 — spec write, owner implementation, PR (council-runner, 2026)

- Spec committed: `docs/superpowers/specs/2026-09-03-EV-10-design.md` (9722421) — records the settled design, the step-6 ruling, and carries O1/O2 as binding spec requirements.
- Card → `In Progress` (9b256bc); handoff to `owner`.
- Owner implemented in worktree `.worktrees/feat-ev10-council-decomposition`: rewrote `council/procedures/features-new.md` steps 1–2 (1dd51f7) — waves 1–3, aggregation contract, two-part gate presentation, dispatch discipline; added the step-3 byte-identity pin to `test/prose.test.ts` (7414fbb); plan doc (2460659).
- **PR #10 open** on `feat/ev10-council-decomposition` (base `main`); `gates` workflow **SUCCESS** on head `2460659`.
- Card → `In Review` per step 8's observed-artifact rule (branch + open PR) — committed to main (387e7ef). Note for the orchestrator's merge: the branch itself carries `state: In Progress` (merge-base 9b256bc; the In Review commit lives on main only) — the merge must not silently regress the card state.
- Branch gates (facilitator-observed, in the branch worktree): `bunx tsc --noEmit` clean; `bun test` **273 pass / 2 skip (integration gated off) / 0 fail**, including `features-new step 3 is byte-identical to the settled draft-then-confirm block`; `python3 council/validate.py` → `All council artifacts valid`.

### SMOKE-1 acceptance smoke run (council-runner, 2026)

Direct headless parent-mode run per SMOKE-1: fresh scratch dir `/tmp/ev10-smoke-QYDMAS` carrying the branch's `council/` + `vault/`, a seed git commit, `.pi/settings.json` with only the two dependency pins (superpowers + rpiv-ask-user-question; no pi-council project pin — package resolves at user scope), the installed deps copied in, and the **rewritten `features-new.md` placed at `<scratch>/.pi/council/procedures/features-new.md`** (the repo-local override path — the user-scope package still ships the old procedure). Invoked `pi -p "/features-new Add a /board-summary slash command that prints each board column with its card count"` with `COUNCIL_SEAT`/`COUNCIL_RUN_ID`/`COUNCIL_JOB_ID`/`PI_SESSION_FILE` unset (parent mode → full dispatchable set including `product-owner`, which child mode inheriting the runner's `spawns` would refuse — the failure mode of the prior attempt).

All three waves ran; **job ids on record** from `<scratch>/.pi/council/runs/2026-09-03T12-59-55-867Z-4105499-dqi2cq/`:

- **job-1 principal** — done, exit 0 (wave 1; authored the decomposition artifact incl. the epic goal as a one-line transcription of the intake — O3 smoke evidence)
- **job-2 skeptic** — done, exit 0 (wave 2; attacked goals/states in **falsifiable form only** — stub-satisfiability, unfalsifiable output format, Ready-vs-Backlog bar; no observational missing-child objections — the O2 per-seat scoping held)
- **job-3 designer** — done, exit 0 (wave 2; surface flags + `Intent` arguments in native format)
- **job-4 product-owner** — done, exit 0 (wave 3; ruled last, ruling-only — ratified/amended child `state` with dissent named (Ready → Backlog on EV-13/EV-14 against principal), escalated 3 items to the human per SEATS-1, in `Ruling`/`Options rejected`/`Grounding`/`Reversibility` format; it never authored a goal sentence — O4 smoke evidence: no self-review, since PO never authored the epic goal)

The presented draft (11.8 KB gate presentation) ran the two-part split per the O1 answer: **Part 1 "Card drafts, exactly as they would be written"** (EPIC-4 + EV-13 + EV-14 + FLLWUP-5, complete frontmatter + Intent) and **Part 2 "Ledger surface (presented, never written)"** with per-card `Contributors:` lines naming principal/skeptic/designer/product-owner and a `Disagreements:` block — 4 named-seat disputes, each carrying `Decision: unresolved — your call`. The draft named the contributing seats and carried unresolved disagreements per the card's `goal`.

**Nothing was written to disk:** the scratch `council/cards/` gained no EPIC-4/EV-13/EV-14/FLLWUP-5 files, the scratch board is unchanged, and the real `council/board.md` is untouched (real-repo `git status` clean).

**Timing note:** the run needs ~22 min (waves ~19 min + parent aggregation/presentation ~3 min). The first attempt was killed by a 20-min cap mid-aggregation (all four seats done, parent killed before presenting); the re-run completed after the cap was lifted once wave 3 was visibly dispatched — the run was progressing, not hung. Smoke evidence: job manifests + the presented draft (recorded in the run ledger; scratch dirs cleaned up after capture).

### Step 9 — skeptic verification, cycle 1 (council-runner, 2026)

Dispatched `skeptic` (job-5.1) at PR #10 head 2460659 with the card, the spec, the O1–O5 settling tests, and the SMOKE-1 evidence. Verdict: **blocks** on one `closed-red` item; the rest green.

- **Gates (all green, skeptic-run in the branch worktree):** `bunx tsc --noEmit` clean; `bun test` 273 pass / 2 skip / 0 fail incl. the byte-identity pin; `python3 council/validate.py` → `All council artifacts valid`; `gh pr checks 10` → `gates` workflow SUCCESS. The byte-identity pin was cross-checked against the pre-rewrite text (583 bytes, sha256 `9ec525dbb1e8d9fa`) and a one-byte mutation flips it — non-vacuous.
- **O1 (step-3 semantic contract): `closed-red`** — the byte-pin and the procedure's two-part separation language hold, and the smoke draft is structurally two-part, but the smoke run's **Part 1 card drafts embed attribution and deliberation narrative in Intent prose that would be written to disk**: e.g. EV-13 Intent `(Designer, wave 2; …)`, `Why state is Backlog (product-owner, wave 3) — …`, `principal argued Ready; overruled …`; EV-14 Intent `Evidence shape (skeptic, wave 2) — …`; FLLWUP-5 Intent `principal flagged the drift in wave 1 …; the product-owner ruled (wave 3) …`; EPIC-4 Intent `Scope ruling (product-owner, wave 3) — …`. This violates the procedure's own mandate ("never in card files") and the step-6 ruling. The separation is stated but not honored in content — the exact blur O1 exists to prevent.
- **O2 `closed-green`** — wave-2 skeptic attacked completeness in falsifiable form only (4 objections, all falsifiable: stub-satisfiability x2, open-untested registration, closed-green correction); no observational missing-child objection. The designer carried the observational arguments. Wave-2 inputs identical; independence preserved.
- **O3 `closed-green`** — principal's wave-1 output contained the epic goal; the presented EPIC-4 goal matches verbatim.
- **O4 `closed-green`** — product-owner's wave-3 ruling amended/overruled principal's positions with named dissent (EV-13/EV-14 Ready → Backlog; three ledger rulings) and never ruled on its own authorship — no self-review loop.
- **O5 `closed-green`** — the rewritten step-2 text contains no facilitator drafting instruction ("You route, wait, aggregate verbatim, and author nothing"; "You author nothing at any step").

**Block handed to the owner:** the presented Part 1 card drafts must be attribution-free — no `(Designer, wave 2)`, `(skeptic, wave 2)`, `(product-owner, wave 3)`, `principal argued Ready; overruled`, `principal flagged the drift in wave 1`, or similar seat/wave/deliberation narrative in the card text destined for disk. Attribution belongs solely in the Part 2 ledger. The fix the skeptic named: the procedure text needs a stronger guard / explicit mandate that Part 1 card drafts carry no seat names, wave numbers, or deliberation narrative. Per council.md step 9, the card returns to `In Progress` and the red item is handed to the owner.
