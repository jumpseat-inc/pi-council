---
id: EV-26
title: Pure model-name filter over the thinking-level cross-product
state: Done
owner: owner
epic: EPIC-6
goal: `filterModelRows` maps the model cross-product rows and a query string to only the rows whose `qualifiedId` contains the query case-insensitively, preserving each thinking-level suffix and returning the unchanged row objects, with an empty result for no match.
---

## Intent

The search input in the `/council-models` model selection modal (EV-27)
renders from a filtered row set; this card extracts the filter itself as a
pure unit mirroring EV-22's resolver so the match contract is testable in
isolation. The contract is pinned: match field is `qualifiedId` only (the
field the modal actually renders — display `name` is never rendered, and
matching an invisible field would make rows vanish under a query that
matches nothing on screen); match algorithm is case-insensitive substring
(the intake's word is "filter"); the filter runs on the model cross-product
rows before the `:level` suffix is applied at render time, so a query like
`:off` never matches a suffix; and the surviving rows are returned as the
identical `PickRow` references so `resolveSelection()` keeps emitting
byte-verbatim keys. An empty query returns all rows; a query matching
nothing returns an empty array — the modal owns the no-match copy (EV-27).

Not user-visible — pure function, no rendering.

## Acceptance

- Unit tests drive `filterModelRows` with `rowsForProvider`-shaped
  cross-product rows: case-insensitive substring matches on `qualifiedId`,
  non-matches excluded, empty query returns all rows, no match returns
  empty array.
- A query containing `:` (e.g. `:off`) does not match thinking-level
  suffixes.
- The returned rows are the identical `PickRow` objects (reference
  equality), not copies.

## Deliberation

### Step 1 gate
Mechanical, not surface-touching (pure function, no rendering — the
no-match copy is EPIC-6 R-1 on the EV-27 face, explicitly out of this
card's scope; a query matching nothing here returns an empty array and
nothing more). No deliberation runs; steps 2–6 are skipped by the
mechanical path. The card's own Intent/goal is the owner's handoff
(council.md step 7 mechanical path) — every design decision is already
settled on the face: match field is `qualifiedId` only; case-insensitive
substring matching; the filter runs on `rowsForProvider`-shaped
cross-product rows before the `:level` suffix is applied at render time,
so a query containing `:` never matches a suffix; empty query returns all
rows; no match returns an empty array; surviving rows are returned as the
identical `PickRow` references.

### Step 7 — In Progress
Handed the card itself to the single owner (`owner`) per the mechanical
path — no spec file. Owner works in an isolated worktree, pushes a
feature branch, opens a PR against `main`.

### Step 8 — In Review
PR #23 open (base `main`, head `feat/ev-26-model-filter` @
`297a8d9b960e0698306a17764d2660f9caacc87f`) — observed via `gh pr
view 23`. Owner reports all four gates green at that head (typecheck,
whole suite 511 pass / 2 pre-existing env skips, validate clean,
frozen-lockfile install).

### Step 9 — verified (cycle 1 of ≤3)
Skeptic dispatched at the branch head (job-8.1). All four gates green at
`297a8d9` in the branch worktree: `bun install --frozen-lockfile` exit 0;
`bunx tsc --noEmit` exit 0; whole suite `511 pass, 2 skip, 0 fail` (2350
expect() calls, 54 files); `python3 council/validate.py` → "All council
artifacts valid". Gate integrity demonstrated: a `query: string → number`
injection turned tsc red (exit 2, 40 errors); a match-on-`name`-
instead-of-`qualifiedId` injection turned the test gate red. Draft
adversarial probe (`test/skeptic-ev26-adversarial.test.ts`, untracked,
17 tests): 15 green, 2 failing — both adjudicated probe-authoring
errors, closed-green ("model" matches only the 4 prov-a rows, not
prov-b/duplicate-id; "One"/"Two" are substrings of the qualifiedIds), no
implementation defect. Skeptic's own 20-test adversarial suite green
(prefix/infix/suffix, numerics, punctuation, `:` queries, whitespace,
level-suffix substrings, reference identity, no mutation, purity). No
blocking objection, no open objection.

### Step 10 — judge PASS
Judge dispatched with the card's `goal` and the Skeptic's step-9
evidence only (job-8.2). Verdict **PASS**: independently re-ran the whole
suite (511 pass / 0 fail) and inspected the implementation, confirming
every clause of the goal (qualifiedId-only match field, case-insensitive
substring, cross-product rows preserved with their `level` suffixes,
unchanged row references, empty array on no match).

### Step 11 — deterministic merge check (features-deliver substitution)
All five criteria met, read fresh against head
`297a8d9b960e0698306a17764d2660f9caacc87f`: (1) owner gates green in
full (Skeptic-run, above); (2) `gates` workflow SUCCESS on the PR head
(`gh pr checks 23` → `[{"name":"gates","state":"SUCCESS",
"workflow":"gates"}]`); (3) no blocking Skeptic objection; (4) judge
PASS; (5) no Needs Human / outstanding ruling. Merged with
`gh pr merge 23 --squash --match-head-commit 297a8d9…` → PR #23 MERGED,
merge commit `b89a93b518b7e72b1ec1cf04a7a7309f9a2cbd17` on `main`.
Gates workflow re-ran on the merged SHA: run 33946186979 → SUCCESS.

### Step 12 — Done
Local `main` fast-forwarded to `b89a93b` (clean ff from `origin/main`).
Board and card set Done; validation clean; reconciliation committed and
pushed.
