---
id: EV-73
title: Gate enablement lives in `.council.json`; the pin stays in code
state: Deliberating
owner: null
epic: EPIC-14
goal: A new loadGateConfig(repoRoot) resolves the decisions gate's enablement solely from .council.json's top-level gate section ({"mode":"off"|"advisory"|"active"}), a repo with no gate section resolves mode "off" byte-identically to today's packaged default, all three mode readers (gate-tool.ts, gate-route-tool.ts, gate-route.ts) resolve through loadGateConfig, mode is removed from policy.json's accepted keys so an existing policy.json carrying it fails loud, and loadGateDecision refuses thresholds.verify 0, an unknown floors/thresholds sub-key, any override.question id absent from weights, and any basis-affecting string whose content can render a multi-line basis, each with a single-line FAIL: naming the file and key.
---

## Intent

Enablement (`mode`) belongs in the committed config file the repo already has, as
a reserved top-level sibling like `theme`/`retry`; the `typesafe/jev-1.13` model
pin and the decisions endpoint stay hard constants enforced in `gate-run.ts`,
never user config. The loader's one-line `FAIL:` is the visible surface on invalid
config.

## Acceptance

- off/advisory/active each resolve from a fixture `.council.json`; absent `gate`
  ⇒ byte-identical `off`.
- The pin still throws in `gate-run.ts`; a non-pinned model cannot reach the
  transport.
- The four refusal classes each throw one line naming file+key — including the
  newline-bearing override id (the restored FLLWUP-74 class).
- Packaged `council/gate/decision.json` validates clean; no `policyVersion` bump
  and no re-registration (no threshold moved).
## Run record (features-deliver, EPIC-14 card 1/5)

- Step 1 — recorded execution mode: **Deliberate** (ROOT manifest job-1, run
  `2026-09-20T08-59-16-719Z-259999-vjf6un`, mode decided from the ledger per
  EV-68; full-vs-mechanical judgment not made). Surface-touching bit: **true**
  — new operator config surface (`.council.json` `gate` section) plus new
  operator-facing refusal `FAIL:` copy; `designer` seated as third generator.
- Phase 1 rulings applied and binding (EPIC-14 card face): R1 (orchestrator
  merges; card left `In Review` with PR open), R2 (record commits ride this
  branch), R3 (card 1/5 of 5), R4 (FLLWUP-74 folded into this card — the four
  refusal classes), R5 (operator copy says "gate", never "jev").
- Binding card-context rulings (EPIC-14 wave-3 PO ruling, job-33):
  `loadGateConfig(repoRoot)` is the ONE resolver of enablement from
  `.council.json`'s reserved top-level `gate` section; all three runtime mode
  readers resolve through it; `mode` removed from `policy.json`'s accepted
  keys; the pin + endpoint stay hard code constants in `gate-run.ts`; no
  `policyVersion` bump, no re-registration; EV-64's budget rule re-expressed
  against the RESOLVED mode with the off short-circuit ordering preserved.
