---
id: EV-73
title: Gate enablement lives in `.council.json`; the pin stays in code
state: Done
owner: null
epic: EPIC-14
goal: A new loadGateConfig(repoRoot) resolves the decisions gate's enablement solely from .council.json's top-level gate section ({"mode":"off"|"advisory"|"active"}), a repo with no gate section resolves mode "off" byte-identically to today's packaged default, all three mode readers (gate-tool.ts, gate-route-tool.ts, gate-route.ts) resolve through loadGateConfig, mode is removed from policy.json's accepted keys so an existing policy.json carrying it fails loud, and loadGateDecision refuses thresholds.verify 0, an unknown floors/thresholds sub-key, any override.question id absent from weights, and any decision.json string that decide() interpolates into a basis line — overrides[i].question, overrides[i].option, overrides[i].basis — whose content can render a multi-line basis, each with a single-line FAIL: naming the file and key.
---

## Intent

Enablement (`mode`) belongs in the committed config file the repo already has, as
a reserved top-level sibling like `theme`/`retry`; the `typesafe/jev-1.13` model
pin and the decisions endpoint stay hard constants enforced in `gate-run.ts`,
never user config. The loader's one-line `FAIL:` is the visible surface on invalid
config.

## Acceptance

- off/advisory/active each resolve from a fixture `.council.json`; absent `gate`
  ⇒ byte-identical `off`.
- The pin still throws in `gate-run.ts`; a non-pinned model cannot reach the
  transport.
- The four refusal classes each throw one line naming file+key — including the
  newline-bearing override id (the restored FLLWUP-74 class).
- Packaged `council/gate/decision.json` validates clean; no `policyVersion` bump
  and no re-registration (no threshold moved).
## Run record (features-deliver, EPIC-14 card 1/5)

- Step 1 — recorded execution mode: **Deliberate** (ROOT manifest job-1, run
  `2026-09-20T08-59-16-719Z-259999-vjf6un`, mode decided from the ledger per
  EV-68; full-vs-mechanical judgment not made). Surface-touching bit: **true**
  — new operator config surface (`.council.json` `gate` section) plus new
  operator-facing refusal `FAIL:` copy; `designer` seated as third generator.
- Phase 1 rulings applied and binding (EPIC-14 card face): R1 (orchestrator
  merges; card left `In Review` with PR open), R2 (record commits ride this
  branch), R3 (card 1/5 of 5), R4 (FLLWUP-74 folded into this card — the four
  refusal classes), R5 (operator copy says "gate", never "jev").
- Binding card-context rulings (EPIC-14 wave-3 PO ruling, job-33):
  `loadGateConfig(repoRoot)` is the ONE resolver of enablement from
  `.council.json`'s reserved top-level `gate` section; all three runtime mode
  readers resolve through it; `mode` removed from `policy.json`'s accepted
  keys; the pin + endpoint stay hard code constants in `gate-run.ts`; no
  `policyVersion` bump, no re-registration; EV-64's budget rule re-expressed
  against the RESOLVED mode with the off short-circuit ordering preserved.

## Round 1 — independent first pass (owner, principal, designer)

### Owner (job-1.1)

loadGateConfig lands in extensions/gate.ts as the single resolver of the reserved top-level `gate` section; loadGatePolicy composes it internally (calls loadGateConfig first, then validates the policy file), so all three readers — and the fourth call site in gate-route-tool.ts's recheck — keep their exact current shape and get mode through the one resolver with no second mode copy and no reader edits.

(a) loadGateConfig(repoRoot): { mode: GateMode }, importing COUNCIL_CONFIG_FILE from seats.ts (gate.ts already imports PKG_ROOT from seats.ts — no new dependency direction). Posture mirrors loadRetryConfig's structural spine (seats.ts:268–283) with gate's FAIL: line for section content: file absent → { mode: "off" }; absent `gate` key → { mode: "off" }; malformed JSON or non-object root → FAIL: <file> has an invalid JSON — not parseable as JSON: … (single line via gateFail's sanitizer; the FAIL: shape rather than seats.ts's bare malformed-JSON convention because the card's intent names the one-line FAIL: as the loader's visible surface); `gate` present but not an object → FAIL: <file> has an invalid gate — expected an object, found …; unknown key inside `gate` → FAIL: <file> has an invalid gate.<key> — unknown key; expected one of mode — …; `gate.mode` not in GATE_MODES → FAIL naming gate.mode; `gate: {}` (absent mode) → resolves off. All messages single-line via gateFail.

(b) Composition, not reader surgery. loadGatePolicy becomes: (1) loadGateConfig first — mode resolved BEFORE the policy file is read (ruling 6's ordering; every reader's off short-circuit sits after its existing loadGatePolicy call, which now resolves mode from .council.json internally); (2) read + validate policy.json exactly as today minus `mode`: POLICY_KEYS drops "mode" → a policy.json carrying `mode` hits the existing unknown-key FAIL (the migration signal, ruling 3), firing regardless of resolved mode — a stale policy file is loudly dead even on the off path; (3) EV-64 clause 2 re-expressed: `if (gateStateBudgetTokens === undefined && config.mode !== "off") throw` — same message, "resolved mode" now meaning the config-resolved mode; (4) return { policyVersion, mode: config.mode, model, endpoint, gateStateBudgetTokens? }. GatePolicy keeps `mode: GateMode`; runGate's off guard and pin guards (gate-run.ts:139–151) untouched; readers change only in comments. Byte-identical off: no file + no section → config off, packaged policy validates clean → same no-op shapes. Considered having the three readers call loadGateConfig directly with a separate loadGatePolicyData — rejected: four call sites re-composing two loaders is how a second mode copy appears.

(c) The four refusal classes, all in loadGateDecision, each a single-line gateFail naming file+key: (1) thresholds.verify requires > 0 (direct keeps ≥ 0 plus the verify ≤ direct cross-check); (2) unknown floors/thresholds sub-key refused (today extras are silently ignored — the hole FLLWUP-74 closes); (3) override.question absent from weights refused (weights parsed before overrides, so no reordering); (4) a basisSafeString check (non-empty, no \r/\n — input refused, not sanitized) on every string that can reach a basis line: overrides.<i>.question, .option, .basis, and the weights keys (basis template `${id}: confidence …`). Order within an override rule: shape checks (class 4) before the referential check (class 3). Packaged decision.json validates clean under all four. No policyVersion bump, no threshold moved.

(d) Budget rule: packaged policy (off resolved from absent config, budget present) validates; repo with gate advisory + budget-less repo-local policy.json FAILs clause-2; off-mode omission legal; present-⇒-validated-unconditionally unchanged and before any short-circuit.

(e) Packaged policy.json loses its "mode" line; pristine repo = loadGateConfig (absent ⇒ off) + packaged file validating clean. Existing repos with mode-carrying repo-local policy.json: every loadGatePolicy call throws the unknown-key FAIL naming mode, including on the off path — intended migration signal; no deprecation shim. Scaffold gains nothing (council/scaffold/.council.json unchanged; gate/policy.json is data-class, never written).

(f) Test plan (TDD, mkdtempSync roots): test/gate.test.ts heaviest (packaged-default asserts off via absent config; loadGateConfig block — absent file/section ⇒ off, three modes resolve, invalid mode FAILs, unknown gate sub-key FAILs, gate:{} ⇒ off, malformed JSON single-line; policy.json with mode FAILs unknown-key with the allowed-key list updated; budget clause-2 tests set mode via .council.json fixture; four refusal classes incl. the newline-bearing override id and weights-key variants; packaged decision.json clean). test/gate-tool.test.ts (off no-op unchanged; advisory reachable purely from .council.json). test/gate-route.test.ts (step-1 short-circuit driven by .council.json). test/gate-run.test.ts (mode originates from config; off/pin guards unchanged).

(g) Open questions: (1) class-4 scope on weights keys — inclusive (they render into basis) vs only the three override fields; leans inclusive. (2) gateFail tail wording for .council.json errors ("remove the key to use the packaged default" is literally true — removing `gate` ⇒ off — but "packaged default" reads oddly about .council.json). (3) Off-path stale-policy FAIL visibility — composition means a repo with config off AND a stale mode-carrying policy.json throws on every gate read; believes it's intended migration friction, wants PO confirmation. Tradeoffs: two file reads per loadGatePolicy call (same I/O order as today, no-cache posture preserved).

Testable claims: (1) fixture repo, no .council.json, packaged tree → loadGatePolicy returns off, gate-tool no-op byte-identical to current. (2) repo-local policy.json with "mode": "off" throws the exact unknown-key FAIL even with config off. (3) gate advisory + budget-less policy.json throws clause-2; flipping config to off makes the identical policy.json load clean. (4) each refusal class throws exactly one line matching ^FAIL: .* has an invalid .+ — .+$ with no embedded newline.

### Principal (job-1.2)

Cross-seam reading. Producers: loadGatePolicy (only producer of GatePolicy; reads mode from policy.json, uses it for the EV-64 budget-absence check and returns it) and resolveGateStateBudget (gate-state.ts ~L110–150) — a SECOND, type-import-only read of the same policy.json, reading only gateStateBudgetTokens, NOT reading mode, NOT rejecting unknown keys. Readers of policy.mode: (1) gate-tool.ts R3 short-circuit; (2) gate-route.ts resolveRoute step 1; (3) gate-route-tool.ts recheck (passes policy to runGate); (4) gate-run.ts's pre-POST off guard — a fourth runtime read the card's "three readers" framing does not name. The transitively-correct design: loadGatePolicy calls loadGateConfig once and injects the resolved mode into the returned object's `mode` field — the three readers keep their policy.mode checks, gate-run.ts keeps reading the same resolved value (no fourth direct caller), the EV-64 budget check can be re-expressed inside loadGatePolicy. Alternative — strip mode from GatePolicy, three direct callers — strictly worse: forces gate-run.ts to become a fourth direct reader, triples the .council.json parse sites, opens a mid-call divergence window, breaks the pinned GatePolicy test fixtures (test/gate-run.test.ts:41, test/gate-route.test.ts:437). Contract sentence: GatePolicy.mode survives as the resolved field; loadGatePolicy populates it from loadGateConfig; the three readers and gate-run.ts's pre-POST guard all read that one resolved value — so "three readers" remains true by construction. Also state `gate: {}` ⇒ off and reject a bare-string `gate` shorthand (parity with theme/retry, ruling #1's object shape).

.council.json seam: loadCouncilConfig reads only parsed.council (seats.ts:478) — a top-level `gate` sibling is invisible to it by construction, like top-level retry (loadRetryConfig L267) and theme (L175). loadGateConfig follows the loadThemeConfig/loadRetryConfig precedent: independent loader, re-parses .council.json, fail-loud naming file+key, absent section ⇒ off. Blast radius: the readers call loadGatePolicy lazily at tool-call time (index.ts only prereads retry in try/catch, L606), so loadGateConfig must be invoked lazily from loadGatePolicy, never eagerly at parent init — a malformed gate section throws one FAIL at the gate/route tool call and never crashes unrelated session startup. Eagerly prereading "for symmetry" would let a typo'd gate section break every session — wrong contract; pin against it.

Migration seam: removing mode from ALLOWED_POLICY_KEYS means an existing repo-local .pi/council/gate/policy.json carrying mode hits the unknown-key FAIL — correct, loud; silently ignoring would leave the repo believing it is opted in while resolving off. Qualifications: (i) the generic tail "set a valid value or remove the key to use the packaged default" — for `mode` the accurate advice is "move it to .council.json"; the ruling only blesses the generic FAIL as the signal, so a specialized copy is a residual, not an objection; (ii) the packaged policy.json must drop mode in the same change, or the package itself fails; validate.py's check_gate_registrations reads policyVersion only — no edit, no registrations.jsonl churn.

Where the four refusals belong: all four in loadGateDecision, gate.ts, adjacent to decide() — the anti-drift choice (renderer's field set and validator's field set side by side); at the loader, not gate-run/gate-transport, so bad data fails before any call. Class (1) closes the exact gap gate-run.ts's header documents as out of scope (verify=0 breaks failed-call re-derivation). Class (2) needs explicit sub-key allowlists on floors (extras silently ignored today) and thresholds. Class (3) is within-decision.json. Class (4) is a single-line \r\n refusal on the fields decide() interpolates into basis: override.question, override.option, override.basis (template `${rule.question}? ${rule.option} (${rule.basis})`).

Blind spots: in-code view can't see gate-run.ts as a fourth policy.mode consumer nor the on-disk consumer migration (repo whose committed policy.json carries mode gets a FAIL after upgrade; any ledger already written under the old opt-in). Card/product view can't see the duplicate lenient policy.json reader in gate-state.ts, nor that gate-state.ts also short-circuits on the reader before ever seeing an off-mode policy. gate-render.ts loads zero policy (ledger-only) — structurally blind to this seam, correctly. Config-author view can't tell gate shorthand/mid-session-flip consequences vs theme/retry.

Testable claims: (1) transitive-equality — loadGatePolicy(repo).mode === loadGateConfig(repo).mode for gate.mode ∈ {absent, off, advisory, active}; absent ⇒ off. (2) single resolution site — no module outside extensions/gate.ts reads .council.json's `gate` key (import/grep canary mirroring gate-route.test.ts's T1 import fence). (3) off short-circuit ordering under the new resolver — budget-less repo-local policy.json + .council.json off loads clean and no-ops; same policy with .council.json active throws one FAIL naming policy.json and gateStateBudgetTokens. (4) migration FAIL — policy.json containing mode throws single-line FAIL naming policy.json and mode; packaged policy.json has no mode key and still resolves off. (5) refusal/renderer coupling — for every decision policy loadGateDecision accepts, firing each override yields a basis containing no \n/\r; each defect class throws exactly one FAIL naming file and key. (6) malformed gate is gate-scoped — session_start completes, throws only at the gate tool call.

Open questions: (a) snapshot vs lazy — loadRetryConfig read once at init and injected; loadGatePolicy lazy at every call; a mid-run .council.json edit flips routing and the merge-check input (run-config-stability hazard). Name the intended behavior. (b) cross-file override invariant (class 3 sufficiency) — override.question ∈ weights is necessary but not sufficient: gate-run.ts filters answers to knownIds from questions.json (~L230), so an override whose id is in weights but absent from questions.json can never fire; the noulProbabilityOf-vs-questions check in gate-run.ts is the precedent; residual beyond the four ruled classes. (c) scaffold visibility — /council-init seeds theme and retry as concrete data but the card seeds no gate section; wiki-only discoverability vs seeding "gate": {"mode": "off"}. (d) wiki drift — vault/wiki/metered-deliberation-routing.md still says consumers opt in via a repo-local policy.json; EV-64/65/66/69 specs and plans encode mode:"off" inside policy.json; sweep or spawn the sweep card.

### Designer (job-1.3)

Position: the {"gate": {"mode": …}} shape is the right home; the four refusal FAILs fit the existing single-line template. One load-bearing copy defect: ruling #3 binds the migration to "the existing unknown-key FAIL:", but that FAIL ends "set a valid value or remove the key to use the packaged default" — the packaged default for policy.json is now mode-less and resolves to off. An operator whose repo-local policy.json carries mode: "advisory" sees a FAIL that, if obeyed, silently demotes their gate from enabled to off, with no in-band signal that enablement moved to .council.json; "set a valid value" is also misleading — there is no valid value for mode in this file post-card. The same gateFail mechanism can carry a specialized `mode` line naming .council.json and gate.mode, keeping ruling #3's fail-loud single line while closing the migration gulf. Second, smaller: council/scaffold/.council.json should seed "gate": {"mode": "off"} mirroring retry/theme seeding, else the only visible form of the new key is a FAIL.

Gulf closed (evaluation, upgrade-day operator with mode: "advisory"/"active" in policy.json): obeying the generic FAIL silently disables the gate; invisible until an ungated card passes. The FAIL should carry the migration: e.g. FAIL: <file> has an invalid mode — gate enablement moved to <repoRoot>/.council.json > gate.mode — remove this key and set gate.mode in .council.json. Load-bearing claim: .council.json and gate.mode are named, not that the wording matches verbatim.

Principle/evidence: knowledge in the world / forcing function (anything the operator must remember is design debt; the FAIL is the one surface they're forced to read — gate.ts:125, test/gate.test.ts:84). Signifier: the unknown-key list is a perfect closed-form negative for a typo'd key and a misleading one for a key removed by design; the mode case is the one that needs a tailored line because the loader recognizes it by name. Conceptual model: policy.json post-EV-73 is the gate's tuning file; .council.json > gate.mode is enablement — the FAIL copy must honor the split.

Falsifiable predictions: P1 case sensitivity — {"gate":{"mode":"Active"}} → FAIL: <repoRoot>/.council.json has an invalid gate.mode — expected one of "off", "advisory", "active", found "Active" — … (dotted key form names both section and field). P2 migration copy broken (the load-bearing critique): existing unknown-key FAIL → obeying silently demotes; should read a line naming .council.json and gate.mode. P3 gateStateBudgetTokens absent under non-off resolved mode → existing test's message at test/gate.test.ts:212 with the mode source updated; falsifier: "resolved mode" sourced from loadGateConfig's output, never re-derived from policy.json. P4 gateStateBudgetTokens: 0 under off mode still FAILs positive-integer (off does NOT relax the present-value shape). P5 newline-bearing override id — the loader detects \n/\r BEFORE gateFail sanitization, else the sanitized message hides the cause; message names the field and states the one-line basis contract. P6 override question absent from weights → FAIL: … invalid overrides.0.question — question id "nope" is not declared in weights (expected one of a, b) — …. P7 unknown floors/thresholds sub-key → each parent enumerates its own allowed set (floors.bogus — unknown sub-key; expected one of choice, score); a unified list hides which parent is wrong. P8 fresh repo byte-identity (snapshot + end-to-end no-call smoke). P9 explicit gate.mode "off" vs absent section both resolve {mode:"off"} (JSON.stringify equality smoke); scaffold seeds "gate": {"mode": "off"}. P10 two-source redundancy ordering (loadGatePolicy's unknown-key FAIL vs loadGateConfig's clean enablement — which is the operator's first signal); current FAIL-on-unknown path is correct.

Preferences (ranked last): seed gate off in scaffold .council.json; keep existing template wording style for the refusal classes; keep the generic "set a valid value" half everywhere except specialize for `mode`; a tiny per-key specialization table over a generic helper.

Sharp critique: P2 — the migration copy must not say "remove the key to use the packaged default" for the `mode` key in policy.json; the packaged default has no mode and obeying silently demotes the gate.

## Round 2 — bounded exchange (owner job-1.4, principal job-1.5, designer job-1.6-hold + job-1.7)

### Owner (job-1.4)

1. Composition + contract pins — take both: `gate: {}` ⇒ off pinned as fixture; bare-string `gate` refused by the existing not-an-object FAIL, never coerced; grep/import canary accepted mirroring gate-route.test.ts's T1 fence (asserts the only module touching the `gate` key is extensions/gate.ts). Contract sentence recorded verbatim in the plan.
2. Specialized mode FAIL copy — accept the designer's P2 INTO EV-73 ("this is my position, not a ruling"): ruling 3 binds the mechanism (existing unknown-key path fires, fail-loud, single line, naming file+key), not the tail bytes; R5-safe. Proposed exact bytes inside the gateFail template: `FAIL: <file> has an invalid mode — unknown key; gate enablement moved to gate.mode in <repoRoot>/.council.json — remove this key and set gate.mode there`, fired only when the unknown key is exactly `mode` (one-key specialization table); every other unknown key keeps the generic tail. Load-bearing claim accepted: .council.json and gate.mode must be named, because obeying the generic tail silently demotes an enabled gate.
3. Class-4 weights keys — hold inclusive: a newline in a weights key renders a multi-line basis exactly like an override field does; excluding weights keys would falsify the renderer-coupling falsifier for that key class.
4. gate-state.ts blind spot — verified unreachable (resolveGateStateBudget runs only inside buildGateState, whose only callers sit after a loadGatePolicy call plus the off short-circuit on the same stack); follow-up hygiene note, not this card's business (an allowlist there duplicates the key list in a second module against no reachable failure).
5. Snapshot vs lazy — confirm lazy-at-call; documented consequence: a mid-run .council.json edit flips routing and the merge-check input on the next gate read; engine comment + wiki page will state it.
6. Scaffold seeding — hold, no scaffold change: absence-is-off is the feature; seeding makes "section absent" and "section set to off" look like two states when the acceptance says they're byte-identical; the FAIL (or EV-75's enriched line) plus the wiki sweep is the discoverability vehicle.
7. Withdrawals: none; round-1 open questions convert to settled positions (inclusive, accept-specialized-copy, migration friction intended).

### Principal (job-1.5)

Cross-seam reading: relocation surface confirmed (packaged policy.json MUST drop `mode` or the package itself fails — a gap the card's Acceptance doesn't name for policy.json, only decision.json); the three readers are already single-site (none parses .council.json directly; grep canary stands; readers need no edits — strongest evidence composition is the right seam). The lenient twin confirmed post-short-circuit only: resolveGateStateBudget never runs the unknown-key loop, but buildGateState is reached only after loadGatePolicy in all three readers (gate-route.ts:194→~205; gate-tool.ts:135–136; gate-route-tool.ts recheck ~105→~124) — the strict throw always wins.

CHAIN FACT that dissolves the item-2 dispute: council/cards/EV-75.md exists and owns exactly the specialized line — "policy.json carries a mode key FAILs loud naming that file, the key, and its .council.json gate.mode replacement" — and EV-75 is next in the chain (EPIC-14 R3). Item 2 is a card-boundary dispute, not a copy dispute: ruling 3's generic unknown-key FAIL is EV-73's signal; the enriched copy is EV-75's acceptance. The designer's line stays within ruling 3 but does not belong in this card; the silent-demotion gulf is real and EV-73's generic throw alone blocks it; EV-75 only improves the remedy. Neither seat needs to win.

One scope correction: EV-73 must drop `mode` from the PACKAGED policy.json in the same change (card Acceptance names only decision.json — a card gap, not a design gap). One added contract clause: loadGateConfig must reject unknown sub-keys inside the gate section, else {"gate":{"mod":"active"}} silently resolves off — the same silent-demotion gulf relocated to the new surface (loadThemeConfig already validates its section keys).

Item deltas: (4) not this card — follow-up scope: "eliminate gate-state.ts's duplicated policy parse by resolving the budget through the validated loader, or pin the load-order invariant with a test; no new runtime gate"; EV-73 may fold in the ordering assertion since the canary family already lives there. (4b) cross-file override invariant is a FIFTH class needing both files — follow-up card: "Every overrides[].question and weights/mechanical id must exist in the loaded questions.json (and a noul override option must name a criterion key); single-line FAIL naming both file and id; a rule that can never fire is refused." (5) no scaffold change — convention 6 doesn't settle it, but the Acceptance does: absent gate ⇒ off is the tested default; seeding makes fresh repos exercise "present, off" and skip the absent path; discoverability covered by /council-gate and EV-77. (6) lazy-at-call confirmed, needs a named hazard note (mid-run flip off→on starts gating dispatches mid-run; on→off silently stops gating — same class as run-config-stability; one line there and in the loadGateConfig docstring). (7) EV-73 sweep: vault/wiki/council-config.md (add gate sibling + table + scaffolding note), vault/wiki/index.md (council-config summary line), vault/wiki/run-config-stability.md (gate.mode hazard line), minimal correction to metered-deliberation-routing.md's "policy.json carries mode" bullet; EV-77 does the full rewrite; deterministic-merge-check.md is FLLWUP-73. Withdrawn: round-1 framing of the lenient read as an EV-73 risk (unreachable) and of the cross-file check as this card's; contract amended with gate-section unknown-sub-key rejection; if round 1 endorsed the specialized migration FAIL as EV-73's, withdrawn to EV-75.

Testable claims: gate-section shape ({gate:{}} ⇒ off; {"gate":{"mod":"active"}} ⇒ FAIL naming gate.mod; bare string rejected; absent ⇒ byte-identical off); single-resolution-site canary (zero .council.json/COUNCIL_CONFIG_FILE literals in the three reader modules, exactly one loadGatePolicy call each, loadGatePolicy precedes buildGateState in every body); reachability pin (policy.json with mode ⇒ loadGatePolicy's FAIL, never a silent pass out of resolveGateStateBudget); cross-file follow-up falsifier (override id in weights but absent from questions.json ⇒ today no throw and decide() never fires the rule).

### Designer (job-1.6 hold, then job-1.7 delta)

job-1.6 held its turn: the record was not at the path given (it lives on the worktree branch); it declined to fabricate positions it could not read and named the two conservative options. Facilitator corrected the path; job-1.7 delivered the delta:

P2 disposition: hold the load-bearing critique but ACCEPT the card-boundary reading — the enrichment belongs to EV-75 (its goal explicitly owns "naming .council.json gate.mode replacement"); the two cards are coupled: EV-73 ships the loud-generic signal (no longer containing correct advice, but fail-loud), EV-75 replaces the message with one that names .council.json. Will not pull the enriched line into EV-73. Gulf: EV-73 closes the silent-opt-in failure (operator now gets a FAIL instead of a silently-disabled gate); the remaining gap (obeying the FAIL still loses enablement, just loudly) is EV-75's.

P10 re-derived under the converged composition order: the upgrade-day operator (mode: "advisory" in policy.json, .council.json clean) sees exactly one FAIL — the policy.json unknown-key one; .council.json parses cleanly and never surfaces a FAIL on this path; the first signal is the generic FAIL with the misleading tail. Falsifier: smoke asserting file=policy.json, key=mode, tail still contains "set a valid value or remove the key"; plus the EV-75 test asserting the tail is later replaced by the .council.json > gate.mode pointer.

P5 basisSafeString bytes pinned: refuse the input, never sanitize through gateFail's newline flattener; exact bytes `FAIL: <file> has an invalid <key> — value contains a line break; basis lines must be single-line` with <key> the dotted path (overrides.0.question, weights.<id>); the template deliberately drops the "set a valid value or remove the key" tail (for a multi-line string there is no in-place valid value); falsifier regex `^FAIL: .+ has an invalid (overrides\.\d+\.(question|option|basis)|weights\..+) — value contains a line break; basis lines must be single-line$` plus a test confirming gateFail's sanitizer is never reached for these fields.

P9/scaffold seeding — concede (withdrawn). Withdrawals: P9 only; P1, P3, P4, P6, P7, P8, P10 hold (P10 re-derived; P2 holds with the EV-75 boundary). Preference: EV-73's generic FAIL is the migration signal, EV-75 specializes mode's message; keep the existing template wording style for the four refusal classes.

### Round-2 state (facilitator)

Stabilized by convergence: composition design (loadGatePolicy composes loadGateConfig; mode rides GatePolicy; readers untouched); gate-section validation shape incl. unknown-sub-key rejection and bare-string rejection; single-resolution-site canary; class-1..3 refusal designs; lazy-at-call with hazard note; no scaffold change; wiki sweep split (EV-73 minimal, EV-77 full). STILL OPEN (for consolidation): (a) the specialized mode-migration FAIL copy — owner wants it in EV-73 (exact bytes proposed), principal and designer route it to EV-75 by card boundary; a genuine card-scope dispute. (b) class-4 scope on weights keys — owner+designer inclusive vs principal's override-fields-only list; both agree it is cheap. (c) gate-state.ts lenient reader — unreachable, agreed follow-up. (d) cross-file override invariant — agreed follow-up card (fifth class).

## Step 4 — skeptic attack (job-1.8): objection ledger

Probes run (real, recorded in run substrate): /tmp/ev73-probe1.ts (A1 byte-identical off; A2 tail verbatim; A3 packaged decision.json clean under the four proposed classes; A3b verify=0 accepted by the current loader; A3c re-derivation disagreement — decide({}, verify:0) → Verify "composite 0.00 ≥ verify threshold 0.00" while a failed call records Deliberate; A4a weights-\n-key basis newline-FREE; A4b answer-id-\n basis CONTAINS newline; A5 loadGateQuestions accepts "q\n1" as a key today and runGate's knownIds filter passes it to decide()'s renderers), /tmp/ev73-probe2.ts (F1 RouteResult off shape; F2 stale-key throw before state build; F3 budget-less/mode-less clean+off; F4 clause-2 FAIL; F5 orderings — gate-tool 136<152, gate-route 194<202, recheck buildGateState@125 textually before its direct loadGatePolicy@136, precedence transitive via resolveRoute), /tmp/ev73-probe3.ts (P10a clean spine; P10b migration FAIL with mode dropped, tail literal). Greps: exactly 3 production loadGatePolicy sites, 0 .council.json literals in the three readers, no eager policy read at init (index.ts/child.ts register tools only), EV-75 goal text verified on disk. Existing gate suites: 131 pass / 0 fail.

- **O1 — closed-red.** Class-4 scope: the inclusive weights-keys justification is FALSE (A4a: weights keys are purely arithmetic in decide(); the floors loop iterates Object.keys(answers), not policy.weights — a newline-bearing weights key renders no newline into any basis), while the actually-renderable surface is missed: newline-bearing QUESTIONS.json ids render multi-line bases through decide() today (A4b/A5), and no designed class covers them. The converged "inclusive" position rests on a falsified mechanism; the acceptance's own "any basis-affecting string whose content can render a multi-line basis" clause reaches questions.json ids. Consolidation must either scope class 4 explicitly to decision.json strings (loadGateDecision can only refuse decision.json strings — the goal line scopes all four refusals there) or extend class 4 to questions.json ids (a loadGateQuestions change beyond the four ruled classes). NOT settled by this test — which reading is the card's is judgment.
- **O2 — closed-green.** thresholds.verify=0 gap is real and live today (A3b/A3c); class 1 closes a reachable defect; packaged verify=2.6 clears the proposed check; zero-answer re-derivation lands Deliberate under 2.6.
- **O3 — closed-green.** P10 migration-FAIL prediction holds verbatim (single line, tail literal "set a valid value or remove the key to use the packaged default"); EV-75's ownership of the enriched copy verified on disk (chain fact).
- **O4 — closed-green.** Off-path byte identity holds for the GatePolicy object, the tool no-op shape, and the RouteResult (caveat: the config-side loader does not exist yet; absent-section ⇒ off simulated via the current absent-mode→off path).
- **O5 — closed-green with one canary nuance.** Unknown-key policy throws before any state build on every production path; the literal "loadGatePolicy precedes buildGateState in every body" is false for the recheck body (transitive precedence via resolveRoute) — the canary test copy must use the transitive form.
- **O6 — closed-green.** Single-resolution-site canary and lazy-at-call hold on the current tree (0 .council.json literals in readers; exactly 3 loadGatePolicy sites; no eager init read). Post-card half untestable until the code lands.
- **O7 — closed-green.** Packaged decision.json validates clean under all four proposed classes (traced against the current loader + mutations).
- **O8 — open-untested by necessity** (post-card-only): gate:{} ⇒ off; bare-string gate refusal; unknown gate sub-key FAIL copy; P1 case-sensitivity copy; specialized-copy placement (a card-scope judgment, not a testable fact; EV-75's existence settles the chain); mid-session-flip hazard note bytes. Each has a named settling test to run once loadGateConfig lands.

Verdict: does not block the card's main lines; O1 is the one position recorded as converged on a falsified premise.

## Step 5 — consolidation (job-1.9): synthesis

**Agreed design (converged):** (1) composition, not reader surgery — loadGatePolicy composes loadGateConfig internally (calls it first, then validates policy.json); mode rides GatePolicy; the three readers + gate-run.ts's pre-POST guard read the one resolved value; no reader edits (O5/O6 closed-green). (2) loadGateConfig is the single resolver: absent file ⇒ off; absent `gate` ⇒ off; `gate:{}` ⇒ off; malformed JSON / non-object root ⇒ single-line FAIL; unknown sub-key inside `gate` ⇒ FAIL; bare-string `gate` refused, never coerced. (3) mode resolved from .council.json BEFORE policy.json is read; EV-64 budget check re-expressed against the resolved mode; off short-circuit ordering preserved (O5). (4) `mode` removed from policy.json's accepted keys — unknown-key FAIL is the loud migration signal; packaged policy.json drops its `mode` line in the same change (O3, O4). (5) single-resolution-site canary (O6). (6) lazy-at-call, documented consequence. (7) no scaffold change (designer withdrew P9). (8) wiki sweep split EV-73 minimal / EV-77 full. (9) refusal classes 1–3 in loadGateDecision, single-line gateFail naming file+key: verify > 0 (direct keeps ≥ 0 + cross-check); unknown floors/thresholds sub-key refused; override.question absent from weights refused (O2, O7). (10) class-4 basisSafeString refuses \r/\n inputs, never sanitizes; exact bytes pinned: `FAIL: <file> has an invalid <key> — value contains a line break; basis lines must be single-line`; applied to overrides.<i>.question/.option/.basis at minimum (O2, O7 for the packaged file). (11) packaged decision.json validates clean under all four proposed classes (O7).

**Settled disputes:** S1 verify=0 live today (O2). S2 migration-FAIL prediction verbatim (O3). S3 off-path byte identity, three forms (O4; config loader itself not yet built). S4 strict throw precedes state build on every path; canary copy must use the transitive form for recheck (O5). S5 single-resolution-site holds on current tree (O6). S6 packaged decision.json clean (O7). S7 specialized copy is EV-75's (chain fact verified on disk; owner accepted, designer accepted). S8 gate-state.ts lenient reader unreachable (O5 probes). S9 no scaffold change. S10 weights keys are NOT basis-affecting — the inclusive mechanism is falsified (O1 closed-red; decide() step 3 uses weights[id] as a numeric multiplier, never interpolated into basis). S11 verify>0 closes a live defect (O2).

**Open judgment (routing):** (a) specialized mode-migration FAIL copy placement — owner: into EV-73 with exact proposed bytes; principal+designer: EV-75 by card boundary (EV-75's goal owns the enriched line, verified on disk); EV-73's generic throw blocks the silent demotion either way. → product-owner. (b) class-4 scope reading — Position 1: scoped to decision.json strings inside loadGateDecision (the goal line scopes all four refusals there); questions.json newline-id surface (skeptic A5: loadGateQuestions accepts "q\n1" today; knownIds passes it to decide()'s renderers) filed as follow-up. Position 2: extend the newline refusal to loadGateQuestions ids (beyond the four ruled classes, second loader). Also: weights-key defensive inclusion now mechanism-false — dropped / kept-defensively / deferred. → product-owner; escalates to steward if Position 2 (fifth class, second loader). (c) packaged policy.json mode-drop gap in the Acceptance wording — SETTLED BY RULING, applied (facilitator, step 6, escalation-contract clause 1): the binding wave-3 PO ruling (job-33) already states "The PACKAGED council/gate/policy.json must also drop its mode key — the packaged default must still resolve off through loadGateConfig, and loadGateDecision/loadGatePolicy must validate clean." The owner implements the drop in this card; no ruling re-asked.

**Open objections:** O8 items open-untested by necessity (post-card-only, named settling tests); the consolidator notes `gate:{}` ⇒ off IS demanded by the Acceptance; unknown-gate-sub-key rejection was added by round-2 convergence and accepted but not Acceptance-named; the settling tests run once loadGateConfig lands (step 9's suite).

**Agreed follow-ups (step 13 feed):** (1) gate-state.ts lenient-reader hygiene (resolve budget through the validated loader, or pin the load-order invariant with a test; no new runtime gate). (2) cross-file override invariant — fifth refusal class (overrides[].question and weights/mechanical ids must exist in questions.json; noul override option must name a criterion key; single-line FAIL naming both file and id). (3) wiki sweep split (already carded: EV-77 full; EV-73 minimal corrections — council-config.md, index.md summary, run-config-stability.md hazard, metered-deliberation-routing.md mode bullet). (4) questions.json newline-id surface (from O1/A5, contingent on the (b) ruling).

## Step 6 — routing state (facilitator): ESCALATION to the ruling seat

Items (a) and (b) of the synthesis's open-judgment list route to product-owner. The facilitator's dispatch of product-owner was refused by the hub (ruling seats are dispatched by the orchestrator only). Card remains `Deliberating`; step 7 does not start until the ruling lands. Item (c) settled by applying the binding wave-3 ruling (job-33) — recorded above. Resumption: orchestrator dispatches product-owner with the escalation packet (recorded verbatim in the step-5 synthesis, items (a) and (b)), then re-dispatches this runner with the ruling verbatim.

## Step 6 — resumption: product-owner ruling (job-2) applied; routing closed

Ruling appended VERBATIM per the escalation contract (full text also at
`vault/raw/2026-09-21-po-ev73-step6-ruling.md` on `main`):

---

Item (a) — the enriched migration `FAIL:` ships on EV-75, not EV-73. EV-73 removes `mode` from `policy.json`'s accepted keys and lets the existing generic unknown-key `FAIL:` fire, verbatim, with no specialization for `mode`. The owner's proposed bytes are adopted as EV-75's candidate copy and recorded on EV-75's face at step 13, under R5's wording constraint (names the file, the key, and the `.council.json` `gate.mode` replacement; says "gate", never "jev"):
```
FAIL: <file> has an invalid mode — unknown key; gate enablement moved to gate.mode in <repoRoot>/.council.json — remove this key and set gate.mode there
```
Reasons binding this: the fold-in test fails (EV-73's goal names the mechanism and the loudness, never the copy; EV-75's goal names the copy itself); shipping it in EV-73 makes EV-75 untestable as written (its loud-migration assertion would pass vacuously on code it did not write); the two binding rulings do not conflict once each is read at its own scope (EPIC-14 R5 constrains the artifact/vocabulary, job-33 constrains EV-73's mechanism). Nothing about the failure is silent (the generic throw fires on every gate read, off path included), and the affected population in this repo is currently zero.

Item (b) — class 4 is a `loadGateDecision` refusal on the three `decision.json` strings `decide()` interpolates (`overrides[i].question`, `overrides[i].option`, `overrides[i].basis`), single-line, refusing input rather than sanitizing it. The `questions.json` newline-id surface goes to a follow-up card. `weights` keys are DROPPED from class 4 — not kept defensively (the skeptic's O1 closed-red falsification is confirmed: `decide()` uses `weights[id]` as a numeric multiplier, never interpolating the key into a basis line; a newline `weights` key matches no question and drags toward Deliberate, the safe side). The goal clause is amended so the judge isn't left holding the ambiguity. Apply EXACTLY this one-line edit to `goal:` (keeps `goal:` last in frontmatter and one physical line; run `python3 council/validate.py` clean after):
```
from: ... and any basis-affecting string whose content can render a multi-line basis, each with a single-line FAIL: naming the file and key.
to:   ... and any decision.json string that decide() interpolates into a basis line — overrides[i].question, overrides[i].option, overrides[i].basis — whose content can render a multi-line basis, each with a single-line FAIL: naming the file and key.
```

Step-13 follow-up feed, as ratified by this ruling:
1. `gate-state.ts` lenient-reader hygiene — ratified as filed (hygiene, not a gate).
2. Merge the synthesis's item (2) (cross-file invariant) with item (4) (`questions.json` newline-bearing ids) into ONE card. Goal sketch to draft against: *"Every gate question id is single-line and cross-referenced: `loadGateQuestions` refuses a question id containing a line break, `loadGateDecision` refuses any `overrides[].question` or `weights`/`mechanical` id absent from `questions.json` and a `noul` override option that names no criterion key, each with a single-line `FAIL:` naming both file and id, and a rule that can never fire is refused."* File it under `epic: EPIC-13` alongside FLLWUP-71 and name that pairing in its Intent.
3. Wiki sweep split (EV-73 minimal: the `metered-deliberation-routing.md` "policy.json carries `mode`" bullet; EV-77 full) — ratified as converged.
No new card is filed for the migration copy — it is EV-75.

---

Facilitator application of the ruling:

- Item (a) — applied. EV-73 ships the generic unknown-key `FAIL:` verbatim with
  no `mode` specialization; the enriched copy is EV-75's (candidate bytes
  recorded on EV-75's face at step 13 of this card). Cites job-2 item (a).
- Item (b) — applied. Class 4 scoped to the three `decision.json` strings
  `decide()` interpolates, refusing input rather than sanitizing; `weights`
  keys dropped from class 4; the `goal:` line amended with the exact edit
  above (one physical line, `goal:` last; `validate.py` clean). Cites job-2
  item (b).
- Routing closure: with (a) and (b) ruled and (c) settled by job-33 (applied
  above), the synthesis's open-judgment list is empty; the only open
  objections are O8's post-card-only settling tests, which have named tests
  running at step 9. Nothing routes to `Needs Human`. Step 7 begins.
- Step-9 recheck note (EV-69): the card's recorded execution mode on the ROOT
  manifest (job-1, run `2026-09-20T08-59-16-719Z-259999-vjf6un`) is
  **Deliberate** — a full route. The `council_route` op `recheck` re-routes
  only a recorded REDUCED route whose rebuilt hash changed
  (extensions/gate-route-tool.ts:99–130: "Returns the unchanged route
  (rechecked: false) unless a recorded REDUCED route's rebuilt hash …"); for a
  Deliberate-recorded card the re-check cannot re-route — the recorded
  decision stands. Applied mechanically; no dispatch needed.
- Step 7 — spec written from the settled record (nothing reopened):
  `docs/superpowers/specs/2026-09-20-EV-73-design.md`. State → `In Progress`.

## Step 9 — verification (skeptic): BLOCK at cycle 1, PASS at cycle 2 (cap 2/3 used)

**Cycle 1 — job-3.2, at head `9597c58d158a7ed508cac876987d8647368181c9`. Verdict: BLOCK.**
All four gates reproduced by the skeptic's own runs (preflight PASS; tsc exit 0;
`bun test` 1229 pass / 0 fail / 5 skip / 100 files; validate.py clean) and the
full O8 settling suite ran green — byte-identical off across all three forms,
transitive equality, migration-FAIL bytes verbatim with NO `mode`
specialization, class-4 pinned bytes on all three fields with weights keys
confirmed dropped, verify=0 refused, per-parent sub-key refusals, packaged
decision.json clean, budget clause-2 against the resolved mode, gate-scoped
failure, canary with the transitive recheck form, no reader/scaffold/pin/
version-bump edits. One `closed-red`: **the class-3 refusal leaked through
prototype-chain keys** — the new check `!((rule.question as string) in
weights)` (extensions/gate.ts:506) accepts `overrides[0].question =
"toString"` (probe: clean return where the goal requires a single-line
`FAIL:` naming `overrides.0.question`). Fix: own-key membership. The identical
`in` pattern on `mechanical` (~line 441) predates the card and is out of
scope.

**Fix cycle 1 — job-3.3, owner.** TDD: failing `toString` test added first
(RED confirmed: clean load), then the one-token fix
`weightIds.includes(rule.question as string)` (own-key membership), GREEN.
Pushed as the only delta on `bbcd07f9db570c68500edb31b49f0ca1e53a5f40` (one
hunk in extensions/gate.ts + one 25-line test in test/gate.test.ts); the
pre-existing `mechanical` `in` left untouched. Owner gates at that head:
preflight PASS, tsc 0, `bun test` 1230 / 0 / 5 skip, validate.py clean.

**Cycle 2 — job-3.4, at head `bbcd07f9db570c68500edb31b49f0ca1e53a5f40`. Verdict: PASS.**
Five settling runs, all `closed-green`: (1) toString/constructor/valueOf/
hasOwnProperty/__proto__ collisions now fail single-line naming
`overrides.0.question`, identical message shape to the ordinary absent-id
case; (2) the fix is own-key membership at exactly that site; (3) packaged
decision.json validates clean; (4) the pre-existing `mechanical` `in` is
byte-identical base↔head (residual noted for the orchestrator, not a block);
(5) the diff 9597c58..bbcd07f is only the fix + its test plus the card/board
state bookkeeping. Full gate set reproduced at head (preflight PASS, tsc 0,
`bun test` 1230 / 0 / 5 skip — the owner's claim reproduced exactly,
validate.py clean). Red-at-base evidence recorded per the red-base
convention: the transplanted test runs RED at base `9597c58` (`expected a
throw` — mechanism-absent class) and GREEN at head `bbcd07f`, same command
(`bun test test/gate.test.ts`), base worktree added detached and removed
after — the gate is capable of failing and the fix is what flips it.

Verify-cycle counter: initial skeptic (1) + one fix-and-reverify (2) of the
hard ≤3 cap. No objection remains `closed-red` or `open-untested`; the only
residual (pre-existing prototype-chain `in` on the `mechanical` record, ~line
441) predates this card and is drafted as a step-13 follow-up for the
orchestrator to confirm.

Step 8 observed artifact: PR #92 open against main (verified via `gh pr view`:
state OPEN, headRefOid matches head); card `In Review` from the moment the PR
was open, per step 8's sole-condition rule.

## Step 10 — judge (job-3.5): VERDICT PASS

Fresh-context judge, input = the amended `goal:` + the skeptic's step-9
evidence only (subject pinned: PR #92 head `db26231138bd3876b79ac7fe230fd0331e2fb1e3`,
worktree `.worktrees/ev-73`; frame: judging precedes the orchestrator's
step-11 merge, which no seat performs). **PASS**, clause by clause — every
goal clause met with named evidence, none goal-ambiguous:
loadGateConfig resolution (`gate.ts:100`); byte-identical off for
absent-file/absent-section/`gate:{}`; all three readers resolve through the
single `loadGatePolicy` call with the canary confirming zero config literals;
`mode` out of `ALLOWED_POLICY_KEYS` with the packaged file mode-less and the
generic unknown-key FAIL verbatim; class 1 (verify=0 refused, direct=0 legal,
cross-check intact); class 2 (per-parent sub-key refusals); class 3
(own-key membership — prototype-key collisions refused); class 4
(basisSafeString `/[\r\n]/` before the class-3 check, pinned message shape, no
tail). "No goal clause remains unmet or untested."

## Steps 11–12 (autonomous substitution, R1/R2)

R1: the ORCHESTRATOR performs every merge — this runner leaves the card `In
Review` with PR #92 open; no seat runs `gh pr merge`. R2: all card+board
record commits ride the PR branch (they do — every state transition in this
record is a commit on `feat/ev-73-gate-config`).

## Step 13 — follow-ups drafted (draft-then-confirm re-homed to the orchestrator)

Per the job-2 ruling's ratified step-13 feed, written on the branch for the
orchestrator's pre-merge confirmation:

1. **FLLWUP-81** — `gate-state.ts` lenient-reader hygiene, ratified as filed
   (hygiene, not a gate; unify the read through the validated loader or pin
   the load-order invariant; no new runtime gate). `epic: EPIC-14`.
2. **FLLWUP-82** — the merged card (synthesis item (2) cross-file invariant +
   item (4) `questions.json` newline-bearing ids), `epic: EPIC-13`, goal =
   the ruling's goal sketch, Intent names the pairing with FLLWUP-71.
3. **FLLWUP-83** — NOT in the ratified feed: the step-9 skeptic's residual
   (pre-existing prototype-chain `in` on the `mechanical` record,
   extensions/gate.ts ~441, byte-identical base↔head, out of EV-73's scope).
   Filed under the everything-surfaced rule; needs orchestrator confirmation.
4. EV-75's face carries the ratified candidate copy for the enriched migration
   `FAIL:` (job-2 item (a)); no new card for the migration copy — it is EV-75.
5. Wiki sweep split ratified as converged: EV-73's minimal correction (the
   `metered-deliberation-routing.md` "policy.json carries `mode`" claim) landed
   with the implementation (commit `2c63aab`); EV-77 does the full sweep.

## Run closure (orchestrator, step 11–12)

- **Merged** as `3386faffdb35ec7ac8fac38f435eeaf5373b6cf4` (PR #92, squash).
- Merge check: mode **Deliberate** (read via `council_route op:authority`, run
  `2026-09-20T08-59-16-719Z-259999-vjf6un`, ROOT `job-3`), all five criteria
  satisfied — criterion 1 owner gates independently re-run by the orchestrator
  on the head worktree `e581a88d`: preflight PASS, `tsc` exit 0, `bun test`
  1230 pass / 5 skip / 0 fail, `validate.py` clean; criterion 2 `gates`
  workflow `SUCCESS` on `e581a88d` (keyed on the `workflow` field) and again on
  the merged SHA `3386faff`; criterion 3 no blocking skeptic objection (skeptic
  PASS, cycle 2); criterion 4 judge PASS; criterion 5 no `Needs Human`, no
  outstanding ruling.
- Admin merge authorized by EPIC-14 R1; pinned with `--match-head-commit`.
- Follow-ups FLLWUP-81 (EPIC-14), FLLWUP-82 and FLLWUP-83 (EPIC-13) confirmed
  by the orchestrator at the step-13 gate and merged with the PR.
