---
id: EV-77
title: Document the gate's `.council.json` surface, its command, and the decide() basis vocabulary
state: In Review
owner: null
epic: EPIC-14
goal: vault/ documents the .council.json gate section and its mode values, the /council-gate toggle, the preflight condition, the decide() basis vocabulary, the callId: null fallback as a tool short-circuit, the intake-recorded-vs-dispatch-re-checked split, and the jev-to-gate-to-decisions-API vocabulary mapping, with every cited code path existing and the page reached from the wiki index.
---

## Intent

Reader-facing wiki copy: a first-time operator finds how to turn the gate on and
what the basis strings mean; the approval-gate `Mode: <mode> — <basis>` line
already exists and is linked.

## Acceptance

- Covers the config surface, the command, the preflight condition, the basis
  vocabulary, `callId:null` semantics, the intake/dispatch split, and the
  vocabulary mapping.
- Cited code paths resolve mechanically; the page is reached from
  `vault/wiki/index.md`.

## Notes carried from upstream cards

- **EV-74 (merged `201abbe0`) — the docs sweep must explicitly call out
  off-as-Deliberate:** `off` routes every card to the full Deliberate panel
  (maximum scrutiny); the naive "off = less rigor" reading is inverted. Also
  carry the timing-precision point: the mode is read lazily at gate-tool-call
  time, so a flip is visible to the current dispatch's next gate read (R5's
  echo wording deliberately undershoots this).
- **EV-74 designer P9 residual:** whether the status read should name
  `.council.json` (P9 said every form; the converged literal names only the
  write/no-op forms).
- **EV-73/EV-75:** the legacy `policy.json` `mode` key fails loud with the
  enriched migration line (file + key + `.council.json` `gate.mode`); `mode` is
  no longer an accepted `policy.json` key. The `metered-deliberation-routing`
  page is the reader-facing home.

## Deliberation record

### Round 1 — owner (job-7.1)

Structure: in-place extension of `vault/wiki/metered-deliberation-routing.md` — no new page. (1) Header quote gains off-as-Deliberate (absent file / absent `gate` / `gate: {}` resolve `off` byte-identically per `loadGateConfig`; `resolveRoute` returns Deliberate with basis `gate mode off — no recorded decision`, gate-route.ts:195) and lazy-read timing (echo wording deliberately undershoots). (2) New section "Turning the gate on: /council-gate" — `runGateCommand` grammar (0 tokens = status read, 1 token ∈ off|advisory|active, >1 usage error), `writeGateMode` byte-region splice, echo-then-run via `loadGateConfig`, no-op line, P9 residual documented as-is. (3) New "Run-start preflight" section — `runStartGatePreflight` truth table; wired at council.md step 0 / features-deliver.md Phase 0. (4) "The basis vocabulary" subsection — exact `decide()` shapes from gate.ts:612-688 plus routing-read and render-fallback strings. (5) "callId: null — the tool short-circuit" — not ledger corruption; cell B vs cell C distinct. (6) intake-vs-dispatch split (intake `touchedFiles: []`; recheck observed numstat set, escalation-only ratchet). (7) "Names and mapping" (R5). (8) EV-75 migration note.

Tradeoffs: includes routing-read + render basis strings (broader than literal "decide()"); avoids hardcoding thresholds 2.6/3.4 (they move with EV-72 tuning) — points at decision.json instead.

Testable: path-existence pin — validate.py today checks nothing about wiki-cited paths; proposes extending validate.py with a check that every backticked `extensions/**`/`council/**` path cited in vault/wiki/*.md exists + every page has an index entry. Copy quoting is safe where byte-pinned by existing tests (ev74 command literals, GATE_RENDER_FALLBACK). off-as-Deliberate pinned by resolveRoute code/tests.

Open: (1) P9 residual — document as-is + carry residual (docs card shouldn't change the literal) but council should rule; (2) where the mechanical path check lives — validate.py lean; (3) basis-table breadth — broad vs narrow reading (narrow can't fully avoid gate-run/gate-render material anyway).

### Round 1 — principal (job-7.2)

Reframe: the reader-facing artifact is the `Mode: <mode> — <basis>` line and its basis is emitted by FOUR modules joined by one formatter — `decisionLine(record)` (gate-ledger.ts:246-249) is the ONE formatter; decide() emits six shapes; gate-run.ts emits `gate call failed: <reason>`; gate-route.ts emits fallback/drift/disagreement strings; gate-render.ts emits cells B/C literals. The goal's "decide() basis vocabulary" and FLLWUP-76's carried "every distinct basis shape the code can emit, each beside its emitting function" are NOT the same set — the union is the correct reading (correctness claim, decidable by reading the four modules). Second: intake-vs-dispatch — recheck fires ONLY when the recorded route is a reduced mode (`rechecked:false` when `route.source !== "recorded" || route.mode === "Deliberate"`); "re-checked at dispatch" is false as a universal — a recorded Deliberate card is never re-checked. Stale-consumer finding: index.md summary still encodes pre-EV-73 "packaged default mode off" model, and the page's Residuals still list FLLWUP-74's verify>0 gap as open though gate.ts now enforces it — the acceptance "cited paths resolve mechanically" can pass while prose is false.

Proposed structure: lead callout (config home; section-absent ⇒ off byte-identical); turning it on (mode table + off⇒Deliberate inversion + lazy timing + /council-gate + EV-75 migration); before the run (preflight condition); what the decision means (modes→MODE_PANELS; high composite = mechanical = cheaper); reading the Mode: line (decisionLine format + emitter-keyed basis table); intake vs dispatch; three names one thing (jev model pin / gate operator / decisions wire).

Pin mechanically: cited file paths; GATE_MODES/GATE_DECISION_MODES literals; /council-gate usage string; the two GATE_RENDER_FALLBACK literals; gate.mode key. Do NOT pin prose explanations (test-as-copy-source is backwards).

Tests: T1 path-existence; T2 mode-identity (page contains every GATE_MODES/GATE_DECISION_MODES member + exact usage tail); T3 basis coverage (drive decide() per branch, assert page names characterizing tokens + byte-exact fallback constants); T4 off⇒Deliberate; T5 index reach + summary de-staled; T6 stale-claim guard (Residuals no longer list FLLWUP-74 as open; policy.json not described as mode-bearing outside the migration paragraph).

Open: (1) vocabulary scope — union vs decide()-only; (2) cell C in or out; (3) P9 residual ruling; (4) does "resolve mechanically" mean a test file — lean yes (house pattern), PO confirm a docs card may add a test; (5) home for the three-names mapping.

### Round 1 — designer (job-7.3)

Two readers, one page: first-time operator (from index) + LLM seat diagnosing a routing decision. Operator section at top answering: how do I turn it on, what does off actually do, what is the Mode: line. Gulf analysis: execution (page shows section shape + command; today it doesn't), evaluation (basis shapes reproduced; today "a deterministic one-line string" only), conceptual (callId:null explained as pre-POST short-circuit; today absent).

Seven basis shape templates with real numeric examples lifted from test/gate-decide.test.ts and test/gate-render.test.ts (override; choice floor; score floor; noul; composite ≥ direct; composite ≥ verify; composite < verify) plus fail-closed shape. EV-75 migration line reproduced byte-for-byte from gate.ts:135, noting the deliberately absent gateFail tail. /council-gate copy lines reproduced verbatim (council-gate-cmd.ts:11-29); status read does not name .council.json — the page must mirror the operator surface exactly.

Predictions P1-P12: P1 off-as-Deliberate one-line callout names the inverted reading + seven seats; P2 every cited path resolves; P3 shape+literal matching; P4 callId:null wording ("the call never issued, no ledger line was written... the ledger itself is intact"); P5 vocabulary table maps typesafe/jev-1.13 ↔ gate; P6 panel composition named per mode; P7 migration line verbatim without gateFail tail; P8 preflight FAIL line verbatim + both remediations + procedure pointers; P9 index reach one click; P10 `typesafe/jev-1.13` exactly once in page (operator section zero jev, R5); P11 callId:null ≠ routing-read matchedCallId; P12 intake (declared, touchedFiles:[]) vs recheck (observed numstat) split named.

Preferences ranked last: operator section above the shipped blockquote; three-column vocabulary table; shape AND literal per basis row; off-callout as bolded paragraph not blockquote (blockquotes reserved for status notes); one-sentence LLM-seat pointer to engine-side pages.

Open: P9 residual (page can say the command reads from .council.json without changing the literal — doc-surface decision); shape-only vs shape+literal; whether vocabulary table needed (leans yes — jev visible in ledger model field/commits); gate-state page link (doesn't exist; out of scope to create); whether to note the gateFail-tail asymmetry in one sentence.

Index: amend summary tail to name off-routes-every-card-to-Deliberate at the index link itself.

### Round 2 — owner (job-7.4)

Verified: recheck fires only when `route.source === "recorded" && route.mode !== STRONGEST` (gate-route-tool.ts:109, STRONGEST=Deliberate :47); `thresholds.verify > 0` enforced (gate.ts:489-491, FLLWUP-74 gap closed — Residuals stale); EV-75 migration FAIL before the key loop (gate.ts:187-192); status literal names no file (council-gate-cmd.ts:20); validate.py has no wiki-path check today.

CONCEDES: emitter-union reading (principal's correctness claim); stale claims in scope; designer's operator-first ordering, shape+literal rows, three-column vocabulary table, bolded-paragraph off-callout, P9 doc-surface framing. HOLDS: in-place extension; validate.py as the mechanical-check home (a docs card extending it is consistent with house pattern). HOLD WITH CORRECTION: principal's "re-checked at dispatch is false as a universal" — the page under-claims (recorded Direct also re-checks); the condition is `source === "recorded" && mode !== "Deliberate"` covering Verify AND Direct.

Settled: (1) P9 document-as-is + residual carried; (2) validate.py path-existence scoped to backticked paths under known roots in vault/wiki/*.md; (3) emitter-keyed union table; (4) stale claims fixed in-card, guarded by T6; (5) page states the full recheck condition. Open (small): gateFail-tail asymmetry sentence (lean yes); T3 byte-exact fallback assertions in test file (lean test file).

### Round 2 — principal (job-7.5)

CONCEDES: no hardcoded threshold numerals; T1 path-existence + index-reach belongs in validate.py (cedes to owner); P9 document-as-is (converged). HOLDS: emitter-union breadth; stale claims in scope; re-check copy carries the reduced-route precondition. Resolved structure converges with owner/designer. Settled: (4) validate.py for T1; T2-T6 as test/ behavioral pins; (5) cell C IN (gate-render.ts:35-38 distinct honest state). Genuinely open: gateFail-tail sentence (lean yes); validate.py filesystem check must NO-OP when vault/ is absent (seam constraint on T1, not a disagreement — packaged default must not fail consumer repos with no wiki). Re-check falsifier: assert copy contains "recorded reduced"/"recorded Deliberate never re-checked".

### Round 2 — designer (job-7.6)

CONCEDES to owner: in-place extension; path pin in validate.py; no hardcoded thresholds. CONCEDES to principal: four-emitter union with decide() shapes distinguished; P12 precision (recheck only on recorded REDUCED routes, gate-route-tool.ts:113); stale-claim sweep (index summary + Residuals FLLWUP-74 closure); cell B/matchedCallId disambiguation. HOLDS: operator section at top (converges with principal's lead-callout shape); shape AND literal per row where byte-pinned; vocabulary TABLE (3×3) after the operator section; P9 doc-surface (page names .council.json in prose; gateStatusLine literal stays); P11 three-way disambiguation (cell B ≠ matchedCallId ≠ R4 short-circuit); index summary amendment naming the inversion.

Full resolved 9-part structure + P1-P12 falsifiable predictions recorded. Settled: items 1-9 as listed. Genuinely open for PO: (a) whether gateStatusLine itself should later name .council.json (future FLLWUP; out of scope); (b) docs-card-adds-tests confirmation (leans yes, house practice).

CONVERGENCE NOTE (facilitator): all three seats converge on the merged design — in-place extension, operator-first, emitter-union vocabulary keyed by module, shape+literal rows, thresholds by reference to decision.json, validate.py path-existence + test-file behavioral pins, stale-claim sweep in scope, P9 documented as-is with the literal untouched, cell C in, recheck precision (recorded reduced only), off-as-Deliberate + lazy timing explicit, vocabulary mapping table, index summary amendment. Positions stabilized in round 2 of 3 — exchange stopped early per the cap discipline.

### Step 4 — skeptic attack (job-7.7)

Ran 43 turns of probes: module reads, bun run probes from /tmp injection trees, 143 gate tests pass / 0 fail, failure-injection on validate.py, byte-exact literal renders.

Objections and results:
1. Recheck condition exactly `route.source !== "recorded" || route.mode === STRONGEST` at gate-route-tool.ts:109 (STRONGEST=GATE_DECISION_MODES[0], :47); recorded Direct DOES fall through to recheck — **closed-green**.
2. `decisionLine` (gate-ledger.ts:248) is the only `Mode: ` formatter; six decide() shapes confirmed (gate.ts:624/643/653/671/678/684); four emitters exist — **closed-green for substance, closed-red for one wording**: gate-route's fallback/drift/disagreement strings NEVER reach decisionLine — they travel only in council_route's JSON RouteResult. Copy must describe them as routing-read tool-JSON bases, not decisionLine-rendered lines.
3. FLLWUP-74 verify>0 enforced (gate.ts:486-492, "expected a finite number > 0"); Residuals listing it open is stale; FLLWUP-71/75 legitimately open — **closed-green**.
4. validate.py has no wiki-path check today (failure injection: nonexistent-path citation stays green, exit 0) — **closed-green**.
5. Byte-exactness: EV-75 migration literal renders without gateFail tail (**closed-red on line citations** — the block is gate.ts:186-189, NOT :135 or :187-192; the page must cite the true lines or the mechanical pins fail); preflight FAIL literal byte-identical (**green**); council-gate four literals + grammar incl. trailing-ws (**green**); cells B/C literals (**green**).
6. off⇒Deliberate via resolveRoute probe: `{"mode":"Deliberate","source":"fallback","basis":"gate mode off — no recorded decision"}` — **closed-green**.
7. callId:null semantics (gate-tool.ts:158-164 → cell B) distinct from matchedCallId (routing-read join) and cell C — **closed-green**.
8. Scope: validate.py extension + test file are the mechanization of "cited code paths resolve mechanically", not creep — **closed-green**. One **open-untested** carried: page/validate.py-extension/test file do not exist yet — byte-exact page quotes, the vault-absent no-op seam, and the index-summary amendment are verify-on-implementation items.

Verdict: no blocking objections. Two record corrections carried into implementation: (a) EV-75 block cite gate.ts:186-189; (b) gate-route strings described as RouteResult bases, never decisionLine output.

### Step 5 — consolidator synthesis (job-7.8)

Buckets — Open objections: NONE (skeptic's 43-turn attack closed everything; the two closed-red wording/citation corrections folded into settled). Open judgment: J1 (gateFail-tail asymmetry sentence in or out — all seats lean yes) and J2 (docs card ships validate.py path-existence extension + bun test file — all seats lean yes; skeptic ruled scope "not creep by the card's letter", closed-green). Settled: S1-S12 as recorded (emitter-union vocabulary with gate-route strings as RouteResult bases never reaching decisionLine; recheck iff source==="recorded" && mode!==Deliberate with recorded Direct re-checking; FLLWUP-74 verify>0 closed and Residuals stale; EV-75 cite gate.ts:186-189; off⇒Deliberate; cell C in; P9 document-as-is; validate.py as T1 home with vault-absent no-op; migration literal without gateFail tail; callId:null three-way disambiguation; R4/R5 binding; stale-claim sweep in scope).

Consolidator's merged-design statement recorded as the step-7 spec basis. Note (facilitator): two consolidator paraphrase slips corrected against the card record — `typesafe/jev-1.13` appears exactly once in the vocabulary-mapping TABLE with zero jev in operator sections (designer P10, R5), and the three-way callId disambiguation is cell B (callId:null, failed-before-recording / short-circuit) vs `matchedCallId` (routing-read result field) vs cell C (recorded callId matching no ledger line).

### Step 6 — routing (facilitator)

J1/J2 are open-judgment items the consolidator routed to product-owner. council_dispatch refused the product-owner seat from this container (ruling seats are orchestrator-dispatched). Phase-1 rulings R1-R5 checked: none answers J1 or J2 (R4 governs scope fold-in, R5 governs wording; neither confirms a copy sentence nor a docs-card test precedent). Escalated to the orchestrator per the escalation contract — facts only, no recommendation. Card held at Deliberating pending the ruling.

### Step 6 — routing (resumed turn) — product-owner ruling applied

The orchestrator obtained the ruling on J1/J2 (product-owner, job-8). Appended VERBATIM:

--- RULING VERBATIM (product-owner, job-8) ---

J1 — RULING: the gateFail-tail sentence is IN. One sentence, placed adjacent to the byte-reproduced EV-75 migration literal (inside the migration paragraph, never in the operator's how-to-turn-it-on section), and it must carry the *reason* — the standard tail's "set a valid value or remove the key to use the packaged default" is wrong advice for this key — not a description of the helper. Page copy only; `extensions/gate.ts:186-189` is not touched.
- Rejected: omitting it as "migration detail / operator-first clutter." The page reproduces the literal byte-for-byte by house practice (designer P7), so the absence of the tail becomes visible on the page itself. Unexplained, a reader who knows the `gateFail` template (`extensions/gate.ts:136`) reads it as truncation — and the likely "consistency fix" would re-attach advice that tells an operator to remove the key and fall back to a packaged default that no longer carries a mode. That is a mechanism risk to the reason the asymmetry exists, not a taste question.
- Also rejected: putting it in the operator section — the operator sees one FAIL line and never learns `gateFail` has a tail to miss.

J2 — RULING: the docs card ships mechanical pins, and all of them land in `test/`, NOT in `council/validate.py`. T1 (cited-path existence + index reach) joins T2–T6 as bun tests over the real `vault/wiki/*.md`.
- Rejected (Position B — "docs cards don't carry test files"): its premise is false on this tree. `test/fllwup25-agents-page.test.ts` was shipped by a docs card to pin a wiki page against `AGENTS.md`, and `test/fllwup58-gates-backstop.test.ts` reads the real `vault/wiki/test-suite-budget.md` and asserts its machine-parity marker against `gates.yml`. FLLWUP-25's settled item even recorded "consistency check under `test/`, not `council/`."
- Rejected (validate.py as T1's home, the three-seat convergence): (1) `council/validate.py` is packaged tooling (`TOOLING_FILES` in `extensions/scaffold.ts:26`, seeded by `/council-init`, refreshable by `/council-update`); a check keyed to `extensions/**` citations hardcodes this package's layout into every consumer repo, where a legitimate wiki citation of package internals resolves under the install clone, not the consumer root — the consumer's wiki would go red for doing nothing wrong. (2) Extending it means the repo copy and `council/scaffold/council/validate.py` diverge unless a docs card also ships a tooling-payload change — consent-gated refresh, `/council-update` surface, semver territory. (3) Enforcement parity is exact: CI runs `bun test` and `python3 council/validate.py` back to back (`.github/workflows/gates.yml:17,19`), and neither `wiki-ingest.md` nor `wiki-lint.md` runs validate.py at all — validate.py has no timing advantage at the moment a wiki page is written; the moment that protects the wiki is the merge gate, and `test/` is already in it. (4) The principal's `vault/`-absent no-op seam constraint disappears entirely: `test/` runs only in a repo that always has `vault/`.
- Not creep, and not optional: the goal's own clause is "every cited code path existing," the Acceptance says "resolve mechanically," and the skeptic's failure injection proved the check is genuinely absent today. Without new surface, "mechanically" is decoration for a human eyeball.
- The how stays with the owner: one file or several; fllwup58's marker shape (test defines the string, page carries it verbatim) is the proven pattern for copy pins; a new live-arm file owes the `[[test-suite-budget]]` header rule. If consumer repos are also to be protected, that is a SEPARATE tooling-class card with the layout-generalization decided as tooling design — a step-13 candidate, not a fold-in (EV-77's goal is met without it).

Carried as binding from the skeptic's step 4 (not reopened): cite the EV-75 block as `extensions/gate.ts:186-189` — the throw sits at :189 — and describe gate-route's fallback/drift/disagreement strings as routing-read `RouteResult` bases that never reach `decisionLine`.

No escalation — the goal is sound, no recorded human decision is overturned, nothing is declined or permanently accepted.

--- END RULING ---

Applied (facilitator, citing escalation-contract step 1): J1 and J2 are closed by the ruling and bind the spec and owner over the round-2 convergence (T1's home is `test/`, not `council/validate.py`). The skeptic's carried constraints bind verbatim (EV-75 cite `extensions/gate.ts:186-189`; gate-route strings as `RouteResult` bases never reaching `decisionLine`). Phase-1 rulings R1–R5 unchanged and applied throughout. No open-judgment items remain; no open objections stand (skeptic step 4 closed all; its two open-untested items are verify-on-implementation, checked at step 9). Routing complete — proceeding to step 7 (spec) and step 8 (owner).

Routing note (facilitator): the decisions gate is off on this tree (no `gate` key in `.council.json`, no gate ledger), so `council_route` has no matching record for this card — step 1's fallback judgment (full-council Deliberate path, surface-touching, designer seated) was the prior runner's recorded basis for steps 2–6 and stands; the step-9 re-check will return the same fallback. This step-6 entry is the open-item routing of council.md step 6 proper, closed by the ruling above.

### Step 8 — owner implementation (job-9.1)

PR #96 opened against `main` at `bbe5cc60b90871b2a588e1e5a7e71333cb10da1b`; record commits ride the branch (R2). Owner gates observed by the owner: preflight PASS, tsc clean, `bun test` 1286 pass / 0 fail / 5 skip, validate.py clean. Deliverables: `vault/wiki/metered-deliberation-routing.md` extended in place; `vault/wiki/index.md` amended; new `test/ev77-gate-docs.test.ts` (T1–T6, J2 honored — validate.py untouched). Declared deviations, all with stated basis: (1) `test/faux-provider-shape.test.ts` `SCAN_EXEMPT_SUFFIXES` extended for the pin file's exemption keys; (2) T1 scoped to vault/wiki top level, excluding `sources/`; (3) 8-entry T1 exemption map with per-entry reasons; (4) `<...>` placeholders in pinned literals backed by per-literal source-drift guards.

### Step 9 — skeptic verification, cycle 1 (job-9.2) — BLOCK

Subject: PR #96 head `63659b6c9bbd4f537f225a80322f2c31b0c59043`. Skeptic re-ran all four gates green at head (1286 pass / 0 fail / 5 skip, 110.08s) and proved each gate can red via injection. Items 1–13 **closed-green** (witness not weakened; sources/ exclusion sound; no dead exemption entries; source-drift guards non-tautological; jev exactly once; J1 sentence placed and reasoned; RouteResult wording correct; no threshold numerals; byte pins tested; vault-absent seam moot; index amended; T4 probe sound; intake/dispatch + callId disambiguation accurate).

**One closed-red (blocking):** the page's Run-start preflight section states "A pass is silent when the gate is off" — falsified by direct tool invocation: `registerPreflightTool`'s `execute()` returns the ok line (`council_preflight: ok — decisions gate off, or an OpenRouter credential resolved`) in the gate-off case; the tool has exactly two outputs (FAIL line or that single ok line). The sentence's two-case grammar misstates the shipped surface goal clause (c) documents. Required change: make the ok line explicit as returned in BOTH pass cases; FAIL condition and literal untouched.

Card returned to `In Progress` per council.md step 9 (verify-cycle 1 of 3). Handed to owner verbatim.

### Step 8 (fix cycle 1) — owner (job-9.3)

Head `88e2bb4e4f86a700bd6de290637cdec1ccb8a2df` pushed (`9f850d1..88e2bb4`). Diff exactly the red item: page pass-case prose reworked (both pass cases report the same single ok line at the tool surface, quoted byte-for-byte; the silent pass is the pure `runStartGatePreflight`'s engine-internal form, never the tool's), ok line byte-pinned with a source-drift guard, T6 `not.toContain("A pass is silent")` added red-first. FAIL paragraph byte-untouched; FLLWUP-59 witness untouched. Gates observed: preflight PASS, tsc clean, 1286 pass / 0 fail / 5 skip (110.01s), validate.py clean.

### Step 9 — skeptic verification, cycle 2 (job-9.4) — PASS

Subject: PR #96 head `88e2bb4e4f86a700bd6de290637cdec1ccb8a2df`. All six fix claims closed-green with real output: diff scoped to 2 files (diff vs extensions/ + council/ + board/cards empty); own falsifier confirmed both pass cases return the identical ok line byte-identically and the pure function returns null (12/12 checks); FAIL literal byte-identical to cycle 1; T6 red-first established against the real cycle-1 blob (red at 9f850d1, green at 88e2bb4); witness untouched; no new surface inaccuracy. The 13 cycle-1 green closures stand (diff doesn't touch their subjects). Gates at head: preflight PASS / tsc 0 / bun test 1286 pass 0 fail 5 skip 109.95s / validate.py clean. **Verdict: PASS — no open objections.** Verify-cycle 2 of 3 used.
