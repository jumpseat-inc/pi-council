---
id: EV-85
title: Seat-composition decisions as repo-overridable packaged data
state: Backlog
owner: null
epic: EPIC-11
goal: council/gate/setup/questions.json and council/gate/setup/decision.json are the packaged seat-composition decision data whose answers and decision policy partition the composite into exactly one of the literals Default or Redefine, resolved whole-file with a repo-local $CONFIG_DIR_NAME/council/gate/setup override first-hit via gateDirs(repoRoot, "setup") and failing loud on malformed JSON, an unknown key, or an unknown disposition literal, proven by a loader test over the packaged files, an override fixture shadowing the packaged file whole-file, and a fail-loud arm for each defect class.
---

## Intent

The packaged seat-composition decision data: the question set and decision policy for the third decision domain. It follows the shipped follow-up domain's shape — `council/gate/setup/questions.json` + `decision.json`, resolved whole-file first-hit via `gateDirs(repoRoot, "setup")`, sharing the loader discipline (`readGateFile`/`gateFail`/`validateGateQuestion`) with the card-dispatch and follow-up domains, and overridable per repository under `$CONFIG_DIR_NAME/council/gate/setup`. The disposition vocabulary is exactly `Default` and `Redefine`; a single-literal collapse is not a partition.

## Acceptance

The packaged pair loads; a repo-local override shadows whole-file (a distinctive question present, the packaged absent); malformed JSON, an unknown key, and an out-of-vocabulary disposition each FAIL naming the offending file and key; no field-level merge occurs; the decision policy's disposition vocabulary is exactly the literals `Default` and `Redefine`.