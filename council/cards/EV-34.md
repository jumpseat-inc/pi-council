---
id: EV-34
title: Compose each tool call and its result into one rendered unit
state: Ready
owner: null
epic: EPIC-8
goal: In the inline progress transcript at width 80 each tool call renders as one unit whose header names the tool and its primary argument and whose result sits indented beneath it, a failed result is marked with a token distinct from a successful one, and a legacy-blocks fixture without identity fields renders without throwing, proven by rendered-line assertions over a fixture with one successful and one failing tool call plus the legacy fixture
---

## Intent

The user-visible surface is the inline progress transcript body beneath pi's
input bar, at width 80, reading the blocks the parser produces. Each tool
call and its own result become one rendered unit: a single composed head
line carrying the tool name, the primary argument, and the failure status,
with the result body indented beneath it when the block is expanded. The
collapsed state shows only the composed head. The failed-result token is
`muted "✗"`; `warning` is already the call's token and cannot carry the
failure without losing the distinction.

The existing head lines this replaces are the `warning` call line and the
`muted` result line in `blockLines`. The empty inline-progress state copy
(`(waiting for output…)` / `(no transcript)`) is part of this body and is
worded `(waiting for output · idle)` so a person can tell a still-running
seat from a broken one.

The full-screen modal transcript is out of scope; it is the legacy path
guarded at navigator.ts:57. Depends on EV-33 for call identity and the error
bit; unpaired or legacy blocks must still render without throwing.

## Acceptance

- At width 80, the call unit's header names the tool and its primary
  argument, and a failed result carries `muted "✗"` distinctly.
- The collapsed-first / expand-with-`e` behavior asserted today
  (`test/navigator.test.ts`: first line visible, detail hidden until `e`)
  survives.
- A legacy-blocks fixture with no identity fields renders without throwing.
- The empty state is visible and worded as above; `test/theme-compliance.test.ts`
  stays green with no new tokens.

## Phase 1 ruling (features-deliver, binding for this run)

**R-COPY (EPIC-8)** — the composed unit is `→ <Tool>  <primary-arg>`; a
failed result appends `muted "✗"` to that head; an expanded result body is
indented 2 spaces beneath the head; the inline empty state is worded
`(waiting for output · idle)`. This is a recorded human decision, immutable
for the run; a runner that hits this dispute applies this ruling and cites
it rather than re-asking.

## Run record (features-deliver / EV-34)

### Step 1 — classification (facilitator)

- **Path: mechanical.** The card is narrowly scoped and its rendered output
  is already pinned by a binding ruling before the run starts. The Intent
  names exactly the two head lines the card replaces — the `warning` call
  line and the `muted` result line in `TranscriptView.blockLines`
  (`extensions/navigator.ts:649-676`) — and R-COPY pins the composed result:
  head `→ <Tool>  <primary-arg>`, `muted "✗"` suffixed on failure, expanded
  result body indented 2 spaces beneath the head, collapsed state showing
  only the head, empty state worded `(waiting for output · idle)`. There is
  no second reasonable design for the goal to admit; the residual latitude
  (how the call↔result pairing is implemented, where the identity map lives,
  how an unpaired result degrades) is implementation choice, which
  council.md step 1 excludes from the full-council test. No cross-seam: the
  parser fidelity EV-34 consumes is EV-33's closed scope, and EPIC-8's own
  Intent states EV-34/35/36 "all edit TranscriptView.render/blockLines in
  one file, so they land in sequence." Per step 1, mechanical cards skip
  steps 2–6 and go directly to step 7.
- **Surface-touching: yes.** The delivered artifact is the rendered inline
  progress transcript body — the composed head, the failed-result token, and
  the empty-state copy are all user-visible. Because the card is
  *mechanical* as well, step 1's second paragraph applies: no `designer` seat
  is seated, and any design concern the run surfaces is filed as a follow-up
  at step 13, not used to reopen the card.
- **Rulings applied:** **R-ORDER** (Phase 1) — predecessor EV-33 is merged to
  local `main` as `186c04dc` (PR #47) and local `main` equals `origin/main`
  at `a75b3c3`, so EV-34 may start. **R-COPY** (Phase 1) — the composed
  unit's exact copy is a recorded human decision; applied, not re-asked.
  **R-MODAL** (Phase 1) — the full-screen modal transcript path is out of
  scope; EV-34's subject is the inline progress body, which `renderProgress`
  hosts at `extensions/navigator.ts:437-466`. (Factual note for the owner,
  not a ruling: the inline body and the legacy modal share the same
  `TranscriptView` class, and this card's Intent names `blockLines` as the
  seam it edits, so the composed unit lands in the shared renderer; the
  modal's own guard/repair remains FLLWUP-4's scope, and no fork of the
  renderer is authorized here.) **R-GATES** (Phase 1) — the owner gate set is
  `bash council/preflight.sh` (card-aware), `bunx tsc --noEmit`, `bun test`,
  `python3 council/validate.py`, with the network integration test gated
  behind `COUNCIL_INTEGRATION=1`.
- **Seat resolution (2026 `<seat_resolution_check>`):** `owner`, `skeptic`,
  and `judge` — the three seats this card's mechanical path dispatches — all
  resolve by name via `loadSeat` against this repo root; no `Unknown seat`
  error. `principal`, `designer`, and `consolidator` also resolve, though
  the mechanical path dispatches none of them.
- **Environment:** `bash council/preflight.sh EV-34` → `PASS: preflight clean`
  on the main checkout at `a75b3c3`. No `FAIL:` line; the FLLWUP-27
  branch-freshness artifact is a by-construction feature of a card worktree
  whose base predates mid-card record commits, and is not expected on
  `main`.