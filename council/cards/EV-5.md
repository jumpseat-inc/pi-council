---
id: EV-5
title: Document the council theme system
state: In Progress
owner: null
epic: EPIC-1
goal: A design spec under docs/superpowers plus AGENTS.md and README entries describe the council theme system so the wiki ingest captures it
---

## Intent

This repo's convention (AGENTS.md) is design spec + implementation plan
under docs/superpowers/ before big changes, then wiki ingest after they
land. This card produces that paper trail for the theme epic:

- Design spec: docs/superpowers/specs/<date>-council-theme-design.md and a
  matching plan, covering the omp palette provenance, the pi theme schema
  mapping, the `.council.json` theme section shape, activation semantics
  (EV-3's precedence decision), and the surface-compliance rules (EV-4).
- AGENTS.md: a hard-convention entry documenting the `.council.json` theme
  key (alongside 9.5's seat overrides) and the rule that council-drawn UI
  draws only from pi theme tokens.
- README: a short section showing the scaffold `.council.json` theme snippet
  and how to recolor the council.
- Ordering: this card lands first among the children — the implementation
  cards follow the spec (each cites it). Wiki ingest is the follow-up
  procedure after implementation lands, not part of this card.

## Acceptance

- Spec + plan files exist under docs/superpowers/ and cover the sections
  above, with the activation decision recorded.
- AGENTS.md documents the `.council.json` theme key and the token-only
  drawing rule.
- README includes the theme customization snippet.
- `python3 council/validate.py` still passes after the docs edits (no card
  or board drift).

## Run Record (features-deliver, autonomous)

- **Step 1 gate:** full council (the spec is the epic's design authority; omp→pi token mapping and the `.council.json` theme shape proposal are spec-ambiguous; the shape is a delegated Council working decision per EV-2's ruling). Not surface-touching (documentation; no designer seat).
- **Rulings applied:** NAME-1 (theme family `pi-council`, variants `pi-council-dark`/`pi-council-light`), EV-3 activation decision (auto via scaffold; explicit concrete settings.json theme wins; `light/dark` auto-follow does not block), EV-2 shape delegation (spec proposes, EV-2 owns final call), CI-1/MERGE-1 (gates SUCCESS on head SHA; deterministic merge).
- **Step 2–3:** `Deliberating` set; owner + principal dispatched in parallel (round 1), then one exchange round (round 2) after which positions stabilized — exchange stopped at 2 of ≤3 rounds. Full positions appended below.
- **Step 4 (Skeptic):** all load-bearing factual claims closed-green (omp 67/66 keys → 51 live tokens == pi's 51 required; zero 256-index in live set; pythonMode≠bashMode; public package index exports only initTheme/Theme/ThemeColor; ui.setTheme(instance) routes to setThemeInstance and never writes settings.json while ui.setTheme(string) persists; resolveThemeSetting collapses the `light/dark` pair but passes literal `dark`/`light` through unchanged; pi's applyFromSettings persists a detected `dark`/`light` into settings.json on high confidence — the load-bearing evidence for the four-state activation reframe; resources_discover→themePaths registration; Theme constructor defaults the 4 optional tokens from siblings; only ANSI in extensions/ is inside a navigator.ts comment; scaffold non-clobbering confirmed; validate.py clean on docs-only changes). One closed-red: the deliberation's "silently dropped" description of a `council.theme` key is inaccurate — it actually creates a phantom seat entry (`{"council": {"theme": {"enabled": true}}}` → `{"theme": {}}` in the output map); the proposed `loadThemeConfig()` sibling loader avoids it; spec must describe current behavior accurately.
- **Open judgment (routes to step 6):** the four-state activation question — pi persists a literal `"dark"`/`"light"` into settings.json on first run (high-confidence terminal detection), and under the "concrete theme name wins" ruling a literal `dark`/`light` is a concrete built-in theme selection that would block council activation, narrowing "auto via scaffold" to fresh installs with an unset theme. Whether that narrowing is acceptable (or whether built-in dark/light literals are treated like the auto-follow pair, i.e. not blocking) is a ruling-seat question, not a working decision. Not covered by any Phase 1 ruling as written.

### Deliberation positions (verbatim)

**OWNER POSITION (ROUND 1)**

Grounding read: `docs/themes.md` + `theme-schema.json` (fetched from pi main, verified 51 required + 4 optional tokens), `theme.d.ts`, `council/scaffold/.council.json`, `extensions/navigator.ts`, `extensions/index.ts`, `extensions/scaffold.ts`, `council/validate.py`, `vault/wiki/council-config.md`, and the actual omp sources (`oh-my-pi` main, `packages/coding-agent/src/modes/theme/{dark,light}.json`, fetched). All numbers below are from those files, not recollection.

1. **omp palette provenance — what the spec must assert.** (a) Sources, pinned: the palette is exactly the two files `dark.json`/`light.json` under `github.com/can1357/oh-my-pi`, path `packages/coding-agent/src/modes/theme/`. The spec must pin a commit SHA and record it so EV-1's conversion and later re-pins are reproducible; upstream drift is a deliberate re-pin, not an implicit sync. (b) The two files are NOT twins: dark has 67 color keys, light has 66 — the sole difference is dark carries a stale `link` key (`#0088fa`, byte-identical to its `mdLink`) that light already dropped. (c) The decomposition is exact and identical in both files: dropping the dead/legacy keys (`statusLine*` ×14, `link` in dark, `pythonMode` in both) leaves exactly 51 live tokens in each file — precisely pi's 51 required tokens, no more, no less. (d) Value classes: both files have 15 `vars` entries (18 distinct names across the pair); colors reference vars, literals, or `""`; zero 256-index integers in the live 51 (integers appear only in the dead `statusLine*` set); empty-string defaults (`text`, `userMessageText`, `customMessageText`, `toolTitle`) in both files. (e) Brand anchors: dark accent `#febc38` (omp orange), border `#178fb9`, borderAccent `#0088fa`, customMessageLabel `#b281d6`, mdCodeBlock `#9CDCFE`; syntax set is VS Code Dark+ literals; light accent is teal `#5a8080`, border `#547da7`; `export` (`pageBg`/`cardBg`/`infoBg`) maps 1:1 to pi's optional export section. (f) Legacy trap: `pythonMode` is *not* an alias of `bashMode` (pythonMode yellow, bashMode cyan dark / green light); pi has no python mode; `pythonMode` maps to nothing and must be dropped, not renamed.

2. **pi theme schema mapping — table as a three-part appendix.** (1) Identity (51): every pi-required token present under the same name in both omp files — a statement, not a mapping effort. (2) Legacy/dead (16 dark / 15 light): `link` → dropped (dark-only stale alias == mdLink); `pythonMode` → dropped (no pi target); `statusLine*` ×14 → dropped (no longer in pi's schema; the only home of 256-index values). (3) Optional-token fallbacks (4): pi's optional `scrollbarThumb`→`selectedBg`, `searchMatchBg`→`selectedBg`, `searchMatchText`→`text`, `thinkingMax`→`thinkingXhigh`; none of the four exist in omp and every fallback source exists — the derived theme validates with zero additions. Stays open for EV-1 (named as open, not decided): var-refs preserved vs resolved-to-hex in the shipped JSON; shipping mechanism (`themes/` dir vs `pi.themes` in package.json vs `resources_discover → themePaths`); whether variant files are hand-written or generated; hot-reload behavior for package-shipped themes; per-token override merge at file vs instance level. Naming constraint (from themes.md): theme `name` required, unique, no `/`; ship exactly the two variant files; treat bare `pi-council` as a *family selector* in config, not a third theme file.

3. **`.council.json` theme section shape — prescriptive, flagged as EV-2's working decision.** Top-level `theme`, a sibling of `council` (not nested — it is not a per-seat override). Working proposal: `{ "council": { ... }, "theme": { "enabled": true, "variant": "auto", "overrides": { "accent": "#febc38" } } }`. `variant` ∈ `auto|dark|light`; `auto` follows terminal background → `pi-council-dark`/`pi-council-light`. `overrides` keyed by pi `ThemeColor`/`ThemeBg` names, values in pi's four accepted formats. `enabled: false` is the explicit off switch; presence implies enabled; deletion and `enabled:false` must behave identically so EV-3 has one code path. Scaffold consequence: `scaffoldInto` copies every entry non-clobberingly — the theme section ships in `council/scaffold/.council.json` and reaches **new installs only**; "auto via scaffold" therefore means "auto for fresh installs".

4. **Activation semantics — EV-3 precedence as a decision table.** settings.json raw `theme` value → council action: concrete non-council name → consumer's theme wins, no activation; concrete council-family name → already on, no-op; unset or `"light/dark"` auto-follow → council activates, `auto` variant resolves via detected terminal theme. Auto-follow nuance (hard requirement): `resolveThemeSetting` resolves `"light/dark"` against the terminal before anything sees it, so the auto-follow string must be detected from the **raw** settings value. Two engineering constraints for EV-3: activation must use instance install (never `setTheme(name)` — that persists to the consumer's settings.json); activation runs at `session_start` alongside `initHubIdentity`/`pruneRuns`/`renderWidget` (index.ts:109-135).

5. **Surface-compliance / token-only drawing rule (AGENTS.md hard convention).** Council-drawn UI draws **only** through `Theme.fg`/`Theme.bg`/`bold`/etc. with pi token names — no ANSI escapes, no literal hex, no 256-index literals in extension code. Enforcement seam exists: `NavTheme = Pick<Theme, "fg" | "bold" | "bg">` (navigator.ts:9); the widget (index.ts:88-107) is plain strings handed to `setWidget`, themed by pi itself. Grep confirms the only ANSI escapes in `extensions/` are inside a comment — the rule is today-compliant, which the convention entry should assert.

6. **README section.** A short subsection under "What you get" (README.md:161,181): the scaffold theme snippet, variant pinning, per-token override example, the off switch, and one precedence line. Keep it to the config surface — mechanics live in the spec.

Open (left for implementation cards): EV-1 file-level conversion details; EV-2 final shape; EV-3 activation implementation except the raw-vs-resolved and no-settings-mutation constraints; EV-4 which surfaces get which tokens beyond the token-only rule.

Testable claims (round 1): (1) mapping completeness script; (2) `resolveThemeSetting("light/dark", "dark")` returns a concrete name; (3) settings.json `theme` byte-identical after activation; (4) scaffold seeds theme section on fresh dirs, skips existing byte-for-byte; (5) validate.py clean on docs-only diff.

**PRINCIPAL POSITION (ROUND 1)**

Config seam (what EV-2 cites). `loadCouncilConfig` (seats.ts:103-132) returns `Record<string, AgentOverride>` and silently ignores top-level keys except `council`; `loadSeat` (seats.ts:167) is its only caller. Today a `theme` key in `.council.json` is accepted-without-error and dropped (spec must describe actual behavior — see Skeptic closed-red: it becomes a phantom seat entry). The spec must pin: (1) return-shape change is a breaking signature change — EV-2's acceptance wording ("loadCouncilConfig returns the parsed theme section") only survives under object-return; the spec must choose object-return vs sibling `loadThemeConfig()`. (2) Merge happens at the JSON-shape level, not the resolved-color level: the omp look depends on `colors.accent` being the var-ref `"amber"`, not the hex; a repo edit to `theme.dark.vars.amber` must transitively recolor `accent`/`border`; merge `vars` and `colors` per-field over the shipped base, var-refs stay unresolved through the merge. (3) Validation vocabulary and off-switch syntax: valid token names = `ThemeColor ∪ ThemeBg` (51 required + 4 optional); valid values = hex / 256-index / var-ref-resolving-to-a-declared-var / `""`; malformed throws naming the file; the off switch needs a concrete shape (spec owns it).

Theme-loader seam (what EV-1/EV-3 cite). Factual correction: EV-3's card says pi exports `getThemeByName`/`loadThemeFromPath`/`initTheme`/`setTheme`/`setThemeInstance`, but the public package index exports only `getLanguageFromPath, getMarkdownTheme, getSelectListTheme, getSettingsListTheme, highlightCode, initTheme, Theme, type ThemeColor`. `setTheme`, `setThemeInstance`, `getThemeByName`, `loadThemeFromPath`, `parseAutoThemeSetting`, `resolveThemeSetting` are NOT on the public surface. The extension can use `ExtensionUIContext`: `ui.theme` (current `Theme`), `ui.getTheme(name)`, `ui.setTheme(theme: string | Theme)`, `ui.getAllThemes()`. Activation must go through `new Theme(fg, bg, mode)` + `ui.setTheme(instance)`, or materialize-to-disk + reload — the spec must pick one; the spec IS that decision.

Drawing/compliance seam (what EV-4 audits). The token-only rule has a boundary today's code already enforces by accident: council-drawn ANSI exists only in `navigator.ts` (`theme.fg(...)`, `theme.bg("customMessageBg")`, `theme.bold`), via `NavTheme = Pick<Theme, "fg"|"bold"|"bg">`; everything else — `renderWidget` (index.ts) builds plain strings into `setWidget("council", lines)`, and `/council-jobs` builds plain strings into `ui.notify(…, "info")` — is pi-drawn. The rule must be two clauses: (a) every color the extension emits itself comes from a `Theme` token via `fg/bg/bold`; no literal hex/ANSI/256-index in council-drawn output; (b) strings handed to `setWidget`/`notify`/`custom` are plain text — styling there is pi's job, never inline ANSI. The widget and `/council-jobs` stay plain text (the `notify` "info"/"warning" kind already styles them).

Docs/wiki-ingest seam. The ingest skill pins a commit and derives pages from `docs/**`, `AGENTS.md`, `README.md`. The spec file is the artifact the wiki page will be derived from, so it must be self-contained: omp source URL/commit, exact token list, exact theme-section shape, activation precedence table, compliance rule. AGENTS.md entry must be a new numbered convention — recommended **9.6**, beside 9.5. README has two existing touch points: the "Git: what to commit" table row for `.council.json` (currently "per-seat model/thinking overrides") and the "How installation works" per-seat-override bullet.

Blind spots. From the config/pipeline slice: a `theme` edit that silently does nothing. From the render/frontend slice: `ui.setTheme`/`ui.theme` are the only public activation surface. From either slice alone: the **name-collision risk** — EV-1 ships `pi-council-dark`/`pi-council-light` assets and EV-3 materializes the configured palette under those same names (two sources of truth, one name).

Reframe. The epic's real risk is **three would-be sources of truth for a single color** — shipped asset (EV-1), scaffold config (EV-2), runtime materialized theme (EV-3) — and EV-2's acceptance "the default scaffold section matches the shipped omp palette" pushes toward a fourth copy of every hex. The spec's job is to declare a single merge chain, not a palette, and to fix the name namespace: shipped asset = the canonical omp base; scaffold config = a delta (`name` + optional `vars`/`colors` per-field overrides), not a full palette dump ("matches the shipped palette" must mean resolves to the same colors after merge because the config is empty); runtime theme = the merge result, constructed in-memory (`new Theme(...)` + `ui.setTheme(instance)`), never a third on-disk copy (if materialize-to-disk, the name must differ, e.g. `pi-council-config-dark`, or the assets stop shipping under those names).

Seam-level correction: EV-3's "explicit settings.json theme choice wins" is currently unfalsifiable because no public API exposes the raw `theme` setting and `parseAutoThemeSetting`/`resolveThemeSetting` are not exported. The spec must pin the detection mechanism — read the settings string (global `getAgentDir()/settings.json` vs project `CONFIG_DIR_NAME/settings.json`, project wins) and the "light/dark auto-follow does not block" parse rule — or the ruling cannot be tested.

Testable claims: (1) var-ref survives merge; (2) fail-fast validation on out-of-union token / undeclared var-ref / bad value, message naming the file; (3) activation precedence as a test — `theme: "gruvbox"` → no-op, `theme: "light/dark"` → council activates, no section → no-op; (4) no-ANSI grep-audit as a test; (5) name-namespace test.

**OWNER POSITION (ROUND 2)**

1. **Activation API surface — principal is correct; R1's import list conceded.** `dist/index.d.ts:29` exports only `initTheme, Theme, type ThemeColor` from theme; the rest are internal-only. Usable surface is `ExtensionUIContext` (types.d.ts:175-184): `readonly theme: Theme`, `getAllThemes()`, `getTheme(name)`, `setTheme(string | Theme)`. The **no-settings-mutation constraint holds via `ui.setTheme(instance)`**: interactive-mode.js:1945-1955 branches on `instanceof Theme` → `themeController.setThemeInstance` → `setGlobalTheme(instance)`, `currentThemeName = "<in-memory>"`, `stopThemeWatcher()`, zero `settingsManager` calls; only the string branch calls `settingsManager.setTheme`. `Theme`'s constructor is public and tolerant — the 4 optional tokens default from siblings, so partial merged maps are legal.

2. Consequences verified: (a) `setThemeInstance` **stops pi's theme file-watcher** and marks the theme `<in-memory>` — hot-reload cannot ride pi's watcher; the spec must make hot-reload the extension's own watcher on `.council.json` that re-runs merge → `new Theme(...)` → `ui.setTheme(instance)`. (b) `Theme` instances are opaque (`fgColors`/`bgColors` private Maps converted to ANSI at construction; `getFgAnsi`/`getBgAnsi` return ANSI, not raw values) — the extension must hold the base JSON and construct a fresh Theme; the merge is data-in/data-out over JSON, never Theme-instance surgery. The base is the shipped asset JSON read from disk (public `getPackageDir`), since it can't be introspected off `ui.getTheme(name)`.

3. **Config shape — vars/colors split required.** `loadCouncilConfig` (seats.ts:103-132) iterates every `council` key through `parseAgentOverride` — a `council.theme` key today parses as an override for a phantom seat named "theme". The spec must pin: `theme` is a reserved key skipped/routed in that loop; the theme parse lives in its own module. Return-shape: choose the **sibling loader** (`loadThemeConfig`), not object-return — verified blast radius: `loadSeat` (seats.ts:179), `scaffold.test.ts:42`, `seats.test.ts:263,270`; a return-shape change breaks all three, a sibling loader breaks none; the theme section has a different validation vocabulary than seat overrides so it shouldn't share a return type. EV-2's acceptance line gets a one-word fix ("loadThemeConfig returns the parsed theme section") at implementation time — the spec outranks the child card's draft wording. Shape: `{ enabled?, variant: "dark"|"light", overrides: { vars?: Record<string,string>, colors?: Partial<Record<ThemeColor|ThemeBg, string>> } }`. The vars/colors split is required because the merge is per-field on each map separately, then vars resolve. Off switch: **absence = off** (matches today's absent-file behavior); `theme: false` and `theme: { enabled: false }` both accepted as explicit off via one falsy/enabled-check expression. Pinned rule: `council.theme` falsy OR `council.theme.enabled === false` ⇒ off; otherwise on.

4. **Name namespace — agree, verified runtime makes it moot.** The in-memory route writes nothing to disk and registers no name (`currentThemeName = "<in-memory>"`), so the materialized theme can never collide with the shipped names in `getAllThemes`. Spec decision: **in-memory, no third on-disk copy**; record the rejected alternative (materialize-to-disk → name must differ, e.g. `pi-council-config-dark`, or EV-1's assets stop shipping under those names).

5. **README placement — both principal touch points are mandates.** README.md:161-162 ("Per-seat model/thinking overrides" bullet) and README.md:181 ("Git: what to commit" row) both describe `.council.json` as containing only model/thinking overrides — both go stale the moment the theme key lands. Spec mandates updating both, plus the card-mandated snippet section. R1's "What you get" suggestion is subsumed.

6. Accept/reject the rest: accept merge at JSON-shape level (verified necessary — opaque instances); accept validation vocabulary = `ThemeColor ∪ ThemeBg`, fail-fast with filename; accept settings-detection mechanism (verified implementable with public symbols only: `getAgentDir()`/`CONFIG_DIR_NAME` exported, settings schema has `theme?: string`); pin: single explicit name ⇒ no-op; `light/dark` pair or absent ⇒ council activates; read global `getAgentDir()/settings.json` and project `CONFIG_DIR_NAME/settings.json`, project wins. Correction to principal's test #3: precedence detection must not read `ui.theme.name` (after any `setTheme(instance)` it's `"<in-memory>"`) — stub the two settings.json files and test the extension's decision function. Accept the two-clause compliance rule; one testability note: a raw grep for `\x1b[` false-positives on navigator.ts:25's own comment — the audit must scan string literals in output-producing paths and whitelist the theme wrapper module. Agree: widget and /council-jobs stay plain text.

**PRINCIPAL POSITION (ROUND 2)**

1. **Activation API surface.** Verified against dist/index.d.ts: public entry exports exactly `initTheme`, `Theme`, `type ThemeColor` from theme.ts — nothing else. The real seam is interactive-mode.js:1945-1956: `getTheme: (name) => getThemeByName(name)`; `setTheme: (themeOrName) => { if (themeOrName instanceof Theme) return this.themeController.setThemeInstance(themeOrName); const result = this.themeController.setThemeName(themeOrName); if (result.success) { ... this.settingsManager.setTheme(themeOrName); } return result; }`. The no-settings-mutation constraint survives only through `ui.setTheme(instance)` (routes to setThemeInstance, `activeThemeName = "<in-memory>"`, never touches settingsManager); the string path rewrites the consumer's settings. `ui.getTheme(name)` is the extension-reachable equivalent of getThemeByName. **No `loadThemeFromPath` equivalent on `ui`** — package themes must be REGISTERED first via the `resources_discover` event returning `ResourcesDiscoverResult.themePaths` (types.d.ts:413, wired in runner.js:895-927, registered in resource-loader.js:233-252 `updateThemesFromPaths`). So the spec must tell EV-1 to declare `themePaths` (or a `themes/` dir / `pi.themes` manifest entry) and EV-3 to `ui.getTheme(name)` + `ui.setTheme(instance)`.

2. **Raw-settings detection — reading settings.json yourself is the only way.** `ExtensionContext` (types.d.ts:208-244) exposes `ui`, `mode`, `hasUI`, `cwd`, `sessionManager` (read-only), `modelRegistry`, `model`, `scopedModels`, `thinkingLevel`, helpers — **no settings accessor of any kind**. `ui.theme` is the already-resolved `Theme`. Detecting the raw `"light/dark"` pair means EV-3 reads and parses `getAgentDir()/settings.json` off disk, bypassing the SettingsManager schema. Precision fix: `resolveThemeSetting` only *collapses* the pair form `"light/dark"`; literal `"light"`/`"dark"` pass through **unchanged**.

3. **Config shape.** Adopt the flat `{ enabled, variant, overrides }`. Refinements: `overrides` keyed by token name is the right consumer surface; do NOT require vars/colors separation in `.council.json` (over-engineering for a recolor use case); but override values may be var-refs which resolve against the *shipped* theme's `vars` — the merge is "resolve shipped vars → apply per-token overrides, with shipped vars still in scope for override var-refs", not a flat `colors` merge. `variant: auto|dark|light` is correct and should NOT mirror pi's `lightTheme/darkTheme` pair — different mechanisms; the council never writes the pair into settings.

4. **Name namespace — in-memory only.** The override-merged instance is installed via `ui.setTheme(instance)`, recorded as `activeThemeName = "<in-memory>"` — no name, and that's correct. No merged file is ever written to disk. Binding choice for the spec: EV-1 ships exactly two files, `pi-council-dark` and `pi-council-light`; EV-3 activates by building a nameless in-memory `Theme` (shipped file + overrides) and installing it; the string `pi-council` is a reserved family selector that appears only in `.council.json` prose and never as a theme name (so `pi-council` can never be a settings value — no theme by that name exists and the council never writes settings).

5. **README placement.** "What you get" subsection is the right primary home; the Git-table row (README.md:181) must be updated regardless (goes stale when EV-2 lands); drop the "How installation works" bullet as redundant; fold the precedence line into the subsection.

6. **Accept/reject the rest.** Accept provenance (commit-pin, 67/66 asymmetry, 51-token decomposition, value classes, brand anchors, pythonMode≠bashMode trap). Schema mapping: accept the three-part appendix; **reject one item in the "stays open" list** — the shipping mechanism is not open: docs/themes.md already documents "Packages: `themes/` directories or `pi.themes` entries in `package.json`", and there is additionally the `resources_discover → themePaths` extension mechanism; name both and let EV-1 pick; don't record "unknown". Accept top-level `theme` sibling, presence-implies-enabled, the two sanctioned off-switches, the honest "auto reaches fresh installs only" narrowing. Accept surface-compliance in full — the NavTheme Pick seam and comment-only ANSI escape both check out.

7. **FOUR-STATE ACTIVATION REFRAME (the big one).** pi's `applyFromSettings` (theme-controller.js:38-54) **persists a detected `"dark"`/`"light"` into settings.json on high confidence**. So a literal `"dark"`/`"light"` in settings.json is a concrete built-in theme selection, and under "concrete wins" it BLOCKS council activation — narrowing "auto via scaffold" to "fresh installs whose settings.json still has an unset theme", and even fresh installs that ran pi once already have `dark`/`light` persisted. The spec's activation table must enumerate FOUR states: (a) unset → activate; (b) pair `"light/dark"` → activate (detected raw); (c) literal `"dark"`/`"light"` → **block** (concrete); (d) any other concrete name → block. The README precedence line must say "a concrete theme in settings.json — including built-in `dark`/`light` — wins" rather than only "light/dark auto-follow does not block", because the auto-follow pair is the rare case and the literal is the common one. Whether the resulting narrow "auto" is acceptable is a product-owner judgment, not the principal's — but the spec must not promise an auto-activation that pi's own persistence defeats.

**SKEPTIC REPORT (STEP 4)**

Objection 1 — omp provenance: 67 keys dark / 66 keys light (verified via fetch, commit eab72e88e447a4be45bea2bc302995844c0c51a2); dead keys dark 16 (`link`, `pythonMode`, `statusLine*` ×14), light 15 (same minus `link`); 67-16=51, 66-15=51 live == exactly pi's 51 required tokens; zero 256-index integers in live set (integers only in dead statusLine*); empty-string defaults text/userMessageText/customMessageText/toolTitle confirmed; pythonMode(yellow #e4c00f) ≠ bashMode(cyan #0088fa) dark — not aliases; export {pageBg,cardBg,infoBg} confirmed; dark accent #febc38, border #178fb9, borderAccent #0088fa, customMessageLabel #b281d6, mdCodeBlock #9CDCFE; light accent #5a8080, border #547da7. **closed-green.** Pivotal nuance: pi's own shipped themes already have 55 keys (51 required + 4 optional) — omp source is upstream provenance, pi ships trimmed files; spec must distinguish "omp source provenance" from "pi's actual shipped theme files".

Objection 2 — public API surface: dist/index.d.ts:29 exports only `getLanguageFromPath, getMarkdownTheme, getSelectListTheme, getSettingsListTheme, highlightCode, initTheme, Theme, type ThemeColor`; getThemeByName/loadThemeFromPath/setThemeInstance/parseAutoThemeSetting/resolveThemeSetting NOT present. **closed-green.**

Objection 3 — ExtensionUIContext: types.d.ts:165-179 has `readonly theme: Theme`, `getAllThemes()`, `getTheme(name)`, `setTheme(string|Theme)`; interactive-mode.js:1945-1956 instanceof branch → setThemeInstance (no persist, activeThemeName="<in-memory>", stops watcher); string branch → setThemeName + settingsManager.setTheme (persists). **closed-green.**

Objection 4 — resolveThemeSetting (theme.js:541-551): pair "light/dark" collapses to terminal-resolved light or dark; literal strings pass through unchanged; otherwise undefined. **closed-green.**

Objection 5 — applyFromSettings (theme-controller.js:27-50): no auto theme, no themeSetting → detect terminal background → `if (detection.confidence === "high") { settingsManager.setTheme(detection.theme); await settingsManager.flush(); }` — CONFIRMED persistence. **closed-green** — load-bearing for the four-state reframe.

Objection 6 — resources_discover → themePaths: ResourcesDiscoverResult { themePaths?: string[] } (types.d.ts:413-414); updateThemesFromPaths (resource-loader.js:233-252); setRegisteredThemes called at session start (interactive-mode.js:403). **closed-green.**

Objection 7 — Theme constructor tolerates partial maps: fg fallbacks thinkingMax→thinkingXhigh, searchMatchText→text; bg fallbacks scrollbarThumb→selectedBg, searchMatchBg→selectedBg (theme.js constructor + theme.d.ts:16 Partial types). **closed-green.**

Objection 8 — loadCouncilConfig with a `theme` key: seats.ts:134-135 iterates ALL entries under `council` — `{"council": {"theme": "dark"}}` parses as a phantom seat override `{"theme": {"model": "dark"}}`; `{"council": {"theme": {"enabled": true}}}` (no model/thinking) → `parseAgentOverride` returns `{}` — empty override for phantom seat "theme". **closed-red** — the "silently dropped" claim is inaccurate; it creates a phantom seat entry, harmless but confusing. The `loadThemeConfig()` sibling-loader fix avoids this entirely; the spec must describe current behavior accurately.

Objection 8b — ANSI only in extensions/ is inside a comment (navigator.ts:25-26). **closed-green.**

Objection 8c — README touch points confirmed: README.md:161-162 "Per-seat model/thinking overrides" bullet (under "How installation works"), README.md:181 Git-table row. **closed-green.**

Objection 8d — AGENTS.md has zero theme/Theme entries currently. **closed-green.**

Objection 8e — scaffold non-clobbering: `if (fs.existsSync(dst)) { result.skipped.push(childRel); }` never overwrites. **closed-green.**

Objection 9 — validate.py clean (exit 0); docs-only changes keep it clean. **closed-green.**

Open-untested (spec prescriptions, falsifiable only after implementation): (1) four-state activation table; (2) merge semantics at JSON-shape level; (3) token-only drawing rule as normative prescription; (4) settings.json raw detection; (5) name namespace in-memory-only; (6) hot-reload via extension's own watcher.

Verdict: no open objections that block the card. The one closed-red (phantom seat entry) is a minor inaccuracy in the description of current behavior, not a defect in the proposed spec.

### Consolidator synthesis (step 5, verbatim)

**SETTLED (assert as fact in the step-7 spec)** — closed-green factual claims (Skeptic step-4, all verified against sources): omp provenance (source github.com/can1357/oh-my-pi packages/coding-agent/src/modes/theme/{dark,light}.json, commit eab72e88e447a4be45bea2bc302995844c0c51a2; dark 67 keys / light 66; dead set dark-16 / light-15 (link dark-only, pythonMode both, statusLine* ×14 both); 67−16 = 66−15 = 51 live tokens == exactly pi's 51 required; zero 256-index in live set; empty-string defaults text/userMessageText/customMessageText/toolTitle; pythonMode ≠ bashMode, drop don't rename; brand anchors verified; export maps 1:1 to pi's optional export section); pivotal provenance nuance — pi's own shipped theme files carry 55 keys (51 required + 4 optional), omp is upstream provenance only, spec must distinguish "omp source provenance" from "pi's actual shipped theme files"; public API surface (package index exports only initTheme, Theme, type ThemeColor; the extension reaches themes only via ExtensionUIContext ui.theme / ui.getAllThemes / ui.getTheme / ui.setTheme(string|Theme)); no-settings-mutation constraint (ui.setTheme(instance) → setThemeInstance, in-memory, activeThemeName="<in-memory>", stops file-watcher, zero settingsManager calls; only the string branch persists); resolveThemeSetting semantics (collapses pair "light/dark", passes literal "light"/"dark" through unchanged); applyFromSettings persistence (theme-controller.js persists detected dark/light into settings.json on detection.confidence === "high" — load-bearing for the four-state reframe); resources_discover → themePaths registration (ResourcesDiscoverResult.themePaths?: string[], updateThemesFromPaths at session start — package themes reach the extension through this event, not a public loadThemeFromPath); Theme constructor tolerates partial maps (4 optional tokens fall back from siblings: thinkingMax→thinkingXhigh, searchMatchText→text, scrollbarThumb→selectedBg, searchMatchBg→selectedBg); surface compliance today (only ANSI in extensions/ is inside navigator.ts:25-26 comment; NavTheme = Pick<Theme,"fg"|"bold"|"bg">; widget and /council-jobs hand pi plain text); README touch points confirmed (README.md:161-162 bullet, README.md:181 Git-table row, both go stale when the theme key lands); AGENTS.md has zero theme entries today (new convention slot — both seats propose 9.6); scaffold non-clobbering confirmed; validate.py clean on docs-only changes. Closed-red (one) and consequence: the "silently dropped" description of a council.theme key is inaccurate — loadCouncilConfig (seats.ts:134-135) iterates every council entry through parseAgentOverride, so {"council":{"theme":"dark"}} becomes a phantom seat override {"theme":{"model":"dark"}} and {"council":{"theme":{"enabled":true}}} becomes an empty override for phantom seat "theme" — harmless but confusing; spec must describe current behavior accurately, and the proposed loadThemeConfig() sibling loader (not a return-shape change on loadCouncilConfig) is the agreed fix (return-shape change rejected on blast radius: loadSeat, scaffold.test.ts:42, seats.test.ts:263,270).

Recorded rulings (binding): NAME-1 (family pi-council, variants pi-council-dark/pi-council-light, pi-council is a family selector in prose only, never a theme name); EV-3 activation decision; EV-2 shape delegation; CI-1/MERGE-1.

Working agreements both seats converged on (settle into the spec as design): theme is a top-level sibling of council, a reserved key skipped/routed in the loadCouncilConfig loop, parsed by a separate loadThemeConfig(); activation in-memory only (ui.getTheme + build merged Theme + ui.setTheme(instance), no third on-disk copy, no name registered — materialize-to-disk rejected or name must differ e.g. pi-council-config-dark); merge at JSON-shape level, never Theme-instance surgery (instances opaque; base = shipped asset JSON read from disk); activation at session_start alongside initHubIdentity/pruneRuns/renderWidget (index.ts:109-135); raw-settings detection is the only path (ExtensionContext exposes no settings accessor; EV-3 must read getAgentDir()/settings.json + CONFIG_DIR_NAME/settings.json off disk, project wins, detect the "light/dark" pair raw); compliance rule two clauses (a: every color the extension emits comes from a Theme token via fg/bg/bold, no literal hex/ANSI/256-index in council-drawn output; b: strings handed to setWidget/notify/custom stay plain text); README must update both touch points plus a "What you get" theme-customization subsection. Open design detail left to EV-2 (record as proposal, not settled): overrides representation — owner proposed vars/colors split, principal proposed flat overrides keyed by token name with values possibly var-refs resolving against shipped vars; EV-2 owns the final call.

**OPEN JUDGMENT #1 (for a ruling seat — the single blocker):** the four-state activation narrowing. Exact question: Is the narrowing of "auto via scaffold" to "fresh installs whose settings.json theme is still unset (or holds the "light/dark" pair)" acceptable — forced by pi's applyFromSettings persisting a literal "dark"/"light" into settings.json on high-confidence terminal detection — or should built-in dark/light literals be treated like the "light/dark" auto-follow pair (i.e., NOT blocking council activation), contradicting the literal reading of the EV-3 "concrete theme name in settings.json wins" ruling? Positions: Side A (principal's reframe, literal ruling reading): a persisted literal dark/light is a concrete built-in theme selection and blocks; activation table must enumerate four states (a unset→activate, b pair→activate detected raw, c literal dark/light→block, d any other concrete→block); README precedence line must say "a concrete theme in settings.json — including built-in dark/light — wins"; auto reaches only fresh installs that never ran pi; spec must not promise an auto-activation pi's own persistence defeats. Side B (implied by the question, adopted by no seat): treat built-in dark/light literals as the auto-follow pair — not blocking — because they are pi's detected defaults, not deliberate consumer choice; "concrete wins" applies to consumer-chosen concrete names only. The owner did not take a position on acceptability; the principal explicitly disclaimed it; the Skeptic's closed-green settled the fact of the narrowing, not its acceptability. **OPEN JUDGMENT #2 (minor, same routing):** README "How installation works" per-seat-override bullet (README.md:161-162) — update vs drop as redundant (owner: update; principal: drop; no test settles it).

**OPEN OBJECTIONS (six, all spec prescriptions falsifiable only post-implementation — all six must be recorded in the step-7 spec as prescriptions, not deferred):** (1) four-state activation table — the form is a prescription, but the content of state (c) is open judgment #1 and cannot be asserted as settled; (2) merge semantics at JSON-shape level — prescription anchored by var-ref-survives-merge and fail-fast-validation testable claims; vars/colors-split vs flat-overrides sub-detail owned by EV-2; (3) token-only drawing rule as normative prescription (two clauses) — today-compliance settled, rule itself prescribed in AGENTS.md 9.6 and the spec, audit-whitelist nuance recorded (raw \x1b[ grep false-positives on navigator.ts:25 comment), audit test deferred to EV-4; (4) settings.json raw detection — implementability settled (no public settings accessor), spec must prescribe raw-disk-read or the EV-3 ruling is untestable — not deferrable; (5) name namespace in-memory-only — record as prescription plus the rejected materialize-to-disk alternative; (6) hot-reload via the extension's own watcher on .council.json (setThemeInstance stops pi's watcher — settled fact) — prescribe re-run merge → new Theme → ui.setTheme(instance), implementation deferred to EV-4.

**Step 6 routing:** open judgment #1 (and #2) → ruling seat (product-owner, escalating to steward). Spec-writing at step 7 holds on #1 only; everything else in the spec can proceed.

### ESCALATION ruling (step 6, binding, appended verbatim)

=== BINDING RULING (product-owner, via ESCALATION resumption) ===

RULING Q1 (activation): Side B — a persisted literal `"dark"` / `"light"` in `settings.json` is NOT treated as a deliberate concrete theme selection and does NOT block council activation. The activation table has four states, but state (c) is activate, not block:

| State | settings.json `theme` raw value | Council action |
|---|---|---|
| (a) | unset | **activate** (auto-detect terminal) |
| (b) | `"light/dark"` (the pair) | **activate** (raw auto-follow) |
| (c) | literal `"dark"` or `"light"` | **activate** (auto-detection carve-out extends to its persisted form) |
| (d) | any other concrete name | **block** (consumer's explicit pick wins) |

README precedence line: "A non-built-in concrete theme in settings.json (e.g. `gruvbox`) wins; the auto-follow pair and a persisted literal built-in `dark`/`light` — pi's recorded auto-detect — do not block council activation. To turn the council theme off, remove the `theme` section from `.council.json` or set `theme.enabled: false`."

RULING Q2 (README economy): Owner's call — update the bullet. Keep README.md:161-162 ("Per-seat model/thinking overrides" under "How installation works"), update it to enumerate both the `council` seat-override block AND the new top-level `theme` key. Update the README.md:181 Git-table row (mandated regardless). Add the "What you get" theme-customization subsection per EV-5's card intent.

=== END BINDING RULING ===

**Step 7 resumption note:** open judgment #1 settled by the ruling above (state (c) = activate, Side B); open judgment #2 settled (update the bullet, keep README.md:161-162). Spec-writing proceeds per the settled design.
