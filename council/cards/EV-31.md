---
id: EV-31
title: Durable usage store outside the pruned run directory, with session provenance
state: Done
owner: null
epic: EPIC-7
goal: A completed invocation writes a usage record into a durable store that survives the pruned run directory being deleted, the record names the session JSONL path and the entry range it was derived from, and a test that removes the run directory after the write still reads the record back with its provenance pointer resolving to the surviving session file.
---

## Intent

The existing run directory is ephemeral telemetry — self-gitignored and pruned
to the last 15 runs at every parent `session_start` — so a usage record stored
there would be silently lost. This card writes a durable record that outlives
that prune. The council's answer to the human's traceability question is
embodied here: the record names the session JSONL path and the entry range it
was derived from, and read-back resolves that pointer to the surviving session
file, which is not under the run directory and therefore survives pruning. The
store lives at `getAgentDir()/council/usage/`, the durable council-state home
that already carries the 0600 atomic-write precedent; the human's proposed
`~/.pi-council` top-level home is not used. A person inspects the directory
directly, so it must be recognisable by naming and must state its empty and
unwritable behaviour.

## Acceptance

- A completed invocation's record is written, the run directory is then removed,
  and the record still reads back with its provenance pointer resolving to the
  surviving session file.
- The record names the session path and the entry range.
- `pruneRuns(repoRoot, 0)` does not remove the record.
- `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` stay green.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-3 store** — the durable store is `getAgentDir()/council/usage/`;
  record files are named `<ISO-basic>_<runId>_<command>.json`; the directory
  carries a `README.md` naming the format; directory mode `0700`, file mode
  `0600`.

## Run record

### Step 1 — classification (facilitator)

- **Path: full council.** Two independent triggers: (a) *spec-ambiguity* —
  the card's `goal` names a durable store and a provenance pointer but not
  where the write is invoked, how the store root is injected so tests never
  touch `~/.pi/agent/` (the Test-environment constraint), what the write does
  when no invoking-session JSONL resolves, or whether a compute time is
  stamped on the store key — more than one reasonable design reaches each; and
  the store-side handoff from EV-30 explicitly leaves the persistence
  idempotence and compute-basis decision (choose-once semantics) to this card.
  (b) *design judgment* — a real tradeoff exists between an exit-time
  write-once record and a store that can recompute, and between treating an
  already-pruned seat session as an error, a null pointer, or a deferred
  resolution; EV-30's step-6 ruling item 6 assigns exactly that basis decision
  here.
- **Surface-touching: yes.** The card states "a person inspects the directory
  directly" — the store's `README.md` is user-visible copy that "must state
  its empty and unwritable behaviour" (an empty state and an error state), and
  the record filename is a naming convention a person reads. That is
  user-visible copy plus an empty state plus an error state, by council.md
  step 1's definition. Seating: `owner`, `principal` and `designer` as the
  three generators in steps 2–3.

### Step 2 — independent first pass (round 1, generators only)

Each seat received the card alone — no other position, no hint. Positions
appended verbatim, labeled by seat. Jobs `job-12.1` (owner), `job-12.2`
(principal), `job-12.3` (designer).

#### Position — `owner`

## Owner — EV-31 first pass (design position)

### Approach

EV-31 is a small, honest persistence layer behind the already-settled EV-30 seam: a new module `extensions/usage-store.ts` (the I/O sibling of the pure `spend.ts`, following the `extensions/mcp/auth-store.ts` pattern — `getAgentDir()/council/usage/`, atomic tmp+rename, 0600 files, 0700 dir, README) that wraps a `SpendRecord` verbatim with a store-side provenance pointer and basis stamps, writes once per invocation, and offers a read-back that resolves the pointer against the surviving invoking-session JSONL. The invoking session's path comes from `ctx.sessionManager.getSessionFile()` (public on `ReadonlySessionManager`, `session-manager.d.ts:140`) — the file lives in pi's session dir (`~/.pi/agent/sessions/<encoded-cwd>/…`), not the run dir, so it survives `pruneRuns` by construction (wiki: [[run-transcripts]]). The write trigger is invocation completion at the earliest of forest-settle (hub `onChange`) or `session_shutdown` sweep — choose-once per marker, never recomputed.

**Stored record shape** (wraps, never mutates, the EV-30 record — ruling item 6: basis belongs store-side):

```ts
// extensions/usage-store.ts
export interface UsageProvenance {
  sessionId: string;
  sessionPath: string | null;     // absolute; null = unresolvable at write (never fabricated)
  firstEntryId: string | null;    // from SpendBoundary
  lastEntryId: string | null;
  boundaryResolved: boolean;      // mirrors SpendBoundary.resolved
  pointerStatus: "ok" | "no-session-file";
}
export interface StoredUsageRecord {
  spend: SpendRecord;             // EV-30 record byte-verbatim
  provenance: UsageProvenance;
  runId: string; command: string; repoRoot: string;
  writtenAt: string;              // ISO-8601 — the ruling-item-6 basis stamp
  basis: { trigger: "forest-settle" | "session-shutdown"; manifestsObserved: number };
}
export function defaultUsageStoreRoot(): string;              // getAgentDir()/council/usage
export function usageRecordName(writtenAtMs: number, runId: string, command: string): string;
export function persistInvocationUsage(storeRoot: string, input: {
  spend: SpendRecord; sessionId: string; sessionPath: string | null;
  runId: string; command: string; repoRoot: string;
  trigger: "forest-settle" | "session-shutdown"; manifestsObserved: number;
  now?: () => number;   // injectable clock, for the monotonic-write-clock test
}): { file: string; record: StoredUsageRecord };
export function readUsageRecords(storeRoot: string): StoredUsageRecord[]; // corrupt → skip (readManifests precedent)
export function resolveProvenance(pointer: UsageProvenance):
  { status: "ok" | "session-missing" | "range-missing"; sessionPath: string | null };
```

**Filename** (R-3, byte-exact): `<ISO-basic>_<runId>_<command>.json` where ISO-basic = `toISOString()` collapsed to `YYYYMMDDThhmmssSSS` (milliseconds kept — ISO 8601 basic permits fractional seconds; they're the collision margin). `runId` is already filename-safe (`mintRunId`), commands are `[a-z-]`. `README.md` names the format, is written non-clobberingly at first use at 0644 (a human-readable doc, deliberately not 0600; record files are 0600, dir 0700 via `mkdirSync` + explicit `chmodSync` so the mode holds regardless of umask).

**Write path**: index.ts's procedure handler already stamps the marker (`index.ts:227`) and today discards the returned id — EV-31 keeps it in a per-session pending ledger `{command, markerId, runId, sessionFile}`. On each hub change, for each pending invocation whose forest (point-in-time filter over `readManifests`) is fully settled → compute `spendRecord` at that moment, persist once, mark done. On `session_shutdown`, sweep every still-pending marker (loop `findLatestInvocationMarker`, chopping the scanned tail per marker), same path. In-memory choose-once set keyed on `markerId`; a filename-key collision (only possible as a genuine double-write given the monotonic clock below) **throws and is caught per-record** in the sweep — reported via `notify`/`console.error`, never crashes teardown, never overwrites, never skips silently.

**Read-back / provenance resolution**: `resolveProvenance` parses `pointer.sessionPath` with pi's exported `parseSessionEntries` (already the test-fixture substrate in `test/spend.test.ts:93`) and asserts both `firstEntryId` and `lastEntryId` exist among entry ids. Entry ids are stable under append and compaction (append-only JSONL; `appendCompaction` re-parents, never rewrites), so a file that grows after the write still resolves. Missing file → `session-missing`; ids absent → `range-missing`.

### The three required decisions

1. **Seat session file already pruned at write time.** The pointer never targets seat sessions — they live under the run dir (`hub-tools.ts` spawns children with `sessionDir: dir`), their usage is already folded into the exit-time `subtree` sum, and the card's provenance pointer is the *invoking* session. If the run dir is pruned before the write, `spendRecord` already degrades to subtree-zero (`test/spend.test.ts` T4) and EV-31 persists that record unchanged — **never recomputing** — with `basis.manifestsObserved` and `trigger` stamped so a reader sees the moment and its horizon. No recompute basis beyond that stamp exists because EV-31 never recomputes (ruling item 6).

2. **Compute time on the store key.** The key carries R-3's ISO-basic write timestamp only; the compute-time/basis honesty lives **inside** the record (`writtenAt`, `basis.trigger`, `basis.manifestsObserved`). One twist the R-3 name forces: a shutdown sweep can emit several same-`(runId, command)` records within one `Date.now()` millisecond, so the sweep uses a **monotonic store-side write clock** (`writtenAt = max(now, prev + 1)` — injectable `now` makes this testable). Each stamp is "at or within this millisecond", documented in the README.

3. **No invoking-session JSONL resolvable** (`getSessionFile() === undefined`, e.g. in-memory sessions). Write anyway with `sessionPath: null`, `pointerStatus: "no-session-file"`. Rationale: EV-30's fail-closed precedent applies to *values*, not existence — silently dropping the write would lose the invocation's very record (the eval-store lesson: "silent loss makes the ledger less honest, not more", wiki [[eval-store-contract]]), and EV-32's R-5 demands explicit states over blanks. Never fabricate a path, never skip silently. Same rule for an unresolved boundary: zero-half record with `boundaryResolved: false` is persisted.

### Edge cases

- Two invocations of the same command in one session share the hub `runId` → distinct markers, distinct records; the monotonic clock is what keeps their R-3 keys distinct in a same-ms sweep.
- Headless teardown mid-forest: the shutdown sweep persists with whatever settled; `basis.trigger: "session-shutdown"` says so. A hard crash persists nothing — honest.
- Corrupt/partial record on read → skipped (`readManifests` precedent); `resolveProvenance` on a corrupt session file → `range-missing`/`session-missing`, never a throw.
- Retention: the store grows unboundedly by design (durable = not pruned); a retention/compaction card is explicitly *forward*, not in EV-31's scope.

### Tradeoffs accepted

- **Forest-settle exit undercounts the parent's post-settle tail**: the record's own-session range stops at the leaf at compute time. Accepted because the basis stamp makes the range explicit (the human's traceability question is answered by the pointer, not by implied completeness), and the alternative — write only at session end — starves EV-32's command-exit blocks of a store record to read.
- **Wiring (ledger + hub onChange + sweep) rides in this card**, slightly past the store module: the goal is behavioral ("a completed invocation writes"), and a store nobody calls is dead code. Kept lean: one pending-ledger list, one composed hub callback.
- **Throw-on-key-collision instead of overwrite**: no silent supersede; the monotonic clock makes a collision bug-shaped, and loud is the house style.
- README at 0644 vs the ruling's "file mode 0600": record files are the 0600 subjects; the README is human-facing documentation. Stated here so the gate argument is settled before implementation, not during it.

### Testable claims (falsifiable tests, `test/usage-store.test.ts`, all mkdtemp-rooted per AGENTS.md)

1. **T-U1 — the acceptance, end to end.** Real session JSONL in a tmp session dir + manifests in a tmp repo run dir → `persistInvocationUsage` → `fs.rmSync` the run dir → `readUsageRecords` returns the record → `resolveProvenance` → `status: "ok"` with both ids found. Fails any design that stores provenance into the run dir.
2. **T-U2** — `pruneRuns(repoRoot, 0)` prunes all runs and leaves the record byte-identical.
3. **T-U3** — fresh store: dir mode `0700`, record file mode `0600` (asserted, per dispatch constraint).
4. **T-U4** — README exists, names the R-3 format, and a second persist never rewrites it (non-clobber, convention-6 spirit).
5. **T-U5** — `usageRecordName` byte-exact against the R-3 pattern (ms-precision ISO-basic pinned).
6. **T-U6** — `sessionPath: null` input → record still written, `pointerStatus: "no-session-file"`, no path fabricated.
7. **T-U7** — unresolved boundary → zero both halves, `boundaryResolved: false`, persisted.
8. **T-U8** — run dir removed *before* the write → record persisted with exit-time (zero) subtree, `manifestsObserved: 0` stamped, pointer still resolves.
9. **T-U9** — choose-once: persisting the same marker twice is a no-op; a direct duplicate-key write throws; no file is ever overwritten.
10. **T-U10** — with `now` frozen, a two-record sweep yields two distinct monotonic `writtenAt` values and two distinct filenames.
11. **T-U11** — `resolveProvenance`: deleted file → `session-missing`; file present, ids absent → `range-missing`.
12. **T-U12** — EV-30 record purity: the persisted `spend` half equals the in-memory `SpendRecord` `toEqual`, and `SpendRecord` itself gained no fields (`!("provenance" in spendRecord(...))`).

**Rejected alternatives:** `~/.pi-council` top-level home (R-3 names `getAgentDir()/council/usage/`; card intent says so explicitly); storing under the repo (`$CONFIG_DIR_NAME/council/usage/`) — inside the consumer tree consumers prune, and cross-repo postmortems lose a single inspection point; one append-only JSONL log instead of record-per-file — breaks the R-3 name and races concurrent writers; recompute-at-persist or recompute-at-read — violates ruling item 6's write-once assignment; env-var injection as the primary test seam — parameter-with-default matches the auth-store precedent and keeps the real code path under test; skipping writes on unresolvable provenance — silent loss, rejected above.

Gates on the implementation tree: `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py`, plus `council/preflight.sh` per convention 13; the data-import and boot/health gates don't apply to this package — the binding gate document for this repo is the three-command green set plus preflight.

#### Position — `principal`

## Principal position — EV-31

**Grounding:** `vault/wiki/index.md` + `vault/wiki/run-transcripts.md`; the wiki has no page on the spend/usage store (EV-28/30 live only in cards and `docs/superpowers/specs/2026-09-11-EV-30-design.md`), so I read the code and specs directly. Files opened: `extensions/spend.ts`, `extensions/runs.ts`, `extensions/index.ts`, `extensions/hub-tools.ts`, `extensions/dispatch.ts`, `extensions/child.ts`, `extensions/seats.ts`, `extensions/mcp/auth-store.ts`, pi's `agent-session.js`/`session-manager.d.ts`/`extensions/types.d.ts`, `council/cards/EV-30.md`.

### Cross-seam reading — what the contract actually is right now

- **There is no invocation-completion seam wired.** `spendRecord` is called only from `test/spend.test.ts`; production has exactly one EV-30 call site, `recordInvocationBoundary` at `index.ts:227`, which stamps the marker *before* the send. The card's "a completed invocation writes" names an event that does not exist in the tree. Extension side, `index.ts` registers `session_start` (144), `turn_end` (186), `session_shutdown` (187) — **no `agent_settled`**, though pi emits it: `agent-session.js:347-352` fires it once per `_runAgentPrompt` after retries/compaction/queued continuations, awaited (`await this._extensionRunner.emit`), before `waitForIdle()` resolves. So `agent_settled` is the only event that is (a) per-invocation, (b) post-settle, and (c) awaited before headless teardown. `turn_end` fires per tool-loop turn (N times per invocation); `session_shutdown` fires per session, not per invocation. The card must name this seam or it is unimplementable as stated.
- **Provenance path is available but only for on-disk sessions.** `ctx.sessionManager.getSessionFile()` is in `ReadonlySessionManager` (`session-manager.d.ts:140,209`) and returns `undefined` for `SessionManager.inMemory` (the substrate EV-30's T11 uses). `getSessionId()` exists but is not a path.
- **The store's injection precedent already exists.** `auth-store.ts:22-33` uses `path.join(getAgentDir(),"council",...)` with a default-parameter file path, and `test/mcp/oauth.test.ts:20-21` isolates via `PI_CODING_AGENT_DIR` (pi `config.d.ts:72,78`, read at call time). The dispatch's "injectable root" is already solved by the repo's own pattern; a second mechanism (a bespoke root param) would be a new convention.
- **The card's survival premise is false for a named invocation class.** `hub-tools.ts:184-185` and `dispatch.ts:108` pass `sessionDir: dir` where `dir = ensureRunDir(repoRoot, runId)` (`runs.ts:101`); `seats.ts:517` turns that into `--session-dir`. A seat child's session — including a council-runner's own — is *inside* the pruned run dir. The card says the session "is not under the run directory and therefore survives pruning." True for an interactive parent; false for the autonomous runner. `pruneRuns` (`runs.ts:164`) deletes exactly that directory.
- **Provenance covers only one of the record's two halves.** EV-30's `SpendBoundary` carries `firstEntryId`/`lastEntryId` for the **ownSession** half only (`spend.ts:17-30,137-201`); the subtree half is summed from `RunManifest.usage` (`spend.ts:181-193`). The card's "the entry range it was derived from" therefore does not explain the subtree number, whose entire basis (manifests + seat sessions) is what pruning removes.

### Blind spots, named separately

- **Store/owner slice** — sees a `ctx` and a settle after which the leaf is stable; cannot see that "a completed invocation" has no per-invocation event today, so it will reach for `turn_end` (fires N times) or `session_shutdown` (never fires in a live TUI session). It also cannot see, from inside the write, that the session path it stores is the very file the runner's run dir contains.
- **Exit/wiring slice** — sees the marker and the manifests; cannot see read-back semantics: a dangling provenance pointer must be a *stated* outcome, not a thrown error, and the store is a global (`getAgentDir()`) while the paths it stores are project- and run-local.
- **EV-32 render slice** — sees a number and a "session path + range" pointer; cannot see that the pointer legitimizes only the own-session half, so a rendered total will look fully traceable when half its basis is pruned. This is the cross-seam failure the successful acceptance test would hide.

### Reframe

**The durable unit is the invocation identity, and provenance resolvability is a recorded property, not an assumption.** Concretely:

1. **Write seam = `agent_settled`**, gated on an un-consumed on-chain `council-invocation` marker. Choose-once = the R-3 filename's `<ISO-basic>` is the **marker's `at`** (the invocation's own clock), not wall-clock compute time; the write then refuses to overwrite an existing file. This makes the key stable across recompute and makes the "second settle with the same still-on-chain marker" (any later plain TUI turn) a no-op. Stamping a *compute* time on the key would silently mint a duplicate record per recompute — reject that reading of R-3.
2. **The record states its provenance class**: `sessionPath` plus a derived `resolvable` (does the path exist *and* is it outside `runsDir`?), and states that the range explains the own-session half only. Read-back never throws; a pruned/dangling pointer reads back as the record with `resolvable:false`.
3. **When no session file is resolvable** (in-memory ctx, or `getSessionFile()` undefined): write the usage record with `sessionPath: null` + reason, or skip — but *state which*, because "drop the numbers" is worse for the traceability question than an explicit null pointer. R-3 does not cover this edge; it is EV-31's to rule.
4. **The acceptance test must include the runner-shaped case**: session file *inside* `runDir`, record written, `pruneRuns(repoRoot,0)`, read-back shows `resolvable:false` and the record survives. The card's current test (session outside the run dir) is a tautology — it constructs the survival it claims to prove and never tests the class that motivates durability.

The store module itself can still be a clean, small, pure-ish `write`/`read` pair; the reframe is about what the record *claims*, not about adding machinery.

### Testable claims

1. **Double-write on the settle seam:** build a session with one `council-invocation` marker, invoke the settle-write twice with no new marker. Claim: naive `findLatestInvocationMarker` + write produces two files (or two overwrites); keying on marker `at` + file-exists produces exactly one, byte-identical on the second call.
2. **Seam multiplicity:** in an invocation with ≥2 tool-loop turns, a `turn_end`-driven write writes ≥2 records; an `agent_settled`-driven write writes exactly 1. (Source-grounded today; a probe can count emissions via a fake agent loop without network.)
3. **Runner provenance dangle:** write a session file at `<repoRoot>/.pi/council/runs/<runId>/s.jsonl`, write the record referencing it, call `pruneRuns(repoRoot,0)`, then `readRecord`. Claim: `fs.existsSync(path)===false` and read-back returns the record with `resolvable:false` and does not throw. (Contrast the card's test, where the file is outside `runDir` and `resolvable:true`.)
4. **In-memory ctx:** drive the write with `SessionManager.inMemory()` (T11 substrate). Claim: `getSessionFile()===undefined` and the write path takes the stated unresolvable branch rather than throwing or writing an empty path.
5. **Store modes:** after a write, `fs.statSync(usageDir).mode & 0o777 === 0o700` and `statSync(file).mode & 0o777 === 0o600`, with the root under a `mkdtemp`/`PI_CODING_AGENT_DIR` dir and never the real home. `pruneRuns(repoRoot,0)` leaves the file's inode present (`runs.ts:164` operates under `runsDir`, not `getAgentDir()`), which is the card's third acceptance line and is trivially true only because the store is elsewhere.

New behavior needs a failing test first (AGENTS.md testing conventions); the store round-trip and the dangle case are the two that can be red before implementation.

#### Position — `designer`

I have grounded the seat: `getAgentDir()/council/` is the durable council-state home already (the `mcp-auth.json` precedent at `extensions/mcp/auth-store.ts:23-44` lives there with the `0600 atomic temp+chmod+rename` pattern); `runs/` is the ephemeral substrate that EV-31 must outlive; `spend.ts` (EV-30 Done) produces the boundary + the two half-`Usage` records; R-3 already binds the directory shape, filename shape, README presence, and modes. My surface is the README, the empty state, the unwritable-state copy, and the discoverability/interpretability of filenames and provenance.

---

- **Design position** — The store's discoverability rests on one self-explaining artifact: a README written **once** at first-mkdir that teaches a cold reader the directory, the filename grammar, the record schema in product voice (with the `subtree = stream-assistant lower bound` asymmetry surfaced prominently, not buried), the provenance pointer's chain-of-custody, and the unwritable-state behaviour; plus, inside each record, a **named provenance block** (`{ sessionPath, sessionId, range: { firstEntryId, lastEntryId } }`) whose `sessionPath` is the absolute path to the parent session JSONL — a path the card acceptance test can pin and a person can verify by hand. The directory is empty-state-coherent: a directory whose only non-system file is the README means "first invocation pending," and that sentence is the first thing the README says.

- **Gulf closed** — Evaluation gulf, primarily, for a maintainer who runs `ls ~/.local/share/<…>/council/usage/` and asks "is this what I think it is, and can I trust the numbers here?" Closes at three moments: arrival (the directory is named and the README leads with identity + empty-state semantics), per-record reading (filename → chronology + session-group + command; body → boundary + provenance + asymmetric accounting), and the failure moment (a write that fails names the absolute path of the failed target, byte-stably). Also closes a narrow execution gulf for a reader who needs to trace a number: opening the provenance `sessionPath` in a text editor must show entries whose ids include `boundary.firstEntryId..boundary.lastEntryId`.

- **Principle and evidence** — *Signifier over decoration.* A first-time reader who runs `ls` and sees only `README.md` + nothing must instantly know whether this is the right place and what to expect next — that's the empty-state signifier. *Conceptual model.* The record carries an *intentional asymmetry*: `ownSession` is exhaustive across assistant/toolResult/compaction/branch_summary entries on the leaf chain (`usageSource: "session-reconciled"`); `subtree` is a stream-projected lower bound (`usageSource: "stream-assistant"` — EV-28 Q4's reserved member, confirmed by the EV-30 ruling at `council/cards/EV-30.md`). A person reading the JSON must encounter that distinction within the first 20 lines of the README, not in a tail comment — the standing concern that "the gap between what the data implies and what the data knows is the largest design hazard" lives here. *Mapping.* The filename grammar `<ISO-basic>_<runId>_<command>.json` already maps three concerns to three tokens in a fixed order — chronological prefix (when the record was written), parent-session middle (`runId` is session-scoped, so all records with the same middle token came from one parent session — `extensions/index.ts:161` mints one `runId` per parent session, surfaced in EV-30's M1), and the procedure that produced it. The README's job is to teach that mapping by example, not to innovate a new filename shape. *Forcing function for unwritable.* A failed write must surface the absolute path in its notify line; we cannot let the silent-loss mode of the previous store ("usage record would be silently lost") persist by an unrelated mechanism. *Familiar-but-unearned check.* Conventions people may bring from comparable products (e.g., the empty-store-contains-a-`.gitkeep`, or directory `.jsonl` append-logs) are not the precedent here; the precedent is `mcp-auth.json` (atomic single-file), and the design owes nothing to either generic pattern.

- **Falsifiable predictions** —
  - **P1 (post-init)**: After `ensureUsageDir()` runs for the first time on a clean `getAgentDir()`: `ls -la <dir>` returns `README.md` and nothing else user-facing; `stat -c '%a' <dir>` returns `700`; `cat <dir>/README.md` line 1 begins with a heading that names "council usage" (or product voice equivalent), and the first paragraph contains the words "completed council invocation" and "read-back is supported". Both assertions fail under current code (no `usage/` dir exists).
  - **P2 (after one completed invocation)**: `ls <dir> | grep -E '\.json$' | wc -l` equals 1. The filename matches `^[0-9TZ._-]+_[0-9TZ._-]+-[a-z0-9]+_.+\.json$` (ISO-basic + runId + command; no whitespace). The README **mtime** is unchanged from P1 (write-once discipline). `stat -c '%a' <dir>/<file>` returns `600`.
  - **P3 (provenance resolves across prune)**: After `pruneRuns(repoRoot, 0)` removes the entire `runs/` directory, opening `<dir>/<file>.json` and reading `.provenance.sessionPath` yields an absolute path; `fs.existsSync(path)` returns true; `fs.statSync(path).isFile()` is true; the file's first line's `id` field equals `.boundary.sessionId` (or, after a marker roll-over, an entry with that id is reachable from the leaf chain — pick one and make it literal). All three checks pass; the test the card names is exactly this run.
  - **P4 (asymmetry is discoverable in the README, not just in the record)**: `grep -ciE 'lower bound|subtree' <dir>/README.md` returns ≥ 1 line; `grep -c 'usageSource' <dir>/README.md` returns ≥ 1 line. A consumer who only reads the JSON and prints `.subtree.cost` will *not* know it's a lower bound; the README has to teach them before they get there.
  - **P5 (unwritable surfaces a path-named failure)**: After `chmod -w <dir>` then triggering a record-write, the TUI/headless output contains a line whose shape is byte-stable and `grep`-able, e.g. `[council-usage] write failed: <absolute-path>: EACCES (chmod 0700 on the store, then retry)`. The exact literal is a candidate Phase-1 ruling; the *presence of an absolute path in the failure message* is not.
  - **P6 (notify for the *successful* write)**: After the first successful record is written, an interactive session receives one notify line whose shape names command, runId, ISO-basic, and the recorded line `usage record written: <file> (<cost> <basis>, subtree = lower bound)`. The exact phrasing is a candidate Phase-1 ruling; the *absolute-path-stamped-with-context* shape is the design claim.

  Each prediction above maps to an **out-of-band CDP smoke** or a pure-seam/unit test: P1/P2/P3 to a `test/usage-store.test.ts` using `fs.mkdtemp` and reading the on-disk shape; P4 to a `grep` over the canonical README asset; P5/P6 to a TUI notify mock that captures `ctx.ui.notify` and asserts the literal. None of these requires a running browser or a real model dispatch.

- **Preferences, ranked last** — I lean toward the README being written as a *shipped asset* (under `council/scaffold/...` or `extensions/usage-store-README.md`) rather than generated from a constant in `index.ts`, because copy that mentions "first invocation pending" reads better as a hand-tuned document, and a TypeScript template literal would impose an unnatural narrative shape. I also lean toward the README ending with a one-paragraph "what this is **not**" section (not the run transcripts, not the MCP secrets, not a transcript log) — adjacent durable homes are confusingly similar and the parent has no reason to remember which is which. Both are taste, neither is a principled position; the owner should pick on writing-convenience grounds. I have **no preference** on whether the record's `schemaVersion` lives inside the JSON body or as a leading README-segmented "current version" header; either works, both are legible.

### Step 3 — bounded exchange, round 2 (cap 3; stopped after 2 — positions stabilised)

Each generator re-dispatched with the others' recorded round-1 positions (read
from this card, verbatim). Jobs `job-12.4` (owner), `job-12.5` (principal),
`job-12.6` (designer). Positions verbatim.

#### Round 2 — `owner`

## Owner — EV-31 round 2 (responds to `principal` and `designer`)

**Concede / hold summary up front:** I concede the store-key argument (marker `at`), the README-at-0600 hold, and the env-var test seam; I hold the hub-onChange primary seam against `agent_settled`-as-primary, with a composite that adopts `agent_settled` as an awaited extra sweep trigger; I fold principal's runner-shaped dangle test and designer's P1–P6 into the test list. Convergence is reached on substance; exact notify/README literals remain Phase-1-ruling candidates as designer already marked.

### 1. The load-bearing dispute: seam (principal's reframe)

I verified both halves. principal is right that `agent_settled` exists and is awaited: pi's `_emitAgentSettled` does `await this._extensionRunner.emit({type:"agent_settled"})` before `waitForIdle()` resolves (agent-session dist, `_emitAgentSettled`), and `extensions/index.ts` registers only `session_start`/`turn_end`/`session_shutdown` — no `agent_settled`. But principal's design has a timing flaw the position doesn't address: **`agent_settled` is per-prompt-run, and on the dispatch turn it fires *before* the forest settles.** `index.ts:227` stamps the marker, then `sendUserMessage(routed)`; the dispatched turn's assistant reply completes while hub children are still running (the headless branch comment at `index.ts` explicitly notes `waitForIdle` gates the *turn*, not the council). An `agent_settled`-gated-on-marker write would compute `spendRecord` mid-forest and persist an undercount presented as final. The settle condition must be forest-settle regardless of event source — so `agent_settled` alone is neither necessary nor sufficient as the seam.

**Resolution (converged composite):** hub `onChange` forest-settle stays the primary per-invocation write trigger (it is the only event that fires exactly when the subtree manifests become final; hub fires it at `hub.ts:163/246/258` on every state transition, and my persist path is synchronous fs per the auth-store pattern, so the monitor tick carries the write to completion — no teardown race). I adopt principal's awaitedness point by adding **`agent_settled` to the sweep trigger list alongside `session_shutdown`** — an awaited per-run last-chance flush for pending markers whose forest has settled, which also captures a more complete own-session tail than forest-settle time. This concedes criterion (c) without accepting a seam that fires mid-forest. principal's "misses the event" claim is therefore half-right: my round-1 seam is not missing per-invocation completion; it was missing an awaited pre-idle flush, which the composite now has.

### 2. Store key: marker `at` vs wall-clock write time — **concede**

`spend.ts:261` stamps `at: Date.now()` — ms-precision epoch. Keying the R-3 `<ISO-basic>` on the marker's `at` makes choose-once a property of the *key itself*: file-exists → no-op, durable across processes. My in-memory choose-once set dies on restart, so a later session re-observing the same still-pending marker would mint a duplicate under my scheme — that's a real defect principal found. I keep `writtenAt`, `trigger`, `manifestsObserved` inside the record body (ruling-item-6 basis honesty), and the README's grammar description must say the prefix is the **invocation** time, not "when the record was written" (correcting designer's P2 wording). Residual corner — two same-command invocations landing on the same `Date.now()` ms — is fail-loud (caught throw, reported), documented; realistically unreachable since dispatch takes seconds.

### 3. Runner-shaped acceptance case — **concede the test, hold on "tautology"**

Grounded: `hub-tools.ts` spawns seat children with `sessionDir: dir` where `dir = ensureRunDir(...)` — seat sessions, including a council-runner's own, live inside the pruned run dir. The pointer targets such a session exactly when a seat with `spawns` dispatches a nested council (the `spawns` field is in the fixed seat schema per AGENTS.md). My round-1 design already reads a dangling pointer back as a recorded property, never a throw (`resolveProvenance` → `"session-missing"` enum) — so principal's claim 3 is a test I add, not a design change: pointer → file inside `runDir` → `pruneRuns(repoRoot, 0)` → read-back `session-missing`, record survives, no throw. I hold only on the framing: the card's primary test is not a tautology — for the interactive parent the session genuinely sits in pi's session dir, and that is the motivating durability case. The runner-shaped case is an *additional* class the card text didn't consider, so the acceptance gains a test; it doesn't invalidate the first. principal's provenance-covers-only-ownSession point: conceded as copy — the README must state the pointer explains the own-session half only, and the record already stamps `manifestsObserved` for the subtree's basis.

### 4. Injection seam: PI_CODING_AGENT_DIR — **concede**

Grounded at `extensions/mcp/auth-store.ts` (`loadAuth(file: string = authFilePath())`, `authFilePath()` = `path.join(getAgentDir(), "council", ...)`) and `test/mcp/oauth.test.ts:20-21` (`beforeEach` sets `process.env.PI_CODING_AGENT_DIR` to a mkdtemp dir; `getAgentDir()` reads it at call time). The repo precedent is parameter-with-default in production **plus** env-var isolation in tests calling the real defaults. My round-1 tests passing explicit `storeRoot` params would leave the real `getAgentDir()/council/usage` composition untested. Tests follow the oauth.test.ts pattern: set `PI_CODING_AGENT_DIR` to mkdtemp, call real entrypoints; the optional param remains for unit-level cases only.

### 5. designer's surface position

Folding the falsifiable ones into the test list: P1/P2 → dir-0700/record-0600 + README present-and-unchanged tests; P3 → my T-U1 verbatim; P4 → README content asserts (asymmetry "lower bound"/"usageSource" present, and within the first 20 lines per designer's "not in a tail comment"); P5 → unwritable-state test with a notify mock asserting an absolute path in the failure line (my caught-per-record sweep error path already produces it); P6 → success-notify test with the notify mock. Two owner decisions on delegated items: **README mode — I concede my round-1 0644 hold; all files 0600, dir 0700** (R-3 satisfied byte-literally, security property unchanged, same-user readability unaffected). **README source — template-literal constant in the usage module, not a shipped asset**: `council/scaffold/` is for consumer repos and this README lives in the agent dir; a PKG_ROOT asset read would be a new resource-resolution mechanism the conventions caution against; tests assert via the exported constant. I accept designer's empty-state first sentence, "what this is not" section, and the named provenance block; `schemaVersion` goes in the JSON body (records must be self-describing standalone).

### Tradeoffs accepted

- `agent_settled` as an extra trigger adds one more registration and a second sweep path for marginal gain (headless shutdown already sweeps) — accepted because it buys the awaited-before-idle property and a fuller parent tail cheaply, and it dissolves the round-1 dispute instead of litigating it.
- Marker-`at` keys mean the filename timestamp is not write time — accepted; the body's `writtenAt` carries write honesty, README states the distinction.

### Testable claims

1. **Seam-order test (red for `agent_settled`-as-primary):** fake agent loop where `agent_settled` fires while one child job is still running → no record written at that settle; when the job's manifest completes (hub `onChange`), exactly one record is written; a second settle writes nothing (key = marker `at`, file exists → byte-identical no-op).
2. **Runner dangle test (principal claim 3):** session file at `<repoRoot>/.council/runs/<runId>/s.jsonl`, record written, `pruneRuns(repoRoot, 0)` → `existsSync(path) === false`, read-back returns the record with pointer status `session-missing`, no throw, record byte-identical.
3. **Choose-once durability (concedes to principal):** same marker, two settles, *across simulated process restarts* (fresh module state) → exactly one file, because the key is the marker's `at`, not an in-memory set. Red for my round-1 design.
4. **Env-seam test:** `PI_CODING_AGENT_DIR` → mkdtemp, call real `persistInvocationUsage()` with no explicit root → record lands at `<mkdtemp>/council/usage/`, dir 0700, files 0600, README present with asymmetry copy in the first 20 lines and unchanged mtime after a second persist.
5. **Notify shapes (designer P5/P6):** unwritable dir (`chmod 0500`) → notify line contains the absolute target path; first success → notify line names command, runId, ISO-basic.

**Convergence:** reached on all five points under the composite seam + marker-`at` key + added tests, in my judgment. Remaining open items are copy literals only (README wording, notify line shapes), which designer already designated as Phase-1-ruling candidates — no ruling seat needed on substance.

#### Round 2 — `principal`

## Principal — EV-31 round 2

**Grounding:** `vault/wiki/index.md` → [[run-transcripts]] (`pruneRuns` keeps 15, "ephemeral telemetry, not durable state"), [[eval-store-contract]], [[council-runner]], [[smoke-test]]; `council/cards/EV-30.md` step-6 ruling + step-9 residual; `council/agents/council-runner.md`; source `extensions/index.ts`, `extensions/spend.ts`, `extensions/runs.ts`, `extensions/hub.ts`, `extensions/hub-tools.ts`, `extensions/dispatch.ts`, `extensions/seats.ts`, `extensions/mcp/auth-store.ts`, `test/mcp/oauth.test.ts`; pi `dist/core/agent-session.js`, `dist/core/session-manager.js`, `dist/core/extensions/types.d.ts`. The wiki has **no** page for this store — [[eval-store-contract]] is the nearest durable-store precedent and I use it as one, not as a spec for EV-31.

---

### Cross-seam reading — what the contract actually is

**1. The seam. My round-1 claim holds, and it is stronger than I stated.** `index.ts` registers `session_start` (144), `turn_end` (186), `session_shutdown` (187) — no `agent_settled`. pi emits `agent_settled` at `agent-session.js:347-351`, called from `_runAgentPrompt`'s `finally` at `:784` *after* the post-run retry/compaction/queued-continuation loop (`:776-778`), awaited via `_extensionRunner.emit`, with the idle-wait resolved only afterwards (`:354` → `:338-346`); `waitForIdle()` awaits that promise (`:1229-1234`), and the headless branch awaits `ctx.waitForIdle()` (`index.ts:~245`). So a handler registered on `agent_settled` is **awaited before headless teardown, once per invocation, post-settle**. `turn_end` fires per tool-loop turn; `session_shutdown` fires per session (`agent-session-runtime.js:296-302`, reason `quit`) — in a live TUI session only at quit.

A second, decisive fact neither seat opened: `session-manager.js:_persist` (`:739-768`) buffers all entries until the first assistant entry exists, then writes **the entire `fileEntries` array** (`:752-764`) with `openSync(..., "wx")` + `writeFileSync`, and appends synchronously thereafter. So at `agent_settled` the session file already contains the marker, the boundary user entry, and the leaf → the stored `firstEntryId`/`lastEntryId` resolve *by construction*. A write made **before** the send (owner's marker time) or on the first `turn_end` races that flush: the pointer may name ids not yet on disk. The seam choice is not cosmetic — it determines whether the pointer is resolvable at write time at all.

**Verdict: `agent_settled` gated on an unconsumed on-chain `council-invocation` marker is the correct seam.** Owner must concede that hub `onChange` forest-settle is not invocation completion: `hub.ts` fires `onChange` on every stdout chunk (`:163`), on timeout (`:246`) and on settle (`:258`) — it is child-process-anchored and fires **mid-turn**, before the parent's own assistant `message_end`. Owner's own stated motivation ("the alternative — write only at session end — starves EV-32's command-exit blocks") is *exactly* what `agent_settled` delivers and forest-settle does not: a record at every invocation end, keyed to the invocation. The falsifier is owner's own record: at forest-settle the recorded `boundary.lastEntryId` (`spend.ts:207-209`) cannot equal the session's final leaf; at `agent_settled` it must. I accept owner's `session_shutdown` sweep as a **backstop only**, on the condition that it is the same code path with `basis.trigger` stamped — not a second, differently-keyed writer.

Honest cost of my own reframe that I did not name in round 1: the write now runs on the awaited settle path, so it delays idle resolution and hence headless teardown by one atomic file write. That is the price of anchoring; it is bounded and it is inspectable, which the alternative is not.

**2. The key. Marker `at`, and the repo already has the precedent.** `recordInvocationBoundary` (`spend.ts:255-274`) mints `{command, runId, at: Date.now()}` **before** the send, on-chain, durable in the session JSONL — invocation identity, available at both write and read. Under marker-`at`, choose-once is structural: same invocation → same filename → the write refuses. Owner's `max(now, prev+1)` is not merely different, it is pointed the other way: it *guarantees* distinct filenames for two settles of the same marker, and its monotonicity guarantee is process-local (`prev` is unknowable after a restart), while the store is durable. The concrete failure is resumption: `pi -c` reloads the session with the marker still on-chain, owner's in-memory choose-once set is gone, `now` differs → a phantom second invocation record. Owner's key has no way to detect it.

**[[eval-store-contract]] already rules this shape for this repo:** the key is the identity tuple `(cellId, repeat, scoredUnder, …)`, "same-payload rewrite is a no-op; divergent payload for an existing key throws (defect signal)". R-3's filename is the *rendering* of an identity tuple — `<ISO-basic>` must therefore be the invocation's stamp, since `runId` and `command` are already the other two tuple members. "Was a compute time stamped on the key" is **not** the decision the card asked to record: EV-30 step-9 note 3 (`EV-30.md:736-740`) assigns "recompute basis and **choose-once semantics**" to EV-31, and EV-30's rejected option 6a (`:582`) rejected compute-time stamping *on the record* precisely because "its consumer (EV-31) is the right place for persistence idempotence". I concede the compute-time stamp belongs store-side; I deny it belongs on the **key**. Owner's own `writtenAt` + `basis.trigger` already carries it — putting it on the key as well is the double-use that breaks choose-once.

**3. The runner-shaped case. Owner's answer changes the subject.** Owner's "the pointer never targets seat sessions" is true, and it is not a reply: the question is not *which* file the pointer names but whether the named file survives. The council-runner is a parent-mode pi process (`council-runner.md` carries `hub` and spawns six seats), spawned with `sessionDir: dir` (`hub-tools.ts:184`), `dir = ensureRunDir(repoRoot, runId)` (`:164`) under `runsDir` (`runs.ts:65-71`), deleted by `pruneRuns` (`runs.ts:165-171`). Its `/features-deliver` invocations call `recordInvocationBoundary` in **its own** session — the file under the run dir. This is not my inference: EV-30's step-9 residual names it verbatim — "seat sessions (including a council-runner's own) live *inside* the pruned run directory (`hub-tools.ts:184-185`) … **EV-31 must own survival/basis for that**".

So the card's acceptance line, read alone, is satisfiable only by constructing a session outside the run dir — the tautology I named in round 1 — while the class that motivates durability (autonomous epic delivery, EPIC-7's whole point) is precisely the class it excludes.

**4. What the pointer covers — correcting my own round-1 overstatement.** I said provenance "covers only the ownSession half". That is too strong. `spend.ts:193-196` gates forest membership on `boundaryEntry.timestamp`, so the pointer's start **does** define the subtree's *window*; what it cannot explain is the subtree's *basis* — `RunManifest.usage` and seat sessions, both under the pruned directory. The accurate statement is: the pointer explains `ownSession` exhaustively and `subtree`'s window, and explains nothing about `subtree`'s contents. That changes what the README owes the reader, not the JSON.

**5. README mode.** R-3 reads "the directory carries a `README.md` naming the format; directory mode `0700`, file mode `0600`" — one mode clause for one store. Owner's rationale ("the README is human-facing documentation") buys nothing: inside a `0700` directory a `0644` README is unreachable by any other user, so the two are functionally identical and the deviation costs a gate argument for zero benefit. Owner should concede the one line. Owner's `mkdirSync` + explicit `chmodSync` point is right and I adopt it (mode is umask-masked otherwise).

**6. The test seam is both, not either.** `auth-store.ts:26` is `loadAuth(file: string = authFilePath())` and `authFilePath()` calls `getAgentDir()`; `oauth.test.ts:20-21` and `commands.test.ts:12` isolate via `PI_CODING_AGENT_DIR`, which pi reads at call time. My round-1 "parameter matches precedent" and owner's parameter-with-default are the same sentence — but the card's constraint is "tests never touch `~/.pi/agent/`", and only the env var lets a test **assert that the real default path was not written**. Parameter-with-default for the module seam; `PI_CODING_AGENT_DIR` for the acceptance test. My round-1 framing of these as alternatives was wrong; concede.

---

### Blind spots, named separately

- **Owner (store/write slice):** sees a `ctx` and a settle after which the leaf is stable — cannot see that "a completed invocation" has no per-invocation event today, so it reaches for the two events it can see (`turn_end`, N times; `session_shutdown`, never mid-session in TUI). Cannot see, from inside the write, that the session path it stores may be *the run dir's own file*, and cannot see that entry persistence is buffered until the first assistant entry — so a pointer written too early names entries not yet on disk.
- **Designer (reader/surface slice):** sees the cold reader and the filename grammar, and assumes the artifact it is describing always resolves. Its P3 asserts `fs.existsSync(path)` is **true** after `pruneRuns(repoRoot, 0)` unconditionally — a prediction shaped so that the dangle case cannot exist. That is the same blind spot as owner's, arriving from the opposite side: owner cannot see the file is at risk; designer cannot see the reader ever needing a non-resolving outcome. Neither can see the seam or the key.
- **EV-32 (render slice, not seated here but the reason the card exists):** will see a record with a pointer and a number pair; nothing in the JSON *forces* it to know the pointer explains only one half's basis. That is the cross-seam failure the card's own acceptance test would hide.

---

### Reframe

The round-1 reframe stands (invocation identity as the durable unit; resolvability as a recorded property, not an assumption). Round 2 adds the piece both other seats lack, and it is small:

**Split write-time facts from read-time facts, and store only the former.** A stored `resolvable: boolean` — which my own round-1 phrasing ("a derived `resolvable`") implied — goes stale the moment the session file is pruned after the write: the same defect class EV-30's rejected option 2a named ("the union becomes a lie the moment any constituent changes"). But one thing *is* knowable at write time and never goes stale, because it asserts a fact about the write: **whether the pointer was survivable** — i.e. whether `sessionPath` is under `runsDir(repoRoot)`. Both inputs are in hand at write time.

So: store `sessionPath` (nullable) + a write-time `pointerSurvivable: boolean`; **derive** present-tense resolution at read, never store it. Read-back then reports three distinguishable outcomes rather than one shrug:

| outcome | meaning |
|---|---|
| `resolved` | path exists, both ids present |
| `pruned-expected` | path was under the run dir at write time; `pointerSurvivable:false` — the store never promised this would survive |
| `missing-unexpected` | `pointerSurvivable:true` and the file is gone — a real defect signal, not an accepted limitation |

This is what makes read-back honest rather than uniform: a ledger that cannot distinguish "this pointer could never have survived" from "this pointer should have survived and didn't" has moved the traceability question, not answered it. It costs one derived boolean at write and no new machinery, and it is what [[eval-store-contract]] already does in spirit by keying records on identity so that absence is meaningful.

**On the required decisions:** (a) in-memory ctx / `getSessionFile() === undefined` → write with `sessionPath: null` + stated reason (owner's #3 and mine agree; EV-30's fail-closed applies to *values*, not existence); (b) unresolved boundary → persist zero-both unchanged, `boundaryResolved:false` (owner and I agree); (c) no recompute ever (agree).

---

### Testable claims

1. **T-D1 — seam, two-part, source-anchored.** (i) One marker, ≥2 tool-loop turns: `turn_end`-driven → ≥2 records; `agent_settled`-driven → exactly 1. (ii) **The oracle that settles it:** the record's `boundary.lastEntryId` equals the session's leaf id at invocation end. `agent_settled` passes; hub-`onChange` forest-settle **fails**, because it fires mid-turn (`hub.ts:163`,`:258`) before the parent's final assistant entry exists. This is a runnable red for owner's seam, not prose.
2. **T-D2 — key, resumed-session resumption.** Persist once; discard all in-memory state; call the write again with the *same on-chain marker* and a different `now`. Claim: marker-`at` key → exactly one file, byte-identical on the second call; wall-clock+monotonic key → two files. Same test with `now` frozen, covering owner's own T-U10 case.
3. **T-D3 — the runner-shaped acceptance.** Session file written under `runDir`, record written, `pruneRuns(repoRoot, 0)`, then read. Claim: `fs.existsSync(sessionPath)===false`; read-back returns the record with `pointerSurvivable:false` and outcome `pruned-expected`; **it does not throw**. Contrast the normal-parent file (outside `runDir`): `pointerSurvivable:true`, outcome `resolved`. Owner's reading of the card has no test that can be red here — which is the shape of the problem.
4. **T-D4 — pointer resolve-outcome is derived, never stored.** Write, snapshot the JSON bytes, delete the session file (not the run dir), re-read. Claim: stored bytes unchanged (`toEqual` the snapshot) and the *derived* outcome is `missing-unexpected`. Red for any design that stores a resolution boolean.
5. **T-D5 — README mode.** `statSync(README).mode & 0o777 === 0o600`. Red today under 0644; passes after the one-line concession.
6. **T-D6 — the env-var leg is not optional.** Acceptance test runs with `PI_CODING_AGENT_DIR` = `mkdtemp` and asserts the resolved store root equals that tmp path **and** that nothing was created at the default `getAgentDir()/council/usage`. This is the leg a pure parameter seam cannot provide; it is why both seams are required, not one.
7. **T-D7 — `pruneRuns(repoRoot, 0)` leaves the record byte-identical** (`runs.ts:165-171` operates under `runsDir`, `getAgentDir()` is elsewhere) — owner's T-U2, accepted as written.

**Designer's predictions.** P1, P2, P4 are fine as smoke targets, with P2's regex tightened to R-3's grammar (as written it admits `_` inside the ISO token and matches more than it should). **P3 must be split into two predictions** — the resolvable case and the `pruned-expected` case — because as one "all three checks pass" statement it encodes the blind spot rather than a prediction. P5/P6 are the one item that needs a ruling: byte-stable notify literals are ruled copy in this repo, not designer copy — [[smoke-test]] records the precedent explicitly ("Phase 5 assertions also source the R-2 usage line and R-3 notify copy from the ruled literals rather than in-repo constants"). The *presence of an absolute path in the failure message* is test-settleable; the *literal's wording* is a Phase-1-style ruling item. That is a `Needs Human` item, and a small one — parked, not blocking.

---

### Conceded / held / convergence

**Conceded:** `session_shutdown` sweep as a backstop (same code path, trigger-stamped); owner's `UsageProvenance` and designer's named `provenance` block are JSON-sufficient — **no new field is needed** for coverage, because `usageSource` (`"session-reconciled"` / `"stream-assistant"`) is already the machine-readable per-half basis statement and EV-30's rejected 5a forbids duplicating it in prose; my round-1 "derived `resolvable`" wrongly implied a stored field — corrected to write-time-fact-stored / present-tense-derived; my round-1 "covers only the ownSession half" overstated the gap; param-vs-env is a both-and, not a choice; owner's `chmodSync`-regardless-of-umask point; owner's T-U2 and in-memory-ctx handling.

**Held:** `agent_settled` is the seam (owner's forest-settle cannot satisfy its own motivation, and the `lastEntryId` oracle is red for it); the key is marker `at` (owner's monotonic clock's ordering guarantee is process-local and the resumed-session test is red for it); the acceptance test must contain the runner-shaped case and read-back must not throw; README at `0600` per R-3.

**Convergence: reached on the design; three disputes are source/test-settleable and require no ruling seat** — the seam (T-D1, source-verified above), the key (T-D2), the README mode (T-D5, R-3 governs). **One ruling item remains**, and it is copy, not architecture: the P5/P6 notify literals, if they are to be byte-stable. I'd send that alone rather than escalading the card. Implementation should not proceed on owner's seam or owner's key while T-D1(ii) and T-D2 are red.

#### Round 2 — `designer`

`agent_settled` is confirmed real in pi's API (CHANGELOG, docs/extensions.md, rpc.md). The repo currently only uses `session_start`, `turn_end`, `session_shutdown` — no `agent_settled`. Principal's `agent_settled` proposal is grounded in pi's actual event surface.

I have enough to write the response. The question is a round-2 design response — I argue design only, ground in the artifact, and stay in the output format.

<output_format>

- **Design position** — Accept `owner`'s `StoredUsageRecord` + `UsageProvenance` shape as the wire contract for EV-31's directory surface, *contingent on one rename* (`pointerStatus` is the runtime signal at write time, but `resolveProvenance` is the read-time signal that is what a person actually reads — the two should be named from the reader's perspective, not the writer's); concede P5/P6 are out of scope for this card and hand them to EV-32 explicitly; concede the README-mode question to `owner` and R-3's literal reading; and hold P1–P4 against the `owner` shape only after teaching the dangling-pointer case in the README, which `principal`'s reframe makes non-optional.
- **Gulf closed** — Evaluation gulf, for a person running `ls ~/.local/share/.../council/usage/` and asking "what is this, and is the provenance I see in a record still reachable?" The `owner` shape closes the *resolving* case cleanly (P3). It does not by itself close the *dangling* case — that gap is the standing hazard `principal` named, and the README must teach it on the same shelf as the resolving case, not buried under "edge cases."
- **Principle and evidence** —
  - **Signifier + conceptual model.** A reader who opens a record and sees `pointerStatus: "no-session-file"` (owner's write-time label) needs a parallel, stable read-time answer to "is this still resolvable?" That is what `resolveProvenance`'s `ok | session-missing | range-missing` status is for. The two labels must agree about which bucket a record lands in; `owner`'s `pointerStatus` should be renamed to `pointerStatusAtWrite` (or equivalent) so it cannot be misread as a present-tense claim. *Evidence:* the `principal` reframe establishes that the pointer's resolvability can change between write and read — naming them as the same field is a slip-trap.
  - **Knowledge in the world.** P1, P2, P3, P4 are satisfiable by the `owner` shape *as long as the README and record teach both the resolving and the dangling cases*. P3 in particular — the card's acceptance — is satisfied by the `ok` branch of `resolveProvenance` for a parent session outside `runsDir`. The `session-missing` and `range-missing` branches are not prediction-elaborating extras; they are the runner-shaped case `principal` named, and the README's job is to surface them as first-class outcomes of `resolveProvenance`, not as footnotes.
  - **Forcing function for unwritable.** The README must state — in the same shelf as the empty-state sentence, not as an aside — that a write that cannot reach `getAgentDir()/council/usage/` because of permissions surfaces the absolute target path. That is a copy/permission surface, not a ruling.
  - **Surface scope (the EV-31 vs EV-32 question).** The directory surface EV-31 owns is: filenames, README, modes, and the on-disk record shape. The TUI `notify` line on success/failure is a *rendered* surface that the `index.ts` handler emits to `ctx.ui.notify` — that emission is EV-31's *code path* (the write happens here) but the rendered wording is EV-32's *design surface* (EV-32 owns what a person reads in the TUI). My P5/P6 predictions named the *literal* of the notify line; the *presence of the notify call* is in scope for EV-31 (a missing notify is a feedback failure at the write site), but the *wording* is EV-32's.
- **Falsifiable predictions** (revised for round 2) —
  - **P1 (post-init, held).** After `ensureUsageDir()` runs on a clean `getAgentDir()`: `ls` shows `README.md` only; dir mode `700`; line1 of the README names "council usage"; the first paragraph contains both "completed council invocation" and "read-back is supported" *and* names the three `resolveProvenance` outcomes (`ok`, `session-missing`, `range-missing`) in the same shelf, not as an appendix. Falsified by current code; falsified by a README that buries the dangling case under "edge cases."
  - **P2 (after one completed invocation, held).** `ls *.json | wc -l` equals 1; filename matches the ISO-basic pattern; file mode `600`; README mtime unchanged from P1. Falsified by a write that re-stamps the README or by a filename that drifts from R-3's shape.
  - **P3 (provenance resolves across prune, held for the parent case, with the runner case added).** `pruneRuns(repoRoot, 0)` then `resolveProvenance(record.provenance)` returns `status: "ok"` for a parent-session JSONL outside `runsDir`; returns `status: "session-missing"` (and *does not throw*) for a runner-session JSONL that was inside the pruned `runsDir`. Falsified by a `resolveProvenance` that throws on missing file (principal's prediction3) or that silently returns `ok`.
  - **P4 (asymmetry discoverable, held; ground in record voice, not README voice).** The README names `subtree` as a stream-assistant lower bound (the asymmetry surfaced prominently) AND names the three `resolveProvenance` outcomes in the same shelf — these are two discoverability requirements, not one. `grep -ciE 'lower bound|subtree' README.md` ≥ 1; `grep -ciE 'session-missing|range-missing' README.md` ≥ 1. Falsified by a README that teaches one but not the other.
  - **P5/P6 (withdrawn from EV-31, handed to EV-32 as a follow-up).** EV-31 owns the *call site* — a successful write MUST emit one notify/console line, and a failed write MUST surface the absolute path. EV-32 owns the *wording*. The hand-off is: EV-31's acceptance test stubs `ctx.ui.notify` and asserts `notify.calls.length === 1` on success and ≥ 1 on `EACCES` with the absolute path as a substring; EV-32's card asserts the literal phrasing. P5/P6's claim "the presence of an absolute path in the failure message" remains EV-31's; the literal "[council-usage] write failed: …" sentence belongs to EV-32.
  - **P7 (new, scope check on README mode).** `stat -c '%a' README.md` returns a value the README-mode ruling selects; until that ruling lands, the README-mode prediction is `unresolved`, not `fails`. The 0600 vs 0644 question is a `product-owner`/R-3 ruling, not a designer call.
- **Preferences, ranked last** —
  - I now lean *toward* `owner`'s "shipped asset" choice for the README, because the document grows a third paragraph (the three `resolveProvenance` outcomes) and a fourth (the unwritable-state path), and a hand-tuned asset reads better than a TS template literal at that size. Still taste.
  - I lean *against* letting `owner`'s `pointerStatus` keep its current name. The rename to a write-time-stamped label is a small change that prevents a real misread; the cost is one identifier. Taste, but cheap.
  - I have **no preference** between `owner`'s `monotonic store-side write clock` and `principal`'s "marker `at` as the key" — both prevent same-ms duplicates; the marker-`at` choice has the stronger conceptual-model argument (the key then names the invocation, not the compute), the monotonic clock has the stronger "write what happened" argument. This is a `principal`/`owner` disagreement I will not resolve from this seat.
 - **Empty-state vs unwritable-state testability.** Both are testable *as recorded-state* (a `test/usage-store.test.ts` reading the canonical README asset and asserting the empty-state and unwritable-state strings are present, plus a `mkdtemp` smoke that an unwritable dir triggers a notify with the absolute path). They are *not* testable as rendered pixels — that is the EV-32 hand-off.
- **Round-1 prediction tally** —
  - **Conceded to EV-32 surface:** P5 (unwritable TUI literal), P6 (success TUI literal). The notify *call* stays in EV-31; the wording goes.
  - **Conceded to R-3 ruling:** README mode (0600 vs 0644). I have no principled position; the literal in R-3 says "file mode 0600," which `owner`'s case for0644 must win or lose on the R-3 wording, not on a design argument from this seat. I flag it here so the gate argument lands before implementation.
  - **Revised:** P3 now has the runner-case half (was single-case). P4 now bundles two discoverability assertions (asymmetry + dangling-pointer teaching). P1 gains the requirement that the dangling case appears in the same shelf as the empty state, not later.
  - **Held:** P1, P2, P4 (as revised).
  - **New:** P3-runner, P7-mode-unresolved.
 - **No change** in stance on the underlying position: the directory's discoverability lives in one self-explaining README and a named `UsageProvenance` block; the asymmetry must be teachable to a cold reader; the unwritable failure must name its absolute target.

</output_format>

### Step 4 — Skeptic attacks and runs tests (job `job-12.7`)

## Skeptic report — EV-31 (step 4, pre-implementation)

Grounded in: `vault/wiki/index.md` → `[[run-transcripts]]`, `[[eval-store-contract]]`, `[[smoke-test]]`, `[[headless-pi]]`, `[[council-runner]]`; cards EV-31 (all positions), EV-30 (step-6 ruling + step-13 residual), EV-32; `extensions/spend.ts`, `runs.ts`, `index.ts`, `hub.ts`, `hub-tools.ts`, `mcp/auth-store.ts`; `test/spend.test.ts`, `test/mcp/oauth.test.ts`; pi shipped `dist/` (`core/agent-session.js`, `core/session-manager.js`, `core/agent-session-runtime.js`, `config.js`). No implementation branch exists — nothing under `extensions/usage-store.ts`; all probes ran in `/tmp/ev31-skeptic/`, main tree untouched (`git status --porcelain` clean after).

### Objections

- **O1a — `agent_settled` exists, is per-prompt-run, and is awaited before idle/teardown — `closed-green` (principal's existence claim holds; I attacked it and lost nothing).**
  Settling test: grep pi's shipped `dist/` for the event, its await, and its position relative to `waitForIdle`.
  Ran: `grep -n "agent_settled\|_emitAgentSettled" dist/core/agent-session.js` → `:347 _emitAgentSettled`, `:350 await this._extensionRunner.emit({type:"agent_settled"})`, `:784 await this._emitAgentSettled()` in `_runAgentPrompt`'s `finally` after the retry/compaction/continuation loop; `waitForIdle()` awaits the idle promise (`:1229-1234`) which `_emitAgentSettled` resolves only afterwards (`:354`); headless branch awaits `ctx.waitForIdle()` (`index.ts:244`). `turn_end` fires per tool-loop turn with `turnIndex` (`agent-session.js:484-492`); `session_shutdown` fires once with `reason:"quit"` (`agent-session-runtime.js:301-304`). Owner's "no `agent_settled` registered in index.ts" half is also true (`index.ts` registers only `session_start`/`turn_end`/`session_shutdown`, lines 144/186/187) — that is agreement, not a find.
- **O1b — hub `onChange` fires mid-turn, before settle — `closed-green` (principal's timing claim holds; owner's own round-2 composite implicitly concedes it).**
  Settling test: `/tmp/ev31-skeptic/probe-seam.ts` — real `Hub.spawnJob` emitting one `message_end` line then sleeping; count `onChange` fires while `exitCode === null`.
  Output: `fires=1 firedWhileRunning=1 exitCode=null state=running` → `O1-HUB-MIDTURN: TRUE`. Source confirms three fire points: stdout chunk (`hub.ts:163`), timeout tick (`:246`), settle (`:258`). A forest-settle-gated write that does not re-check at a later seam therefore persists before the parent's final assistant entry exists — exactly principal's `lastEntryId` oracle argument.
- **O1c — which seam is *primary* (owner's "forest-settle primary + `agent_settled` sweep" vs principal's "`agent_settled`-gated-on-marker") — `open-untested`.**
  Both sole-primary designs have a source-plausible defect (O1b kills forest-settle-only completeness; owner's "dispatch turn's assistant reply completes while children still run" kills settle-only freshness — `index.ts:227` stamps then `sendUserMessage`, children run async), but the ranking between them and the composite's correctness need the implementation plus owner's own seam-order test (fake loop: settle-while-running → no write; manifest-complete → one write; second settle → no-op). Nothing to run it against pre-implementation. This is the load-bearing residual.
- **O2 — store key: marker `at` vs wall-clock+monotonic — `closed-green` for marker-`at`, `closed-red` for owner's round-1 key (concession confirmed by test, not just conceded in prose).**
  Settling test: `/tmp/ev31-skeptic/probe-key.ts` — resumed-session double-write (fresh memory, same on-chain marker, different `now`).
  Output: `marker-at files=1 secondWrote=false` → `O2-MARKER-AT: TRUE`; `wallclock files=2` → `O2-WALLCLOCK-DUP: TRUE`. Marker durability premise also verified: `recordInvocationBoundary` stamps `at: Date.now()` on-chain (`spend.ts:261`), and EV-30's step-6 ruling item 6 assigns choose-once to this card while rejecting compute-time stamping on the record (6a) — putting compute time on the *key* is the same defect one layer over. Owner conceded; the test closes it.
- **O3a — runner-shaped dangle filesystem facts — `closed-green`.**
  Settling test: `/tmp/ev31-skeptic/probe-runner.ts` — session file at `<runDir>/job-1.jsonl` (the `hub-tools.ts:184` `sessionDir: dir` shape), record beside it, `pruneRuns(repoRoot, 0)`, inspect.
  Output: `underRuns=true pruned=1 gone=true recSurvives=true`; contrast `outsideSurvives=true`. Principal's "card's primary test constructs its own survival" stands as far as it goes; owner's "additional class, not invalidation" also stands — both halves are facts, and the runner case is real (EV-30 step-13 residual names it verbatim).
- **O3b — read-back semantics (`resolveProvenance` enum vs `pointerSurvivable`+derived split) — `open-untested`.**
  No `resolveProvenance`/`usage-store.ts` exists to throw-or-not-throw. What *is* settled: the write-time fact principal names is computable at write time today (`sessFile.startsWith(runsDir(repo))` demonstrated `underRuns=true` in O3a), and the derived-vs-stored distinction is forced by EV-30's own rejected-2a logic (a stored `resolvable` goes stale on later prune — owner's `pointerStatus` has the same staleness unless write-time-named, which is designer's rename point). The enum's exact shape and the never-throw property need the implementation + principal's T-D3/T-D4.
- **O4 — `session-manager._persist` buffering — `closed-green` (principal's claim holds; I ran it, it passed).**
  Settling test: `/tmp/ev31-skeptic/probe-persist.ts` — file-backed `SessionManager.create`, append marker + user *before* any assistant, check file existence; append assistant, check flushed contents.
  Output: `existsBefore=false` … `existsAfter=true lines=4 hasMarker=true hasUser=true` → `O4-BUFFERING: TRUE`. Source: `session-manager.js:739-768` (buffer until first assistant, then write whole `fileEntries` via `"wx"` + `writeFileSync`). Consequence stands: a pointer written before the send/first-assistant can name ids not yet on disk — this is independent evidence for the `agent_settled`-side of O1c and for O7-of-EV-30's fixture caveat. (First attempt with `new SessionManager({dir})` threw in `normalizePath` — wrong constructor arity, my harness error, corrected to `create(cwd, sessionDir, {})`; the passing run above is the real one.)
- **O5 — README mode — not a test; governed by R-3. Saying so, not re-ruling.**
  R-3 binds "directory mode `0700`, file mode `0600`". Owner conceded 0600-for-all in round 2. There is nothing falsifiable left — any 0644 implementation fails the gate against the ruling, not against a test. Closed by concession + ruling, no probe applicable.
- **O6 — `PI_CODING_AGENT_DIR` call-time seam — `closed-green`.**
  Settling test: `/tmp/ev31-skeptic/probe-env.ts` — set env to two mkdtemp dirs in sequence, call real `getAgentDir()`, then unset.
  Output: `r1=/tmp/ev31-env1-… r2=/tmp/ev31-env2-… r3=/home/tista/.pi/agent` → `O6-CALLTIME: TRUE`. Precedent confirmed: `test/mcp/oauth.test.ts:20-21` sets it per-test; `auth-store.ts:26` pairs parameter-with-default and env isolation. Principal's round-2 "both, not either" is therefore the only testable posture: a parameter-only seam cannot assert the real default path was untouched. A parameter-only design is testable but insufficient for the acceptance test — conceded by both seats already.
- **O7 — module shape, `total`, EV-30 record purity — `closed-green`.**
  Settling test: `/tmp/ev31-skeptic/probe-shape.ts` — build a real `spendRecord` over a parsed fixture session, assert field set.
  Output: `keys=boundary,ownSession,subtree hasTotal=false own=session-reconciled sub=stream-assistant` → `O7-PURITY: TRUE`. EV-30 step-6 ruling items 2 (no `total`, no new `UsageSource` member) and 5 (`stream-assistant` is the lower-bound statement) are intact in the tree; EV-31's design wraps without adding fields to `SpendRecord` in every position on record. Nothing to attack.
- **O8 — notify literal: EV-31 vs EV-32 surface — no test settles it; flagged as a ruling item, not ruled.**
  EV-32's goal owns "the concluding output contains a usage block" at five exit points with byte-stable rendering tests; `[[smoke-test]]` records the precedent that ruled copy is sourced from ruled literals, not in-repo constants ("Phase 5 assertions also source the R-2 usage line and R-3 notify copy from the ruled literals"). Designer handed P5/P6 wording to EV-32 in round 2; principal parked it as `Needs Human`. The *presence* of a notify call with an absolute path on failure is test-settleable in EV-31 (mock `ctx.ui.notify`); the *literal* is not EV-31's to fix. I do not rule it.

### What I ran
Probes (all under `/tmp/ev31-skeptic/`, main tree never mutated): `probe-seam.ts` (Hub mid-turn fire — green), `probe-persist.ts` (buffering — green after one harness-attiude fix), `probe-key.ts` (double-write — marker-at green, wall-clock red), `probe-runner.ts` (prune dangle — green), `probe-env.ts` (call-time env — green), `probe-shape.ts` (record purity — green), `probe-inmem.ts` (`getSessionFile()===undefined` — green). Source greps: `agent_settled`/`waitForIdle`/`turn_end`/`session_shutdown` sites, hub `onChange` sites (`hub.ts:163/246/258`), `index.ts` registrations, marker `at: Date.now()` (`spend.ts:261`). `git status --porcelain` clean after.

### Verdict — `blocks`, on two named unverified items
- **O1c**: the primary-seam choice has no passing settling test. Owner's own seam-order test must be green at implementation before merge; neither sole-primary seam may ship on prose.
- **O3b**: read-back must-not-throw with a stated miss (whichever enum shape) must be green via the runner-shaped test (`pruneRuns(repoRoot,0)` → record survives, pointer reports its class, no throw) before merge.
- Everything else is `closed-green`, `closed-red`-and-conceded (O2 wall-clock key — dead by test and by owner's own concession), or ruled (O5, O8). No open objection beyond O1c/O3b; no gate-integrity injection run (tree mutation forbidden pre-branch — named, not hidden).

### Step 5 — synthesis (job `job-12.8`), verbatim

# Synthesis — EV-31 (step 5, consolidator)

## Agreed design

Stated plainly; each item carries its settling basis (Phase 1 ruling R-3, an EV-30 binding ruling, a Skeptic probe, or undisputed convergence — convergence is labelled as such, never as test evidence).

- **Store home and modes** — `getAgentDir()/council/usage/`; directory mode `0700`, record-file mode `0600`. **Settled by Phase 1 ruling R-3.** (The README-mode sub-question — 0600 vs 0644 — is closed by R-3's literal "file mode 0600" plus owner's round-2 concession; O5 is not a test.)
- **Filename grammar** — `<ISO-basic>_<runId>_<command>.json`, byte-exact per R-3; `<ISO-basic>` is the **marker's `at`** (the invocation's own clock), not wall-clock write time. **Settled by Phase 1 ruling R-3 (shape) + O2 probe (which clock).** The body's `writtenAt`/`basis.trigger`/`basis.manifestsObserved` carry write-time honesty; the README must state the prefix is invocation time, not write time (owner conceded designer's P2 correction).
- **New module** `extensions/usage-store.ts`, the I/O sibling of pure `spend.ts`, following the `mcp/auth-store.ts` atomic tmp+rename pattern. **Undisputed convergence.**
- **Record wraps the EV-30 `SpendRecord` byte-verbatim; `SpendRecord` gains no fields.** **Settled by O7 probe (`closed-green`: `keys=boundary,ownSession,subtree hasTotal=false own=session-reconciled sub=stream-assistant`) + EV-30 step-6 ruling item 2 (no `total`, no new `UsageSource` member).**
- **Store key = marker `at`** ⇒ choose-once is structural: same invocation → same filename → the write refuses; durable across process restarts (an in-memory choose-once set is not). **Settled by O2 probe: `marker-at files=1 secondWrote=false`; `wallclock files=2` (`closed-red` for owner's round-1 key, conceded).**
- **No recompute ever.** **Settled by EV-30 step-6 ruling item 6 (binding): the record is valid only when written once at exit; EV-31 owns choose-once and any recompute-basis decision store-side, never on the EV-30 record).** Both seats converge.
- **`session_shutdown` sweep as a backstop**, same code path, `basis.trigger`-stamped. **Undisputed convergence** (owner proposed, principal conceded).
- **In-memory ctx / `getSessionFile() === undefined`** → write with `sessionPath: null` + a stated reason; never fabricate a path, never skip silently. **Undisputed convergence** (both agree; EV-30's fail-closed applies to values, not existence; EV-32 R-5 demands explicit states over blanks).
- **Unresolved boundary** → persist zero-both unchanged with `boundaryResolved: false`. **Undisputed convergence** (carries EV-30's ruling into the store).
- **Test seam is both, not either**: parameter-with-default for the module seam **plus** `PI_CODING_AGENT_DIR` env-var isolation for the acceptance test (so a test can assert the real default path was never written). **Settled by O6 probe (`closed-green`: env read at call time) + undisputed convergence** (both conceded both-and).
- **Runner-shaped dangle test is added to acceptance** (session file written under `runDir`, record written, `pruneRuns(repoRoot, 0)`, read-back survives and reports its class without throwing). **Filesystem facts settled by O3a probe (`closed-green`: `underRuns=true pruned=1 gone=true recSurvives=true`); the test's addition is undisputed convergence** (owner conceded principal's reframe).
- **README**: written once at first `mkdir`, non-clobbering (mtime unchanged on a second persist); names the format; surfaces the `subtree = stream-assistant` lower-bound asymmetry within the first 20 lines (not a tail comment); teaches the three `resolveProvenance` outcomes on the same shelf as the empty-state first sentence; carries a "what this is not" section. **Undisputed convergence** (owner folded designer's P1/P2/P4 into T-U4).
- **Notify call presence**: a successful write emits one notify/console line; a failed write surfaces the **absolute target path**. **Undisputed convergence** (designer's P5/P6 *presence* claim, conceded into EV-31's test list). The *wording* of that line is EV-32's surface — see open judgment item 2.
- **`schemaVersion` in the JSON body** (records self-describing standalone). **Settled by owner's call; designer had no preference.**
- **README source** — a template-literal constant in the usage module, asserted via the exported constant (not a `PKG_ROOT` shipped asset). **Settled by designer's explicit deference to owner's writing-convenience call** (designer: "taste, neither is a principled position; the owner should pick"). Labelled as deference, not test evidence and not a ruling.

## Settled disputes

Each closed by a Skeptic probe, a Phase 1 / EV-30 ruling, or undisputed convergence — labelled honestly.

- **O1a — `agent_settled` exists, is per-prompt-run, awaited before idle/teardown.** Probe greps of pi's shipped `dist/`: `_emitAgentSettled` at `agent-session.js:347`, `await this._extensionRunner.emit({type:"agent_settled"})` at `:350`, called from `_runAgentPrompt`'s `finally` at `:784` after the retry/compaction/continuation loop; `waitForIdle()` awaits the idle promise (`:1229-1234`) which `_emitAgentSettled` resolves only afterwards (`:354`); headless awaits `ctx.waitForIdle()` (`index.ts:244`). `turn_end` is per-tool-loop-turn; `session_shutdown` is per-session. **`closed-green`.**
- **O1b — hub `onChange` fires mid-turn, before settle.** Probe `probe-seam.ts` (real `Hub.spawnJob`, one `message_end` then sleep): `fires=1 firedWhileRunning=1 exitCode=null state=running` → `O1-HUB-MIDTURN: TRUE`. Source: three fire points — stdout chunk (`hub.ts:163`), timeout tick (`:246`), settle (`:258`). A forest-settle-gated write that does not re-check at a later seam therefore persists before the parent's final assistant entry exists. **`closed-green`.**
- **O2 — store key.** Probe `probe-key.ts` (resumed-session double-write, fresh memory, same on-chain marker, different `now`): `marker-at files=1 secondWrote=false`; `wallclock files=2`. **`closed-green` for marker-`at`; `closed-red` for owner's round-1 wall-clock+monotonic key (owner conceded; EV-30 step-6 ruling item 6 + rejected option 6a reject compute-time stamping one layer over).**
- **O3a — runner-shaped dangle filesystem facts.** Probe `probe-runner.ts`: session file at `<runDir>/job-1.jsonl`, `pruneRuns(repoRoot, 0)` → `underRuns=true pruned=1 gone=true recSurvives=true`; contrast `outsideSurvives=true`. The runner-shaped class is real (EV-30 step-13 residual names it verbatim). **`closed-green`.**
- **O4 — `session-manager._persist` buffers entries until the first assistant entry.** Probe `probe-persist.ts`: `existsBefore=false` … `existsAfter=true lines=4 hasMarker=true hasUser=true` → `O4-BUFFERING: TRUE`. Source `session-manager.js:739-768`. A pointer written before the send/first-assistant can name ids not yet on disk — independent evidence for the `agent_settled`-side of O1c. **`closed-green`.**
- **O5 — README mode.** R-3 binds "directory mode `0700`, file mode `0600`"; owner conceded 0600-for-all in round 2. **Closed by Phase 1 ruling R-3 + concession; no probe applicable** (the Skeptic explicitly did not re-rule).
- **O6 — `PI_CODING_AGENT_DIR` is read at call time.** Probe `probe-env.ts`: two mkdtemp dirs in sequence → `r1=/tmp/ev31-env1-… r2=/tmp/ev31-env2-… r3=/home/tista/.pi/agent` → `O6-CALLTIME: TRUE`. Precedent `test/mcp/oauth.test.ts:20-21` + `auth-store.ts:26`. **`closed-green`.**
- **O7 — EV-30 `SpendRecord` shape unchanged, no `total`.** Probe `probe-shape.ts`: `keys=boundary,ownSession,subtree hasTotal=false own=session-reconciled sub=stream-assistant` → `O7-PURITY: TRUE`. **`closed-green` + EV-30 step-6 ruling item 2.**

*(Convergence-only items — module location, stamp call-site inherited from EV-30, `subtree.usageSource = "stream-assistant"`, own half `session-reconciled`, zero-both-halves, rejections — are listed under Agreed design above, labelled as convergence, not here as test-settled.)*

## Open judgment — for `product-owner`, escalating to `steward`

No test can settle these; both sides at equal weight.

1. **O3b — the read-back outcome distinction (the enum shape).** Owner holds `resolveProvenance` → `{ "ok" | "session-missing" | "range-missing" }` — a *resolution-mechanism* distinction (file-missing vs ids-absent-in-a-present-file). Principal holds stored write-time `pointerSurvivable` + a *derived* read-time outcome `{ "resolved" | "pruned-expected" | "missing-unexpected" }` — a *survivability-class* distinction (could-never-have-survived vs should-have-survived-and-didn't). Both are honest; they answer different questions and lose different information (owner loses the survivability defect-signal; principal loses the ids-absent-in-present-file case). No test *chooses* between two honest distinctions — a test only *encodes* one (principal's T-D4 encodes `missing-unexpected`; an owner-written counterpart would encode `session-missing`). The card's `goal` (traceability across pruning) leans principal's but does not force it. **Routes to `product-owner`.** *(The mechanics around this — never-throw, and the write-time-fact-stored / read-time-derived split forced by EV-30's rejected-2a staleness logic, refined by designer's `pointerStatusAtWrite` rename — are converged in substance and are listed under Open objections as confirmations, not here.)*

2. **O8 — the notify literal wording.** Designer handed P5/P6 *wording* to EV-32; principal parked it as `Needs Human`; the `[[smoke-test]]` precedent records that ruled copy is sourced from ruled literals, not in-repo constants. The *presence* of a notify call with an absolute path on failure is EV-31's and test-settleable (settled, above); the *literal* is a Phase-1-style copy ruling and is EV-32's rendered surface. **Routes to `product-owner` for ruling, to be applied on EV-32's card.** Non-blocking for EV-31's implementation.

## Open objections

Skeptic objections whose settling test has not passed.

- **O1c — which seam is *primary* (owner's "forest-settle primary + `agent_settled` sweep" composite vs principal's "`agent_settled`-gated-on-marker" primary).** `open-untested` — settling test not **run** (pre-implementation: no `extensions/usage-store.ts` exists). The settling tests are owner's own seam-order test (fake agent loop: settle-while-running → no write; manifest-complete → exactly one write; second settle → byte-identical no-op) and principal's T-D1(ii) `lastEntryId` oracle (the record's `boundary.lastEntryId` must equal the session's leaf at invocation end). The two sole-primary designs carry *different* defects (O1b kills forest-settle-only completeness — it fires mid-turn; owner's "dispatch turn's assistant reply completes while children still run" kills `agent_settled`-only freshness), so a strict test distinguishes them. **Judged: test-settleable-at-implementation.** *Contingent risk:* if the implemented seam-order test passes for *both* designs (test not strict enough to distinguish), O1c degrades into an open-judgment values call (which seam to favour) and would then route to `product-owner` — named here, not resolved.

- **O3b (mechanics) — never-throw confirmation + the write-time-stored / read-time-derived split.** `open-untested` — settling tests not **run** (pre-implementation). Settling tests: principal's T-D3 (runner-shaped: `pruneRuns(repoRoot,0)` → record survives, pointer reports its class, **no throw**) and T-D4 (delete the session file, not the run dir; re-read; stored JSON bytes `toEqual` the snapshot; the *derived* outcome reflects the deletion). The never-throw requirement and the write-time-stored/read-time-derived pattern are converged in substance (owner's `pointerStatus` + read-time `resolveProvenance` already separate the two times; designer's rename refines it; principal's split is the same pattern); the tests confirm it at implementation. **Judged: test-settleable-at-implementation.** (The outcome *distinction* those mechanics surface is the open-judgment item 1 above, not this.)

- **Gate-integrity injection not run.** `open-untested` — pre-implementation: the Skeptic named (did not hide) that no gate-failure injection was run because tree mutation is forbidden pre-branch; `council/preflight.sh` plus `bun test` / `bunx tsc --noEmit` / `python3 council/validate.py` must pass on the implementation tree per AGENTS.md convention 13.

## Ready to hand off?

**No — not yet to an implementation owner.** Blocked on exactly one item that needs a ruling seat before an owner can be dispatched:

- **Open judgment item 1 (O3b outcome-distinction)** — the owner of an implementation cannot write the read-back test without first encoding one of the two honest enum shapes, which is a values choice the consolidator may not make for them. This needs a `product-owner` ruling (escalating to `steward` per that seat's criteria) selecting the distinction the record owes a reader in the non-surviving case.
- **Open judgment item 2 (O8 notify wording)** is parked to EV-32 and does **not** block EV-31's implementation; EV-31 ships the notify *call* (presence test-settleable) and EV-32 picks up the *wording*.

The **open objections (O1c, O3b-mechanics, gate-integrity) are all pre-implementation artifacts** — there is nothing to run them against before a branch exists. Per the EV-30 precedent (whose step-5 synthesis held identical-shape pre-implementation `open-untested` items to block implementation *verification*, not hand-off), they do not block handing the card to an owner; they become the owner's **failing-first gating tests** that must pass before merge:
- O1c → the seam-order test (owner's) / T-D1(ii) (principal's) must be green at the implementation head before merge; neither sole-primary seam may ship on prose.
- O3b-mechanics → T-D3 (no-throw) and T-D4 (derived-vs-stored) must be green at the implementation head before merge.

**Recommendation:** route open-judgment item 1 to `product-owner` (escalating to `steward`) now; once the outcome-distinction is ruled, hand the card to a single implementation owner with O1c's seam-order test and O3b's T-D3/T-D4 as failing-first gates, and the O8 wording ruling queued for EV-32. The card remains `Deliberating` until the O3b ruling is received.

### Step 6 — routing (facilitator)

Phase 1 rulings consulted first. **R-3 (store)** binds the location, filename
grammar, README presence and file/dir modes; **R-SCOPE / R-ORDER / R-PROMOTE /
R-GATES / R-MERGE** contain no ruling on read-back outcome semantics. EV-30's
binding step-6 ruling item 3 defines the *unresolved-boundary label*
(`entries=unresolved` + `resolved` flag) and item 6 assigns choose-once/basis
to EV-31; neither defines how the store's read-back reports a pointer whose
session file has been pruned. Nothing on record answers the consolidator's
open-judgment item 1, so it is not re-asked as covered.

Per the consolidator's sort, **one item is open judgment** — open-judgment
item 1 (the O3b read-back outcome distinction: `owner`'s resolution-mechanism
enum vs `principal`'s stored write-time survivability + derived
survivability-class outcome). No test settles it (it is a choice between two
honest information trade-offs), and the owner cannot encode the read-back test
without it. Per `<escalation_contract>`, the facilitator may not dispatch
`product-owner`; the routing is carried by this card's `ESCALATION` report to
the orchestrator, which services it per `features-deliver.md` Phase 2. Facts
only; no recommendation is carried.

Open-judgment item 2 (the O8 notify *literal* wording) is EV-32's rendered
surface and non-blocking for EV-31; carried in the escalation as a forward
item, not as a blocker.

Open **objections** (O1c primary-seam choice; O3b never-throw/derived-vs-stored
mechanics; gate-integrity injection) are all pre-implementation artifacts with
no passing settling test yet. The consolidator judged O1c and O3b-mechanics
test-settleable-at-implementation and assigned them to the owner as
failing-first gating tests; they block implementation verification, not the
ruling. The card remains `Deliberating`.

### Step 6 — product-owner ruling (received; binding)

Phase 1 rulings applied first: **R-3 (store)** binds the store home
(`getAgentDir()/council/usage/`), the record filename
`<ISO-basic>_<runId>_<command>.json`, the README's presence, and the file
(`0600`) / directory (`0700`) modes — it does not define read-back outcome
semantics. The step-6 routing escalated the consolidator's open-judgment item
1 (the O3b read-back outcome distinction: `owner`'s resolution-mechanism enum
vs `principal`'s stored write-time survivability + derived survivability-class
outcome) to `product-owner`; no `steward` escalation was required. R-3 is
applied to the store home, filename grammar, README and modes; the ruling
below resolves the read-back outcome distinction R-3 does not cover. The
ruling is appended **verbatim** and is binding on every remaining step of this
card.

Appended verbatim:

---

- **Ruling** — (1) **Read-back outcome distinction**: position B (`principal`) wins on the stored write-time fact and the primary read-time distinction. Store the write-time field **`pointerSurvivable: boolean`** — true iff `sessionPath !== null && !path.relative(runsDir(repoRoot), sessionPath).startsWith('..')` (i.e., the session file was *outside* the pruned run dir at write time, or there was no file). Read-back derives the outcome via a never-throwing pure function `resolveProvenance(pointer)` returning **`ResolveOutcome = "resolved" | "pruned-expected" | "missing-unexpected" | "no-session-file" | "range-missing"`**, mapped: `sessionPath === null` → `"no-session-file"`; file present and both `firstEntryId`/`lastEntryId` resolve → `"resolved"`; file absent and `pointerSurvivable === false` → `"pruned-expected"`; file absent and `pointerSurvivable === true` → `"missing-unexpected"`; file present but one or both ids absent → `"range-missing"`. The `pruned-expected` / `missing-unexpected` split is the survivability-class distinction (the question the card's goal asks); `range-missing` is the resolution-mechanism refinement, riding alongside as a fifth value because both are honest and the goal permits neither to be silently folded away. Never throw. (2) **Non-blocking forward item (notify literal wording)**: confirm the deferral — the wording of the success/failure notify line is a Phase 1 copy ruling applied on **EV-32's** card, not EV-31's; EV-31 owns the *call site* (one notify on success, ≥1 notify on `EACCES` containing the absolute target path) and that property is test-settleable in EV-31 via a stubbed `ctx.ui.notify`.

- **Options rejected** — (A) **Position A's sole resolution-mechanism enum** (`"ok" | "session-missing" | "range-missing"`) — rejected: loses the survivability-class distinction the runner case requires; a `session-missing` outcome cannot tell a reader whether the store knew it would happen. (B) **Stored computed `resolvable` boolean** — rejected by EV-30 step-6 ruling item 2. (C) **Folding `range-missing` into one of the four survivability-class outcomes** — rejected: it is an orthogonal, real read-time state (file present, ids absent — a partial-backup or post-compaction-mutation case). (D) **Binding the notify literal wording in EV-31** — rejected by the `smoke-test` wiki precedent (ruled copy comes from rulings, not in-repo constants); EV-32 hosts the ruled-copy structure.

- **Grounding** — `vault/wiki/run-transcripts.md` (seat sessions, including the council-runner's own, live under the pruned run dir; the runner case is the motivating class for durability). `vault/wiki/eval-store-contract.md` (absence-by-design vs absence-by-defect must be distinguishable on read). `vault/wiki/smoke-test.md` (ruled-copy precedent). `council/cards/EV-30.md` step-13 residual (seat sessions live inside the pruned run directory; EV-31 must own survival/basis for that). EV-30 step-6 ruling items 2, 3, 6 (stored computed resolvability rejected; unresolved-boundary label intact and orthogonal; choose-once/recompute-basis belong store-side — marker `at` key, no recompute, converged). EV-31 card `goal` + `Intent` (the human's traceability question is the answer the store embodies).

- **Reversibility** — Pre-implementation: no worktree/PR, no `extensions/usage-store.ts`, no persisted records. Changing the stored field name is a one-line shape change; renaming `ResolveOutcome` values is a one-commit change. The five-value enum collapses to four (drop `range-missing`) if a later ruling decides the mechanism distinction is not worth a first-class outcome, but the `pruned-expected` / `missing-unexpected` split is load-bearing for the runner case. Notify-literal deferral moves one comment from EV-31's card to EV-32's; no code paths touched.

---

**Effect on this card.** The consolidator's open-judgment item 1 is resolved:
the store holds the write-time `pointerSurvivable` fact and read-back derives
a five-value `ResolveOutcome` (`resolved | pruned-expected |
missing-unexpected | no-session-file | range-missing`) via a never-throwing
`resolveProvenance`. Open-judgment item 2 (the notify *literal* wording) is
confirmed deferred to EV-32; EV-31 ships the call site only (one notify on
success, ≥1 notify on `EACCES` naming the absolute target path), asserted via
a stubbed `ctx.ui.notify`. The open objections (O1c seam choice; O3b
never-throw/derived-vs-stored mechanics; gate-integrity injection) remain
pre-implementation and become the owner's failing-first gating tests, per the
step-6 routing above; this ruling is what unblocked the hand-off.

**Drafting note on `pointerSurvivable` (applied reading, not a new ruling).**
The ruling defines the field twice: by a formula, and by an explicit gloss plus
the read-back mapping. They disagree. `path.relative(runsDir(repoRoot),
sessionPath)` starts with `..` exactly when `sessionPath` is *outside*
`runsDir` — so the formula as literally written
(`!path.relative(…).startsWith('..')`) yields `true` for a path *inside* the
run dir and `false` for a path outside it. The gloss says the opposite ('the
session file was *outside* the pruned run dir at write time'), and the mapping
makes `false` the `pruned-expected` case — i.e. the inside case, which is what
pruning is expected to remove. The gloss and the mapping are mutually
consistent and are the ruling's operative semantics; the literal formula
fragment is arithmetically inverted against both. This card and the spec
implement the operative semantics:
`pointerSurvivable === true` iff `sessionPath !== null &&
path.relative(runsDir(repoRoot), sessionPath).startsWith('..')` (outside the
run dir at write time). The `sessionPath === null` case is caught by the
mapping's first rule, `no-session-file`, before `pointerSurvivable` is
consulted. Recorded here so a later reader sees both the literal text and the
reading actually applied; it is the ruling being applied, not a dispute being
re-decided.

### Step 7 — settled design spec written; handed to one owner

Spec committed at `docs/superpowers/specs/2026-09-11-EV-31-design.md` — a
write-up of the settled design applying the step-6 ruling, not a
re-derivation. Self-reviewed for placeholder text, internal consistency with
the deliberation and the ruling, scope (nothing beyond the card's `goal`),
and ambiguity (an owner reading only the spec cannot reach two designs): the
store home/filename/modes come from R-3; the record shape, the marker-`at`
key, the gated write path (which settles open objection O1c at
implementation), the five-value `ResolveOutcome` and never-throw read-back,
the README shelf, and the notify presence tests all come from the ruling and
the deliberated convergence. State set to `In Progress` on the card
frontmatter and on `council/board.md`. Handed to a single `owner` in an
isolated worktree, branched from local `main` (EV-30 merged at `f33cbdf`).

### Step 8 — owner plans, implements, opens PR (job `job-15.1`)

One `owner` dispatched in the pre-existing worktree
`.worktrees/ev-31-usage-store` (branch `feat/ev-31-usage-store`), handed the
committed spec and the card's binding step-6 ruling. Main-repo immutability
restated in the dispatch. Owner completed in 18.5 min.

**Observed artifacts (facilitator-verified, not written from the seat's
report):**

- PR **#44** `https://github.com/jumpseat-inc/pi-council/pull/44` — state
  `OPEN`, base `main`, head branch `feat/ev-31-usage-store`.
- Head SHA **`22cc08ab8144f2efdbd4eb4d6b3aec0dfdf0f991`**; `git ls-remote
  origin feat/ev-31-usage-store` returns the same SHA; the worktree HEAD
  equals it; the main checkout remains on `main`, clean, untouched.
- Files: new `extensions/usage-store.ts`, new `test/usage-store.test.ts`, new
  `docs/superpowers/plans/2026-09-11-EV-31-plan.md`, edited
  `extensions/index.ts`.

**Owner's local gate results (unverified by the facilitator at this step;
step 9 re-runs them at the head):** `bash council/preflight.sh` → `PASS:
preflight clean`; `bunx tsc --noEmit` → exit 0; `bun test` → 619 pass, 2 skip,
0 fail (integration gated behind `COUNCIL_INTEGRATION=1`); `python3
council/validate.py` → `All council artifacts valid`.

**Owner-stated deviations from the spec:** (1) the gated path's
`PendingInvocation` + `flushPendingInvocations` live in `usage-store.ts`,
not inline in `index.ts`, to make T-U13/T-U14 testable — behaviorally
identical to spec §3; (2) after a caught write failure the pending entry is
dropped (notify once with the absolute path, no retry); (3) T-U14's `EACCES`
fixture uses a read-only *parent* dir because `ensureUsageDir` re-asserts
`0700` on the store root; (4) notify lines are placeholders (wording is
EV-32's per the ruling).

`In Review` set from the sole observed-artifact condition — an open PR — per
council.md step 8. Next: step 9 Skeptic verification at `22cc08ab`.

### Step 9 — Skeptic verification at the branch head (job `job-15.2`, cycle 1 of ≤3)

Dispatched at the pinned subject — PR #44 head SHA
`22cc08ab8144f2efdbd4eb4d6b3aec0dfdf0f991`, worktree
`.worktrees/ev-31-usage-store` — with the loop frame pinned (step 9 verification
precedes step 10 judging and step 11's mechanical merge, which the facilitator
executes and no seat performs). The Skeptic confirmed `rev-parse HEAD` equalled
the pin before and after, re-ran the full gate set itself, and attacked the
implementation with independently constructed fixtures.

**Gates re-run by the Skeptic at the head (real output):**

- `bash council/preflight.sh` → **`FAIL: local history does not descend from
  origin/main (stale before running a card)`**. Root cause established, not
  assumed: `origin/main` = `2290e61` (this card's step-8 record commit, landed
  after the branch's implementation commits). `merge-base --is-ancestor
  origin/main HEAD` → not-ancestor; `HEAD..origin/main` is exactly that one doc
  commit. The owner's green preflight predates the base move. **Resolution per
  recorded practice, not facilitator judgment:** [[preflight]] defines preflight
  as the gate that must pass *before a run starts* (run-start and owner-time),
  and [[deterministic-merge-check]] names criterion 1's step-11 re-run set as
  `tsc` / `bun test` / `validate.py`; the red history line is the by-construction
  artifact of the recipe "push records as they happen" ([[union-merge-reconcile]],
  FLLWUP-27) — the same shape EV-28's step 11 recorded. Not a deliverable defect,
  not a criterion-1 failure. The base-advance artifact is already filed
  (FLLWUP-27, `Backlog`).
- `bunx tsc --noEmit` → exit 0, no output.
- `bun test` → **619 pass / 2 skip / 0 fail**, 3131 expects, 60 files (the 2
  skips are the pre-existing network-gated tests; integration stays behind
  `COUNCIL_INTEGRATION=1`).
- `python3 council/validate.py` → `All council artifacts valid`, exit 0.

**Independent probes:** `/tmp/ev31-step9/probe.ts`, own fixtures, every store
under `fs.mkdtempSync`, `PI_CODING_AGENT_DIR` set to a tmp dir → **33/33 PASS**.
Covers acceptance-with-prune (`resolved`), runner-shaped `pruned-expected` (no
throw, record survives), `missing-unexpected` with stored bytes byte-identical
across the outcome flip, `no-session-file`, `range-missing`, choose-once across
a simulated process restart (fresh state, `now` 2026→2099), the seam-order
oracle (unsettled → 0 files; settled → exactly 1 with `lastEntryId === leaf`;
re-settle → byte-identical no-op), `pruneRuns(repo,0)` byte-identity, modes
`700/600/600` + README non-clobber, the env seam (real `~/.pi/agent` untouched),
corrupt/partial-record skip, and filename byte-exactness.

**Gate-integrity injection:** mutated `"pruned-expected"` → `"pruned"` in
`resolveProvenance` in the head worktree → `bun test test/usage-store.test.ts`
went **red** (`15 pass, 2 fail`: T-U10, T-U12); restored byte-identically and
re-ran → `17 pass, 0 fail` + `tsc` green. The gate can fail.

**Objections (all `closed-green`, none `open-untested`, none `closed-red`):**

- **O1c** (seam order / `lastEntryId === leafId` oracle) → `closed-green` — probe
  P6 + owner T-U13: no write while unsettled, exactly one on settle with
  `lastEntryId = a1 = leaf`, byte-identical no-op on re-settle.
- **O3b** (never-throw + derived-vs-stored, five-value ruling mapping) →
  `closed-green` — probes P2/P3/P4/P11 exercised every mapping row, including a
  throwing injected reader, without a throw; stored bytes immutable across
  outcome flips.
- **O2** (marker-`at` key) → `closed-green` — probe P5 + owner T-U9; the
  resumed-session phantom-duplicate class is structurally closed.
- **O4/O5/O6/O7** (step-4 residuals: session buffering, README-0600, env
  call-time seam, `SpendRecord` purity) → `closed-green` — probes P8/P10/P1-spend
  + owner T-U3/T-U15/T-U16.

**Verdict: `PASSES` — no open or red blocking objection on the deliverable.**
The Skeptic independently reproduced the card goal's acceptance path and found
the implementation consistent with the binding ruling byte-for-byte. Hygiene:
head worktree `git status --porcelain` clean before and after; main checkout
untouched. Two facts the Skeptic named as facilitator-owned, neither an owner
fix: the preflight history red line (above) and the fact that the card file in
the head worktree predates the step-8 record (board bookkeeping; the record is on
`main`).

### Step 10 — judge verdict (job `job-15.3`)

Dispatched with **the card's `goal` verbatim and the step-9 Skeptic evidence
only** — no spec, no deliberation, no facilitator framing — at the same pinned
subject (`22cc08ab…`) with the step-10 frame pinned (judging precedes step 11's
mechanical merge, executed by the facilitator). The judge re-ran the decisive
test itself at the head: `bun test test/usage-store.test.ts` → **17 pass / 0
fail**, including the acceptance test T-U1 (write → `fs.rmSync` the run dir →
read back → `resolveProvenance` = `resolved`, both `firstEntryId` `u1` and
`lastEntryId` `a1` present in the surviving session file); full suite 619 pass /
0 fail / 2 skip. **Verdict: `PASS`** — all three clauses of the card `goal`
are satisfied; the preflight base-staleness is a workflow artifact, not a
deliverable defect.

### Step 11 — deterministic five-criteria check and merge

Checked at PR #44 head `22cc08ab8144f2efdbd4eb4d6b3aec0dfdf0f991`. All five
criteria satisfied, verified directly by the facilitator:

1. **Every owner gate green, in full.** Facilitator re-run on the head worktree
   at the pinned SHA: `bunx tsc --noEmit` → exit 0; `bun test` → 619 pass / 2
   skip / 0 fail; `python3 council/validate.py` → `All council artifacts valid`
   (exit 0). `bash council/preflight.sh` was green for the owner at
   implementation time; the step-11 re-run set is tsc / `bun test` / `validate.py`
   per [[deterministic-merge-check]], with preflight the run-start and owner-time
   gate per [[preflight]] — the red history line at re-run is the by-construction
   artifact of mid-card record pushes (FLLWUP-27; same shape as EV-28's step 11).
2. **GitHub Actions green on the PR head SHA.** `gh pr checks 44 --json
   name,state,workflow` → `[{"name":"gates","state":"SUCCESS",
   "workflow":"gates"}]` — the `gates` workflow present with `state: SUCCESS`,
   keyed on `workflow`.
3. **No blocking Skeptic objection.** Step 9 verdict `PASSES`; no `open-untested`
   and no `closed-red` items.
4. **Judge verdict `PASS`** (step 10).
5. **No `Needs Human` state or outstanding ruling.** Card was `In Review`; the
   step-6 `product-owner` ruling is applied and recorded, nothing outstanding.

Not the first merge of this run (EV-28 PR #42 and EV-30 PR #43 already merged),
so no first-merge watch. Pre-merge re-read: `headRefOid` =
`22cc08ab…`, unchanged. Merge executed with the SHA pinned:
`gh pr merge 44 --squash --match-head-commit
22cc08ab8144f2efdbd4eb4d6b3aec0dfdf0f991` → exit 0.

### Step 12 — sync, `Done`, push

- Squash-merge SHA: **`f3bcd8a4e58d748907ef8d101ec13995e91d974b`** (PR #44,
  merged 2026-09-11T07:36:34Z).
- GitHub Actions `gates` on the **merged** SHA: run `34575186051` →
  `gh run watch 34575186051 --exit-status` exit 0, all steps green
  (`bun install --frozen-lockfile`, `bunx tsc --noEmit`, `bun test`,
  `python3 council/validate.py`).
- Local `main` fast-forwarded `63fcb7b..f3bcd8a` (`git merge --ff-only
  origin/main`, exit 0) — no divergence, no [[union-merge-reconcile]] needed this
  card. Merged-tree gate evidence is the green gates run above.
- Card and board set `Done` from the observed artifact alone (merged + gates green
  on the merged SHA), then `python3 council/validate.py` clean.

**Caps used:** step-3 exchange 2 of ≤3 rounds (steps 1–7, prior container); step-9
verify→fix cycles **1 of 3** (initial verification only — no fix cycle). Step-6
ruling applied; no `steward` escalation was required. Phase 1 rulings applied:
R-3 (store) plus R-SCOPE/R-ORDER/R-PROMOTE/R-GATES/R-MERGE.
