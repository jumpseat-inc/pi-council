---
id: EV-39
title: Hub-level retry of a seat dispatch with exponential backoff
state: In Review
owner: null
epic: EPIC-9
goal: A seat dispatch whose settled report is classified retry is re-spawned with exponential backoff until the budget is exhausted or an attempt settles without a retryable error, and the final report names how many attempts were made.
---

## Intent

This card does not address the intake; it fixes a child seat's provider error.
Council already owns this path — the child's error is captured at
`hub.ts:196-205` and the parent can re-spawn the job — so it is the second,
easier half of the epic.

The budget source is the `retry` section's `maxAttempts`, `baseDelayMs`,
`maxDelayMs`, and `jitter` fields (R2), and the delay follows pi's existing
shape (`baseDelayMs * 2^(attempt-1)`, capped at `maxDelayMs`, jittered when
`jitter` is true). The final report carries an explicit attempts sentence.

## Acceptance

Amended per the step-6 `product-owner` ruling (Q4 amendment) and the step-6
`steward` ruling (Escalation 2). Binding on implementation and verification
alike.

- A dispatch whose settled report `classifyRetry` classifies `retry` is
  re-spawned with `computeBackoffDelay` backoff until the budget is exhausted
  or an attempt settles non-retryably; the final report names the attempts
  made (`attempts=N/M` only when `N > 1`, plus one per-outcome sentence), and
  a single-attempt dispatch's manifest and report stay byte-identical to
  pre-EV-39.
- One job id, one manifest, one job-tree row per dispatch; the row is labeled
  `attempt N/M` while `N > 1` (R4).
- **Q4 disclosure (mandatory — the silent partial is forbidden).** The
  dispatch's provider-reported figure is, at EV-39, the final attempt's alone.
  It must be disclosed **machine-readably and durably**: a record-level reason
  on the provider sibling (`ProviderCostReport`), in the existing
  record-only-reason pattern (`ProviderUnavailableReason`-style literal set at
  `extensions/provider-cost.ts`), persisted on the [[usage-store]] record and
  rendered through the usage block's conditional legend
  (`extensions/usage-block.ts`). A prose-only note is insufficient; a stored
  figure that reads whole a month later is the falsification. The
  final-attempt-only figure must never render as *the dispatch's* reported
  figure unqualified. Wholeness itself is EV-42's closing deliverable.
- `bunx tsc --noEmit`, `bun test`, `python3 council/validate.py` stay green;
  the owner gates are met in full regardless of change size.

## Orchestrator rulings (Phase 1, binding)

Recorded human decisions for this run. Immutable and binding on every seat,
`steward` included; a runner that hits one applies it and cites the ruling
rather than re-asking.

- **R4 (intermediate-attempt visibility).** One job-tree row per dispatch,
  labeled `attempt 2/3` while retrying — no extra rows and no separate status
  line, so the tree never shows two rows for one intent. The final report
  still names the attempt count.

## Run record (features-deliver / EV-39)

### Step 1 — classification (facilitator)

- **Path: full council.** The card is cross-seam — the hub job supervisor
  (`extensions/hub.ts`: `spawnJob`/`settle`/`writeJobManifest`/`tick`/`report`),
  the dispatch+wait tool layer (`extensions/hub-tools.ts`:
  `council_dispatch` builds the child argv and mints the job id;
  `council_wait` reads `JobReport`s and formats them), the run substrate
  manifests (`extensions/runs.ts`), and the job-tree renderer
  (`extensions/tree.ts`, `extensions/navigator.ts`) — and it is
  spec-ambiguous: the `goal` fixes the outcome (a retry-classified settled
  report is re-spawned with backoff until budget exhausted or a clean settle,
  and the final report names the attempt count) but the mechanism is open —
  where the loop lives (inside `Hub` vs. the tool layer that holds the dispatch
  inputs), whether a re-spawn reuses the job id/manifest/session file or mints
  siblings, how the wait path observes an in-flight retry, how the backoff
  interacts with the stall/timeout monitor and `cancel`, and what the final
  report's attempt sentence is. That is design judgment, not one
  implementation choice. Per council.md step 1 this card runs steps 2–6 and
  then 7–14.
- **Surface-touching: yes (recorded).** R4 is a recorded human decision that
  fixes user-visible job-tree copy (`attempt 2/3` on the row of a retrying
  dispatch) and the final report's attempt count. A full-council,
  surface-touching card seats `designer` as a third generator in steps 2–3.
  R4 bounds that seat's subject matter (one row per dispatch is ruled, not
  open) but does not unseat it: where on the row the label renders, how it
  reads while the state glyph is `running` vs `stalled`, and the final
  report's attempt sentence remain design questions.
- **Rulings applied (Phase 1, binding): R4 applies to this card as written and
  is quoted verbatim above; it is applied, not reweighed.** R1 (classifier
  superset — `extensions/retry.ts` `classifyRetry`), R2/R3 (retry defaults
  `enabled:true, maxAttempts:3, baseDelayMs:2000, maxDelayMs:30000,
  jitter:true` + validation — `extensions/seats.ts` `loadRetryConfig`,
  mirrored in `council/scaffold/.council.json`) are this card's inputs and are
  cited where they bind. R5 (parent-turn copy) is EV-40's surface, not this
  card's. No ruling is extended to a question it did not answer.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `principal`, `designer`, `skeptic`, `consolidator`, and `judge`.
  All six resolve by name: packaged seats at
  `/home/tista/.pi/agent/git/github.com/jumpseat-inc/pi-council/council/agents/*.md`,
  each file's `name:` matching its filename, verified first-hand this run.
  This checkout has **no `.pi/agents/`** directory, so nothing shadows them and
  no repo-local override exists. `product-owner` / `steward` are not dispatched
  by this container (escalation-only). No `Unknown seat` is possible on the
  seats named here.
- **Environment (facilitator-read, first-hand):** main checkout clean at
  `f3c2ed18a5c8830e5d1428759dc305ae0ad94ecc` == `origin/main`; `gh`
  authenticated as `tistaharahap`; no open PRs. EV-37 (`6a375c4`), EV-38
  (`3e39e66`), EV-43 (`8853712`) and EV-40 (`6ab0e48`) are ancestors of `HEAD`
  with their cards `Done`. This card was `Ready`; no `Needs Human` state and no
  outstanding ruling — deterministic merge check criterion 5 holds at the start
  of the card.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md`
  does not exist here):** `bash council/preflight.sh EV-39`,
  `bunx tsc --noEmit`, `bun test`, `python3 council/validate.py` (AGENTS.md
  §Commands + `.github/workflows/gates.yml`). This repo defines no
  database/import/server gate; `COUNCIL_INTEGRATION=1` stays gated and is not
  run. The `preflight.sh` branch-freshness line (FLLWUP-27) fires by
  construction once a facilitator record commit advances `origin/main` past a
  branch cut — recorded verbatim, never reclassified, and never used to weaken
  a criterion.
- **Grounded facts carried into deliberation (inputs, not a settled design;
  the Council verifies them at this tree):**
  - `Hub.spawnJob` (`extensions/hub.ts`) takes the full child spec
    (`id`, `seat`, `model`, `command`, `args`, `cwd`, `env`, `timeoutMs`,
    `stallMs`, `cleanup`) and stores it on the `Job`; the `Job` object holds
    `startedAt`, `lastActivityAt`, `timeoutMs`, `stallMs`, accumulated
    `usage`, `stopReason`, `errorMessage`. **The Hub never sees the seat
    `input` or the prompt file — those are baked into `args` and `env` by
    `council_dispatch`.** A Hub-owned re-spawn therefore either reuses
    `command`/`args`/`env` or must move the loop up to the tool layer.
  - `Hub.report()` returns the `JobReport` shape `classifyRetry` consumes;
    `Hub.settle()` is the single settle point (`proc.on("close")` → state
    `done`/`failed`, `writeJobManifest`, `onChange`); `Hub.tick()` sets
    `stalled` (kills the process group) after `stallMs` of no activity and
    informational `timeout` after `timeoutMs` (never killed).
  - `extensions/retry.ts` already exports `classifyRetry(report)` (EV-37,
    keys on state+stopReason+errorMessage) **and** the pure
    `computeBackoffDelay(policy, attempt, rand)` (EV-40). A hub loop reusing
    these two is the path of least divergence; minting a second delay
    function would create the two-loop divergence EV-40's watch item warns
    about.
  - `loadRetryConfig(repoRoot)` (EV-38, `extensions/seats.ts`) always returns
    a full policy and throws on malformed input; this repo's `.council.json`
    carries **no** `retry` section, so the R2 defaults apply here.
  - `RunManifest` (`extensions/runs.ts`) has **no** `attempt` field today;
    `writeJobManifest` writes `id/seat/model/parentJobId/pid/sessionId/state/
    startedAt/settledAt/exitCode/usage/stopReason`. The `attempt` field and
    the sibling-binding id are EV-42's named deliverable; EV-39 precedes it
    in the build order.
  - The tree row text is `tree.ts`'s `textTree` (`glyph id seat state Xm
    pid=`) and the navigator's row renderer; R4's `attempt 2/3` is a new
    fragment on that existing row.
- **Watch item for the deliberation (roster interaction, carried from
  EV-40's settled spec):** the two backoff loops key on different subjects —
  EV-40's on a parent provider call, this card's on a settled child job
  report — and neither may read the other's counter. EV-40's spec (merged
  `6ab0e48`) records the agreed **field name `attempt`** and nothing more;
  per-attempt run-substrate identity (`attempt` manifest field +
  `attemptGroupId`) is EV-42's card, scheduled immediately after this one.
  AGENTS.md §7 binds too: `hub.ts`'s stall/timeout/kill semantics are
  battle-tested — **change behavior only with a failing test first**.
- **`brainstorming` step (council.md step 2's first pass) is satisfied by this
  dispatch.** No separate brainstorming session is opened: the deliberation
  below is the brainstorming discipline, and the human's intake is the card's
  `Intent`. The card's `Intent` already fixes the budget source (R2 fields),
  the delay shape (`baseDelayMs * 2^(attempt-1)`, capped, jittered) and the
  explicit attempts sentence in the final report.

### Step 2 — independent first pass (round 1)

Dispatched in parallel, each given **only the card** (no seat saw another's position). Jobs: `owner` `job-19.1` (16 turns), `principal` `job-19.2` (17 turns; its session ended with a `Network connection lost` provider line after the position was complete), `designer` `job-19.3` (8 turns). All three settled `done`. Positions below are extracted verbatim from the seat session JSONL under `.pi/council/runs/`.

#### Owner position (`job-19.1`) — verbatim

I have read everything first-hand. Here is my independent first-pass position.

---

# EV-39 — owner first-pass position

## Position

### 1. Where the loop lives

**A new module, `extensions/job-retry.ts`, supervising per-dispatch — not a loop inside `Hub`, not a loop around the tool calls.** The decisive fact is in `hub-tools.ts:243-261` (`council_dispatch` execute): the seat `input` exists only as a local (`params.input`), the prompt file is a per-dispatch `mkdtempSync` tmpdir, and the cleanup closure that deletes it is handed to `hub.spawnJob`. `Hub.spawnJob` (`hub.ts:106`) stores `command`/`args`/`env` only implicitly as the spawn call's inputs — the `Job` object holds none of them, and `settle()` (`hub.ts:251-259`) **runs and nulls `cleanup`**, deleting the prompt file the moment attempt 1 closes. So:

- **Hub-owned re-spawn** reusing stored `command`/`args`/`env` is impossible as-is: the spec isn't stored, the prompt file is already deleted, and the retry *decision* (policy classification) is not Hub's business — `Hub` would import policy concerns, violating its supervision-pure role.
- **A loop around dispatch/wait at tool granularity** is impossible: `council_dispatch` is one-shot stateless per call and `council_wait` *blocks the parent's turn* — nobody is home between tool calls to run a loop.
- **The seam is therefore post-spawn, pre-forget**: `council_dispatch` already holds everything the retry needs (the full spawn spec, the prompt tmpdir, the resolved seat/model). It hands that bundle to a `RetrySupervisor` (new `extensions/job-retry.ts`) as fire-and-forget (`void`), which subscribes to the Hub's existing `onChange` (composed in `getHub`, `hub-tools.ts:34-52`) to observe settles — no new Hub→supervisor callback API. The supervisor classifies, sleeps (injected timer, unref'd), re-spawns via `hub.spawnJob`/a small new `Hub.respawn`, and finalizes. `hub-tools.ts` keeps only wiring; the supervisor's decision core is injectable and unit-testable. Keeping it out of `hub.ts` honors AGENTS.md §7 — the Hub gains only small, additive, individually-tested deltas (listed under 4), never a policy import.

Cost accepted: one more module and one more indirection (dispatch → supervisor → hub) versus a Hub that "just knows". The indirection is the price of keeping `hub.ts`'s battle-tested stall/kill semantics untouched.

### 2. Identity across attempts

- **Job id: reused.** R4 ("one job-tree row per dispatch") makes this non-optional — the tree (`tree.ts:64-67`, `navigator.ts:344`) reads `readManifests` keyed by manifest `id`; a sibling id would mint a second row, which R4 forbids. The supervisor re-spawns under the same id; `Hub.jobs.set(id, job)` (`hub.ts:156`) replaces the Job object.
- **Manifest: one file, overwritten per attempt.** `writeManifest` (`runs.ts:111-113`) writes `<id>.json` atomically-overwriting; the final write is the last attempt's settled state. That is exactly R4's "no extra rows". EV-39 must **not** invent per-attempt manifest filenames — that plus `attemptGroupId` is EV-42's deliverable.
- **`attempt` field: added now, name agreed, semantics minimal.** R4's `attempt 2/3` is rendered from manifests (the only channel the passive renderers read — the navigator is a component over `readManifests`, `navigator.ts:344`), so without the field R4 is unexpressible. EV-40 §2.8 (spec:313-316) explicitly leaves the run-substrate `attempt` field to "EV-39, EV-42" with only the *name* agreed — so EV-39 mints `attempt?: number` (current attempt; **absent for attempt 1**, so every existing non-retry manifest stays byte-identical) and EV-42 later adds `attemptGroupId` + sibling binding. EV-39 must not pre-empt: no `attemptGroupId`, no sibling ids, no second field name for the total.
- **The "of 3" total** comes from the policy, not the manifest: the renderers compose `attempt ${n}/${loadRetryConfig(repoRoot).maxAttempts}` — no new un-agreed manifest field.
- **Session dir: same run dir.** **Session file: one per attempt, session-id-suffixed for n ≥ 2** (`--session-id ${id}-attempt2`). Today `writeJobManifest` hardcodes `sessionId: job.id` (`hub.ts:96`); two attempts sharing it would leave two `.jsonl` files with identical first-line ids and `findSessionFile` (`runs.ts:139-160`) returning an arbitrary one — the navigator would tail the *wrong* attempt's transcript live. `id` vs `sessionId` are already distinct manifest fields, so the substrate supports this without new identity machinery; the suffix naming is **provisional, EV-42 finalizes it**.
- **Usage accumulator: carried, cumulative.** A fresh `spawnJob` zeroes usage; attempt 2's spend would be lost from the manifest and the parent's usage block would under-count (an EPIC-7 honesty violation). New `Hub.respawn(id, opts)` carries `prev.usage` into the new Job (resetting `startedAt`/`output`/`stopReason`/`errorMessage`/`exitCode`/state). Every attempt's manifest then carries the *cumulative* tuple; `sumSubtreeUsage` stays correct (one file per id, containing the true total); per-attempt breakdown lives in the per-attempt session JSONLs.

### 3. The retry decision

**Reuse `classifyRetry(report)` (`retry.ts:103-123`) and `computeBackoffDelay(policy, attempt, rand)` (`retry.ts:155-168`) verbatim — no second copy.** The EV-40 watch item warns precisely against two diverging delay functions, and `classifyRetry`'s state-independent clause is the settled R1 semantics; re-deriving either is divergence by construction. The supervisor's decision step is: on settle → `classifyRetry(hub.report(job))`; `"retry"` and `attempt < maxAttempts` and `policy.enabled` → backoff+respawn; `"retry"` with budget spent → exhausted finalize; `"terminal"`/`undefined` → final settle (unchanged behavior).

**`loadRetryConfig(repoRoot)` is read per dispatch** inside `council_dispatch` execute (cheap file read; a mid-session policy edit applies to the next dispatch; tests inject the policy directly into the supervisor, bypassing the file). On **malformed config**: catch the throw, **dispatch proceeds with retry disabled, and the tool result carries a loud warning line** (same posture as EV-40's parent path — "a throw disables retry and notifies; the malformed-config session must not crash", EV-40 spec §3.3). Refusing the dispatch because its *policy file* is malformed would conflate a legitimate dispatch with a config defect. This does not violate gate-parity (that rule constrains write/validation layers being stricter than runtime, not runtime read-leniency).

### 4. Interaction with the supervisor

The Hub deltas — each additive, each with a failing test first (AGENTS.md §7):

- **New `JobState "retrying"`** (`hub.ts:5` union) used only during the backoff window: set by the supervisor synchronously in the settle-observation path, *before* any 200ms wait poll can see the settled inter-attempt state. During `"retrying"`, `tick()` already ignores the job (it monitors only `"running"`, `hub.ts:233`) — correct: no live process to stall or time out. An attempt that itself stalls or times out is unaffected (stall → terminal per `classifyRetry`; timeout informational → close → classify).
- **`isSettledForWait` gains a `"retrying" → false` clause** (`hub.ts:291-297`) — this is the Q5 fix; see 5.
- **`cancel` handles `"retrying"`** (`hub.ts:261-274`): today it bails on `exitCode !== null` — true during backoff — so a parent couldn't abort a retry. New: state `"retrying"` → set `"cancelled"`, write manifest, fire `onChange`; the supervisor's post-sleep check (`job.state !== "retrying"` → abort loop, run cleanup) is the arbitration point — no new Hub→supervisor edge needed. Cancel during attempt-N *running* is today's path and lands as terminal.
- **`shutdown`/teardown during backoff**: the supervisor gets `dispose()` called from the same place `shutdownHub()` runs in `index.ts` (session_shutdown), disarming pending timers (all unref'd so nothing holds the event loop) and finalizing supervised jobs (run real cleanup, leave the last manifest). `shutdown()`'s live-pid loop naturally skips retrying jobs (no live pid).
- **`writePids`** needs no change: during backoff `exitCode !== null` so the pid file correctly omits the job; after respawn the new pid is listed (`hub.ts:160`, `writePids` at settle/spawn).
- **Cleanup lifetime: the supervisor owns it under retry.** `settle()` runs `job.cleanup?.()` (`hub.ts:254`) — if the dispatch's tmpdir-removal closure reaches `spawnJob` while a retry is armed, attempt 1's settle deletes the prompt file out from under attempt 2. So: retry disabled → pass the closure through unchanged (byte-identical behavior); retry armed → pass a no-op to the Hub, hold the real one, and run it exactly once at loop end (clean settle, exhaustion, cancel-during-backoff, or dispose), best-effort wrapped.

### 5. Observation from a blocked `council_wait`

The hazard is real and I traced it: attempt 1 settles → `exitCode` set, state `done/failed` → `hub.wait`'s poll (`hub.ts:300-311`, 200ms interval, `isSettledForWait` `hub.ts:291-297`) would resolve **mid-retry**, handing the parent a report that looks final; the parent then `cancel`s or moves on while the supervisor is about to re-spawn — a cancelled-but-actually-retrying job. The fix is the `"retrying"` state + the `isSettledForWait` clause (4): the supervisor flips the state synchronously inside its settle observation, which fires before the next 200ms poll, closing the window by construction. `council_wait` therefore resolves once, after the *final* attempt — or at its deadline, returning an honest `state=retrying` report with the `attempt 2/3` label (the parent re-waits; that is the documented never-cancel-on-timeout contract).

`council_cancel` during backoff: "Cancelled job-5.", the chain aborts, no attempt 2 ever spawns (spawn-count falsifiable, P2 below), row renders `⊘ … attempt 2/3`.

### 6. The final report

Produced in **`formatWaitReport` (`hub-tools.ts:106-122`)** — it already receives `manifests` (source of `attempt`) and is called from `council_wait` execute, which has `repoRoot` (for the policy's `maxAttempts`); plus one line in **`formatReport` (`hub-tools.ts:78-104`)**. Shape:

- **Head fragment** (only when the job made >1 attempt; single-attempt reports stay byte-identical — EV-28's byte tests hold): `[job-5] seat=skeptic state=done stopReason=stop attempts=2/3 elapsed=3.1m <usage-segment>` — `key=value` matches the head's existing grammar.
- **Explicit sentence** after the body/error sections, before the usage block, for jobs with `attempt ≥ 2` in the manifest:
  - clean: `Settled on attempt 2 of 3.` 
  - exhausted: `Retry budget exhausted after 3 of 3 attempts; last error: <errorMessage>.`
- The usage block (EV-32 point 5) is appended unchanged and now reflects cumulative cross-attempt spend via the carried usage.

The exact sentence copy remains designer territory (R4 leaves it open — facilitator record, step 1); my commitment is the *shape*: head fragment + one sentence, count = attempts made, total = policy max.

### 7. Budget exhaustion

The last failed attempt leaves: state = that attempt's settle state (typically `done` with `stopReason="error"` + a retryable `errorMessage` — EV-37's own comment, `retry.ts:5-8`, notes provider-errored children exit 0), manifest `attempt = maxAttempts` with cumulative usage, cleanup run, supervisor disarmed for the id. The loop distinguishes exhausted from clean-settle-on-2 **without extra state**, derivable in `formatWaitReport`: `classifyRetry(finalReport) === "retry"` ∧ `attempt === policy.maxAttempts` ⇒ exhausted sentence; `attempt ≥ 2` otherwise ⇒ clean sentence. No supervisor memory leak, no new persistence.

### 8. Tests (failing first, in order)

New `test/job-retry.test.ts` + additions to `test/hub.test.ts`, `test/runs.test.ts`, `test/navigator.test.ts`:

1. **T1 (red-first, core loop)** — stub child scripted fail-once-then-succeed (existing `STUB_MODE=error` emits "Provider returned 502: upstream unavailable", matches the "502" pattern); supervisor with `{enabled:true, maxAttempts:3, baseDelayMs:1, maxDelayMs:1, jitter:false}`, injected timer. Assert: `council_wait`-equivalent resolves **once**, final state `done`, output from attempt 2, head carries `attempts=2/3`, sentence "Settled on attempt 2 of 3."
2. **T2 (exhaustion)** — fail all 3: exhausted sentence, manifest `attempt: 3`, cleanup ran (tmpdir gone), no further respawn.
3. **T3 (backoff math)** — respawn timestamps honor `computeBackoffDelay` with injected `rand`; `jitter:true` widens per the EV-40 spec.
4. **T4 (the Q5 regression)** — assert `hub.wait` never resolves during the backoff window and returns exactly one report per dispatch (fails today: today it can't retry at all, and with any naive implementation it resolves between attempts).
5. **T5 (cancel during backoff)** — `hub.cancel` mid-backoff → wait resolves `cancelled`, spawn count stays 1, cleanup ran.
6. **T6 (dispose)** — teardown during backoff → no respawn, cleanup ran.
7. **T7 (Hub units)** — `isSettledForWait("retrying") === false`; `cancel` on a retrying job returns true + sets cancelled; `respawn` carries usage; `spawnJob`/`respawn` `sessionId` opt lands in the manifest.
8. **T8 (manifest identity)** — retry writes `attempt: 2` into `<id>.json` (one file, no sibling); **snapshot**: a non-retry dispatch's manifest is byte-identical to pre-EV-39 (no `attempt` key).
9. **T9 (renderers)** — manifest `attempt:2` renders `attempt 2/3` in `textTree` and the navigator row; `"retrying"` glyph/state copy; `STATE_ORDER`/`STATE_TOKEN` entries.
10. **T10 (disabled path)** — `enabled:false` → single spawn, report identical to today.
11. **T11 (malformed policy)** — `loadRetryConfig` throws → dispatch succeeds, warning line present, no retry.

Gates in the repo's order: `bash council/preflight.sh EV-39` → `bunx tsc --noEmit` → `bun test` → `python3 council/validate.py`.

## Evidence (first-hand)

- `hub.ts:5` (JobState union), `:88-104` (`writeJobManifest`, `sessionId: job.id` at :96), `:106-159` (`spawnJob` — spec not stored on Job), `:233-248` (tick: stall kill / informational timeout, running-only), `:251-259` (`settle` — runs and nulls `cleanup`), `:261-274` (`cancel` bails when `exitCode !== null`), `:291-297` (`isSettledForWait`), `:300-311` (`wait` 200ms poll), `:328-341` (`shutdown`).
- `hub-tools.ts:34-52` (`getHub` — the single `onChange` composition point), `:78-104` (`formatReport` head grammar), `:106-122` (`formatWaitReport` + `sumSubtreeUsage`), `:229-299` (`council_dispatch` — `params.input`, tmp prompt file, cleanup closure, `hub.spawnJob` at :268-285), `:301-317` (`council_wait` — has `repoRoot` in scope).
- `retry.ts:5-8` (provider-errored children exit 0 → `done`), `:103-123` (`classifyRetry`), `:129-153` (`classifyParentTurnRetry` — the loop-boundary proof that parent and child predicates are separate), `:155-168` (`computeBackoffDelay`, attempt-2 exponent `-2`, jitter [0.5, 1.5)).
- `seats.ts:217-234` (`RetryPolicy` + R2 defaults), `:267-310` (`loadRetryConfig` — always full policy, throws naming file/key), `:600-621` (`buildChildArgv` — `--session-id` from the session param, input as final argv element).
- `runs.ts:58-67` (RunManifest — no `attempt` today), `:111-113` (`writeManifest` overwrite), `:139-160` (`findSessionFile` first-match), `:176-195` (`sumSubtree` — per-id node sum).
- `tree.ts:64-67` (row text + GLYPH map), `navigator.ts:32-50` (STATE_ORDER/STATE_TOKEN), `:224-231` (`stateOf`/`glyphFor`), `:251-272` (`settledCopy`), `:344` (`refresh` reads manifests only), `:352-374` (`rowLine`).
- EV-40 spec `docs/superpowers/specs/2026-09-16-EV-40-design.md:311-322` (§2.8: loop + counter + `attempt` field to EV-39/EV-42; name only agreed; malformed-config-notifies posture in §3.3), `:437` (traceability: "Attempt field name `attempt`, no cross-loop reads").
- Cards: `council/cards/EV-39.md` (Intent, R4, grounded facts), `council/cards/EPIC-9.md`. Wiki: `hub-job-supervision.md` (stable semantics, AGENTS.md 7), `run-transcripts.md`, `usage-accounting.md` (honest-spend invariant), `main-repo-immutability.md` (complied with — no git state touched, no files written).

## Risks & unknowns

- **EV-42 pre-emption is the biggest risk.** The session-id suffix (`-attemptN`) and cumulative-vs-per-attempt manifest usage are provisional decisions EV-42 may want to make differently. Mitigation: record both as provisional in the card record; EV-42 owns the final naming and must keep `sumSubtreeUsage` from double-counting once per-attempt manifest files exist.
- **The `"retrying"` state leaks into every state consumer** (tree glyph, navigator copy/order/tokens, `classifyRetry`'s SETTLED_STATES — which correctly returns `undefined` for it, `formatWaitReport`'s settled set — which must exclude it so no usage block renders mid-backoff). Each is a small additive edit with a test, but the blast radius is wider than hub.ts alone.
- **The settle-observation window** relies on the supervisor's synchronous flip ahead of the 200ms poll. It is order-by-construction (same synchronous callback chain), but if a future change makes `onChange` async, the window reopens — P4's test pins it.
- **A mid-run policy edit** changes only the denominator "of N" in rendered copy for already-running jobs (read per render). Cosmetic, accepted.
- **Wiki gap:** no wiki page covers retry; the hub page's "stable semantics" warning is the only standing constraint. The landed design should seed a wiki page (hub-retry) at close.

## Predictions (falsifiable)

- **P1** — With the supervisor armed (fail-once stub), `council_wait` resolves exactly once, after attempt 2, and never during the backoff window. *Settling test: T1 + T4.*
- **P2** — A `council_cancel` issued during backoff aborts the chain: stub spawn count stays 1, final report `state=cancelled`, cleanup ran. *Test: T5.*
- **P3** — The final manifest's cumulative usage equals the sum of the per-attempt session spends within the stream-projection caveat, and the parent's usage block matches it. *Test: T1's usage assertion against both attempt JSONLs.*
- **P4** — A dispatch that never retries produces a manifest byte-identical to pre-EV-39 (no `attempt` key, same `sessionId`). *Test: T8 snapshot.*

## Split judgment

**Keep EV-39 whole; do not split.** The tempting split — "renderer attempt label" from "loop" — would ship a loop invisible to R4's row, i.e., fail the card's own user-visible ruling; the label without the loop is a constant string. The identity work (`attempt` field, session-id suffix, usage carry) is inseparable from making R4 expressible and is exactly what EV-40 §2.8 assigned jointly to "EV-39, EV-42". **EV-42 remains the named successor** and must land immediately after: it owns `attemptGroupId`, sibling manifest binding, and finalizing the provisional session-id naming; the handoff note (provisional suffix, cumulative-usage semantics, single-file manifest overwrite) should be written into EV-42's card by this card's record.

#### Principal position (`job-19.2`) — verbatim

## Position

**The epic's "hub retry is the second, easier half" is false at this tree, and it is false in a way that changes who owns the card.** `Hub` owns *settle observation*; it does not own *spawn*. `council_dispatch` owns spawn and bakes the seat `input`, the prompt file path, the model and the MCP allowlist into `args`/`env` at the moment of spawn (`hub-tools.ts:170-206`). Three consequences are already visible in the code, not hypothetical:

1. `Job` does **not** store the spawn spec. The facilitator's grounded fact ("takes the full child spec … and stores it on the `Job`") is wrong — `Job` carries `seat/model/pid/state/timeoutMs/stallMs/cleanup/usage` (`hub.ts:11-30`) but **no `command`, `args`, `cwd` or `env`**. The Hub cannot re-spawn from its own table.
2. The prompt file is deleted *at settle* — `settle()` calls `job.cleanup?.()` and then nulls it (`hub.ts:253-255`), and that cleanup is `fs.rmSync(tmpDir)` (`hub-tools.ts:203-208`). So even handing the Hub the argv would re-spawn against a deleted `--append-system-prompt` file.
3. `pi -p --session-dir D --session-id X` with an existing X **resumes** that session (`dist/main.js:335-341`: `findSessionByExactId` → `SessionManager.open`). `buildChildArgv` sets `sessionId = jobId` (`hub-tools.ts:195-198`, `seats.ts:600-625`). So a re-spawn that *reuses the job id* hands attempt 2 attempt 1's errored assistant turn in context — the exact payload class EV-40's structural filter exists to strip (EV-40 spec §2.4), and the Hub has no such filter.

**Reframe:** the loop is not "Hub or tool layer" — it is a **split-ownership capability**: the Hub owns the lifecycle state machine (attempt counter, backoff timer, cancel, wait visibility, manifest state), and the **dispatch layer owns a `respawn` closure** (fresh prompt file, fresh session id, fresh argv) registered at `council_dispatch` time. Neither existing layer owns retry today, and the reason is structural: retry needs settle observation (Hub-only) *and* spawn inputs (tool-layer-only) *and* a timer that must outlive the parent's `council_wait` call.

The two latent alternatives, both of which I name and reject:

- **`council_wait`-driven loop** (tool layer classifies the returned report and re-dispatches): fails the card's goal. `council_wait` is LLM-invoked; if the parent doesn't re-call it, no retry happens. A module-level registry with an unref'd timer would survive the call, but then a respawn can be armed while the parent's turn is ending — exactly the window where `flushUsage("agent-settled")` (`index.ts:741`) freezes the usage record. It also makes `cancel` impossible mid-backoff (the parent is blocked inside the tool call).
- **A parent-invoked `council_retry` tool**: worse — the retry becomes a request the model may forget. The epic's word is "is re-spawned", not "may be re-spawned".

**Blind spots, named separately.**

- *From inside the pipeline/hub slice:* the Hub cannot see that its spawn inputs came from a tool-layer closure, nor that `settle()` deletes them; it reads `Job` as self-sufficient. It also cannot see that `wait`'s settledness is what the *parent's next action* keys on.
- *From inside the tool/UI slice:* the tool layer cannot see that `settle()` is the only place the retry decision can be taken without a window (see §Wait), nor that `buildTree`'s orphan rule (`tree.ts:17`) will light up during every backoff.
- *From inside the run substrate:* "one row per dispatch" (R4) looks like a render request; it is actually a statement about **manifest cardinality**, which is why EV-42's premise is at stake (below).

## Evidence

All first-hand at `f3c2ed1` (card's recorded environment). Wiki used: `vault/wiki/index.md`, `hub-job-supervision.md`, `run-transcripts.md`, `spend-record.md`, `usage-block.md`, `headless-pi.md`.

- **Settle is the single decision point.** `proc.on("close")` → state done/failed → `settle()` (`hub.ts:166-181`); `settle()` = clear proc, `cleanup()`, `writeJobManifest`, `writePids`, `onChange` (`hub.ts:251-259`). Anything decided after `settle()` returns is decided after the manifest and the `onChange` fan-out have already fired.
- **`onChange` fans out to the usage gate.** `index.ts:708-712`: hub `onChange` → `renderWidget()` + `void flushUsage("forest-settle", uiCtx)`.
- **The flush gate is a snapshot, and choose-once is permanent.** `usage-store.ts:392-397`: every manifest with `startedAt >= markerAt` must have `exitCode !== null`; then `398-402`: if the record file exists, `existing` — never recomputed. `spend.ts:244-250`: subtree = roots whose `startedAt >= boundaryMs`. `usage-block.ts:184-205`: `sumSubtreeUsage` walks `parentJobId` children and sums `usage` per manifest.
- **`wait` keys on settledness only.** `hub.ts:291-295`: `timeout` → true, `running` → false, else `exitCode !== null`. `hub.ts:297-311`: 200 ms poll against a deadline.
- **`cancel` refuses a settled job.** `hub.ts:263-265`: `if (job.exitCode !== null) return false;`.
- **Orphan rendering keys on `running` + dead pid.** `tree.ts:17`; also `navigator.ts:203`/`wheel` GLYPH.
- **Policy load site.** `loadRetryConfig` (`seats.ts:267`) returns a full policy and throws on malformed input; `index.ts:585-592` loads it once at init for the parent loop. The Hub reads no policy.
- **Sibling loop state is closure-local.** `index.ts:329-337`: `retryAttempt`, `pendingError`, `originalPrompt`, `sendTimer`, `controller`, `contextFilter` all live inside `registerParentTurnRetry`. `retry.ts` contributes only pure functions (`classifyRetry`, `computeBackoffDelay`, `decideSettleRetry` is in `parent-retry.ts`).
- **EV-40 §2.8** (`docs/superpowers/specs/2026-09-16-EV-40-design.md:311-318`) leaves the hub loop, its counter and the manifest `attempt` field to EV-39/EV-42, agreeing only the **field name** `attempt`.
- *(inferred)* EV-42's goal text ("Each attempt currently mints a fresh job id, a fresh manifest, and a fresh session file") describes **manual re-dispatch today**; it is not a claim about EV-39's mechanism. That is the gap.

### Wait semantics — the precise hazard

Today, with the loop nowhere, attempt 1's provider error sets `state=failed, exitCode!=null` in `settle()`, `onChange` fires, and `wait` returns the retryable report. Any design that arms a backoff **after** that point has already handed the parent a settled report. Concretely, an unmodified `council_wait` returns during backoff because:

- `isSettledForWait` is true (exitCode set) — `hub.ts:291-295`;
- even if you keep the job `running`, the 200 ms poll (`hub.ts:307`) races the timer arm.

And if you keep the job `running` with attempt 1's `startedAt`, a second, sharper defect appears: `isSettledForWait` returns **true for `timeout`** (`hub.ts:292`), and `tick()` sets `timeout` on `now - startedAt > timeoutMs` (`hub.ts:244-247`) — so a dispatch whose first attempt used most of its 15-minute ceiling flips to `timeout` at attempt 2's start and `wait` returns mid-retry. Meanwhile `tree.ts:17` renders that same job `☠ orphaned` (state `running`, pid dead) during every backoff — precisely the window R4 wants labelled `attempt 2/3`.

Minimal honest fix, and it is *cheap under §7*: retry gets its **own non-settled job state** (e.g. `retrying`), with (a) `tick()` unchanged — it already `continue`s on `state !== "running"` (`hub.ts:233`), so no stall/timeout interplay and no stale-pid SIGTERM; (b) `isSettledForWait` false for it; (c) `cancel` disarms the timer and returns true; (d) `startedAt`/`lastActivityAt` re-based at re-spawn; (e) `orphaned` false for it. Yes, `wait` must see the retry: it must block, so that the parent's next action (`council_cancel`, or a duplicate dispatch of the same seat) cannot race a pending re-spawn. The identity line of a window-elapsed report must also carry the attempt count, or the parent cannot distinguish "attempt 2 of 3 in flight" from "attempt 1 failed".

## Interfaces & risks

**Must be agreed now (EV-39), because EV-40 and EV-42 both build on it:**

| Item | Value | Owner |
|---|---|---|
| Loop state machine (counter, timer, cancel, wait visibility) | Hub-held, resetting one job across attempts | EV-39 |
| Re-spawn inputs | `respawn` closure registered by `council_dispatch`; Hub stores `respawn`, not argv | EV-39 |
| Manifest state during backoff | non-settled, `exitCode: null` — the *post-decision* state written by `settle()` | EV-39 |
| `JobReport.attempt` / `maxAttempts` (the "attempts sentence") | additive optional fields on `JobReport` | EV-39 (name `attempt` agreed, EV-40 §2.8) |
| Id minting | unchanged: `hub.allocateId()` per job; EV-39 must not reuse an id, and must not mint a second id-space without a ruling | EV-39 |
| `attemptGroupId` | **not EV-39's** — do not mint the name; EV-42's deliverable | EV-42 |
| Per-attempt *records* in the substrate (separate manifests/transcripts per attempt) | EV-42's, **conditional on the cardinality ruling below** | EV-42 |
| Policy load site | one per process; pass the same resolved `RetryPolicy` object the parent loop uses, do not re-read `.council.json` in the Hub | EV-39 (interface), else two-loop divergence |

**The interface that is a ruling, not code: manifest cardinality.** R4 (one row, `attempt 2/3`) plus "sum every attempt's spend" is satisfiable two ways, and they are not equivalent:

- **(A) one Job / one manifest**, reset to non-settled across attempts, `attempt` counter on the manifest, `usage` **carried over** rather than re-zeroed (`spawnJob` constructs a zeroed usage literal, `hub.ts:120-131` — a re-spawn that calls `spawnJob` silently discards attempt 1's spend and overwrites `<id>.json` via `writeManifest`, `runs.ts:98-100`). Under (A) the EV-31/30/32 chain stays correct **with zero new coupling**: `settle()` writes `exitCode: null` during backoff, so `forestFullySettled` (`usage-store.ts:393`) stays closed naturally, `sumSubtreeUsage` still sums the dispatch, R4 gets its single row for free, and the wait report is coherent.
- **(B) one manifest per attempt** (EV-42's stated premise). Then R4's one row requires a grouping key *inside EV-39*, `sumSubtreeUsage(manifests, dispatchId)` shows only the last attempt in the wait block, and — the sharp one — **the flush gate opens during every backoff**, because all manifests carry `exitCode !== null` in the window before the next attempt spawns. `flushUsage("forest-settle")` (`index.ts:712`) would then persist a record that is permanently short the retried attempts' tokens (choose-once, `usage-store.ts:398-402`), and it would be persisted *before* attempt 2's tokens are even spent. That is a silent, unfixable under-count of exactly the epic's "sum every attempt's spend".

My position: **choose (A)**, and state explicitly to the council that this re-scopes EV-42 — its "fresh id/manifest/session per attempt" premise becomes false, and its remaining subject is per-attempt attribution *within* the dispatch's record plus the tree/usage copy. If the epic instead wants per-attempt manifests, EV-39 cannot avoid pre-empting `attemptGroupId` and the group-scoped sum, and EV-42's build order is wrong. Either way, EV-39 must not choose (B) *implicitly*: choosing (B) without the group key ships an under-counted usage record and a two-rows-per-intent tree.

**Risks.**

1. **§7 blast radius is under-stated by the card.** A new `JobState` value ripples through `RunManifest.state` (`runs.ts:46`), `tree.ts` GLYPH (`tree.ts:48-59`), `navigator.ts` GLYPH + `STATE_TOKEN` + `STATE_ORDER`, `hub-tools.ts`'s `settled` set, and `tree.ts:17`'s orphan rule. The alternative (keep `state: "running"` through backoff) trades that for a live stall/timeout path and a `☠` row during exactly the labelled window. New state is the smaller risk, and §7 is satisfiable: no kill semantics change at all, because `tick()` already skips non-`running` states.
2. **`timeoutMs`/`stallMs` semantics were never defined per attempt.** `council_dispatch` defaults 15/4 min (`hub-tools.ts:199-200`). Re-basing `startedAt` makes `timeout_minutes` a *per-attempt* ceiling — a contract change to the tool (a 3-attempt dispatch may now run 45+ min). The opposite (per-dispatch ceiling) makes a late attempt get `timeout` immediately, and `isSettledForWait` treats `timeout` as settled → the wait race returns. This must be stated in the card, not discovered.
3. **`cancel` currently lies during backoff** (`hub.ts:263-265` returns false for a matched id, i.e. "already settled") and, if the timer is not disarmed, the re-spawn resurrects a cancelled dispatch.
4. **Same session id resumed across attempts** (mechanism 3 above) — must be a fresh session per attempt; then `writeJobManifest`'s hardcoded `sessionId: job.id` (`hub.ts:96`) and `findSessionFile`'s first-hit-by-first-line scan (`runs.ts:134-152`) need the attempt's actual session id, or the navigator tails the wrong transcript.
5. **Two-loop compounding (not shared state).** Verified no shared state: EV-40's `RetryController`/`contextFilter`/`retryAttempt` are closure-local to `registerParentTurnRetry` (`index.ts:329-337`), and the hub loop would live on `Hub`; `computeBackoffDelay` takes the policy as an argument and holds no counter. But they *can* compound: the parent turn settles with a provider error while a hub child is in backoff (possible if the parent dispatched without waiting, or `council_wait` returned `timeout`/window-elapsed), then EV-40's loop re-sends `originalPrompt` and re-dispatches the same seat while the hub's pending re-spawn fires. That is not "correct", but it is pre-existing (a parent could always re-dispatch) and EV-40's R5 copy is the human-facing mitigation; the engine-level mitigation is `wait` blocking through retries, which is this card's job.
6. **Policy divergence:** two `loadRetryConfig` readers in one process (init-time parent loop vs a Hub read) would diverge on a mid-session `.council.json` edit. Pass one resolved policy.

## Falsifiable predictions

Each is runnable against `Hub` with `test/stub-child.ts` (`STUB_MODE=error` already produces `stopReason=error` + a retryable message — `test/hub.test.ts:53-60`), so no network is needed.

1. **The re-spawn gap is real (pre-implementation red).** Test: dispatch the stub in `error` mode against a real `Hub` with a `run` dir; assert the manifest of the settled job has `exitCode !== null` and that no call path exists to re-spawn it (attempt 2 cannot be produced by `Hub` alone — the argv/prompt file are gone). *Settles:* `test/hub.test.ts` new case asserting `spawnJob` opts are the only spawn entry point and `Job` has no `args`/`env` keys.
2. **Wait must not return a retryable report.** With the fix: after attempt 1's provider error, call `hub.wait([id], 60_000)` in the same tick as the backoff; assert it does **not** return until attempt 2 settles, and that `report(id).state` is never `failed`/`timeout` while an attempt is armed.
3. **Cancel during backoff.** `hub.cancel(id)` while the backoff timer is armed returns `true`, and after ≥ 3× the delay no second child has been spawned (assert the stub's spawn count / manifest `startedAt` unchanged).
4. **Flush gate stays closed through a retry.** Drive `flushPendingInvocations` with a marker whose window contains the retrying job; assert `status === "gate-closed"` (not `written`) at every moment between attempt 1's close and attempt 2's settle, then `written` once with the *final cumulative* usage. *Settles:* a `test/usage-store.test.ts` case; a `written` outcome mid-backoff falsifies prediction 4 and proves the under-count bug.
5. **Cumulative usage is never zeroed by a re-spawn.** Seed attempt 1 with the `ACC_FIXTURE` (`test/hub.test.ts:167-181`), retry, assert the final manifest `usage.input/output/cost` is the **sum** of both attempts (not attempt 2's alone) and that `sumSubtreeUsage(manifests, id).subtree` equals it.
6. **No orphan glyph / no stall kill during backoff.** With `stallMs: 400` and `monitorIntervalMs: 50` (`test/hub.test.ts:88-95` harness) and a backoff longer than `stallMs`, assert the job is not `stalled`, no `killGroup` fired for attempt 1's pid, and `buildTree(...)` reports `orphaned === false` for the row.
7. **One row.** `flattenTree(buildTree(manifests))` yields exactly one node for a 3-attempt dispatch, and `textTree`/`rowLine` render the `attempt 2/3` fragment on it (R4) — with the state glyph legible.
8. **No cross-loop reads.** A parent-turn retry in `index.ts` and a hub retry in the same process: assert the hub's attempt counter is unchanged by the parent loop's `decideSettleRetry`/`computeBackoffDelay` calls, and vice versa (static: grep for a shared module-level counter in `retry.ts`/`parent-retry.ts` returns none; dynamic: the counter values are independent across a chained scenario).
9. **(B)-premise falsifier, if the council overrides my recommendation):** with per-attempt manifests and no group key, assert the failure modes directly — `sumSubtreeUsage(manifests, dispatchId)` excludes earlier attempts, and `council_wait`'s block shows one attempt's spend. A green version of this test is the evidence that (B) was chosen with eyes open.

## Split judgment

- **Agree with the card's inputs without reservation:** R1/R2/R3/R4 apply; `classifyRetry` and `computeBackoffDelay` are the reusable pieces; a second delay function would be the divergence EV-40 §2.8 warns about; the defaults (2000/30000/jitter, `maxAttempts 3`) bind here since `.council.json` carries no `retry` section (`seats.ts:267-290`; the card's own record).
- **Split from the epic's framing, not from the card's goal:** "second, easier half" is wrong. The Hub is the *harder* re-spawn site and the *only* correct settle-observation site; the card should say so, because it determines the design (split ownership) and the testing plan (§7's failing-test-first applies to a `retry` state and a `settle()` ordering, both of which are behavior).
- **Where I will not concede:** the retry decision must be taken inside `settle()` synchronously, before `writeJobManifest`/`onChange` (`hub.ts:251-259`). A design that arms the backoff after the settle fan-out is measurably wrong (prediction 4) and no amount of display-layer copy repairs it.
- **Open question I am handing to the ruling, not deciding:** manifest cardinality (A vs B). I recommend (A) and would accept (B) only with an explicit `attemptGroupId` + group-scoped sum *inside* EV-39 and a re-scoped EV-42; what I will not accept is (B) chosen by default because "re-spawn" sounded like "spawnJob again".
- **Unverified/inferred, marked:** pi's session-resume path is read first-hand from `dist/main.js:296-341` but not executed here; the claim that attempt 2 on a resumed session re-sees the errored turn is inferred from EV-40 §2.4's premise (the parent loop needed a structural filter for exactly that) — it is cheap to settle with a two-attempt stub probe, and it should be, because it decides whether a fresh session id per attempt is mandatory or merely preferable.

#### Designer position (`job-19.3`) — verbatim

I'm the designer seat on EV-39, first-pass independent position. R4 is binding (one row per dispatch, labeled `attempt 2/3` while retrying). My subject is everything R4 leaves open: the row grammar, the backoff gap, the attempt-budget's legibility over time, the report's attempt sentence, headless parity, and width-clamp.

The card's facilitator step-1 already declares the brainstorming discipline satisfied by this deliberation, and the seat prompt forbids me from re-opening R4. I hold that and proceed.

---

## Surface state machine

The retry loop's two states that the row must distinguish are **(a) an attempt is running** and **(b) the loop is between attempts in backoff**. R4 forbids extra rows; it does not forbid the manifest's existing state from reflecting "between attempts." I propose the hub writes a sidecar `{id}.retry.json` next to the manifest (one sidecar per dispatch, owned by the loop in `extensions/hub-tools.ts`; the manifest schema is unchanged — EV-42 owns the `attempt` manifest field, scheduled after EV-39). The sidecar carries `{attempt: number, maxAttempts: number, status: "running"|"waiting"|"done"|"failed"|"exhausted", nextAttemptAt?: number}` and is the single source the renderer reads for `attempt N/M` plus the backoff copy.

The widget reads manifest + sidecar atomically per refresh. R4's "labeled `attempt 2/3` while retrying" is read as: **show `attempt N/M` whenever `N > 1`**, including after the final attempt settles (so the operator opening the tree at attempt-3-clean sees the count, not a mysterious `settled`). The label is omitted when `N = 1` (no retry happened; the row reads as it does today).

The label sits **between the seat field and the `·` separator**, not at the tail. Three reasons:
- The label is a status fragment; it groups with `state`, not with `pid`/`elapsed` (debugging metadata).
- "running attempt 2/3" reads as a phrase; "running 0.5m attempt 2/3" reads as a mistranslation.
- Truncation at narrow widths preserves the high-priority `attempt N/M` and drops the trailing age, not the count.

### Concrete row grammar — TUI inline (`extensions/navigator.ts` `CouncilTreeWidget.rowLine`)

The seat padding rule (`pad = max(0, 14 - visibleWidth(styled))`) is preserved. The verb-first copy is the existing `activityCopy` (running) / `settledCopy` (settled) seam.

| Case | Row text (visible, ANSI stripped) |
|---|---|
| (a) attempt 1, running | `● designer       · → ran ls 12s` *(unchanged today)* |
| (b) attempt 2/3, in backoff after attempt 1 settled retryable | `⏸ designer attempt 2/3 · retrying in 1s` |
| (c) attempt 2/3, running | `● designer attempt 2/3 · → ran ls 12s` |
| (d) attempt 3/3, settled clean | `✓ designer attempt 3/3 · settled 3m` |
| (e) attempt 3/3, exhausted | `✗ designer attempt 3/3 · failed 2s` *(glyph `failed`, copy "failed")* |
| (f) attempt 2/3, stalled (mid-attempt) | `⏸ designer attempt 2/3 · stalled 4m` *(existing stalled glyph, label preserved)* |
| (g) attempt 2/3, cancelled (mid-attempt or mid-backoff) | `⊘ designer attempt 2/3 · cancelled` |

A few specifics worth naming:

- **(b) reuses the `⏸` glyph** (the existing stalled glyph). I considered a new glyph and rejected it: AGENTS.md 9.6 and the EPIC-1 token-only drawing rule favor minimizing the glyph vocabulary; ⏸ already reads as "the system is paused waiting for something," which is exactly the backoff meaning. The widget's `STATE_TOKEN["waiting"] = "warning"` matches the existing `stalled` token (operator already learns ⏸+warning = paused).
- **(b) uses `retrying in Ns`** (not `waiting 2s`). The word "retrying" tells the operator *why* the system is paused; "waiting" is ambiguous (waiting for what?). EV-40's input-bar countdown uses `Retrying in2s (attempt 2 of 3)` — the same word. This is the right place to reuse vocabulary across surfaces.
- **(e) keeps the `failed` copy**. The widget's existing `settledCopy` already returns `failed` for state `failed`. No new copy. The label `attempt 3/3` carries the exhaustion signal; the verb-first `failed` carries the failure signal. Both are necessary.
- **(g) cancelled**: if `cancel` lands during the backoff gap (state `waiting` → `cancelled`), the row reads `⊘ designer attempt 2/3 · cancelled`. The sidecar's status transitions `waiting → cancelled`. The `cancelled` copy is the existing widget copy.
- **Stall during backoff is impossible** — there is no process to stall. The widget only renders `stalled` for `state === "stalled"` which requires the hub's tick to have killed a live process. During `waiting` the hub does not run `tick().stall` (the job's state is `waiting`, not `running`; the existing `if (job.state !== "running") continue` guard prevents it).

### Concrete row grammar — headless `textTree` (`extensions/tree.ts`)

The textTree row is `glyph id seat state Xm pid=`. The label inserts between `seat` and `state`. Today's textTree includes `id` and `pid` — the headless surface is debugging-rich.

| Case | textTree line |
|---|---|
| (a) attempt 1, running | `  ● job-1 designer running 0.5m pid=12345` *(unchanged)* |
| (b) attempt 2/3, in backoff | ` ⏸ job-1 designer running attempt 2/3 0.5m pid=12345` *(state is `waiting` in sidecar; manifest still `done`/`failed` from attempt 1 — the widget reads sidecar.status, renders ⏸)* |
| (c) attempt 2/3, running | `  ● job-1 designer running attempt 2/3 0.5m pid=12345` |
| (d) attempt 3/3, settled clean | `  ✓ job-1 designer done attempt 3/3 3.0m pid=12345` |
| (e) attempt 3/3, exhausted | `  ✗ job-1 designer failed attempt 3/3 2.0m pid=12345` |
| (f) attempt 2/3, stalled | `  ⏸ job-1 designer stalled attempt 2/3 4.0m pid=12345` |
| (g) attempt 2/3, cancelled | `  ⊘ job-1 designer cancelled attempt 2/3 0.1m pid=12345` |

The two surfaces agree on:
- The label `attempt N/M` (slash-separated, per R4's literal wording).
- The glyph vocabulary (`●⏸✓✗⊘⚠☠` unchanged; `⏸` reused for the new `waiting` state).
- The verb-first copy (TUI only; textTree uses the raw `state` field).

### The final report's attempt sentence — `extensions/hub-tools.ts` `formatReport`

The sentence is appended at the end of `formatReport`'s output, **after the `err` clause and before the trailing newline**, so it lands at the very end of each per-job block. `formatWaitReport`'s `\n\n====\n\n` separator still separates jobs; the runner-usage block is appended after the attempt sentence (the runner-usage block is meta-aggregated and the attempt sentence is per-job — keeping the attempt sentence on the per-job line keeps the per-job block self-contained).

The four cases:

| Outcome | Sentence |
|---|---|
| N = 1, clean settle | `Completed after 1 attempt.` |
| N = 1, non-retryable failure | `Failed after 1 attempt.` |
| N > 1, clean settle (retry succeeded) | `Completed after 3 attempts (1 initial + 2 retries).` |
| N = maxAttempts, exhausted | `Exhausted after 3 attempts.` |

The singular `attempt` vs. plural `attempts` is the small but real affordance — a first-time reader sees `Completed after 1 attempt.` and learns that "completed" without retry is the default shape; the plural form signals "this took multiple attempts."

The exhaustive copy deliberately does NOT mirror EV-40's `Retries exhausted after ${maxAttempts} attempts. The provider kept failing. Press Enter to try again.` (R5 verbatim). EV-40's copy invites the user to press Enter; EV-39 has no input bar (it's a tool layer, not the parent turn). Mirroring the full sentence would import a half-action ("Press Enter to try again") that does nothing here. I propose `Exhausted after3 attempts.` — the operator on a hub-retry-exhausted case can read the parent's tool result and decide whether to re-dispatch.

**Open question** (filed below): does the exhausted copy need a verb explaining *what happened* (e.g., "Exhausted after 3 attempts; final error: Provider finish_reason: error")? My position is no — the `--- provider error ---` clause is already in the report, just above the attempt sentence, with the final attempt's full errorMessage. The attempt sentence and the error clause together tell the whole story. Naming the error in the sentence is redundant.

---

## Discoverability & failure tolerance

**At the moment of "is this seat retrying?"** — the operator glances at the row. The `attempt N/M` label answers: "yes, on attempt 2 of 3" (or 3 of 3). Without the label, the row reads as a normal running job; the operator cannot distinguish a retrying dispatch from a single-shot dispatch until the report lands. **The label closes the evaluation gulf** at the moment the operator wonders "why is this taking so long?"

**At the moment of "how long until the next attempt?"** — the backoff gap's `retrying in Ns` copy answers (case (b)). Without this copy, the row briefly reads `✓ settled4s` after attempt 1 settles retryable, and the operator concludes the job is done. **The retrying copy is the forcing function** against the slip "the seat finished; let me move on." This is Norman's distinction: a slip (right intention, wrong execution) deserves a control fix; the row needs to *tell* the operator the system is in the middle of a retry.

**At the moment of "how many attempts in total?"** — R4 forbids extra rows for history, so the tree shows only the current state. The operator who lands on attempt-3-clean sees `✓ designer attempt 3/3 · settled 3m` and learns the count. **The label is the comprehension affordance for "this was a multi-attempt dispatch."** Whether it's *enough* depends on whether the operator needs to know WHY it took 3 attempts. The final report carries the last attempt's errorMessage; prior attempts' errors live in their (now-overwritten) manifests and their (preserved) JSONL session transcripts. The operator who needs the history reads the report and the prior transcripts. R4 explicitly accepts this trade-off ("no extra rows").

**Failure tolerance — the slip "I assumed attempt 2 of 3 means attempt 2 succeeded."** The label is `attempt 2/3`, not `attempt 2/3 (retrying)`. A naive read could conflate "on attempt 2" with "attempt 2 done." Mitigation: the backoff copy (`retrying in Ns`) and the running copy (`→ ran ls`) disambiguate by context. The row is never `attempt 2/3` alone — it's always paired with a state (`running`, `waiting`, `stalled`) and a copy. The label is a status fragment, not a complete sentence.

**Failure tolerance — the mistake "I thought the row was a different dispatch."** R4 forbids two rows for one intent. The manifest's `id` is constant across attempts (the loop reuses the same id; the manifest is overwritten). The widget sees one row per id. There is no possible confusion. **R4 eliminates this whole class of mistake.**

**Failure tolerance — the slip "I read the row and assumed the seat failed."** On attempt 2 of 3 running, the glyph is `●` (running), not `✗` (failed). The state and the label are independent; the operator reads the glyph for state and the label for retry context. **No false-positive failure signaling.**

**Comparison with EV-40's input-bar countdown (R5):** the two surfaces are adjacent but non-redundant. The parent input bar shows `Retrying in 2s (attempt 2 of 3) — Esc to abort` for the **parent's own provider-error retry** (a separate loop keyed on `agent_settled`, settled by EV-40). The hub tree row shows `⏸ designer attempt 2/3 · retrying in 1s` for a **child dispatch's retry** (EV-39's loop). The two loops read different signals (`classifyParentTurnRetry` on a `message_end` vs `classifyRetry` on a settled `JobReport`); per AGENTS.md 12 and the EV-40 watch item, they never share state. An operator watching a child retry and the parent idle sees the hub row's countdown; an operator whose parent turn itself failed sees the input-bar countdown. The two surfaces never both fire at once for the same event.

**The slight format tension — `attempt 2/3` (R4) vs `attempt 2 of 3` (R5).** R4's ruling-text specifies the slash form on the tree row; R5 specifies the spelled-out form on the input bar. Both forms reference the same number with the same word `attempt`; the visual difference is one punctuation. I preserve R4's literal wording on the tree row and flag the format divergence as a discoverability question (below).

---

## Falsifiable predictions

Each claim is a hypothesis the skeptic may run as a pure-seam test (no gate config touched, no production changes made by me). The smoke names the artifact, the inputs, and the asserted output.

### Row grammar (pure-seam against `CouncilTreeWidget.rowLine` + sidecar)

- **P1.** For a dispatch with sidecar `{attempt: 2, maxAttempts: 3, status: "waiting", nextAttemptAt: now + 2000}` and manifest `state: "done"` (attempt 1 settled, attempt 2 not yet spawned), `rowLine(node)` returns a string that, after ANSI stripping, equals `⏸ designer attempt 2/3 · retrying in 2s` at `now === nextAttemptAt − 2000` and `⏸ designer attempt 2/3 · retrying in 1s` at `now === nextAttemptAt − 1000`. **Smoke:** instantiate `CouncilTreeWidget` with a synthesized TreeNode (manifest from the run dir, sidecar from a temp dir), pin `now()`, assert the visible string at three `now` offsets (2000, 1000, 0 ms before nextAttemptAt).

- **P2.** For a dispatch with sidecar `{attempt: 2, maxAttempts: 3, status: "running"}` and manifest `state: "running"`, `rowLine(node)` equals `● designer attempt 2/3 · → ran ls 12s` (assuming a transcript tail with a `toolCall` block at `now − 12000`). **Smoke:** as P1 but with status `running`.

- **P3.** For a dispatch with sidecar `{attempt: 3, maxAttempts: 3, status: "done"}` and manifest `state: "done"`, `rowLine(node)` equals `✓ designer attempt 3/3 · settled 3m`. **Smoke:** as P1 with status `done` and `settledAt = now − 180000`.

- **P4.** For a dispatch with sidecar `{attempt: 3, maxAttempts: 3, status: "exhausted"}` and manifest `state: "failed"`, `rowLine(node)` equals `✗ designer attempt 3/3 · failed 2s`. **Smoke:** as P1 with status `exhausted`, manifest `state: "failed"`, `settledAt = now − 2000`.

- **P5.** For a dispatch with **no sidecar** (attempt 1, never retried) and manifest `state: "running"`, `rowLine(node)` equals `● designer       · → ran ls 12s` — **no `attempt N/M` fragment**. **Smoke:** instantiate widget with a manifest-only node, assert the visible string contains no `attempt` substring. This is the negative case that proves the label is conditional on `N > 1`.

- **P6.** For a dispatch with sidecar `{attempt: 2, maxAttempts: 3, status: "waiting"}` that has been **cancelled** mid-backoff (manifest state `cancelled`, settledAt set), `rowLine(node)` equals `⊘ designer attempt 2/3 · cancelled`. **Smoke:** as P1 with manifest state `cancelled`, sidecar status unchanged. The widget reads the manifest's state for the glyph, the sidecar's status only for the verb-first copy when status is `waiting`.

- **P7.** For a dispatch with sidecar `{attempt: 1, maxAttempts: 3, status: "running"}` (the loop armed the sidecar preemptively even though no retry has occurred yet — a defensive write to make the sidecar present from attempt 1), `rowLine(node)` still omits the label (the rule is `N > 1`, not "sidecar exists"). **Smoke:** widget with sidecar `{attempt: 1, ...}`, assert no `attempt` substring in the visible row. This pins the "while retrying" reading of R4.

### Sidecar semantics (pure-seam against the loop's lifecycle helpers)

- **P8.** The loop writes the sidecar with `{attempt: N+1, status: "waiting", nextAttemptAt: now + computeBackoffDelay(...)}` **before** the backoff `setTimeout` fires, and updates to `{status: "running"}` immediately after the next `hub.spawnJob` returns. **Smoke:** wrap `hub.spawnJob` in a recorder; drive the loop through one full retry transition; assert the sidecar's `status` field transitions in order `waiting → running` and `nextAttemptAt` is non-null in `waiting` and null in `running`.

- **P9.** The loop never writes a sidecar for a non-retryable settled report. **Smoke:** drive the loop with a `JobReport` whose `stopReason === "stop"`; assert no sidecar file exists in the run dir after `council_wait` returns.

- **P10.** The loop's sidecar is **deleted** when the retry chain concludes terminally (clean or exhausted) — or, alternatively, retained with a terminal `status` so the widget can render the final attempt count. I argue for retention (terminal status) so the row keeps the `attempt N/M` after settle. **Smoke:** drive the loop through exhausted; assert sidecar exists with `{status: "exhausted", attempt: maxAttempts, maxAttempts}`.

### textTree parity- **P11.** `textTree(repoRoot, runIds)` for a dispatch with sidecar `{attempt: 2, maxAttempts: 3, status: "waiting"}` and manifest `state: "done"` emits a line whose visible (ANSI stripped) form equals `⏸ job-1 designer running attempt 2/3 0.5m pid=12345`. **Smoke:** synthesize the manifest + sidecar in a temp run dir; call `textTree`; assert the line. (Note: textTree today uses the manifest's `state` field verbatim — `"done"` or `"running"`. I propose textTree reads the sidecar and substitutes `"running"` for `status: "waiting"` regardless of the manifest state, so the headless operator sees the loop is alive. This is a deliberate divergence from "manifest state is the truth" — justified because the sidecar's `status` IS the loop's truth.)

- **P12.** `textTree` for a dispatch with no sidecar emits the existing line unchanged (negative case, proves textTree is backward-compatible with attempt-1 dispatches).

### Final report (`formatReport`)

- **P13.** `formatReport({id, seat, state: "done", elapsedMs: 180000, usage: {...}, stopReason: "stop"})` with `attempts: 1` (a clean settle on the first try) returns a string whose last non-empty line is `Completed after 1 attempt.` **Smoke:** unit test on `formatReport` with synthesized `JobReport` and `attempts: 1`.

- **P14.** `formatReport` with `attempts: 3, state: "done"` returns a string whose last non-empty line is `Completed after 3 attempts (1 initial + 2 retries).` **Smoke:** as P13 with `attempts: 3`.

- **P15.** `formatReport` with `attempts: 3, state: "failed", stopReason: "error", errorMessage: "Provider finish_reason: error"` returns a string whose last non-empty line is `Exhausted after 3 attempts.` and whose `--- provider error ---` clause appears above it with the errorMessage intact. **Smoke:** as P13 with the failure fields.

- **P16.** `formatReport` with `attempts: 1, state: "failed", stopReason: "error", errorMessage: "Provider finish_reason: error"` (a non-retryable failure on first try — but this case shouldn't reach the retry loop because `classifyRetry` would say "retry," not "terminal"; if it does reach formatReport, the sentence is `Failed after 1 attempt.`) **Smoke:** as P13 with the failure fields and `attempts: 1`. This is the edge case where the classifier is bypassed (a future EV-42 might widen the classifier; this test pins the current boundary).

- **P17.** `formatWaitReport([r1, r2])` joins two reports with `\n\n====\n\n`; each report's last non-empty line is its attempt sentence. **Smoke:** unit test asserting the join order.

### Width / accessibility

- **P18.** At `width === 50`, `rowLine(node)` for a `running attempt 2/3` row is truncated to `● designer attempt 2/3 · → ran…` (the verb-first copy is truncated, the label is preserved). **Smoke:** widget render at `width ∈ {30, 40, 50, 80, 120}`; assert `truncateToWidth` preserves the label.

- **P19.** At `width === 30`, `rowLine(node)` for a `running attempt 2/3` row is truncated to `● designer attempt 2…` (the label is partially truncated — the `/3` is dropped, the verb-first is gone). **Smoke:** as P18 at width 30. This is the failure mode: at30 cols the label itself truncates. The seat field takes 14 cols, the glyph takes 1 col, the `·` takes 1 col, and the label takes 12 cols — total 28 cols plus separator. The verb-first copy is what gets eaten.

- **P20.** At `width === 80`, the row is never truncated for any of the 7 cases. **Smoke:** as P18 at width 80, assert no `…` truncation marker.

- **P21.** `textTree` at a narrow console (40 cols) wraps each line; the truncation behavior is the terminal's, not the renderer's. **Smoke:** headless harness at COLUMNS=40, assert each row wraps to2+ lines, the `attempt N/M` substring appears on the first wrapped line in all7 cases.

### Theme compliance (per AGENTS.md 9.6 and `vault/wiki/council-theme.md`)

- **P22.** `rowLine` introduces no new `theme.fg/bg/bold` token calls beyond the existing palette. Grep-audit on `extensions/navigator.ts` after the change returns the eight documented tokens (`accent`, `border`, `customMessageBg`, `dim`, `success`, `warning`, `muted`, `bold`). **Smoke:** static grep; assert no new literal hex/ANSI.

- **P23.** No new glyph beyond `●✓✗⏸⊘⚠☠`. Grep-audit on the changed code path; assert the `⏸` glyph is the same constant used for `stalled` (no new U+ escape).

### Comprehension (judgment — settles only on a human read)

- **C1.** A first-time operator who sees `⏸ designer attempt 2/3 · retrying in 1s` on the tree can answer, without reading docs: (i) **which seat is retrying?** — the seat name; (ii) **on which attempt?** — `2/3`; (iii) **what is the system doing right now?** — `retrying in 1s` (the loop is in backoff, attempt 2 spawns in 1 second); (iv) **is this normal?** — the ⏸ glyph + warning token says "paused, not failed."

- **C2.** A first-time operator who sees `✓ designer attempt 3/3 · settled 3m` after the loop concludes can answer: (i) **what just happened?** — the seat settled; (ii) **how many attempts?** — 3 of 3; (iii) **should I read the report for details?** — yes (the operator who needs to know *why* 3 attempts opens the parent's tool result; the operator who just needs the count is satisfied). **The label is the right level of detail for a row; the report is the right level for the conversation.**

- **C3.** The format divergence `attempt 2/3` (tree) vs `attempt 2 of 3` (input bar) does not confuse a first-time operator — both forms name the same number with the same word `attempt`; the slash and "of" are punctuation variants that read as equivalent. (This is a hypothesis I cannot test without a user study; if the divergence does confuse, the fix is to unify on one form — see open questions.)

---

## Open design questions (flagged for ruling)

These I could not ground and would not have the designer seat unilaterally close. Each names what needs a ruling.

1. **Format divergence `attempt 2/3` vs `attempt 2 of 3`.** R4 says `attempt 2/3`; R5 says `attempt 2 of 3`. Both forms coexist on adjacent surfaces. A first-time operator may or may not parse them as equivalent. **Needs:** a unifying ruling — either both surfaces use `attempt N/M` (slash) or both use `attempt N of M` (spelled). I lean slash for the row (compact) and spelled for the input bar (sentence), but the divergence is a discoverability hazard and the ruling should be deliberate. **My recommendation: keep R4's literal wording on the row (slash), keep R5's literal wording on the input bar (spelled), and document the equivalence in `vault/wiki/` so the operator learns the mapping.** Risk: a brand-new operator sees both forms and asks "are these different?" in a forum thread.

2. **Where does `attempt N/M` sit on a row whose state is `failed` (settled non-retryable on attempt 1)?** Today this never occurs — `classifyRetry` would say "retry" and the loop would re-spawn. But if the classifier is bypassed (a future EV-42 change, or a `policy.enabled = false` case), the row is `✗ designer · failed` with no label. **My position:** if `N = 1` and the row is `failed`, the label is omitted (matches the no-label rule for `N = 1`). If `N > 1` and the row is `failed` (exhausted), the label is `attempt M/M`. **Needs:** a ruling that confirms this — or a ruling that says the label should always be present (even on `N = 1`) for consistency.

3. **Does the final report accumulate prior attempts' error history?** Today the report carries only the final attempt's `errorMessage` clause. The operator on attempt-3-exhausted sees `Exhausted after 3 attempts.` + `--- provider error --- Provider finish_reason: error` (the last attempt's error). Prior attempts' errors are NOT in the report; they live in the (preserved) JSONL session transcripts. **My position:** the attempt sentence + the last error is enough for the operator; prior errors are deep-debug material accessible via transcripts. **Counter-argument:** a multi-attempt exhaustion is a "this failed repeatedly" signal and the operator should see all N errors inline. **Needs:** a ruling on whether the report's attempt sentence should include a one-line error history (e.g., `Exhausted after 3 attempts: Provider finish_reason: error, Provider finish_reason: error, Provider finish_reason: error.`) — and whether the JSONL transcripts are a sufficient alternative.

4. **Does the backoff gap's `retrying in Ns` copy need a countdown to the second (matching EV-40's input bar)?** Today the widget renders the verb-first copy once per refresh (every 2s for the inline widget; instantly for the modal). The `retrying in Ns` value would update on each refresh — meaning the operator sees `retrying in 2s` → `retrying in 1s` → `retrying in 0s` → spawn. **My position:** the copy is `retrying in ${ceil((nextAttemptAt - now) / 1000)}s` and refreshes with the widget's 2s tick. The operator sees whole-second ticks, not sub-second. **Counter-argument:** EV-40's input bar ticks every 1s; the hub row should match for cross-surface consistency. **Needs:** a ruling on the tick interval — 1s (matches EV-40) or 2s (matches the existing widget refresh). I lean 2s (no new tick, no new render cost) but the consistency argument is real.

5. **The sidecar file's location and lifetime.** I propose `{runDir(repoRoot, runId)}/{id}.retry.json`. The run dir is gitignored (`runs/` is ephemeral telemetry). The sidecar lives and dies with the run dir (AGENTS.md 12: `runs/` is ephemeral; pruned to last 15 at parent `session_start`). **Needs:** confirmation that a sidecar in the run dir is consistent with AGENTS.md 12 (it is — the run dir is the substrate, and the sidecar is part of it). No new directory needed.

6. **The widget's read path for the sidecar.** The widget refreshes every 2s and reads `readManifests(repoRoot, runId)`. I propose adding `readSidecars(repoRoot, runId)` that returns `Map<id, RetrySidecar>` for the same run dir; the widget reads both. **Open:** whether the sidecar read should be inside the manifest read (one file system walk) or separate (two walks). **My position:** separate, lazy — the widget doesn't need the sidecar for non-retrying dispatches. **Needs:** confirmation that the per-refresh cost (one extra `fs.readdirSync` + a few `fs.readFileSync`s) is acceptable.

7. **Stall during backoff — does the operator need a different copy from the `⏸ stalled4m` copy?** Stall requires a live process (hub.ts tick kills on stall). During backoff there is no process; stall cannot fire. The widget's `settledCopy` returns `stalled {age}` for `state === "stalled"`; the sidecar's `status: "waiting"` produces `retrying in Ns` instead. **No conflict.** But the row uses the same glyph (`⏸`) for both states — a first-time operator might confuse "stalled mid-attempt" with "waiting in backoff." **Needs:** a ruling on whether the glyph should differ (a new glyph would violate the minimal-vocabulary preference) or whether the verb-first copy is enough to disambiguate. **My position:** the verb-first copy disambiguates (`stalled 4m` vs `retrying in 1s`); one glyph is fine.

8. **Is `Exhausted after3 attempts.` the right copy, or should it mirror EV-40's `Retries exhausted after ${maxAttempts} attempts. The provider kept failing. Press Enter to try again.`?** My current position: no — EV-40's copy invites an action (Enter) the hub cannot honor. But the divergence means the operator on a hub-exhausted case sees a terser message than on a parent-exhausted case, which could read as "the hub exhausted less gracefully." **Needs:** a ruling on whether the two exhaustion copies should be unified (and, if unified, which form wins — R5's verb-heavy form or the hub's terser form).

---

## What I am not arguing

- I am not arguing for a new state in `JobState` (`waiting`). The hub's `Job.state` remains `running | done | failed | cancelled | stalled | timeout`. The sidecar's `status` field is the loop's vocabulary, not the hub's. **The widget reads the sidecar's status; the manifest's state reflects the current attempt's lifecycle, which during backoff is `done`/`failed` (the prior attempt's final state).** This keeps the hub's stable state machine untouched (AGENTS.md 7: `hub.ts` is battle-tested; behavior changes need a failing test first). The sidecar is a non-invasive layer above the hub.
- I am not arguing for an `attempt` field on `RunManifest`. EV-42 owns that field; EV-39 precedes it. The sidecar is the temporary carrier that EV-39 ships; EV-42 will likely promote sidecar fields into the manifest.
- I am not arguing for a new glyph. `⏸` reused. AGENTS.md 9.6 and the EPIC-1 token-only drawing rule favor minimal vocabulary.
- I am not arguing for a new theme token. The widget's existing `STATE_TOKEN["stalled"] = "warning"` is reused for `status: "waiting"` (same semantic family: paused, waiting for something).
- I am not arguing for changes to `extensions/runs.ts`'s retention policy. `runs/` is ephemeral; pruning the last 15 runs at parent `session_start` keeps sidecars in the same lifecycle as manifests.

---

## Yield

This is the design seat's first-pass independent position on EV-39. R4 is binding and is not reweighed. The subject I hold open is the row grammar (cases (a)–(g)), the backoff gap's surface, the attempt-budget's legibility over time, the final report's attempt sentence (four cases), headless parity, and width-clamp. The design introduces a sidecar `{id}.retry.json` in the run dir as the loop's vocabulary carrier — non-invasive above the hub, pre-EV-42. The widget reads manifest + sidecar atomically per refresh. The final report's sentence is conditional on `attempts: N` (singular at N=1, plural at N>1, distinct wording for exhaustion).

Twenty-three falsifiable predictions (P1–P23) and three comprehension checks (C1–C3) are the inputs the skeptic may run as out-of-band pure-seam smokes after the owner implements. Eight open questions are flagged for ruling — four are wording/format tensions that need a deliberate decision (the `attempt 2/3` vs `attempt 2 of 3` divergence, the `N=1 failed` label question, the prior-error-history question, the exhaustion-copy-mirroring question) and four are confirmations of the sidecar approach (location, lifetime, read-path, vocabulary).

No files written this round (per the round's hard constraint). No application code edited. No worktree created. No branch checked out.


### Step 3 — bounded exchange (round 2, final round)

Each generator re-dispatched with the others' round-1 positions (read from this card) and asked to respond to what the others actually said. `principal`'s first round-2 dispatch (`job-19.5`) hit its 15-minute window with no output (`state=timeout`, 17 turns of investigation); per dispatch discipline it was cancelled and re-dispatched once as `job-19.7` (2.9m, 13 turns), which settled `done`. Positions stabilised on the loop seam, cardinality, state and carrier; the residual is `startedAt` semantics (D1: `owner` re-bases per attempt, `principal` holds it stable with a separate attempt clock) — a testable dispute handed to the Skeptic rather than a third exchange round. Jobs: `owner` `job-19.4` (15.1m, 8 turns), `designer` `job-19.6` (15.1m, 9 turns), `principal` `job-19.7` (2.9m, 13 turns). All settled `done`. Positions extracted verbatim from the seat session JSONL.

#### Owner round 2 (`job-19.4`) — verbatim

All load-bearing claims verified first-hand at this tree (`f3c2ed1`). Round-2 position follows. No files written, no git state touched.

---

# EV-39 — owner round-2 position

## Response to principal

**Your reframe survives contact with the code, and I concede the load-bearing part of it.** Verified first-hand:

- `Job` (`hub.ts:11-30`) carries no `command/args/cwd/env` — your correction of the facilitator's grounded fact is right, and my round-1 position already assumed it, so we never actually disagreed on the premise; we disagreed on where the counter/timer live.
- `settle()` runs and nulls `cleanup` (`hub.ts` — `job.cleanup?.(); job.cleanup = undefined;`), and that cleanup is `fs.rmSync(tmpDir)` (`hub-tools.ts` dispatch) — the prompt file is gone at attempt 1's close. Confirmed.
- **The resume claim is verified, not just read**: `dist/main.js:335-341` — `if (parsed.sessionId)` → `findLocalSessionByExactId` → `SessionManager.open(existingSession.path)`, and `buildChildArgv` (`seats.ts`) passes `--session-id <session.sessionId>`, today hardcoded to the job id at `hub.ts` (`sessionId: job.id`). Reusing the job id as session id resumes attempt 1's session with the errored assistant turn in context. **Fresh session id per attempt is mandatory, not preferable.** Your "unverified/inferred" flag can be retired.

**Where I still split from you — timer/counter ownership.** Your binding requirement is the **synchronous settle-point**, and there you are right for a reason sharper than the wait poll: I verified the flush gate (`usage-store.ts` — `manifests.some(m => m.startedAt >= p.markerAt && m.exitCode === null)` → gate-closed; choose-once at the record file) and its trigger (`index.ts` — hub `onChange` → `void flushUsage("forest-settle")`). A decision armed *after* `onChange` races the usage fan-out in the same synchronous dispatch: the gate reads a settled manifest and writes a permanently-short record before the supervisor flips anything, even though the flip lands before the next 200 ms poll. So the retry decision must run inside `settle()` before `writeJobManifest`/`onChange`. That is your strongest claim and I adopt it.

But "Hub owns the counter and the backoff timer" does not follow from that. What Hub must hold is: the `retrying` state, the settle-time hook point, a `respawn` that carries usage and takes a session id, and the cancel/wait clauses. The counter, the timer, and the classification can live in the supervisor module, invoked **synchronously from `settle()` via a Hub-held hook** — functionally identical to your split ownership, with a smaller §7 delta (hub.ts gains a hook field and clauses, not timer machinery). Your table's "Hub-held, resetting one job across attempts" and my hook-plus-module are the same design at the interface level; the difference is which file the timer sleeps in, and §7's text ("battle-tested; change behavior only with a failing test first") argues for the smaller hub.ts diff. If you insist the timer must live in Hub to make cancel-during-backoff atomic, that is a legitimate concession — but the test below settles whether it's needed.

## Response to designer

**Your sidecar is rejected, and not on taste — on a hard ingest defect you did not check.** `readManifests` (`runs.ts`) ingests **every** `*.json` in the run dir except `run.json`. Your `{id}.retry.json` would be parsed as a `RunManifest`, enter `buildTree` as a second node (R4's one-row rule broken by your own carrier), enter `sumSubtree`/`sumSubtreeUsage` walks, and surface in `pruneRuns` liveness checks. Filtering it out means editing the shared pure ingest path anyway — at which point you've spent the same blast radius you were avoiding, for a second channel that duplicates one optional field.

Also: the navigator reads manifests only (`navigator.ts:152, 344` — `flattenTree(buildTree(readManifests(...)))`) and so does `textTree` (`tree.ts`). Your P1–P12 smokes all require **new read paths in both renderers**, which contradicts your own "non-invasive layer above the hub" framing. One optional field on `RunManifest` (`attempt?: number`, absent when no retry) reaches both renderers through the channel they already read.

What I take from you, verified as sound: the **row grammar** (7 cases, label between seat and separator, `⏸` reuse, `retrying in Ns` with 2 s refresh), the **persist-after-settle label** (case d/e — works under cardinality A because the final manifest write keeps `attempt`), and the **exhaustion copy argument** (don't mirror R5's "Press Enter" — the hub can't honor it; the `--- provider error ---` clause already carries the last error, so `Exhausted after 3 attempts.` suffices). Your four-sentence table is rejected on one ground only: `Completed after 1 attempt.` / `Failed after 1 attempt.` fire on every ordinary dispatch and break EV-28's landed byte tests — reports stay byte-identical when no retry occurred; sentences exist only when `attempt ≥ 2`.

## Where I was wrong

1. **The decision point.** My round-1 supervisor subscribed to `onChange` — post-`writeJobManifest`, post-flush-fan-out. That is measurably wrong by principal's prediction 4 (verified flush-gate path above). The retry decision must be taken synchronously inside `settle()`, before the manifest write and the fan-out.
2. **ExitCode during backoff.** I said "set `retrying` before any poll can see the settled state" without noticing `job.exitCode` is already set by the close handler before `settle()` runs. The settle-time flip must also clear `exitCode` to null (the supervisor captures the real report first) so the manifest written during backoff is non-settled and the flush gate stays closed naturally. Principal's table already said this; I under-specified it.
3. **"Per-dispatch `loadRetryConfig` read"** — I concede principal's single-resolved-policy position: `index.ts` already loads once at init for the parent loop; passing that same object into the hub tools removes the two-reader divergence entirely and moves malformed handling to the one site that already has EV-40's disable-and-warn posture. A mid-session `.council.json` edit then applies on restart — same as the parent loop today.

## Standing disagreements

None remaining that prose can settle — the two below are the only places another seat could still flip me, and both are falsifiable by test:

1. **Timer in Hub vs supervisor module** (principal's table vs my §1). *Settling test:* T5 variant — arm a retry with a long injected backoff, call `hub.cancel(id)`, and assert (a) no second spawn ever occurs, (b) `council_wait` resolves `cancelled`, (c) cleanup ran. This passes under either placement; it only settles that the arbitration is race-free. The placement itself is then a §7-minimality judgment call, and I claim the hook-plus-module diff is strictly smaller than timer-in-Hub. If the skeptic shows the hook variant needs a second Hub→supervisor edge to make cancel atomic, I fold.
2. **`attempt` on `RunManifest` vs designer's EV-42-deferral.** *Settling test:* T9 — synthesize a run dir with one manifest carrying `attempt: 2`; assert `textTree` renders `attempt 2/3` and the navigator row renders the label with **zero** new read paths in either renderer. The sidecar variant of this test requires a `readSidecars` pass in both renderers — the diff size is the evidence.

## Converged design

1. **Loop seam / ownership.** Split ownership, principal's shape, one amendment: `settle()` calls a Hub-held settle-hook **synchronously, before `writeJobManifest`/`onChange`**; the hook's owner (a new `extensions/job-retry.ts` supervisor) classifies with `classifyRetry`, owns the counter and the unref'd injected timer, and re-spawns via a new `Hub.respawn(id, spec)` (fresh prompt tmpdir, fresh session id, fresh argv, carried usage). Hub holds: `retrying` state, the hook, `respawn`, cancel/wait clauses, and the stored `respawn`-enabling closure registration point. `council_dispatch` builds and registers the respawn closure (it owns spawn inputs). Surviving interface: **Hub = lifecycle state machine + hook; supervisor = policy decision + timer; dispatch = spawn inputs.**
2. **Manifest cardinality: (A)** — one Job / one `<id>.json`, overwritten per attempt, non-settled (`exitCode: null`) during backoff, `usage` carried cumulative across attempts (a bare `spawnJob` re-call zeroes it — `respawn` must carry `prev.usage`; EPIC-7 honesty invariant). This **re-scopes EV-42 explicitly**: its "fresh id/manifest/session per attempt" premise is false under (A); EV-42's subject becomes per-attempt attribution *within* the dispatch's record (per-attempt session JSONLs, history copy) plus tree/usage copy. The card record must say so, and EV-42 stays next in build order.
3. **`attempt` carrier: `RunManifest.attempt?: number`**, present only when ≥ 2 (attempt-1 manifests stay byte-identical — T8 snapshot); renderers read it through the existing `readManifests` path. Report-side: `formatWaitReport`/`formatReport` derive the fragment from the manifest (already in scope) — no `JobReport` schema change needed; the "of M" total comes from the one resolved policy at format time. No `maxAttempts` in the manifest, no `attemptGroupId` (EV-42's).
4. **Backoff state: new `JobState "retrying"`** on the Hub, set synchronously in the settle-path, manifest written non-settled. `tick()` unchanged (verified: skips non-`running`) — no stall/timeout interplay, no `killGroup` for a dead pid; `buildTree`'s orphan rule cannot fire (state ≠ `running`). Additive: `STATE_ORDER`/`STATE_TOKEN`/GLYPH entries in `navigator.ts` and `tree.ts` (`retrying` → `⏸`/`warning`). Designer's keep-`running` alternative is falsified first-hand: state `running` + dead pid ⇒ `☠ orphaned` during exactly R4's labelled window.
5. **Session identity: fresh session id per attempt, mandatory** (resume verified at `dist/main.js:339-341`). `Job` gains a `sessionId` field (defaults to id); `writeJobManifest` writes it instead of hardcoding `job.id` (today `hub.ts:96`); `buildChildArgv` receives it. Manifest `sessionId` always names the *current* attempt, so `findSessionFile`'s first-hit scan (verified: matches first-line `id`) resolves to the live transcript — the navigator tail is correct by construction, and the collision that makes first-hit ambiguous is exactly what fresh ids eliminate.
6. **Wait/cancel.** `isSettledForWait` gains an explicit `retrying → false`. `cancel` on a retrying job: set `cancelled`, synthetic `exitCode = 0` (wait's settledness keys on `exitCode !== null` — verified; no close event will fire mid-backoff), write manifest, fire onChange; the supervisor observes the state left `retrying` without its own respawn and aborts, running cleanup exactly once. Window-elapsed: `hub.wait` returns at its deadline with `state=retrying`, the identity line carrying `attempt=2/3`, **no usage block** (`formatWaitReport`'s settled set excludes `retrying`); parent re-waits per the never-cancel-on-timeout contract.
7. **Copy.** Row: designer's grammar verbatim, but state/glyph come from the real `retrying` state, not a sidecar status; `retrying in ${ceil((next−now)/1000)}s` on the 2 s widget refresh; R4 literal `attempt 2/3`; label shown whenever manifest `attempt > 1`, including after final settle; omitted at attempt 1. Report: head fragment `attempts=N/M` only when N>1; one sentence only when N>1 — clean: `Completed after ${n} attempts (1 initial + ${n-1} ${retries}).`; exhausted: `Exhausted after ${n} attempts.` (last error already in the `--- provider error ---` clause above). Single-attempt reports byte-identical.
8. **Timeout/stall: per-attempt.** `startedAt`/`lastActivityAt` re-based at each respawn; `timeout_minutes`/`stall_minutes` are per-attempt ceilings; the `council_dispatch` description is updated to say so. The alternative (per-dispatch ceiling) makes a late attempt flip to `timeout` immediately and `isSettledForWait` returns true → the wait race returns; per-attempt is the only coherent option. Elapsed in the report is the last attempt's — accepted, disambiguated by the attempts fragment.
9. **Malformed policy / read site.** One `loadRetryConfig` per process at init (already exists for the parent loop, `index.ts`), passed as one resolved object into the hub tools; a throw disables retry everywhere with EV-40's loud-warning posture. No per-dispatch re-read, no Hub policy import.

## Final split judgment

**Keep EV-39 whole; no split.** My round-1 "supervisor subscribing to `onChange`" is dead — the settle-point is inside `settle()`, and that single concession buys the flush-gate invariant, the wait invariant, and the manifest's non-settled backoff state with zero display-layer compensation. Principal's split-ownership framing and my module placement are one design; the surviving disagreement (timer residence) is a §7-diff-size question, and its settling test is named above. Designer's surface work survives nearly intact — grammar, glyphs, copy, countdown — minus the sidecar, which is superseded by the manifest field and independently dead on the `readManifests` ingest defect. EV-42 is re-scoped by ruling (A) and must be edited at this card's close, not silently.

Gates noted for the implement phase, unchanged from round 1: `bash council/preflight.sh EV-39` → `bunx tsc --noEmit` → `bun test` → `python3 council/validate.py`; AGENTS.md §7 satisfied — every hub.ts delta lands behind a failing test first.

#### Principal round 2 (`job-19.7`) — verbatim

**Cross-seam reading.** The contract this card actually has to move is narrower than "add a retry loop" and touches four independent readers of the same fields:

- `Hub.spawnJob` stores no spawn inputs — `Job` (`hub.ts:11-30`) has `seat/model/pid/state/timeoutMs/stallMs/cleanup/usage`, no `command/args/env`; `settle()` runs and nulls `cleanup` (`hub.ts:251-259`), and that cleanup is the dispatch's prompt-tmpdir `rmSync` (`hub-tools.ts:200-208`). So a Hub-owned re-spawn is impossible without a tool-layer closure. Owner's split ownership is correct.
- `Job.startedAt` is not a private field. It is the subtree-root boundary key (`spend.ts:224-249`: roots are `parentJobId === null && startedAt >= boundaryMs`) and the flush-gate key (`usage-store.ts:392-397`: any manifest with `startedAt >= markerAt && exitCode === null` closes the gate). Re-basing it per attempt moves a job across invocation boundaries.
- `writePids` filters `exitCode === null && pid`; the manifest gate requires `exitCode === null` during backoff. So "null the exitCode for the gate" and "keep the pid file clean" are in tension unless the retry decision also clears `pid` (or `writePids` guards on state).
- One manifest per dispatch collapses N session files into the single `sessionId` field, and `usage-store.ts` builds EV-29's provider job list as `findSessionFile(repoRoot, runId, m.sessionId)` — one path per manifest. Retried attempts' provider sessions are therefore invisible to the reported half.

**Blind spots, named separately.**
- *From the hub slice:* it reads `startedAt` as "when this job started" and cannot see that `spend.ts` and `usage-store.ts` use it as a window key; it also cannot see that nulling `exitCode` publishes a dead pid through its own `writePids`.
- *From the tool/dispatch slice:* it cannot see that the prompt tmpdir cleanup it hands to the Hub is exactly what must be withheld under retry, nor that the MCP allowlist it bakes into argv is frozen at dispatch (a re-spawn reuses a possibly-stale tool set).
- *From the renderer slice:* it cannot see that `nextAttemptAt` is not derivable from the manifest (during backoff `settledAt` is null, and jitter's `rand` draw is unrecorded — `retry.ts:155-168`), nor that R4's denominator `/3` has no carrier unless one is persisted.
- *From the run-substrate/usage slice:* EV-42's premise ("each attempt mints a fresh job id/manifest/session") is a description of *manual re-dispatch*, not of EV-39's mechanism; and EV-29's reported half cannot see past the final attempt once sessions are per-attempt.

**Reframe.** The per-attempt ceiling is a *monitor* concern, not an identity concern: don't express it by mutating the dispatch's identity clock. `startedAt` stays the first-spawn time (stable across attempts); the Hub gains an internal attempt clock for `tick()`. This is small (one `Job` field, two comparisons in `tick`) and it removes the spend/gate/pid knot in one move. The split-ownership reframe itself is satisfied as owner states it; no further reframe is needed there.

---

**Response to owner.** Split ownership as you now state it is exactly the round-1 reframe: Hub = lifecycle state machine + synchronous settle-hook before `writeJobManifest`; supervisor (`extensions/job-retry.ts`) = classification, counter, timer; dispatch = spawn inputs + the respawn closure. Two corrections: (1) the settle-hook cannot be the tool-layer `onChange` — `onChange` fires *after* `writeJobManifest` (`hub.ts:251-259`), so it must be a distinct synchronous Hub option, invoked while `exitCode` is still set, before the manifest write. (2) Your round-1 pid claim ("during backoff `exitCode !== null` so the pid file correctly omits the job") is inconsistent with the gate: the retry decision must null `exitCode` *before* `writeJobManifest` or the flush gate (`usage-store.ts:393`) opens mid-backoff. Nulling it also makes `writePids` publish attempt 1's dead pid unless `pid` is cleared in the same step (or `writePids` skips `state === "retrying"`). Timer residence (Hub vs supervisor) is not material to any invariant I can name — it's a §7/ownership judgment, and the deciding argument is that policy + timer + injected clock in one module is what makes T1/T3/T4 testable without touching `hub.ts`.

**Response to designer.** Yielding the sidecar is right: one writer, the manifest. But two consequences follow that your round-1 sidecar had been absorbing. (a) `nextAttemptAt` is persist-or-drop, not re-derivable — keep it only if the `retrying in Ns` countdown ships; if kept, the Hub writes it in the same atomic manifest write that sets `state=retrying` and clears it on respawn/settle, so no stale countdown renders on a settled row. (b) R4's `/3` denominator has no carrier: if `formatWaitReport`/`textTree`/`rowLine` re-call `loadRetryConfig`, that is a second load in the render path and diverges from the one-load-at-init ruling. Persist the denominator (`maxAttempts`, matching `RetryPolicy`) at dispatch, or pass the init snapshot into the renderers — but pick one and only one. Row grammar, glyph reuse (`⏸`), and the attempt sentence converge as you state; I have no design objection.

**Where I was wrong.** Two round-1 statements, both mine:
1. My interface table said "EV-39 must not reuse an id." That contradicts my own (A) recommendation. Under (A) the job id *is* the dispatch's identity and is deliberately reused across attempts; the attempt is distinguished by the `attempt` field and the session-id suffix. EV-42 must not assume a second id-space.
2. I wrote "(d) `startedAt`/`lastActivityAt` re-based at re-spawn." Wrong for `startedAt`: it is the spend boundary key (`spend.ts:245`) and the flush-gate key (`usage-store.ts:393`). Re-basing can move a retry across an invocation boundary and count cumulative usage in two records. With a stable `startedAt`, a job that began before marker M2 is excluded from M2's subtree and included once in M1's; re-based, it is included in both.

**Standing disagreements** (each with its settling test):
- **D1 — `startedAt` stable; per-attempt ceiling via a separate attempt clock.** Test: drive error-then-succeed with `timeout_minutes`/`stall_minutes` smaller than the backoff; assert `tick()` never stalls/times-out attempt 2 (uses the attempt clock), the final manifest's `startedAt` equals the first spawn's, and `report.elapsedMs` spans both attempts.
- **D2 — `exitCode` and `pid` nulled at the retry decision; `writePids` clean during backoff.** Test: after attempt 1 settles retryable, `readManifests(...)[id].exitCode === null` and `` `.pids.json` `` does not contain attempt 1's pid.
- **D3 — one denominator source.** Test: a rendered row and the final report agree on `maxAttempts` after a mid-session `.council.json` edit; and `grep -n loadRetryConfig extensions/` shows no call in the render path.
- **D4 — EV-29 reported half is partial for retried dispatches.** Test: a 2-attempt `openrouter/` dispatch yields a provider job list containing one session path (the final attempt's). Either persist the attempt session list or mark the reported row as final-attempt-only; a silent partial reported figure falsifies the honesty invariant.
- **D5 — window-elapsed retrying report.** Test: `council_wait` at its deadline returns `state=retrying` with the attempt count, no usage block (retrying is not in `formatWaitReport`'s settled set, `hub-tools.ts:107`), and `elapsed` = dispatch age.

**Converged design** (numbered):
1. One job id, one manifest, one job-tree row per dispatch (cardinality **A**); `attempt` = ordinal of the latest/imminent attempt, absent ⇒ 1; conditional spread so non-retry manifests stay byte-identical.
2. `JobState "retrying"`; `tick()` unchanged (already skips non-`running`); `isSettledForWait` gains `retrying → false` **first**, before the `exitCode` check; `cancel` returns true for `retrying`, sets `cancelled`, writes the manifest; `buildTree` orphan rule unchanged (`running`-only, `tree.ts:17`); `GLYPH`/`STATE_TOKEN`/`STATE_ORDER` gain the entry in both `tree.ts` and `navigator.ts`; `formatWaitReport`'s settled set does **not** gain it.
3. Retry decision inside `settle()`, synchronously, before `writeJobManifest`: classify the still-settled report, then set `state=retrying`, `exitCode=null`, `pid=undefined`, arm the supervisor timer, and only then write the manifest and fire `onChange`.
4. Supervisor owns counter, timer (injected, unref'd), and classification via `classifyRetry`/`computeBackoffDelay`; dispatch registers `{argsForAttempt(n), env, cwd, timeoutMs, stallMs, cleanup, policy}`; the prompt tmpdir survives (cleanup withheld from the Hub) and runs exactly once at chain end; `startedAt` stays the first spawn; the Hub gains an internal attempt clock for `tick`.
5. Fresh session id per attempt (`<id>-attemptN`); `writeJobManifest` carries the attempt's session id (byte-identical for non-retry since it equals `job.id`); final manifest's `sessionId` is the last attempt's. Prior attempt transcripts survive in the run dir but are unlinked from the manifest.
6. Cumulative usage carried on `respawn`; the single manifest holds the dispatch total, so `sumSubtreeUsage` stays correct.
7. Manifest additions: `attempt?`, `maxAttempts?` (denominator carrier), `nextAttemptAt?` only if the countdown ships — all conditional; EV-42 owns final naming.
8. One `loadRetryConfig` per process: reorder `index.ts` so the load precedes `registerHubTools(pi, repoRoot, { policy: () => retryPolicy })`; malformed → retry disabled, loud warning, dispatch proceeds; broaden the session-start copy from "parent-turn retry disabled" (`index.ts:682-686`) to both loops; add `retrying` to `council_wait`'s and `council_cancel`'s descriptions.
9. EV-42 split: EV-39 decides (A) and records it, because (B) without a group key ships an under-counted record and a two-row tree; EV-39 does **not** re-scope or absorb EV-42 (that is `steward`'s). At close, EV-42's Intent premise is replaced ("one dispatch = one id = one manifest; N session files; `attempt` on the manifest") and its handoff notes name `attemptGroupId` as possibly redundant with the job id, the provisional `maxAttempts`/`nextAttemptAt`, and the EV-29 session-list limitation.

**Final recommendation.** Adopt the converged design with D1 and D2 as binding corrections (they are correctness, not taste); ship `nextAttemptAt` only with the countdown and always clear it; persist the denominator or inject the init snapshot, never re-read in the renderer; record (A) as EV-39's ruling and hand EV-42 the premise correction plus its four handoff notes, leaving re-scoping to `steward`. The single test that falsifies the shipped design if the settle-point hook is misplaced: a usage-store test that drives an error-then-succeed stub inside an open invocation window and asserts the flush outcome is `gate-closed` at attempt 1's `onChange` and `written` exactly once, after the final settle, with `subtree.cost` equal to both attempts' session spend. A `written` outcome mid-backoff is the under-count bug.

#### Designer round 2 (`job-19.6`) — verbatim

I've read the card in full, the round-1 positions of both other seats, and re-verified the load-bearing code paths in `extensions/navigator.ts` (`rowLine` at L352-374, `GLYPH`/`STATE_TOKEN`/`STATE_ORDER` at L18-49), `extensions/tree.ts` (`textTree` at L58-67), `extensions/runs.ts` (`RunManifest` and `readManifests`), `extensions/hub.ts` (`JobState` at L5, `writeJobManifest` at L88-104, `settle` at L251-259, `cancel` at L261-274, `isSettledForWait` at L291-297), and `extensions/hub-tools.ts` (`formatReport` at L78-104). My round-2 position follows.

---

# Designer round-2 position on EV-39

## Response to owner

I yield the sidecar. Your argument is binding: `attempt 2/3` is unreachable from the renderers without `attempt` on the manifest they already read (`readManifests` is the single substrate path; `navigator.ts:344` and `tree.ts:64-67` read manifests only — the widget has no second read seam). My round-1 sidecar proposal asked the renderer to grow one, which is precisely the second file the render loop must read that you named. That cost is not worth the marginal separation of loop-vocabulary from hub-vocabulary — and the separation evaporates anyway under EV-42, which owns the substrate.

I accept the supervisor module and the `JobState "retrying"` with the guards you listed: `tick()` already `continue`s on non-`running`, so the kill semantics are untouched; `isSettledForWait` gains one clause; `cancel` gains one branch; `writePids` needs nothing. Each is the §7 "small additive change with a failing test first" pattern, and the tests you named (T7) cover them. AGENTS.md §7 is satisfied by construction.

I accept the session-id suffix for n ≥ 2. The same-session-resume hazard (attempt 2 seeing attempt 1's errored turn in context — EV-40 §2.4's structural filter is for the parent loop; the hub has no filter) is real, and `findSessionFile`'s first-hit scan (`runs.ts:139-160`) will return the wrong JSONL without the suffix. I record it as provisional pending EV-42's final naming.

I accept the cumulative-usage carry as the manifest's `usage` shape. The flush-gate concern (principal's `(B)` cardinality finding) closes only under `(A)`: one manifest, `exitCode: null` during backoff, `usage` accumulated across attempts. Under `(B)` every backoff opens the gate, and the record is permanently short the retried attempts' tokens. We agree on `(A)`.

One addition to your `attempt` field, not in your round 1: I argue the manifest also needs a transient `nextAttemptAt?: number`, set only during the `"retrying"` state, cleared the moment the next attempt spawns. The widget's countdown copy (`retrying in 1s`) needs the actual delay the loop chose, not a re-derivation from policy — jitter makes the re-derivation wrong. This is one nullable, ephemeral field, distinct from the durable `attempt`, and it lives on the same substrate the renderers already read. Without it, the row during backoff is `⏸ designer attempt 2/3 · retrying` with no countdown, and the operator loses the forcing function against the slip "the seat finished; let me move on."

On the report shape: I unify on your form — `attempts=N/M` head fragment (omitted at N=1 for byte-identity with pre-EV-39) plus a per-outcome sentence. The specific copy and the four cases are spelled out in *Converged surface* below.

## Response to principal

I yield the carrier. Your manifest-cardinality argument `(A)` vs `(B)` is decisive — I had under-counted how badly `(B)` corrupts the usage record (your prediction 4 pins this with a `gate-closed` assertion at every backoff moment). The under-count is silent and unfixable post-hoc; closing it is mandatory, not stylistic. I now hold `(A)`.

I accept the split-ownership framing as a description of where the seam lands (lifecycle state machine in the hub, respawn closure in the dispatch layer). I had not named it that cleanly in round 1; naming it sharpens who owns the cleanup closure, who mints the fresh prompt file, who mints the fresh session id. The supervisor module is the artifact of that split; the hub only grows additive deltas.

I accept the same-session-resume hazard and the fresh-session-per-attempt consequence. Your inference (attempt 2 on a resumed session re-sees the errored turn in context) is cheap to falsify with a two-attempt stub probe; I record it as a T1/T2 sub-assertion rather than a free-standing risk.

I accept the cumulative-usage carry and the resulting `flushUsage("forest-settle")` correctness under `(A)`.

I do not concede the timeoutMs/stallMs per-attempt semantics change without naming it. You flagged that re-basing `startedAt` at respawn makes `timeoutMs` a per-attempt ceiling (a 3-attempt dispatch may now run 45+ minutes against the parent's 15-minute default). This is a contract change to the dispatch tool. My position: the per-attempt ceiling is the *correct* reading — a single attempt that timed out would have timed out regardless of how many retries you wrap around it, and the retry budget is orthogonal to the per-attempt budget. The runner/dispatcher's caller is the right place to widen `timeout_minutes` if a 45-minute total ceiling is wanted; that is not the hub's call. The card should record this explicitly so the next runner doesn't conflate the two budgets.

On my round-1 textTree substitution (`"running"` substituted for `status: "waiting"` because the manifest "lies"): with the new `JobState "retrying"`, the manifest does not lie. `state` is `"retrying"` — that is the truth of the loop. textTree reads the manifest's `state` directly and renders `⏸ job-N designer attempt 2/3 retrying 0.5m pid=…`. No substitution. Your concern is moot under the new carrier.

I accept that two-loop compounding (your risk 5) is pre-existing and not EV-39's to solve; the `wait` blocking through retries is the engine-level mitigation this card delivers, and EV-40's R5 copy is the human-facing mitigation. They are non-overlapping.

## What I withdraw or change

**Withdraw (8 → 1):**

- The sidecar carrier (`{id}.retry.json`). Replaced by manifest `attempt?` + `nextAttemptAt?` + new `JobState "retrying"`.
- Q1 (format divergence `attempt 2/3` vs `attempt 2 of 3`). R4 binds the slash form on the row; R5 binds the spelled-out form on the input bar. The divergence is a deliberate cross-surface grammar — keep both, document the equivalence in the wiki.
- Q2 (`attempt N/M` on N=1 failed row). Rule is consistent: label iff `attempt` field is set AND N > 1. N=1 failed renders `✗ designer · failed` with no label, same as N=1 running renders `● designer · ran ls 12s` with no label.
- Q3 (prior-error history in report). Owner's exhausted form (`Retry budget exhausted after M of M attempts; last error: …`) carries the *last* error inline; per-attempt history lives in the preserved per-attempt JSONLs and is reachable via transcripts. The card's Intent asks for an explicit attempts sentence, not a full error history. Operator who needs the history reads the transcripts.
- Q5 (sidecar location and lifetime). N/A — no sidecar.
- Q6 (widget read path). Manifest only; single substrate read.
- Q7 (stall vs waiting glyph collision). ⏸ is reused. Disambiguation lives in the verb-first copy (`stalled 4m` vs `retrying in 1s`), which the operator reads along with the glyph. No new glyph; no new theme token.
- Q8 (exhaustion copy mirroring R5). Owner's exhausted form is the hub-side sentence; EV-40's R5 ("Retries exhausted after N attempts. The provider kept failing. Press Enter to try again.") is the input-bar-side and ends in an action the hub cannot honor. The two surfaces end in different actions on purpose.

**Change:**

- The countdown copy (`retrying in Ns`) now derives from `nextAttemptAt` on the manifest, set by the supervisor at the moment the backoff timer is armed, cleared at the moment the next attempt spawns. One new manifest field, transient, nullable. The widget reads manifest only.
- The report sentences converge on owner's shape; my "Completed after N attempts (1 initial + 2 retries)" form is withdrawn (the breakdown is implicit in the policy and in the per-attempt JSONLs; not worth the row's complexity).

## Converged surface

### Carrier

- `RunManifest` gains two optional fields: `attempt?: number` (current attempt index; absent for attempt 1, present and ≥ 2 for retries) and `nextAttemptAt?: number` (set only while `state === "retrying"`; cleared when the next attempt spawns).
- `JobState` union gains one value: `"retrying"` (backoff window only).
- No sidecar. No new module file beyond the supervisor; no second carrier.

### Row grammar — TUI widget (`extensions/navigator.ts` `rowLine`, L352-374)

The seat field rule (`pad = max(0, 14 - visibleWidth(styled))`) is preserved for rows without a label; rows with a label have `seat + " attempt N/M"` followed by `pad` applied to the combined visible width, accepting that the `·` separator shifts right on retrying rows. The label is high-priority information; alignment of the decoration `·` is not load-bearing.

| Case | Manifest | Rendered row (ANSI stripped) |
|---|---|---|
| (a) attempt 1, running | `state=running`, `attempt` absent | `● designer       · ran ls 12s` |
| (b) attempt 2/3, backoff | `state=retrying`, `attempt=2`, `nextAttemptAt` set | `⏸ designer attempt 2/3 · retrying in 1s` |
| (c) attempt 2/3, running | `state=running`, `attempt=2` | `● designer attempt 2/3 · ran ls 12s` |
| (d) attempt 3/3, settled clean | `state=done`, `attempt=3` | `✓ designer attempt 3/3 · settled 3m` |
| (e) attempt 3/3, exhausted | `state=failed`, `attempt=3` | `✗ designer attempt 3/3 · failed 2s` |
| (f) attempt 2/3, stalled mid-attempt | `state=stalled`, `attempt=2` | `⏸ designer attempt 2/3 · stalled 4m` |
| (g) attempt 2/3, cancelled | `state=cancelled`, `attempt=2` | `⊘ designer attempt 2/3 · cancelled` |

State vocabulary additions: `GLYPH["retrying"] = "⏸"`, `STATE_TOKEN["retrying"] = "warning"`, `STATE_ORDER["retrying"] = 0` (same slot as `running` — the loop is alive, the job is mid-execution). New branch in `settledCopy`: returns `{copy: "retrying in ${ceil((nextAttemptAt - now) / 1000)}s", age: ""}` for `state === "retrying"`. The label rule in `rowLine`: append ` attempt ${m.attempt}/${maxAttempts}` to `seat` only when `m.attempt` is present and `> 1`; `maxAttempts` is `loadRetryConfig(repoRoot).maxAttempts` at render time.

### Row grammar — headless textTree (`extensions/tree.ts` `textTree`, L58-67)

The label inserts between `seat` and `state`, mirroring the TUI widget. The manifest's `state` is read directly; no substitution.

| Case | textTree line (visible, ANSI stripped) |
|---|---|
| (a) attempt 1, running | `  ● job-1 designer running 0.5m pid=12345` (unchanged) |
| (b) attempt 2/3, backoff | `  ⏸ job-1 designer attempt 2/3 retrying 0.5m pid=12345` |
| (c) attempt 2/3, running | `  ● job-1 designer attempt 2/3 running 0.5m pid=12345` |
| (d) attempt 3/3, clean | `  ✓ job-1 designer attempt 3/3 done 3.0m pid=12345` |
| (e) attempt 3/3, exhausted | `  ✗ job-1 designer attempt 3/3 failed 2.0m pid=12345` |
| (f) attempt 2/3, stalled | `  ⏸ job-1 designer attempt 2/3 stalled 4.0m pid=12345` |
| (g) attempt 2/3, cancelled | `  ⊘ job-1 designer attempt 2/3 cancelled 0.1m pid=12345` |

### Final report — `formatReport` (`extensions/hub-tools.ts`, L78-104)

The sentence is appended after the `err` clause, before any trailing newline, so it lands at the very end of the per-job block (the `\n\n====` separator in `formatWaitReport` still separates jobs).

**Head fragment (in `identity` prefix):** `attempts=N/M` appended iff `N > 1`. At `N = 1`, head is byte-identical to pre-EV-39.

**Sentence:**

| Outcome | Sentence |
|---|---|
| `N = 1`, clean | (none — byte-identical) |
| `N = 1`, non-retryable failure | (none — the `--- provider error ---` clause already carries the error; the head's `state=failed` carries the outcome) |
| `1 < N < M`, clean | `Settled on attempt ${N} of ${M}.` |
| `1 < N < M`, failed (classifier flipped terminal mid-budget) | `Failed on attempt ${N} of ${M}.` (no error inline — the `--- provider error ---` clause handles it) |
| `N = M`, exhausted | `Retry budget exhausted after ${M} of ${M} attempts; last error: ${errorMessage}.` |
| `N = M`, exhausted, `errorMessage` empty | `Retry budget exhausted after ${M} of ${M} attempts.` (drop the `; last error: …` tail — defensive against the classifier-says-retry-but-message-empty edge case) |

The head fragment and the sentence together satisfy the card's Intent ("an explicit attempts sentence") while preserving byte-identity for the unchanged N=1 path (EV-28's byte tests hold).

## Open questions still requiring a ruling

**Q1 — Countdown tick interval: 1s (cross-surface consistency with EV-40 R5) vs 2s (match the existing widget refresh).** The widget refreshes every 2 seconds (`extensions/navigator.ts` widget timer). A 2s tick means the operator sees the countdown jump in whole-second steps and occasionally skip a value (e.g., 2 → 0). A 1s tick would match EV-40's input-bar countdown (`Retrying in 2s → 1s → 0s`) at the cost of a second ticker. The countdown's value as a forcing function against the "is this seat finished?" slip depends on the operator seeing it update. I lean **2s** (no new ticker; matches the existing widget refresh; the operator still sees the countdown update, just at 2s granularity), but the cross-surface consistency argument for 1s is real and the council should rule. This is the one question I genuinely cannot close from this seat.

No other questions are ruling-required. The remainder are settled by this round's convergence or by the other seats' positions.

## Falsifiable predictions (revised)

Each claim is a hypothesis the skeptic may run as a pure-seam test. The smoke names the artifact, the inputs, and the asserted output.

### Manifest substrate (`writeJobManifest` at `extensions/hub.ts:88-104`)

- **P1.** A non-retryable settled outcome (state `done`, `attempt` undefined) produces a manifest byte-identical to pre-EV-39: no `attempt` key, no `nextAttemptAt` key, same `sessionId`, same field order. *Smoke:* call `writeJobManifest` on a synthesized `Job` with `state=done`, `attempt` not assigned; assert the JSON output string-equals a pre-EV-39 baseline manifest.
- **P2.** A backoff state writes `state="retrying"`, `attempt=2`, `nextAttemptAt=<future>`, `settledAt=<attempt 1's settle>`, `exitCode=null` (the process is gone but the manifest is non-settled). *Smoke:* synthesize, assert.
- **P3.** An in-flight attempt 2 (post-spawn) writes `state="running"`, `attempt=2`, `nextAttemptAt=null` (cleared at spawn), `startedAt=<attempt 2's start>`, `settledAt=null`. *Smoke:* synthesize, assert.

### Row grammar — TUI widget (`extensions/navigator.ts` `rowLine`, L352-374)

- **P4.** For a node with `state="retrying"`, `attempt=2`, `settledAt=now-1000`, `nextAttemptAt=now+1000`, `rowLine(node)` returns (ANSI-stripped) `⏸ designer attempt 2/3 · retrying in 1s`. *Smoke:* instantiate widget with synthesized TreeNode, pin `now()`, assert.
- **P5.** Same node with `nextAttemptAt=now+2000`: `retrying in 2s`. With `nextAttemptAt=now+500`: `retrying in 1s` (ceil). With `nextAttemptAt=now`: `retrying in 0s`. *Smoke:* as P4 at three `now` offsets.
- **P6.** For a node with `state="running"`, `attempt` undefined: row is `● designer       · ran ls 12s` (no label, byte-identical to today's row). *Smoke:* instantiate widget with attempt-1 manifest, assert visible string contains no `attempt` substring.
- **P7.** For a node with `state="running"`, `attempt=2`, transcript tail with a `toolCall` block at `now-12000`: row is `● designer attempt 2/3 · ran ls 12s`. *Smoke:* synthesize tree node + tail.
- **P8.** For a node with `state="cancelled"`, `attempt=2`: row is `⊘ designer attempt 2/3 · cancelled`. *Smoke:* synthesize.

### Row grammar — headless textTree (`extensions/tree.ts` `textTree`, L58-67)

- **P9.** For a node with `state="retrying"`, `attempt=2`, `textTree` emits `⏸ job-N designer attempt 2/3 retrying 0.5m pid=…` (no substitution — the manifest's `state` is read directly). *Smoke:* synthesize manifest in temp run dir, call `textTree`, assert.
- **P10.** For a node with `attempt` undefined: line is byte-identical to pre-EV-39 (`● job-N designer running 0.5m pid=…`). *Smoke:* synthesize, assert.

### Report — `formatReport` (`extensions/hub-tools.ts:78-104`)

- **P11.** `formatReport({id:"job-5", seat:"designer", state:"done", elapsedMs:180000, stopReason:"stop", usage:<fixture>})` (N=1, clean) returns a string byte-identical to pre-EV-39 output. *Smoke:* unit test, string equality.
- **P12.** `formatReport(<N=2, clean>)` returns a string whose `identity` prefix ends with `attempts=2/3` and whose tail has the line `Settled on attempt 2 of 3.`. *Smoke:* synthesize.
- **P13.** `formatReport(<N=3, exhausted, errorMessage:"Provider finish_reason: error">)` returns a string whose `identity` prefix ends with `attempts=3/3` and whose tail has the line `Retry budget exhausted after 3 of 3 attempts; last error: Provider finish_reason: error.`. *Smoke:* synthesize.
- **P14.** `formatReport(<N=3, exhausted, errorMessage="">)` returns a string whose tail has `Retry budget exhausted after 3 of 3 attempts.` (no `; last error: …` tail). *Smoke:* synthesize with empty errorMessage.
- **P15.** `formatReport(<N=2, failed, errorMessage:"…">)` returns a string whose tail has `Failed on attempt 2 of 3.` (no error inline — the `--- provider error ---` clause carries it). *Smoke:* synthesize.

### Width / accessibility

- **P16.** At `width=50`, attempt-2 running row renders as `● designer attempt 2/3 · ran…` (label preserved, verb truncated). At `width=30`, the row renders as `● designer attempt…` (label partially truncated; `/3` dropped). At `width=80`, no truncation. *Smoke:* widget render at `width ∈ {30, 50, 80}`; assert `truncateToWidth` preserves the label where it can.
- **P17.** At `width=80`, all 7 cases (a)–(g) render with no `…` truncation marker. *Smoke:* as P16 at width 80.

### Theme / glyph compliance

- **P18.** `GLYPH["retrying"] = "⏸"`; `STATE_TOKEN["retrying"] = "warning"`; `STATE_ORDER["retrying"] = 0`. No new glyph, no new theme token. *Smoke:* static assertion on the three constants in `extensions/navigator.ts:18-49`.
- **P19.** `extensions/navigator.ts` introduces no new `theme.fg/bg/bold` token calls beyond the existing palette; no literal hex/ANSI escapes. *Smoke:* `grep -nE '#[0-9a-fA-F]{6}|\\x1b' extensions/navigator.ts` returns zero matches after the change.
- **P20.** `settledCopy` gains one branch for `state === "retrying"`: returns `{copy: \`retrying in ${…}s\`, age: ""}`. No other branches change. *Smoke:* snapshot diff.

### Loop semantics (settled at implementation, but pin the seam now)

- **P21.** The supervisor sets `state="retrying"` and writes the manifest with `nextAttemptAt` *before* the `setTimeout` is armed (i.e., before the 200ms `isSettledForWait` poll can observe a settled state). *Smoke:* drive a fail-once stub; record the order of `(state flip, manifest write, setTimeout arm, setTimeout fire)`; assert the first three happen in the same synchronous tick after `settle()`.
- **P22.** `hub.wait([id], 60_000)` does not return during a backoff window whose delay is < 60s. *Smoke:* drive a fail-then-succeed stub with `baseDelayMs=1000, maxDelayMs=1000, jitter:false`; call `hub.wait` with `timeoutMs=60_000`; assert the wait does not resolve until attempt 2 settles.
- **P23.** `hub.cancel(id)` during `"retrying"` returns `true`, marks the manifest `state="cancelled"`, disarms the timer (no second spawn). *Smoke:* drive a fail-then-succeed stub, cancel mid-backoff, assert spawn count = 1.
- **P24.** A non-retryable settled outcome (`classifyRetry` returns `"terminal"`) writes no `nextAttemptAt` field, leaves `attempt` undefined, and produces a manifest byte-identical to pre-EV-39 (P1 holds for this case). *Smoke:* drive a `stopReason=stop` stub; assert.

## Preferences, ranked last

- The label placement (between seat and `·`) is a small taste call within R4's literal wording. I prefer it there because it groups with the seat identifier (a status fragment, not trailing metadata); truncation preserves the label ahead of the verb. This is a preference, not a grounding — `attempt 2/3` after `mins` would also be readable. The owner or consolidator may move it.
- The verb `retrying in Ns` over `backoff in Ns` is a small copy preference (matches EV-40's vocabulary; one fewer word to learn). The widget would render just as well with `backoff` or `waiting`. Preference.
- `Completed after N attempts.` was my round-1 wording; I yielded to owner's `Settled on attempt N of M.` because "on attempt N of M" tells the operator *which* attempt landed, not just *how many* ran. The grammatical shift (singular "attempt" inside a "of N" phrase vs. plural "attempts" after "after") is a side effect of the choice and reads better. Preference in wording, not in structure.

---

No files written this round. No application code edited. No worktree created. No branch checked out.



### Step 4 — Skeptic attacks and runs tests

Skeptic `job-19.8` (2.9m, 20 turns), pre-implementation at `f3c2ed1`; all probes under `/tmp/ev39`, no tracked file touched, no branch state mutated.

**Result terms (facilitator index of the report below):**

- **O-1 `closed-red`** — `owner` point 8 (re-based `startedAt`) falsified by a live `spendRecord` probe: a re-based cumulative manifest is counted in **two** invocation subtrees (M1 90 + M2 90). `principal`'s D1 (stable `startedAt` + internal attempt clock) holds; owner's point 8 falls. **Blocking, now closed by test.**
- **O-2 `closed-red`** — nulling `exitCode` during backoff while retaining the dead pid publishes it: `writePids`' filter is `exitCode === null && pid` with no state guard (verified live). `principal`'s D2 stands: clear `pid` in the same step, or guard `writePids` on `state === "retrying"`. **Blocking, now closed by test.**
- **O-3 `closed-red`** — the designer's `{id}.retry.json` sidecar is worse than a second row: `readManifests` ingests it (only `run.json` excluded) and `buildTree` **crashes** (`TypeError ... 'a.id.localeCompare'`). Sidecar dead; designer already yielded to the manifest carrier.
- **O-4 `closed-green`** — fresh session id per attempt is **mandatory**: live two-arm `pi -p --session-id` probe resumed arm 1's session and answered in its context.
- **O-5 `closed-green`** (five sub-claims, one half `open-untested`) — `isSettledForWait` true for `timeout`; `cancel` false once `exitCode !== null`; `tick()` skips non-`running`; `buildTree` renders `orphaned` for `running` + dead pid; `formatWaitReport` emits no usage block for a non-settled state. The `retrying`-specific half of the `tick()` claim is `open-untested` until the state exists.
- **O-6 `open-untested`** — the card `goal` is satisfiable by the converged design with only the agreed field name `attempt` and no `attemptGroupId`/per-attempt manifests (verified against EV-40 spec §2.8), **but** `owner` ("EV-42 must be edited at this card's close") vs `principal` ("EV-39 does not re-scope or absorb EV-42; that is `steward`'s") is a live contradiction about who corrects EV-42's "fresh id/manifest/session per attempt" premise. Not runnable — a ruling question.
- **O-6b (denominator carrier) — testable gap, blocking** — `owner` ("no `maxAttempts` in the manifest"), `principal` (persist `maxAttempts?`), `designer` (read `loadRetryConfig` at render time) are three-way inconsistent; the render-time read is exactly the second-load D3 forbids. O-8 confirms no policy carrier exists at either render site today.
- **O-7 `open-untested`** — byte-identity of a non-retry manifest and an N=1 `formatReport` is a pre-implementation baseline only; named implementation gates T8/P1 (snapshot string-equality; EV-28 byte tests stay green).
- **O-8 `closed-green`** — no `loadRetryConfig` call exists in `navigator.ts`/`tree.ts`; neither render site has a policy carrier, so a persisted/injected denominator is required.
- **Also verified:** `principal`'s D4 at code level (`usage-store` builds EV-29's provider job list from `findSessionFile(m.sessionId)` — one path per manifest, so per-attempt sessions beyond the final are invisible to the reported half); D5's window-elapsed shape is pre-supported by O-5's `timeout`-no-block finding.

**Blocking summary (verbatim from the report):** the design as converged blocks on three items — O-1 (adopt D1's stable `startedAt`), O-2/D2 (null `pid` alongside `exitCode`, or guard `writePids`), and the denominator carrier (pick one; never re-read in the render path). Non-blocking named implementation gates: T8/P1 byte-identity snapshots, the `retrying`-state units (wait-blocks-through-backoff, cancel-disarms-timer, no-stall-kill, one-row + `attempt N/M`, gate-closed-at-`onChange` + written-once-with-cumulative-usage), the D4 session-list limitation disclosure, and the EV-42 premise correction (ruling).

#### Skeptic report (`job-19.8`) — verbatim

All probes run. Compiling the verdict.

**Skeptic step-4 report — EV-39, branch `f3c2ed1` (pre-implementation; no tracked file touched, all scratch under `/tmp/ev39`)**

Wiki used: `vault/wiki/index.md`, `hub-job-supervision.md`, `run-transcripts.md`, `spend-record.md`, `usage-store.md`, `usage-block.md`, `main-repo-immutability.md` (complied with: no checkout/switch/reset, no repo writes).

---

**O-1 — `startedAt` semantics (D1). Claim under attack:** owner's converged point 8 (re-base `startedAt`/`lastActivityAt` per attempt) vs principal's D1 (`startedAt` stable, separate attempt clock).
**Settling test as run:** `bun /tmp/ev39/probe-o1.ts` — drives the real `spendRecord` (`spend.ts:224-249`, roots `parentJobId===null && startedAt>=boundaryMs`) twice (markers M1, M2; M2 lands between attempt-1 settle and attempt-2 spawn) with one cumulative-usage manifest under (a) rebased clock, (b) stable clock; plus the real flush-gate predicate (`usage-store.ts:392-397`).
**Actual output:**
```
O1 backoff(stable,exitCode null) gate-closed: true (expect true: flush stays shut)
O1 settled(exitCode 0) gate-closed: false (expect false: flush may write)
O1 REBASED: M1 subtree.input = 90 | M2 subtree.input = 90 (cumulative 90 counted in BOTH => double-count)
O1 STABLE: M1 subtree.input = 90 | M2 subtree.input = 0 (counted once)
```
**Result: `closed-red`** — re-basing is falsified. A re-based cumulative manifest is counted in two invocation subtrees (90 + 90). Principal's D1 holds; owner's point 8 must fall. **Blocks.**

**O-2 — `exitCode`/`pid` during backoff (D2). Claim under attack:** owner's round-1 "writePids needs no change" vs principal's D2 (null both `exitCode` and `pid`, or guard `writePids`).
**Settling test as run:** `bun /tmp/ev39/probe-hub.ts` + `probe-pid.ts` — real `Hub` with `test/stub-child.ts`; pid-file contents while running vs after settle; plus source read of the filter.
**Actual output:**
```
O2 pid file while running: [787963] contains pid: true
pid file after cancel-settle: []
O2 writePids filter line: "const pids = [...this.jobs.values()].filter((j) => j.exitCode === null && j.pid).map((j) => j.pid);"
O2 writePids mentions state/retrying: no
```
**Result: `closed-red`** — the filter (`hub.ts`, `writePids`) keys on `exitCode===null && pid` with no state guard (verified live in both reachable states). Nulling `exitCode` while retaining the dead pid *would* publish it. Owner's round-1 claim is dead (owner already conceded in round 2 §"Where I was wrong" 2); principal's D2 stands: clear `pid` in the same step or add the state guard. Manifest gate (`usage-store.ts:393`) confirmed to require `exitCode===null` via O-1's gate-closed probe. **Blocks the design as written unless D2's null-both step is in the plan.**

**O-3 — sidecar ingest defect. Claim under attack:** designer's round-1 `{id}.retry.json` sidecar is a non-invasive carrier.
**Settling test as run:** `bun /tmp/ev39/probe-pure.ts` (O3 section) — temp run dir, one valid manifest + `job-1.retry.json`, real `readManifests` → `buildTree`.
**Actual output:** `O3 THREW: TypeError: undefined is not an object (evaluating 'a.id.localeCompare')` at `runs.ts:114` — the sidecar is ingested (only `run.json` is excluded, `runs.ts:107`) and crashes the sort because it has no `id`. Worse than a second row: a crash.
**Result: `closed-red`** — sidecar dead, as owner argued and designer conceded. No standing dispute; record the crash (not just the double-row) as the reason.

**O-4 — session-resume hazard. Claim under attack:** principal's "fresh session id per attempt is mandatory" (owner round-2 agrees, claims verified).
**Settling tests as run:** (a) bundle inspection — `sed -n 270,345p dist/main.js` shows `parsed.sessionId` + existing → `findLocalSessionByExactId` → `SessionManager.open` (resume), else `create`; (b) live two-arm probe — `pi -p --session-dir $D --session-id ev39probe` twice ("HELLO" then "WORLD"), then read the JSONL.
**Actual output:** one file `..._ev39probe.jsonl`, 7 entries: `user HELLO / assistant … / user WORLD / assistant WORLD` — arm 2 resumed arm 1's session and answered in its context.
**Result: `closed-green`** — reuse-the-job-id provably resumes the errored turn's session. Fresh-session-per-attempt is mandatory. No dispute remains; the plan must include it.

**O-5 — supervisor/state clauses vs the real `Hub` + stub child.**
- `isSettledForWait` true for `timeout`: `bun /tmp/ev39/probe-hub.ts` → `O5a wait returned state: timeout | exitCode (null=alive): null`. **closed-green** (code `hub.ts:291-292` + live).
- `cancel` false once `exitCode!==null`: same run → `O5c settled state: done | cancel after settle returns: false`. **closed-green** (`hub.ts:263-265` + live).
- `tick()` skips non-`running`: code `hub.ts:233` (`if (job.state !== "running") continue`) — no live backoff state exists to exercise; existing suite covers the stalled-kill path. **closed-green on code reading, backoff-specific half open-untested** (needs the `retrying` state to exist).
- `buildTree` orphan for `running`+dead pid: pure probe → `O5 orphaned(running+dead pid): true` (`tree.ts:17` + live pure call). **closed-green** — this is what falsifies designer's keep-`running` alternative.
- `formatWaitReport` no usage block for non-settled: pure probe → running: false, timeout: false (settled set `hub-tools.ts:107` excludes both). **closed-green**, with the note that `timeout` is settled-for-*wait* yet blockless — the D5 `retrying` report inherits this shape naturally.

**O-6 — EV-40 §2.8 framing (constructor's interview).** Cited text verified first-hand: spec §2.8 "The hub/seat retry loop, its attempt counter, and the run-substrate `attempt` manifest field (EV-39, EV-42). Only the **field name `attempt`** is agreed here; neither loop reads the other's state." The card goal (re-spawn with backoff till budget/clean + attempts sentence) is satisfiable by converged points 1–6 with only the agreed name — no `attemptGroupId`, no per-attempt manifests. But converged point 2 (cardinality A) plus owner's "EV-42 must be edited at this card's close" vs principal's "does **not** re-scope or absorb EV-42 (that is `steward`'s)" is a live process contradiction about who re-scopes EV-42's "fresh id/manifest/session per attempt" premise. **Result: `open-untested`** (not runnable; needs a steward ruling, not a test).
Additionally flag: principal's point 7 (`maxAttempts?` persisted) vs owner's point 3 ("No `maxAttempts` in the manifest") vs designer's round-2 rowLine ("`maxAttempts` is `loadRetryConfig(repoRoot).maxAttempts` at render time") — the denominator carrier is **three-way inconsistent**; designer's render-time read is exactly the second-load principal's D3 forbids. **Testable gap, blocks.**

**O-7 — R4 byte-identity.** Baseline recorded pre-implementation: `formatReport` head today is `[job-5] seat=skeptic state=done stopReason=stop elapsed=3.1m turns=1 …`, no `attempt` substring; manifest keys are exactly `id,seat,model,parentJobId,pid,sessionId,state,startedAt,settledAt,exitCode,usage,stopReason` — no `attempt/maxAttempts/nextAttemptAt`. The conditional-spread instrument (absent-at-1 ⇒ byte-identical) does not exist yet, so the claim is untestable now. **Result: `open-untested`** with named implementation gate: T8/P1 snapshot — non-retry manifest string-equals baseline, N=1 `formatReport` string-equals baseline (EV-28 byte tests must stay green).

**O-8 — R4 `/3` denominator.** Verified: `loadRetryConfig` occurs in `index.ts` (once, line 588) and nowhere else — `false` in both `navigator.ts` and `tree.ts`; neither `rowLine` nor `textTree` takes a policy/maxAttempts parameter. **Result: `closed-green`** for the factual half (no carrier exists today; a persisted/injected denominator is required), which is precisely why the O-6 denominator contradiction blocks.

**Anything else:** principal's D4 verified at code level — `usage-store.ts` builds EV-29 jobs as `findSessionFile(input.repoRoot, p.runId, m.sessionId)` (grep true), one path per manifest, so per-attempt sessions beyond the final are invisible to the reported half (**closed-green** on shape; live OpenRouter fetch half open-untested). D5's window-elapsed shape is pre-supported by O-5's timeout-no-block finding. Designer's 1s-vs-2s tick question is untestable pre-implementation — implementation gate, default 2s (existing widget refresh) unless ruled. Gate-integrity note: my probes demonstrably go red (O3 threw, O1 showed the double-count), so the harness is not decoration.

---

**Blocking summary.** The design as converged **blocks** on three items: (1) O-1 — owner's point 8 re-base must be replaced by principal's D1 stable-`startedAt` + attempt clock (double-count proven); (2) O-2/D2 — the retry-decision step must null `pid` alongside `exitCode` (or guard `writePids`); the dead-pid publish is proven by the filter's exact shape; (3) the denominator carrier — owner/principal/designer are three-way inconsistent (no-manifest vs `maxAttempts?` vs render-time re-read); pick one (principal's D3: persist or inject, never re-read). Non-blocking named implementation gates: T8/P1 byte-identity snapshots, `retrying`-state units (T7/P21–P23: wait-blocks-through-backoff, cancel-disarms-timer, no-stall-kill, one-row + `attempt N/M`, gate-closed-at-`onChange` + written-once-with-cumulative-usage), D4 session-list limitation disclosure, and the steward ruling on EV-42's premise correction (O-6).


### Step 5 — synthesis

Consolidator `job-19.9` (1.6m, 4 turns) returned the three-bucket sort below, written to this card verbatim.

### Step 6 — routing (facilitator)

The consolidator's own closing answer is **"Ready to hand off? no"**: two blocking open-judgment calls must be ruled before a plan can lock, plus one scope/values call on the honesty disclosure.

**Phase 1 rulings checked first (`<escalation_contract>` step 1).** None applies to any of the four items below: R1 is the classifier, R2/R3 the policy defaults/validation, R4 the one-row visibility ruling (already applied — it is *why* a denominator is needed, but it does not say who carries it) and R5 the parent-turn copy. No item is "the same question, or squarely covered by a ruling already on record"; nothing is re-asked and nothing is extended.

**Consequently the facilitator decides nothing here.** The four items are carried to the orchestrator as an `ESCALATION` (facts only, no recommendation), and the card stays `Deliberating` — matching the EV-40 precedent (`934d406`: card `Deliberating` through its step-5+6 escalation). No spec is written, no owner dispatched, no code touched.

- **Open judgment 1 (blocking)** — denominator carrier: inject the resolved `RetryPolicy` init snapshot into the renderers (`owner`) vs persist `maxAttempts?` on the manifest at dispatch (`principal`). `designer`'s render-time `loadRetryConfig` read is already `closed-red` by O-8 + `principal`'s D3. No test settles the remaining pick.
- **Open judgment 2 (blocking)** — who corrects `EV-42`'s "fresh id/manifest/session per attempt" premise: `owner` ("EV-42 must be edited at this card's close") vs `principal` ("EV-39 does **not** re-scope or absorb EV-42; that is `steward`'s"). Skeptic O-6 `open-untested` — no test possible.
- **Open judgment 3 (non-blocking; default 2s)** — countdown tick interval 1s (cross-surface consistency with R5) vs 2s (existing widget refresh).
- **Open judgment 4 (blocks the honesty disclosure)** — `principal`'s D4: EV-29's provider-reported figure being final-attempt-only acceptable as a *disclosed limitation*, or the attempt-session list must be persisted / the reported row marked in this card.

The consolidator's **open objections** (O-7 byte-identity snapshots, the `retrying`-half of the `tick()` claim, the T7/P21–P23 `retrying`-state units, the gate-closed/written-once usage test, and the network-gated D4 live fetch) need no ruling — they are named implementation/integration gates the step-9 verification must close.

#### Consolidator synthesis (`job-19.9`) — verbatim

I have the complete record. Let me synthesize.

---

# EV-39 — Consolidator synthesis (step 5)

Quoting the record only; no new design, no resolution of open disputes. Wiki pages cited: `vault/wiki/hub-job-supervision.md`, `run-transcripts.md`, `usage-store.md`, `spend-record.md`, `usage-block.md`, `main-repo-immutability.md` (complied with — no git state touched, no files written).

## Settled

1. **Split ownership / loop seam.** Hub = lifecycle state machine + a synchronous settle-hook invoked *before* `writeJobManifest`/`onChange`; a new `extensions/job-retry.ts` supervisor = classification (`classifyRetry`) + counter + unref'd injected timer + `computeBackoffDelay`; `council_dispatch` = spawn inputs + the respawn closure (fresh prompt tmpdir, fresh argv, MCP allowlist). Converged round 2 (owner §1, principal §"Response to owner", designer §"Response to owner"). No Skeptic probe contested the seam.

2. **Retry decision point is inside `settle()`, synchronously, before `writeJobManifest`/`onChange`.** Owner conceded round-2 ("Where I was wrong" 1) that a supervisor subscribing to `onChange` races the flush fan-out. The flush-gate predicate (`usage-store.ts:392-397`: `startedAt >= markerAt && exitCode === null` ⇒ gate-closed; choose-once at `:398-402`) was verified live in **O-1's gate-closed probe** (`O1 backoff(stable,exitCode null) gate-closed: true` / `O1 settled(exitCode 0) gate-closed: false`). Closed by deliberation + O-1's gate-predicate output.

3. **Re-based `startedAt` falsified → stable `startedAt` + internal attempt clock (principal D1).** **O-1 `closed-red`**: `O1 REBASED: M1 subtree.input = 90 | M2 subtree.input = 90 (cumulative 90 counted in BOTH => double-count)` vs `O1 STABLE: M1 subtree.input = 90 | M2 subtree.input = 0 (counted once)`. Owner's converged point 8 falls; principal's D1 holds. **Blocking, closed by test.**

4. **Dead-pid publish → clear `pid` alongside `exitCode`, or guard `writePids` (principal D2).** **O-2 `closed-red`**: the filter is `const pids = [...this.jobs.values()].filter((j) => j.exitCode === null && j.pid).map((j) => j.pid)` with `writePids mentions state/retrying: no`. Nulling `exitCode` while retaining the dead pid would publish it. Owner's round-1 "writePids needs no change" is dead (owner already conceded round-2 "Where I was wrong" 2). **Blocking, closed by test.**

5. **Sidecar rejected → manifest carrier (`RunManifest.attempt?: number`).** **O-3 `closed-red`**: `readManifests` ingests `job-1.retry.json` (only `run.json` excluded, `runs.ts:107`) and `buildTree` **crashes** — `O3 THREW: TypeError: undefined is not an object (evaluating 'a.id.localeCompare')` at `runs.ts:114`. Worse than a second row: a crash. Designer yielded round-2 ("I yield the sidecar"). Closed by test.

6. **Fresh session id per attempt is mandatory.** **O-4 `closed-green`**: live two-arm `pi -p --session-id ev39probe` probe produced one JSONL with `user HELLO / assistant … / user WORLD / assistant WORLD` — arm 2 resumed arm 1's session and answered in its context. The resume path is verified at `dist/main.js:335-341` and now live-confirmed. Closed by test.

7. **Manifest cardinality (A): one job id / one `<id>.json`, overwritten per attempt, non-settled (`exitCode: null`) during backoff, `usage` carried cumulative across attempts via `Hub.respawn`.** Converged round 2 (all three seats). The (B) alternative (per-attempt manifests) is falsified by the flush-gate predicate verified in O-1/O-5: under (B) every backoff opens the gate and `flushUsage("forest-settle")` writes a permanently-short record (principal's prediction 4 + the gate-closed probe). Closed by deliberation + the gate-predicate code reading.

8. **`JobState "retrying"` clauses.** **O-5 `closed-green`** (code reading + live where reachable): `isSettledForWait` true for `timeout` (`O5a`); `cancel` false once `exitCode !== null` (`O5c`); `tick()` skips non-`running` (`hub.ts:233`, `if (job.state !== "running") continue`); `buildTree` renders `orphaned` for `running` + dead pid (`O5 orphaned(running+dead pid): true` — this is what falsifies designer's keep-`running` alternative); `formatWaitReport` emits no usage block for a non-settled state (settled set `hub-tools.ts:107` excludes both `running` and `timeout`). Additive deltas: `isSettledForWait` gains `retrying → false` **first**, before the `exitCode` check; `cancel` returns true for `retrying`, sets `cancelled`, writes manifest; `GLYPH["retrying"]="⏸"`, `STATE_TOKEN["retrying"]="warning"`, `STATE_ORDER["retrying"]=0` in both `tree.ts` and `navigator.ts`; `formatWaitReport`'s settled set does **not** gain it. Closed by test (code-reading half).

9. **Wait/cancel semantics.** `council_wait` resolves once, after the final attempt; a window-elapsed deadline returns `state=retrying` with the `attempt=2/3` label and **no usage block** (D5 shape, pre-supported by O-5's timeout-no-block finding); `cancel` during backoff sets `cancelled`, synthetic `exitCode=0` so wait's `exitCode !== null` settledness keys correctly, disarms the supervisor timer (spawn count stays 1), cleanup runs once. Converged round 2 + O-5 shape.

10. **Report shape.** Head fragment `attempts=N/M` appended iff `N > 1`; one per-outcome sentence iff `N > 1` — clean: `Settled on attempt ${N} of ${M}.`; exhausted: `Retry budget exhausted after ${M} of ${M} attempts; last error: ${errorMessage}.` (drop the `; last error:` tail when `errorMessage` is empty); `1 < N < M` failed: `Failed on attempt ${N} of ${M}.` Single-attempt (`N=1`) reports are byte-identical to pre-EV-39. Converged round 2 (owner §6/§7, designer "Converged surface"). Exhaustion copy deliberately does **not** mirror EV-40's R5 "Press Enter" (the hub cannot honor it) — converged.

11. **Per-attempt timeout/stall semantics.** `timeoutMs`/`stallMs` are per-attempt ceilings, expressed via the internal attempt clock (D1), **not** by re-basing `startedAt`. `startedAt` stays the first-spawn time. Settled as a consequence of D1 (O-1). Principal flagged the contract change (a 3-attempt dispatch may now run 45+ min against the 15-min default) and that `council_dispatch`'s description must record it — an implementation note inside the settled constraint, not an open question.

12. **No `loadRetryConfig` in the render path; a persisted/injected denominator is required.** **O-8 `closed-green`**: `loadRetryConfig` occurs once at `index.ts:588` and `false` in both `navigator.ts` and `tree.ts`; neither `rowLine` nor `textTree` takes a policy parameter. Designer's round-2 render-time re-read is forbidden by principal's D3 (and by O-8). Closed by test.

13. **EV-29 provider-reported figure is final-attempt-only (principal D4) — shape verified.** **Skeptic verified at code level**: `usage-store.ts` builds EV-29's provider job list as `findSessionFile(repoRoot, runId, m.sessionId)` — one path per manifest, so per-attempt sessions beyond the final are invisible to the reported half. Closed-green on shape. (The live OpenRouter fetch half is open — see Open objections.)

14. **Malformed policy / read site: one `loadRetryConfig` per process at init, passed as one resolved object into the hub tools; a throw disables retry everywhere with EV-40's loud-warning posture.** Converged round 2 (owner "Where I was wrong" 3 concedes principal's single-resolved-policy position).

15. **Two-loop compounding is pre-existing, not EV-39's to solve; `wait` blocking through retries is the engine-level mitigation this card delivers; EV-40's R5 copy is the human-facing mitigation.** Converged round 2 (principal risk 5, designer "Response to principal").

16. **Row grammar (7 cases (a)–(g)), glyph reuse `⏸`, label between seat and `·`, R4 literal `attempt 2/3` (slash), label shown iff `attempt > 1`, omitted at `N=1`.** Converged round 2 (designer "Converged surface", owner §7 accepts). `textTree` reads manifest `state` directly (no substitution — the manifest no longer "lies" under the new `retrying` state).

## Open judgment — for `product-owner`, escalating to `steward`

1. **Who corrects EV-42's "fresh id/manifest/session per attempt" premise?** `owner` ("EV-42 must be edited at this card's close — not silently") vs `principal` ("EV-39 does **not** re-scope or absorb EV-42; that is `steward`'s"; at close, EV-42's Intent premise is replaced and handoff notes named, but re-scoping is left to `steward`). **Skeptic O-6 `open-untested`** — explicitly "not runnable; needs a steward ruling, not a test." No test can settle who holds the pen on a sibling card's premise; it is a process-authority question. Both sides agree cardinality (A) re-scopes EV-42's premise; they disagree on whether EV-39's close edits EV-42's card. Routes to `product-owner` → `steward`.

2. **Denominator carrier — `owner` ("no `maxAttempts` in the manifest"; inject the init snapshot into the renderers) vs `principal` (persist `maxAttempts?` on the manifest at dispatch) vs `designer` (read `loadRetryConfig` at render time).** **Skeptic O-6b.** The testable third option (designer's render-time re-read) is **closed-red** by O-8 + principal's D3 — it is the second load D3 forbids, and O-8 confirms no carrier exists at either render site today. What remains is a choice between the two D3-compliant carriers: **inject the init snapshot into the renderers** (owner) vs **persist `maxAttempts?` on the manifest at dispatch** (principal). This is a values/tradeoff call (manifest-schema footprint vs a render-path dependency on the init snapshot), not something a test settles. It is a *blocking* open judgment — the Skeptic's blocking summary item (3) — but the blocking dimension (never re-read) is already settled; only the pick remains. Routes to `product-owner`.

3. **Countdown tick interval — 1s (cross-surface consistency with EV-40 R5's input-bar countdown) vs 2s (existing widget refresh, no new ticker).** Designer Q1, flagged "the one question I genuinely cannot close from this seat." Skeptic: "untestable pre-implementation — implementation gate, default 2s (existing widget refresh) unless ruled." No test can settle a tick granularity taste call. Routes to `product-owner` (designer surface), default 2s if no ruling.

4. **Principal's D4 — is EV-29's provider-reported figure being final-attempt-only acceptable as a *disclosed limitation*, or must it be fixed in this card?** The shape is settled (Skeptic closed-green on shape: one session path per manifest). The open question is whether EV-39 ships a disclosure ("reported figure is final-attempt-only") and defers the fix, or persists the attempt-session list / marks the reported row in-card. Principal named both options ("Either persist the attempt session list or mark the reported row as final-attempt-only; a silent partial reported figure falsifies the honesty invariant"). No test decides whether disclosure-vs-fix is the right posture for this card's scope; it is a scope/values call. Routes to `product-owner` → `steward`.

## Open objections — Skeptic objections whose settling test has not passed

1. **O-7 — byte-identity of a non-retry manifest and an `N=1` `formatReport`.** `open-untested` — the conditional-spread instrument does not exist yet; only a pre-implementation baseline was recorded (manifest keys today are exactly `id,seat,model,parentJobId,pid,sessionId,state,startedAt,settledAt,exitCode,usage,stopReason`; `formatReport` head today has no `attempt` substring). **Settling test:** T8/P1 snapshot — non-retry manifest string-equals the baseline; `N=1` `formatReport` string-equals baseline; EV-28's landed byte tests stay green. **Closes when:** the snapshot test passes post-implementation.

2. **O-5 `retrying`-half of the `tick()` claim.** `open-untested` — `tick()` skipping non-`running` is closed-green on code reading (`hub.ts:233`), but no live `retrying` state exists to exercise; the existing suite covers the stalled-kill path only. **Settling test:** a live backoff with the `retrying` state armed, asserting `tick()` `continue`s (no stall kill, no `killGroup` for a dead pid, no timeout flip) across the backoff window. **Closes when:** that test passes.

3. **`retrying`-state unit gates the Skeptic named (T7 / P21–P23).** Not run — pre-implementation. **Settling tests:** wait-blocks-through-backoff (`hub.wait([id], 60_000)` does not resolve during a backoff < 60s); cancel-disarms-timer (spawn count stays 1); no-stall-kill during backoff; one-row + `attempt N/M` rendered; gate-closed-at-`onChange` + written-once-with-cumulative-usage (the principal-prediction-4 falsifier: drive an error-then-succeed stub inside an open invocation window; assert flush outcome is `gate-closed` at attempt 1's `onChange` and `written` exactly once after final settle, with `subtree.cost` equal to both attempts' session spend — a `written` outcome mid-backoff is the under-count bug). **Closes when:** each passes.

4. **D4 live OpenRouter fetch half.** Shape closed-green (one session path per manifest, verified at code level); the live half — a real 2-attempt `openrouter/` dispatch yielding a provider job list containing one session path (the final attempt's) — is **open-untested** (network-gated, `COUNCIL_INTEGRATION=1`). **Settling test:** a 2-attempt openrouter dispatch asserting the reported figure is final-attempt-only (or, if open judgment 4 rules "fix in card," that the attempt-session list is persisted). **Closes when:** the integration test runs.

5. **O-6 (process half) — EV-42 premise correction.** `open-untested` — "not runnable; needs a steward ruling, not a test." This is the same item as Open judgment 1; carried here as the standing objection. **Settling test:** none possible. **Closes when:** `steward` rules who corrects EV-42's premise.

## Disposition

| Open item | Routes to |
|---|---|
| Open judgment 1 (who corrects EV-42's premise) | `product-owner` → `steward` |
| Open judgment 2 (denominator carrier: inject-snapshot vs persist-`maxAttempts?`) | `product-owner` (blocking; the "never re-read" constraint is already settled) |
| Open judgment 3 (countdown tick interval 1s vs 2s) | `product-owner`; default 2s if no ruling — otherwise implementation choice |
| Open judgment 4 (D4: disclose final-attempt-only vs fix in card) | `product-owner` → `steward` |
| Open objection 1 (O-7 byte-identity snapshots) | no ruling needed — implementation gate (T8/P1) |
| Open objection 2 (O-5 `retrying`-half of `tick()`) | no ruling needed — implementation gate |
| Open objection 3 (T7/P21–P23 `retrying`-state units + gate-closed/written-once) | no ruling needed — implementation gates |
| Open objection 4 (D4 live OpenRouter fetch) | no ruling needed — integration gate (`COUNCIL_INTEGRATION=1`) |
| Open objection 5 / Open judgment 1 (O-6 EV-42 premise) | `product-owner` → `steward` |

**Ready to hand off?** `no`. Two blocking open judgment calls must be ruled before implementation can lock a plan: **Open judgment 1** (who corrects EV-42's premise — `owner` vs `principal`, O-6 `open-untested`) and **Open judgment 2** (the denominator carrier — `owner`'s inject-snapshot vs `principal`'s persist-`maxAttempts?`, O-6b blocking; designer's render-time re-read is already closed-red). Open judgment 4 (D4 disclose-vs-fix) is not strictly blocking on the loop but blocks the honesty-invariant disclosure copy and should be ruled alongside. Open judgment 3 (tick interval) is non-blocking with a stated default. The remaining open objections are implementation/integration gates, not rulings.

### Step 6 — rulings returned and applied (resumed container)

Resumed container. The two rulings below were returned on the step-6
`ESCALATION` — `product-owner` (`job-20`) on Q1–Q4, then `steward` (`job-21`)
on the two items `product-owner` escalated — and are appended here
**verbatim**, per `features-deliver.md` Phase 2 and the escalation contract.
They are **binding** on every seat, `steward` included.

#### Ruling 1 — `product-owner` `job-20` (binding)

> **Ruling** — Q1: inject the init-time `RetryPolicy` snapshot into `CouncilTreeWidget` and `textTree`. Q2: escalate to `steward` (portfolio-level — re-scoping a `Ready` sibling). Q3: ratify the 2 s default. Q4: escalate to `steward` (permanent-residual vs scope-expansion). One acceptance amendment and one mandatory condition: whichever Q4 path lands, the silent partial is forbidden.
>
> **Options rejected** — Q1 persist `maxAttempts?` on `RunManifest` (encodes session-level state as per-dispatch, widens substrate EV-42 owns, harder to reverse than inject); Q1 render-time `loadRetryConfig(repoRoot)` (Skeptic O-8 `closed-green` + principal D3 — a second load is forbidden); Q2 rule it myself (changing what a `Ready` card is for is the steward's authority); Q4 fix in card silently (would re-scope EV-39 into EV-29 territory without a ruling).
>
> **Grounding** — Q1: `extensions/index.ts:588` is the sole `loadRetryConfig` call site; `extensions/navigator.ts:344` and `extensions/tree.ts:58-67` are the two render sites needing a parameter; `vault/wiki/hub-job-supervision.md` (battle-tested surfaces, additive-only convention); O-6b, O-8. Q3: `extensions/navigator.ts` widget refresh is 2 s. Q4: `extensions/usage-store.ts:426` calls `findSessionFile(input.repoRoot, p.runId, m.sessionId)` — one path per manifest (O-5 `closed-green` on shape); `vault/wiki/usage-accounting.md` (EPIC-7 honesty invariant).
>
> **Reversibility** — Q1 low (switching to persist later is +1 substrate field and dropping the renderer parameter); Q3 trivial; Q4 moderate.
>
> Five named gates ride into step 7: **G1** (O-7 byte-identity snapshots), **G2** (`retrying`-half of `tick()`), **G3** (`retrying`-state units + gate-closed/written-once cumulative usage), **G4** (D4 live OpenRouter fetch under `COUNCIL_INTEGRATION=1`), **G5** (EV-42 premise correction — closes only when `steward` rules). Internal dissent not adopted: `owner`'s timer-residence preference (Hub vs supervisor module) — arbitrated by the consolidator's settling test, deferred to an implementation choice under §7-minimality. Mandatory: the silent partial reported figure is forbidden; the `Acceptance` must carry a disclosure or a fix before merge. Full document: `vault/raw/2026-09-16-po-ev39-step6-ruling.md`.

#### Ruling 2 — `steward` `job-21` (binding)

> **Escalation 1 (Q2) — EV-42's premise.** (a) **Pen:** EV-39 records the mechanism it shipped **in its own card record** — one job id / one manifest / one row, `attempt` on the manifest, carried cumulative usage, per-attempt session ids with the `<id>-attemptN` suffix marked provisional — and **does not touch EV-42's card**. EV-42's premise text is goal-wording, which is the steward's. The edit lands as a between-cards board+card write executed by the orchestrator verbatim (already done). EV-39 may also not *absorb* EV-42's remaining work — no persisting the attempt-session pointers "while we're here". (b) **Disposition: re-scope in place, same id, same slot.** Keep-as-is rejected (false premise; its goal is already satisfied by EV-39's row label + carried cumulative usage). Retire rejected (a real residual remains: per-attempt attribution/provenance). Re-decompose rejected. **`attemptGroupId` is dropped**; `attempt` itself is fixed by the EV-40 §2.8 agreement and may not be renamed. (c) **Corrected EV-42 goal** (already applied): "Every attempt of a retried dispatch is recoverable from that dispatch's manifest alone — its session transcript and its spend — and the dispatch's provider-reported figure sums every attempt." G5 closes on this ruling.
>
> **Escalation 2 (Q4) — partial provider-reported figure.** **Disclose in EV-39** (Acceptance amendment) and **fix in the re-scoped EV-42** — not a fix in EV-39, not a separate card, not a permanent residual. The disclosure must be **machine-readable and durable**: a record-level reason on the provider sibling, in the existing record-only-reason pattern, rendered through the usage block's conditional legend. Prose-only copy is insufficient — a stored figure that looks whole a month later is the falsification. The final-attempt-only figure must never render as *the dispatch's* reported figure unqualified. **No change to EV-39's build-order position or the run-end condition**; G4's EV-39 form becomes "assert the disclosure is present, true, and persisted"; the wholeness assertion moves to EV-42.
>
> **Build order (explicit): unchanged.** EV-37 → EV-38 → EV-43 → EV-40 → **EV-39** → EV-42 → EV-41. No retirement, no early run end. G5 closed; G1–G3 remain implementation gates; G4 stays the network-gated integration gate in the form above.

#### Applied (facilitator, resumed container)

- **Q1 — applied (binding).** The init-time `RetryPolicy` snapshot is injected
  into `CouncilTreeWidget` (the renderer's `rowLine`/`settledCopy` seam,
  `extensions/navigator.ts`) and `textTree` (`extensions/tree.ts`). No
  `maxAttempts` field is persisted on `RunManifest`, and no `loadRetryConfig`
  call is added to a render path (that option is `closed-red` by O-8 + D3).
- **Q3 — applied (binding).** The 2 s countdown default is ratified; the
  existing widget refresh interval is the tick, no second ticker.
- **Q2 — resolved by `steward` (Escalation 1, Ruling 2).** EV-39 records the
  shipped mechanism in its **own** card record (below) and does **not** touch
  `council/cards/EV-42.md`; the orchestrator already re-scoped EV-42 in place.
  EV-39 does not absorb EV-42's remaining work.
- **Q4 — resolved by `steward` (Escalation 2, Ruling 2).** EV-39 carries a
  **machine-readable, durable** disclosure of the final-attempt-only
  provider-reported figure on the provider sibling (record-only-reason
  pattern, rendered through the usage block's conditional legend) — recorded
  in the `## Acceptance` section above; the wholeness fix is EV-42's.
- **G5 — closed** by the `steward` ruling's corrected EV-42 premise. **G1–G3**
  remain step-7/step-9 implementation gates; **G4**'s EV-39 form is now
  "assert the disclosure is present, true, and persisted," with the wholeness
  assertion moved to EV-42.

**Criterion 5 of the deterministic merge check now holds:** no `Needs Human`
state and no outstanding ruling on this card. The card proceeds to step 7.

### Mechanism correction recorded (steward Escalation 1)

Per the `steward` ruling, EV-39 records the retry mechanism it ships **in its
own card record** — not in EV-42's card, which the orchestrator re-scoped in
place:

- **One job id, one `RunManifest`, one job-tree row per dispatch.** The loop
  reuses the dispatch's id across attempts; `<id>.json` is overwritten per
  attempt; the tree renders one row per dispatch (R4). This is manifest
  cardinality **(A)** — the alternative (one manifest per attempt) was
  `closed-red` by the flush-gate test (O-1/O-5: every backoff would open the
  `forest-settle` flush gate and persist a permanently-short usage record).
- **`attempt` on the manifest** — `RunManifest.attempt?: number`, present only
  when `>= 2` so a non-retry manifest stays byte-identical to pre-EV-39. The
  name is fixed by the EV-40 §2.8 agreement and may not be renamed.
- **Carried cumulative usage** — `Hub.respawn` carries the prior attempt's
  full usage tuple into the new `Job`, so the single manifest holds the
  dispatch total and `sumSubtreeUsage` stays correct (EPIC-7 honesty
  invariant).
- **Per-attempt session ids** — each attempt spawns under a fresh session id
  `<id>-attemptN`; the suffix is **provisional** and EV-42 finalizes it. Fresh
  ids are mandatory (O-4 `closed-green`: reusing the id resumes the errored
  attempt's session with its errored turn in context). Prior-attempt
  transcripts survive in the run dir but are unlinked from the manifest, which
  is exactly the per-attempt provenance EV-42 closes.

EV-42's card is untouched by this container (its `goal` and `Note` already
carry the orchestrator's verbatim re-scope), and EV-39 does not persist
attempt-session pointers "while we're here" — that is EV-42's remaining work.

### Step 7 — spec written and handed to one owner

- **Spec:** `docs/superpowers/specs/2026-09-16-EV-39-design.md`. It is the
  write-up of steps 2–6 + both step-6 rulings, not a re-derivation; the rulings
  win on any conflict. Committed with this record.
- **Self-review before commit (facilitator):** no placeholder or unresolved
  `TODO` text; internally consistent with R4, the `product-owner` Q1/Q3/Q4
  rulings and the `steward` mechanism/Q4 ruling; the O-1/O-2/O-3 `closed-red`
  corrections (stable `startedAt` + attempt clock, clear `pid` with `exitCode`,
  no sidecar) and the O-4 fresh-session `closed-green` are pinned as mandatory
  gates; scope confined to the card's `goal` (EV-42 per-attempt provenance,
  EV-41's fixture, EV-40's parent loop, and `attemptGroupId` explicitly out of
  scope); the loop seam, cardinality, state clauses, carrier fields, renderer
  grammar, report shape, policy injection, and Q4 disclosure are pinned so an
  owner reading only the spec reaches one design.
- **Named gates carried into step 8/9:** **G1** byte-identity snapshots,
  **G2** the `retrying` half of `tick()`, **G3** `retrying`-state units +
  gate-closed/written-once cumulative usage, **G4** the Q4 disclosure (present,
  true, persisted; live OpenRouter fetch moves to EV-42), **G5** closed by the
  `steward` ruling. Mandatory corrections D1/D2 and fresh-session O-4 ride as
  D-section gates; denominator injection (D3/O-8) and malformed/disabled policy
  are additional named gates.
- **State:** `In Progress`; the single owner is dispatched at step 8.

### Step 8 — owner implementation, branch and PR (observed artifacts)

- **Branch:** `feat/ev-39-hub-retry`, PR **#55**
  (https://github.com/jumpseat-inc/pi-council/pull/55) against `main`.
  **Head SHA at `In Review`: `048844da8e795c6811e99bef392cb8b440adfbb8`**
  (authoritative: `gh pr view 55 --json headRefOid`; `state: OPEN`). Worktree
  `/home/tista/codes/pi-council/.worktrees/ev-39`. Criterion 2 observed by the
  facilitator: `gh pr checks 55 --json name,state,workflow` →
  `[{name: gates, workflow: gates, state: IN_PROGRESS}]` — the `gates`
  workflow is present and running; criterion 2 is adjudicated at step 11.
- **Dispatch history (three dispatches; facts, not a judgment):**
  - `job-22.1` — settled `done`/`stopReason=error` ("Upstream idle timeout
    exceeded") after 13.5 m / 26 turns with **no artifact landed** (clean
    worktree, no commits). Treated as a no-output settle; re-dispatched.
  - `job-22.2` — 45.1 m / 110 turns; landed 4 commits (plan, `STUB_MODE=flaky`,
    `RunManifest` fields, `extensions/job-retry.ts`) plus uncommitted
    `hub.ts`/`hub.test.ts`, then **hung** with no turn progress for ~45 m on a
    test that spawned a real `pi` session (an earlier `respawn` hardcoded
    `command: "pi"`). Informational `timeout`; cancelled per dispatch
    discipline; its own working tree already carried the `Hub.commands` fix.
  - `job-22.3` — resumed the same worktree, finished and pushed; settled
    `done`/`stopReason=stop`, 28.1 m / 112 turns.
  Stray `test/stub-child.ts` orphans from the hang were reaped by the
  facilitator before the resume.
- **Owner gate results (reported; step 9 re-runs independently at this head):**
  `bash council/preflight.sh EV-39` exit 0 (`PASS: preflight clean`);
  `bunx tsc --noEmit` exit 0; `bun test` via `timeout 900` → **819 pass / 2
  skip / 0 fail** (821 tests, 74 files, 56.5 s), exit 0;
  `python3 council/validate.py` → `All council artifacts valid`, exit 0.
  `COUNCIL_INTEGRATION=1` stays gated and unrun.
- **Owner-recorded deviations (facts for step 9, not a facilitator judgment):**
  1. **Attempt-counter double-count corrected by a red test.** The spec §2.2
     `respawn` pseudo-code and §2.3 `onSettle` both advanced `attempt`;
     combined they produced `attempt: 3` on a fail-once-then-succeed chain.
     The D1/O-4 failing test pinned the fix: `onSettle` advances the ordinal
     when arming (retrying manifest reads `attempt 2`), `respawn` carries it.
     This is an implementation detail inside a settled design, not a reopening.
  2. **Spec §2.7's exhausted-tail-drop branch is unreachable:** an empty
     `errorMessage` makes `classifyRetry` return `undefined`, so the table
     routes it to the terminal-settle sentence. The test pins the table.
- **State:** `In Review` written from the observed artifact (PR open). Step 9
  is dispatched at head `048844d`.

### Step 9 — skeptic verification (`job-22.4`) — **no blocks**

Subject: PR #55 head `048844da8e795c6811e99bef392cb8b440adfbb8`, worktree
`/home/tista/codes/pi-council/.worktrees/ev-39`. 5.2 m / 38 turns, settled
`done`. The Skeptic re-ran the full gate set and its own probes; the worktree
was clean and the branch unmutated throughout.

- **Gate set at the head:** `bash council/preflight.sh EV-39` → `OK` lines plus
  the known stale-by-construction `FAIL: local history does not descend from
  origin/main` (the FLLWUP-27 line recorded at step 1 — the branch cut predates
  the facilitator's record commits; not a product signal, never used to weaken
  a criterion); `bunx tsc --noEmit` exit 0; `timeout 900 bun test` → **819 pass
  / 2 skip / 0 fail** (821 tests, 74 files); `python3 council/validate.py` →
  `All council artifacts valid`. `COUNCIL_INTEGRATION=1` unrun per the card's
  gate set.
- **Objections — every one `closed-green`, none open:** D1 (stable
  `startedAt`, `elapsedMs` spans attempts, single-subtree count); D2
  (`exitCode: null` + dead pid absent from `.pids.json` during backoff); O-4
  (attempt-2 argv carries `<id>-attempt2`, manifest `sessionId` names the live
  attempt, `findSessionFile` resolves it); G1 (byte-identity snapshots +
  EV-28 tests green); G2 (`retrying` half of `tick()` — no stall kill, no
  `killGroup`, no timeout flip); G3 (wait resolves once post-final, never
  mid-backoff; cancel mid-backoff → 1 spawn + cleanup exactly once; one row +
  `attempt N/M` + `retrying in Ns`; **gate-closed at attempt-1 `onChange`,
  written once with cumulative cost**); G4 (`provider.partial ===
  "final-attempt-only"` persisted on disk and the qualifying legend renders;
  absent ⇒ byte-identical); denominator injection (sole `loadRetryConfig` at
  `index.ts:587`, none in a render path); malformed/disabled policy; cleanup
  across all four chain ends; both owner deviations (the ordinal is advanced in
  `onSettle` and carried by `respawn` — a fail-once chain reads `attempt: 2`,
  exhaustion reads `attempt: 3` from three real spawns, not coincidentally;
  the empty-`errorMessage` branch routes to the terminal sentence per the
  spec table).
- **Gate-integrity check:** the Skeptic injected a defect (commented out
  `job.pid = undefined` in `job-retry.ts`) → the D2 test went red and named it;
  restored byte-exact → green. The gates are decoration-free.
- **Verdict:** no open objections — does not block. The card stays `In Review`;
  step 10 judging may proceed.
