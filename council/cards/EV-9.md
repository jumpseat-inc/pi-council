---
id: EV-9
title: Open the selected subagent's progress from the inline tree
state: Done
owner: null
epic: EPIC-2
goal: Pressing enter on the selected row of the inline tree opens that subagent's live progress view with the same streaming transcript content the modal viewer shows today, and closing it returns focus to the tree with the selection preserved
---

## Intent

Today, enter on a selected job inside the modal opens TranscriptView:
a live, auto-following view of the seat's session transcript with
expand/collapse, thinking toggle, and follow mode. This card wires
that progress experience to the inline tree from EV-7 and EV-8: enter
on the highlighted row opens the selected subagent's progress, and the
driver comes back to the tree afterwards with the selection intact.

User-visible surface: the progress view itself. Open question for the
council and designer: whether it renders as the current modal overlay
(reusing TranscriptView wholesale, lowest risk) or inline as an
expansion of the tree panel's region (more consistent with the new
surface, more work). Either way, the content contract is parity with
the current modal viewer — same streaming blocks, same expand and
follow behavior — and the tree's last-activity rows keep updating
while progress is open. If the council keeps the modal, opening it
from the inline tree must not destroy the tree panel or its focus
state on close.

Depends on EV-7 and EV-8.

## Phase 1 rulings (binding, immutable for EPIC-2)

1. **Progress renders inline, as an expansion of the tree panel's region.**
   The selected subagent's live progress view (TranscriptView content
   contract: same streaming blocks, expand, thinking, follow) is rendered
   inline in the below-editor region as an expansion of the tree panel,
   not as the full-screen modal overlay. The tree panel and its rows stay
   visible above the progress expansion. The driver comes back to the
   tree with the selection intact. (Orchestrator Phase 1, 2026-08-26.)

## Acceptance

- Enter on a highlighted running job shows its transcript streaming live
  (user/assistant/tool blocks appear as the seat works).
- Closing the progress view returns to the inline tree with the same row
  selected; tree last-activity values kept updating while progress was open.
- Transcript parity with the current modal viewer is covered by tests on
  shared rendering code; `bun test` passes.

## Council deliberation

### Round 1 — independent first pass

**owner (job-9.1):** Mechanical extension of EV-8's surface machine. Enter stops opening the modal and flips `TreeFocusState.surface` to a new `"progress"` value; the below-editor widget renders tree rows (unchanged, top) + a flush-left `TranscriptView` child (no `withModalFrame`); escape routes back to `surface==="tree"` with `selectedSessionId` untouched. Because EV-8 makes `selectedSessionId` the sole sessionKeyed arbiter, selection preservation is nearly free — we must NOT call `exit()` (which nulls selection) on the way out of progress. Routing kernel stays pure; progress keys go through a new pure `classifyProgressKey(data)`; `CustomTreeEditor` consumes them and dispatches raw data into a `viewHost` reference the widget installs on the controller. `TranscriptView` reused verbatim → structural parity. Decisions: (1) three-valued surface not a sub-flag; (2) single widget stacked lines `[...treeLines, ...viewLines]`, not a second setWidget/overlay (belowEditor is one container per placement, grows with content, clips bottom); (3) view ownership in widget, key delivery through editor (pi widgets are render-only, Pattern 5; editor is always-focused OJ-1); (4) enter opens any selected row running or settled (gating would fork from modal); (5) viewport budget from `tui.terminal.rows` (same source as `termRows - 4`). Wiring delta small: `installTreeEditor`'s onSelect changes from `activateSession`(modal) to `controller.enterProgress(sid)`. Modal `openTranscript`/`activateSession` stay for legacy `/council-tree` modal path (OV-2, FLLWUP-4). Tradeoffs: typing while progress open lands in editor buffer (non-view keys forward, consistent with EV-8 T4); escaping disposes view + rebuilds fresh TranscriptTail on re-enter; two TranscriptTail instances poll same JSONL (independent byte-offset readers, fine). Testable claims T1-T6 (exitProgress preserves selection vs exit clearing; progress-key routing consumes+dispatches other keys forward; widget tree-above/transcript-below/marker; live-update independence; selection survives re-sort; no content-logic fork — existing TranscriptView tests stay green).

**principal, job-9b7:** Reframes: "progress" is not a view bolted onto the tree; it is a third value of `controller.surface` — a keyboard mode of the editor with its own consumed-key set and a tree-preserving exit. Esc-from-progress must restore `surface="tree"` WITHOUT calling `exit()` (which clears selection) — API change to `TreeFocusState`, not wiring detail. Pin two implicit contracts: (i) a BOUNDED inline viewport (not `termRows - 4`), (ii) transcript 1s poll runs independently of tree 2s refresh. Parity test lives at block-renderer level (same JSONL+width+viewportRows → byte-identical transcript lines, modal chrome excluded). Blind spots flagged: owner-data-view misses that inline widget never receives handleInput so `e/t/f/g/G` need a consumed entry in routeEditorFocus or they land in editor buffer; misses that `exit()` clears selection; misses cache-signature trap (`CouncilTreeWidget.render` memoizes on `sig=surface:selectedSessionId`, third surface value must fold in or transcript won't paint until unrelated invalidation — same EV-4 `CouncilTree.cached` bug class). Designer-render misses that below-editor region is one VStack with the conversation (unbounded expansion pushes conversation off-screen) and that the focus kernel's key classification is asserted by EV-8 tests as fixed 5-way split (progress makes it surface-parameterized). Testable TC-1..TC-6 (transition surface→tree w/ selection unchanged; in-progress e/t/f/g consumed; widget cache signature includes progress; block-renderer parity; cadence decoupled; OV-2 TUI-only reachability + modal path untouched).

**designer, (job-9b3):** The progress expansion is the same belowEditor widget component, extended: factory returns a Container whose first N children are the existing tree rows and last M+1 are a dim divider row + existing `TranscriptView.render(width)` lines. No second widget slot, no modal, no withModalFrame. Editor stays focused (EV-8 binding): progress focus = `controller.surface === "progress"`. Editor bottom-border mode label becomes `-- PROGRESS --` (mirror `-- TREE --`, EV-8 ruling 3). Evaluation gulf closed: tree rows above progress so operator sees which seat is watched (▌ row above), other seats still ticking, their seat settles (glyph flips ●→✓/✗ live). Storage: three signifiers (editor border label, tree ▌ row, progress divider) all read from `controller` on same TreeFocusState (single-source-of-truth). Constraint-driven vehicle: exactly two widget-region Maps, one factory-per-key → one factory renders both. Reuse-not-redesign: TranscriptView implements full content contract; EV-9 relocates it from modal wrapping into factory. Forward-unhandled inherited. Token-only drawing (AGENTS.md 9.6). FALSIFIABLE P1-P12 (line-count tree+divider+transcript; divider ▾ + seat + dim-only; content parity byte-for-byte width 96; esc→tree selection preserved and ▌ on same row; progress doesn't freeze 2s tree refresh; editor border label per surface; no setFocus; no ctx.ui.custom/withModalFrame in EV-9 change; no new theme tokens; token-only emission; height budget tree+divider+view fits within remaining (owner must pass viewportRows capped at floor(remaining/2) — split evenly); factory dispose clears timers + progressSessionId, reopen constructs fresh view). Comprehension C1-C4 settle only on a human's read (which seat am I watching — highlighted row above; what can I do — divider esc back + TranscriptView keymap; how do I leave — Esc named in divider; seat-above glyph-flip when another settles is load-bearing). Preferences: divider `▾ <seat> progress · esc back`; 50/50 height split, escalate to product-owner if smoke flags unreadable terminal; mode label -- PROGRESS -- (style binding, word taste). ESCALATION: surface type expansion from `editor|tree` to `|progress` is a type change to settled EV-8 type; designer argues for union; product-owner may rule binary-only → fallback parallel progressSessionId field.

### Round 2 — exchange

**owner (job-9.4):** Single owned number for viewport (resolves designer/principal: same constraint). Budget NOT termRows-4 (modal chrome; widget render(width) gets no height; over-return collapses the dock to minSize:0). Budget = `clamp(terminal.rows - editorReserve - messageReserve - footer, 8, 10)` captured ONCE in the widget factory where tui.terminal.rows is visible, never in render. Not hard 50/50 "to be safe": give tree `min(remaining/2, ROWS_MAX)` and transcript the rest. Enter-in-progress = explicit consumed no-op (TranscriptView has no Enter branch; unconsumed Enter could submit an unsent draft — real footgun). Esc in progress returns to tree WITHOUT exit() — a NEW controller method `back()` (set surface="tree", keep selectedSessionId), because exit() nulls it. UNION recommended over flag: surface is the documented sole arbiter; a parallel progressSessionId:null leaves reachable tree∧flag!=null (4th impossible state); discriminated unions make impossible states unrepresentable; every consumer of surface becomes &&progress-leaky under the flag (pure anti-rule). Flag fallback would work (~3 more invariants: no tree move while progress, no enter-exit, memo folds flag) but worse modeling. NEW 5th blind spot beyond principal's four: the render-memo sig also doesn't fold the transcript REVISION — a 1s append or f/t toggle produces no sig change, so widget returns canned tail and live transcript silently lags. Memo must fold surface+selected+transcriptRev (increment on poll/focus/expand) or cache fully via invalidate. Concrete tests: progress third surface consumed keys; | back() returns to tree preserving selectedId ("b"), exit() still nulls; progress render memo busted by transcript appends not just surface change.

**principal (job-9.5):** contract today = surface is a discriminated state, not boolean. Cache section: widget memo sig = surface:selectedSessionId so tree→progress repaints free; real cache seam is one layer DEEPER — sig doesn't change when new blocks arrive inside progress (surface+selectedId constant), and council widget's render is memoized on (w,sig). Fix = view.onChange → widget.invalidate() + requestRender. The modal has no such cache — openTranscript renders fresh every frame. So inline transcript freezes at first paint unless onChange wire; owner's "two tails fine" glosses this — tails fine (read-only/independent offsets), CACHE is the bug. Viewport: widget render receives only width, no rows; budget must capture at factory time; termRows-4 WRONG (modal chrome ≠ editor+ambient stack height); overflow belowEditor VStack 39, clips conversation off-screen. Blind spot flags: owner sees transition not memoization path ("first frame re-focus works, streaming needs view.onChange reach into widget.invalidate"); termRows-4 "same source" not same number. Designer: progressSessionId as stored widget field is second name for "which transcript am I watching" — controller already holds selectedSessionId; single source = surface==="progress" && AxProps!==null; stored parallel-flag = EV-8 anti-pattern (setFocus-derived rejection). "50/50 split, escalate to PO if unreadable" is NOT taste — it is the ONLY bound on a region that otherwise grows unbounded and clips conversation; must be named pinned number. (a) Agree floor(remaining/2) but "remaining" must be named: CHROME pinned constant, pure function progressViewportRows(termRows) pinned at 24/40. Ownership: designer owns the ratio; owner implements the fn; a test owns the exact value. Invariant: treeRows(≤ROWS_MAX)+1+viewport ≤ CHROME ROWS. (b) Enter consumed — side w/ designer, sharper reason: it's not "other", it's the tree's structural key (classifyTreeKey returns "enter"); in progress it has no meaning; forwarding would insert newline in draft while border reads -- PROGRESS -- (signifier contradiction, EV-8 ruling precedession), so it belongs in the progress HANDLED set as no-op; forward-unhandled preserved. (c) Union OK — additive monotonic; EV-9 Phase 1 ruling already supersedes modal-assumption part; mechanical encoding of binding ruling. Only catch: existing checks are 3 of tree==="tree" string comparisons, tsc won't exhaust that; union bites only if every === tree site gains a "progress" branch AND a test. "Number must never appear". (d) classifyTreeKey's 5-way split asserted as fixed (T7); progress key space differs — needs new classifyProgressKey, NOT surface-param rewrite of classifyTreeKey (generalizing it changes tree branch silently weakens T4); EV-8 test passes unmodified; new file; tree/editor branch byte-identical. Gate assertion: EV-9 diff touches ev8 test only additively or not at all. TC-1 tree-preserving exit; TC-2 cache doesn't freeze stream (sharpest); TC-3 cadence decoupled; TC-4 viewport pinned; TC-5 progress key routing; TC-6 EV-8 intact; TC-7 block-render parity; TC-8 mode label.

**designer (job-9.6):** REVISED viewport: drop 50/50 — tree is spatial anchor, gets EV-7 cap (≤11 rows); progress gets `max(3, availableRows - treeLinesCount - 1)` where availableRows = max(1, termRows - 5). Reaffirm Enter consumed no-op (title line + mode label publish handled set excluding Enter; forwarding to view.handleInput=silent drop or super= draft-submit surprise; documented no-op is best). Pin shared-VStack hard bound: factory render(width).length ≤ terminal.rows - 5 always; tree windowing wins over progress expansion at small budgets. Predictions indifferent to union-vs-rods (predicate "is progress open?" is boolean either way). Gulf: evg at Enter — tree above shows which seat watch / others tick / flip aka settle live; exec at close — Esc restores same tree state; tree-as-anchor preserves "back to same place". Preferences: divider ▾ <seat> progress · esc back; viewport floor 3 (preserve anchor), alternative suppress-hide at small budget if smoke flags; mode label -- PROGRESS -- (style binding word taste); NO glyph in divider (row above shows it); tree-windowing wins at small budgets, alternative suppress tree / transcript full budget. Predictions P1-P12 with P11 REVISED (render.length ≤ max(1,termRows-5); viewportRows = max(3, remaining-tree-1); tree wins) + P13 NEW (memo folds progress predicate) + P14 NEW (independent 1s/2s clocks) + P15 NEW (EV-8 kernel surface-param in NEW file, ev8 test unchanged, classifyKey preserved + new classifyProgressKey, TS exhaustiveness) + P16 NEW (operational: render(100).length ≤ operator on 14-row/500-block) + P17 NEW (selection survives re-sort during progress). Comprehension C1-C4 unchanged; C2 load-bearing (row above flips ●→✓/✗ when ANOTHER settles). Preferences de-escalated: height budget split no longer escalated; ONE PO escalation retained (surface type union vs flag-rods).

### Round 3 — final exchange (cap)

**owner (job-9.7):** Accepts designer's tree-as-anchor; ABANDONS own round-2 clamp (wrong axis). Final formula: `CHROME=5, MIN_PROGRESS=3, DIVIDER=1, ROWS_MAX=11`; `treeViewportRows = min(treeLines, ROWS_MAX, max(1, termRows-CHROME) - DIVIDER - MIN_PROGRESS)`; `progressViewportRows = max(1,termRows-CHROME) - treeViewportRows - DIVIDER`. Pinned: term=24/tree=11 → progress=7 (11+1+7=19=avail); term=40/tree=11 → progress=23; term=12 → tree 3 / progress 3. -MIN_PROGRESS wedge is the closure (naive treeLines overshoots at term<21). Union encoding confirmed: keep `"editor"|"tree"|"progress"`, enter-inside-progress = consumed no-op, escape→tree via new backFromProgress transition (never exit(); selection preserved). Render-memo: fold transcriptRev into widget render key (layout=[surface,treeRows,progressPredicate], content=[transcriptRev]); both must be in the key else cross-window goes stale.

**principal (job-9.8):** Concedes own R2 formula's arity and CHROME=4 (were modal-era), keeps only the discipline. Accepts tree-as-anchor. Named pair: `INLINE_CHROME=5, PROGRESS_DIV=1, PROGRESS_MIN=3`; `avail = max(1, termRows - CHROME)`; `treeRowsForProgress(termRows) = min(ROWS_MAX, avail - PROGRESS_DIV - PROGRESS_MIN)` (THE TREE FLEXES FIRST); `progressViewportRows(termRows, treeLines) = avail - treeLines - PROGRESS_DIV`. Invariant treeLines+DIV+progress ≤ termRows-CHROME with tree the one that yields. Pinned: progressViewportRows(24,10)=8, treeRowsForProgress(24)=9 (note: own formula yields 11 — pinned vector has value), treeRowsForProgress(14)≤5 + progressViewportRows(14,5)=3 (5+1+3=9=14-5). UNION AFFIRM: discriminated union is right; worth only as much as kernel is EXHAUSTIVE — the 3 surface==="tree" sites (routeEditorFocus, CustomTreeEditor.render, CouncilTreeWidget.render) each need a progress branch with a `never` default proving coverage, or progress silently falls through to forward-to-editor. Cache: HOLDS R2 — view.onChange → widget.invalidate()+requestRender, NO transcriptRev in sig (owner's in-progress counter is "a monotonic shadow of a dirty bit"; toggles live inside TranscriptView behind the onChange hook). Testables VP-1/VP-2, UN-1 (enter→progress consumes e/t/f/g/G/up/down/enter-noop/esc-back preserving selection, forwards "x"), UN-2 (add 4th surface → tsc fails at never default = compile-enforced exhaustiveness), CA-1 (append JSONL block + tick 1s poll → next render shows new block no sig change no 2nd factory call), CA-2 (toggle t shows thinking blocks — append-revision sig misses this, onChange passes free).

**designer (job-9.9):** FINAL single formula `computeWidgetLayout(termRows, treeContentLines) → {treeLines, progressViewport}`. Constants CHROME=5, SEPARATOR=1, TREE_FLOOR=3, PROGRESS_FLOOR=3, TREE_MIN_BUDGET=7, ROWS_MAX=11. Normal regime (avail = max(1,termRows-5) ≥ 7): treeLines = max(TREE_FLOOR, min(ROWS_MAX, treeContentLines, avail-SEPARATOR-PROGRESS_FLOOR)); progressViewport = max(PROGRESS_FLOOR, avail - treeLines - SEPARATOR). Tiny regime (avail < 7): split 50/50 with floor 1 each (half=max(1,floor(avail/2)), treeLines=half, progress=avail-half). Factory invariant render.length ≤ termRows-5. Pinned: (40,8)→(8,26); (24,8)→(8,10); (12,8)→(3,3); (10,5)→(2,3); (8,5)→(1,2). Properties: treeLines≥1 ∧ progress≥1 ∀ termRows≥6; treeLines+1+progress ≤ termRows-5; treeLines ≤ 11. C2 (glyph flip above editor) lives in chrome, NOT load-bearing for tree budget; tree floor-1 in tiny regime preserves Gulf of Evaluation independently. SURFACE-UNION SETTLED (confirmed, not reopened). 

**CONVERGENCE STATE (round 3):** All gates settled: three-valued surface union; inline progress = tree rows + stacked TranscriptView in single below-editor widget (no modal/withModalFrame); enter on any tree row → progress (running or settled); escape → back to surface==="tree" preserving selection via NEW transition (never exit()); TranscriptView reused verbatim; enter-inside-progress = consumed no-op; new classifyProgressKey + routeEditorFocus progress branch (exhaustive, never-default); EV-8 test file passes unmodified + new additive progress test file; cache sig folds progress predicate; view.onChange → widget.invalidate (principal holds; no row for transcriptRev in sig — timestamp on the hook); independent 1s transcript / 2s tree clocks both disposed; viewport = tree-as-anchor two-regime formula above. Only client-of-record disharmony: principal's pinned vector VP9 (treeRowsForProgress(24)=9) vs formula (11) — Skeptic owns exact value.

### Skeptic attacks (step 4, job-9.10) — 253 pass / 2 skip / 0 fail baseline

Objections & ACTUAL results:
1. Viewport formula tiny-regime underflow — probe termRows=6, treeContent=5 → progress=0, violates "progress ≥ 1 (tiny)" invariant and the ≤ avail bound (2>1). **closed-red** — formula has a hole at 5 ≤ termRows ≤ 6. Pinned (24→11,7),(40→11,23),(12→3,3),(8→1,2) all match.
2. `Surface` type / routing kernel / state machine lack "progress" — grep focus-nav.ts:15 `"editor" | "tree"`, forcing `surface="progress"` then routeEditorFocus(escape) → forward (not consumed). **closed-red** — the widening does not exist yet; every progress key-routing claim unimplementable without it. (Confirms the design's required widening, not a flaw in the design direction.)
3. `backFromProgress()` absent; `exit()` nulls selection. **closed-red** — method absent, exit() unconditionally nulls selectedSessionId; a naive exit()-based escape would destroy selection preservation. (Confirms the design's "new transition, never exit()" is load-bearing.)
4. EV-8 gate integrity — injected up/down move swap → 4 EV-8 failures. **closed-green** (gate fails on real defect). Open gap: Enter activator never verified (tests use no-op onActivate); T8 negative case (marker hidden when surface≠tree) not asserted. **open-untested** (add coverage).
5. `invalidate()` alone = stale tree rows — widget 1 job, render, add 2nd job, invalidate() only, render → 2nd job absent; refresh() → appears. **closed-red** — the design's "onChange→invalidate" cache path does NOT refresh the row list, so a new job appearing during progress leaves a stale list on escape-back until the 2s timer. NEEDS onChange→refresh (not just invalidate).
BONUS: TranscriptView expanded-state persists across render calls — **closed-green** (inline embed feasible).
What ran: read navigator.ts/focus-nav.ts/transcript.ts/tests/wiki; bun test ev7+ev8 (23 pass); bunx tsc --noEmit clean; wrote+ran test/ev9-skeptic-probes.test.ts (18 pass / 42 expects); gate-integrity injection swap up/down → EV-8 4 failures (restored); bun test full 253/2skip/0fail; probe file removed.
VERDICT: BLOCKS with 3 closed-red (viewport underflow ≤6, missing surface widening [=required implementation], stale tree-row cache path) + 1 open-untested (Enter activator not verified, T8 negative gap).

Design refinement required (route back to owner at step 9): (a) viewport formula must clamp progress ≥ 1 AND total ≤ avail in the tiny regime (termRows 5-6); (b) the cache path must call widget.refresh() (not just invalidate()) so the tree row list reflects structural changes during progress viewing.

### Consolidator synthesis (step 5, job-9.11)

SETTLED: same 10-point core (surface union widening, inline no-modal widget, enter-any-row→progress, backFromProgress-never-exit, enter consumed no-op, exhaustive classifyProgressKey + routeEditorFocus progress branch with EV-8 preserve, render cache folds progress, two independent clocks, tree-as-anchor viewport, token-only). Settled-by-test: O1 red (formula hole 5≤termRows≤6), O2 red (widening required impl), O3 red (exit() nulls — new-transition load-bearing), O5 red (invalidate() insufficient, need refresh()), O4-gate green (EV-8 gate fails on real defect), Bonus green (TranscriptView state persists). Surface-union-vs-flag settled (union).

OPEN JUDGMENT (→ product-owner, escalating to steward): Tiny-regime behavior at the mathematical floor. The agreed invariant `progress ≥ 1` AND `progress ≤ avail = termRows - 5` is UNSATISFIABLE for termRows ≤ 6 (O1's red test proves progress=0 there). No test can decide the UX when there is no room. Two live positions, equal weight:
  (a) Degrade gracefully / refuse-to-open below floor — Enter does not open progress (or a single-line overflow indicator), preserving treeLines ≥ 1, invariant non-negotiable.
  (b) Shrink the floor / admit a sub-1 progress band — compress CHROME/SEPARATOR or the tree floor so progress ≥ 1 in tiny regime; "Enter always opens something" non-negotiable.
  The minimum supported terminal height + which side bends is a VALUES CALL. O1's fix can't be written until ruled.

OPEN OBJECTIONS (no passing settling test): O1-fix (no green test for corrected formula at 5≤termRows≤6), O4 (Enter-activator wiring + T8-negative not asserted), O5-fix (refresh() path fresh-rows not verified), new-code-green blanket (classifyProgressKey kernel, backFromProgress(), two-clock dispose, viewport pins unchanged).
Hand-to-owner recommendation: rule tiny-regime first, then owner TDD-fails-first on the open objections.

### Open judgment routed to product-owner (step 6)

The consolidator sorted ONE open-judgment item requiring a ruling: the termRows ≤ 6 tiny-regime viewport behavior (O1-fix is blocked until ruled). Per escalation contract step 1, checked Phase 1 rulings (EV-9 R1 inline-bounded, EV-7 R1 last-activity→designer, OV-1 coexist, OV-2 RPC guard, PO EV-8 editor-driven) — NONE cover the tiny-regime floor. Not re-asked; routed per contract step 2 → ESCALATION to orchestrator. No recommendation carried. Card held in Deliberating pending the ruling (do not advance to step 7 spec write until ruled).

### Step 6 resolution — product-owner ruling (verbatim, binding on all seats including steward)

**Ruling (EV-9 viewport floor):** Choose **option (a), silent variant, with the minimum supported terminal height pinned at 7 rows**. At `termRows ≤ 6`, Enter on a highlighted tree row is consumed as a no-op: `controller.surface` stays `"tree"`, no progress viewport opens, no separator row, the tree rows remain visible (`treeLines ≥ 1`). Esc continues to do the right thing. At `termRows ≥ 7`, the existing two-regime formula runs as designed. Implemented as a single guard `if (termRows < 7) return;` at the top of the controller's `enterProgress(sid)`. Source document: `vault/raw/2026-08-26-po-ev9-tiny-regime-floor.md`.

**Options rejected:** (b) Shrink the floor / admit a sub-1 progress band — incoherent (progress is integer rows); compression breaks binding constraints (Phase 1 tree-rows-visible, platform chrome, separator). (a) with single-line overflow indicator variant — infeasible at termRows<=6 (treeLines≥1 + footer 1 = 2 > avail=1).

**Grounding:** invariant is non-negotiable (settled across three seats); O1 closed-red is a fact at termRows<=6. Phase 1 ruling forbids chrome/floor compression. Platform floor: navigator.ts:416,651 already enforces `termRows = Math.max(10, ...)` for the modal — an explicit refusal-to-render at degenerate heights; `7` is the natural breakpoint (designer's tiny-regime entry `avail < 7`).

**OUT OF SCOPE BUT FLAGGED (owner responsibility in the SPE-9 spec, NOT this ruling):** the formula's UPPER-bound invariant violation at `termRows ∈ {7..11}` — the pinned case `(8,5)→(1,2)` overflows `avail=3`, and `(12,8)→(3,3)`, `(40,11)` correctly. This was not surfaced by the consolidator. The owner MUST address this in the spec write so the whole formula is consistent, not just the lower bound this ruling fixes. Carry this explicitly into step 7.

**Reversibility:** Low. One guard + three tests.

---

**Step 7 (this run):** The owner MUST write the settled design spec at `docs/superpowers/specs/2026-08-26-EV-9-design.md` including the ruling's floor guard AND an addressed upper-bound fix for `termRows ∈ {7..11}`. See the facilitator log in EPIC-2 step-7 spec file.

### Step 8 — owner implementation (job-11.1)

Branch `feat/ev9-inline-progress`, PR #9 (open, head SHA `0f6f9ae6`). Worktree `.worktrees/ev9-inline-progress`, main untouched, board/card unmodified by owner. Changed 4 files: `extensions/focus-nav.ts` (surface union `editor|tree|progress`, `enterProgress(sid)` with floor guard `if (termRowsCap < 7) return false`, `backFromProgress()` preserving selection, `classifyProgressKey`, `computeProgressLayout` two-regime upper-bound-fixed), `extensions/navigator.ts` (`CouncilTreeWidget` stacked `[...treeRows, separator, ...viewLines]`, lazy live `TranscriptView`, `onChange → refresh()`, dispose clears both clocks), wiring. EV-7/EV-8 test files + `hub.ts` byte-identical. TDD fail-first for O1 floor, upper-bound pin `(8,5)→(1,1,1)`, O3 setProgress preserves, O4 ▌-hidden + real Enter, O5 refresh-vs-invalidate, classify exhaustiveness. Gates (owner's actual output): tsc clean, `bun test` 268 pass / 2 skip / 0 fail (253 baseline + 15 EV-9), `validate.py` clean. No boot gate claimed (TUI-only widget path, unit-covered). Status In Review set from observed artifact (PR open).

### Skeptic verify — cycle 1 (job-11.2), verdict BLOCKS

Ran full gates at branch head `0f6f9ae6`: `bunx tsc --noEmit` clean; `bun test` 268/2/0; `validate.py` clean. EV-8 test + `hub.ts` + `transcript.ts` + `classifyTreeKey` byte-identical; injected swap → 4 EV-8 failures, restore → 14/14 (gate integrity green). Probes: O1-lower (floor guard) closed-green; O1-upper bound (every termRows 7..11, tree+sep+progress ≤ avail, each ≥1) closed-green; O2 union widening closed-green (three sites handle progress; tsc exhaustive); O3 never-exit closed-green; O5 refresh-vs-invalidate closed-green; O4/principal cache folds surface closed-green; dual-clock dispose closed-green; parity closed-green; **O4 Enter-activator closed-red**: `CustomTreeEditor.handleInput` line ~314 calls `this.onActivate(...)` (→ `activateSession` → modal `openTranscript`), NOT `controller.enterProgress(sid)`. `enterProgress` is defined but never called from extension code — the inline progress surface is unreachable by any user-triggerable action. **This violates the card's primary acceptance criterion (Enter opens the inline progress view, not the modal).**

Red item handed to owner (exact): change `focus-nav.ts:314` from `if (key === "enter") this.onActivate(...)` to `if (key === "enter") this.controller.enterProgress(this.controller.selectedSessionId ?? "");` and add a test proving Enter in tree mode transitions `controller.surface` to `"progress"` (no existing test covers this integration point). Everything else closed-green.

### Owner fix (job-11.3) — verify cycle 1 resolved

`focus-nav.ts:314` now calls `this.controller.enterProgress(this.controller.selectedSessionId ?? "")`; modal `onActivate`/`activateSession`/`openTranscript` preserved for FLLWUP-4/legacy. New integration test (Enter from `surface==="tree"` → `"progress"`, selection preserved) + tiny-regime no-op test, additive to `test/ev9-progress.test.ts` (EV-8 unmodified). Gates: tsc clean, `bun test` 270/2/0, validate.py clean. PR #9 head → `e3866501`.

### Skeptic verify — cycle 2 (job-11.4), verdict PASS

Full gates at head `e3866501`: tsc clean; `bun test` 270/2/0 (1244 expect); validate.py clean. All 10 objections closed-green: O4 Enter-activator now green (integration test passes, focus-nav.ts:314 calls `enterProgress`, `onActivate` never invoked from handleInput, modal off-ramp fully removed from the Enter path); O1-lower floor guard; O1-upper (every termRows 7..11, (8,5)→(1,1,1), normal pins); O2 union widening (tsc exhaustive); O3 never-exit; O4 cache; O5 refresh; dual-clock dispose; parity; EV-8 gate integrity (file byte-identical SHA 76d9899, injected regression fails, restore green). PASS — no open objections.

### Step 10 — judge (job-12), verdict PASS

Judge input was strictly the card goal + recorded Skeptic evidence (cycle 2), per council.md step 10. Verdict **PASS**: O4 Enter-activator integration test proves a real Enter on a tree row opens inline progress via `enterProgress` (not the modal); transcript parity test confirms shared streaming content with the modal viewer; O3 `backFromProgress` via escape returns to `surface === "tree"` with selection preserved because `exit()` (which nulls `selectedSessionId`) is never called; O1 floor guard + upper-bound pins verified; EV-8 gate integrity (file byte-identical, injected regression fails then restores green). No goal-coverage gap.

### Step 11 — deterministic merge gate (pre-authorized autonomously)

Five criteria checked at head `e3866501` (PR #9): (1) owner gates green in full (tsc clean, bun test 270/2/0, validate.py clean); (2) GitHub Actions `gates` workflow SUCCESS at head SHA; (3) no blocking Skeptic objection (cycle 2 PASS, all 10 closed-green); (4) judge PASS (step 10); (5) no Needs Human / outstanding ruling. Merged with `gh pr merge 9 --match-head-commit e3866501`; merged SHA `3582a14`, CI green on merged SHA (gates workflow, on main, success).

### Step 12 — sync and reconcile

origin/main advanced to the merge `3582a14`. Local record commits for steps 8-9 lived on a divergent local branch (the facilitator's post-PR record writes were local-only); the merged tree carries the deliberation + spec but not those records. Reconciliation: `main` reset onto `origin/main` (`3582a14`), and this card record + board were rebuilt to carry the full record — step-8 owner, step-9 cycles 1-2, step-10 judge, step-11 merge — with the card set `Done` from the observed artifact (merged + CI green on merged SHA), not from any seat's report. validate.py clean.

## Done (observed artifact)
EV-9: PR #9 merged `3582a14`, gates SUCCESS on merged SHA. Card `Done`.
