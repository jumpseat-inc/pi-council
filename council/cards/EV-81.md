---
id: EV-81
title: Reuse the gate's Jev transport with a human-bound fail-closed posture
state: Done
owner: null
epic: EPIC-10
goal: The follow-up decision calls the pinned `typesafe/jev-1.13` decisions endpoint and, on any refusal, timeout, non-2xx, unparseable body, or unresolved credential, records `gate call failed: <reason>` and falls back to the human pre-write confirm gate while producing zero `Drop` and zero automatic `Merge` dispositions, proven by an unreachable-endpoint fixture.
---

## Intent

The follow-up decision is not worth shipping without a safe failure. This card
reuses the card gate's pinned model, endpoint, and fail-closed transport, and
pins the failure posture at the module boundary: any refusal, timeout, non-2xx,
unparseable body, or unresolved credential records a `gate call failed:` basis
and falls back to the human confirm gate — never dropping a candidate, never
auto-merging one, never writing before confirmation.

## Acceptance

- The call uses `GATE_PINNED_MODEL` and `GATE_ENDPOINT` (never a
  chat/completions path) — one pin, one endpoint, asserted by a test importing
  the constants rather than re-declaring literals.
- An unreachable-endpoint fixture asserts the posture at the module boundary —
  the recorded `gate call failed: <reason>` basis, exactly zero `Drop`
  dispositions, no automatic `Merge`, and the human-confirm fallback — all
  provable offline today; the end-to-end "reaches step 13" arm belongs to
  EV-84's third arm.
- No pathology of the call produces an automatic `Merge` or any write before
  confirmation.
- Availability/credential resolution reuses the resolver the run-start
  preflight already uses; under `gate.mode` on with no credential the run fails
  loud at run start exactly as the card gate does (the FLLWUP-89–92 class, not
  a fork).
- The repo's typecheck, the full test suite, `python3 council/validate.py`, and
  the repo's preflight pass.

## Deliberation record

**Routing (step 1):** recorded mode `Deliberate` (fallback — run input carries the
recorded routing; `council_route` is a parent-only tool not exposed in this container,
per the EV-79 step-9 precedent). Surface-touching bit: **not** surface-touching —
EV-81 ships a transport/orchestration module + offline fixtures, no visible surface in
its own diff; the failure fallback's render (the human pre-write confirm arm,
unavailable-state lines) is EV-82's surface, and the exact copy (`gate call failed:
<reason>`) is already pinned verbatim by the intake R0 ruling and EV-82's card face —
so `designer` is not seated.

### Round 1 — owner (first pass, job-9.1)

**Module placement: new sibling `extensions/gate-followup-run.ts` exporting
`runFollowupGate(state: FollowupState, questions, decisionPolicy, opts)` — a mirror of
`runGate`, not a parameterization of it.** `runGate` is hard-coupled to the card
vocabulary in three places (`decide`, the `Deliberate` fallback via `MODE_PANELS`,
`GateDecisionPolicy`); generalizing means threading a decide-fn and a fallback factory
through the battle-tested orchestrator; EV-80 already set the no-shared-core precedent.
Reuse is of the TRANSPORT SEAM (`GateTransport`, `toWireQuestions`,
`parseDecisionsResponse`, `GATE_REFUSAL_MARKER`) — imported verbatim. Doc consequence:
gate-transport.ts's "gate-run.ts is the only runtime consumer" header claim becomes
false; amend in the same change.

**Return shape** — `FollowupRunResult` mirrors `GateRunResult` field-for-field:
`{ status, decision: FollowupDecision, callId, failure?: {kind, reason},
reportedModel?, modelDrift? }`. On any refusal/timeout/non-2xx/unparseable/unresolved-
credential: `{ status: "failed", decision: { disposition: "File", basis: "gate call
failed: <reason>" }, callId, failure }`. `File` IS the human-confirm fallback (per
decideFollowup's fail-safe design); no separate fallback field, no new vocabulary. No
`basisSuffix` opt (EV-69 ratchet note is a card-gate concept, no followup analogue).

**Ledger posture: the orchestration appends — never the transport.** Exactly one call
line per call, both arms: failure line = `answers: {}` (appendGateCall fills every
asked id null), `resolvedMode: "File"` (the disposition string verbatim, as runGate
records the verdict), `failureClass` from the same taxonomy
(`no-api-key|timeout|network|http-<status>|http-400-refusal|invalid-response|internal-error`),
basis exactly `"gate call failed: " + <reason>`, `drops = state.drops` (EV-80's
assignability pin). Success line = knownIds filter + `unknownAnswerIds`, answers
verbatim, model/provider/usage/generationId, `resolvedMode` = disposition, advisory
mirrored. Routing safety: `gate-route.ts:231` rejects non-`GATE_DECISION_MODES`
resolvedMode → followup lines can never produce a reduced mode; followup stateHash
cannot collide with a card's (different pack loop).

**Pre-POST guards mirrored verbatim (all throwing, zero POSTs, zero ledger lines):**
mode off → throw programming error (R1: same resolver, `loadGateConfig`, feeds both);
model pin; endpoint never-path; noul join check (live here — packaged followup data is
two noul + one choice). Credential: `opts.apiKey !== undefined ? opts.apiKey :
resolveOpenRouterApiKey()` — the identical expression preflight.ts runs, runtime parity
by construction; the no-api-key `failRun` stays as defense-in-depth; the run-start loud
arm is EV-76's, already shipped, no second preflight forked.

**Probability-range residual — pick: fix the SHARED `sideProbability` helper; the
step-13 re-card trigger does not fire.** Choice branch: a finite value outside [0,1]
THROWS naming the id; absent/non-numeric still contributes 0 (missing evidence drags
toward the safe side — must not become an error or the gate's unanswered posture
breaks). Grounds: (a) bullet 3 says "no pathology of the call" — a parse-site patch
leaves the pathology one call-site away; (b) the throw lands in both domains'
already-fail-closed catch-alls (followup: decideFollowup throw → invalid-response →
File/gate-call-failed basis; card gate: decide() throw → invalid-response →
Deliberate); (c) the helper fix also closes the shipped card-gate arm, where out-of-
range probs could today push a composite toward Direct — the dangerous direction.
Explicitly: **helper, no re-card.**

**Offline fixture:** injected transport doubles only; test imports
`GATE_PINNED_MODEL`/`GATE_ENDPOINT` and asserts captured request `url ===
GATE_ENDPOINT`, `body.model === GATE_PINNED_MODEL`, plus the forbidden-path guard
throwing on a mutated endpoint. Variants (each asserting full posture: status failed,
disposition File, basis exactly `gate call failed: <reason>`, exactly one ledger line
resolvedMode "File", failureClass, every asked id null, zero Drop/Merge anywhere):
network, timeout, http-500, http-400-refusal (body with `GATE_REFUSAL_MARKER`,
imported), 2xx garbage → invalid-response, explicit `apiKey: null` → no-api-key,
out-of-range probability → invalid-response → File. State = minimal synthetic
`FollowupState` literal (runner needs only stateHash/stateBytes/drops); no fs, no
timers in the orchestration.

**Test plan:** (1) `test/ev81-followup-run.test.ts` — fixture matrix; guards throw with
zero POSTs and ledger absent; success path over packaged policy (alreadyDone?yes →
resolvedMode "Drop" line, duplicate?yes → "Merge", else "File"); unknownAnswerIds;
unanswered → nulls in record, absent from decideFollowup's input. (2) Range residual
RED-AT-BASE: `decideFollowup` with weighted choice `probabilities{no:1.5}` → composite
2.3 ≥ 2.0 → Drop today (observed red at base); after fix throws → orchestration returns
File via invalid-response. (3) Card-gate side of the helper fix: out-of-range probs →
decide throws → runGate records invalid-response → Deliberate (was: possibly Direct)
— honest accounting of blast radius, not an extra feature. (4) Doc-claim updates:
gate-transport single-consumer comment; EV-80 assignability test gains a real consumer.

**Tradeoffs:** ~140 mirrored lines over generalizing runGate; helper fix widens change
into the card gate (strictly toward the safe side, own red-at-base test); `File` does
double duty (composite else-arm + failure fallback) — vocabulary fixed at three values
(R2), failure {class} + basis distinguishes computed-File from failed-call-File, same
discrimination the card gate makes for Deliberate.

**Open points for the principal:** (1) `policyVersion` on the followup line —
`runGate` passes `policy.policyVersion` (the gate tuning policy's version,
`gate-policy-1`), not `decisionPolicy.version`; re-derivation keys on policyVersion, and
the policy that decides the disposition is `followup-decision-1` — owner leans
`decisionPolicy.version` but that is a deliberate divergence from runGate's literal
choice; principal should rule. (2) Advisory posture — assumed: disposition computed and
recorded with `advisory: true`, EV-82 renders as advisory (EV-66 intake semantics);
confirm. (3) Card-gate blast radius of the helper fix — owner judges it inside the
fold-in ruling; if the principal prefers zero card-gate delta, the fallback is
parse-site-only plus the step-13 re-card; owner recommends against. (4) Transport header
amendment — one-way import edge (gate.ts/gate-ledger.ts never import the transport)
survives with two consumers; rewrite the comment, don't leave it stale.

usage (job-9.1): 9.3m/10 turns, cost ≈ $0.0142.

### Round 1 — principal (first pass, job-9.2)

**Contract reading.** `runGate` is a card-domain orchestrator (guards → credential →
wire shaping → one transport call → failRun or light parse → decide() → one
appendGateCall); "reuse the transport" is satisfied by importing the transport seam;
the ORCHESTRATION is what EV-81 mirrors and is not exported as reusable pieces.
`decisionLine` is generic over `{resolvedMode, basis}` → renders `Mode: <disposition> —
<basis>` for EV-82 unchanged. `FollowupState.drops` already pinned assignable to
`GateDropRecord[]`.

**Load-bearing points the card does not spell out:**
1. **The fail-safe target differs from runGate's — failure decision must be a LITERAL
   `{disposition:"File"}`, never routed through decideFollowup, never touching
   MODE_PANELS.** "Zero Drop, no automatic Merge" only holds if the failure constructor
   is File by construction. (Agrees with owner's literal-File pick.)
2. **`failure.kind` must ride the RETURNED result, not only the basis.** EV-82's
   unavailable-state bullet lists `gate call failed: <reason>` AND `credential
   unresolved` as distinct literals; with only a `FollowupDecision` EV-82 would have to
   string-match the basis. Export `FollowupRunResult { status, decision, callId,
   failure?:{kind,reason} }` mirroring `GateRunResult`; EV-83's `ESCALATION` needs the
   same kind+reason.
3. **The ledger line's `policyVersion` semantics are already broken in the shipped card
   gate; EV-81 must not inherit the bug.** `runGate` writes `policyVersion:
   policy.policyVersion` ("gate-policy-1") but `resolveRoute` validates
   `record.policyVersion !== decisionPolicy.version` ("gate-decision-1",
   gate-route.ts:219) — on real data the two never match, so every live card-gate line
   is classified as policy drift and routes full Deliberate; the recorded-decision fast
   path EV-69 exists to provide is dead. Tests miss it because `appendRecorded`
   fabricates lines with `decisionPolicy.version` (test/gate-route.test.ts:142), not
   runGate's actual output. For EV-81 the field must carry the FOLLOWUP DECISION
   policy's version (`followup-decision-1`) and `questionSetVersion` the followup set's.
   The shipped defect is not EV-81's to fix, but it must be named and carded; mirroring
   `policy.policyVersion` would replicate it.
4. **No line domain discriminator exists.** Both domains write kind:"call",
   schemaVersion 2 to the same gate-ledger.jsonl with `resolvedMode` an open string.
   Shipped consumers inert (resolveRoute filters by stateHash first + rejects
   non-GATE_DECISION_MODES; decisionLine generic; gate-render joins by callId). EV-84
   must identify followup lines. Options: (a) vocabulary disjointness pinned by a test
   using the exported constants (principal LEANS (a) — the ledger deliberately keeps
   resolvedMode a string; no shipped consumer needs a tag); (b) additive optional
   `domain` field. Card should decide explicitly.
5. **Off-mode ownership:** `buildFollowupState`'s own comment assigns the off
   short-circuit to "the EV-81/82 call site … before the state build";
   `runFollowupGate` throws loudly on off (mirroring runGate); short-circuit lives in
   the caller BEFORE buildFollowupState (else an off policy with no budget key throws
   from the packer). EV-82's "off leaves the prose review unqualified" depends on this
   ordering.
6. **Module placement: COLOCATE `runFollowupGate` in gate-run.ts** (opposite the
   owner): `transportFailure`/`FailureShape` are private in gate-run.ts; exporting them
   widens the surface EV-82/83's reason grammar depends on; a private duplicate is the
   drift the taxonomy fears. Smallest import delta; type-only import of `FollowupState`
   from followup-state.ts (avoids pulling followup-state→gate-route→runs.ts'
   child_process edge into runtime). If the owner insists on domain separation, export
   the failure taxonomy to one shared home rather than copy it.
7. **Range residual:** agrees — fix the SHARED `sideProbability` choice branch to
   reject [0,1], closing both arms and discharging the PO's re-card trigger; parse-site-
   only leaves the shipped decide() arm accepting out-of-range probabilities = the named
   NEW card at step 13. Cost named: the thrown error is `gate:`-prefixed inside the
   followup domain, weakening EV-79's `followup:`-prefixed grammar for that one arm —
   acceptable (EV-82/83 surface the invalid-response class at the call boundary, not
   the prefix). Owner must record which seam was chosen so the re-card trigger's status
   is unambiguous.
8. **EV-84 wiring (build nothing):** injectable transport; repoRoot+ledgerPath so the
   fixture's line lands in a tmpdir; result carrying status:"failed" + failure.kind +
   decision.disposition==="File"; appends exactly one line per call on failure, writes
   no card (EV-82's pin). EV-84's off arm depends on point 5; unreachable arm on 1–2.
9. **FLLWUP-96's composition site — mis-assignment risk:** if EV-81 mirrors runGate's
   "loads nothing itself" posture, the site holding BOTH questions and decisionPolicy
   is the tool layer EV-82/83 builds, not EV-81's module. Either EV-81 exports the
   composition entry point that loads+composes both files, or FLLWUP-96's promotion
   blocker is mis-assigned. The card should name which.

**Blind spots named:** transport/pipeline view can't see two consumers with
incompatible vocabularies or the policyVersion writer/reader disagreement; render view
(EV-82) assumes it can recover the failure KIND from the decision; routing view assumes
lines were written by runGate (its discriminator is vocabulary inference, not a
contract); decision view (EV-79) is pure and cannot close the range residual.

**Testable claims:** (1) injected timeout transport → status failed, File,
failure.kind "timeout", exactly one line resolvedMode "File", failure.class "timeout",
basis `gate call failed: timeout after <n>ms`; any Drop/Merge on injected failure
fails. (2) followup-path appendGateCall writes `policyVersion ===
"followup-decision-1"` (asserted against loadFollowupDecision(repo).version). (3)
`sideProbability` throws on choice probs 1.5 and −0.1; decide() returns File (or the
caller's invalid-response catch does), never Drop; frozen shipped gate-decide
expectations for absent/non-numeric probabilities byte-identical. (4) resolveRoute over
a fixture with a followup line + a card line returns the card line's result,
unaffected — pinning vocabulary disjointness (`GATE_DECISION_MODES` vs
`FOLLOWUP_DISPOSITIONS`). (5) off → throws before any POST, zero ledger lines; the
call-site short-circuit prevents buildFollowupState's absent-budget throw.

usage (job-9.2): 9.3m/17 turns, cost ≈ $0.0355.

### Round 2 — owner (job-9.3) and principal (job-9.5)

**Placement — owner CONCEDED; principal accepted: `runFollowupGate` colocated in
`gate-run.ts`, fenced section, zero new exports, `import type { FollowupState }`**
(type-only import is neutral between the options — erased either way; the zero-export
delta is the real decider). Growth precedent: EV-79 accepted gate.ts growth;
gate-run.ts is ~250 lines and the same class of contract-bearing orchestrator. Principal
nit: `runGate` keeps `policy`/`decisionPolicy` inside `RunGateOpts`; the followup
signature lifts them positional — two call conventions for two sibling orchestrators;
if positional, say so in the header. Principal also notes: type `questions` as the
followup question set, not `GateQuestionSet`.

**`policyVersion` — the principal's shipped-defect claim VERIFIED by both seats on all
four legs:** (writer) runGate writes `policy.policyVersion` on both arms (gate-run.ts
:198, :268); (data) packaged `policy.json` = "gate-policy-1" ≠ `decision.json`
"gate-decision-1"; (reader) `resolveRoute` (gate-route.ts:219) classifies mismatch as
`policyDrift` → routes full; (live) every line in `.pi/council/gate-ledger.jsonl`
carries "gate-policy-1" — **every live card-gate line is classified as policy drift;
EV-69's recorded-decision fast path is dead in production**; (fixture)
test/gate-route.test.ts:142 fabricates lines with `decisionPolicy.version`, which is
why the suite misses it. Ruling (both seats converge): the writer is the buggy side —
a recorded decision is a function of (answers, decisionPolicy); `runFollowupGate`
writes `policyVersion = decisionPolicy.version` ("followup-decision-1") and
`questionSetVersion = questions.version` ("followup-questions-1") — a DELIBERATE
divergence from runGate's literal; the shipped runGate writer fix is **carded at step
13** (out of EV-81's scope by the fold-in test), with the honest blast-radius note:
existing mismatched lines keep routing full (safe direction); only new lines regain the
fast path.

**Domain discriminator — BOTH pick (a): vocabulary disjointness pinned by test**
(`GATE_DECISION_MODES ∩ FOLLOWUP_DISPOSITIONS = ∅` asserted by importing both
constants, plus `resolveRoute` over a mixed followup+card fixture returns the card
line's result unchanged); NO `domain` field — fully redundant state, derivable from
`resolvedMode` under disjointness, and a contradictable redundant field is worse; EV-71
precedent (a field needs a named consumer). No gate-ledger.ts change.

**FLLWUP-96 — no composition entry point from EV-81; the blocker note is corrected at
step 13.** runGate's "loads nothing itself" posture mirrored: the first site holding
both followup files is the EV-82/83 tool layer; a load+compose helper fails the fold-in
test. Principal residual for FLLWUP-96's own promotion: if EV-82 and EV-83 EACH compose
the pair, the "one site that legitimately holds both" assumption needs re-examination
there.

**Advisory + off — confirmed both seats:** signature corrected to
`runFollowupGate(state, questions, policy: GatePolicy, decisionPolicy, opts)` (mode
read from the shared gate policy); `advisory = policy.mode === "advisory"` recorded on
the line; throws on `mode === "off"` (mirrored runGate); the off short-circuit lives in
the CALLER before `buildFollowupState` (followup-state.ts's own comment assigns it
there; an off policy may omit the budget key).

**Range residual — shared-helper fix, final:** `sideProbability`'s choice branch throws
on a finite value outside [0,1] (names id, option, value, [0,1]); absent/non-numeric
still contributes 0 (frozen gate-decide expectations byte-identical). **Grammar
amendment (principal, round 2):** the new choice-branch throw is brand-new → write the
message DOMAIN-NEUTRAL from birth (neither `gate:` nor `followup:` prefixed) — the
reason rides verbatim into `gate call failed: <reason>` which EV-82 renders. AND the
principal surfaced the live leak: the helper's EXISTING noul-branch throw is
`gate:`-prefixed and already reachable from `decideFollowup` — the live ledger shows
`basis: "gate call failed: gate: decide — answer …"`, so the followup reason grammar is
already polluted for the noul arm. Do NOT change the noul message in EV-81 (it would
change pinned card-gate basis strings); RECORD it as a step-13 residual. Re-card
trigger status: **discharged — shared-helper fix, no step-13 re-card.**

**Step-13 residuals accumulated this round (for step 13, not this card's diff):**
(a) the shipped runGate `policyVersion` writer bug (with a route-a-real-runGate-line
test, not a fabricated one); (b) the noul-branch `gate:`-prefix leak into the followup
reason grammar; (c) owner's data-quality flag: every real card-gate call in the live
ledger to date died at decide() with "missing a usable probability" → always routes
full Deliberate (model systematically omitting probabilities is a token-spending
quality observation, outside EV-81).

**Converged in round 2 (both final positions align):** colocated
`runFollowupGate(state, questions, policy, decisionPolicy, opts)` in gate-run.ts
exporting `FollowupRunResult { status, decision: FollowupDecision, callId,
failure?: {kind, reason}, reportedModel?, modelDrift? }` mirroring `GateRunResult`;
failure decision is a LITERAL `{disposition: "File", basis: "gate call failed:
<reason>"}` — never routed through decideFollowup, never touching MODE_PANELS; no
`basisSuffix` opt; one call line per call appended by the orchestration (failure:
answers {}, every asked id null, resolvedMode "File", failureClass from the shared
taxonomy, basis exact; success: knownIds filter + unknownAnswerIds, answers verbatim,
model/provider/usage/generationId, resolvedMode = disposition verbatim); guards
mirrored (off/model-pin/endpoint-never-path/noul-join, all pre-POST throwing, zero
lines); credential expression identical to preflight (`opts.apiKey !== undefined ?
opts.apiKey : resolveOpenRouterApiKey()`), no-api-key failRun as defense-in-depth, the
run-start loud arm is EV-76's (no fork); discriminator (a); range residual via the
shared helper with domain-neutral message; FLLWUP-96 blocker note corrected at step 13.

**Open after round 2 (narrow):** (1) positional-vs-opts signature shape — a nit the
implementing owner settles in the spec/header (principal: if positional, say so in the
header); (2) the noul-prefix residual recording — principal recommends recorded; no
dispute (owner's round-2 record already lists it). Nothing left that needs a third
round.

usage: owner job-9.3 15.1m/10 turns ≈ $0.0136; principal job-9.4 timed out at the
window, cancelled, re-dispatched once → job-9.5 8.8m/11 turns ≈ $0.0164.

### Step 4 — skeptic attack (job-9.6)

Baseline: `bunx tsc --noEmit` exit 0; targeted gate-suite `bun test` 218 pass / 0 fail
(797 expect); `python3 council/validate.py` clean. Repo tree byte-identical after
probes (probes under /tmp/ev81-probe/; pre-existing untracked vault/raw files only).
Wiki gap noted: no EPIC-10 followup page exists yet — grounding was shipped code +
live data.

**Settled by probes (all with raw output, /tmp/ev81-probe/):**
- **O1 closed-green — policyVersion defect, all four legs verified end-to-end.** Writer
  gate-run.ts:198/:268; packaged "gate-policy-1" ≠ "gate-decision-1"; live ledger 8/8
  lines "gate-policy-1"; reader gate-route.ts:219 → policyDrift → routes full; fixture
  test/gate-route.test.ts:142 fabricates the matching version. Probe1 through the REAL
  `resolveRoute`: a runGate-written line routes full Deliberate with the
  policyVersion-mismatch basis; the same line with the decision policy's version routes
  `source:"recorded"` → Direct. **EV-69's recorded-decision fast path is dead in
  production** (today outcome-harmless: 8/8 live lines are failed-Deliberate, full
  routing agrees — the safe direction).
- **O2 closed-green — range residual live in the followup domain.** decideFollowup,
  packaged policy, choice `probabilities{no:1.5}` → **`{Drop, "composite 2.30 ≥ drop
  threshold 2.00"}`** (contrast `no:1` → Merge). The card's red-at-base premise
  replicates.
- **O3 closed-green — range residual live in the card gate.** Shipped decide(),
  probs{no:0.8} → Verify (2.90); probs{no:1.5} → **Direct (3.60)** — the EV-79-recorded
  flip reproduced.
- **O4 closed-red (falsifies a round-2 record claim) — the noul-prefix leak is NOT
  reachable from decideFollowup.** Probe4: weighted noul `probability: 2.0` through
  decideFollowup throws **`followup: decideFollowup — answer duplicate of type noul is
  missing a usable probability (expected a number in [0, 1])`** — `followup:`-prefixed,
  never `gate: decide`. `followupFloor` pre-validates every weighted (1a) and
  unweighted (1b) noul answer with the identical [0,1] check before the helper's noul
  arm is reachable (floors → overrides → composite). Ledger domain check: the 8 live
  `gate call failed: gate: decide — …` lines are ALL card-domain
  (`questionSetVersion: "gate-questions-1"`); there are ZERO followup lines in the live
  ledger. **CORRECTION APPLIED TO THE ROUND-2 RECORD:** the principal's "already
  reachable from decideFollowup / the followup reason grammar is already polluted for
  the noul arm" claim is retracted; step-13 residual (b) is withdrawn as a non-leak.
  The domain-neutral-message decision SURVIVES on corrected grounds: the NEW
  choice-branch throw WILL fire from the followup path once the fix lands (the choice
  branch has no floor-arm pre-validation of probabilities), so its message is written
  domain-neutral from birth.
- **O5 closed-green — owner's data-quality flag quantified:** 8/8 live lines died at
  decide() with invalid-response "missing a usable probability", all answers all-null,
  all resolvedMode Deliberate.
- **O6 closed-green — vocabulary disjointness + mixed fixture.**
  `GATE_DECISION_MODES ∩ FOLLOWUP_DISPOSITIONS = ∅` at runtime; resolveRoute over a
  ledger holding a followup line + a card line returns the card line's result,
  unaffected (probe5).
- **O7 closed-green — guard parity.** All five runGate guards present as claimed;
  credential expression byte-identical to preflight.ts:47; parseDecisionsResponse is
  domain-neutral (non-empty model string + answers record; usage/provider/generationId
  defensive/nullable) — nothing card-domain for the followup parse to hit differently.
- **O8 closed-green with two corrections — colocation/type-only erasure.** bun-build
  probe: type-only import erases followup-state/parseCardFile/gate-route from the
  runtime bundle (0/0/0; runtime-import control 8/2/1); no cycle. Corrections: (i) the
  child_process edge lives in **gate-route.ts:31** (`execFileSync`), not runs.ts — the
  round-2 chain citation was mis-located; (ii) the avoidance motivation is partly moot
  (gate-run's existing runtime graph already pulls node:child_process via
  @earendil-works/pi-coding-agent); the real demonstrated win is keeping
  gate-route/runs out of the runtime graph. (iii) gate-run.ts is 301 lines, not "~250".

**Skeptic verdict: blocks on ONE item — the O4 record correction.** Nothing else
blocks; the card's engineering decisions (colocation, `policyVersion =
decisionPolicy.version` writer, shared-helper range fix with a domain-neutral message,
vocabulary-disjointness discriminator) survived every settling test run.

**Correction applied (facilitator recording act, per the EV-80 O2 precedent):** the
round-2 record's residual list is amended — residual (b) ("noul-branch `gate:`-prefix
leak into the followup reason grammar") is WITHDRAWN (non-leak; the noul arm is
floor-pre-validated with the `followup:` grammar before the helper is reachable).
Step-13 residual list now reads: (a) the shipped runGate `policyVersion` writer bug
(own card; route-a-real-runGate-line test); (c) the owner's data-quality flag (model
systematically omitting probabilities → the card gate always routes full Deliberate).
The domain-neutral choice-branch message requirement stands on corrected grounds.

usage (job-9.6): 11.6m/32 turns, cost ≈ $0.0402.

### Step 5 — consolidator synthesis (job-9.7)

**SETTLED (all engineering disputes):** placement (colocated `runFollowupGate` in
gate-run.ts, fenced section, zero new exports, type-only `FollowupState` import — O8
probe); signature `(state, questions, policy: GatePolicy, decisionPolicy, opts)`;
`FollowupRunResult` mirroring `GateRunResult` field-for-field; failure decision a
LITERAL `{disposition: "File", basis: "gate call failed: <reason>"}` — never routed
through decideFollowup, never touching MODE_PANELS; orchestration appends (failure:
answers {}, every asked id null, resolvedMode "File", failureClass shared taxonomy,
basis exact; success: knownIds filter + unknownAnswerIds, answers verbatim,
model/provider/usage/generationId, resolvedMode = disposition verbatim); guards
mirrored (off/model-pin/endpoint-never-path/noul-join — all pre-POST throwing, zero
lines); credential expression byte-identical to preflight.ts (O7); no-api-key failRun
defense-in-depth; off throws (mirrored) with the short-circuit in the CALLER before
buildFollowupState; `advisory = policy.mode === "advisory"` on the line;
discriminator (a) vocabulary-disjointness pinned by test, no `domain` field, no
gate-ledger.ts change (O6); range residual via the SHARED sideProbability choice-branch
throw, domain-neutral message from birth, absent/non-numeric still contributes 0,
**re-card trigger discharged** (O2/O3 red-at-base confirmed both domains);
policyVersion = decisionPolicy.version + questionSetVersion = questions.version
(deliberate divergence, corrective direction; O1 verified the shipped defect
end-to-end); fixture design (injected doubles, imported constants, minimal synthetic
FollowupState literal, no fs/timers in orchestration); gate-transport single-consumer
header claim amended in the same change.

**SETTLED-AS-RESIDUAL (named homes):** R-1 shipped runGate `policyVersion` writer bug —
step-13 card (fold-in test keeps it out of EV-81; safe-direction blast-radius note:
existing mismatched lines keep routing full, only new lines regain the EV-69 fast
path). R-2 owner's data-quality flag — step-13 observation (8/8 live card-gate calls
died at decide() missing-probability → always routes full Deliberate; O5 quantified).
R-3 FLLWUP-96 blocker-note reassignment — corrected at step 13 to the EV-82/83 tool
layer (EV-81 exports no composition entry point); the "one site" assumption is
FLLWUP-96's to re-examine at promotion. R-4 recording-only corrections: the
child_process edge is gate-route.ts:31 (not runs.ts); type-only erasure is the real
win; gate-run.ts is 301 lines.

**OPEN JUDGMENT: none** — the positional-vs-opts signature nit is within the
implementing owner's discretion and the spec pins it (positional, with the header note
the principal asked for). Nothing routes to `product-owner` or `steward`; nothing
reaches `Needs Human`.

**OPEN OBJECTIONS: none** — O4's correction resolved the only blocking item; all eight
probes closed.

**Verdict: ready to hand off.** Owner test obligations: 11 items as itemized in the
consolidation (fixture matrix; guard parity; policyVersion/questionSetVersion on the
line; range residual red-at-base → green; card-gate side of the helper fix;
disjointness + mixed-fixture routing; forbidden-path/pin request assertions; doc-claim
updates; frozen gate-decide expectations; one-line-per-call failure posture; off
short-circuit ordering) + the four repo gates. Spec MUST carry: the corrected residual
list (residual (b) withdrawn as a non-leak), the deliberate-divergence note, the
domain-neutral-message grounds, the FLLWUP-96 reassignment.

usage (job-9.7): 3.3m/4 turns, cost ≈ $0.0044.

**Step 6 — routing:** no open-judgment item and no blocking open objection remains —
nothing routes to `product-owner` and nothing reaches `Needs Human`. Proceed to step 7.

### Step 9 — skeptic verification, cycle 1 (job-9.10)

**Routing re-check (step-8→9 boundary):** recorded mode `Deliberate`, source
`fallback`; `council_route op:"recheck"` is a parent-only tool not exposed in this
container — with fallback source the re-check is not applicable (EV-79 precedent);
nothing re-routes. Proceeding at the branch.

**Verification subject (pinned):** PR #100, head SHA
`9960191cfe2e0d3b94e194b8819dd4eec5a9ca09`, head worktree
`/home/tista/codes/pi-council/.worktrees/ev-81` (branch `ev-81-jev-fail-closed`).
Loop frame: verification precedes step-10 judging and step-11's mechanical merge; the
merge has not happened.

**Gates (skeptic's own runs at head, all four):** `bunx tsc --noEmit` exit 0; FULL
`bun test` → **1391 pass / 5 skip / 0 fail** (8342 expect, 108 files, 110.14s) —
reproduces the owner's numbers exactly; `python3 council/validate.py` → "All council
artifacts valid"; `bash council/preflight.sh` → `PASS: preflight clean`. Shipped-domain
frozen suites (gate-decide/ev79-followup-decide/gate-run/gate-route) 88 pass / 0 fail
— byte-identical expectations under the shared-helper fix.

**Red-at-base reproduced (seven fields, equal triple):** base
`8df2774` (merge-base, required, pre-mechanism); transplant
`test/ev81-sideprobability-range.test.ts` from head; command `bun test
test/ev81-sideprobability-range.test.ts`; raw red 3 pass / 4 fail with every per-failure
line naming the absent range guard (incl. the O3 dangerous direction: base `decide()`
→ `{mode: "Direct", basis: "composite 3.90 ≥ direct threshold 3.40"}` and base runGate
→ `status: "ok"`); detached base worktree, removed after; bare copy set + node_modules
symlink; head half 7 pass / 0 fail. All seven fields verbatim in the PR body.

**Gate-integrity injection:** the `[0,1]` check removed from `sideProbability` → card
suite red 22/5, every failure naming the removed check (incl. followup-run matrix
"Expected: failed / Received: ok"); restored byte-identical (sha256 both sides) → 27/0
again. The gate fails when the defect is present.

**Adversarial probes (all closed, real modules, /tmp scratch):** out-of-range
probability rides the orchestration to `File` + `gate call failed: answer actionable …
probability 1.5 — expected a number in [0, 1]` + `failure{invalid-response}` on the
result; exactly one line (resolvedMode File, failureClass invalid-response,
policyVersion followup-decision-1, questionSetVersion followup-questions-1 from the
loaders); every asked id null; zero Drop/Merge in result or line; advisory rides the
failure line too; `apiKey:null` → no-api-key, 0 POSTs, 1 line; success arm 1 POST, 1
line, unknownAnswerIds filtered; directory snapshot → the only mutation is the ledger
line (zero writes before confirmation); a real runFollowupGate-written failure line
alongside a card line → resolveRoute returns the card's recorded result (a hostile
File line carrying the card's stateHash still renders the card line); one-pin contract
enforced via imported constants (only deliberately-different literals in tests); diff
confinement exact (the two branch commits touch exactly the declared files; package.json
version-only 0.30.0 → 0.31.0; zero council/vault/specs paths); final worktree clean
(pre-existing node_modules symlink only).

**Verdict: PASS — no blocking objection, none open.** Verify-cycle counter: **1 of 3**
used.

usage (job-9.10): 15.9m/40 turns, cost ≈ $0.0403.

### Step 10 — judge (job-9.11, fresh context)

**Verdict: PASS.** Input: the card's goal + the step-9 skeptic evidence only. The
judge independently ran the card's suite at the pinned head (1391/5/0) and verified
each goal claim in the code: the pin enforced by pre-POST guards on BOTH orchestrators
with the test asserting `url === GATE_ENDPOINT` and `body.model === GATE_PINNED_MODEL`
(imported constants); `failRun` builds `gate call failed: <reason>` on every failure
path (matrix: network/timeout/http-500/http-400-refusal/2xx-garbage/no-api-key/
out-of-range→invalid-response, each with `resolvedMode: "File"`); the failure
disposition is the literal File — the human pre-write confirm fallback, never routed
through decideFollowup; `assertFailedPosture` explicitly asserts neither the result
nor the line contains `Drop` or `Merge`; the unreachable-endpoint fixture
(`getaddrinfo ENOTFOUND gate.test`) proves the posture offline. Full suite at head
reproducible and clean.

usage (job-9.11): 2.5m/12 turns, cost ≈ $0.0227.
### Step 12 — merged, reconciled, Done

Merge criteria (mode Deliberate, all five, checked before merging): (1) owner gates
green in full, re-run by the Skeptic at the pinned head (tsc 0; 1391/5/0; validate
clean; preflight PASS); (2) `gh pr checks 100` — `gates` workflow `state: SUCCESS`
(keyed on `workflow`; the absent `[code]smith` row is `SKIPPED`, not a check); (3) no
blocking Skeptic objection (step-9 PASS, cycle 1 of 3, zero open objections); (4) judge
`PASS` (job-9.11); (5) no `Needs Human` state, no outstanding ruling (every dispute
closed by deliberation, test, or applied ruling).

Merged: `gh pr merge 100 --squash --admin --match-head-commit
9960191cfe2e0d3b94e194b8819dd4eec5a9ca09` (P1-1 run-scoped authorization; headRefOid
re-read immediately before merging and matched) → squash commit
`690127bf0d3450e44800bcccd73e011d4184a696` on `main` (PR #100, state MERGED). CI on the
merged SHA confirmed green post-merge: `gates` = completed/success on `690127b`.

Union-merge reconcile: local `main` (record side, through step 10) vs `origin/main`
(squash `690127b` — which carried the branch's inherited record commits, since the
facilitator's record commits were local-only when the PR opened). Both conflicted files
(`council/board.md`, `council/cards/EV-81.md`) verified before resolving: the merge
side is a strict PREFIX of the local record side (EV-81.md: 467 lines ending at the
step-6 routing line, state `In Progress` vs local 540 lines with the step-9/step-10
records; board: EV-81 under `In Progress` vs local `In Review`) — resolved to the
local side, no side discarded, conflict-marker sweep clean, never-force guard intact.
Card and board set `Done` only after the merge landed and CI was confirmed green on
`690127b`.

**EV-81 Done.** Usage (runner container): round-1 owner job-9.1 9.3m/10 turns ≈
$0.0142; round-1 principal job-9.2 9.3m/17 turns ≈ $0.0355; round-2 owner job-9.3
15.1m/10 turns ≈ $0.0136; round-2 principal job-9.4 timed out at the window,
cancelled, re-dispatched once → job-9.5 8.8m/11 turns ≈ $0.0164; step-4 skeptic job-9.6
11.6m/32 turns ≈ $0.0402; consolidator job-9.7 3.3m/4 turns ≈ $0.0044; owner impl
job-9.8 5.4m/26 turns ≈ $0.0568 (mid-work at turn end; no deliverable landed —
re-dispatched once per the dispatch discipline); owner impl job-9.9 26.9m/49 turns ≈
$0.1434; step-9 skeptic job-9.10 15.9m/40 turns ≈ $0.0403; judge job-9.11 2.5m/12 turns
≈ $0.0227.
