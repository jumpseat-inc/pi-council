---
id: EV-84
title: End-to-end falsifier for the Jev-gated follow-up review
state: Done
owner: null
epic: EPIC-10
goal: An end-to-end falsifier carries a run that surfaces three candidates — two technically close, one clearly new — through the follow-up decision and asserts the board gains exactly two `FLLWUP-` cards (the close pair merged, the new one filed) with one recorded line per candidate, and a second arm with the decision off or failing asserts the human pre-write confirm fires and zero cards are written before approval.
---

## Intent

The claim "the review is decided by Jev" has to be falsifiable end to end. This
card ships the opt-in falsifier: a run surfacing three candidates (two close,
one new) through the decision, with the fixture pinning `overrides` so the
exact-two-card assertion is model-independent and the arm reads as a wiring
falsifier — candidate → decision → write → ledger — rather than evidence of
Jev's judgment quality. Two further arms cover the off mode and the
unreachable endpoint, each asserting the human pre-write confirm and zero cards
before approval.

## Acceptance

- The falsifier is opt-in (network/gated); the default suite stays green
  without it, and it is the only new network-touching arm.
- With `gate.mode` `active` and a resolvable credential, the fixture pins the
  follow-up policy's `overrides` in a fixture-local
  `council/gate/followup/decision.json` (resolved through EV-78's whole-file
  first-hit path, never an edit to packaged thresholds) so the two dispositions
  are forced and the exact-two-card assertion is model-independent; it asserts
  exactly two cards are written, the merged card's `## Merged from:` names both
  sources, and the ledger carries one line per candidate with a `File` or
  `Merge` disposition.
- With `gate.mode` `off`, the same fixture asserts the step-13 prose confirm
  fires — carrying the unconditional dedup pass — and nothing is written before
  approval.
- With the endpoint unreachable, the same fixture asserts the human fallback
  fires, zero `Drop` dispositions are recorded, and nothing is written before
  approval.
- Red-at-base evidence is recorded per the repo's `red-base evidence`
  convention.
- The repo's typecheck, the full test suite, `python3 council/validate.py`, and
  the repo's preflight pass.

## Deliberation record

**Routing (step 1):** recorded mode `Deliberate` (fallback — the run input carries the
recorded routing; `council_route` is a parent-only tool not exposed in this container,
per the EV-79/80/81/82/83 precedent). Surface-touching bit: **not** surface-touching —
the card's own diff ships opt-in gated test arms + fixture data; no visible surface, no
user-visible copy changes (EV-81 precedent: fixtures with no visible surface in the diff
are not surface-touching, `designer` not seated). The falsifier exercises the step-13
surface but changes nothing on it. Phase-1 rulings applied: P1-4 (live/gated arms ARE
run in this card), R5 (fixture-local forcing via whole-file first-hit; wiring falsifier,
not judgment-quality evidence), R0a (steps, never line ranges), red-base convention
(seven fields, comparison triple), confirmation-authority ruling (step-13 candidates →
escalation for ratification).
### Round 1 — owner (first pass, job-17.1; 14.9m, 12 turns, ≈$0.0282)

Pure test card — zero engine, procedure, scaffold, or validate.py diff. One new file,
`test/ev84-followup-falsifier.test.ts`, holding all three arms plus a mechanism-obedient
writer helper (EV-83's `ev83-fixture-stub.ts` pattern, in-process). The falsifier drives
the real exported composition `composeFollowupReview` over a `mkdtemp` repoRoot whose
repo-local `$CONFIG_DIR_NAME/council/gate/followup/{questions,decision}.json`
(whole-file first-hit, never packaged edits; CONFIG_DIR_NAME, never a hardcoded `.pi`)
forces the dispositions, then applies step-13's prose to the *rendered lines* — the
writer parses the disposition from the `Mode: <disposition>` prefix of engine-derived
bytes, never reading the ledger (the tool's own opacity contract).

**R5 forcing fixture.** `.council.json` `gate.mode: "active"` (mode lives in config, never
in policy.json — EV-75); packaged `policy.json` supplies model/endpoint/budget via
first-hit fallback. Fixture `decision.json`: `version: "ev84-fixture-v1"` (asserted
verbatim as every ledger line's `policyVersion` — this *is* the first-hit proof at
composition level); weights `{duplicate: 1.0}`; overrides
`[{question:"duplicate", option:"yes", basis:"verbatim duplicate", disposition:"Merge"}]`;
`thresholds {merge: 5.0, drop: 9.0}` — strictly ordered and above any achievable composite
(≤ 1.0), so Merge/Drop are **structurally unreachable via the composite**; `noulThreshold`
at (or near) 0 so a confident noul answer never trips the R4 floor. Merge therefore has
exactly one route — the override lane, which bypasses the composite (R4: floors →
overrides → composite; File is the else-arm). Residual model dependence is confined to
the model's noul yes/no on a constructed verbatim-duplicate vs a disjoint-topic candidate.

**Fixture shape.** Open board card `X` in the tmpdir `council/cards/` plus three
candidates in draft order: A and B, each a near-verbatim variant of X; C, clearly new.
A and B fire the override → `Merge` (targets supplied as the dedup pass's data:
`{A-title: X-id, B-title: X-id}`, resolved board-id-exact through
`resolveFollowupTarget`'s open-only projection); C → `File`. The write set is exactly two
cards: modified `X` (gaining `## Merged from:` with two bullets, recorded order, both
source titles verbatim) and C's new `FLLWUP-` card.

**The three arms and their exact assertions.**

1. **Active, live (gated)** — `test.skipIf(!process.env.COUNCIL_JEV_LIVE)`; no transport
   injection (production `openRouterDecisionsTransport`; `apiKey` undefined → the
   byte-identical preflight credential expression). Asserts: `result.mode === "active"`,
   3 candidates all `status:"ok"`, distinct non-null callIds; ledger exactly 3 call lines,
   every line's `policyVersion === "ev84-fixture-v1"` and `questionSetVersion` = the
   fixture questions' version, `resolvedMode ∈ {File, Merge}` with exactly two `Merge` +
   one `File`, zero `Drop`, zero `failureClass`, `drops: []`; writer produces the two-card
   write set with `X`'s section byte-shaped per the prose template; render shows one
   `Mode:` line per candidate in draft order, the Merge lines carrying `→ <X-id> (active)`,
   C's line arrow-less. P1-4: the arm **is executed** during this card's implementation
   with the real credential, evidence recorded in the card; the default suite stays green
   without the var.
2. **Off (offline, always runs)** — same fixture files present, `gate.mode: "off"`, spy
   transport that fails the test if invoked. Asserts: result is *exactly*
   `{mode:"off", recorded:0}` with no candidates array (proves the R3 short-circuit
   precedes the loaders); transport invocation count 0; ledger file absent;
   `composeFollowupReview` lines `=== ["off"]`; writer writes nothing — cards dir empty
   before and after. The unconditional dedup pass is prose; the falsifier adds one
   anchored scan that step 13 still carries "The dedup pass is unconditional".
3. **Unreachable endpoint (offline, always runs)** — active mode + same fixture;
   transport injected returning `{ok:false, kind:"network", message:"connect ECONNREFUSED
   127.0.0.1:1"}` (deterministic, no real network); explicit `apiKey:"k-test"` bypasses
   resolution. Asserts: all 3 candidates `status:"failed"` with non-null callIds
   (**recorded** failures — one ledger line per candidate even on failure); every line
   `resolvedMode === "File"` (the literal fail-safe, never `decideFollowup`'s),
   `failureClass === "network"`, basis exactly `gate call failed: <reason>`, drops `[]` —
   zero `Drop` dispositions recorded anywhere; render yields per-candidate
   `gate call failed: <reason>` lines (the callFailed cell — network is not a D4 total
   literal), no `Mode:` prefix anywhere; writer writes nothing — the human pre-write
   confirm is what remains, zero cards before approval.

**Red-at-base plan (seven fields).** (1) Base = full 40-hex sha of the commit immediately
preceding EPIC-10's first mechanism merge (EV-78's); role `required` — at any later base
the mechanism is fully merged and the falsifier is green at base; mirrors EV-41's founding
record. (2) Transplant = exactly one file, `test/ev84-followup-falsifier.test.ts`, from
EV-84's head sha (fixture data is in-file literals). (3) Exact command
`bun test test/ev84-followup-falsifier.test.ts`, verbatim, both halves; live arm skips
(env unset) so the red comes from the offline arms. (4) Raw red output — at base the
imports of the followup modules fail; per-failure lines recorded verbatim (mechanism-absent
material for the skeptic's derivation). (5) Worktree provenance — detached
`git worktree add` at the base sha under `.worktrees/`; main checkout untouched; removed
after the run. (6) Copy set — bare copy. (7) Head half — EV-84 head sha, same command,
`0 fail`.

**Gating env var**: `COUNCIL_JEV_LIVE=1`, dedicated — reusing `COUNCIL_INTEGRATION=1`
would couple this arm to the dispatch integration test so both flip together.

**Tradeoffs accepted:** one irreducible live-model dependence (the noul duplicate answer)
remains in arm 1 — R5 accepts it; the "write" half is a test-side prose-obedient stub, not
a card-writing tool (presented-never-written preserved, EV-83 pattern); the red is an
import-level failure, not behavioral — the epic-preceding base makes the *entire* mechanism
the absent artifact set. Expected engine diff: none; a missing seam would be a recorded
spec deviation, not a silent scope add.

**Open points:** (1) fixture shape — card's "exactly two cards written" + "`## Merged
from:` names both sources" holds jointly in owner's shape (modified X + new C-card, two
bullets on X); the epic's "the board gains exactly two `FLLWUP-` cards" reads as two *new*
cards, which forces a sibling-target variant and leaves only one bullet — which reading
governs? (2) base-selection rule — confirm the epic-preceding base and that a whole-file
module-resolution red counts as mechanism-absent; (3) env var name; (4) loader range edges
in the fixture (implementation-time checks only).

### Round 1 — principal (first pass, job-17.3; 13.7m, 17 turns, ≈$0.0292)

**Cross-seam contracts.** (EV-78) `gateDirs(repoRoot,"followup")` resolves
`[<repo>/$CONFIG_DIR_NAME/council/gate/followup/, <PKG_ROOT>/council/gate/followup/]` —
repo override first, whole file, no merge; `loadFollowupDecision` requires the *complete*
policy, so a partial fixture file fails loud; a fixture file at `<repo>/council/gate/
followup/decision.json` (no `$CONFIG_DIR_NAME` prefix) falls through to packaged
thresholds silently. (EV-79) **An override is answer-conditional: pinning `overrides`
does not force a disposition unless the answers are also pinned** — the load-bearing seam
R5's wording elides. (EV-80) `boardPath = <repoRoot>/council/board.md`, cards dir =
`dirname(boardPath)/cards`, siblings by object identity; `FollowupReviewCandidate` is
verdict-free — per-candidate dispositions must be read from the ledger or rendered lines,
never the tool result; pinned-callId assertions must call `composeFollowupReview` with
`{callId}` directly. (EV-81) one POST, one `appendGateCall` line per candidate,
`resolvedMode = disposition`, `policyVersion = decisionPolicy.version`; offline transport
doubles already exist in `test/ev81-followup-run.test.ts`. (EV-82) line =
`decisionLine(record) + " — " + title + (target ? " → "+target : "") + " (" + qualifier +
")"`; `resolveFollowupTarget` board-id-exact before sibling-title-exact. (EV-83 + step 13)
every surfaced candidate ends step 13 as `ESCALATION` before any write; §13 writes cards
**in prose, by the facilitator — "there is no card-writing tool"**; the engine has no
write path for follow-up cards.

**Blind spots.** Owner: (a) fixture override path needs the `$CONFIG_DIR_NAME` segment;
(b) the ledger/render is the *end* of the engine's responsibility — a test-local
card-writer risks letting the test define the `## Merged from:` convention R7 explicitly
refused to let a falsifier own; (c) `opts.callId` opacity. Skeptic: needs a forcing-fires
probe (mutate the fixture override, observe the count change), else "model-independent" is
an assertion the test cannot falsify. PO/consolidator: EV-83's confirmation-authority
ruling makes a single-turn runner write impossible — any arm claiming a runner writes two
cards in one flow contradicts shipped prose. Designer correctly not seated.

**Reframe — two layers with separately named actors.**
1. **Decision leg (offline, deterministic).** Pin the fixture policy **and the decision
   answers** (an injected transport returning fixed noul/choice answers), not merely
   `overrides` — then the two dispositions are genuinely forced. A live model plus
   overrides is *not* model-independent — the model can answer the non-firing option — so
   a live arm cannot honestly be called model-independent.
2. **Write leg (named actuator).** State who performs the confirmation+write: a gated
   live facilitator run of §13 (a compliance test, say so), a resumed confirming dispatch
   through the runner, or a small named harness. Do not let a test-owned actuator silently
   *define* the `## Merged from:` convention. Assert the engine leaves the tree
   byte-identical before the actuator (zero engine writes).

**Gating granularity:** arms off and unreachable need no network; only the active-success
arm does — split into an always-on offline file (off + unreachable) and one gated
live/integration arm, rather than gating everything and losing offline coverage from the
default suite.

**Arithmetic underdetermination:** "three candidates → exactly two cards + a
`## Merged from:` naming **both** sources" only holds for a specific
candidate→disposition→target table (A `File`, B `Merge` into A, C `File` yields one
bullet; two Merge into one open card + one File yields one new card + the modified open
card). Pin the table as fixture data and derive the expected card count and bullet set
from it as consequences.

**Testable claims.** T1 path contract (fixture at the unprefixed path returns packaged
thresholds; at the `.pi`-prefixed path returns the fixture's — assert both). T2 forcing
fires (mutate the fixture override to the opposite disposition, observe the count change).
T3 no engine write (snapshot cards/ + board.md around the engine call → byte-identical).
T4 table derives assertions (expected count + bullets computed from the pinned table).
T5 red at base — the correct base is pre-EV-78 (parent of EPIC-10's first merge), not
current `main`, where the transplant would be green; record the base-selection rule in
field 1 and the transplant set in field 2.

### Round 2 — owner (job-17.4; 4.9m, 9 turns, ≈$0.0152) and principal (job-17.5; 4.9m, 13 turns, ≈$0.0238)

**Converged in round 2 (exchange stopped at two rounds; cap 3):**

1. **Live arm vs offline decision leg — both, relabeled.** Owner conceded the live arm is
   not model-independent (`overrideFires` reads the answer, gate.ts:1052) but held R5 never
   claimed it was — R5 named the off/failing arms the model-independent structural coverage.
   Principal answered plainly: **(b) compatible strengthening, not escalation** — the live
   arm keeps the fixture `overrides` and the exact-two assertion with the residual
   dependence on the model's `duplicate?` answer recorded honestly; an ADDITIONAL
   always-on offline decision leg (active mode + fixture policy + injected transport
   returning pinned answers) lands in the default suite and carries the honest
   model-independent exact-two claim. Add-only; nothing binding weakened. The card's
   "model-independent" sentence is amended to attach to the offline leg (card-text edit,
   recorded at step 7; assertion unchanged).
2. **Fixture arithmetic — settled by shipped R7 text (facilitator-verified).** Owner's
   round-1 board-target table withdrawn. New pinned table: A (near-verbatim of B) →
   `duplicate: yes` → Merge via override → **sibling target B**; B → `duplicate: no` →
   File → **new FLLWUP- card**; C → `duplicate: no` → File → **new FLLWUP- card**. R7's
   sibling branch (EV-82.md:148–156, verified verbatim: "a sibling target means the target
   candidate's own written card names **both** source candidates") makes B's own written
   card carry `## Merged from:` naming both A and B. Board gains exactly two net-new
   `FLLWUP-` cards; both clauses satisfied jointly. The principal's round-2
   joint-unsatisfiability claim was refuted by the shipped sibling branch; its enumeration
   settling test (count net-new files under cards/) predicts 2 — the table delivers 2.
3. **Write leg — named actuator, test does not own the convention.** In production the
   actuator is the facilitator executing step-13 prose (no card-writing tool; EV-83's
   confirmation-authority ruling). In the falsifier: an in-file **step-13 transcription
   helper** whose `## Merged from:` heading bytes are pinned to the shipped procedure's own
   step-13 literal (prose.test.ts:563 pins `## Merged from:\n\n- <source-title>\n-
   <source-title>`); if the prose changes, the pin fails and the helper follows — never
   vice versa. Helper's only judgment: parse `Mode: <disposition>` from engine-derived
   rendered bytes, apply the pinned table's targets.
4. **Gating — confirmed.** One test file, three always-on arms (off; unreachable endpoint;
   offline pinned-answers decision leg) + one gated live arm (`COUNCIL_JEV_LIVE=1`,
   dedicated, not `COUNCIL_INTEGRATION=1`). Default suite green without the var and still
   carries the offline coverage.
5. **Probes T1–T5 adopted by both** (T2 amended, code-grounded: override dispositions are
   restricted to `{Merge, Drop}`, so the forcing-fires probe flips A's rule `Merge → Drop`
   and observes the write set go 2 cards → 1). T1 path contract (unprefixed fixture path
   silently falls through to packaged — the negative half is the actual first-hit
   falsifier); T3 zero-engine-write snapshot (cards/ + board.md byte-identical around
   `composeFollowupReview`); T4 count + bullets derived from the pinned table; T5 red at
   base = parent of EPIC-10's first mechanism merge (pre-EV-78), transplant = the single
   test file, bare copy, import-level mechanism-absent red.

**Remaining open points:** none on design. One implementation-time check (not a dispute):
the offline decision leg must assert the pinned answers actually reach `decideFollowup`
past the floors phase (fixture `noulThreshold` low enough that a pinned confident answer
never trips R4's floor) — failing-then-passing probe per TDD.

### Step 4 — skeptic attack (job-17.6; 11.8m, 26 turns, ≈$0.0439)

Baseline: `bun test test/ev82-followup-tool.test.ts test/ev81-followup-run.test.ts` →
30 pass / 0 fail (265 expect calls). All settling tests actually run via a scratch harness
(tmptree repos, injected transports, no network).

- **O1 — closed-red:** the T2-amended forcing probe as recorded ("flip A's rule
  `Merge → Drop`, write set 2 cards → 1") is not realizable under the adopted round-2
  table — the run shows **2 → 2** (A moves Merge→Drop; B and C's Files are untouched; B's
  card loses its `## Merged from:` section, 1→0). The predicted arithmetic belongs to the
  withdrawn round-1 table. The probe remains a forcing-fires witness but must be restated
  to assert the disposition distribution and/or the bullet-set disappearance — never a
  card-count delta. Fix belongs at step 7 (spec), not shipped code.
- **O2 — closed-green:** the offline pinned-answers leg is realizable and deterministic —
  the planned fixture loads under every loader validation rule; pinned answers pass the
  floors (A certainty 0.97 ≥ noulThreshold 0) and reach the override; ledger 3 lines, all
  `policyVersion=ev84-fixture-v1`, bases `Merge :: duplicate? yes (verbatim duplicate)` /
  `File :: composite 0.20 < merge threshold 5.00` ×2; write set 2 cards; transport invoked
  exactly 3×.
- **O3 — closed-green:** T1's negative half realizable — unprefixed fixture path falls
  through to packaged (`followup-decision-1`, thresholds 1/2); `.pi`-prefixed resolves
  `ev84-fixture-v1`.
- **O4 — closed-green:** T3 realizable — cards/ + board.md byte-preserved around
  `composeFollowupReview`; only the ledger is written.
- **O5 — closed-green:** off arm — result exactly `{"mode":"off","recorded":0}`, lines
  `["off"]`, transport invocations 0, ledger absent, cards/ untouched.
- **O6 — closed-green:** unreachable-endpoint arm — 3× failed with non-null callIds,
  ledger `resolvedModes=File` (literal fail-safe), `failureClasses=network`, three
  `gate call failed: connect ECONNREFUSED 127.0.0.1:1` render lines, no `Mode:` prefix,
  zero Drop.
- **O7 — closed-green:** the pinned table is realizable through
  `composeFollowupReview` + `resolveFollowupTarget` — A's line renders
  `Mode: Merge — duplicate? yes (verbatim duplicate) — <A-title> → <B-title> (active)`
  (sibling-title-exact); B/C arrow-less in draft order; an unresolvable id fails loud.
- **O8 — closed-green:** the off arm's anchored scan phrase exists verbatim —
  `council/procedures/council.md:402` carries `**The dedup pass is unconditional.**`.
- **O9 — closed-green (all seven field identities + mechanism-absent derivability); head
  half open-untested:** base identity pinned `d94b33c9ce0d32380e8e0891b32c3298d6861047`
  = parent of `92855385` (EPIC-10's first mechanism merge), role `required`; mechanism-
  absent red demonstrated in a detached nested worktree at base —
  `0 pass / 1 fail / 1 error`, per-failure line naming the absent mechanism module
  (`Cannot find module '../extensions/followup-tool.ts'`); bare copy suffices (bun climbs
  to the main checkout's node_modules). The head half (`0 fail`) cannot run until the
  transplant exists — a recorded commitment, not a contradiction.
- **O10 — closed-green for the offline claim; one stale-text residual:** the split
  (live = wiring smoke with recorded residual; offline = honest model-independent
  exact-two) is coherent and runnable. Residual: Round 1's arm-1 text still says
  "exactly two `Merge` + one `File`" — under the round-2 table the ledger reads
  **one `Merge` + two `File`** (verified). Step 7 must reconcile; implementation must not
  inherit the stale count.

**Skeptic verdict:** one concrete red finding (O1, a record defect with its fix named —
restate the T2 probe); everything else engine-side closed-green; open-untested items are
recorded commitments (red-at-base head half; live-arm residual model dependence), not
contradictions.

### Step 5 — consolidator synthesis (job-17.7; 1.4m, 3 turns, ≈$0.0032)

**SETTLED (14):** S1 fixture arithmetic (pinned table A→Merge/sibling-B, B→File, C→File →
2 net-new FLLWUP- cards; R7 sibling branch verified; round-1 board-target table
withdrawn); S2 write-leg actuator (in-file step-13 transcription helper pinned to the
shipped prose literal, prose.test.ts:563 — the test never owns the convention); S3 gating
(three always-on offline arms + one gated live arm, `COUNCIL_JEV_LIVE=1`); S4 probes
T1–T5 with T2 amended (assert disposition distribution + bullet-set disappearance, never
a card-count delta — the 2→1 figure belongs to the withdrawn table); S5 live arm kept
with recorded residual + offline pinned-answers leg carries the honest model-independent
exact-two claim (add-only, nothing binding weakened); S6–S14 skeptic O2–O10
closed-green items.

**SETTLED DISPUTES (with settling tests):** fixture arithmetic (R7 text + net-new-file
enumeration → 2); write-leg ownership (confirmation-authority ruling + prose pin); live
model-independence (compatible-strengthening reading); offline leg realizability
(injected transport, deterministic, 3 ledger lines, 2-card write set); T1 negative half;
T3 zero-engine-write; T2 forcing fires (disposition distribution + bullet set; count
2→2).

**OPEN JUDGMENT: none.** Round 2 converged on all five disputed points; the residual
"open points" are implementation-time checks, not design disputes.

**OPEN OBJECTIONS: none.** O1 is a record defect with its fix named (step-7 spec
restates the T2 probe); O9-head and O10 are open-untested recorded commitments.

**Verdict: ready for step 7.** The spec must (1) restate the T2 probe (O1 fix);
(2) reconcile the stale "exactly two `Merge` + one `File`" figure to the adopted table's
**one `Merge` + two `File`** (O10 residual — superseding Round 1 arm-1 text wherever
inherited); (3) carry the pinned fixture table, gating split, write-leg actuator
contract, and probes T1–T5 as settled.

### Step 6 — routing

No open-judgment items and no blocking open objections remain (consolidator verdict
above) — nothing routes to a ruling seat, and the card does not reach `Needs Human`.
Proceeding to step 7.

### Step 8 — owner implementation (job-17.8; 29.5m, 51 turns, ≈$0.1181)

Deliverable: `test/ev84-followup-falsifier.test.ts` (683 lines) — the only diff; zero
engine/procedure/scaffold/validate.py changes; no `package.json` bump (test-only card).
Worktree `.worktrees/ev-84`, branch `ev-84-followup-falsifier`, **PR #103 open at head
`eb212d29c4fc58d7553c3e3bd2560c7e2bed72de`** (facilitator-observed: `gh pr view 103` →
OPEN, headRefOid matches). Card set `In Review` from the observed open PR.

Arms: offline pinned-answers decision leg (always-on, model-independent — carries the
exact-two claim); off (exact no-op, transport never invoked, ledger absent, zero cards);
unreachable endpoint (3 recorded failures, `resolvedMode=File` literal fail-safe, zero
Drop, zero cards); live (gated `COUNCIL_JEV_LIVE=1`). Probes T1–T5 (T2 restated), the
step-13 transcription helper with the `## Merged from:` bytes pinned to the shipped
prose literal.

One recorded spec deviation (implementation-time): the spec's `drops: []` assertion is
unreachable — the packer always records one per-section measurement record
(`truncated: false` when nothing was cut); corrected to the intent: zero truncation
records. Noted on the PR.

**Red-at-base record (seven fields, executed):**
1. Base identity: `d94b33c9ce0d32380e8e0891b32c3298d6861047` — commit immediately
   preceding EPIC-10's first mechanism merge (`9285538`, EV-78 PR #97); role
   **`required`**.
2. Transplant identity: exactly one file, `test/ev84-followup-falsifier.test.ts`, from
   head `eb212d29…`; bare copy.
3. Exact command: `bun test test/ev84-followup-falsifier.test.ts` (verbatim, both
   halves).
4. Raw red output: `0 pass / 1 fail / 1 error` — per-failure line verbatim:
   `error: Cannot find module '../extensions/followup-tool.ts' from
   '…/.worktrees/ev-84-base/test/ev84-followup-falsifier.test.ts'` (mechanism-absent
   class candidate; boundary skeptic-derived, never owner-classified).
5. Worktree provenance: detached `git worktree add --detach` at the base sha under
   `.worktrees/ev-84-base`; main checkout untouched; removed after the run.
6. Copy set: bare copy.
7. Head half: `eb212d29…`, same command — **7 pass / 1 skip / 0 fail**.

**Live arm (P1-4, executed) — raw evidence, a legitimate recorded finding:**
`COUNCIL_JEV_LIVE=1 bun test test/ev84-followup-falsifier.test.ts` → 7 pass / 1 fail
(offline arms green; `OPENROUTER_API_KEY` set). Arm 4: all 3 candidates
`status:"failed"` with non-null callIds; ledger `failure.class=invalid-response`,
`resolvedMode=File`, basis verbatim: `gate call failed: followup: decideFollowup —
answer duplicate of type noul is missing a usable probability (expected a number in
[0, 1])`. Root cause verified by raw POST: the API (typesafe/jev-1.13-20260917) now
returns noul answers as `{"type":"noul","noul":0.59}` — the engine
(`sideProbability`/`decideFollowup`/`overrideFires`) expects `probability`. The repo's
own live contract fails identically (`COUNCIL_INTEGRATION=1 bun test
test/gate-run-live.test.ts` → 1 pass / 2 fail). Every live gate/followup call currently
fails closed (`invalid-response` → File/Deliberate) — the fail-safe direction holding,
the live shape contract drifted. No retry-loop; no engine diff (test-only card).

**Gates (owner, from the worktree, all green in order):** `bunx tsc --noEmit` exit 0;
`bun test` 1453 pass / 6 skip / 0 fail (112.4s, within the CI-timeout budget policy);
`python3 council/validate.py` clean; `bash council/preflight.sh EV-84` PASS.

### Step 9 — skeptic verification (job-17.9; 7.7m, 41 turns, ≈$0.0301) — cycle 1 of 3

Verdict: **PASS — no blocking objection.** Subject: PR #103 head `eb212d29…` at
`.worktrees/ev-84`; every claim re-run at the head worktree (preflight at the main
checkout per its `$PWD`-scoped contract).

- O1 closed-green — diff is exactly `test/ev84-followup-falsifier.test.ts`; worktree
  clean.
- O2 closed-red (expected red, non-vacuity probe) — flipping the off fixture to active
  fails the off arm's assertion; the assertion discriminates.
- O3 closed-red (expected red, non-vacuity probe) — pinned transport answering "yes"
  for C too fails the exact-two write-set claim; the claim discriminates on data.
- O4 closed-red — live arm reproduced verbatim at head (`COUNCIL_JEV_LIVE=1` →
  7 pass / 1 fail; 3× `failure.class:"invalid-response"`, `resolvedMode:"File"`,
  non-null callIds, the noul-probability basis byte-identical). **Classified
  non-blocking:** (a) the model-independent exact-two claim is settled S5 onto the
  offline leg, which is green; (b) P1-4/R5 require execution + recorded evidence, not
  green — the recorded failure is the falsifier working, catching a live upstream shape
  drift (API returns `{"type":"noul","noul":…}`, engine expects `probability`;
  deterministic per gate.ts:916–919 + gate-run.ts:298–301); (c) the fail-safe direction
  held by construction (invalid-response → literal File → zero Drop, zero cards before
  the human confirm).
- O5 closed-green — red-at-base reproduced exactly in the skeptic's own detached
  worktree (comparison triple **equal** → counts reproduced: `0 pass / 1 fail /
  1 error`, per-failure line identical, transplant sha1 identical both sides).
  **Mechanism-absent boundary (skeptic-derived):** the error names
  `../extensions/followup-tool.ts` — a mechanism module verified absent at base (0
  `followup*` files in base `extensions/`; the rest of the import graph present);
  nothing inside the bare copy set is referenced in the red → mechanism-absent class.
- Fresh gate runs: head — `bunx tsc --noEmit` exit 0; `bun test` 1453 pass / 6 skip /
  0 fail (111.3s); `python3 council/validate.py` clean; single file 7 pass / 1 skip /
  0 fail (104 expects). Main checkout — `bash council/preflight.sh EV-84` PASS.
  Main checkout `--porcelain` clean apart from the 8 known untracked `vault/raw/*`.

Verify-cycle counter: **1 of 3 used.** No new objections filed; none open-untested.

### Step 10 — judge (job-17.10; 3.0m, 13 turns, ≈$0.0210)

**PASS.** Basis: goal verified at the branch head — arm 1 carries the goal's first clause
end-to-end (three candidates → one ledger line each → dispositions `Merge, File, File` →
exactly two net-new `FLLWUP-` cards, the merged card's `## Merged from:` naming both
sources); arms 2 and 3 carry the second clause (human pre-write confirm as the gate;
zero cards written; engine writes nothing); non-vacuity independently verified (T2's
distribution flip dissolves the merged-from section); red-at-base + live-arm finding
accepted as recorded (non-blocking). Judge re-ran: the falsifier file 7 pass / 1 skip /
0 fail (104 expects), the live arm reproduced, `bunx tsc --noEmit` exit 0, full suite
1453 / 6 / 0, preflight clean.

### Steps 11–12 — deterministic merge check, merge, reconcile, Done

**Step 11 — deterministic merge check (mode `Deliberate`, all five criteria, observed
artifacts, facilitator-executed):**
1. Owner gates green in full, re-run fresh by the facilitator at head `eb212d29`:
   `bunx tsc --noEmit` exit 0; full `bun test` 1453 pass / 6 skip / 0 fail (111.2s);
   `python3 council/validate.py` clean; `bash council/preflight.sh EV-84` PASS.
2. `gh pr checks 103 --json name,state,workflow` → `{"name":"gates","state":"SUCCESS",
   "workflow":"gates"}` (keyed on `workflow`; headRefOid `eb212d29c4fc58d7553c3e3bd
   560c7e2bed72de` re-read immediately before merging — unchanged).
3. No blocking Skeptic objection (step 9 PASS).
4. Judge verdict PASS (step 10).
5. No `Needs Human` state, no outstanding ruling.

Merged: `gh pr merge 103 --squash --admin --match-head-commit eb212d29c4fc58d7553c3e3bd
2560c7e2bed72de` (P1-1 run-scoped admin authorization) → merged SHA
**`03925df015fb6d9b26931d8558cc11322a0782c2`**; `gates` workflow observed
`completed / success` on that exact SHA (criterion confirmed on the merged SHA).

**Step 12 — reconcile:** `git pull --rebase` hit the EV-83-documented spurious-conflict
class (the squash `03925df` already carries the content of the seven pre-branch record
commits `5a1d503..89e6a9f` — the worktree branch was cut from local main at `89e6a9f`
while origin/main sat at `5a1d503`, so the PR diff included them; git replayed them as
duplicates). Aborted; containment verified (`git diff 5a1d503 03925df --stat` shows
exactly board.md + EV-84.md record content + the spec + the deliverable test file);
replayed only the four genuinely-unmerged record commits with
`git rebase --onto origin/main 89e6a9f main` (`162a784` In Review, `d5aa11a` Needs-Human
header restore, `b13cf90` step 9, `4d73fff` step 10). Union-keep honored — no side's
content lost, the squash already carried the rest. Conflict-marker sweep clean;
`validate.py` clean.

Card set `Done` from the observed merged SHA + green CI on it. Record commit pushed
directly to `main` under P1-RP (run-scoped authorization). No `package.json` bump —
the deliverable is a test-only file (no behavior change).
