---
id: EV-71
title: Decisions-aware gate spend accounting
state: Done
owner: null
epic: EPIC-13
goal: Gate spend is journalled by the ledger's single writer storing the decisions response's usage.cost figure verbatim, with no synthesised and no second cost field on the emitted call line, and journalled with the decision generation id that response reported, and the provider reconciliation accounts for gate spend by accepting decision generation ids sourced from the gate ledger, proven by an injected-fetch-double test in which a ledger-sourced generation id beginning gen-dec- is looked up and its reported total_cost resolves into the gate total while a seat transcript that carries no decision generation id triggers no decision lookup and no gate call's cost is ever derived from a catalogue estimate.
---

## Intent

The gate's own spend is the cheapest number in the feature and it must not be
the dishonest one. The decisions response carries its cost inline, so the ledger
records it with a reported basis rather than an estimate — the only basis the
provider actually gave.

The gap this card closes is narrower than it first appears, and the correction
matters. A decision generation id is not invisible to the generation-lookup API:
`GET /api/v1/generation?id=<gen-dec-…>` returns `200` carrying `total_cost`,
`provider_name`, `api_type: "decisions"`, and native token counts, and the
existing extraction reads exactly those fields. What the reconciler lacks is the
id itself: it harvests generation ids only from assistant entries in seat
session transcripts, and a gate call produces no session entry, so the id never
enters its input set. Left alone, gate spend would be silently under-reported by
a path that believes it has accounted for everything, which is exactly the
failure class the usage-accounting work exists to prevent.

The shape is settled. The ledger carries the reported cost and the generation id
that produced it, and the reconciliation path accepts ids sourced from the
ledger, so gate spend is accounted for and independently verifiable rather than
silently absent. The usage block still names gate spend as excluded from the
invocation total, because folding a reported-basis number into a half whose
`basis=` label would then assert a provenance the number does not have is the
same class of plausible-but-false assertion the block's runner-omission rule
exists to prevent. The ledger remains the authoritative gate total.

What is not acceptable is an unaccounted call whose cost is neither carried nor
named.

## Acceptance

The ledger's gate total equals the sum of reported per-call costs; a
ledger-sourced decision generation id is looked up by the reconciliation path
and its reported `total_cost` resolves into that total, proven with an injected
fetch double; an invocation whose window contained gate calls carries a
conditional legend line `usage  gate = excluded from this total`, present iff at
least one gate call falls in the window and absent otherwise, composed with the
existing prefix, label column and legend stack order, and a golden test proves
the block's grammar is unchanged when gate calls are present and no legend is
emitted when they are absent; and no gate call's cost is derived from a
catalogue estimate.

### Grammar scoping (product-owner ruling 2026-09-21, verbatim — binds the Acceptance and every golden's header)

`"The block's grammar is unchanged" means row grammar and state exclusivity, not line count. A render is grammar-preserving iff, for every input: (1) every emitted line is one of the three bound whole-block state lines (or the usage  accounting failed — <reason> prefix line) or a row carrying the literal "usage  " prefix; (2) every row keeps its ruled key and "key = short definition" shape, label column unpadded as ruled; (3) row order is the ruled order and each conditional legend keeps its slot, the stack reading reported → partial → n/a → gate with gate last; (4) at most one whole-block state line appears, never two, precedence failed > unresolved > empty unchanged, and the state line is always the block's first line; (5) with zero gate calls in window the block is byte-identical to the pre-EV-71 render.`

Binding acceptance clarification: `"Present iff ≥1 gate call falls in the window"` scopes to blocks rendered from a persisted record; `{ failed: string }` is a named, chosen exception, and its golden comment must read "chosen: no persisted record to render from", NEVER "impossible" (skeptic O10 closed-red against the impossibility wording — the caller's probe printed the second line).

Adopted: O-C — `sumGateSpend` goldens compare through one float path (`0.0042 − 0.001 = 0.0031999999999999997`), never a hand-written decimal.

## Phase 1 Rulings (recorded before dispatch — binding for this run)

- **R1 — merge authorization, every card in this run.** The human authorized
  the run-scoped admin bypass: the merge is `gh pr merge <PR> --squash --admin
  --match-head-commit <X>`, where `<X>` is the exact head SHA the `gates`
  workflow `SUCCESS` (merge-check criterion 2) was read against. This
  authorization is for this run only and is not extended to any later run; a
  SHA mismatch is a HALT, not a retry.


## Step 6 ruling — product-owner (2026-09-21), verbatim (appended per escalation contract on resumption)

J1 — `costBasis`: NO field. Basis is structural. `gate-ledger.ts` is not touched: `appendGateCall` stays the sole writer of the v2 line and computes nothing, storing the response's `usage.cost` as it arrived. No `costBasis` key, no schema bump. Binding rule: a field on the durable line needs a named consumer, not a named sentence. Enforced by T-B round-trip (`0.0042` in, `0.0042` out exactly), T-C the emitted line carries no cost-bearing key other than `usage.cost`, T-D no catalogue-cost path reachable from the gate path. AMEND the `goal:` (one physical line, `goal:` last) to EXACTLY:
`Gate spend is journalled by the ledger's single writer storing the decisions response's usage.cost figure verbatim, with no synthesised and no second cost field on the emitted call line, and journalled with the decision generation id that response reported, and the provider reconciliation accounts for gate spend by accepting decision generation ids sourced from the gate ledger, proven by an injected-fetch-double test in which a ledger-sourced generation id beginning gen-dec- is looked up and its reported total_cost resolves into the gate total while a seat transcript that carries no decision generation id triggers no decision lookup and no gate call's cost is ever derived from a catalogue estimate.`

J2 — whole-block states: PINNED (framing B), as an application of R-6, not an amendment. Legend granted after the state line in `no usage recorded` and `accounting boundary unresolved`; denied in `accounting failed`. Exact grammar-scoping wording (Acceptance + goldens' header):
`"The block's grammar is unchanged" means row grammar and state exclusivity, not line count. A render is grammar-preserving iff, for every input: (1) every emitted line is one of the three bound whole-block state lines (or the usage  accounting failed — <reason> prefix line) or a row carrying the literal "usage  " prefix; (2) every row keeps its ruled key and "key = short definition" shape, label column unpadded as ruled; (3) row order is the ruled order and each conditional legend keeps its slot, the stack reading reported → partial → n/a → gate with gate last; (4) at most one whole-block state line appears, never two, precedence failed > unresolved > empty unchanged, and the state line is always the block's first line; (5) with zero gate calls in window the block is byte-identical to the pre-EV-71 render.`
The relaxation, as a rule: a whole-block state may be followed by conditional LEGEND rows, and only by legend rows, when a card's own acceptance makes that legend's presence an iff over the invocation and suppressing it would assert that nothing was recorded when something was. Measurement rows, the boundary row and the reported row NEVER appear in a whole-block state. This is R-6 ("grammar identity, not line-count") applied, not amended. Not a general license: a future card must show the same shape, and every relaxed or denied state is named in its own golden with a `// Decision:` comment.

Binding acceptance clarification (amendable surface): "Present iff ≥1 gate call falls in the window" scopes to blocks rendered from a persisted record; `{ failed: string }` is a named, chosen exception, and its golden comment must read "chosen: no persisted record to render from", NEVER "impossible" (skeptic O10 closed-red — the caller's probe printed the second line).

Adopted: O-C — `sumGateSpend` goldens compare through one float path (`0.0042 − 0.001 = 0.0031999999999999997`), never a hand-written decimal.

## Deliberation record
### Round 1 — owner

## Position — EV-71, owner first pass

### 1. What the goal requires (my read)

Three obligations, one per existing seam, and I verified each against the tree:

- **Ledger half — already built, needs proving, not rebuilding.** EV-65 landed `GATE_LEDGER_SCHEMA_VERSION = 2` (`extensions/gate-ledger.ts`): `appendGateCall` writes `usage {input_tokens, output_tokens, cost}` and `generationId` atomically on the call line, taken verbatim from the decisions response (`gate-run.ts` success path; `gate-transport.ts` `parseDecisionsResponse`). The reported basis is **structural, not a field**: the ledger's only writer path stores the response's `usage.cost` verbatim and computes nothing, so `costBasis REPORTED (never estimate)` holds by construction. What EV-71 adds here is a pure total helper + the test that "the ledger's gate total equals the sum of reported per-call costs".
- **Reconciliation half — the real gap.** `fetchProviderReport` (`extensions/provider-cost.ts`) harvests generation ids **only** from seat session transcripts via `collectGenerationIds` (assistant `responseId`s), and its eligibility filter is `model.startsWith("openrouter/")`. A gate call produces no session entry and its model (`typesafe/jev-1.13`) is not openrouter-prefixed — the id is doubly invisible. The reconciler must accept decision ids **sourced from the gate ledger**, look each up through the existing `GET /api/v1/generation?id=` transport (the existing `toGeneration` extraction already reads `total_cost`/`provider_name`/native tokens; `api_type` is simply tolerated/ignored — no extraction change), and the reported `total_cost` resolves into the reconciliation's gate total. The **ledger stays the authoritative gate total** — the lookup is independent verification riding the durable record, not an alternate source.
- **Surface half — a conditional legend, nothing more.** `formatUsageBlock` gains `usage  gate = excluded from this total`, present iff ≥1 gate call falls in the window, composed with the existing `usage  ` prefix, the single-word label column (sibling of `n/a`, `partial`), and the legend stack order. Gate spend is **never** folded into `ownSession`/`subtree` — a reported-basis number inside a half labelled `basis=session-reconciled`/`stream-assistant` would assert provenance it doesn't have. No gate cost row is rendered; the ledger is the total.
- **Hard edge:** no gate call's cost is ever derived from a catalogue estimate — the only two gate cost figures in the system are the ledger's stored `usage.cost` and the lookup's reported `total_cost`.

### 2. Design

**Modules and import edges** (all one-way, no cycles; no new network surface — the generation transport is reused):

- **`extensions/gate-ledger.ts`** — add pure `sumGateSpend(calls: GateLedgerRecord[]): number | null`: Σ of `usage.cost` over call lines where `usage?.cost` is a finite number; `null` iff none carried. A v1 line or a failed call (no `usage`) contributes nothing — absent, never zero-derived. No schema change, no writer change.
- **`extensions/provider-cost.ts`** — extend `FetchProviderReportInput` with optional `gateCalls?: { callId: string; generationId: string | null }[]` (values passed in; this module stays ledger-agnostic). Add `GateProviderReport { callsInWindow: number; lookedUp: number; failedLookups: <class count>; generations: ProviderGeneration[]; totalCost: number | null }`. `ProviderCostReport` gains optional `gate?: GateProviderReport`. Gate lookups run through the same injected `fetchGeneration`/apiKey/timeout **after** the seat-job loop; each id → `toGeneration` with `jobId` = the gate `callId` (R-7 attribution unit = the call) plus a new optional `source?: "seat" | "gate"` discriminator (absent = seat, so persisted legacy records stay byte-identical). **Gate generations never enter the seat `generations` array** — `routedMultiset`, `totalCost`, and `providerComponentFigure` are seat-scoped and must not be polluted. Two control-flow changes: (a) the `jobs.length === 0 → null` early return becomes "no jobs **and** no callable gate ids"; (b) the `apiKey === null` early return still returns, but carries `gate.callsInWindow` so the legend survives a keyless run. A failed gate lookup (timeout/HTTP error) lands in `gate.failedLookups` and does **not** poison seat-scoped `report.status` — the ledger carries the cost, so nothing is unaccounted.
- **`extensions/usage-store.ts` (`flushPendingInvocations`)** — after `readManifests`, one `readGateLedger(input.repoRoot)` per flush pass; per pending invocation, window-filter `Date.parse(call.recordedAt) >= p.markerAt` and build `gateCalls`. The persisted `provider` sibling therefore carries `gate`, and the write-transition notify composes the block from the **persisted** record — shown == disk by construction. Import edge `usage-store → gate-ledger` is clean (gate-ledger imports only node + `CONFIG_DIR_NAME`). `USAGE_RECORD_SCHEMA_VERSION` stays 2 (additive-inside-provider; the EV-42 precedent).
- **`extensions/usage-block.ts`** — `GATE_LEGEND_LINE = "usage  gate = excluded from this total"`, pushed after `LEGEND_LINE` (last in the stack), iff `provider?.gate !== undefined && provider.gate.callsInWindow > 0`. No other row changes.

**Tests** (`test/gate-spend-reconcile.test.ts` new; additions to `usage-block.test.ts`, `usage-store.test.ts`; all mkdtemp + injected doubles, no network, no timers): ledger-fixture `sumGateSpend` = Σ reported per-call costs; injected fetch double receives the ledger-sourced `gen-dec-…` id exactly once and its `total_cost` equals both `gate.totalCost` and `sumGateSpend` over the same fixture; a seat transcript with ordinary `responseId`s triggers no decision lookup; window filter (call `recordedAt` < `markerAt` → no lookup, no legend); failed-call shape (legend present, zero lookups, `totalCost: null`, never an estimate); golden block tests — gate present ⇒ existing rows byte-identical + one gate line in the stack, gate absent ⇒ byte-identical to today's render (existing goldens stay green untouched); flush integration proving the persisted record carries `provider.gate`.

### 3. Open questions / risks

- **Legend predicate on failed calls.** The card's IFF is "at least one gate **call** falls in the window". A failed call (e.g. `no-api-key`) carries no `usage` — zero spend — yet the literal reading still emits "gate = excluded from this total". I take the literal reading (deterministic, ledger-derived, matches the acceptance text) but flag it: the alternative (key on calls with `usage.cost != null`) makes the legend a spend claim rather than a call claim. Worth a skeptic falsifier, cheap to flip.
- **Ledger `costBasis`.** The binding context says the ledger records cost "with costBasis REPORTED", but the PO-ruled v2 key set carries no `costBasis` field — the reported basis is structural. I read it as a statement of fact about EV-65's ledger, not a new field; if the judge reads it as a schema requirement, that's an escalation (a v2→v3 additive field), not something I'd decide unilaterally.
- **Cross-session attribution.** The ledger is repo-scoped and committed; another session's gate call landing inside this invocation's window would be included in the lookups. The legend is unaffected (still true), but the reconciliation's gate figure could over-attribute. No cheap fix exists (no session id on the call line); accepted and named, not silently ignored.
- **Runner form.** `formatRunnerUsageBlock` (hub-tools.ts) has no provider sibling and no legend stack. The acceptance speaks of the invocation usage block's legend stack — I scope to `formatUsageBlock`; extending the runner form is a separate concern I'd flag rather than grow this card.
- **`api_type: "decisions"`** is returned by the lookup but read by nothing today. I won't add a field for it; the `source: "gate"` discriminator plus the `gen-dec-` prefix is sufficient provenance. (If the judge wants it captured verbatim, it's one optional field.)

### 4. Falsifiable claims for the Skeptic

- **C1 (red-at-base):** `fetchProviderReport` accepts no `gateCalls` input — the injected-double test cannot exist at base. Record per the seven-field convention: base sha = current HEAD, transplant = the new test file alone, command `bun test test/gate-spend-reconcile.test.ts`, raw red = compile/type error naming the missing input.
- **C2:** with a v2 ledger fixture (`generationId: "gen-dec-123"`, `usage.cost: 0.0042`) the double is called with exactly `gen-dec-123` once, `report.gate.totalCost === 0.0042 === sumGateSpend(calls)` — the reported figure resolves into the gate total.
- **C3:** a seat transcript whose assistant entries carry ordinary `responseId`s yields **no** lookup of any `gen-dec-` id — gate ids enter the reconciler only through the ledger-sourced input (source pin: no gate-ledger import in provider-cost.ts).
- **C4:** a failed gate call in window → legend present, zero lookups, `gate.totalCost === null`, and no code path consults the catalogue for the gate model (source pin: no `catalogue.ts` import reachable from the gate-spend path).
- **C5 (golden):** block with gate present ⇒ every existing row byte-identical, exactly one new final line `usage  gate = excluded from this total`; block without gate calls ⇒ byte-identical to today's render.
- **C6:** gate call `recordedAt` before `markerAt` → no lookup, no legend.
- **C7:** gate generations ride a separate array — `routedMultiset`, seat `totalCost`, and `providerComponentFigure` outputs are unchanged when a gate component is present.
- **C8:** mixed v1/v2 ledger plus a v2 line with `usage: null` → `sumGateSpend` skips it (absent contributes nothing; no zero-derivation, no estimate).

**Tradeoffs accepted:** the lookup is verification telemetry, not the displayed total — the block gains a legend, not a gate cost row, so a reader who wants the gate dollar figure goes to the ledger (by design; the block's basis labels stay honest). `jobId` on gate generations carries the `callId` — a deliberate attribution overload, contained by the `source` discriminator. The window predicate is call-line-based, which emits the legend for a zero-spend (failed-call-only) window under the literal reading.


### Round 1 — principal

# Principal — EV-71 first pass

**Grounding read:** `vault/wiki/index.md`, `usage-accounting.md`, `spend-record.md`, `usage-block.md`, `usage-store.md`, `cost-provenance.md`; code on both sides — `extensions/provider-cost.ts`, `usage-store.ts`, `usage-block.ts`, `gate-ledger.ts`, `gate-run.ts`, `gate-transport.ts`, `gate-tool.ts`, `index.ts`. The wiki `metered-deliberation-routing.md` is stale ("Backlog") and did not help; I relied on the landed source.

## 1. What the goal requires

Four conjuncts, one of which names a seam and is the whole card:

1. The ledger line carries the decisions response's `usage.cost` with **reported** basis and the response's `generationId` (`gen-dec-…`).
2. The **provider reconciliation** accepts decision generation ids **sourced from the gate ledger** (in addition to transcript-harvested seat ids), proven with an injected fetch double: a `gen-dec-…` lookup's reported `total_cost` resolves into a **gate total**.
3. A seat transcript with no decision generation id triggers **no** decision lookup; no gate cost is ever catalogue-derived.
4. The usage block emits a **conditional** line `usage  gate = excluded from this total`, present iff ≥1 gate call is in the invocation window, composed into the existing prefix/label column/legend stack; golden test pins grammar identity and absence.

The card is explicit that the gate spend is **excluded from the invocation total** and "the ledger remains the authoritative gate total." So (2) is a *second* reconciliation and a *second* total, not a fold-in.

## 2. Cross-seam reading — what the contract actually is right now

**The ledger (writer side).** `gate-ledger.ts:63-67` — `GateUsage = {input_tokens, output_tokens, cost}`; there is **no `costBasis` field anywhere** on `GateLedgerRecord` (`gate-ledger.ts:79-130`). `gate-run.ts` success path appends `usage: parsed.usage` and `generationId: parsed.generationId` (from `gate-transport.ts`'s `parseDecisionsResponse`). The record is **repo-scoped and append-only** (`gateLedgerPath`, `gate-ledger.ts:135-137`) and carries **no invocation/marker/run key**; the only time field is `recordedAt`, a write clock. So "a ledger-sourced generation id" has no invocation-window membership built in — the card's phrase "falls in the window" has to be *defined*, and the only candidate field is `recordedAt`.

**The reconciliation (reader side).** `provider-cost.ts` is the single "provider reconciliation." `collectGenerationIds` (`:117-135`) harvests **only assistant `responseId`s** from session entries. `fetchProviderReport` (`:286-350`) filters `jobs` by `j.model.startsWith("openrouter/")` (`:296`), and a job with `sessionPath === null` pushes `session-missing` into `failures`, which makes `status: "unavailable"` (`:340`). `totalCost` is Σ generation `total_cost` (`:336-339`), and it is rendered as the invocation's `usage  reported …` row. `ProviderCostReport` also drives the `routed=` multiset and the `partial` predicate.

**The store (window + materialization).** `usage-store.ts` `flushPendingInvocations` (`:343+`) is where the window is known: `openrouterInWindow = manifests.filter(m => m.model.startsWith("openrouter/") && m.startedAt >= p.markerAt)` (`:417-420`), then `fetchProviderReport` (`:444`), then `persistInvocationUsage` (`:452`), then `notify(formatUsageBlock({record: res.record.spend, provider: res.record.provider}))` (`:466`).

**The block (surface).** `usage-block.ts` `formatUsageBlock` is pure, no `node:fs` (stated in its header, `:1-4`). Row stack: two measurement rows → boundary row → conditional reported row → conditional partial legend → conditional `n/a` legend.

**The intake seam.** `council_gate` is registered parent-side (`gate-tool.ts:130`) and invoked by `/features-new` after the invocation marker is stamped (`index.ts:167-178`), so gate calls *do* land inside a marker window — the intake invocation's, not the deliberation's.

### Invariants that must hold

- **INV-1 (no fold-in).** Gate spend must never reach `ProviderCostReport.totalCost`, `generations[]`, the `routed=` multiset, or `partial`. This is the card's own "excluded from this total" clause; folding also asserts a provenance the record does not have.
- **INV-2 (separate vehicle).** Gate ids cannot ride `fetchProviderReport`'s `jobs` array. Two independent hazards: (a) a gate pseudo-job with the pinned model `typesafe/jev-1.13` is **silently dropped** by the `openrouter/` prefix filter at `provider-cost.ts:296` — the exact silent under-report the card exists to kill; (b) forcing an `openrouter/` model to pass the filter, with `sessionPath: null`, pushes `session-missing` and flips the invocation report to `unavailable` (`provider-cost.ts:308-318, 340`), corrupting the seat report to account for the gate.
- **INV-3 (window predicate is a write-time fact).** The block is pure and fs-free, so "present iff ≥1 gate call in the window" must be materialized into `StoredUsageRecord` at flush, exactly as `provider.partial` is. A block that re-reads the ledger at render time violates the purity contract and the "what was shown == what is on disk" property (`usage-store.ts:459-466`).
- **INV-4 (no catalogue estimate).** If a call's `usage.cost` is absent and its `generationId` yields no `total_cost`, the gate total must name it unaccounted, never estimate. A costless/failed call (`gate-run.ts` `failRun` writes **no** `usage` and **no** `generationId`) is a real, reachable shape the acceptance does not name.

## 3. Blind spots by vantage

- **Provider-cost vantage (EV-29 author):** sees `fetchProviderReport` as *the* reconciliation and `jobs` as the extension point. Cannot see that its `sessionPath`-null and `openrouter/`-model gates encode "seat spend on OpenRouter models," so gate ids are either dropped (silent) or turn the seat report unavailable. Also cannot see that `api_type` is not part of `GenerationResponse` at all — the card's claim "the existing extraction reads exactly those fields" (listing `api_type: "decisions"`) is **false**; grep finds `api_type` only in the card text.
- **Ledger vantage (EV-61/65 author):** sees the call line as a complete record. Cannot see that it has no invocation key and that `recordedAt` is the write clock, so "a ledger-sourced id in the window" is undefined; the same repo ledger is shared across `/features-new` intake and later `/features-deliver`/`/council` invocations, and any window rule with no upper bound attributes one call to every invocation whose marker precedes it.
- **Usage-block vantage (EV-32 author):** sees a pure render over the persisted record. Cannot see that the card's conditional legend asks for a predicate the record does not yet carry — so "the block was not changed, only the store" is not available; the store must gain a sibling first.
- **Procedure/merge-check vantage (EV-69/70):** cannot see usage accounting at all; will not notice that the intake invocation is the honest attribution target for gate spend, or that `gen-dec-…` ids only ever originate from `council_gate`.

## 4. Reframe

**The proposed design is sound in shape, but the vehicle named by the goal is the wrong one, and this card earns a concrete reframe: reconcile gate spend as a second sibling parallel to `provider`, not as extra entries in the provider report.**

Concretely:

1. **A distinct gate-spend reconciliation** (new function, sharing `FetchGeneration` only) that reads `readGateLedger(repoRoot)`, selects calls by the pinned window predicate over `recordedAt`, looks up each `generationId` via `fetchGeneration`, and produces `{calls, totalCost, basis: "reported"}` — leaving `fetchProviderReport`, `ProviderCostReport`, and its `jobs`/`sessionPath`/`routed`/`partial` semantics byte-identical.
2. **A new sibling on `StoredUsageRecord`** (e.g. `gate?`), present iff the window contained ≥1 gate call; the block's conditional legend reads that sibling, exactly as it reads `provider.partial`.
3. **An explicit `costBasis: "reported"` on the ledger call line**, added to the v2 union additively (EV-65's reader is tolerant; `EV-66/67/71` read call-line fields). Otherwise "journalled with costBasis reported" has no machine-readable home, and INV-4 has no anchor.
4. **Pin the window predicate**: gate calls with `recordedAt >= markerAt` (no upper bound needed within a session, but the test must prove a prior invocation's call is excluded), attributed to the intake invocation that issued the call.

If the owner instead folds ledger ids into `fetchProviderReport`, INV-1/INV-2 break and the card's own acceptance contradicts itself. That is the falsifiable core of this position.

## 5. Open questions / risks

- **Q1 — Total precedence.** The card says both "the ledger's gate total equals the sum of reported per-call costs" and "the lookup's `total_cost` resolves into that total." When ledger `usage.cost` and generation `total_cost` disagree, which is authoritative? Ledger-word vs lookup-word gives different totals. Pin it.
- **Q2 — Legend slot.** "Composed with the existing prefix, label column and legend stack order" does not say whether `usage  gate = …` goes before or after the `partial` and `n/a` legends. The golden test will silently pick one; the ruling should pin it.
- **Q3 — Costless/failed calls.** A `failRun` line has neither `usage.cost` nor `generationId` (`gate-run.ts` `failRun`). Does the legend fire (a call happened) and is that call named unaccounted? "An unaccounted call whose cost is neither carried nor named" is the card's stated unacceptable, so the failed-call path needs a named disclosure or a scoped exclusion.
- **Q4 — Attribution command.** Gate calls happen inside `/features-new` intake; the legend will therefore appear on the intake invocation's block, not the deliberation's. The card's test language ("a seat transcript that carries no decision generation id") mixes the two windows. Name the target invocation in the spec.
- **Q5 — `api_type`.** The card asserts the existing extraction reads `api_type: "decisions"`. It does not; `GenerationResponse` has no such field. Either drop the assertion from the spec or add the field — but the spec must not claim a mechanism that is absent.

## 6. Falsifiable claims (for the Skeptic)

- **C1 — window predicate.** Marker at `t1`, ledger calls at `t0<t1` and `t2>t1`: the persisted gate total equals only the `t2` call's reported cost; the `t0` call contributes nothing. Red-at-base: no gate sibling exists.
- **C2 — no fold-in.** Same manifests, with vs without a ledger gate call: `ProviderCostReport.totalCost`, `generations.length`, and the `routed=` multiset are byte-identical; the only delta is the gate legend line. Breaks the reframe if the owner adds ledger ids to `jobs`.
- **C3 — naive fold-in is provably wrong (red at base).** Call `fetchProviderReport` with a job `{model: "typesafe/jev-1.13-…", sessionPath: null, ...}`: it is dropped by the `openrouter/` filter (`provider-cost.ts:296`) ⇒ zero lookup, silent under-report. Same job with `model: "openrouter/…"` and `sessionPath: null` ⇒ `status: "unavailable"`, `reason: "session-missing"`. Both assert current behavior; the card must route around it, not through it.
- **C4 — block purity.** Render `formatUsageBlock` from a record that lacks the gate sibling while a ledger file containing gate calls exists on disk: no gate legend is emitted. Proves INV-3 (predicate is persisted, not fs-read at render).
- **C5 — no catalogue estimate.** A ledger line with `usage.cost: null` and a `generationId` whose double returns `total_cost: null`: the gate total is unaccounted/`n/a`, never a positive catalogue-derived number; a source canary shows the gate-spend path imports no catalogue rate table or `calculateCost`.
- **C6 — `api_type` claim.** A `gen-dec-…` generation double returning `{total_cost: 1, provider_name: "x", api_type: "chat"}` is accepted identically to one with `api_type: "decisions"`, because the extraction never reads `api_type` (`grep api_type` over `extensions/` = 0 hits). If the design *requires* `api_type` to discriminate decisions generations, it must add it; the card's "existing extraction already reads those fields" is false as written.
- **C7 — transcript silence.** A seat-session harvest whose entries carry no `gen-dec-…` responseId issues zero calls to the decision lookup (spy on `fetchGeneration` call args: every id passed either comes from the ledger or is a non-decision transcript id; no `gen-dec-` id is ever synthesized from a transcript).


### Round 1 — designer

# Designer position — EV-71 (Decisions-aware gate spend accounting)

I am the `designer` seat on EPIC-13's EV-71. The card is surface-touching: one
new conditional legend line at the bottom of an existing block. I do not
implement and do not rule; I argue design before anything is built. The
grounding comes from `extensions/usage-block.ts` (existing legend discipline),
`extensions/usage-format.ts` (shared fragments), `extensions/gate-ledger.ts`
(the v2 call-time union), `extensions/provider-cost.ts` (the reconciler that
will now accept ledger-sourced `gen-dec-` ids), `vault/wiki/usage-block.md`,
`vault/wiki/cost-provenance.md`, `vault/wiki/figure-scoped-disclosure.md`,
and the EV-42 design / PO ruling pair
(`vault/raw/2026-09-16-design-ev42-partial-legend.md`).

---

## (1) How a person reads the block today, and what this legend changes

The resolved form a reader meets today is, with both conditional legends:

```
usage  ownSession  basis=session-reconciled  turns=N tokens=… cost≈$X.XXXX (catalogue)
usage  subtree     basis=stream-assistant   turns=N tokens=… cost≈$Y.YYYY (catalogue)
usage  boundary=session=… entries=… jobs=…
usage  reported  cost=$Σ.XXXX (reported) routed=<name>x<n>     ← conditional
usage  partial = reported figure excludes unaccounted attempts ← conditional
usage  n/a = provider figure unavailable                      ← conditional
```

A reader's eye-path is a **scan-and-land**: scan the two catalogue rows for
a number, land on `cost≈$… (catalogue)`, glance at the boundary row for
provenance, and decide whether the legends matter to them. The existing
legends work because they **qualify a figure the reader already saw**:
`partial` explains why a reported figure is short of whole; `n/a` explains
why a catalogue slot rendered `n/a`. Both legends have an antecedent on
the screen.

EV-71 adds one conditional legend:

```
usage  gate = excluded from this total         ← conditional, NEW
```

The legend does not qualify a figure the reader already saw. It
**annotates the block as a whole**: "by the way, the figures you just
scanned are not the whole story — gate spend is in the ledger, not in
these halves." Its work is therefore different from `partial` and `n/a`.
That difference is the load-bearing design fact and the source of every
comprehension hazard below.

A second change happens at the same time and is invisible on the surface:
the reported row's `routed=<multiset>` now includes the gate calls'
`gen-dec-…` ids alongside the seat transcripts' ids (the reconciliation
path is widened; the multiset field is unchanged in grammar). A reader
who counts entries in the multiset will see a higher count than they
would have before, with no per-row marker telling them which entries are
seat calls and which are gate calls. This widens a Gulf of Evaluation the
new legend only partially closes.

---

## (2) Comprehension predictions (falsifiable)

Each prediction names what a cold reader does, and the seam-test or smoke
that would falsify it.

**P1 — "this total" anchors to the wrong antecedent.** A reader who lands
on the legend without re-scanning the block will most naturally read
"this total" as the reported row's dollar sum (the most dollar-prominent
figure on the screen), not the catalogue halves. The intended antecedent
is the catalogue halves; the strongest visual antecedent is the reported
row. *Falsifier*: copy-string read-cold test — show a fresh reader the
resolved block and ask, without prompting, "what does 'this total'
refer to?" Record what they point at. A majority pointing at the
reported row falsifies the legend as written.

**P2 — the legend reads as boilerplate.** Tail-of-block annotations on
deterministic grammar look identical to legal disclaimers. A reader
already satisfied by the catalogue rows (their daily case) will skip it.
*Falsifier*: time-to-correct-recall — show the block, give the reader
60 seconds of unrelated work, then ask "what does the gate line say?"
If recall drops below the existing `n/a` legend's recall rate, the
position in the tail is the cause and a different surface is needed
(an inline note on the affected half, or a different key that does not
sound like a legal disclaimer).

**P3 — the legend leaves the reader stranded.** It says gate is excluded
from the total but does not say where the reader can find it. A reader
who wants to audit will look at the block, see no figure, and have no
signposted next step. *Falsifier*: a "find the gate spend" task — give
a reader the block, the repo, and the wiki; measure time-to-source. If
the median time exceeds the median for "find the `partial` reason in the
ledger," the legend is silently dropping information that needs a
signposted affordance.

**P4 — the legend reads identically whether gate ran once or many
times.** It does not distinguish a 1-call gate from a 12-call gate; it
does not say which mode the gate ran in (Deliberate / Verify / Direct);
it does not say whether any gate call failed. The reader gets a binary
"gate spend is excluded" with no granularity. *Falsifier*: a count-bearing
counterpart — does adding `usage  gate = N call(s) excluded from this
total` collapse the legend's grammar or break the conditional predicate?
If the legend stack can carry a count, the current literal is the
less-informative variant of the same shape.

**P5 — the legend will not render in the whole-block empty / unresolved
/ failed states.** The card's conditional predicate ("iff at least one
gate call falls in the window") is silent on what happens when the block
falls into `no usage recorded`, `accounting boundary unresolved`, or
`accounting failed`. If a gate call ran and the catalogue halves are
both zero, the whole-block state takes precedence today and the
gate-legend never appears — leaving the gate spend silently absent from
the visible surface, which is precisely the failure class the card
exists to prevent. *Falsifier*: a fixture where `allZeroUsage(ownSession)
&& allZeroUsage(subtree) === true` AND ≥1 gate call in the window — does
the rendered block include the `usage  gate = …` line? If no, the
card's intent ("What is not acceptable is an unaccounted call whose
cost is neither carried nor named") is violated in the edge case it
needs most. **This is the load-bearing prediction; it is the one I would
falsify first.**

**P6 — the legend is invisible to the reported row's multiset.** The
multiset shows provider names; the gate calls' `gen-dec-` ids route to a
`provider_name` that lands in the multiset alongside the seat calls. A
reader who looks at `routed=Infermaticx3,BlackForestx2` cannot tell which
entries are gate calls. The legend says gate spend is excluded from the
catalogue halves but is **not** excluded from the reported row — that
fact is nowhere on the surface. *Falsifier*: a "which entries in the
multiset are gate calls?" task. The legend as specified gives no
signposted affordance for this distinction.

**P7 — a first-time reader does not know what "the gate" means.** The
key `gate` is project-internal vocabulary (the EPIC-13 deliberation
gate). A cold reader who has never run `/council` against a metered repo
will not parse `gate` as a noun referring to a routing decision. The
existing `n/a` and `partial` keys are self-describing (`n/a` is a
typographic convention, `partial` is a property of the figure); `gate`
is the first key that requires external knowledge. *Falsifier*: a
read-cold test on the key in isolation — "what does `gate = …` mean
here?" A majority failing to identify it as the EPIC-13 deliberation
gate falsifies the key choice.

---

## (3) Composition concerns

**Stack order.** The existing discipline (PO C/D/E/F, EV-32;
`usage-block.ts:88-89, 116-119, 142-146`) is: ownSession → subtree →
boundary → reported → partial → n/a. Every legend is a **qualifier on a
figure already on screen**. EV-71's legend is **not** a per-figure
qualifier — it is a global annotation on the block. There is no
precedent for that shape in the existing stack.

The card says "composed with the existing … legend stack order," which
does not pin a position. Three defensible placements:

- **After `n/a`** (tail) — matches the "legends at the bottom"
  convention; reads as a closing caveat. Risk: it is far from the figures
  it qualifies, and P2's boilerplate trap bites.
- **Before the reported row, after the boundary row** — anchors it
  physically to the catalogue halves it qualifies ("these two exclude
  gate; the figure below is what the provider reports, including gate").
  Breaks the convention that legends only stack at the bottom. Risk: the
  boundary row is currently followed by a conditional row; inserting a
  legend there is the first mixed-position legend.
- **After the reported row, before partial** — couples it to the
  reported figure but inverts the natural reading order ("here is the
  total … and gate is excluded from it … and by the way the total is
  partial").

I argue for **after `n/a`** as the position of least surprise: it keeps
the legend-stack convention intact, costs no rule change, and makes the
legend the last thing a reader sees. P1 (wrong antecedent) is the price
of that choice; the legend's wording, not its position, is what makes the
antecedent ambiguous.

**Label column.** The existing convention is `usage  ` (literal `usage`
+ two ASCII spaces) + `<key>` (no padding) + ` = ` + `<short definition>`.
The proposed line `usage  gate = excluded from this total` follows this
exactly. Compositionally clean. The label column width is **not** fixed
(the partial legend's key is `partial`, 7 chars; the n/a key is `n/a`,
3 chars; `gate` is 4 chars). The proposal sits in the middle. No issue.

**Prefix grammar.** `usage  ` is the prefix of every line in the block,
including the conditional legends. The proposed line uses it. No issue.

**"key = short definition" shape.** The proposed definition is **12
words** ("excluded from this total"). Compare:

- `n/a = provider figure unavailable` (4 words, the noun phrase)
- `partial = reported figure excludes unaccounted attempts` (6 words)
- `gate = excluded from this total` (4 words)

Word count is fine, but **shape** differs: `n/a` and `partial` both
name the property being qualified; `gate = excluded from this total`
names a verb phrase describing an action on an unnamed antecedent. The
definition reads as a sentence fragment rather than a definition. P1 is
the surface symptom.

**Conditional predicate.** "Present iff at least one gate call falls in
the window" matches the existing legend discipline (PO D/E):
`input.X === <state>` ⇒ legend; otherwise silent. The block does not
need a counter — `count > 0` is sufficient. P4's prediction (1 call vs
12 calls reads identically) is a follow-on question, not a grammar
violation.

**Golden test (acceptance).** The card commits to a golden test that
proves the block's grammar is unchanged when gate calls are present and
no legend is emitted when they are absent. I would add three more
golden assertions to the same file:

- A fixture where ≥1 gate call AND the block is in
  `no usage recorded` state (whole-block precedence). Asserts whether
  the gate legend renders in a whole-block state — see P5, which the
  acceptance does not cover.
- A fixture where ≥1 gate call AND `n/a` legend already renders.
  Asserts the **exact stack order** of `partial` → `n/a` → `gate`
  (or whatever position the card settles on).
- A fixture where ≥1 gate call AND no reported row (no `provider`
  sibling). Asserts the gate legend renders without a reported figure
  — the catalogue halves are the only thing the legend qualifies, and
  the legend must work in that case.

---

## (4) Open design questions I believe need a ruling or a test

**O1 — Whole-block state precedence (load-bearing).** Does the gate
legend render in the `no usage recorded`, `accounting boundary
unresolved`, or `accounting failed` states when ≥1 gate call falls in
the window? The card's conditional predicate says "present iff at least
one gate call falls in the window," which on its face covers those
states. The existing whole-block-state precedence (failed > unresolved
> empty, `usage-block.ts:108-114`) returns before any conditional row.
A gate call in an unresolved-boundary invocation would today be silently
absent from the surface, which violates the card's intent. I argue the
gate legend must render in the `no usage recorded` state (the empty
state) when ≥1 gate call is in the window — that is the only state
where the legend is **both** conditional AND not preceded by other
content. The unresolved and failed states are harder — the legend
would have to render after the whole-block line, breaking the
single-line whole-block-state rule. *Recommendation*: a PO ruling
fixing this; failing that, a test that pins the behavior so the
question is not silently decided at implementation time.

**O2 — Antecedent for "this total".** The wording is ambiguous between
(a) the catalogue halves (ownSession/subtree, the figures most
prominently scanned) and (b) the reported row (the dollar figure with
the strongest visual weight). The card's Intent makes clear the
intended antecedent is (a), but the literal text reads more naturally
as (b) for a cold reader. *Recommendation*: the wording is settled by
the card; the falsifier for P1 should run on the literal as given. If
P1 is confirmed (majority of cold readers point at the reported row),
I would push back to the card, not to the implementer.

**O3 — Where can the reader find the gate spend?** The legend names the
exclusion but does not point at the ledger. A reader who wants to audit
must already know about `gate-ledger.jsonl` (and the
`extensions/gate-ledger.ts` shape) — that knowledge is not in the
block. Two design responses: (a) extend the legend to name the ledger
("`gate = excluded from this total — see gate-ledger.jsonl`"), which
widens the definition past the "short definition" shape; or (b) accept
that the block is not the audit surface — the ledger is — and do not
extend the legend. *Recommendation*: (b) on the grounds that the
reported row's multiset already surfaces the gate calls' provider
names and ids, and a reader who follows the multiset gets to the
ledger. The legend's job is the **exclusion**; the multiset's job is the
**trace**. I would write this into a one-line note in the implementation,
not into the legend.

**O4 — Reported-row multiset composition.** The multiset
(`usage-block.ts:65-78`) currently aggregates `providerName × count`
over `status === "reported"` generations from seat-transcript
harvesting. EV-71 widens the input set to also include ledger-sourced
`gen-dec-` generations. The multiset's count will increase with no
signifier distinguishing seat calls from gate calls. The block's reader
sees a higher count and may form a wrong model of "how many provider
calls this invocation made." *Recommendation*: a downstream test
(not this card) that pins a count assertion comparing pre-EV-71 and
post-EV-71 multiset counts on a fixture with ≥1 gate call. The card's
acceptance does not require this, but the prediction is falsifiable.

**O5 — Runner form.** `formatRunnerUsageBlock` (`usage-block.ts:153-161`)
is used at the runner's own autonomous exit. Does the runner invoke
the gate? If yes, the gate legend must also be added to the runner
form's conditional stack — and the runner form has **no** conditional
legend stack today (the boundary row is composed directly). If no,
the runner form is unaffected. *Recommendation*: a one-line ruling from
PO: does the runner ever emit a gate call? If yes, EV-71 must touch
both forms; if no, the runner form is out of scope and a test should
pin "runner invocation never issues a gate call" as an upstream
property.

**O6 — Advisory-mode gate calls.** EV-65 wrote `advisory: true` onto
the ledger. An advisory gate call still spends tokens (the decisions
endpoint is still called). The card's predicate "at least one gate
call falls in the window" naturally includes advisory calls. *No
design issue*, but a fixture should pin this so a later implementation
choice does not filter advisory calls out and silently under-report
again.

**O7 — The literal text vs the wording alternatives.** I considered
and rejected the following before arguing for the card's literal as
given:

- `usage  gate = not in ownSession or subtree` — too algebraic; breaks
  prose shape; widens the label column.
- `usage  gate = catalogue totals exclude gate spend` — narrows the
  antecedent correctly but reads as a sentence, not a definition.
- `usage  gate = ledger-only (see <path>)` — too long; the ledger
  path is environment-specific.
- `usage  gate = accounted in ledger, not in halves` — describes the
  mechanism, not the block's exclusion.

The card's literal is the cleanest of the alternatives that preserve
the "short definition" shape. P1 is a real comprehension hazard but is
not resolvable inside the legend without widening the definition past
its current word count.

---

## Summary

The surface change is small and the legend discipline is honored. The
card closes a real Gulf of Evaluation — the **silent absence of gate
spend from the visible block** — with a tail-of-block annotation. The
load-bearing risks are P1 (wrong antecedent), P5 (whole-block-state
precedence swallowing the legend), and P3 (no signposted affordance
for finding the gate spend). P5 is the only one I would not let
through to implementation without an explicit ruling, because it
silently reproduces the failure class the card exists to prevent in
exactly the edge case the card needs most: a gate call on a
no-other-spend invocation.

I have not changed any application code or branch state; this is a
position, not an implementation. I yield to `consolidator` (or to
`owner` if no other seat has weighed in this round).

### Round 2 — owner

Grounding verified before taking stance: `grep api_type` over `extensions/` and `test/` → **0 hits** (only `council/cards/EV-71.md:20`); `GenerationResponse` (`provider-cost.ts:93-101`) carries `total_cost`/`provider_name`/native tokens only; `formatUsageBlock`'s whole-block states (`usage-block.ts:105-107`) return `UNRESOLVED_LINE`/`NO_USAGE_LINE` **before any conditional row**; `GateLedgerRecord` has no `costBasis` anywhere; `failRun` (`gate-run.ts:185-191`) writes neither `usage` nor `generationId`.

---

## Owner round-2 position — EV-71

### Delta 1 — Vehicle: **concede to the principal.**

My nested `gate?` inside `ProviderCostReport` has a hidden cost I under-weighted: to make the legend survive keyless and no-seat-job states, my design forced `ProviderCostReport` to *exist* in states where EV-29's grammar previously had it null or absent (I even had to mutate the `jobs.length === 0 → null` early return). That changes the persisted record's shape for seat-only invocations' neighbors — exactly the kind of grammar drift the golden tests would then have to defend. The principal's vehicle — a separate gate-spend reconciliation function sharing only `fetchGeneration`, producing a top-level `gate?` sibling parallel to `provider` — keeps `fetchProviderReport`, `ProviderCostReport`, `jobs`, `sessionPath`, `routed`, and `partial` **byte-identical**, which converts their INV-1/INV-2 hazards from "test-enforced" to "structurally inapplicable." It also dissolves the designer's P6/O4 (multiset widening) by construction: gate generations never touch `ProviderCostReport.generations`, so the reported row's multiset is provably unchanged. The designer's third golden case (gate legend with **no** `provider` sibling) falls out naturally instead of needing my contortions.

What I keep from my r1: the store-side wiring — one `readGateLedger` per flush pass, window filter `recordedAt >= markerAt`, the gate fact materialized into the persisted record at flush, block composed from the persisted record (principal's INV-3, "shown == disk"). Both designs agree on that.

### Delta 2 — `costBasis`: **concede to the principal, with one pin.**

The card goal is the judge's contract; the principal's reading gives it a checkable artifact, mine gives only a negative assertion across the whole codebase ("nothing computes it"), which is expensive to prove and cheap to break. Pin I add: the field is **additive within v2** — no schema bump, the v2 writer emits `costBasis: "reported"`, the reader tolerates its absence on pre-field v2 lines, and `sumGateSpend` is **not gated on the field** (an old v2 line's `usage.cost` is still reported-basis structurally; skipping it would re-create silent under-report). The field is disclosure and acceptance hook, not a summation filter.

### Delta 3 — `api_type`: **principal is factually right; tolerate-and-ignore, plus a spec correction.**

Verified: zero hits in `extensions/` and `test/`; the only occurrence is the card's own text. `GenerationResponse` has no such field and `toGeneration` never reads it. My r1 said "tolerated/ignored — no extraction change," which is the correct *behavior*, but I echoed the card's false premise while doing it. Stance: **do not add the field** — a field nothing reads is dead surface and violates minimal implementation. The card's sentence claiming the existing extraction reads `api_type: "decisions"` is false as written and needs a **spec correction at merge-check** (restate as: the decisions response may carry `api_type`; the existing transport ignores it; decisions provenance rides the `gen-dec-` ledger ids and the source of the lookup input). The principal's C6 test pins that a double with `api_type: "chat"` is accepted identically to `"decisions"`.

### Delta 4 — Whole-block states and the failed-call predicate: **designer's P5 is real and load-bearing; here is my per-state stance.**

Verified mechanically: `formatUsageBlock` returns `UNRESOLVED_LINE` and `NO_USAGE_LINE` before any conditional row, so a gate call in an all-zero-halves invocation renders **no gate legend today** — the card's core failure class (silent unaccounted spend) reproduces in the exact state that matters most: an intake invocation whose only spend is the gate call.

- **`no usage recorded` (empty):** the gate legend **must render**, stacked after the state line, when ≥1 gate call is in window. This breaks the single-line whole-block rule — first multi-line whole-block state — but the rule's purpose is honesty, and the honest render here is "no seat usage recorded" *plus* "gate = excluded from this total." The alternative is the silent absence the card exists to kill. Worth a PO ruling (designer's O1); failing that, the golden test pins it so it isn't decided silently at implementation time.
- **`accounting boundary unresolved`:** same treatment — legend after the unresolved line. An unresolved invocation's gate call is equally unrecoverable later, because the window predicate (`recordedAt >= markerAt`) means the *next* invocation's flush will not re-include it: a gate call missed at this flush is permanently off the surface.
- **`accounting failed`:** **stays single-line.** The failure line is itself the disclosure (nothing in that state is named — seat spend included); the ledger carries the cost. I distinguish this from empty/unresolved because the failed input carries no persisted record at all — there is nothing to compose the legend from, and inventing one would fabricate a record shape.

On my failed-call flag (zero-spend call): I **retain the literal reading** — the legend is a **call-claim**, not a spend-claim. Under the converged design + P5 stance, the intake invocation whose only gate call failed renders `no usage recorded` + `usage  gate = excluded from this total`. That is the designer's P5 fixture and my C4/C8 fixture converging on one test.

### Delta 5 — Legend slot: **confirmed, after `n/a` (last).**

Stack order: `ownSession → subtree → boundary → reported (cond) → partial (cond) → n/a (cond) → gate (cond, last)` — and, per Delta 4, in whole-block states: `state line → gate`. Golden assertions that pin it:

1. Gate + partial + n/a all present → exact full-block string equality with the 4-line tail in order (pins the slot absolutely).
2. Gate + n/a, no partial → `n/a` then `gate` (pins relative order against the other tail legend).
3. Gate + all-zero halves → `no usage recorded` then `gate` (pins the new multi-line whole-block state; P5).
4. Gate absent → byte-identical to today's render (existing goldens stay green untouched).

### Remaining open items needing a ruling

- **R-A (O1, from designer):** the multi-line whole-block state is a rule change — PO ruling recommended; my per-state stance above is the proposal.
- **R-B (principal's Q1):** on ledger-vs-lookup disagreement, the **ledger is authoritative** for `gate.totalCost`; the lookup equality is the acceptance property; runtime disagreement lands record-only in the gate sibling (e.g. an unaccounted/discrepant count), never the render. Flag for the judge whether discrepancy needs surface disclosure.
- **R-C (api_type):** spec correction to the card text; no field added.
- **R-D (designer's O3/O5):** accept (b) — the block is not the audit surface, the ledger is; no signpost in the legend. Runner form untouched: `council_gate` is parent-side registered (`gate-tool.ts:130`), so the runner never issues a gate call — pin as an upstream property test, not a code change.
- **R-E (principal's Q4):** attribution target is the intake invocation; the card's test language mixing seat-transcript windows should be read as "no seat transcript id ever triggers a decision lookup," which the separate vehicle makes structural.

### Falsifiable claims for the Skeptic (converged C-list)

- **C1 (red-at-base, seven-field record):** no `gate` sibling on `StoredUsageRecord` and no gate-reconciliation function — the persisted-sibling test cannot exist at base; command `bun test test/gate-spend-reconcile.test.ts`.
- **C2:** v2 ledger fixture, `gen-dec-…` lookup double: the separate reconciler receives the ledger-sourced id exactly once; `gate.totalCost === sumGateSpend(calls) === double.total_cost`.
- **C3:** `fetchProviderReport` + `ProviderCostReport` byte-identity: running with vs without gate input produces identical seat-side outputs (fold-in impossible, not just prevented).
- **C4 (P5, load-bearing):** all-zero halves + ≥1 gate call in window → rendered block is `no usage recorded\nusage  gate = excluded from this total`. Red at base (today renders one line, no legend).
- **C5:** failed gate call (no `usage`, no `generationId`) in window on an otherwise-spending invocation → legend present, zero lookups, no catalogue import reachable from the gate path.
- **C6:** `api_type` acceptance-identity — a double with `api_type: "chat"` resolves identically to `"decisions"`; `grep api_type extensions/` = 0.
- **C7 (window):** calls at `t0 < t1` and `t2 > t1` around marker `t1`: only `t2` contributes to the persisted gate total; a prior invocation's call is excluded.
- **C8:** mixed v1/v2 ledger, pre-field v2 line, `usage: null` line → sum counts only finite `usage.cost` values; `costBasis`-absent old lines still count (the field is not a summation filter).
- **C9 (block purity, INV-3):** ledger file on disk with gate calls + record lacking the `gate` sibling → no legend. The predicate is persisted, never fs-read at render.
- **C10 (golden, delta 5):** the four golden assertions above, including gate-absent byte-identity.

**Tradeoffs accepted:** the multi-line whole-block state is a real precedent break, bought to avoid the card's own failure class in the state where it bites hardest; the gate sibling carries record-only disclosure counts (failed lookups, discrepancies) with no render, leaving the block's literal unchanged; and `costBasis` creates within-v2 field heterogeneity that the reader must tolerate — the price of a machine-checkable basis claim without a schema bump.


### Round 2 — principal

# Principal — EV-71 round 2

Grounding re-read for this round: `extensions/provider-cost.ts` (early returns `:296-300`, seat-array status `:343-345`, `toGeneration` `:246-268`), `extensions/usage-block.ts` (`:110-119` unavailable→subtree n/a; `:133-147` whole-block precedence; `:159-161` runner form), `extensions/usage-store.ts` (`:61-75` `StoredUsageRecord`, `:443-470` flush), `extensions/gate-ledger.ts` (`GateUsage` v2, `appendGateCall`), card `council/cards/EV-71.md`; wiki `usage-block.md`, `cost-provenance.md`, `figure-scoped-disclosure.md`.

## Cross-seam reading — what the contract is now

The owner's refined design is materially different from the naive fold-in I objected to in R1. I verified it against both sides:

- Gate ids enter via a **separate optional input** (`gateCalls`), looked up **after** the seat-job loop, and land in a **separate `GateProviderReport`**; seat `generations[]`, `totalCost`, `routedMultiset`, `partial`, and `status` are untouched. That is INV-1 and INV-2 respected. The two hazards I named are avoided: the `openrouter/` prefix filter (`provider-cost.ts:296`) never sees a gate pseudo-job, and no `sessionPath: null` gate entry can push `session-missing` and flip the seat report (`:308-318, 340-345`).
- **So INV-1/INV-2 no longer stand against the refined design — only against the naive fold-in, which the owner is not doing.** I concede that plainly.

The two designs now differ in exactly one place: **where the gate result is persisted**. The owner nests `gate` inside `ProviderCostReport` (`StoredUsageRecord.provider.gate`); I argued a top-level `StoredUsageRecord.gate` sibling parallel to `provider`.

That is not a taste difference. Two concrete consequences follow from nesting, both grounded:

1. **A gate-bearing provider report poisons the seat half.** `usage-block.ts:116-119` marks the subtree half `cost=n/a` whenever `input.provider.status === "unavailable"`. The owner's changed early return (`jobs.length === 0 → null` becomes "no seat jobs **and** no callable gate ids") means a gate call in the window with **no** OpenRouter seat job (the `/features-new` intake case) produces a non-null report whose seat `generations` is `[]` ⇒ `status: "unavailable"` (`provider-cost.ts:343-345`). The block then renders `subtree cost=n/a` and the `n/a` legend, even though the subtree's catalogue cost is a real, measured number. Today a no-seat-job invocation returns `null` (`:297`) and renders that cost. Nesting introduces a false `n/a` — the precise "plausible-but-false assertion" class the card's Intent invokes.
2. **`null` collapses two different facts, and the legend predicate loses one.** The owner's legend predicate is `provider?.gate !== undefined && provider.gate.callsInWindow > 0`. A window containing only a failed gate call (`gate-run.ts` `failRun`: no `usage`, no `generationId`) has **zero callable ids** ⇒ the changed early return still returns `null` ⇒ no `provider` ⇒ **no legend**, despite a gate call in the window. That contradicts the owner's own literal reading of the card's IFF ("present iff at least one gate **call** falls in the window") and the owner's own stated choice to key on calls, not spend.

Neither defect is expressible through a top-level sibling: `record.gate` is set by callsInWindow independently of `provider`, and `provider` stays `null` when no seat job ran, so the seat half is untouched.

I accept the **input edge** on `fetchProviderReport` (`gateCalls?: …`) — the card's wording ("the provider reconciliation accounts for gate spend by accepting decision generation ids") plausibly demands it, and it's a clean seam. My disagreement is only the **output/persistence location and the `status` coupling**. Concretely: the gate reconciliation can live behind `fetchProviderReport`'s input while its result is returned and persisted as a sibling of `provider`, not inside it.

## Blind spots

- **Owner (provider-cost vantage):** sees `fetchProviderReport` as the reconciliation and the natural extension point, and reads "no eligible seat job" and "no gate call" as one condition (both are "nothing to reconcile"). Cannot see from inside `provider-cost.ts` that `usage-block.ts:116-119` converts `status: "unavailable"` into a **subtree-half disclosure**, so attaching gate data to that report silently re-scopes an EV-29 surface it doesn't own.
- **Designer (surface vantage):** sees the legend as a pure function of persisted record state and correctly identifies P5/O1, but cannot see from inside `usage-block.ts` that the predicate source is the owner's `provider?.gate` — so the whole-block-state fix it argues for is *unimplementable* under nesting when no seat job ran (`provider` is `null` and `provider?.gate` is `undefined`). P5 and delta-1 are the same bug seen from two sides.
- **My own blind spot, named:** I cannot overrule the card's wording that names the provider reconciliation as the acceptor. If the judge reads that literally as "the gate result must be part of `ProviderCostReport`," my sibling defence loses and the owner's nesting wins with the two fixes below (or a v-tagged sub-field). I flag that as the ruling this hinges on.

## Reframe

**The proposed design is sound; no reframe of the vehicle beyond what I state below.** Concretely, my converged position:

1. **Vehicle.** Concede the separate reconciliation, the `gateCalls` input edge, and gate isolation from seat `generations`/`totalCost`/`routed`/`partial`. **Defend one change:** persist as a top-level `StoredUsageRecord.gate` sibling, present iff `callsInWindow > 0`, independent of `provider`; the block reads `record.gate`, not `provider.gate`. If the judge binds "part of the provider reconciliation," the minimum acceptable fallback is (a) the report's `status` must be computed over seat generations only and (b) the legend predicate must be derivable when there is no seat report — both of which the sibling gives for free.
2. **`costBasis` field — concede the owner: no field.** The goal's phrase describes the basis as structural (the single writer `appendGateCall` stores `usage.cost` verbatim, computes nothing) — not a schema key. Every gate line would carry the identical constant, so a `costBasis` field discriminates nothing and is not read by any acceptance. Adding one is a v2→v3 escalation the card does not require; **do not add it.** If the judge reads the wording as a schema requirement, that is an escalation, not an owner-unilateral. (This is me withdrawing R1 Q-point 3 and INV-4's field anchor.)
3. **Q1 precedence — accept.** Ledger `usage.cost` is authoritative; the lookup is verification. But keep them **distinct fields** on the reconciliation result: ledger Σ (`sumGateSpend`) and lookup Σ (`gate.totalCost`). A verification that cannot disagree is not a verification; the mismatch must be observable, even if no surface renders it.
4. **Q2 legend slot — accept after `n/a`.** The legend is a whole-block annotation, not a per-figure qualifier; tail placement preserves the legend-stack convention, and it is also the only placement consistent with P5 (the legend must render even when neither `partial` nor `n/a` is present).
5. **P5/O1 — take a stance: the legend renders in the `no usage recorded` and `accounting boundary unresolved` states when `record.gate.callsInWindow > 0`**, appended after the whole-block line. The `{ failed }` variant carries no record (`usage-block.ts:fine-107`), so it cannot carry the legend — and should not: the failure is the accounting write, not the gate. This does relax the one-line whole-block-state rule, which is why it needs an explicit ruling rather than an implementer's coin-flip.
6. **Q4 attribution — accept the owner's predicate**, with the spec sentence pinned: the legend renders on the invocation whose marker window contains the gate call's `recordedAt` — currently the `/features-new` intake invocation. The lower-bound predicate (`recordedAt >= markerAt`) naturally excludes *earlier* calls from *later* invocations; only concurrent-session over-attribution remains, and it is accepted-and-named.
7. **Q5 `api_type` — minimal correct treatment: correct the spec sentence, add no field.** `api_type` appears nowhere in `extensions/`; `GenerationResponse` (`provider-cost.ts:99-113`) does not carry it and `toGeneration` never reads it. The provenance discriminator is `gen-dec-` + ledger sourcing. The double should return `api_type` and the test must prove behavior is identical with and without it.

## Testable claims

- **T1 — nesting masks the seat half (falsifies the owner's current persistence shape).** Fixture: gate call in window, no OpenRouter seat job, no `OPENROUTER_API_KEY`; call `formatUsageBlock` on the persisted record. Assert: subtree does **not** render `cost=n/a`. Current owner design fails (produces `subtree cost=n/a` via `usage-block.ts:116-119`); the top-level sibling passes.
- **T2 — gate-only, failed-call window still renders the legend.** Fixture: one window `GateLedgerRecord` with `failure.class` set, no `usage`, no `generationId`, no seat jobs. Assert `usage  gate = excluded from this total` is present. Current owner design fails (`null` early return ⇒ predicate false).
- **T3 — P5/O1 empty state.** Fixture: `allZeroUsage(ownSession) && allZeroUsage(subtree)`, `record.gate.callsInWindow === 1`. Assert the render contains `NO_USAGE_LINE` **and** the gate legend. Red at base (legend absent).
- **T4 — Q1 precedence.** Double returns `total_cost = 9.99` for a ledger line whose `usage.cost = 0.0042`. Assert the authoritative total is `0.0042`, and the `9.99` lookup value remains observable as the lookup sum (not overwritten).
- **T5 — Q5 `api_type` is inert.** `gen-dec-…` double returning `{total_cost: 1, provider_name: "x", api_type: "decisions"}` and one with `api_type: "chat"` produce byte-identical reconciliation output. `grep api_type extensions/` = 0 hits.
- **T6 — stack order and absence.** With gate + `partial` + `n/a` all present, assert the exact order `… partial, n/a, gate`; with `gate.callsInWindow === 0`, assert byte-identity to today's render.

**Remaining items needing a ruling, not an implementation choice:** (i) is the gate result a sibling of `provider` or a field inside it (T1/T2 decide which is correct, but the card's wording decides which is permitted); (ii) whether the whole-block-state single-line rule yields to the card's IFF (T3). Both are PO questions; I would not let either be resolved by whoever writes the golden test first.


### Round 2 — designer

Confirmed: `formatUsageBlock` (lines 100-128) returns whole-block-state lines **before** any conditional row is built. A gate call in a `no usage recorded` window today returns `NO_USAGE_LINE` alone. My P5 stands as load-bearing.

---

# Designer position — EV-71, round 2

I have read `/tmp/ev71-owner-r1.md` and `/tmp/ev71-principal-r1.md`. I respond to each of the chair's deltas, concede what the design surface resolves, and press what is still open. I do not rule on the owner's vs principal's vehicle dispute (gate as `provider.gate` sibling vs top-level `gate?` peer); that is `principal`'s seat and the surface contract is identical either way.

## What I concede

**P6 resolves.** Owner R1 §2 is explicit: "Gate generations never enter the seat `generations` array — `routedMultiset`, `totalCost`, and `providerComponentFigure` are seat-scoped and must not be polluted." Principal R1's INV-1 corroborates from the other direction ("gate must never reach `routed=`"). The reported row's multiset is unchanged when gate calls are present. The Gulf of Evaluation concern I named — "reader sees a higher count with no per-entry marker for which are gate calls" — is closed.

**Stack order after `n/a` is confirmed.** Owner R1 §2 agrees. The final stack reads:

```
usage  ownSession …
usage  subtree    …
usage  boundary   …
usage  reported   …      ← conditional on provider.reported
usage  partial    …      ← conditional on provider.partial
usage  n/a        …      ← conditional on provider.unavailable
usage  gate       …      ← conditional on provider.gate.callsInWindow > 0   ← NEW
```

**P4 (count-bearing legend) and P7 (key vocabulary) are follow-up cards, not fold-ins.** The card's literal `usage  gate = excluded from this total` is fixed by the goal; the judge reads the goal. Adding `N call(s)` or renaming the key changes the spec, not the implementation. Both ship as separate cards if at all.

**P1 (wrong antecedent) is settled by the literal.** The card's wording is fixed. The falsifier for P1 (a read-cold test on "this total") would test the literal as given. If a majority of cold readers point at the reported row, the push-back is to the card author, not the implementer.

**O3(b) is my final word for this card.** Owner R1's design (no gate cost row, ledger is the audit surface) plus principal R1's INV-1 (no fold-in) lock this in. The legend's job is the **exclusion**; the ledger is the **trace**. The literal does not widen to name the ledger path. **But:** P3 sharpens rather than closes. With P6 resolved, the gate legend is the **only** signposted surface that gate calls happened in the window. A reader who skips the legend (P2's boilerplate trap) gets zero signal. I am not changing my position — the card's literal is fixed — but the follow-up card (a real audit affordance) is now more urgent than I named in R1. I want a one-line note in the implementation's docstring or wiki that names "block = exclusion surface, ledger = trace surface" so the next iteration knows where to add the affordance. That is a documentation ask, not a code ask.

## What I press

**P5 / O1 — whole-block state precedence (still open, load-bearing).** Owner R1 did not address this. The card's predicate "present iff ≥1 gate call falls in the window" on its face covers whole-block states. But `formatUsageBlock` (`extensions/usage-block.ts:100-128`) returns the whole-block-state line **before** any conditional row is built — three early returns at lines 102, 105, 107. With current behavior, a gate call in an empty window renders only `usage  no usage recorded` with no legend. That is precisely the silent under-report the card exists to prevent.

Crisp, decidable proposal — what each whole-block state should render when ≥1 gate call is in the window:

| Whole-block state | Without gate calls | With ≥1 gate call in window |
|---|---|---|
| `no usage recorded` | single line `usage  no usage recorded` | **same line + gate legend** |
| `accounting boundary unresolved` | single line `usage  accounting boundary unresolved` | **same failure line + gate legend** |
| `accounting failed` | single line `usage  accounting failed — <reason>` | **same failure line + gate legend** |

**What I insist on:** the `no usage recorded` row when ≥1 gate call is in the window. This is the load-bearing edge case: the reader sees a single line saying nothing was recorded, no other figure to anchor against, and the card's intent — "an unaccounted call whose cost is neither carried nor named" — is violated in exactly the case the card needs most. A block whose only content is "nothing happened" alongside a gate call that *did* happen is the silent-under-report failure class itself.

**What I accept as a test-pin (not a ruling):** the `unresolved` and `failed` states. Argument: a reader who lands on a failure line is already being told accounting broke; the gate legend is a secondary signal. If implementation pins "failure states silently swallow the gate legend," that is acceptable **only if the test explicitly asserts the chosen behavior with a named decision**, not letting it slip in incidentally. (I prefer the legend-present reading on principled grounds — the failure line names that seat/catalogue accounting broke, not that gate accounting broke — but I would not block on it.)

**Either a PO ruling or a named-decision golden is needed before implementation.** Without one, the load-bearing edge case is decided by whichever implementation detail lands first.

## Falsifiable predictions (golden assertions for Skeptic)

**G1 — Stack order, all conditions normal.** Fixture: ≥1 gate call + reported present + no `partial` + no `n/a` → assert exact byte-identity of existing six rows **plus** the seventh row `usage  gate = excluded from this total`. Falsifier: any character drift on rows 1–6, or stack order other than `… → reported → gate` (no partial, no n/a), fails.

**G2 — Stack order with `partial` legend.** Fixture: ≥1 gate call + `partial: true` → assert stack order is `… → reported → partial → gate`. Pins `gate` after `partial`.

**G3 — Stack order with `n/a` legend only.** Fixture: ≥1 gate call + `unavailable: true` (no `partial`) → assert stack order is `… → n/a → gate`.

**G4 — Gate legend without reported row.** Fixture: ≥1 gate call + the `provider` sibling lacks `reported` (no `totalCost`, no `routed`) → assert the gate legend **still renders**. The legend qualifies the catalogue halves, not the reported figure; if it disappears when `reported` is absent, the antecedent collapses and P1 (wrong antecedent) bites in a new way — the reader gets a legend whose referent is no longer on screen.

**G5 — No-gate byte-identity.** Fixture: ledger contains gate calls but window contains zero (all calls have `recordedAt < markerAt`) → assert byte-identity with today's render. Existing goldens stay green untouched. Falsifier: any drift on rows 1–6 fails.

**G6 — Empty state + gate legend (P5 pin, load-bearing).** Fixture: zero catalogue halves + ≥1 gate call in window → assert the block is `usage  no usage recorded\nusage  gate = excluded from this total`. **Falsifier: missing the second line fails.** This is the load-bearing golden.

**G7 — Failure state + gate legend (P5 pin, scoped).** Fixture: `accounting boundary unresolved` + ≥1 gate call → assert the rendered block; either pin "gate legend follows the failure line" or pin "gate legend absent" with a `// Decision:` comment naming the choice. The same fixture for `accounting failed`. This makes the choice auditable rather than incidental.

**G8 — Whole-block returns stay single-line without gate.** Fixture: zero catalogue halves + zero gate calls → assert the block is exactly `usage  no usage recorded` (no trailing newline, no legend). Pins the existing single-line invariant for the empty state.

## Items needing a ruling or a test before implementation

1. **P5 empty-state question (load-bearing open).** Either a PO ruling ("render the gate legend in `no usage recorded` when ≥1 gate call is in the window") or a named-decision golden in the test file. Without one, the load-bearing edge case is decided by whichever implementation detail lands first. This is the single item I would not let through to implementation without an explicit decision.

2. **Runner form scope.** Owner R1 §3 scoped `formatRunnerUsageBlock` out ("separate concern"). The runner form (`usage-block.ts:147-161`) currently has **no conditional legend stack at all** — no `partial`, no `n/a`, no `gate`. Design-side observation: principal R1 §2 confirms the intake invocation (parent) is what calls `council_gate`, and the runner is the parent's own autonomous exit — so the runner form's invocation does **not** itself issue a gate call. Owner's scoping is defensible. But I want a **named test** that pins "the runner invocation never issues a gate call" (an upstream property) or "the runner form is out of scope for this card by design." Without a pin, the runner form is a future silent hole — a card that later adds runner-side accounting would discover the gap and either widen scope or silently drop the legend.

3. **Cross-session attribution.** Owner R1 §3 names this: "another session's gate call landing inside this invocation's window would be included in the lookups … no session id on the call line." The legend still reads true; the dollar figure over-attributes. I want a **property test**: prior invocation's gate call + current marker → current invocation's legend fires; that call is **not** attributed to any invocation whose marker precedes it. This makes the absence of a session id an explicit invariant rather than an incidental one.

4. **Costless / failed-call path.** Owner R1 §3 reads the literal predicate (a failed call still emits the legend because "≥1 gate call falls in the window"). Principal R1 Q3 wants the failed call "named unaccounted" or scoped out. Design-side, the literal reading is correct — the legend is about exclusion, not success — but I want a test pinning: a failed-call-only window emits the legend and `gate.totalCost === null`, **never** a catalogue-derived positive number. Owner C4 and principal C5 cover this; both are sufficient.

## Preferences, ranked last

- **Internal field naming.** Owner places gate under `provider.gate`; principal proposes a top-level `gate?` sibling parallel to `provider`. Both deliver identical render. I lean principal's way because of INV-1 — naming gate as a peer of provider makes the no-fold-in property explicit in the type — but this is taste, not grounded. **Owner and principal should pick; the surface contract is the same.**

- **Self-describing key (P7).** `gate = excluded from this total` is project-internal vocabulary. `deliberation = excluded from this total` is self-describing. The card's literal is fixed; this is at best a follow-up card. Ranked last.

- **Count-bearing legend (P4).** `usage  gate = N call(s) excluded from this total` would distinguish 1 call from 12 calls. Out of scope for the literal; follow-up card.

---

**Summary for the chair:** the surface contract is mostly settled (P6 closed, stack order pinned, no fold-in, ledger as audit surface). The single load-bearing open question is P5/O1 — the empty-state and failure-state whole-block precedence. I want a PO ruling on the empty state, or at minimum a named-decision golden that pins the chosen behavior rather than letting implementation decide it incidentally. The runner form needs a scope-pin test. P4 and P7 are follow-up cards, not fold-ins. I yield to `consolidator` (or back to `owner` if no other seat weighs in).

### Round 3 — owner

## Owner round 3 — EV-71 final

### Delta 1 — `costBasis`: **final stance — NO field.** (Crossed concession resolved toward the principal's r2, which matches the principal's own r1.)

Deciding reason: the card **context** describes the existing EV-65 ledger's journalling as a statement of fact, and the goal sentence describes *how the journalling takes the cost* ("taken from the decisions response's usage.cost") — that is the single-writer structural property, not a schema demand. `appendGateCall` stores `usage.cost` verbatim and computes nothing; the basis is uniformly "reported" by construction, so a constant field on every line discriminates nothing and no acceptance reads it. My r2 rationale ("the field gives a checkable artifact") is answered by a cheaper artifact: a test asserting the verbatim-storage property (write `usage.cost = 0.0042`, read back `0.0042` exactly; nothing else sums or transforms at write). That pins the basis claim without v2 field heterogeneity and without a schema question at all. I withdraw my r2 additive-field pin, including the reader-tolerance clause — moot when the field never exists.

### Delta 2 — whole-block states: **confirmed, one split, both pinned as named decisions.**

- `no usage recorded` + ≥1 gate call in window → state line **+ gate legend** (designer's G6; load-bearing, red at base).
- `accounting boundary unresolved` + ≥1 gate call in window → unresolved line **+ gate legend** (same permanent-loss logic: the next invocation's window will not re-include this call).
- `accounting failed` → **stays single-line, no legend** — the `{failed}` variant carries no persisted record at all, so there is nothing to compose a legend from; the failure line is itself the disclosure and the ledger carries the cost. This is written as a `// Decision:` golden per the designer's G7 pattern, not left incidental — the designer's preference for legend-present-everywhere is honored as far as the record shape permits, and the residual split is an explicit, auditable choice, not an implementation accident.

### Delta 3 — Final converged design (spec-ready)

1. **Modules:** `provider-cost.ts` gains a separate gate-spend reconciliation function (shares `fetchGeneration`, never touches `fetchProviderReport`/`ProviderCostReport` — byte-identical seat path); `usage-block.ts` gains one conditional legend row + whole-block-state branches; `usage-store.ts` flush materializes the gate fact; `gate-ledger.ts` untouched (sole writer, verbatim `usage.cost`).
2. **Input edge:** reconciliation accepts optional `gateCalls` (ledger-sourced `gen-dec-…` ids), processed after the seat-job loop; `api_type` unread (spec correction at merge-check).
3. **Persistence:** top-level `gate?` sibling on `StoredUsageRecord`, parallel to `provider` — present iff `callsInWindow > 0`, independent of `provider` null; kills T1 (seat-half poisoning) and T2 (failed-call mask) structurally.
4. **Authoritative totals:** `gate.totalCost = sumGateSpend(calls)` (ledger-authoritative); lookup Σ kept as a distinct observable field; disagreement is record-only, never rendered.
5. **Window predicate:** `recordedAt >= markerAt` (one `readGateLedger` per flush pass); prior-invocation calls excluded by the lower bound; concurrent-session over-attribution accepted-and-named (property test).
6. **Legend slot:** `usage  gate = excluded from this total`, last in the conditional stack — `reported → partial → n/a → gate`.
7. **Per-state behavior:** normal → tail row; `no usage recorded` / `unresolved` → state line + legend; `failed` → single-line (named-decision golden); no-gate → byte-identity to today (G5/G8).
8. **Runner form:** untouched — `council_gate` is parent-side registered; pin "runner invocation never issues a gate call" as an upstream property test.
9. **Tests:** red-at-base C1 (seven-field record) + C4/G6 (P5, load-bearing); C2 (single lookup pass, totals equal), C3 (seat-path byte-identity), C5 (failed call: legend/zero-lookups/no catalogue import), C6/T5 (`api_type` inert), C7 (window exclusion), C8 (mixed ledger: finite-`usage.cost` only, verbatim-storage basis pin), C9 (persisted predicate, no fs at render), C10 + G1–G8 (stack order, absence, per-state goldens).
10. **Rulings carried to the judge:** (i) sibling-vs-nested — sibling, per T1/T2; (ii) whole-block rule change — granted for empty/unresolved, denied for failed; (iii) `costBasis`/`api_type` — no fields, spec text corrected; (iv) follow-up cards only: count-bearing legend (P4), key vocabulary (P7), audit affordance (P3), with the designer's docstring note ("block = exclusion surface, ledger = trace surface") landing in the implementation.

**Tradeoffs accepted:** no `costBasis` field means the basis claim is enforced by test, not schema — acceptable because the writer is single and the property is trivially checkable; the whole-block rule break for empty/unresolved is a real precedent, bought because those states are exactly where the card's failure class bites hardest.

**Testable claims:** none new — the converged C/G list above is the full falsifier set for the Skeptic; T1, T2, T4, and G6 are the ones that decide the disputes this round closed.


### Round 3 — principal

# Principal — EV-71 round 3 (final)

Grounding re-confirmed this round: `extensions/usage-block.ts:100-107` (failed→unresolved→empty precedence, all before the row stack), `:110-119` (provider `unavailable` → subtree `n/a`), `extensions/usage-store.ts:61-75` (`StoredUsageRecord` has `provider?` sibling, no `gate`), `extensions/gate-ledger.ts` (`GateUsage` v2 has `{input_tokens, output_tokens, cost}`; `appendGateCall` is the single writer; no `costBasis`), card `council/cards/EV-71.md`.

## Cross-seam reading

The two sides of the seam are now:

- **Ledger side** (`gate-ledger.ts`): one writer `appendGateCall`, `usage.cost` stored verbatim from the decisions response, computed by nothing; no `costBasis` key today.
- **Block side** (`usage-block.ts`): three whole-block states returned *before* any conditional row is built (`:102` failed, `:105` unresolved, `:107` empty); the legend stack is assembled only in the resolved path; `input.provider?.status === "unavailable"` re-scopes the subtree half to `n/a` (`:116-119`).

The convergence across all three seats on the load-bearing items is real, not premature: gate isolation from seat `generations`/`totalCost`/`routed`/`partial` (`ProviderCostReport` byte-identity), ledger-authoritative total, and the `no usage recorded` legend are each grounded in the files above, not in taste.

## Delta 1 — `costBasis`: **flip to the owner. Add the additive-in-v2 field.**

The deciding reason is the goal text and nothing else: the goal is the judge's contract and it reads *"journalled with costBasis reported taken from the decisions response's `usage.cost`."* That names `costBasis` as a journalled artifact, not a prose property. My r1 no-field stance left the contract's named artifact with **no checkable object** — the judge would have to accept an absence ("nothing computes it") as the proof, which is exactly the negative-across-the-codebase assertion the owner showed is expensive to prove and cheap to break.

The owner's r2 concession removes my only objection: **additive within v2, no schema bump**, writer emits `costBasis: "reported"`, reader tolerates absence on pre-field v2 lines, and `sumGateSpend` is **not gated on the field**. Mixed v1/v2 files are already a reader-tolerance requirement (`gate-ledger.ts` header: "mixed v1/v2 files read with identical rederiveResolvedMode"), so the tolerance cost is already paid. I withdraw my r2 "do not add it" and my INV-4 field anchor.

Two pins I attach, both from the existing single-writer invariant:
- `costBasis` is written by `appendGateCall` (the sole writer) on the call line — never derived at read, never re-serialized onto old lines.
- Exact name and value: `costBasis: "reported"` (the goal's byte string), so merge-check can grep it.

**Final stance: add the field, additive-in-v2, not a summation filter.**

## Delta 2 — whole-block states: **confirm.**

All three seats now agree on `no usage recorded`: legend present, appended after the state line (`record.gate.callsInWindow > 0`). Confirm `accounting boundary unresolved`: legend present, same treatment — a gate call missed at an unresolved flush is permanently off-surface because the window predicate (`recordedAt >= markerAt`) means the next flush will not re-include it.

`accounting failed` stays single-line — and I refine the reason: it is **not a ruling**, it is a structural impossibility. `formatUsageBlock` returns `FAILED_PREFIX` on the `{ failed }` input variant (`:102-104`), and that variant carries no `StoredUsageRecord` at all (the accounting write failed), so there is no persisted record from which to compose a legend. A judge cannot be asked to choose between two options when one of them is unrepresentable in the input type. Pin it as a named-decision test (`// Decision: failed carries no record ⇒ no legend`) so a future record-carrying failure variant is a visible scope break rather than a silent hole.

Designer's preference for legend-present on `unresolved`/`failed` is satisfied on `unresolved`; on `failed` its own accept-as-test-pin applies.

## Delta 3 — vehicle: **confirmed, one residual condition.**

Final: a **top-level `StoredUsageRecord.gate` sibling** parallel to `provider`, present iff `callsInWindow > 0`, independent of `provider`; the separate gate reconciliation shares only `fetchGeneration`; `gateCalls?: …` is the input edge on the reconciliation path. This keeps `fetchProviderReport`/`ProviderCostReport`/`generations`/`totalCost`/`routed`/`partial`/`status` byte-identical (owner r2 confirmed), which makes the INV-1/INV-2 hazards structurally inapplicable and dissolves designer P6/O4 by construction.

**Residual condition:** the card's acceptance says "the provider reconciliation accounts for gate spend **by accepting** decision generation ids sourced from the gate ledger." My vehicle satisfies this on the reading `accepting = input edge`; the result is *returned and persisted* as a sibling. If the judge instead reads it as "the gate result must be **part of** `ProviderCostReport`," I lose, and the minimum acceptable fallback is the two structural requirements I named in r2: (a) `ProviderCostReport.status` computed over seat generations only, and (b) the legend predicate derivable when no seat report exists (provider `null`). Both are free under the sibling and both are the T1/T2 falsifiers. I attach no other condition.

## Testable claims (unchanged where already stated; the deciding ones)

- **T1 (falsifies nesting).** Gate call in window, no seat job, no `OPENROUTER_API_KEY` → `formatUsageBlock` must **not** render `subtree cost=n/a`. Owner-r2 nesting fails via `usage-block.ts:116-119`; sibling passes. This is the unresolved vehicle falsifier.
- **T2 (falsifies nesting, keyless window).** Window with only a failed gate call (`failRun`: no `usage`, no `generationId`) → legend present. Nesting fails (early return still `null` ⇒ `provider?.gate` undefined). Sibling passes.
- **T3 (P5 pin).** `allZeroUsage(ownSession) && allZeroUsage(subtree)`, `record.gate.callsInWindow === 1` → block is exactly `NO_USAGE_LINE\nusage  gate = excluded from this total`. Red at base.
- **T7 (delta-1, new).** v2 fixture: `appendGateCall` with `usage.cost = 0.0042` → persisted call line carries `costBasis === "reported"`; a pre-field v2 line (no `costBasis`) still contributes its finite `usage.cost` to `sumGateSpend` (field is not a summation filter). Red at base (no `costBasis` key exists).
- **T8 (delta-2, new).** `{ failed }` input plus a ledger window containing gate calls → render is byte-identical to today's single-line failure render; asserts the structural-impossibility claim.

The proposed design is sound; the only reframe this card ever needed was the vehicle, and that is now converged. No further reframe.


### Round 3 — designer

Using the seat's output contract to answer all three deltas in order.

## Delta 1 — whole-block states: **concede to the structural argument; pin the resolution in the golden.**

The owner's read is mechanically correct: the `{ failed: string }` variant in `gate-run.ts:185-191` is a **lateral input** to the block with no persisted record object on either side of the seam — `formatUsageBlock` would have nothing to source the legend from, and synthesizing one would mint a record shape (`provider: null, gate: { ... }` in some state) that has no fixture precedent in `StoredUsageRecord`. The principal's convergence confirms the same conclusion from the other direction. **The failure line is the disclosure.** I withdraw my prior preference for legend-present on `accounting failed` and accept:

| State | Calls in window = 0 | Calls in window ≥ 1 |
|---|---|---|
| `no usage recorded` | one line (existing golden G8) | **state line + gate legend** |
| `accounting boundary unresolved` | one line (existing) | **state line + gate legend** |
| `accounting failed` | one line (existing) | **one line — by design** |

**Pin the `accounting failed` non-decision explicitly in the golden.** Without a `// Decision:` comment, an implementer reading "the block is multi-line now" will extend the multi-line rule to the failed state by reflex. The comment must name: *"the `{failed}` input variant carries no persisted record; the failure line is the accounting disclosure; the ledger carries the cost."* That's the decision-record the seat needs.

I retain the empty-state and unresolved-state legend-present reading — those are load-bearing, not preference.

## Delta 2 — vehicle: **confirmed.**

Owner r2 §1 concedes to the principal. Top-level `StoredUsageRecord.gate` sibling, populated independently of `provider`, set by `callsInWindow > 0` on disk. Under this shape:

- The reported row's multiset is untouched **by construction** (gate generations never enter `ProviderCostReport.generations`).
- `provider` stays `null` when no seat job ran — neither the false-`n/a` (principal T1) nor the legend-loss-on-failed-call-only (T2) can occur, because the legend predicate reads `record.gate`, not `provider.gate`.
- **G4 (legend without reported row) is structural**, not test-enforced — I release it from the mandatory golden list down to a regression check.

P6 closed. G4 closed structurally.

## Delta 3 — mandatory pin set for the card:

**Mandatory (these must be in the suite before merge):**

1. **Stack order, all conditions normal** — ≥1 gate + reported + partial + n/a → exact byte-equality with the seven-row block in canonical order (pins the literal absolutely).
2. **`no usage recorded` + ≥1 gate call** → multi-line whole-block state. Load-bearing per P5. *(my G6)*
3. **`accounting boundary unresolved` + ≥1 gate call** → multi-line whole-block state. *(my G7 high-bar)*
4. **`accounting failed` + ≥1 gate call** → single-line by design, with `// Decision:` comment naming the structural reason. *(my G7 scoped)*
5. **No-gate byte-identity** — existing goldens untouched. *(my G5)*
6. **Gate legend without `provider` sibling** — `provider: null && record.gate?.callsInWindow > 0` → legend renders. Now a regression check, not a structural claim. *(my G4)*
7. **Gate-only, no-seat-job window, real catalogue cost** (principal T1) — proves the nested-`provider.gate` poisoning cannot recur; pins `provider.status` is computed over seat generations only.
8. **Failed-call-only window still renders the legend** (principal T2 / owner C5) — pin: zero callable ids, `usage: null`, no `generationId`, `record.gate` still set; legend renders; no lookup observed; `record.gate.totalCost === null`. Pins the call-claim, not spend-claim reading.
9. **`api_type` inert** (principal T5 / owner C6) — `decisions` and `chat` resolve byte-identically; `grep api_type extensions/` = 0; spec sentence corrected at merge. No field added.
10. **Window predicate** (owner C7) — calls at `recordedAt < markerAt` excluded; calls `>= markerAt` included; a prior invocation's call never contributes to the current flush's record.
11. **Ledger authority vs lookup** (principal T4) — ledger Σ and lookup Σ are **distinct observable fields** on `record.gate`; ledger wins for `totalCost`; a mismatch is observable in the persisted record even though not surfaced in the render.

**In-scope clarifications (named in code or test comment, not full goldens):**

- Runner form: pin as upstream property — `gate-tool.ts:130` registers `council_gate` parent-side; the runner never issues a gate call. One-line test comment in the runner-form golden, not a new test file.
- Cross-session attribution: `recordedAt >= markerAt` filters earlier invocations; concurrent-session over-attribution is accepted-and-named in a sibling test comment.
- `costBasis`: not a field, not a summation filter; v2 lines without the value still count. Owner C8 covers this.

**Nice-to-have, ranked last, explicitly out of scope for this card:**

- P4 (count-bearing legend) — follow-up card.
- P7 (`deliberation = excluded from this total` self-describing key) — follow-up card.
- O3(b) ledger affordance — the post-merge trace-surface card I flagged in R1 §"What I press."

**Tradeoffs I accept and want named:** the multi-line whole-block-state rule relaxes for `no usage recorded` and `unresolved`, and the implementation must comment that the rule's purpose is honesty, not one-line-ness — the relaxation is the rule's first consequence, not its first exception.

Two PO questions remain on the chair's plate: (a) the relaxation of the one-line whole-block-state rule for empty and unresolved states (mandatory for the card; load-bearing); (b) the spec correction on `api_type` (merge-check item, not implementation). I do not need a PO ruling on `accounting failed` — the structural argument is sufficient and the comment is the decision-record.

Ending the turn.

### Step 4 — Skeptic attack (round 4)

# Skeptic report — EV-71, round 4 (post-cap attack)

**Scope executed:** read all nine positions + the actual code (`usage-block.ts`, `provider-cost.ts`, `gate-ledger.ts`, `gate-run.ts`, `gate-transport.ts`, `usage-store.ts`, `hub-tools.ts`, `index.ts`, card EV-71.md); ran 15 targeted probes in `/tmp/ev71-skeptic/` (symlinked `node_modules`, importing the repo's real engine modules) and 4 existing suite files. **No repository files created/modified; no branch state touched** (verified: only `/tmp` written; `git status` untouched).

## Objections

**O1 — P5/G6 "red at base" (load-bearing convergence).** Claim: all-zero halves + a gate call in window renders one line/no legend today. *Test:* probe `P5` — `formatUsageBlock({record: zeroRecord, provider})` and with a real ledger file containing a gate call on disk. **closed-green** — render is exactly `"usage  no usage recorded"` (1 line); the input type has no position for a gate; disk ledger changes nothing (purity holds). The convergence on G6 is a genuine behavior change against base.

**O2 — T1 poisoning mechanism.** Claim: an `unavailable` provider report masks a real catalogue subtree cost as `n/a` (`usage-block.ts:113-115`; seats cited 116-119, so the mechanism is real but the citation drifts ~2 lines). *Test:* probe `T1` — measured record (subtree `cost≈$0.2010` real) + `{status:"unavailable", totalCost:null, generations:[]}` renders `cost=n/a`, legend last. **closed-green** — the plausible-but-false `n/a` the principal's T1 names is reachable in base code; the sibling design's promise ("provider stays null when no seat job ran") is the only thing that avoids it, which is a future-implementation invariant, not a base property.

**O3 — T2 failed-call shape.** Claim: `failRun` writes no `usage`/`generationId`; a failed-call-only window has zero callable ids. *Test:* probe `C2` — a failRun-arg-identical `appendGateCall` round-trips with **both keys absent** (`"usage" in line === false`, `"generationId" in line === false`); confirmed by code read of `gate-run.ts:191-224`. **closed-green.**

**O4/O5 — Principal C3, naive fold-in hazards.** Claims: (a) a `typesafe/jev-1.13` job is silently dropped by the `openrouter/` filter; (b) an `openrouter/` job with `sessionPath: null` flips the report to `unavailable/session-missing`. *Tests:* probes `C3a`/`C3b` against real `fetchProviderReport` + spy doubles. **closed-green** — (a) returns `null`, zero fetches; (b) `{"status":"unavailable","reason":"session-missing","totalCost":null,"generations":[]}`, zero fetches.

**O6 — `api_type` inert.** Claim: the extraction never reads `api_type`; the card's "the existing extraction reads exactly those fields" (listing `api_type: "decisions"`) is false. *Tests:* `grep api_type extensions/ test/ council/` = **0 hits**; probe `C6` — `api_type:"chat"` vs `"decisions"` doubles (stabilized `now`) produce `toEqual` reports; `parseDecisionsResponse` reads only `model/answers/usage/provider/["id"]`. **closed-green** — spec correction owed at merge-check, as all three seats converged.

**O7 — `costBasis` current-writer evidence (informs the routed dispute).** *Test:* probe `C8` — the raw line the writer emits today: `{"schemaVersion":2,...,"usage":{"input_tokens":1200,"output_tokens":90,"cost":0.0042},"generationId":"gen-dec-abc123"}` — **no `costBasis` key**; `grep costBasis extensions/gate-*` = 0. Verbatim storage holds: read-back `cost === 0.0042` exactly. The dispute stays routed; the evidence is: if the judge reads "journalled with costBasis reported" as a journalled artifact, the current writer does **not** emit it (principal r3's T7 is red at base); if structural, it holds by construction. I do not adjudicate taste.

**O8 — `sumGateSpend` on mixed v1/v2 via the real reader.** Claim: absent/null/failed contribute nothing; never zero-derived; `null` iff none carried. *Test:* probe `mixed` — a 7-line ledger (v1 line, v2 success 0.0042, v2 negative −0.001, v2 zero 0, `usage: null`, `usage.cost: null`, failed line, plus a torn tail) round-trips through `readGateLedger` (torn skipped, 7 calls); spec'd semantics sum to `0.0031999999999999997` = 0.0042 − 0.001 + 0. **closed-green** — with two notes: (1) **negative and zero costs are finite and are counted** (consistent with "reported verbatim" but a golden asserting `0.0032` needs `toBeCloseTo`/the same float path — float-exactness is a trap for the golden author, since 0.0042−0.001 is not exactly representable); (2) a window with only `cost: 0` lines yields `0` (correct), which a reader cannot distinguish from zero-spend — fine for the design, but "never zero-derived" really means "absent never becomes 0", which holds.

**O9 — Window predicate edges.** Claims: `recordedAt >= markerAt`, equality inclusive, prior-invocation calls excluded. *Tests:* probes `C7`/`skew` — `recordedAt` is stored as an ISO string, `markerAt` numeric; `Date.parse` units align; equality is `>=`-inclusive; **closed-green** on mechanics. **open-untested — one falsifiable gap in the C-list:** the predicate has **no upper bound**, so a gate call issued by the *intake* invocation lands in the window of **every later invocation in the same session** — the design's "only concurrent-session over-attribution remains" is false as stated; same-session *sequential* over-attribution is the common case (`/features-new` then `/features-deliver` — the deliver invocation's own block will show the gate legend for the intake's gate call, unless the deliver marker predates it). My skew probe demonstrates the mechanism (a 5-min-ahead call is attributed to my window). Settling test for the future implementation: a property test with two sequential invocations and one gate call — the later invocation's legend must **not** fire. This is not in the converged C/G list, which only pins the exclusion side (owner C7).

**O10 — "{failed} is a *structural impossibility*" (principal r3) / "nothing to compose a legend from" (owner r3 — the one claim I could not square with the code).** The block input variant `{ failed }` is a closed union — verified, the type cannot carry gate. But the *emission* is caller-composed text: `flushPendingInvocations`' catch → `notify(formatUsageBlock({failed: …}), "warning")`, and the flush holds the read ledger in scope at that point. Nothing type-level or mechanical stops the flush from appending a second line — my probe prints exactly `usage  accounting failed — write failed for /x: boom\nusage  gate = excluded from this total`. **closed-red against the "impossibility" wording** — the failed state's legend is a *policy choice* (defensible: no record exists, so "shown == disk" would break), i.e. a ruling, not a fact. The pinned `// Decision:` golden the seats propose is exactly right; the *justification* should say "we choose not to" rather than "it cannot be." This does not change the design outcome — all three seats agree the failed line stays single-line — but the record of *why* is wrong as written.

**O11 — Runner-form scope.** Claim: runner never issues a gate call; runner form untouched. *Tests:* no seat has `gate` in its tool grant (`council/agents/*.md`); `gate` appears nowhere in `seats.ts`/`child.ts` (not a grantable child tool); `council_gate` registered parent-side (`gate-tool.ts:104`, `index.ts:618`); the runner form is **parent-rendered** (`hub-tools.ts:112-126` — "The parent renders; the runner never self-computes") and has no legend stack (probe `runner`). **closed-green** — structurally sound, stronger than "pin as an upstream property test."

**O12 — Whole-block rule change vs existing goldens.** Claim: gate-absent renders byte-identical; existing goldens stay green. *Tests:* `bun test test/usage-block.test.ts test/gate-ledger.test.ts` → **45 pass / 0 fail**; `test/usage-store.test.ts` → **31 pass / 0 fail**; `test/ev66-advisory-intake.test.ts test/provider-cost.test.ts` → **23 pass / 0 fail**. T3/T5/T8/T-B6/point-6 pin single-line whole-block states and none can construct a gate sibling (input type has none at base) — **closed-green**. The G6 multi-line precedent break for empty/unresolved is a real rule change and is correctly routed to PO; no existing test conflicts.

**O13 — `gen-dec-` prefix as provenance.** Claim: prefix identifies decision generations. *Test:* grep — no code validates the prefix; `gate-transport.ts:215` stores `o["id"]` verbatim; the prefix is a fixture/card convention (EV-65 card, test fixtures). The converged design's `source: "gate"` discriminator is the provenance carrier — correct. **closed-green** (no live data available offline to confirm the endpoint's id format; not a defect, a documented convention).

**O14 — line-number citations.** Principal r2's `usage-block.ts:116-119` (poisoning) is actually 113-115 (mechanism) with 116-119 the rows array; designer r2's "early returns at 102, 105, 107" are 102/108/109. Mechanisms all hold; citations drift 2-3 lines. **closed-green** (non-material).

## What I ran

1. `bun test /tmp/ev71-skeptic/probes.test.ts` → **15 pass / 0 fail / 38 expect** — key raw lines:
   - `P5 render: "usage  no usage recorded"`
   - `T1 render:` shows `subtree ... cost=n/a` masking `cost≈$0.2010`
   - `C3a result: null` · `C3b result: {"status":"unavailable","reason":"session-missing",...}`
   - `RAW LEDGER LINE: {"schemaVersion":2,...,"usage":{...,"cost":0.0042},"generationId":"gen-dec-abc123"}` — no `costBasis`
   - `failed line fields: {... "basis":"gate call failed: HTTP 500: boom","failure":{"class":"http-500"}}` — no `usage`, no `generationId`
   - `spec'd sumGateSpend over real reader output: 0.0031999999999999997`
   - `skewed call in my window (wrongly attributed): 1`
   - `caller CAN append: "usage  accounting failed — ...\nusage  gate = excluded from this total"`
2. `bun test test/usage-block.test.ts test/gate-ledger.test.ts` → 45 pass / 0 fail
3. `bun test test/usage-store.test.ts` → 31 pass / 0 fail
4. `bun test test/ev66-advisory-intake.test.ts test/provider-cost.test.ts` → 23 pass / 0 fail
5. Greps: `api_type` in `extensions/ test/ council/` = 0 hits (only EV-71.md prose); `costBasis` in gate modules = 0 hits; `appendGateCall` callers = exactly 2, both in `gate-run.ts` (single-writer holds); `gate-ledger` import in `provider-cost.ts` = none (owner C3 pin holds); `node:fs` in `usage-block.ts` = none (purity holds); `gate` in `BUILTIN_MAP`/seat grants = none.

## Verdict

**No blocks on base-verifiable claims.** Every runnable convergence holds against the current code: P5/G6 red-at-base, T1/T2 mechanisms, fold-in hazards, `api_type` inert, failed-call shape, verbatim cost storage, mixed-ledger sum semantics, goldens green.

**Open-untested (future-implementation falsifiables, named so you can pin them):**
- **O9b** — sequential same-session over-attribution: the C/G list pins the exclusion side (earlier calls excluded by lower bound) but never the inclusion side (an *earlier invocation's* call inside a *later invocation's* window). Settling test: two sequential invocations, one gate call → the later `record.gate.callsInWindow` must be 0 (or the attribution story must be re-worded).
- **O10** — the failed-state legend is a ruling, not an impossibility; the `// Decision:` golden must say "chosen" not "structural".
- **costBasis dispute** — routed; my evidence: the current writer emits no `costBasis` key, so principal r3's T7 (add field) is red at base and owner r3's no-field stance rests on the structural/verbatim-storage argument, which my C8 probe confirms holds by construction today.
- **float-exactness** in the C2 golden (`sumGateSpend === double.total_cost` — 0.0042−0.001 ≠ exactly `0.0032`); the golden must compare via a consistent float path.

One objection lost, stated plainly: I attacked the `{failed}`-legend as a real requirement gap and it held *against the converged decision's outcome* only as a wording defect — the sentence "no gate legend on `accounting failed`" is right; the word "impossible" is wrong. And I found **no** path at base where gate generations could pollute the seat half under the sibling design — the only vehicle-sensitive lever is `provider.status`, which the separate-reconciliation design never touches.


## Consolidation — EV-71 (decisions-aware gate spend accounting)

### SETTLED

1. **Vehicle: top-level `StoredUsageRecord.gate` sibling parallel to `provider`.** Owner r2 conceded to principal r2; designer r2 confirmed; owner r3 confirmed. Principal's T1/T2 falsifiers (seat-half poisoning via `status: "unavailable"` masking real catalogue cost as `n/a`; failed-call window losing the legend) proved the nesting shape structurally wrong; the sibling dissolves both by construction. Skeptic O2 confirmed the poisoning mechanism is real in base code. Skeptic O12 confirmed all 99 existing tests pass; no golden conflicts.

2. **Gate isolation from seat reconciliation (no fold-in).** `ProviderCostReport`, `generations[]`, `totalCost`, `routedMultiset`, `partial`, and `status` stay byte-identical when gate calls are present. Gate generations never enter the seat `generations` array. Owner r2 confirmed; principal r2 confirmed (INV-1/INV-2 now inapplicable under the sibling, not just test-enforced). Skeptic O4/O5 (C3a/C3b) closed-green: naive fold-in is provably wrong — `typesafe/jev-1.13` is silently dropped by the `openrouter/` filter; `sessionPath: null` flips the report to unavailable. Designer P6/O4 (multiset widening) dissolved by construction.

3. **Legend slot: last in the conditional stack (`reported → partial → n/a → gate`).** Owner r3 confirmed; designer r2 confirmed; principal r2 confirmed. After `n/a` preserves the legend-stack convention; no rule change; tail placement is the only position consistent with whole-block states.

4. **Ledger-authoritative total with distinct observable lookup Σ.** `gate.totalCost = sumGateSpend(calls)` (ledger wins); lookup Σ kept as a separate observable field on the gate sibling for verification. Owner r3 confirmed; principal r3 confirmed (delta 3, item 4). Ledger-word vs lookup-word disagreement is record-only, never rendered.

5. **Runner form untouched.** `council_gate` is parent-side registered (`gate-tool.ts:130`); the runner never issues a gate call. Owner r3 confirmed; designer r3 confirmed (upstream property test, not a code change); skeptic O11 closed-green — structurally stronger than "pin as property test," it is a type-level impossibility (`BUILTIN_MAP` has no `gate`; no seat grants it).

6. **`api_type` spec correction, no field added.** All three seats converged (owner r2, principal r2/r3, designer r2): `api_type` appears nowhere in `extensions/`; `GenerationResponse` has no such field; `toGeneration` never reads it. The card context sentence ("the existing extraction reads exactly those fields" listing `api_type: "decisions"`) is false. Spec sentence corrected at merge-check; no field added. Skeptic O6 closed-green: `grep api_type extensions/ test/ council/` = 0 hits; `api_type:"chat"` and `"decisions"` resolve byte-identically.

7. **`api_type` is inert (test-enforced).** Double returning `api_type: "chat"` produces identical reconciliation output to `api_type: "decisions"`. Principal T5 / owner C6 / designer mandatory item 9.

8. **Red-at-base C1.** No `gate` sibling on `StoredUsageRecord` and no gate-reconciliation function exist at base; the injected-double test cannot compile. Seven-field record established. Owner r1; skeptic confirmed.

9. **Window predicate mechanics (lower-bound exclusion).** `recordedAt >= markerAt` is inclusive; equality aligns (ISO string parsed via `Date.parse`, units match); calls at `recordedAt < markerAt` are excluded. Skeptic O9 closed-green on mechanics.

10. **Block purity (INV-3).** `formatUsageBlock` reads no `node:fs`; the gate fact is materialized into the persisted record at flush; "shown == disk" holds. Owner r1 INV-3; skeptic O12 confirmed (`node:fs` in `usage-block.ts` = 0 hits).

11. **Failed-call shape and call-claim (not spend-claim) reading.** `failRun` writes no `usage`, no `generationId` (skeptic O3 closed-green). A failed-call-only window still emits the legend (`record.gate.callsInWindow > 0`, not keyed on spend). `totalCost: null`; no catalogue estimate consulted; no catalogue import reachable from the gate path. Owner C4/C5; principal C5; designer mandatory item 8.

12. **Mixed v1/v2 ledger sum semantics.** `sumGateSpend` counts only lines where `usage.cost` is a finite number; absent/null/failed contribute nothing; pre-field v2 lines without `costBasis` still count (field is not a summation filter). Skeptic O8 closed-green. Float-exactness caveat: 0.0042 − 0.001 ≠ exactly 0.0032; goldens must use `toBeCloseTo` or equivalent.

13. **Cross-session attribution is accepted-and-named.** The ledger is repo-scoped and committed; another session's call landing inside the window is included. The legend stays true; the reconciliation figure could over-attribute. No session id on the call line. Owner r1; designer r3 confirmed as test comment; principal r3 accepted. **Note: this is accepted-and-named, NOT settled-as-correct — it is an accepted limitation with a property test pinning the lower-bound exclusion side.**

14. **Advisory-mode gate calls included.** `advisory: true` on the ledger; an advisory call still spends tokens. The legend predicate (`callsInWindow > 0`) naturally includes it. Designer O6; owner/designer agree a fixture should pin this.

15. **Follow-up cards explicitly out of scope.** P4 (count-bearing legend: `usage  gate = N call(s) excluded from this total`), P7 (self-describing key: `deliberation = excluded from this total`), O3(b) (audit affordance / ledger signpost in the legend). Designer r3 ranked last; owner r3 confirmed. The designer's docstring note ("block = exclusion surface, ledger = trace surface") lands in the implementation.

16. **11-item mandatory golden set (designer r3).** All 11 items are testable claims carried in the converged C/G list. Each has a named owner (test file) and a red-at-base or mechanism-falsified anchor.

### SETTLED — WITH SKEPTIC WORDING CORRECTION

17. **`accounting failed` stays single-line, no gate legend.** All three seats agree on the outcome. Owner r3: "no persisted record to compose from"; principal r3: "structural impossibility"; designer r3: "failure line is the disclosure"; skeptic O10 **closed-red against the "structural impossibility" wording**: the flush *can* append a second line (the caller composes text and holds the ledger in scope); the type union `{failed: string}` just cannot carry the data. **The design outcome is right; the justification must change from "cannot" to "choose not to"** — it is a policy choice (no record exists, so "shown == disk" would break), not a mechanical fact. The `// Decision:` golden the seats propose is exactly the right vehicle; its comment must say "chosen" not "impossible."

### OPEN JUDGMENT — routes to `product-owner`, escalating to `steward`

**J1 — `costBasis`: ADD additive-in-v2 field, or NO field?**

The seats swapped positions across r2/r3 and remain split:

- **Owner r3 final: NO field.** The card *context* describes the existing EV-65 ledger's journalling as a statement of fact ("reported" names the structural verbatim-storage property); the goal sentence describes *how* the journalling takes the cost, not a schema demand. `appendGateCall` stores `usage.cost` verbatim; the basis is uniformly "reported" by construction; a constant field on every line discriminates nothing and no acceptance reads it. The basis claim is enforced by a test (write 0.0042, read back 0.0042 exactly), not schema.

- **Principal r3 final: ADD field `costBasis: "reported"`.** The goal is the judge's contract and reads *"journalled with costBasis reported"* — that names `costBasis` as a journalled artifact, not a prose property. A no-field stance leaves the contract's named artifact with no checkable object. The owner's r2 additive-within-v2 proposal removes every prior objection: no schema bump; `sumGateSpend` not gated on the field; pre-field v2 lines tolerated.

- **Skeptic O7 evidence:** the current writer emits no `costBasis` key; both stances' mechanisms hold by construction.

- **Card goal text:** "Gate spend is journalled with costBasis reported taken from the decisions response's usage.cost." The phrase is structurally ambiguous between reading `costBasis reported` as a parenthetical describing the basis (owner's reading) and as a named artifact on the call line (principal's reading). No test settles which reading the goal intends. This is a values/ruling question about how the judge's contract text binds implementation.

**J2 — Whole-block-state rule relaxation: settled by convergence, or open judgment requiring a PO ruling?**

All three seats converged on the behavior: empty and unresolved states carry the gate legend after the state line; failed stays single-line. The skeptic confirmed no existing golden conflicts and that the mechanism is reachable (O12). But this IS a rule change — the one-line whole-block-state rule (`usage-block.ts:102-109`) is relaxed for two of three states — and the designer explicitly framed it as "load-bearing, not preference" needing a ruling. The card acceptance ("a golden test proves the block's grammar is unchanged when gate calls are present") must be read against a multi-line whole-block state. Two framings:

- **Framing A (settled by convergence):** All three seats converged, the skeptic confirmed no conflicts, and the behavior is the only one that satisfies the card's intent ("What is not acceptable is an unaccounted call whose cost is neither carried nor named"). The convergence is the ruling; no PO escalation needed.

- **Framing B (open judgment):** The relaxation is a precedent-breaking grammar change. The seats converged on the outcome, but the *rule itself* — "the one-line whole-block-state rule yields to the card's IFF for empty and unresolved states" — is a statement about future cards' grammar, not just this card's behavior. A PO ruling pins the grammar; convergence only pins this instance.

**J3 — Failed-state legend: is the failed variant's inability to carry the gate legend a structural property or a policy choice?**

All three seats agree the outcome: single-line. The disagreement is the *recorded why*:

- **Principal r3:** "a structural impossibility; the type cannot carry it; a judge cannot be asked to choose between two options when one is unrepresentable in the input type."
- **Skeptic O10:** "closed-red against the 'impossibility' wording — the flush CAN append a second line (it holds the ledger in scope); the type union just can't carry it; this is a policy choice, not a mechanical fact."

The skeptic's probe proved the caller CAN print a two-line string including the gate legend on the failed path. The type-level constraint is real (`{failed: string}` carries no `StoredUsageRecord`), but the *emission* is caller-composed text, not a type-level operation. The question is which level of analysis the `// Decision:` golden should cite. This matters for future cards: if the next failure variant gains a record, the comment determines whether the implementer treats "no gate legend" as a permanent invariant or a recoverable choice.

### OPEN OBJECTIONS

**O-A — O9b: sequential same-session over-attribution (skeptic open-untested).**

The window predicate (`recordedAt >= markerAt`) has no upper bound. A gate call issued by the `/features-new` intake invocation lands in the window of every later invocation in the same session (e.g., `/features-deliver`). The seats' claim "only concurrent-session over-attribution remains" (owner r1, designer r3) is **false as stated**: same-session sequential over-attribution is the common case. The skeptic's skew probe demonstrated the mechanism (a 5-min-ahead call is attributed to the skeptic's window).

**Settling test (proposed by skeptic, not yet run):** two sequential invocations, one gate call after the first marker but before the second → the later invocation's `record.gate.callsInWindow` must be 0, OR the attribution story must be re-worded to say "calls in the window by `recordedAt`" without claiming sequential exclusion. The test has not been run. This is both a blocking objection (the spec sentence attributing only to "concurrent-session" is wrong) and a potential design gap (if the test fails, the predicate needs an upper bound or a different attribution strategy).

**O-B — costBasis: current writer emits no `costBasis` key (skeptic O7, informing J1).**

The skeptic's O7 confirms `grep costBasis extensions/gate-*` = 0; the current writer emits `usage.cost` verbatim without a `costBasis` key. This is evidence for J1, not a separate objection. If the judge rules for the principal's "add field" reading, the T7 golden (additive-in-v2, red at base) is the settling test. If the judge rules for the owner's "no field" reading, a test asserting verbatim-storage (write 0.0042, read back 0.0042 exactly) is the settling test. Neither test has been run because the dispute is about which test the card requires, not about what the code does today.

**O-C — float-exactness in golden assertions (skeptic O8 caveat).**

The skeptic's `sumGateSpend` probe over a mixed ledger produced `0.0031999999999999997` (not `0.0032`) because 0.0042 − 0.001 is not exactly representable in IEEE 754 float. Any golden asserting `sumGateSpend === expectedTotal` must use `toBeCloseTo` or the same float path, not strict equality. This is a test-authoring trap, not a design dispute, but it will cause a false red if missed. The settling test is known (float-comparison golden) and not yet written.

**Carried forward (not blocking, but open):**
- **J3** (failed-state "impossibility" vs "choice" wording) — affects the `// Decision:` golden's comment text, not the design. Can be resolved at golden-writing time.
- **O-B** (costBasis writer evidence) — evidence for J1, resolved when J1 is ruled.
- **O-C** (float-exactness) — test-authoring trap; resolved when goldens are written.

---

## Step 4b — Skeptic targeted follow-up: O9b SETTLED (closed-green)

**Verdict: closed-green — the objection's claimed sequential path does not exist; the mechanics exclude it in every sequential case traced.** Probe `/tmp/ev71-o9b-probe/probe.test.ts` (real engine modules): Q1 pass (2 invocations → 2 markers, distinct ids, strictly increasing `at`, each pending entry carrying its own markerAt); Q2 pass (`M_A=…913 t=…916 M_B=…918 boundaryB=…918`; B's record excludes gate-A: `t >= M_B` false); Q3 pass (real timeline: A flushed at its own settle before B invoked; `A record=7 tokens | B record=9 | no crossing`); Q4 pass (deferred read: `B-window=["gate-B"] A-window=["gate-A","gate-B"]` — the reverse direction, already documented as T8's point-in-time drift). Base anchors: `bun test test/spend.test.ts test/usage-store.test.ts test/gate-ledger.test.ts` → 65 pass / 0 fail.

Mechanics: `markerAt` is per-invocation (`recordInvocationBoundary`, spend.ts:67–79, appends a NEW `council-invocation` entry per invocation; `stampProcedureInvocation`, index.ts:158–182, reads that entry's `data.at` into the `PendingInvocation`'s own `markerAt`); the flush's forest gate uses the per-entry anchor (`m.startedAt >= p.markerAt`, usage-store.ts:355, 377–378); a stale pending entry re-flushed during a later invocation re-derives its OWN boundary (T8 in the shipped suite tests this isolation). Since `M_B > t` in any sequential A→B, `t >= M_B` is false — the later invocation never sees the earlier invocation's gate call.

**Consequence:** O-A falls. The surviving "no upper bound" residual is the REVERSE direction (later calls into an earlier window on deferred reads) — already named/documented (T8) and accepted. The attribution story as converged ("only concurrent-session over-attribution remains") is correct as stated.

**Routing state after O9b settles:** two open-judgment items remain for a ruling seat — J1 (costBasis) and J2 (whole-block rule relaxation). O-C (float-exactness) is a test-authoring note for the spec; J3 is resolved by the skeptic's wording correction (choose-not-to, pinned by the `// Decision:` golden).


## Step 9 — Skeptic verification (job-27.2, head `c64d8d8`)

**Verdict: CLEAR — no open objections.** All four gates re-run fresh at the branch head, exit 0 each: preflight `PASS: preflight clean`; `tsc --noEmit` clean; `bun test` **1203 pass / 5 skip / 0 fail** (109.06s, integration gated); `validate.py` clean. 11 independent probes × 4 runs green: T-B/T-C/T-D, injected-double resolution (each ledger-sourced `gen-dec-` id looked up exactly once, `total_cost` resolves into the gate total), transcript silence (double saw exactly `["gen-f1"]` without a gate call), `apiKey: null`/failed-call figures never derived, J2 grammar clauses 1–5 (state line first, legend after in empty/unresolved, failed single-line, stack `reported → partial → n/a → gate`, zero-gate byte-identity vs base `a257523` across a 9-input battery), no-fold-in (`provider`+`spend` deep-equal with vs without gate calls), O-C float paths only, EV-66 canary amendment preserved-strength, red-at-base C1 reproduced verbatim (mechanism-absent class).

Non-blocking findings (both recorded, neither blocks):
1. The runner-form pin comment (spec §1.6) did not land — behavior verified correct (runner form byte-identical, structurally gate-less), missing documented deliverable only.
2. The red-at-base record's literal sha `6eccde0` is a pre-rebase fork; the current-history base satisfying the record's rule is `a257523` (tree differs only by the ruling doc), and the red reproduces identically there.

## Step 10 — Judge verdict (job-27.3, head `dcd2617`)

**Verdict: PASS.** Each goal conjunct verified at branch head `dcd26178c2b64804dc26fdbe181f7ed8fb08e651`: (1) verbatim `usage.cost` storage — direct assignment, no transformation; (2) no synthesised/second cost field — key-walk + grep 0 hits for `costBasis`/`estimated`/`catalogue` on the gate path; (3) `generationId` journalled verbatim; (4) reconciliation accepts ledger-sourced ids via the caller-passed input edge, source pin structural (no gate-ledger import in provider-cost); (5) injected double: `lookupCost === totalCost === 0.0042 === sumGateSpend`, confirmed in the flush integration test; (6) transcript silence — double saw exactly `["gen-f1", "gen-dec-1"]`, no `gen-dec-` id from transcripts; (7) no catalogue estimate — T-D grep 0. Targeted test subset 85/85 green; full-suite pass confirmed against the Skeptic's run.

## Step 9 loop, cycle 2 — CI red on head `989bd67`, returned to In Progress

Observed artifact: `gh pr checks 90` → workflow `gates` state FAILURE on run
35484939058 at head `989bd6741fb6b0bfbcb8bd8f6c3c1ceecf683d06`. Failing test:
the EV-66 two-arm headless falsifier, at `test/ev66-advisory-intake.test.ts:554` —
`readManifests(workDirA, readdirSync(runs)[0]!)` assumes the runs dir's first
entry is a run directory; CI's entry order served the self-gitignore FILE
(`runs/.gitignore`, written by `runs.ts` ensure) first, so `runId=".gitignore"`,
`existsSync` true, `readdirSync` → `ENOTDIR` (verbatim in the run log). Not an
EV-71 mechanism failure (passes locally; `dispatchSets` at line 330 already
dot-filters) — a latent nondeterministic test assumption exposed on this head.
Owner fix dispatched: make line 554 directory-selecting (reuse `listRunIds`,
which stats for isDirectory) and land the missing runner-form pin comment
(skeptic step-9 finding (a), spec §1.6).

## Step 9 loop, cycle 2 — Skeptic re-verify (job-27.5, head `1ab63af`)

**Verdict: CLEAR.** Delta verified line-for-line: `listRunIds` substitution byte-exact with engine untouched (`git diff --stat -- extensions/` empty in range); falsifier green 5/5 consecutive runs; usage-block change comment-only, closing finding (a); nothing else moved since `c64d8d8`. All four gates green at the new head: preflight exit 0, tsc exit 0, `bun test` 1203 pass / 0 fail (109.02s), validate.py clean. Probe battery 85/85 (T-B, stack position, zero-gate byte-identity, no-fold-in).

## Step 10 (re-confirmation at final head) — Judge PASS (job-27.6, head `dbfc969`)

All seven goal conjuncts verified unchanged at `dbfc9695cd4a04619f00da498ee1ae6305ae1124`: T-B verbatim round-trip; T-C sole cost-bearing key `usage.cost`; T-D no catalogue import; structural ledger-sourcing pin; acceptance test item 13 (`gen-dec-123` looked up exactly once, `lookupCost == totalCost == sumGateSpend == 0.0042`); item 12 zero lookups on null generationId; targeted subsets green (gate-spend-reconcile 12/12, usage-block 37/37, ev66 4/4). The two post-PASS diffs (scope-pin comment, EV-66 runId fix) neutral to the goal.

## Outcome

- Merged PR #90 as `d090c1d484e7ca2ab46d7f125fe3e2a4703dd715` (squash, admin
  bypass per R1, pinned to the checked head
  `97de78f726dc9fa175bff472fed562cf388250d5`). The PR head still carried the
  card at `In Progress` (the final `In Review` record was not committed to the
  branch head); the orchestrator reconciled it to `Done`.
- One escalation during deliberation (product-owner job-26): J1 ruled NO
  `costBasis` field (basis structural; goal amended with exact text); J2 pinned
  the usage-block grammar scoping. Ruling appended verbatim; no steward
  escalation.
- One verify fix-cycle: a CI red where the EV-66 falsifier's `readdirSync(runs)[0]`
  handed the runs dir's self-gitignore file to `readManifests` on CI entry order;
  fixed to `listRunIds(...)` (engine untouched). Verify-cycle 2 of ≤3.
- Deterministic merge check: all five criteria satisfied at the checked head —
  owner gates green in full under CI-hermetic conditions (`env -u
  OPENROUTER_API_KEY`; preflight PASS, `tsc --noEmit` clean, `bun test` 1203 pass
  / 5 skip / 0 fail, `validate.py` clean, independently re-run by the
  orchestrator on the head worktree), `gates` workflow `SUCCESS` on head,
  skeptic CLEAR, judge `PASS` (re-confirmed post-PASS), no `Needs Human`.
- Runner left three follow-up DRAFTS unwritten (correctly deferring to step 13):
  (P4) count-bearing gate legend; (P7) self-describing `deliberation` key vs
  `gate`; (designer O3(b)) block→ledger audit affordance. Held for the run-close
  confirmation pass.
