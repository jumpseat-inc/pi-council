---
id: EV-83
title: Route the follow-up decision through /features-deliver Phase 3 and the council-runner escalation path
state: Done
owner: null
epic: EPIC-10
goal: `council/procedures/features-deliver.md` Phase 3 and `council/agents/council-runner.md` both route each surfaced follow-up candidate through the follow-up decision, and a container whose decision fails or cannot resolve returns the candidate as an `ESCALATION` on its report and writes no card, proven by a runner fixture asserting `ESCALATION` and zero unconfirmed cards.
---

## Intent

Surface: the `/features-deliver` Phase 3 ledger line and the `council-runner`
completion report tag copy. Two more emitters consume the follow-up rule:
`features-deliver`'s Phase 3 at run scope, and the `council-runner` container.
This card routes both through the follow-up decision and pins the container's
failure route: an unresolved candidate returns as `ESCALATION` before any
write, never a silent drop. The disposition outcome rides `DONE` and
`ESCALATION` only; `RETIRED` keeps its documented card-withdrawal meaning.

## Acceptance

- Phase 3's follow-up sentence names the follow-up decision as the disposition
  source, agreeing with step 13 rather than restating an unbounded per-item
  rule.
- `council/agents/council-runner.md` records the decision per candidate and, on
  a failed/unresolved call, returns the candidate(s) as `ESCALATION` before any
  write; it never silently drops one.
- The report carries the disposition outcome at the action point on `DONE` and
  `ESCALATION` only — `DONE` carries the per-candidate outcome, `ESCALATION`
  carries the candidate id plus the `gate call failed: <reason>` / unresolved
  basis, and `HALT` pins "no disposition reached" or the partial state.
  `RETIRED` keeps its documented card-withdrawal meaning verbatim and never
  carries a candidate `Drop`; `<return_contract>` is not edited.
- A `test/hub.test.ts`-style runner dispatch on a failed call returns
  `ESCALATION` with the candidate in the report; a runner that writes a card
  without a resolved decision, or drops a candidate, fails the test.
- The repo's typecheck, the full test suite, `python3 council/validate.py`, and
  the repo's preflight pass.
## Deliberation record

**Routing (step 1):** recorded mode `Deliberate` (fallback — run input carries the recorded
routing). Surface-touching bit: **YES** — EV-83 changes the Phase-3 ledger line and the
`council-runner` report copy; `designer` seated as third generator. Phase-1 rulings applied:
R6 (disposition outcome rides `DONE`/`ESCALATION` only; `RETIRED` verbatim;
`<return_contract>` not edited), R0a (steps, never line ranges), EV-78 (thresholds named-key,
overrides `{Merge, Drop}`, vocabulary `File | Merge | Drop`), EV-82 composition-site answer
(`runFollowupReview` is the ONE composition; runner path is its second caller).

### Round 1 — owner (first pass, job-13.1)

**Module placement: a `followup` grant keyword mirroring `hub`, exposing the existing
parent tool pair to the granted seat.** The runner is a child seat; `extensions/child.ts`'s
`isCallAllowed` structurally blocks every tool not builtin-, hub-, or MCP-granted. The
`council_followup_gate`/`council_followup_render` pair is registered only on the parent
path (`extensions/index.ts` ~628, behind a comment claiming "EV-83's runner seam is the
exported runFollowupReview, **not a tool**"). Evaluated as an open design question, that
comment is **half right**: right that the composition must stay `runFollowupReview`
(FLLWUP-96: one site, up to two callers — `extensions/followup-tool.ts:103–156`); wrong
that the seam isn't a tool. A child seat is an LLM in a sandboxed pi session — it cannot
`import` a TS function. The only ways it reaches engine capability are (a) a registered,
granted tool, or (b) shelling out through `Bash`. A bash-invoked importer is a second
composition surface with no sandbox accounting, exit-code error handling, and no
ledger/widget parity — it fragments exactly what FLLWUP-96 exists to keep single. The
`hub` grant is the in-repo precedent for the correct answer: **a grant keyword in seat
frontmatter that mints engine tools** (`grantsFor` in seats.ts:579–581, `buildChildArgv`
seats.ts:614–616, `runChildMode` child.ts:22–27).

**Proposed mechanism: the `followup` grant, mirroring `hub` exactly.**

1. `extensions/seats.ts` — `grantsFor` returns `{ hub, followup }`; `followup:
   t.has("followup")`. `buildChildArgv` pushes `council_followup_gate,council_followup_render`
   into `--tools` when followup-granted (same line as the hub tools push).
2. `extensions/child.ts` — `runChildMode` calls
   `registerFollowupGateTool`/`registerFollowupRenderTool` when `grantsFor(seat).followup`;
   `isCallAllowed` permits those two names under the grant. The tools' `execute` already
   guards `ctx.hasUI` (`followup-tool.ts:197`), so headless child sessions work unchanged —
   `setWidget` is optional by design (`RunFollowupReviewOpts`, followup-tool.ts:93–96,
   "optional so tests and EV-83's runner path can run headless" — the code itself
   anticipated this seam).
3. `council/agents/council-runner.md` frontmatter — `tools: Read, Grep, Glob, Edit, Write,
   Bash, task, hub, followup`.
4. The index.ts comment is rewritten to name the actual seam: the *composition* is the
   exported `runFollowupReview` (never a second composition); the *transport* into a child
   container is the granted tool pair. Registration stays out of `registerHubTools`
   (hub-granted seats must not get it implicitly — only the explicit `followup` grant).
5. `council/procedures/features-deliver.md` and `council/agents/council-runner.md` prose
   changes below.

Conventions check: this extends the tool-grant vocabulary (convention 8) with one keyword,
exactly as `hub` did; it does not touch the seat schema's frontmatter fields (convention 2
— `followup` is a `tools:` value, not a new field); no hardcoded `.pi`, no clone paths.

**Deliverable 1 — exact prose changes.**

`council/procedures/features-deliver.md` Phase 3 — current sentence:

> **Every follow-up filed** during the run, the same way `/council` step 13 requires of an
> attended run: never a prose bullet, always its own card, drafted and confirmed before
> it's written.

Replacement (keeps the prose.test.ts-pinned anchors `Every follow-up filed` and `usage
block`; names the decision as the disposition source *and* the failure arm at run scope):

> **Every follow-up filed** during the run, the same way `/council` step 13 requires of an
> attended run: never a prose bullet, always its own card, drafted and confirmed before
> it's written. Each candidate's disposition is the one recorded follow-up decision at
> step 13's confirm gate — the ledger names the recorded disposition per candidate and
> never re-decides one — and a candidate whose decision failed or did not resolve appears
> in this ledger only through its runner's `ESCALATION`, never as a filed card and never
> silently dropped.

`council/agents/council-runner.md` — `<return_contract>` is untouched (R6). All copy lands
in a new block, `<followup_decision>`, placed after `<escalation_contract>`:

> **At step 13, record the follow-up decision in-container.** Call `council_followup_gate`
> ONCE with every drafted candidate in draft order, then `council_followup_render`, and
> present each returned line verbatim under its draft — the same tools the attended
> procedure names, granted to this seat by its `followup` grant. The recorded decision is
> the only disposition source; you never re-decide a candidate the render already answered.
>
> **A failed or unresolved decision is an `ESCALATION` before any write.** If any
> candidate's call fails or cannot resolve — a `status: "failed"` entry, a
> `gate call failed: <reason>` line, `credential unresolved`, `model-card coming-soon`,
> `followup decision not recorded`, or `recorded followup call not found in ledger` — end
> your turn with an `ESCALATION` report per `<escalation_contract>` (facts, never a
> recommendation) carrying each affected candidate's id and the verbatim basis. Write
> nothing first: no card to `council/cards/`, no board transition for the affected
> candidate, no candidate silently dropped, no retry that would double-record the call.
>
> **On `DONE`, the per-candidate outcome rides the report's follow-up section** — each
> candidate's rendered disposition line verbatim. A candidate disposition never rides
> `RETIRED` (that tag keeps its documented card-withdrawal meaning only) and never rides
> `HALT`: a `HALT` report states **`no disposition reached`** for any candidates in
> flight, or names the partial state.

R6 audit: outcome vocabulary is `File | Merge | Drop` on `DONE`/`ESCALATION` only;
`RETIRED` untouched; the four-tag list byte-unchanged; `HALT` copy pinned as the binding
context requires. EV-78 audit: the runner names the decision, never overrides it —
`Merge`/`Drop` come from the recorded line; the prose forbids re-deciding. R0a: the block
names step 13, no line ranges. Prose guards: no banned tokens in the new copy.

**One open point for deliberation** (owner flags, not decides): the gate-`off` arm.
Packaged default is `off` — `{ mode: "off", recorded: 0 }` records nothing, so there is no
disposition to route. Owner's lean: under `off` the runner writes no follow-up card (the
same before-any-write arm) and carries the unconfirmed drafts on its `DONE` report as
unconfirmed-presented, for the orchestrator's attended review; this repo runs `active` so
the arm is cold here, but the packaged seat must not silently file cards under a consumer's
`off`. Alternatively `off` → `ESCALATION`. This should be settled before implementation.

**Deliverable 2 — the failure route, mechanically.** The failure taxonomy already exists in
shipped code and the runner prose merely consumes it: recorded failure (transport/HTTP:
one ledger line, `basis: "gate call failed: <reason>"`, rendered verbatim);
thrown pre-POST failure (`callId: null`, generic message, zero ledger lines); total-failure
literals (`credential unresolved`, `model-card coming-soon`); join misses (the two
`FOLLOWUP_RENDER_FALLBACK` literals). "Never silently drops one" pinned three ways: prose
enumerates all five unresolved shapes and mandates `ESCALATION` naming candidate id +
verbatim basis; the before-any-write ordering is prose-pinned; the fixture fails on any of
report not `ESCALATION`, candidate id absent, basis absent, or a new card file.

**Deliverable 3 — test plan.** (1) Grant/sandbox unit tests (`grantsFor`, `buildChildArgv`,
`isCallAllowed` positive and negative). (2) The runner fixture (test/hub.test.ts-style): a
stub child (the `stub-child.ts` pattern — a separate process; the stub itself passes a
`failDouble` transport to the exported `runFollowupReview`, legitimate since transport is
an opts field) spawned via `hub.spawnJob` against a `mkdtemp` fixture repo; asserts on the
settled `JobReport.output`: first line `ESCALATION`, candidate id present,
`gate call failed: <reason>` basis present; `council/cards/` has zero new card files. This
is the card-goal falsifier and must be recorded red-at-base per the seven-field convention.
(3) Prose tests: Phase 3 anchors + escalation-only-failure sentence; the new block's pins;
`<return_contract>`'s four-tag list byte-unchanged (the R6 guard); frontmatter carries
`followup`. Red-at-base for (2); (1) and (3) conventional failing-test-first pins.

**Deliverable 4 — acceptance mapping.** Bullet 1: the Phase 3 replacement sentence (prose
test). Bullet 2: the `<followup_decision>` block + the grant mechanism. Bullet 3: the
block's third paragraph + the byte-unchanged `<return_contract>` pin. Bullet 4: the
fixture with red-at-base. Bullet 5: gates in full.

**Testable claims.** (1) "The runner seam can't be a tool" is false as written — with the
`followup` grant and child registration, a child-mode session's
`isCallAllowed("council_followup_gate")` returns true; today's code returns false.
(2) A bash-imported seam would be a second composition surface — no wrapper needed;
`runFollowupReview`'s signature covers the child case (headless, optional widget).
(3) The failure route drops nothing — the fixture: failed call yields `ESCALATION` +
candidate + basis in the report and zero card writes; a runner that wrote a card or
omitted the candidate fails red.

### Round 1 — designer (first pass, job-13.3)

**Design position.** The two surfaces EV-83 changes are read at two different action
points — the Phase 3 ledger line by the human/orchestrator after a run closes (deciding
what got filed), and the `council-runner.md` `<return_contract>` clauses by the orchestrator
the moment a runner's report lands (deciding what to do next). Both must **enumerate the
surfaced follow-up candidates at the read point** — Phase 3 by agreeing with step 13's
follow-up decision as the disposition source (not restating step 13's per-item rule), and
the runner's report by carrying the per-candidate disposition (`DONE`), the candidate id
plus the verbatim `gate call failed: <reason>` / unresolved basis (`ESCALATION`), and a
"no disposition reached" or "partial state — dispositions reached for N of M candidates"
pin (`HALT`). The four-tag list and `RETIRED`'s verbatim meaning are load-bearing
constraints, not anchors to drift around — they bound what the runner can ever say.

**Gulf closed.** Gulf of **Evaluation**, for two readers at two distinct moments:
(1) the **orchestrator at the report's arrival** — must perceive which candidate was held
without writing, on what basis, and what to record in Phase 3; (2) the **human reading the
Phase 3 ledger line after the run** — must perceive, per surfaced candidate, whether
disposition was reached and which path it took (so the ledger audits against the gate
ledger and against any candidate the runner ESCALATION'd).

**Principle and evidence.** Signifier + knowledge-in-the-world: the disposition outcome
rides verbatim from the gate ledger's recorded line into the report — not a paraphrase.
EV-82 already ships the verbatim grammar in `decisionLine` (`extensions/gate-ledger.ts`)
and exports `FollowupRunResult.failure: {kind, reason}` precisely so EV-83's report copy
doesn't have to string-match `basis`. The runner's report must **cite the literal
`Mode: <disposition> — <basis>` byte sequence per candidate on `DONE`** and the verbatim
`gate call failed: <reason>` (where `reason` is the ledger's failure.reason) on
`ESCALATION` — both already exist as bytes elsewhere in the system; the report copy's job
is to surface them, not invent new strings. Constraint: the four-tag list is structural —
`features-deliver.md` Phase 2's rows are the orchestrator's only routing key; adding a
fifth tag forks the table; the compression is on the prose inside each tag's clause, not
the tag list. Mapping — report line ↔ gate ledger row: the runner's `ESCALATION` report
names candidate ids that map 1:1 to `FollowupReviewCandidate.title`; the per-candidate
outcome on `DONE` maps 1:1 to the gate-ledger.jsonl line via `callId`. The contract must
make that mapping explicit so the orchestrator can audit.

**Falsifiable predictions** (all cold-readable against the current bytes; the Skeptic can
run them as pure-text byte checks):

- **P1 — Phase 3 ledger line currently restates step 13.** Current bytes
  (`features-deliver.md:130-132`): "Every follow-up filed during the run, the same way
  `/council` step 13 requires of an attended run: never a prose bullet, always its own
  card, drafted and confirmed before it's written." Failure prediction: a runner that
  ESCALATION'd on a failed follow-up call produces a Phase 3 ledger that reads "no
  follow-ups filed" — the human cannot tell from the ledger alone whether the candidate
  was held, dropped, or routed around. Passing shape: name the follow-up decision as the
  disposition source and surface the failure case — e.g. per-candidate disposition and
  verbatim basis, with an escalated candidate recorded so the next runner can resume it.
  Falsifier: cold-read persona (the orchestrator role, given only the Phase 3 text and the
  runner's `ESCALATION` report): "was any candidate silently dropped?" — current bytes
  can't be answered.
- **P2 — `ESCALATION` clause is silent on surfaced follow-up candidates.** Current bytes
  (`council-runner.md:307-309`). Passing shape: explicit enumeration of surfaced
  candidates and their disposition, with the no-card invariant stated. Falsifier:
  cold-read "for each surfaced follow-up, was a card written?" — current bytes cannot be
  answered. Byte-check: `gate call failed` never appears inside `<return_contract>`
  currently.
- **P3 — `DONE` clause is silent on per-candidate follow-up outcome.** Current bytes
  (`council-runner.md:311-314`): "any follow-up cards filed" is a count claim, not an
  audit claim. Passing shape: per-candidate `Mode: <disposition> — <basis>` lines so the
  orchestrator can audit against `council/gate-ledger.jsonl`. Falsifier: cold-read "for
  each surfaced follow-up, what was its disposition and on what basis?"
- **P4 — `HALT` clause is silent on whether disposition was reached.** Current bytes
  (`council-runner.md:323-328`). Passing shape: explicit partial-state pin —
  `no disposition reached` for all, or `partial: dispositions reached for N of M
  candidates — <list>`. Falsifier: cold-read "for each surfaced follow-up, was its
  disposition reached?"
- **P5 — `RETIRED` holds the load-bearing constraint.** If `RETIRED`'s prose is edited to
  add a candidate `Drop`, the withdrawal surface and the follow-up disposition surface
  collapse into one tag, breaking R6. Passing shape: **no edit**; byte-identity check —
  a diff of `RETIRED`'s clause bytes between current and post-EV-83 is empty;
  `grep -c "Drop"` in that section stays zero.

**Failure-copy analysis.** Two distinct things must be in the report for the orchestrator
not to silently drop a candidate: (1) what was NOT done — a candidate id + "no card was
written" + the basis the runner would have filed under (the current `ESCALATION` clause is
silent on the surfaced-candidate enumeration entirely; a reader of the contract alone
doesn't know to expect candidate ids); (2) why — the verbatim `gate call failed: <reason>`
literal, never a paraphrase. For `DONE`'s per-candidate outcome line: it carries enough to
audit iff it enumerates each surfaced candidate with the disposition line; a count audits
to nothing against per-callId ledger records.

**Preferences (ranked last).** Bullet-list enumeration for per-candidate outcomes (one
bullet per candidate id), not comma-joined — taste. The "held" word pairs with the
no-card-was-written invariant — taste. Verbal "the gate ledger" reference over the path —
taste. The `gate call failed: <reason>` literal stays identical across all three places it
appears after EV-83 (the ESCALATION report, the render failure cell, the gate-run basis)
— the audit-trail invariant (a reader can grep the literal and land on every mention).

### Round 1 — principal (first pass, job-13.4; first dispatch 429-rate-limited, re-dispatched once)

**Cross-seam reading.** `council.md` step 13 is the canonical contract: record via
`council_followup_gate` → render via `council_followup_render` → present verbatim →
confirm before any write → apply the recorded disposition. `features-deliver.md` Phase 3
currently offers only a pointer with no disposition-source and no failure route.
`council-runner.md` says nothing about the follow-up decision at all. `runFollowupReview`
(`extensions/followup-tool.ts:96–158`) is the one composition site;
`renderFollowupLinesFromRepo` (`extensions/followup-render.ts:193–197`) is the pure render
half; both exported. But the only callers are the two parent-session tools registered at
`extensions/index.ts:636–637` on the parent path only. `runChildMode`
(`extensions/child.ts:15–40`) registers only hub tools plus built-ins; the child process's
tool surface is frozen at spawn (`buildDispatchArgv` emits an exact-name `--tools`
allowlist, `extensions/seats.ts:615–617`); `isCallAllowed` blocks anything else at
`tool_call`.

**The index.ts comment is wrong as written.** "EV-83's runner seam is the exported
runFollowupReview, not a tool" is a category error: an LLM container does not import TS
functions. A child-mode runner can only act through `--tools`-listed, child-registered
tools. As shipped, a runner following EV-83's prose would have **no callable surface** that
reaches `runFollowupReview`. The comment's operative purpose — never fold into
`registerHubTools` — is correct for the parent pair, but it conflates "the parent tools are
not exposed" with "the runner has a TS-import path." EV-82 explicitly deferred this
("runner-side access is EV-83's seam, not pre-built here"), and the deferral is real engine
work.

**The fail-safe trap on the data side.** `runFollowupGate` writes a ledger line on both
arms, and on a transport/credential failure the recorded `resolvedMode` is the literal
`File` — the human-confirm fail-safe default (`docs/superpowers/specs/2026-09-21-EV-81-design.md:88–89`;
`extensions/gate-run.ts:363`). The discriminator is the sibling `failure: {kind, reason}`,
which the mechanical result (`FollowupReviewCandidate`) deliberately drops — it carries
only `status` + a generic `message`. Per-candidate failure identity therefore only survives
through the render half joining the ledger by `callId`. Any runner that keys "resolved" off
`resolvedMode` will write a card on a failed call — precisely the silent write the card
forbids.

**Blind spots.** Owner cannot see the dispatch allowlist — the capability boundary
(`--tools` in `seats.ts`, `isCallAllowed` + registration in `child.ts`) is three files away
and not named by the card; the likely wrong turn is folding the follow-up tools into
`registerHubTools` or writing prose that names a tool the runner cannot see. Designer
cannot see that a "success-looking" line is the failure case (`Mode: File — …` is also
what a failed call records) and cannot see that R6 forbids editing `<return_contract>`, so
the disposition-outcome content contract has to live *outside* the block a reader would
check. Skeptic cannot see that a Hub+stub "runner dispatch" proves plumbing, not obedience
— the house pattern (`test/ev69-verify-manifests.test.ts:236–270`) dispatches a stub-child;
EV-69 itself recorded the honesty caveat that this claims NOTHING about whether a live
facilitator would call the tool. Product-owner cannot see that `off` has no autonomous
meaning — in an autonomous container there is no human, so `off` is neither a failure nor a
decision; the card's R6 doesn't assign it an arm.

**Reframe.** The proposed design is **not** sound as framed; it needs a scope correction,
not a replacement. EV-83 was carved by the design seat as a *copy/ledger* card (Finding E,
`vault/raw/2026-09-21-design-epic10-re-cut-surface.md:181–200`), but the composition it
requires is **engine**, not copy. **Minimal engine change = one child-mode tool that wraps
both exported halves.** A `council_followup_review` tool whose implementation calls
`runFollowupReview(...)` then `renderFollowupLinesFromRepo(...)`, returning the mechanical
per-candidate facts *plus* the rendered lines (so the failure basis is the exact bytes
`gate call failed: <reason>` / `credential unresolved` / `followup decision not recorded`).
That is still FLLWUP-96's one composition function — the tool is a second *caller*, not a
second composition. It must be wired at all three allowlist sites: a grant token
mapped/allowlisted in `seats.ts:615–617`, registered inside `runChildMode` (`child.ts`), and
permitted by `isCallAllowed`. Registering it only in `child.ts` is insufficient — the model
never sees a tool absent from `--tools`. **If product-owner rules the engine tool out of
EV-83**, then the card cannot meet its own Acceptance ("a container … returns
`ESCALATION`", "a runner fixture asserting `ESCALATION`") and must split into a prose card
plus an engine-card — the current goal is unachievable by prose alone.

**The two surfaces, worded to agree without restating step 13.** Phase 3: one sentence
naming *step 13's follow-up decision* as the disposition source for filed follow-ups, and
that a candidate whose decision failed/unresolved is recorded as an escalation, never a
filed card. Pointer, not rule. `council-runner.md`: a new block (not `<return_contract>`)
that (a) says the decision comes from step 13's mechanism, (b) pins
**ESCALATION-before-any-write** and **never silently drops one**, and (c) pins that a
*failed* call is never applied — keyed on the record's failure state, never on
`resolvedMode` (the fail-safe `File` trap above). The four-tag list stays byte-unchanged,
so the disposition-outcome content contract is stated in this block; the flagship risk is
a reader of `<return_contract>` alone not knowing DONE should carry it. `HALT`'s "no
disposition reached / partial state" pin belongs here too, and `off` in a container must
route (ESCALATION or HALT), not default to `File`.

**Testable claims.** (1) Runner reachability (red at base, engine half): spawn the real
child-mode tool surface in-process against a `mkdtemp` repo with `gate.mode: active`, a
failing `GateTransport`, and a drafted candidate; assert the runner-visible tool set
contains the follow-up review tool and that calling it returns `status: "failed"` plus the
rendered basis. Falsifier: at base the tool is absent from `buildDispatchArgv`'s output for
seat `council-runner` — `expect(tools).toContain("council_followup_review")` fails today.
(2) No silent drop / no write on failure: after the same call, `council/cards/` gains zero
files and the ledger has exactly the one recorded failure line; a variant that treats
`resolvedMode: "File"` as resolved must fail. (3) ESCALATION bytes are engine-derived, not
stub-canned: if a pure report-composer is added, its output for the failed candidate equals
`ESCALATION` + the runner card id + the candidate *title* + the rendered basis. Contract
collision: drafts have no allocated id, so "candidate id" in the Acceptance must be the
candidate **title**; a test should assert no `FLLWUP-` id appears on the failure path.
(4) `<return_contract>` byte-unchanged: prose test slices the four tag bullets and asserts
byte identity against head; a second prose test pins the new block's strings and Phase 3's
step-13 pointer (R0a: locate by heading, never line range). (5) `off` arm named: a prose
pin asserting the runner block assigns `off` an explicit autonomous route, so the mode
cannot silently fall through to File.

### Round 2 — owner (job-13.5)

**What changed — two concessions, both verified against code first.**

1. **Grant mechanism — owner concedes to the principal's ONE tool.** On the FLLWUP-96
composition axis the two designs are equal — `council_followup_review` wrapping
`runFollowupReview` (`followup-tool.ts:87`) + `renderFollowupLinesFromRepo`
(`followup-render.ts:242`) is a second *caller* of two exported functions. The tiebreaker
is the runner's actual flow and the copy-fidelity risk the pair design carries: with the
two-tool pair, `FOLLOWUP_RENDER_PARAMS` (`followup-render.ts:211–239`) forces the model to
echo the full mechanical result — title, callId, status, boardIds, siblingTitles — into
the render call; a mangled echo lands in `FOLLOWUP_RENDER_FALLBACK.joinMiss` → spurious
ESCALATIONs. The combined tool joins the ledger in-process against the actual result
object; the basis bytes are engine-derived by construction; the runner's step-13 flow
collapses to one call. Conditions on the concession: the tool is **child-mode-only** (the
parent path keeps the existing pair untouched); `mergeTargets` stays a **tool parameter**
(passed through to the render half — the tool must not absorb the dedup pass); grant
keyword stays `followup` wired at the three sites (`grantsFor`+`buildChildArgv` seats.ts,
registration+`isCallAllowed` child.ts, runner frontmatter `tools:`). The index.ts comment
is rewritten to name the real seam.

2. **The `off` arm — owner concedes to `off` → `ESCALATION`; its round-1 DONE-lean is
withdrawn.** (a) The card goal's own words — "fails **or cannot resolve**" — cover off: in
a container with no confirming human, an off gate is an unresolvable decision. (b) DONE's
per-candidate outcome must be a disposition in the `File | Merge | Drop` vocabulary; an
off draft has none, so "unconfirmed-presented" would introduce a fifth outcome shape.
(c) Uniformity: every other unresolved shape escalates. Converged rule, one sentence:
**any drafted candidate that ends the review without a resolved recorded disposition —
failed call, thrown call, total-failure literal, join miss, fall-to-human line, or gate
`off` — returns as `ESCALATION` before any write.** Honest cost stated: an autonomous
runner under the packaged default escalates every follow-up candidate — the safe direction,
matching the card goal; attended runs under `off` are untouched (council.md step 13
governs there).

**What owner holds/verified.** The fail-safe trap verified at the followup site with a
correction: the followup `failRun` (`gate-run.ts:455–484`) records `disposition: "File"`,
`resolvedMode: decision.disposition`, `basis: "gate call failed: <reason>"` — a failed
call's ledger line is mode-indistinguishable from a resolved File. Correction: the
**card-gate** `failRun` (`gate-run.ts:186–214`) records `resolvedMode: "Deliberate"` — the
trap is followup-specific; the Skeptic should test the followup site. The tool-result
discriminator is already sound: `FollowupReviewCandidate.status` is `"failed"` on every
recorded-failure arm (`followup-tool.ts:122,133`), and the rendered line for `callFailed`
is the `gate call failed:` literal, never a `Mode:` line. Contract sentence to go verbatim
into the block: **resolved ⟺ `status: "ok"` with a rendered decision line; a
`status: "failed"` candidate is unresolved no matter what mode its recorded line names.**

**Identifier — converged on title.** No `FLLWUP-` id exists at the ESCALATION action point
(ids are allocated at write time); the identifier is the candidate's **title, verbatim**.
Fixture asserts zero `FLLWUP-` tokens in the escalation report and zero new card files.

**Designer P1–P5 under R6.** P1: owner's Phase-3 replacement stands. P2–P4: closures live
in `<followup_decision>`; folded: per-candidate bullet enumeration on DONE (rendered line
verbatim — `decisionLine` owns the bytes); ESCALATION carries title + explicit "no card
was written" + verbatim basis; HALT pins `no disposition reached` or
`partial: dispositions reached for N of M candidates — <titles>`. Audit-mapping clause:
DONE lines audit against the gate ledger by callId; ESCALATION titles map to
`FollowupReviewCandidate.title`. P5: byte-identity test on RETIRED's clause plus zero
`Drop`-count grep.

**Newly flagged open point (owner flags, not decides): in-container confirmation
authority for resolved dispositions under `active`.** Step 13 applies a disposition "only
after confirmation," and the no-human argument cuts both ways: it proves `off` escalates,
and it begs who confirms a resolved `File` before the runner writes on DONE. Owner's
proposal for the block copy: the recorded gate decision is the container's confirmation
(the orchestrator's dispatch stands in for the human), and the runner never substitutes
its own judgment — anything short of a resolved recorded line escalates. Needs a ruling
before implementation.

**Converged/unconverged.** Converged: (1) ONE child-mode tool wrapping both halves; parent
pair unchanged; grant keyword `followup` at three sites. (2) `off` → `ESCALATION`, no
special case. (3) Resolved-vs-failed keys off `status`/failure state, never the disposition
string. (4) Identifier = title. (5) `<return_contract>` byte-unchanged; P2–P4 closures in
the block. (6) RETIRED byte-identity + zero-`Drop` pin. (7) FLLWUP-96 intact. Unconverged:
(a) the in-container confirmation basis for resolved dispositions — needs a ruling;
(b) the fail-safe-trap claim still Skeptic-tested at step 4; (c) exact block wording once
(a) settles.

### Round 2 — principal (job-13.6)

**Cross-seam reading (verified this round).** Capability boundary confirmed: the pair is
parent-path only (`index.ts:637–638`); `runChildMode` registers only hub tools
(`child.ts:22–27`); the child's tool surface is the exact-name `--tools` allowlist
(`seats.ts:615–617`) enforced by `isCallAllowed` (`child.ts:10–23`). Today
`runFollowupReview`'s only caller is the gate tool (`followup-tool.ts:178`).

**The fail-safe trap — confirmed, and narrower than first stated.** `runFollowupGate`'s
`failRun` builds `decision = { disposition: "File", basis: "gate call failed: <reason>" }`
and writes `resolvedMode: decision.disposition` (`gate-run.ts:476–484`); the mechanical
result carries `status: "failed"` (`gate-run.ts:214`). **But the render half never emits a
`Mode: File` success line for it** — `classify` maps a `failureClass` record to
`{kind:"callFailed"}` and renders `gate call failed: <reason>`
(`followup-render.ts:137–140,196–197`). The trap bites **only a runner that consults
`council/gate-ledger.jsonl` directly**, not one that uses the tools. Sharpened pin: the
block must either forbid ledger-direct reading or carry the warning sentence.

**`off` is mechanically distinct, with no candidate arm.** `runFollowupReview`
short-circuits to `{mode:"off",recorded:0}` before the loaders (`followup-tool.ts:103–106`);
the render returns the bare `["off"]` literal (`followup-render.ts:156`); there are no
candidate entries. `runFollowupGate` itself throws on `off` (`gate-run.ts:414–418`), so
`off` never reaches a per-candidate disposition. The owner's round-1
DONE-with-unconfirmed lean is mechanically well-founded, not a fall-through.

**1. Wrapper vs grant — principal concedes to the owner's grant pair.** Deciding weights:
step-13 parity (the attended procedure names *these two tools*; naming a wrapper forces the
reader to map a new tool name back onto step 13's pair); engine surface (the wrapper adds a
tool body + schema + a second composition-wiring path; the grant adds two child
registrations, one `--tools` push, one `isCallAllowed` arm — sanctioned by convention 8's
`hub` precedent); both preserve FLLWUP-96. Conditions of the concession (the wrapper's real
benefit becomes pins, not mechanism): the two-call protocol is pinned —
`council_followup_gate` ONCE → pass its result **verbatim** to `council_followup_render` →
present lines verbatim; the render schema already requires the gate result
(`FOLLOWUP_RENDER_PARAMS.result`), so a dropped/absent result fails loud as a join miss;
a negative test that the render tool cannot be satisfied without a gate result.

**2. Owner's block copy vs principal's requirements.** Covers ESCALATION-before-any-write
and never-silently-drop and the HALT pin; keys off `status: "failed"` which *is* keying off
failure state. Pins it misses: (i) no sentence guarding the ledger's `resolvedMode` — add:
*a recorded failure carries the fail-safe `File` as its ledger `resolvedMode`; the
`status: "failed"` / `gate call failed:` basis is the discriminator, never the mode
string*; (ii) "candidate id" is undefined pre-write — the identity is the **drafted title**.

**3. `off` arm — principal withdraws its round-1 ESCALATION/HALT demand and converges to
the owner's round-1 DONE-with-unconfirmed arm.** Mechanically, `off` is neither a failure
nor an unresolved machine decision, so `ESCALATION` and `HALT` are both wrong tags; the
DONE-with-unconfirmed arm writes nothing and does not silently drop. One cross-seam
condition: the owner's Phase-3 replacement sentence routes only the failed/unresolved class
to `ESCALATION` and says such a candidate appears "only through its runner's `ESCALATION`" —
under `off` the runner reports on DONE, so that sentence omits the third category entirely;
Phase 3's ledger line must gain the presented-unconfirmed category or the `off` candidates
are dropped at the ledger seam.

**4. Designer P1–P5 endorsed** (P4: prefer the designer's concrete
`partial: dispositions reached for N of M candidates — <list>` form; P5: byte-identity +
zero-`Drop` grep).

**Blind spots named.** Owner cannot see that `off` forms a third report category its own
Phase-3 sentence doesn't name, nor that the ledger's `resolvedMode` is a fail-safe `File`
on failure despite the render never printing a File line. Designer cannot see the
child-mode capability boundary. Skeptic cannot see that a Hub+stub-child fixture proves
plumbing and stub obedience, not that a live runner calls the tools in order — the EV-69
caveat holds; the fixture is necessary but not sufficient.

**Reframe: none needed** — the owner supplied the seam; agreement, not reframe, is the
contribution now.

**Testable claims.** (1) `grantsFor(followup).followup === true`; `buildChildArgv` output
for `council-runner` contains the pair; `isCallAllowed` flips true (false at base). (2) The
render tool cannot be satisfied without a gate result: absent/forged `callId` yields the
`recorded followup call not found in ledger` fallback, never a disposition line. (3)
Failure-shaped, ledger-independent: failing transport → `status:"failed"`, `gate call
failed:` line, zero new card files, exactly one ledger line with `resolvedMode:"File"` — a
fixture treating `resolvedMode:"File"` as resolved must fail. (4) Identity is the title,
not an id: title present, `/\bFLLWUP-\d+/` absent on the failure path. (5) `<return_contract>`
byte-unchanged; RETIRED clause byte-identical; the new block carries the `resolvedMode`
warning and the off arm; Phase 3 names the step-13 decision **and** the presented-unconfirmed
category.

**Converged:** grant pair; ESCALATION-before-any-write/never-drop/HALT pins; `off` →
DONE-with-unconfirmed (withdraws ESCALATION/HALT); P1–P5 shapes; RETIRED byte-identity.
**Unconverged:** (a) whether the `resolvedMode`-trap warning is required copy (principal:
required — cheap, makes the shape invariant); (b) the Phase-3 third category for
off/unconfirmed drafts — absent from the owner's replacement sentence, the one place the
two surfaces genuinely disagree.

### Round 2 — designer (job-13.7)

**Design position.** The owner's `<followup_decision>` block is structurally right but
needs five redlines before it survives a cold read: the candidate identifier must be the
**draft title**, not `id` (principal is right on the contract collision); the no-card
invariant must carry the explicit **"held, not filed"** word so the next runner can resume;
the DONE per-candidate outcome must be locked to the literal
`Mode: <disposition> — <basis>` byte sequence, formatted as a **bullet list**; the HALT
partial state must be structured `partial: N of M — <list>, held: <list>`; RETIRED's
constraint pinned as a **byte-identity test**, not a paraphrase.

**Redlined block copy** (placed after `<escalation_contract>`; `<return_contract>`
unchanged): the redline adds to the owner's draft — "A candidate's identifier at the
report's action point is its **draft title** — drafts carry no `FLLWUP-N` id until a card
is written"; ESCALATION carries "**each affected candidate's draft title and the verbatim
basis**"; "The affected candidate is **held, not filed**, recorded by its draft title in
your report and resumable by the next runner against the same drafted title"; DONE as "a
bullet list — one bullet per candidate, keyed by its draft title — each bullet carrying
the rendered disposition line verbatim (the literal `Mode: <disposition> — <basis>` byte
sequence produced by `renderFollowupLinesFromRepo`; you do not paraphrase)"; HALT as
"`partial: dispositions reached for N of M candidates — <list of draft titles whose
disposition was reached>, held: <list of draft titles whose disposition was not
reached>`".

**Redlined Phase-3 sentence** (owner's replacement + the held word + draft-title +
resume path): "Each surfaced follow-up candidate's disposition is the recorded follow-up
decision at step 13's confirm gate — the ledger names the recorded disposition per
candidate by its draft title and never re-decides one — and a candidate whose decision
failed or did not resolve is **held** by its draft title in this ledger, recorded only
through its runner's `ESCALATION` (no card filed, no silent drop), so the next runner can
resume it against the same drafted title."

**Q2 (mechanism, comprehension side only).** Neither mechanism changes what the runner can
present at the action point; both preserve the EV-82 rendered-line grammar. The mechanism
choice affects the runner's tool surface, not the orchestrator's reading. Net: tie on
comprehension; the runner-tooling tradeoff is the owner's grant vs the principal's bundled
tool — routed to `owner` for the engineering call; the design seat takes no position.

**Q4 (RETIRED pin).** Converged; the exact pin: the byte sequence from `<return_contract>`
through the closing fence is byte-identical to before; `Drop` count zero in RETIRED's
span; **and `candidate` count zero in that span** (the load-bearing third assertion —
prevents a future editor from quietly adding candidate-disposition content to RETIRED).

**What changed from round 1.** P1–P4 absorbed into the redlines; contract collision made
explicit (title replaces id); `held` promoted from preference to load-bearing requirement
(resume loop depends on it); mechanism question reframed as a runner-tooling call this seat
does not own; P5 pinned as a three-assertion test.

**Unconverged (designer's view).** Mechanism (owner grant vs principal wrapper) —
runner-tooling call, both valid, routed to owner. **`off` mode routing in an autonomous
container — escalated to `product-owner`: under `off` in an autonomous container, does the
runner ESCALATE, HALT, or carry unconfirmed drafts on a `DONE` report?** Bullet-list
locked. Audit-pointer shape locked as implicit.

**Round-2 cross-read note (facilitator):** the seats were dispatched simultaneously and
CROSSED on both open items — the owner conceded to the principal's round-1 wrapper (the
one-tool mechanism) while the principal conceded to the owner's round-1 grant pair; and
the owner conceded its round-1 DONE-with-unconfirmed arm to `off` → `ESCALATION` while the
principal withdrew its round-1 ESCALATION demand to the owner's round-1 DONE-with-unconfirmed
arm. Round 3 (the cap) is required to resolve both crossings.

### Round 3 — owner (job-13.8, FINAL)

**1. Mechanism: the wrapper — owner concedes back to the principal's round-1 design, now
adopted by both.** The crossing resolved on the asymmetry between the two weights: the
principal's step-13 parity concern is a *naming* concern, dischargeable by prose; the
fidelity concern is *mechanical*, and no prose can fix it. Verified: `FOLLOWUP_RENDER_PARAMS`
(`followup-render.ts:211–239`) requires the model to re-transmit title, callId, status,
boardIds, siblingTitles verbatim into the render call; a mangled echo lands in
`FOLLOWUP_RENDER_FALLBACK.joinMiss` and renders an unresolved-shape line for a call that
actually succeeded — a spurious `ESCALATION` every run is exposed to. The wrapper joins the
ledger in-process against the actual result object; basis bytes engine-derived by
construction. Step-13 parity answered: parity holds at the composition level (the wrapper
calls `runFollowupReview` then `renderFollowupLinesFromRepo` in the same order — a second
caller, FLLWUP-96 intact); the attended surface keeps the parent pair byte-unchanged; the
autonomous runner is a fresh reader surface — parity discharged with one mapping sentence
in the runner block plus the wrapper's tool description. Conditions carried: **child-mode-only**
(parent pair untouched), **`mergeTargets` stays a tool parameter**, grant keyword
**`followup`** at the three verified sites, index.ts comment rewritten.

**2. The `off` arm: `off` → `ESCALATION`, keyed at the result level — owner flips back to
its round-2 position, defeating its own round-1 lean on the principal's mechanical facts.**
Under `off`, `runFollowupReview` short-circuits to `{mode:"off", recorded:0}` before the
loaders (`followup-tool.ts:96–97`) — zero candidate entries exist; the render returns the
bare `["off"]` literal; `runFollowupGate` throws on `off` as a programming error
(`gate-run.ts:415–418`) — the engine's own stance is that `off` is a policy state, not a
reachable failure. An ESCALATION under the card's acceptance shape must carry candidate id
+ verbatim basis — under `off` there is no per-candidate artifact and no engine-derived
basis bytes; a DONE-with-unconfirmed arm would ride drafts with no disposition value, and
the routing is keyed on the result-level `mode` flag: `result.mode === "off"` → no resolved
recorded disposition → `ESCALATION` naming every drafted candidate by title, before any
write. Honest cost stated: an autonomous run under the packaged default escalates every
surfaced candidate — the safe direction; attended runs under `off` untouched. Submitted
under the designer's standing product-owner escalation as the owner's recommendation, not
the ruling.

**3. `resolvedMode`-trap warning: CONFIRMED required copy.** Verified as above; the warning
sentence goes in verbatim as the principal drafted it; the block must also forbid
ledger-direct reading. The pin is followup-specific (card-gate `failRun` records
`Deliberate`); the Skeptic tests the followup site.

**4. Confirmation authority: CONFIRMED.** Under `active`, the recorded gate decision is
the container's confirmation — the orchestrator's dispatch stands in for the human confirm
step; the runner never substitutes its own judgment; anything short of a resolved recorded
line (`status: "ok"` with a rendered decision line) escalates.

**Residual open list:** (1) the `off` arm — under the designer's product-owner escalation;
(2) exact block wording once `off` settles; (3) the trap claim — Skeptic-tested at step 4.

### Round 3 — principal (job-13.9, FINAL)

**1. Mechanism: FINAL — one child-mode tool `council_followup_review` (principal adopts
the owner's round-2 concession).** The fidelity weight is correctness-on-the-failure-path;
the parity weight is a naming-mapping weight. Correctness dominates. Verified: the join is
by random `callId` ONLY (`followup-render.ts:19–22, 74–78`); `FOLLOWUP_RENDER_PARAMS`
forces the model to re-transmit the mechanical facts by hand; a dropped/mangled echo lands
in `classify`'s `!record` branch → `FOLLOWUP_RENDER_FALLBACK.joinMiss`, and an omitted
`candidates` array renders zero lines — each is unresolved under the converged rule →
spurious `ESCALATION`. The pair makes fidelity a model obligation on the failure path; the
wrapper makes it an engine invariant. Step-13 parity answered: (a) child-mode-only, parent
pair untouched — attended step 13 byte-unchanged; (b) the runner block names the wrapper
with one mapping sentence; (c) second caller of the same composition, FLLWUP-96 intact,
and the child surface is one `--tools` name instead of two. The owner's conditions become
pins.

**2. The `off` arm: FINAL — `off` → `ESCALATION` (principal flips; adopts the owner's
round-2 position).** The mechanical-distinctness finding does not block ESCALATION — it
dictates where the routing is keyed: `off` has no candidate entries, so ESCALATION cannot
be synthesized from `result.candidates`; it is keyed on the result-level `mode` flag —
`result.mode === "off"` → no resolved recorded disposition → `ESCALATION` naming every
drafted candidate by draft title (from the runner's own draft set), before any write. This
removes the principal's round-2 Phase-3 third-category objection: the existing
"only through its runner's `ESCALATION`" clause now covers `off`; no third category needed;
the surfaces agree. Keeps DONE's vocabulary exactly `File | Merge | Drop`; one unresolved
path. Honest cost stated as above.

**3. `resolvedMode`-trap warning — required copy; confirmed** (plus: the block forbids
ledger-direct reading).

**4. Confirmation authority — confirmed, with two pins.** Under `active`, the recorded
(engine-minted) gate decision is the container's confirmation; the orchestrator's dispatch
is the standing authorization; the runner never substitutes its own judgment. Pins:
(1) "confirmed" keyed on `status:"ok"` plus a rendered decision line, never the ledger's
`resolvedMode`; (2) **scoped to `active`** — under `advisory` the lines are "information,
never enforced" (EV-82 spec), so a recorded advisory decision is NOT a confirmation and an
autonomous container has no authority to apply it.

**Residual open list:** (1) **`advisory` in the autonomous container** — not covered by the
`active`-scoped confirmation pin; proposed `advisory` → `ESCALATION`, keyed on the
result-level mode flag exactly as `off`; needs a ruling before implementation (the designer
escalated only `off`). (2) The `off`-arm fixture must assert ESCALATION is synthesized from
the result-level mode flag + the runner's draft set. (3) The fail-safe-trap Skeptic test at
step 4 — the followup site. (4) Final block wording once mechanism and off-arm are fixed.

**Round-3 convergence (facilitator record):** both crossings closed — mechanism = the
wrapper (`council_followup_review`, child-mode-only, parent pair untouched, `mergeTargets`
a tool parameter, `followup` grant at three sites, index.ts comment rewritten);
`off` → `ESCALATION` keyed on the result-level mode flag with the draft-set titles. Both
seats converged; the designer's standing product-owner escalation on the `off` arm and the
principal's new `advisory` residual go to step 5/6. Positions have stabilised — the
exchange closes at three rounds (cap respected).

### Step 4 — skeptic attack (job-13.10)

Probe suite: `bun test /tmp/ev83-skeptic-probes/probes.test.ts` — **13 pass / 0 fail / 86
expect** — with absolute imports into the repo; every fixture repo under `os.tmpdir`; the
real `.council.json` never touched; `git status --porcelain` clean after (only the
pre-existing untracked `vault/raw/*`).

- **1. Fail-safe trap at the followup site — `closed-green`.** Fixture repo
  `gate.mode: active`, `failDouble` transport: mechanical candidate `status: "failed"` with
  non-null callId; ledger exactly one line with `resolvedMode: "File"`,
  `failure.class: "network"`, `basis: "gate call failed: transport exploded"`; render
  `["gate call failed: transport exploded"]` — zero `Mode:`-prefixed lines. A
  `record.resolvedMode === "File"` check returns *resolved* for the failed call; only
  `status`/`failure` discriminates. Owner's correction verified: card-gate `failRun` writes
  `resolvedMode: "Deliberate"`.
- **2. Capability base — `closed-green` (the real red-at-base seam).** `grantsFor(runner)`
  has keys exactly `["hub"]`; `buildChildArgv`'s `--tools` for `council-runner` contains
  zero followup names; `isCallAllowed` false for `council_followup_gate/_render/_review`;
  control `council_dispatch` true.
- **3. Echo fidelity — `closed-green`.** After a genuinely resolved review, the correct
  echo renders `Mode: File — <basis> — … (active)`; a forged `callId` echo renders
  `FOLLOWUP_RENDER_FALLBACK.joinMiss` = `recorded followup call not found in ledger` — an
  unresolved-shape line for a resolved call. Confirms the pair's model-echo fragility the
  wrapper removes.
- **4. `off` shape — `closed-green`.** `runFollowupReview` → byte-exact
  `{ mode: "off", recorded: 0 }`, zero ledger lines, transport never called; render →
  `["off"]`; `runFollowupGate` throws the programming-error guard.
- **5. `advisory` shape — `closed-green`** (informs the unruled residual). No enforcement
  surface exists anywhere: the result carries no enforcement field, the ledger line only
  `advisory: true`; the rendered line differs from `active` by the parenthesized qualifier
  alone. The "advisory is not confirmation in a container" pin rests entirely on the runner
  prose — no code enforces it. A boundary the block copy must carry.
- **6. RETIRED baseline — `closed-green`.** The RETIRED bullet span has zero `Drop` and
  zero `candidate` occurrences.
- **7. Byte baselines — `closed-green`.** Phase-3 sentence contains no
  `ESCALATION`/`disposition`/`failed`; `<return_contract>` contains no
  `gate call failed`/`Mode:`/`candidate`; `no disposition reached` absent from runner.md;
  the tool pair absent from runner.md; frontmatter tools line unchanged.
- **8a. Plan defect — `closed-red`.** The promised "runner fixture … red-at-base" is
  unsatisfiable as specified: the fixture mechanism (stub child spawned via `hub.spawnJob`
  as a bare `bun` process — no pi, no `--tools`) composes the report from *exported
  functions present at base*. The skeptic built that exact composition at base and every
  promised fixture assertion passed at base (first line `ESCALATION`, title present,
  `gate call failed: boom` present, zero new card files). The mechanism-under-test
  (wrapper + grant + allowlist) is never touched by the fixture as specified. To be
  genuinely red-at-base the fixture must fold in the tool-surface assertions (allowlist/
  `isCallAllowed`); otherwise the red-base record is unit-test-only.
- **8b. Plan defect — `closed-red`.** "Zero `FLLWUP-` ids on the failure path" is vacuous
  on realistic draft titles: this repo's own fixture idiom drafts titles of the literal
  form `FLLWUP-101 probe`, so `/\bFLLWUP-\d+\b/` over the report matches the draft title —
  red on realistic runs, trivially green on custom titles. The meaningful zero-check is on
  `council/cards/` (no new card files); that discriminator must be the pin.

Also verified (not an objection): child tool registration runs through `pi.registerTool`
inside `runChildMode`, so the wrapper's child-mode registration is an established working
path; `runChildMode`'s registration-vs-`registerHubTools` separation keeps the parent pair
parent-path only.

**Skeptic verdict:** no open objections on the engine design — every engine claim the
convergence rests on held under test. Two plan-level defects stand (`closed-red`, both
demonstrated): the fixture's red half must be the tool-surface assertions, and the id
assertion must target `council/cards/` file writes. The test plan must be re-specified
before the fixture claims its recorded red. These do not block the convergence; they are
spec-input corrections.

### Step 5 — consolidator synthesis (job-13.11)

**Agreed design** (every seat converged by round 3, cap respected):

1. **Mechanism:** one child-mode-only tool (`council_followup_review`) wrapping
`runFollowupReview` + `renderFollowupLinesFromRepo` — a second caller of the same FLLWUP-96
composition, not a second composition. The parent-session tool pair untouched. Granted via
the `followup` keyword in seat frontmatter, wired at three sites: `grantsFor` +
`buildChildArgv` in `seats.ts`, registration + `isCallAllowed` in `child.ts`, and
`council-runner.md` frontmatter `tools:`. `mergeTargets` stays a tool parameter. The
`index.ts` comment rewritten to name the actual seam.
2. **`off` arm:** `off` → `ESCALATION`, keyed on the result-level `mode` flag; drafts ride
by title; before any write. Attended runs under `off` untouched (council.md step 13).
3. **Phase-3 prose:** the replacement sentence names step 13's follow-up decision as the
disposition source and the `ESCALATION` route for failed/unresolved candidates.
4. **`<followup_decision>` block in council-runner.md** after `<escalation_contract>` (not
inside `<return_contract>`, R6): ESCALATION-before-any-write; never silently drops; the
`resolvedMode`-trap warning (forbids ledger-direct reading); confirmation-authority
statement (recorded gate decision = container's confirmation under `active`); per-candidate
bullet enumeration on DONE; HALT partial-state pin; RETIRED byte-identity.
5. **Identifier:** the drafted title, not an `FLLWUP-N` id; the id-assertion test targets
`council/cards/` file writes, not a regex over draft titles (skeptic 8b's fix).
6. **Resolved-vs-failed discriminator:** keyed on `status`/failure state, never the
disposition string; the trap is followup-specific.
7. **Prose baselines:** `<return_contract>` byte-unchanged; RETIRED byte-identical with
zero `Drop` and zero `candidate`; Phase-3 byte pins against current text.

**Settled disputes** (closure types): mechanism — convergence + test (probes 2, 3 green);
`off` arm — convergence + test (probe 4 green); the designer's round-2 escalation on `off`
routing is superseded by the round-3 convergence + green test; `resolvedMode`-trap warning
— convergence + test (probe 1 green; card-gate records `Deliberate` confirmed); skeptic
plan defects 8a/8b — closed-red facts, fix directions given (fixture red half = tool-surface
assertions; id pin = `council/cards/` writes); prose baselines — byte tests green (probes
6, 7); R6/R0a/EV-78/EV-82 — settled by recorded ruling; FLLWUP-96 intact — ruling + the
wrapper's caller topology verified.

**Open judgment (routes to `product-owner`, escalating to `steward`):**

1. **Confirmation-authority design judgment.** Under `active`, does the recorded
(engine-minted) gate decision constitute the container's confirmation — does the
orchestrator's dispatch standing in for the human confirm step suffice for the runner to
apply the disposition and write a card? For: the run is autonomous; the gate was called;
`status: "ok"` means the recorded line is the confirm-gate product; the dispatch is the
standing authorization. Against: step 13 says "only after confirmation" — a recorded
machine result is not the same consent as a human confirming; the runner may apply a
disposition the human would have rejected; the dispatch is authorization to *run*, not to
*confirm*. The principal scoped the converged pin to `active` only. Both seats converged
round 3; no test can settle an authority question; no ruling covers it.

2. **`advisory` arm in the autonomous container.** The principal's round-3 residual:
proposed `advisory` → `ESCALATION`, keyed on the result-level mode flag exactly as `off`.
No seat convergence; no ruling. The skeptic's probe 5 verified the mechanical facts green
(no enforcement surface exists anywhere; the ledger line carries only `advisory: true`; the
rendered line differs from `active` by the parenthesized qualifier alone) — but the green
test confirms only mechanical shape, not the routing. An `advisory` result in a container:
escalate, halt, or carry unconfirmed drafts?

**Open objections:** none — the skeptic left no objection without a passed settling test;
the two closed-red plan defects carry given fix directions and are spec-input.

**Consolidator's hand-off verdict:** not ready — the two open-judgment items block step 6
routing. Everything else (mechanism, `off` arm, prose surfaces, plan defects, binding
rulings, trap warning, FLLWUP-96 intactness) is settled by test or ruling, and the
implementation spec is otherwise complete.

### Step 6 (resumed, 2026-09-22) — product-owner ruling, binding

Full ruling document: `vault/raw/2026-09-22-po-ev83-confirmation-authority-ruling.md`.
Appended verbatim per `<escalation_contract>` step 3 and treated as binding:

> **Q1 — No. The recorded (engine-minted) gate decision is the container's disposition
> *source*, never its confirmation.** In-container the confirmation act belongs to a
> ruling seat reached through the orchestrator's existing `ESCALATION` service. So a
> runner with a *resolved* `active` disposition also escalates: it carries each drafted
> candidate by title with the recorded `Mode:` line verbatim as the disposition to be
> **ratified**, writes nothing, and applies + writes only on the resumed dispatch whose
> input contains the confirming ruling — at which point `DONE` carries the per-candidate
> outcome lines.
>
> `active` still buys something real, and it is not a write licence: it converts the
> confirming seat's act from re-deciding into ratifying. The three modes differ only in
> what the packet carries — `off`: no line, decide from scratch; `advisory`: line as
> information, the reader's decision governs; `active`: the recorded disposition applies
> once confirmed. `gate.mode` keeps meaning *whose decision is applied*.
>
> **Q2 — `advisory` → `ESCALATION`, keyed on the result-level `mode` flag exactly as the
> settled `off` arm**, escalated by draft title, writing nothing, carrying the rendered
> advisory lines verbatim as information only, with the basis naming advisory-only rather
> than a call failure. Attended `advisory` runs untouched (§13 governs there).
>
> **Binding deltas (the *what*; the *how* stays yours):** (1) the converged sentence
> "the recorded gate decision is the container's confirmation" is **replaced** in the
> block copy; (2) the `resolved ⟺ status:"ok"`-with-a-rendered-line discriminator keeps
> the two escalation arms *distinguishable by basis* — failed/unresolved vs
> confirmation-pending; (3) Phase 3's sentence must cover the confirmation-pending arm as
> a pointer agreeing with §13, and `council.md` §13 is **not** edited by EV-83 (EV-82's
> shipped, prose-tested surface) — byte baselines re-expressed, never deleted; (4)
> `DONE`'s per-candidate section is the post-confirmation arm, so bullet 3 stays
> non-vacuous. The wrapper mechanism, the `followup` grant at three sites, the
> `resolvedMode` fail-safe-`File` warning, identifier = draft title, R6, and skeptic
> 8a/8b are untouched and not reopened.
>
> **Options rejected:** (Q1) recorded decision = confirmation (authority-map exhaustivity,
> gate circularity, contradicts shipped §13, makes "unconfirmed" weightless); orchestrator
> confirms on dispatch (no such row); standing pre-authorization (a human Phase-1 act,
> named for a later run). (Q2) `HALT` (a policy state, not an environment failure; would
> freeze a working run); carry unconfirmed drafts on `DONE` (needs a third Phase-3
> category R6 forbids; drops at the ledger seam).
>
> **Grounding:** authority map exhaustivity clause; `council/procedures/council.md` §13
> "in every mode"; `council/agents/council-runner.md` `<escalation_contract>` and
> `<convergence_is_not_evidence>`; `test/prose.test.ts:526`; `council/cards/EV-82.md`
> (this surface's premise); `council/cards/EPIC-10.md` bullet 4 + Intent;
> `vault/raw/2026-09-21-po-epic10-recut-ruling.md` R0b/R4/R6/R9; the three prior step-13
> confirmations in this run. **Not escalated.**
>
> **Reversibility:** wrong ruling costs one ruling-seat round trip per follow-up-bearing
> card (already paid three times); the opposite call would leave unapproved cards on the
> board under `FLLWUP-69`'s unenforced write path.

**Routing (step 6 closed):** both open-judgment items — confirmation authority (Q1) and
the `advisory` arm (Q2) — are settled by the ruling above; no open objection remains (the
skeptic's 8a/8b plan defects carry given fix directions and are spec-input, per step 5).
No item routes to `Needs Human`. The card proceeds to step 7.

### Steps 7–9 (2026-09-22)

- **Step 7:** spec written to `docs/superpowers/specs/2026-09-22-EV-83-design.md`
  (commit `d9e4518`), implementing the round-3 convergence plus the binding ruling's four
  deltas. Card set `In Progress` (`11e3865`).
- **Step 8:** owner implemented in worktree `.worktrees/ev-83`, branch
  `ev-83-runner-followup-routing`, PR #102 open at head `3e8b9b3` (job-15.1, 31.6m).
  Owner gates claimed green: `tsc` clean; `bun test` 1446 pass / 5 skip / 0 fail;
  `validate.py` clean; preflight clean. Red-at-base recorded: base `4aae0fe` →
  `2 pass / 14 fail`, head → `16 pass / 0 fail`, red half = tool-surface assertion
  (skeptic 8a), no-write pin = cards-dir snapshot (skeptic 8b). Version bumped
  0.32.0 → 0.33.0. Card set `In Review` from the observed open PR (`1df2141`).
- **Step 9:** skeptic verification at the pinned head (job-15.2, 11.7m). Nine objections:
  eight closed-green, one closed-red **non-blocking** (objection 7 — the
  `confirmation-pending`/`advisory-only` basis tokens are present in shipped prose but
  not pinned by EV-83's own tests; advisory arm shape covered by EV-82's passing suite;
  "worth a future prose-pin commit" — carded at step 13). All four owner gates re-run by
  the skeptic, green; red-at-base reproduced exactly; failure-injection probes
  (write-a-card, drop-a-candidate) both red as required; resolvedMode trap demonstrated
  and pinned; main repo `--porcelain` clean. **Verdict: PASS — no blocking objection.**
  Facilitator observed CI at the pinned head: `gates` workflow `state: SUCCESS`
  (`gh pr checks 102`), read against `headRefOid 3e8b9b3`. Routing note: `council_route`
  is not granted in this container; the card's recorded `Deliberate` mode (step 1
  fallback, run input) stands; no re-route observed. Proceeding to step 10.
- **Step 10:** judge PASS (job-15.3, 2.2m) — goal met at the branch head; basis: the
  EV-83 suite 16/16 at head (2/14 at base), the goal falsifier asserting `ESCALATION`
  first line + zero new card files with genuinely-failing discriminators, full suite
  1446/0, CI `gates` SUCCESS at the pinned SHA.
- **Merge (step 11 executed deterministically per the merge protocol, P1-1 run-scoped
  admin authorization):** all five Deliberate criteria observed — (1) owner gates green
  in full (skeptic re-run); (2) `gh pr checks 102` → `gates` workflow `state: SUCCESS`,
  read against `headRefOid 3e8b9b3`; (3) no blocking Skeptic objection; (4) judge PASS;
  (5) no `Needs Human`/outstanding ruling. Merged
  `gh pr merge 102 --squash --admin --match-head-commit 3e8b9b3…` → merged SHA
  **`6a5d70c854f2690d8b1e1db467de656723be4c4f`**; CI `gates` observed `completed /
  success` on the merged SHA.
- **Step 12:** `git pull --rebase` hit spurious conflicts (the squash commit already
  carries the content of the pre-`4aae0fe` record commits — git replayed them as
  duplicates). Aborted; containment verified (`git diff 4aae0fe 6a5d70c` shows only the
  branch's ten deliverable files; all record files byte-identical); replayed only the two
  genuinely-unmerged record commits with `git rebase --onto origin/main 4aae0fe main`
  (`761181f` In Review, `f19c2dc` step 9) — union-keep honored: no side's content lost,
  the squash already carried the rest. Conflict-marker sweep clean; `validate.py` clean.
  Card set `Done` from the observed merged SHA + green CI on it. Record commit pushed
  directly to `main` under P1-RP (run-scoped authorization). No `package.json` bump
  needed at step 12 — the PR itself bumped 0.32.0 → 0.33.0.
