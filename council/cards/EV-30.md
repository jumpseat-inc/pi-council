---
id: EV-30
title: Invocation-scoped spend record from the invoking session plus its job subtree
state: Deliberating
owner: null
epic: EPIC-7
goal: Given a session JSONL carrying assistant, tool-result and compaction usage plus a run directory carrying that invocation's job manifests, the accounting function returns a spend record whose own-session fields equal the hand-summed usage of the entries at or after the invocation boundary and whose subtree fields equal the sum over the invocation's job forest, and an automated test over a fixture session and manifest set asserts both halves against the hand sum.
---

## Intent

The invoking session has no manifest at all — the hub writes manifests only for
jobs it dispatches — so the hub's job forest is not the invocation's spend. This
card is the accounting function: given the invoking session's JSONL and the
invocation's run directory, it returns a spend record whose own-session half is
the hand-summable usage of entries at or after the invocation boundary
(assistant, tool-result and compaction usage; pi counts all entries, including
those compacted away) and whose subtree half is the sum over the invocation's
job forest. The boundary is the entry id of the user message the procedure
injects. The record names the boundary it covers, so a reader knows what the
number includes. Attended runs cannot express a run total today because every
top-level manifest has `parentJobId` null; the record is the first object that
can. Edge cases — zero dispatched jobs, a rotated session, an already-pruned run
directory — resolve to a zero subtree, never an error.

## Acceptance

- A fixture session plus a fixture manifest set yields own-session fields equal
  to the hand sum of entries at or after the boundary and subtree fields equal
  to the hand sum over the job forest, asserted by an automated test.
- A zero-job invocation returns a zero subtree.
- The record carries the boundary it covers.
- `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` stay green.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-4 boundary label** — the spend record names its boundary as
  `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>`.

## Run record

### Step 1 — classification (facilitator)

- **Path: full council.** Two independent triggers: (a) *spec-ambiguity* —
  the card's `goal` names an accounting function over "the invoking session's
  JSONL" and "the invocation's run directory" but not where that function
  lives, how it reaches the session's entries (the live
  `ctx.sessionManager` vs. the session file path), or how the *boundary* entry
  id is captured at injection time — the procedure injects its user message
  through a generic command handler (`extensions/index.ts:205-236`), which is
  the only place the boundary exists, and more than one reasonable design
  reaches it; (b) *design judgment* — the card's own Intent requires the
  attended-run rootlessness fact to be reconciled explicitly (top-level
  manifests carry `parentJobId === null`, so `sumSubtree(manifests, "job-1")`
  covers one seat, not the run), and the record's own-session field names and
  the zero-subtree edge cases admit more than one shape consistent with the
  goal.
- **Surface-touching: no.** The deliverable is a data record plus an
  automated test; nothing in this card renders a surface a person reads or
  acts on. EV-32 owns rendering the record at exit points, and the record's
  boundary string is already bound by Phase 1 ruling R-4. Seating: `owner` and
  `principal` as the two generators in steps 2–3; `designer` is not seated.

### Step 2 — independent first pass (round 1, generators only)

Each seat received the card alone — no other position, no hint. Positions
verbatim, labeled by seat. Jobs `job-9.1` (owner), `job-9.2` (principal).

#### Position — `owner`

**Approach.** A new pure module `extensions/spend.ts` owns the accounting; the invocation boundary becomes durable session data via a `pi.appendEntry` marker stamped in the procedure command handler (`extensions/index.ts:215-231`) — the only call-site change EV-30 lands. The subtree half is a *forest* rule (time-eligible top-level roots + `parentJobId` descendant closure), which reconciles attended rootlessness without a root parameter.

---

### (a) The design

**Where it lives.** New module `extensions/spend.ts`, pure (no `node:fs`, no ctx, no hub). Rationale: `runs.ts` is the on-disk substrate and already carries `sumSubtree` (`extensions/runs.ts:188-201`) whose single-metric, single-root signature would need a breaking reshuffle to serve this; `transcript.ts`/`tree.ts` are separate pure modules by the same pattern. It imports only types from `@earendil-works/pi-coding-agent` (package root re-exports `SessionEntry`, `parseSessionEntries`, `CompactionEntry` — verified at `node_modules/@earendil-works/pi-coding-agent/dist/index.d.ts:19`) and `Usage`/`RunManifest` from `./runs.ts`.

**Exported signatures.**

```ts
export interface SpendRecord {
  sessionId: string;      // invoking session (getSessionId())
  firstEntryId: string;   // the boundary user-message entry id (R-4 <firstId>)
  lastEntryId: string;    // last entry on the active branch at compute time (R-4 <lastId>)
  jobCount: number;       // manifests in the invocation forest
  ownSession: Usage;      // usageSource: "session-reconciled"
  subtree: Usage;         // usageSource: "session-reconciled"
  total: Usage;           // ownSession + subtree, fieldwise
}
export function spendRecord(opts: {
  entries: SessionEntry[];        // ctx.sessionManager.getEntries()
  leafId: string | null;          // ctx.sessionManager.getLeafId()
  sessionId: string;
  markerId: string | null;        // the council-invocation marker entry id
  manifests: RunManifest[];       // readManifests(repoRoot, runId)
}): SpendRecord;
export function formatBoundaryLabel(r: SpendRecord): string;
// → `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>` (R-4, byte-exact)
export function recordInvocationBoundary(pi, ctx, command: string, runId: string): string | null;
// the stamp helper (below); returns the marker id
```

`total` serves EV-32 (report at every autonomous exit) so it doesn't hand-roll a 14-field sum — flagged for the ruling seat below.

**How the boundary is obtained.** `pi.sendUserMessage` is fire-and-forget `void` (`agent-session.js:2020-2028`) that internally awaits the *whole turn* (`sendUserMessage` → `await this.prompt(...)`, `agent-session.js:1161`), and the handler's `ctx.sessionManager` is a `ReadonlySessionManager` — no `appendCustomEntry`, no `getChildren` (`extensions/types.d.ts:225`; the Pick list at `session-manager.d.ts:163`). So the id cannot be captured from the send, but `pi.appendEntry(customType, data)` **is** on the ExtensionAPI (`extensions/types.d.ts:985,1249`; wired to `sessionManager.appendCustomEntry` at `agent-session.js:2029`) and is synchronous. `recordInvocationBoundary` stamps `pi.appendEntry("council-invocation", { command, runId, at: Date.now() })`, then reads `ctx.sessionManager.getLeafId()` — append advances the leaf to the new entry, so the leaf id *is* the marker id. The injected user message then appends as the marker's **child**. Resolution is therefore deterministic at any later time, from entries alone: `boundary = entries.find(e => e.parentId === markerId && e.type === "message" && e.message.role === "user")`. The marker survives `/reload` and session restarts; the record's `firstEntryId` is the user-message id, not the marker id, so R-4 holds verbatim.

**Own-session half.** Pure branch walk mirroring `getBranch` semantics: build a byId map from `entries`, walk `parentId` from `leafId` to root, reverse, slice from the boundary (inclusive). The raw chain is walked deliberately — **not** `buildContextEntries`, which omits compacted-away entries (`session-manager.d.ts:150-158`); the card requires "pi counts all entries, including those compacted away". Per entry: `type:"message"` + `role:"assistant"` → accumulate `message.usage` (required on `AssistantMessage`, `pi-ai/dist/types.d.ts:308`), `turns++`; `role:"toolResult"` → accumulate optional `usage` (`types.d.ts:347`), no turn; `type:"compaction"` → accumulate optional `entry.usage` (`session-manager.d.ts:53`). Wire→record flattening reuses the EV-28 Q1 clause verbatim: `cost.{input,output,cacheRead,cacheWrite,total} → costInput…cost`, missing components coerce to 0, never `NaN`. Entries before the boundary contribute nothing.

**Subtree half — attended-run rootlessness reconciled.** Attended top-level manifests have `parentJobId === null` (`hub.ts:94` — the parent's hub is minted with no `parentJobPath`, `index.ts:161`), so there is no single root and `sumSubtree(manifests, "job-1")` covers one seat only. The rule: **eligible roots = manifests with `parentJobId === null && startedAt >= Date.parse(boundaryEntry.timestamp)`; the forest is each root closed over `parentJobId` descendants.** `startedAt` is `Date.now()` at spawn (`hub.ts:119`), and jobs spawn only after the boundary entry exists — same process clock, no skew. This one rule covers every shape: (i) attended `/council` — sibling top-level seats are all eligible roots; (ii) autonomous parent → runner → `job-1.x` — the runner `job-1` is a time-eligible root and its descendants (written by the child's hub with `parentJobPath` = its `COUNCIL_JOB_ID`, `child.ts:20`) close under it; (iii) multiple invocations in one session share a run dir (`initHubIdentity(mintRunId())` is per-**session**, `index.ts:161`) — the time filter excludes earlier invocations' jobs; (iv) the runner's own manifest is never double-counted because the record is computed from the parent, whose boundary predates it. Deliberate scope cut: a runner **child** computing a record for its own card gets a zero subtree under this rule (its jobs carry `parentJobId "job-N" ≠ null`); child-side exit reporting is EV-32's concern and would need an optional `rootJobId` param then — not now.

**R-4 label.** `formatBoundaryLabel` renders the exact literal `boundary=session=${sessionId} entries=${firstEntryId}..${lastEntryId} jobs=${jobCount}`. Structured fields ride the record so EV-31 persists provenance without re-parsing.

**Edge cases.** Zero dispatched jobs → eligible roots empty → subtree all-zeros, `jobs=0`, own-session intact. Pruned run dir → `readManifests` already returns `[]` for a missing dir (`runs.ts:102-108`) → same as zero jobs. Rotated session (boundary unresolvable: marker id absent, or marker has no user-message child — `switchSession`/`fork`/`navigateTree`, `extensions/types.d.ts:254-296`) → **all-zero record**, no throw; the label still renders with the ids as known. Legacy manifests without `usage` count in `jobCount` and contribute 0 per metric (same tolerance as `sumSubtree`, `runs.ts:196-198`).

**Call sites.** Land in EV-30: (1) `extensions/index.ts` command handler — stamp the marker in **both** TUI and headless branches (synchronous, so the TUI fire-and-forget contract at `index.ts:221-223` is untouched) before `sendUserMessage`. Deliberately untouched: `hub.ts` (stable per AGENTS.md 7), `runs.ts` (substrate unchanged), `navigator.ts`/`transcript.ts` (the parent session isn't a run-dir JSONL), and the exit-time compute/write/report sites — those are EV-31's store and EV-32's reporting.

### (b) Falsifiable tests (`test/spend.test.ts`)

Fixture: write a session JSONL to `mkdtemp` tmp (marker + pre-boundary assistant with usage + boundary user message + assistant with cacheRead/cost + toolResult with usage + compaction with usage + an off-branch sibling with usage), parse with pi's exported `parseSessionEntries`; manifests via `writeManifest`/`readManifests`. Hand sums asserted literal.

- **T1** full fixture: ownSession == hand sum (only entries ≥ boundary, on-branch); subtree == hand sum over the forest; `jobCount`; label byte-equals `boundary=session=… entries=… jobs=…`.
- **T2** the pre-boundary assistant's usage is absent from the record.
- **T3** zero jobs (empty manifest set) → subtree all-zero, `jobs=0`, no throw.
- **T4** pruned dir (`fs.rmSync` then `readManifests`) → same as T3.
- **T5** rotated: marker id absent from entries → all-zero record, label still renders, no throw.
- **T6** off-branch entry (parentId not on the leaf chain) with usage is excluded; compacted-away on-branch entry is included.
- **T7** assistant with partial wire cost `{total:0.001}` → components 0, `cost=0.001`, never NaN.
- **T8** two invocations, one run dir: the second record's forest excludes the first invocation's jobs and vice versa.
- **T9** `total` = fieldwise sum; `reasoning` accumulated independently, never re-added into `output` (asserts the EV-28 invariant survives reconciliation).
- **T10** legacy manifest (`usage = {input:1, output:2, cost:0.5, turns:1}` cast) counted in `jobCount`, adds 0 to cacheRead.
- **T11** `recordInvocationBoundary` with a stub `pi`/`ctx`: appendEntry called with `customType "council-invocation"`, returned id equals the stubbed post-append leaf id.

### (c) Open questions for a ruling seat

1. **What is "a rotated session"?** No rotation concept exists in pi's current session API (zero hits for "rotate" in `session-manager`/`agent-session`). I read it as boundary-unresolvable (session switched/forked, or marker off-branch). The card says these resolve "to a zero subtree" — my design zeroes **both** halves (no boundary timestamp → the forest can't be time-scoped). If a ruling wants the subtree computed anyway from the run dir, the record's honesty claim changes. Ruling needed.
2. **Unresolved-boundary label form.** R-4 defines `entries=<firstId>..<lastId>` only for resolved ranges. Empty ids render `entries=..` — acceptable, or `entries=none`?
3. **Subtree `costBasis`/`usageSource` under mixed constituents** — latent until EV-29 lands "reported". My default: `catalogue-estimate` whenever any constituent is an estimate (conservative label). Does the ruling want that, or per-half basis lists?
4. **`branch_summary` usage** (`session-manager.d.ts:63-64`, same mechanism class as compaction): the card names assistant/tool-result/compaction only. Include or exclude?
5. **`total` on the record now** (serving EV-32) or EV-32's render concern?
6. **`turns` for tool-result/compaction usage**: assistant-messages-only (mirrors `hub.ts:197`), or pi's own counting? I propose assistant-only.

### (d) Rejected alternatives

1. **Extend `sumSubtree` / put the function in `runs.ts`** — wrong home (substrate vs. reconciliation) and `sumSubtree`'s `(manifests, rootId, metric)` shape can't return a full `Usage` or `jobCount`; cost is a breaking reshuffle of tested substrate code.
2. **`buildContextEntries`-based scoping** — silently drops compacted-away entries; contradicts the card's explicit "including those compacted away"; would undercount long compaction-heavy runs.
3. **Post-hoc "last user message on the branch" boundary (no marker)** — zero call-site change, but breaks on multiple invocations per session, dies on `/reload` (in-memory capture), and misidentifies the boundary after any follow-up user message. The marker costs one session entry.
4. **Awaiting `pi.sendUserMessage` to capture the entry id** — it's fire-and-forget void (`agent-session.js:2020-2028`) whose internal promise spans the whole turn (`agent-session.js:1161`); awaiting would violate the TUI handler contract (`index.ts:221-223`) and invert the headless teardown hazard documented at `index.ts:224-227`.
5. **Single-root `sumSubtree(manifests, "job-1")`** — valid only when a root exists; attended runs dispatch siblings with `parentJobId null` (`hub.ts:94`), so there is no root; the forest rule covers both shapes with one rule.

<output_format>
- **Approach** — Pure `extensions/spend.ts` returns a `SpendRecord` (ownSession + subtree + total + R-4 boundary fields); boundary made durable by a synchronous `pi.appendEntry("council-invocation", …)` marker stamped in the procedure handler, resolved post-hoc as the marker's user-message child; own-session half = hand-sum over the raw leaf chain at/after the boundary (compacted-away entries included, `buildContextEntries` rejected); subtree half = forest of time-eligible top-level manifests closed over `parentJobId` descendants, which covers attended rootlessness, nested autonomy, and multi-invocation sessions with one rule. Only call-site change in EV-30: the stamp at `extensions/index.ts:215-231`; store/report wiring stays with EV-31/EV-32.
- **Tradeoffs accepted** — One extra session entry per invocation (durable, reload-proof, invisible to context); child-side (runner-internal) spend records deliberately out of scope (would need a `rootJobId` param when EV-32 demands it); `total` included now as a two-line derivation serving EV-32, flagged for veto.
- **Testable claims** — T1–T11 above: each fails against current code (no `spend.ts` exists) and pins one behavior — hand-sum equality, zero-subtree edges, rotation zeros, off-branch exclusion, partial-cost coercion, invocation isolation, R-4 label bytes.
</output_format>

#### Position — `principal`

### 1. Cross-seam reading — what the contract actually is today

**The invoking session (the "own-session" side).** The procedure handler is `extensions/index.ts:215-236`; it injects via `pi.sendUserMessage(routed)` (`:222` tui fire-and-forget, `:228` headless). That call is typed `void` on the extension API (`pi-coding-agent/dist/core/extensions/types.d.ts:980-983`), and no event carries the appended entry id (`MessageStartEvent` carries only `message`, `types.d.ts:592-595`; `InputEvent` carries only text/source, `:657-676`). The live session is reachable as read-only `ctx.sessionManager` (`types.d.ts:218-219` → `ReadonlySessionManager`, `session-manager.d.ts:140`), whose `getEntries()` is `fileEntries.filter(e => e.type !== "session")` — raw append order, abandoned branches included (`session-manager.js:995-997`). pi's canonical usage enumeration is four kinds, not three: assistant `message.usage`, toolResult `message.usage`, and `branch_summary` / `compaction` `entry.usage` (`pi-coding-agent/dist/core/usage-totals.js:18-32`; `core/agent-session.js:2658-2700`). `Usage.cost.total` is computed client-side from catalogue rates (`pi-ai/dist/models.js:540-548`) — so "catalogue estimate" is correct for both halves today.

**The run directory (the "subtree" side).** `initHubIdentity(mintRunId())` runs once at parent `session_start` (`index.ts:161`) and is frozen into the hub singleton on first `getHub` (`hub-tools.ts:28-37`, created at `index.ts:163`) — `getHub` never re-reads the identity. Every dispatch writes into that one `runId` dir (`hub-tools.ts:163-164`, `dispatch.ts:99-108`). Manifests are written by `Hub.writeJobManifest` (`hub.ts:88-101`) with `parentJobId: this.run.parentJobPath ?? null` (`:94`) and `id` from `allocateId()` (`:83-86`) — so at top level every job is `job-N` with `parentJobId: null`. Nesting only exists because a hub-granted child re-inits with `parentJobPath = COUNCIL_JOB_ID` (`child.ts:24`). Persisted usage is assistant-stdout-only (`hub.ts:189-199`), stamped `usageSource: "stream-assistant"` (`hub.ts:138`), and only flushed at spawn/stall/timeout/settle (`hub.ts:96, :154, :181, :241`).

**The mismatch the premise hides (five, in order of blast radius):**

- **M1 — the run directory is session-scoped, not invocation-scoped.** `runId` is minted per parent session (`index.ts:161`), so two `/council` invocations in one session share one directory and one forest. The card says "the invocation's run directory / that invocation's job manifests"; no artifact carries an invocation marker (`RunManifest` fields, `runs.ts:42-57`), so the invocation's job set is not derivable from the manifests alone.
- **M2 — the boundary id is produced by nobody.** `sendUserMessage` returns `void` and no hook exposes the appended entry id. The card's "the boundary is the entry id of the user message the procedure injects" is an intent, not a seam that exists.
- **M3 — the two halves are on different accounting bases.** Own-session = session enumeration (4 kinds); subtree = per-manifest stream projection (assistant only). EV-28 diagnosed this exactly (`council/cards/EV-28.md:206`) and Q4 reserved `usageSource:'session-reconciled'` "for the session-enumeration half — EV-30's goal" (`EV-28.md:617-627`). A single `Usage.usageSource` (and `costBasis`) cannot be single-valued across both halves.
- **M4 — the reconciliation data for the subtree already exists and is unused.** `RunManifest.sessionId === job.id` (`hub.ts:96`), children spawn with `--session-dir <runDir> --session-id <jobId>` (`hub-tools.ts:184-185`, `seats.ts:517`), and `findSessionFile(repoRoot, runId, sessionId)` resolves them (`runs.ts:134-152`). Each subtree node's full session (toolResult + compaction usage) is one call away; the card narrowly scopes reconciliation to the invoking session.
- **M5 — the card's enumeration omits `branch_summary`**, diverging from the pi enumeration it cites as its model ("pi counts all entries").

Related cross-card fact for EV-31: seat sessions live *inside* `runDir` (`sessionDir: dir`), including a council-runner's own session — so exit point 5's invoking session does not survive `pruneRuns` the way EV-31's "not under the run directory" premise assumes.

The only named consumer of a run total today is prose — `council/procedures/council.md:336` (per-run token ceiling), unimplemented and filed as FLLWUP-26.

### 2. Blind spots, named separately

- **Engine/hub view:** sees stdout assistant `message_end` and its own job-id counter; cannot see that its `runId` is session-scoped (it treats `parentJobPath` as the only identity it has), that its manifests carry no boundary marker, or that its seat's own session JSONL holds strictly more usage than the tuple it persists.
- **Session-JSONL/data view:** sees all four usage kinds and a flat append order; cannot see that file order ≠ descendant order (a branch written after the boundary is not the invocation's), that the boundary has no in-file marker, or that `getEntries()` includes compacted-away *and* abandoned-branch entries.
- **Procedure/injection view:** holds `ctx.sessionManager` and could snapshot `getLeafId()` or `appendEntry` a marker (API at `types.d.ts:984-985`), but is written fire-and-forget (`index.ts:222, :228`); it cannot see that the "run total" it is told to track has no object behind it.
- **Consumer (EV-31/EV-32) view:** will be handed "the invocation's spend"; cannot see the subtree undercount or the session-scope hazard, because EV-30's acceptance supplies exactly the right manifest set.

### 3. Reframe

**The seam is right; the deliverable's boundary is drawn one step too far out.** EV-28's principal already established the stream-vs-enumeration split and EV-28's Q4 ruling reserved this card's half — so bridge-building is correct. But the card frames the answer as a pure function over two inputs with an acceptance test that *constructs* both inputs. That makes the boundary and the forest given by the fixture, so the test proves the arithmetic and never exercises the two things that are actually missing: **who supplies the boundary id, and what scopes the manifest set.** A `spend.ts` that sums whatever lists it is handed will be called by EV-32 with the session's run dir and will report the whole session's spend as one invocation's — the attended-case overcount — and will sum a session-reconciled half into a stream-projected half, the basis defect the epic exists to remove (`EPIC-7` / `EV-30.md` Intent).

Concretely, I would draw the card's boundary as: (a) the pure accounting function **takes the boundary explicitly and does the manifest filtering itself** (so scoping is inside the tested unit), and its fixture manifest set must contain a pre-boundary job that it excludes; (b) the injection site gains a boundary-capture leg, tested at least once end-to-end; (c) the subtree-basis question is decided and recorded in the card rather than left for EV-32.

The alternative seam I reject: "ship the pure summer, leave all wiring to EV-32." Its concrete cost is that EV-32's five exit points (two of which run in one shared session) each need the same unwritten scoping logic, and the durable record EV-31 writes fixes a wrong number on disk. Rejecting it costs one extra tested seam (index.ts) and one recorded ruling; accepting it costs a wrong durable number.

### 4. Testable claims

1. **Pre-boundary exclusion.** Fixture: session with boundary at entry `u2`; manifest set with `job-1` (`startedAt` < `u2.timestamp`) and `job-2` (`startedAt` >). Assert the record's subtree excludes `job-1`. A sum-all-manifests implementation fails.
2. **`branch_summary` inclusion.** Session fixture carrying one `branch_summary` entry with `usage`. Assert the own-session cost/tokens include it (against `usage-totals.js:31`). Literal reading of the card's enumeration fails.
3. **Two invocations, one run dir.** Drive two procedure injections in one session with dispatches after each. Assert invocation 2's subtree ≠ Σ over all manifests in the run dir. Any session-scoped sum fails.
4. **Basis expressibility.** Assert the record can carry `own.usageSource === "session-reconciled"` and `subtree.usageSource === "stream-assistant"` simultaneously — or that the chosen type makes this a compile error (which is the defect, not a pass).
5. **Boundary capture without content matching.** Invoke the same procedure command twice with identical `$ARGUMENTS` in one session; assert the second record's `firstId` is the second injected user entry, not the first. A content-prefix implementation fails.
6. **Zero/edge.** Pruned run dir → subtree all zeros, no throw; `getSessionFile() === undefined` (in-memory session) → own half zeros, no throw.
7. **R-4 label.** Assert the exact `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>` string, with `sessionId` = the *invoking* session's id (not `RunManifest.sessionId`, which is a child id, `hub.ts:96`).

### 5. Open questions for a ruling seat

- **Q1 — shape:** one combined `Usage` for the record, or one per half? `usageSource` and `costBasis` are single-valued on `Usage` (`runs.ts:19, :34-35`) and cannot describe a reconciled own half plus a stream-projected subtree half.
- **Q2 — subtree basis:** stream-projected (today's manifests) or reconciled per node via `findSessionFile` (`runs.ts:134`)? If stream-projected, must the record state that its subtree half is a lower bound?
- **Q3 — scoping:** does the function filter manifests by boundary (time or id range), or does the caller pre-scope? The card's goal is silent and the manifests carry no invocation marker.
- **Q4 — `lastId`:** what fixes it — session leaf, last file entry, or an end marker? R-4 fixes `firstId..lastId` but the card's own-session rule ("entries at or after the boundary") has no end. Branches/abandoned entries make this load-bearing.
- **Q5 — `jobs=<n>`:** count of all nodes in the forest, or roots only?
- **Q6 — signature/location:** session *file path* (re-parse, per the card's wording) or live `ReadonlySessionManager` (cheaper boundary capture, but the card says JSONL)? And which module — `extensions/runs.ts` or a new one?
- **Q7 — exit point 5:** the runner's own session sits under the pruned run dir; does the record carry a session path EV-31 can rely on, or does EV-30 need to guarantee the file survives?
- **Q8 — `cacheWrite1h`:** the JSONL `cost.cacheWrite` folds the 1h premium (`models.js:540-548`) while the flat `Usage` has no `cacheWrite1h`; is the resulting own-half divergence from its own cost components acceptable pending FLLWUP-29?

I agree the card is the right bridge and that EV-28 explicitly reserved the `session-reconciled` member for it; my disagreement is only that its acceptance test, as written, cannot fail on the two facts (boundary capture, manifest scoping) that are actually unowned.

### Step 3 — bounded exchange, round 2 (2 rounds total; cap 3)

Each generator re-dispatched with the other's recorded round-1 position (read
from this card, verbatim). Jobs `job-9.3` (owner), `job-9.4` (principal).
Positions stabilised; stopped early after 2 of the ≤3 permitted rounds.
Positions verbatim.

#### Round 2 — `owner`

All principal's load-bearing citations verified against source. Here is my round-2 position.

### 1. Point-by-point response to `principal`

#### M1–M5

- **M1 (run dir is session-scoped) — concede the demand, hold the remedy is already mine.** Factually correct: `initHubIdentity(mintRunId())` runs once per parent `session_start` (`extensions/index.ts:161`), the identity is frozen into the hub singleton on first `getHub` (`extensions/hub-tools.ts:22-37`), and `RunManifest` (`extensions/runs.ts:42-57`) carries no invocation marker. But my round-1 forest rule already scopes *inside the function*: eligible roots are `parentJobId === null && startedAt >= Date.parse(boundaryEntry.timestamp)` (`extensions/hub.ts:119,126` — `startedAt` is `Date.now()` at spawn, same process clock as the boundary entry). What I concede: the acceptance fixture must *prove* it — T1's manifest set gains a pre-boundary job that must be excluded, per your claim 1. That is a fixture construction change, not a design change.
- **M2 (boundary id produced by nobody) — concede the tested capture leg.** Correct: `sendUserMessage` is void (`types.d.ts:980-982`) and no event carries the entry id. My marker (`pi.appendEntry`, `types.d.ts:985`, wired to `appendCustomEntry` which returns the id and advances the leaf — `session-manager.js:835-845`) is the remedy, and my round-1 already landed the stamp in `index.ts:215-231`. Conceded: T11's stub-pi test upgrades to a real `SessionManager` in a tmpdir with a faithful `pi.appendEntry` shim (mirroring the `agent-session.js` wiring), asserting marker → injected-user-message-child resolution end-to-end at the unit level. I do not concede "end-to-end" means a live runtime — bun:test has no session runtime, and the stub-with-real-SessionManager shape exercises the same parent/child resolution without network.
- **M3 (two accounting bases, single-valued `usageSource`) — concede; my round-1 annotation was wrong.** Verified: `UsageSource` is single-valued (`extensions/runs.ts:19-35`), and my round-1 marked *both* halves `session-reconciled`. The subtree half is a sum of manifest tuples stamped `stream-assistant` at spawn (`extensions/hub.ts:138`); calling it session-reconciled is false. Change: the record carries **two `Usage` values, each with its own `usageSource`** — ownSession `session-reconciled`, subtree `stream-assistant`. This is also the answer to Q1.
- **M4 (per-node reconciliation data exists) — hold.** Correct facts: `RunManifest.sessionId === job.id` (`extensions/hub.ts:96`), children spawn with `--session-dir <runDir> --session-id <jobId>` (`extensions/hub-tools.ts:184`, `extensions/seats.ts:517`), `findSessionFile` resolves them (`extensions/runs.ts:134-152`). But the card's goal explicitly scopes reconciliation to the invoking session, and EV-28's Q4 ruling reserved `session-reconciled` for exactly that half (`council/cards/EV-28.md:617-627`), anticipating a future flip "without re-declaring the type." Per-node reconciliation is a future card; doing it now is scope expansion. Hold, recorded as the forward path.
- **M5 (`branch_summary` omitted) — concede outright.** Verified: pi's own enumeration counts `branch_summary` and `compaction` identically (`usage-totals.js`, `entry.type === "branch_summary" || entry.type === "compaction"` → same bucket; `session-manager.d.ts:49,55` — branch_summary entries carry `usage?`). My round-1 open question 4 is answered by the evidence: include `branch_summary`, accumulate `entry.usage` exactly like compaction. My design text changes; record shape and R-4 label do not.

#### Reframe

Conceded on all three legs, with scope notes: (a) my design already filters inside the function — the concession is the fixture (pre-boundary job in T1); (b) conceded as the upgraded T11 above; (c) conceded — the subtree-basis ruling is recorded in the card as part of this convergence. Your rejection of "ship the pure summer, leave wiring to EV-32" is not a disagreement — I never proposed it; the stamp is an EV-30 call-site change in my round-1.

#### Testable claims 1–7

1. **Pre-boundary exclusion — concede/agree.** My design's time filter does exactly this; fixture upgraded per M1.
2. **`branch_summary` inclusion — concede.** See M5.
3. **Two invocations, one run dir — agree; already covered.** This is my round-1 T8 verbatim; your version with two driven injections is a stricter construction — adopt it as the fixture shape.
4. **Basis expressibility — concede the shape, which is the fix.** With per-half `Usage` values the record *can* carry `own.usageSource === "session-reconciled"` and `subtree.usageSource === "stream-assistant"` simultaneously. Test: assert both fields on one record.
5. **Boundary capture without content matching — concede, add the test.** Marker resolution is structural (parentId, not content — `session-manager.js:835-845`), so identical `$ARGUMENTS` twice yields two markers each resolving its own child. New test over a two-injection session fixture.
6. **Zero/edge — agree.** My T3/T4/T5 plus in-memory-session (`getSessionFile()` undefined → entries still reachable via the live manager; if genuinely empty, own half zeros, no throw).
7. **R-4 label — agree.** `sessionId` is the invoking session's `getSessionId()`; `RunManifest.sessionId` being a child id (`extensions/hub.ts:96`) never enters the label. Converged.

#### Q1–Q8

- **Q1 (shape) — answered: two half-`Usage` values.** Each half carries its own single-valued `usageSource`/`costBasis`. The remainder — **`total`** — is genuinely stuck: a fieldwise sum of a reconciled half and a stream half has no honest single basis, and `UsageSource` has no member for it. **Ruling needed** (keep with a new union member vs. drop and let EV-32 render the sum presentationally).
- **Q2 (subtree basis) — hold: stream-projected this card.** The subtree half's `usageSource: "stream-assistant"` **is** the machine-readable lower-bound statement — that field exists for exactly this purpose per EV-28 Q4 (`EV-28.md:617-627`). Per-node reconciliation via `findSessionFile` (`runs.ts:134-152`) is the future flip, per M4.
- **Q3 (scoping) — converged: inside the function** (time-eligible roots), proven by the fixture per M1/claim 1.
- **Q4 (`lastId`) — hold with a concrete rule: last entry id on the active leaf chain at compute time** (walk `parentId` from `leafId`, the same chain own-session sums). "Last file entry" wrongly includes abandoned branches; nobody writes an end marker. R-4 binds only the label format, so I argue this is implementation judgment — but I'll list it for the ruling seat since branches make it load-bearing.
- **Q5 (`jobs=<n>`) — hold: all nodes in the forest** (roots + descendants). The label describes the covered job set; the sum covers every node.
- **Q6 (signature/location) — hold: pure function over parsed `entries: SessionEntry[]`**, in a new `extensions/spend.ts`. `getEntries()` is literally the file entries minus the header (`session-manager.js:995-997`), so the card's "session JSONL" is satisfied whether the call site passes the live manager's entries or re-parses the file. `runs.ts` stays untouched (`sumSubtree` at `runs.ts:188-201` is tested substrate; its `(manifests, rootId, metric)` shape can't return a `Usage` anyway — round-1 rejected-alt 1 stands).
- **Q7 (exit point 5) — hold, flag forward.** Verified: seat children — including a council-runner's own session — live under `runDir` (`hub-tools.ts:184`), so `pruneRuns` does threaten that file. But R-4 binds only the label; the record carries no session path in my shape, and store-location is EV-31's decision. Record the fact on EV-31's board; nothing for EV-30 to do.
- **Q8 (`cacheWrite1h`) — hold, and the premise is imprecise.** The premium fold is real (`pi-ai/dist/models.js:540-548` folds 1h writes at 2× base input into `usage.cost.cacheWrite`), but **both halves' cost components fold identically** — the own-half's `costCacheWrite` is flattened from the same wire `cost.cacheWrite` (EV-28 Q1 clause, `runs.ts:6-16`). There is no half-vs-half cost divergence. The only loss is the optional `cacheWrite1h` *token* split (`pi-ai/dist/types.d.ts:270-271`), which the EV-28-shipped `Usage` (`runs.ts:19-35`) has no field for. That's EV-28's shape decision, owned by FLLWUP-29 — not an EV-30 defect.

**Factually wrong in principal's position:** nothing material. Two precision nits: flush-point line refs (`hub.ts:96,154,181,241` vs. actual `writeJobManifest` calls at `hub.ts:151,237,245,256,266`) and the Q8 divergence framing above. Neither changes a conclusion.

### 2. Converged round-2 position

**Conceded:** `branch_summary` inclusion (M5); subtree half is `usageSource: "stream-assistant"`, own half `session-reconciled` — per-half `Usage` shape (M3/Q1); acceptance fixture must contain a pre-boundary excluded job and a two-invocation one-run-dir construction (M1/claims 1,3); boundary capture leg lands in EV-30 with a real-SessionManager test including identical-`$ARGUMENTS` double injection (M2/claims 5,11); subtree-basis decision recorded in the card (reframe c).

**Held:** subtree stays stream-projected this card, `usageSource` as the machine-readable lower-bound statement (Q2); no per-node reconciliation (M4); pure `extensions/spend.ts` over parsed entries (Q6); `lastId` = leaf-chain end (Q4); `jobs=<n>` = all forest nodes (Q5); Q7/Q8 out of scope, flagged to EV-31/FLLWUP-29.

**Still open for a ruling seat:** (1) `total` — keep (new `UsageSource` member) or drop from the record; (2) unresolved-boundary label form (`entries=..` vs `entries=none`) — my round-1 OQ2, unaddressed by principal; (3) rotated-session semantics: zero *both* halves (my round-1 choice) or compute subtree anyway — my OQ1, unaddressed; (4) optionally, bind Q4's `lastId` rule beyond implementation judgment.

### 3. Convergence status

The **shape is settled** between us: two half-`Usage` values each carrying its own basis/source, marker-based structural boundary capture at the procedure handler, in-function time-eligible-root forest scoping, four-kind enumeration (assistant, toolResult, compaction, branch_summary), R-4 label from the invoking session's id, zero-subtree edges as all-zero records. Every remaining disagreement was resolved by reading the same source, not by compromise.

Four items need a ruling seat, not implementation judgment: **`total`'s existence and basis**, the **unresolved-boundary label form**, **rotated-session zeroing semantics**, and (soft) **binding the `lastId` rule**. Items 2 and 3 were in my round-1 open questions and principal's position is silent on both; I won't decide them unilaterally because both change what the persisted record (EV-31's input) claims to cover.

#### Round 2 — `principal`

### 1. Point-by-point response to `owner`

#### (a) The `pi.appendEntry("council-invocation")` marker — closes M2, but the child resolution does not hold

**Movement found (concede).** The marker does close M2's *writer* gap. `pi.appendEntry` is wired to `sessionManager.appendCustomEntry` (`agent-session.js:2029-2030`), and `_appendEntry` both persists to the JSONL and sets `this.leafId = entry.id` (`session-manager.js:769-774`; `appendCustomEntry` at `:835-844`). So `pi.appendEntry(...)` → `ctx.sessionManager.getLeafId()` returning the marker id is sound, and the marker is durable across `/reload`. My round-1 claim that the injection site *cannot* produce the boundary id is withdrawn: it cannot produce the **user-message** id, but it can produce a durable anchor, which is what M2 actually needed.

**Push back — the resolution rule fails on a reachable path.** `boundary = entries.find(e => e.parentId === markerId && e.type === "message" && e.message.role === "user")` assumes the injected user message is the marker's direct child. It is not, in general:

- `pi.sendUserMessage` → `sendUserMessage` → `await this.prompt(...)` (`agent-session.js:1161`).
- `prompt()` runs `_flushPendingBashMessages()` / `_flushPendingCustomMessages()` (`agent-session.js:874-875`) and `await this._checkCompaction(lastAssistant, false)` (`agent-session.js:895`) **before** the user message is ever persisted.
- `_checkCompaction` → `_runAutoCompaction` → `appendCompaction` (`agent-session.js:1826`), and `appendCompaction` parents on `this.leafId` and advances the leaf (`session-manager.js:798, 831`).
- The injected user message is persisted later, at its `message_end` in `_handleAgentEvent` (`agent-session.js:398`), parented on the leaf **as of that moment** (`appendMessage` → `parentId: this.leafId`, `session-manager.js:788-798`).

So on any session where the pre-prompt compaction check fires — i.e. exactly the long, compaction-heavy attended runs this record exists to measure — the user message's `parentId` is the compaction entry, the `find` returns `undefined`, and owner's code takes the "rotated session" branch and returns an **all-zero record**. That is a silent zero on the ordinary path, not an edge case.

**Required fix (small, keeps content-independence):** resolve by append order, which `getEntries()` guarantees (`fileEntries.filter(...)`, order preserved by `_appendEntry`'s `push`, `session-manager.js:770, 995-997`). Boundary = first entry with index > the marker's index that is `type === "message" && message.role === "user"` and lies on the active branch. This survives intervening compaction/bash/custom entries and still never matches procedure content.

**Hardening (fail closed):** `pi.appendEntry` returns `void` (`extensions/types.d.ts:985`), so the marker id can only be read back through `ctx.sessionManager.getLeafId()` (`session-manager.d.ts:240`) — a *different object* from the one that appended. After the append, assert the leaf entry is `custom`/`council-invocation`; otherwise record an unresolved boundary rather than a wrong one. Rationale is documented: captured ctx getters call `assertActive()` and throw after session replacement (`vault/wiki/headless-pi.md`, "Stale ctx after session replacement"). The stamp itself is safe — the handler's ctx is created per invocation (`agent-session.js:963`) — but the invariant is worth asserting at the one place it can rot.

**Scope note (agree, with an edge):** the stamp is parent-mode only — `index.ts:118-121` returns into `runChildMode` before procedure registration, so seats never stamp. Any invocation path that is not a registered slash command also gets no marker. That matches the card ("the user message the procedure injects"), but EV-31/32 must not assume a marker always exists.

#### (b) Forest rule as the answer to M1 — right shape, but owner's own T8 is false under it

**Accept:** `startedAt >= Date.parse(boundary.timestamp)` is the best available scoping without a schema change — `RunManifest` carries no invocation field (`runs.ts:42-57`), and `parentJobId: this.run.parentJobPath ?? null` with no parent path at top level (`hub.ts:94`; identity minted once per session at `index.ts:161`, frozen in the singleton at `hub-tools.ts:28-37`). Time-eligibility closes the attended-rootlessness case, the nested-runner case (`allocateId`, `hub.ts:83-86`), and the backward direction of multi-invocation sessions. Clock alignment is real: entry timestamps are `new Date().toISOString()` at append (`session-manager.js:792`) and `job.startedAt = Date.now()` at spawn (`hub.ts:117-126`), same process clock; `>=` is the correct edge.

**Push back — T8 as written cannot pass.** "the second record's forest excludes the first invocation's jobs **and vice versa**": under this rule `job-2` has `startedAt >= boundary2 > boundary1`, so if both records are computed at the same instant (the only way a pure-function test can test both), `job-2` **is** in invocation 1's forest. The "vice versa" holds only if record 1 is computed before invocation 2 exists — a sequencing property, not a scoping property. Rewrite T8 as the direction the rule guarantees (record 2 excludes invocation 1's jobs), and record the consequence separately: the forest is a **point-in-time filter, not an invocation identity**. A durable record written at exit (EV-31) is fine; a *recompute* later from the same run dir silently grows. That is an EV-31 honesty question (`jobCount`/subtree drift, R-4's label carries no timestamp) → ruling, below.

#### (c) `SpendRecord` shape — per-half `Usage` is right; two of the three annotations are wrong

**Concede Q1:** one `Usage` per half is the only honest shape — `usageSource` (`runs.ts:19`) and `costBasis` (`runs.ts:18`) are single-valued, so a combined record cannot describe a reconciled own half and a stream-projected subtree half. Owner's answer to my Q1 is correct.

**Push back #1 — `subtree: Usage // usageSource: "session-reconciled"` is inverted.** The subtree is summed from `RunManifest.usage` (`runs.ts:52`; `sumSubtree`, `runs.ts:188-201`), which the hub stamps `usageSource: "stream-assistant"` (`hub.ts:138`) and fills from assistant `message_end` on child stdout only (`hub.ts:189-199`). EV-28's binding Q4 ruling reserves `session-reconciled` for "the session-enumeration half — EV-30's goal" and states the other value "is not written this card" (`EV-28.md:619`). The subtree half must read `"stream-assistant"` (mixed only if EV-29 reconciles nodes). As annotated, the record's central honesty claim is backwards.

**Push back #2 — veto `total: Usage`.** It reintroduces exactly the defect the per-half split removes: summing a `session-reconciled` half into a `stream-assistant` half and stamping one `usageSource` — both union members are lies for that value. Owner flagged it for veto; I veto it. EV-32 can derive totals at render, where the two bases are visible, or the record exposes `{ ownSession, subtree }` plus a derived accessor.

#### (d) Own-session enumeration — `branch_summary` excluded

**Hold, with the caveat that this is a ruling, not implementation judgment.** pi's canonical session-cost enumeration includes it: `getSessionStats()` adds `(entry.type === "branch_summary" || entry.type === "compaction") && entry.usage` over **all** entries, with the explicit comment "including history that was compacted away" (`agent-session.js:2658-2663`), and `getUsageCostBreakdown` does the same (`usage-totals.js:31`). `BranchSummaryEntry.usage` is a real field (`session-manager.d.ts:62-64`) and that cost is really billed. The card's Intent says "pi counts all entries, including compacted away" — matching pi's count is the point; the three-kind list reads as the author's summary. If the ruling excludes it anyway, the record must carry the enumerated-kind scope explicitly rather than diverging silently from pi's own number — a silent undercount is the exact defect class EPIC-7 exists to remove.

**Agree:** rejecting `buildContextEntries` is right (`session-manager.d.ts:150-158` drops summarized-away entries); `getEntries()` is the correct source. `turns` assistant-only, mirroring `hub.ts:197`, is right.

#### (e) Zero-both-halves on the rotated-session edge

**Concede — it is forced, not chosen.** With the marker absent or off-branch there is no resolved boundary entry: no timestamp to time-scope the forest by *and* no boundary to slice the own half from. The card's "resolve to a zero subtree" is a subset; zeroing both is the conservative superset (under-report, never over-report). Owner's Q1 is also right that "rotate" has no referent in pi — zero hits for `rotate` under `dist/core`; the reachable senses are boundary-unresolvable (`switchSession`/`fork`/`navigateTree`) or file replacement. The wording needs interpretation recorded in the card, since the card uses the word without defining it.

One consequence owner already flagged and I confirm: the unresolved label. R-4 binds only the resolved form; `entries=..` is shape-identical to a resolved-and-empty range. That needs a ruling (R4 below).

#### (f) Elsewhere

- **`extensions/spend.ts` as a new pure module — agree.** `runs.ts` is the substrate (`runs.ts:1-60`); `sumSubtree(manifests, rootId, metric)` cannot return a `Usage` or `jobCount` (`runs.ts:188`). Rejection d1 is right.
- **Call-site stamp inside EV-30 — agree; this is my round-1 reframe (b) accepted.** The pure-summer-only alternative stays off the table.
- **Rejections d2, d3, d4, d5 — accept all four.** `sendUserMessage` is typed `void` (`extensions/types.d.ts:980-983`) and its promise spans the turn (`agent-session.js:1161`), so awaiting it is out; the single-root `sumSubtree(manifests, "job-1")` is invalid in attended mode by `hub.ts:94`.
- **Citation nit:** owner cites `extensions/types.d.ts:225, 985, 1249` — that file is pi's `dist/core/extensions/types.d.ts`, not a repo path; the repo has no `extensions/types.d.ts`. Worth fixing so a reader doesn't grep the wrong tree.

### 2. Converged round-2 position

**Conceded (I am changing my round-1 position):**
1. The marker is the right M2 closure — a durable, reload-proof, synchronous anchor; my "the boundary id is produced by nobody" claim was too strong.
2. The stamp lands in EV-30 at `index.ts:215-231`; the pure-function-only scope is withdrawn.
3. Per-half `Usage` (owner's answer to my Q1); I drop "one combined `Usage`".
4. Zero-both-halves on an unresolvable boundary — forced by the absence of any anchor.
5. Time-eligibility as the forest rule, including for attended rootlessness and multi-invocation backward scoping; no `RunManifest` schema change.
6. All five rejected alternatives (d1–d5).

**Held (with the concrete reason):**
1. **Boundary resolution must be append-order-based.** Owner's `parentId === markerId` yields an all-zero record whenever the pre-prompt compaction check fires (`agent-session.js:895` → `:1826`), because the user message is persisted later at `message_end` (`agent-session.js:398`) on the then-current leaf. One-line fix; not a taste difference.
2. **`subtree.usageSource` must be `"stream-assistant"`.** The manifests are stream-projected (`hub.ts:138, 189-199`) and EV-28's ruling reserves the other member for this card's own half (`EV-28.md:619`).
3. **No `total: Usage` on the record.** It re-collapses the basis split the per-half shape exists to express.
4. **`branch_summary` belongs in the own half** (`usage-totals.js:31`, `agent-session.js:2661-2663`); flag it as a ruling, not a silent default.
5. **T8 must be rewritten** — "vice versa" is false under the time rule; add the point-in-time / recompute-drift property instead.
6. **T1's fixture must be produced by driving the real sequence** (marker → intervening compaction → injected user message). A hand-written JSONL with `parentId === markerId` validates the assumption rather than the behavior, and would pass while the real path zeroes.
7. **The stamp must fail closed** if the post-append leaf is not the marker (`headless-pi.md`, stale-ctx).

**Still open for a ruling seat:**
- **R2** — `branch_summary` in or out; if out, does the record carry an explicit enumerated-kind scope field?
- **R3** — `total` on the record or derived at render.
- **R4** — the unresolved-boundary label (`entries=..` vs `entries=none` vs a resolved/unresolved discriminator), since R-4 binds only the resolved form.
- **R5** — record the interpretation of "rotated session" (boundary-unresolvable) in the card; no such concept exists in pi.
- **R6** — subtree basis statement: `stream-assistant` is honest, but the record should say the subtree half is a lower bound relative to the seat session files sitting beside the manifests (`findSessionFile`, `runs.ts:134-152`; `sessionDir: dir`, `hub-tools.ts:184-185`), or the ruling should state that the card deliberately scopes reconciliation to the invoking session (M4).
- **R7** — record idempotence under time-eligibility: does the record carry its compute time / the job-id set, or is it valid only when written once at exit?

### 3. Convergence status

**The shape is settled except for one implementation delta I do not concede.** Module location, boundary mechanism, per-half `Usage`, zero-both edges, and the one-rule forest are converged; owner's `subtree.usageSource` annotation and `total` are, I believe, a typo and a self-flagged veto rather than disagreements.

Exactly one item is a real delta and it is **test-settleable, not a ruling**: boundary resolution (Held #1). A fixture that appends the marker, then a compaction entry, then the injected user message will fail owner's `parentId === markerId` rule and pass the append-order rule — I would have the ruling seat convert that from a prose dispute into the named test rather than arbitrating it.

Everything else that remains is a **value/scope ruling, not disagreement between the two of us**: R2 (`branch_summary`), R3 (`total`), R4 (unresolved label), R5 ("rotated"), R6 (lower-bound statement), R7 (idempotence). None of these is answerable from the code alone, and none is a reason to keep the two of us talking.

### Step 4 — Skeptic attacks and runs tests (job `job-9.5`)

Skeptic dispatched with the full deliberation record. It ran real probes
(`/tmp/ev30-skeptic/*`) against pi's shipped `dist/` and the repo source, and
re-established the pre-change baseline (`bunx tsc --noEmit` exit 0; `bun test`
587 pass / 2 skip / 0 fail, 58 files). Main tree untouched (`git status` clean
before and after). Objections with their **actual** results:

- **O1 — boundary resolution: `parentId === markerId` FAILS on a reachable path
  — `closed-red`.** Probe `bun /tmp/ev30-skeptic/probe-boundary.ts` on a real pi
  `SessionManager`: `user parentId === markerId: false` ;
  `user parentId === compId: true` ;
  `OWNER rule hit: undefined (MISSES -> all-zero record)` ;
  `PRINCIPAL rule hit: 325c083e` / `principal hit === injected user: true`;
  control with no intervening entry hits. Source chain: `session-manager.js:769-774`,
  `:835-844`; `agent-session.js:874-875,895` (`prompt()` flushes pending
  messages then `await this._checkCompaction(...)` **before** the user message
  exists), `:398` (the user message is persisted later at `message_end`,
  parented on the then-current leaf), `:1826` (`_runAutoCompaction` →
  `appendCompaction` re-parents the leaf). **Principal's append-order rule
  survives; owner's does not.** The miss is exactly the long
  compaction-heavy runs this record exists to measure.
- **O2 — `pi.appendEntry` semantics — `closed-green`, with a precision nit.**
  Probe output confirms `_appendEntry` pushes and advances the leaf,
  `appendCustomEntry` returns the id (string), and `getEntries()` preserves
  append order minus the header. Nit: `pi.appendEntry` itself returns `void`
  (`agent-session.js:2029-2033` discards the id), so the marker id is
  observable only via `getLeafId()` — the test must assert the leaf read-back,
  never the wrapper's return value.
- **O3 — own-session enumeration — `closed-red` for the card's literal 3-kind
  reading; `closed-green` for the 4-kind reading and the `buildContextEntries`
  rejection.** Probe output: `hand 4-kind input sum: 25 breakdown tokens: 25
  MATCH: true` / `3-kind sum (excl branch_summary): 22` / `getEntries count: 5
  buildContextEntries count: 2` / `context omits compaction? true`. `branch_summary`
  is counted by pi (`usage-totals.js:18-32`; `agent-session.js:2658-2663`) and
  carries `usage?` (`session-manager.d.ts:62-64`).
- **O4 — forest-rule facts hold — `closed-green`; T8's "vice versa" is FALSE —
  `closed-red`.** Probe output: `forest(inv1, T1): ["job-1","job-1.1","job-2"]` /
  `forest(inv2, T2): ["job-2"]` / `job-2 in inv1 forest (computed now)? true` /
  `T8 vice-versa (inv1 excludes job-2)? false` / `nested child job-1.1 in inv1
  forest? true` / `readManifests(missing dir): []`. The forest is a
  point-in-time filter, not an invocation identity; the "vice versa" holds only
  as a sequencing property.
- **O5 — per-half `Usage` shape — `closed-green` on the factual premise**
  (`runs.ts:18-19` single-valued; `hub.ts:138,195-199` subtree is
  stream-projected). A combined `total: Usage` cannot honestly carry one basis.
- **O6 — edge-case substrate — `closed-green`; end-to-end zero-record behavior
  `open-untested`.** Probe output: `readManifests(missing dir): []` (and
  `runs.ts:102-108`), `inMemory getSessionFile(): undefined` / `getEntries(): []`
  / `getLeafId(): null`, `absent-marker find threw? false hit: undefined`,
  `file-backed leaf===marker? true`. The full zero-record acceptance has no
  implementation to run against yet.
- **O7 — marker durability caveat — `closed-green` (precision finding).**
  `_persist` defers file creation until an assistant message exists
  (`session-manager.js:739-768`), so a marker stamped into a session with zero
  assistant messages yet is held in memory and the JSONL path does not exist
  yet. The stamp test's fixture must contain an assistant message, or assert the
  in-memory fallback.
- **O8 — citation nit — `closed-green`, immaterial.** `extensions/types.d.ts`
  does not exist in the repo; owner's citations resolve to pi's
  `dist/core/extensions/types.d.ts`.

**Settled by these tests (binding on the spec):**

1. **Boundary resolution is append-order-based** — the first on-branch
   `message`/`user` entry after the marker in `getEntries()` order. Owner's
   `parentId === markerId` rule is dead.
2. **T8's "vice versa" is false** — the test asserts only the direction the
   time rule guarantees (a later invocation's record excludes an earlier
   invocation's jobs), plus the point-in-time/recompute-drift property.
3. **The own half counts assistant + toolResult + compaction + `branch_summary`**
   (pi's own four kinds).
4. **Per-half `Usage`** values, each with its own single-valued basis/source.
5. **Marker id is read back via `getLeafId()`**, never from `appendEntry`'s
   (void) return.

**Skeptic verdict: `blocks`** — two specific items must be rewritten before
implementation: (a) the boundary rule (O1); (b) T8's "vice versa" (O4). Both
are closed by the tests above rather than by either seat's rhetoric.

**Open-untested (named, not hidden):** the full live
`sendUserMessage → prompt() → _checkCompaction → message_end` sequencing (needs
network/model); end-to-end zero-record behavior (no implementation exists
pre-implementation); live session-file-under-run-dir behavior; gate
failure-injection (tree mutation forbidden pre-branch).

**Ruling questions the Skeptic says no test can settle** (its own list, before
the consolidator's sort): (1) `branch_summary` in or out of the own half;
(2) `total` on the record vs. derived at render by EV-32, and under what basis;
(3) the unresolved-boundary label form (`entries=..` vs `entries=none` vs a
resolved/unresolved discriminator); (4) the meaning of "rotated session" to be
recorded in the card; (5) whether the record must state its subtree half is a
lower bound, or invoking-session-only scoping is recorded as deliberate;
(6) idempotence under time-eligibility (compute-time/job-id-set, or
valid-only-if-written-once-at-exit); (7) soft — bind the `lastId` rule
(leaf-chain end) or leave it as implementation judgment.
