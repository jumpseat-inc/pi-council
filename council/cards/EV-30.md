---
id: EV-30
title: Invocation-scoped spend record from the invoking session plus its job subtree
state: Deliberating
owner: null
epic: EPIC-7
goal: Given a session JSONL carrying assistant, tool-result and compaction usage plus a run directory carrying that invocation's job manifests, the accounting function returns a spend record whose own-session fields equal the hand-summed usage of the entries at or after the invocation boundary and whose subtree fields equal the sum over the invocation's job forest, and an automated test over a fixture session and manifest set asserts both halves against the hand sum.
---

## Intent

The invoking session has no manifest at all — the hub writes manifests only for
jobs it dispatches — so the hub's job forest is not the invocation's spend. This
card is the accounting function: given the invoking session's JSONL and the
invocation's run directory, it returns a spend record whose own-session half is
the hand-summable usage of entries at or after the invocation boundary
(assistant, tool-result and compaction usage; pi counts all entries, including
those compacted away) and whose subtree half is the sum over the invocation's
job forest. The boundary is the entry id of the user message the procedure
injects. The record names the boundary it covers, so a reader knows what the
number includes. Attended runs cannot express a run total today because every
top-level manifest has `parentJobId` null; the record is the first object that
can. Edge cases — zero dispatched jobs, a rotated session, an already-pruned run
directory — resolve to a zero subtree, never an error.

## Acceptance

- A fixture session plus a fixture manifest set yields own-session fields equal
  to the hand sum of entries at or after the boundary and subtree fields equal
  to the hand sum over the job forest, asserted by an automated test.
- A zero-job invocation returns a zero subtree.
- The record carries the boundary it covers.
- `bun test`, `bunx tsc --noEmit`, `python3 council/validate.py` stay green.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-4 boundary label** — the spend record names its boundary as
  `boundary=session=<sessionId> entries=<firstId>..<lastId> jobs=<n>`.

## Run record

### Step 1 — classification (facilitator)

- **Path: full council.** Two independent triggers: (a) *spec-ambiguity* —
  the card's `goal` names an accounting function over "the invoking session's
  JSONL" and "the invocation's run directory" but not where that function
  lives, how it reaches the session's entries (the live
  `ctx.sessionManager` vs. the session file path), or how the *boundary* entry
  id is captured at injection time — the procedure injects its user message
  through a generic command handler (`extensions/index.ts:205-236`), which is
  the only place the boundary exists, and more than one reasonable design
  reaches it; (b) *design judgment* — the card's own Intent requires the
  attended-run rootlessness fact to be reconciled explicitly (top-level
  manifests carry `parentJobId === null`, so `sumSubtree(manifests, "job-1")`
  covers one seat, not the run), and the record's own-session field names and
  the zero-subtree edge cases admit more than one shape consistent with the
  goal.
- **Surface-touching: no.** The deliverable is a data record plus an
  automated test; nothing in this card renders a surface a person reads or
  acts on. EV-32 owns rendering the record at exit points, and the record's
  boundary string is already bound by Phase 1 ruling R-4. Seating: `owner` and
  `principal` as the two generators in steps 2–3; `designer` is not seated.
