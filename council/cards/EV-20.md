---
id: EV-20
title: Matrix runner with repeat aggregation
state: Ready
owner: null
epic: EPIC-4
goal: A slash command runs a declared task-and-model matrix with a configurable repeat count, writes one results record per run, and aggregates mean and variance per cell so a comparison is reproducible from the records alone
---

## Intent

User-visible surface: a new slash command (name settled in deliberation —
candidates `/eval`, `/council-eval`) with an argument hint like
`[task] [model...]`. The person types the command, sees progress in the
session transcript, and receives a summary when the matrix finishes. It runs
each task under each requested model for N repeats (N configurable), writes
one on-disk record per run, and aggregates mean and variance per
task-and-model cell. Results records live outside the ephemeral `runs/`
telemetry boundary (convention 12) — the spec (EV-16) names the storage
location.

This is the command surface: which command, which screen (the in-session
transcript where progress and the summary render), and which states
(in-progress, done, failed).

## Acceptance

- The command accepts a task, one or more models, and a repeat count, runs
  the full matrix, and reports a per-cell summary.
- One results record per run is written, and records alone suffice to
  recompute every aggregate (mean and variance) shown in the summary.
- Progress and the final summary render in the session transcript.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-3 (command name)**: the command is **`/council-eval`** (not `/eval`).
  Argument hint: `[task] [model...] [--repeat N]`. The no-argument form
  lists available tasks from the fixture set. EV-21's copy references this
  name.
- **R-4 (repeat default)**: default repeat count **3**, overridable via
  `--repeat N`. No implicit all-models sweep — the matrix contains only the
  tasks and models the caller names.
- **R-5 (storage)**: one results record per run is written under
  `council/eval-results/` (local-only, gitignored — see R-1 on EV-16).
  Records alone suffice to recompute every aggregate shown in the summary.

## Promotion ruling (product-owner, EPIC-4 cadence, 2026-09-03)

Stays `Backlog` until EV-19 lands `Done`; then promote `Backlog` → `Ready`
per `vault/raw/2026-09-03-po-epic4-promotion-cadence.md`. EV-20 runs the
matrix over fixtures that need grading; without the scorer, matrix output
cannot satisfy EV-19's re-grade-reproducibility clause. R-3, R-4, R-5 travel
with this card and are unaffected. The orchestrator applies this promotion
automatically as soon as EV-19's merge SHA is on local main and
`python3 council/validate.py` is clean; no further packet is required.

## Promotion record (orchestrator, 2026-09-03)

Promoted `Backlog` → `Ready` per the binding chain-promotion ruling
(`vault/raw/2026-09-03-po-epic4-promotion-cadence.md`): predecessor EV-19
landed `Done` (PR #15 merged `402abb7`, gates green on merged SHA), merge
SHA on local main. Applied automatically per the ruling; no further packet.
