---
id: EV-2
title: Theme configuration in the scaffold .council.json
state: Deliberating
owner: null
epic: EPIC-1
goal: The scaffold .council.json carries an editable theme section that loadThemeConfig parses and validates and that merges repo overrides over the shipped omp defaults
---

## Intent

User-visible surface: the customization file the consumer edits — this card
makes the theme editable without touching engine code.

`.council.json` already exists as the committed repo override file for seat
model/thinking (AGENTS.md 9.5, `loadCouncilConfig` / `applySeatOverride` in
seats.ts). The theme section lives alongside as a top-level `theme` key
(sibling of `council`) carrying `enabled`, `variant` (`auto|dark|light`),
and per-variant `dark`/`light` override blocks each with palette `vars`
and per-token `colors` overrides (settled shape, Round 2 — no `name` key:
family identity is fixed by NAME-1, see Deliberation Record). The exact
shape was for the Council to fix, subject to these requirements:

- Lives in the scaffold's `.council.json` so `/council-init` writes it into
  consumer repos, non-clobbering (a consumer's existing `.council.json` is
  never overwritten — scaffold invariant).
- Parsed and validated by the same config machinery: malformed JSON or an
  invalid token name / value throws with a message naming the file, like the
  seat overrides do today.
- Merge semantics: repo values win over shipped defaults; an absent section
  means "no theme config" (activation card decides what that implies).
- Editing a var or token recolors the next session; the shape should be
  small enough to hand-edit in a text editor.

> EV-2 reads the shipped theme JSON off disk via
> `path.join(PKG_ROOT, "themes", ...)`. Do NOT use `getPackageDir()` — that
> is pi's install dir, not pi-council's root, and would silently load pi's
> built-in theme (accent `#8abeb7`) instead of the shipped omp palette. See
> spec §4 (corrected).

## Acceptance

- `loadThemeConfig` returns the parsed theme section (spec §8 one-word fix:
  the sibling loader, with `theme` a reserved key skipped by
  `loadCouncilConfig`); unit tests cover happy path, missing section,
  malformed JSON, invalid token, and override merge (repo wins).
- Scaffold test: a fresh `scaffoldInto` into an empty temp repo writes the
  theme section; re-running into a repo with an existing `.council.json`
  leaves it byte-for-byte untouched.
- The default scaffold section matches the shipped omp palette from EV-1.

## Phase 1 Ruling (recorded, binding)

Recorded 2026 by the human before the run: the exact shape of the `.council.json` theme section was surfaced in the rulings preflight and **deliberately delegated to the Council** ("let the Council decide"). The Council fixes the shape within this card's stated requirements (sibling of `council`, name `pi-council`, repo-over-shipped merge semantics, non-clobbering scaffold) and must **not** escalate the shape question — it is a working decision, not an open-judgment dispute.

## Deliberation Record

### Round 1 — independent first pass (owner, principal, designer)

**owner** (engineering): Shape fixed: top-level `theme` (sibling of `council`) as `{ "enabled"?: boolean, "variant": "auto"|"dark"|"light", "dark"?: { "vars"?, "colors"? }, "light"?: { "vars"?, "colors"? } }` — replaces the spec's variant-agnostic `overrides` working proposal with per-variant override blocks. Forced, not cosmetic: the two shipped variants have **disjoint var namespaces** (dark: 15 vars incl. `accent`/`cyan`; light: 14 vars incl. `teal`; only ~11 names overlap — verified against both files), so a variant-agnostic `vars` override silently no-ops in whichever variant lacks that var name, and per-variant blocks are the only way to validate "var-ref resolves to a declared var" exactly (a `light.colors.accent = "cyan"` must throw: `cyan` is undeclared in light). Also matches the spec's *settled* merge text, which already names `theme.dark.vars.accent` (§3). Implementation: sibling `loadThemeConfig(repoRoot): ThemeSection | undefined` — spec-settled, zero blast radius since `loadCouncilConfig`'s loop reads only `parsed.council` so a top-level `theme` is skipped with **no code change at all** to the existing loader — plus a pure `mergeThemeSection(baseJson, overrides?)` that shallow-spreads vars then colors over the base read via `path.join(PKG_ROOT, "themes", ...)`. One correctness trap on the record: **never resolve the base's var-refs to hex before merging** — keep un-overridden tokens as var-refs so a consumer vars edit transitively recolors everything referencing it. Scaffold: add `{ "enabled": true, "variant": "auto" }` (no override blocks — delta, not palette dump). Drops the card's `name` (default `pi-council`) key: the family is fixed, NAME-1 forbids bare `pi-council` as a theme name, and a key with exactly one valid value is speculative. Tradeoffs: per-variant blocks ~2 lines more than one flat `overrides` when recoloring both variants; `export` (pageBg/cardBg/infoBg) not overridable in EV-2 (validation vocabulary is ThemeColor ∪ ThemeBg; export survives merge untouched, deferred); strict unknown-key throwing inside `theme`; seeded `enabled: true` redundant (presence implies enabled) but accepted so the off switch is discoverable. Testable claims: (1) `loadThemeConfig` returns `undefined` for absent section, `"theme": false`, `"theme": {"enabled": false}`; returns `{variant:"auto"}` for `"theme": {}` — one off-path for EV-3. (2) malformed JSON → throws naming the file, same message shape as `loadCouncilConfig`. (3) `theme.dark.colors.nonexistent` throws naming the token; `accent = "#fff"` (3-digit hex, rejected by pi's `hexToRgb` at construction) throws at parse; `light.colors.accent = "cyan"` throws (undeclared in light). (4) Merge transitivity: `dark.vars.cyan = "#123456"` → `merged.vars.cyan === "#123456"` AND `merged.colors.borderAccent` still `"cyan"` (var-ref survives) AND `merged.colors.bashMode` still `"cyan"`. (5) Repo-wins: `dark.colors.mdHeading = "#123456"` → merged value is the override; untouched tokens byte-equal the shipped file. (6) Delta acceptance: `mergeThemeSection(loadShippedTheme("dark"), seededSection.dark)` deep-equals the shipped dark JSON, same for light. (7) Scaffold: fresh `scaffoldInto` → `.council.json` created and `loadThemeConfig(root)` returns the seeded section; rerun over a user-written file → `skipped`, bytes identical. (8) Zero blast radius: `loadCouncilConfig` on `{"theme": {...}}` returns `{}`, no existing test changes.

**principal** (cross-seam): Seam is config-surface ↔ engine ↔ merge-base. `scaffoldInto` (scaffold.ts:35-57) copies `council/scaffold/.council.json` non-clobberingly; `loadCouncilConfig` (seats.ts:116-140) iterates only `Object.entries(parsed.council)` (seats.ts:133-135) so a top-level `theme` sibling is invisible to that loop — adding it breaks nothing; the reserved-key skip is a guard for the mis-nested `council.theme` case only. Return-shape blast radius verified: `loadSeat` (seats.ts:179) passes `loadCouncilConfig` into `applySeatOverride` which does `config[seat.name]`; scaffold.test.ts:42 and seats.test.ts:263/270 read the return map directly — a return-shape change breaks all three, the sibling `loadThemeConfig()` breaks none. Merge-base side: shipped files are `{vars, colors, export}`; var sets differ per variant (dark has `vars.accent` #febc38, `vars.cyan`; light has `vars.teal` #5a8080, **no `accent` var**); var names and token names **collide** (in dark both `vars.accent` and `colors.accent` exist; `colors.accent = "accent"` + `colors.mdListBullet = "accent"` reference the var while `colors.border = "blue"` references another). The binding settled constraint is §3's transitive recolor — "a repo edit to `theme.dark.vars.accent` must transitively recolor `accent`/`border`" — which already spells the path **per-variant** with a **`vars` layer distinct from `colors`**. Blind spots: config-surface vantage cannot see that a var override has transitive downstream effects across multiple tokens, so it is tempted to flatten vars/colors into one `overrides` map — exactly what the settled semantics forbids; engine/activation vantage cannot see that `accent` is both a var and a token; the card's own draft is internally split ("dark and light palette vars" reads per-variant, but "name (default pi-council)" collides with NAME-1). Reframe: the shape is more constrained than "let the Council fix it" suggests — two decisions are effectively already made by settled constraints, only the third is open: (1) vars/colors split is **forced** — a flat `overrides` map cannot express "recolor the accent *var* (so `mdListBullet` follows transitively)" distinctly from "recolor only the accent *token*" because `accent` is both; the flat proposal is mutually incompatible with §3's settled transitive-var-recolor semantics. (2) Per-variant keys (`dark`/`light`) over a single `overrides` block — settled text already spells `theme.dark.vars.accent`, card says "dark and light palette vars", var sets genuinely differ per variant, so a single `vars` block is incoherent (an `accent` var override would be dark-only, meaning under `variant: auto` undefined). (3) The one genuinely open call: cosmetic shells — recommend scaffold ships minimal `{ "enabled": true, "variant": "auto" }` omitting empty `dark`/`light` shells (absent variant key = no delta for that base); satisfies "default scaffold section matches shipped omp palette" *because it is a delta*. `name` dropped (family fixed; NAME-1). Shape proposed: `theme: { enabled: true, variant: "auto", dark: { vars: {...}, colors: {...} }, light: { vars: {...}, colors: {...} } }` with `variant` ∈ `auto|dark|light` validated but **not resolved** by EV-2 (auto→dark/light is terminal-dependent, EV-3's job), and per-variant validation (a `vars` key must be a *declared* var of that variant — `theme.dark.vars.teal` throws; `colors` keys must be ThemeColor ∪ ThemeBg; values in the four accepted formats). Testable claims: (a) the flat shape has no defined semantics for colliding names — a test setting flat `overrides.accent: "#123"` and asserting `mdListBullet` can pass or fail depending on which side of the ambiguity the implementer picks, proving the shape underspecified. (b) Transitive recolor pin: `theme.dark.vars.accent → "#123"` recolors both `accent` and `mdListBullet`; `theme.dark.colors.accent → "#123"` recolors only `accent` — two assertions, same input key name, different merge layer. (c) Per-variant var validation: `theme.dark.vars.teal` throws (teal is light-only). (d) Spec §3's first "verified" example is **stale**: under current code `{"council":{"theme":"dark"}}` hits `qualifiedOrThrow` (seats.ts:82-86) and **throws** "must be qualified as provider/id" — it does not produce `{"theme":{"model":"dark"}}`; the object form `{"council":{"theme":{"enabled":true}}}` → phantom empty seat is accurate; the reserved-key skip resolves both. (e) Reserved-key isolation: `{"council":{"theme":{...}}, "theme":{...}}` → `loadCouncilConfig` returns no `"theme"` seat entry and `loadThemeConfig` returns the sibling. (f) Scaffold non-clobber with the split: fresh scaffold writes a `.council.json` whose `theme` resolves to the shipped palette post-merge (delta, not dump); re-run over a pre-existing `council`-only `.council.json` leaves it byte-identical — meaning existing repos get *no* theme config until the user adds it (absence = off) — that last consequence should be an explicit assertion, not an accidental one.

**designer** (human-centered): The consumer-developer's hand-edit mental model is flat — "I see X color in the TUI, I want to change X" — and the override surface should mirror what they perceive, with the layered vars↔colors abstraction available but not pushed in their face. Argues for **`theme.overrides` split into two optional sub-objects `colors` and `vars`**, each with its own validation vocabulary and merge order: `vars` resolves first (transitively driving any tokens whose shipped values are var-refs), then `colors` wins last so a specific token can always be pinned independently of any `var` change. Keys in `colors` are pi ThemeColor ∪ ThemeBg names; keys in `vars` are the shipped var names; values in either follow pi's accepted formats. Variant is a single top-level `variant: "auto" | "dark" | "light"`. Off is `enabled: false` (and absence; spec already accepts `theme: false` as equivalent). Gulf closed: *Execution* for the consumer who wants to recolor the accent — `colors` gives a 1:1 mapping from TUI-perceived color to a key they can type, no need to read the shipped asset; *Execution* for the rarer shared-color retune — `vars` is the right knob, discoverable from the seeded scaffold; *Evaluation* — empty `colors: {}` and `vars: {}` in the seeded scaffold teach the affordance before the user has typed anything; a one-line diff + next-session recolor is immediate feedback. Principles: the shipped file's layered vars↔colors structure (EV-1 round-2 verdict: "the affordance that makes the palette teachable"); hiding it behind a flat `overrides` is a teachability regression — but requiring the consumer to learn the structure to recolor one token is also wrong; splitting re-exposes the distinction at the user-facing edge. Knowledge in the world: with the split, the layered structure is on screen. Mapping: `colors.accent` ↔ accent in TUI (1:1); `vars.accent` ↔ every token whose shipped value references `"accent"` (transitive). Forcing function/error tolerance: validation rejects unknown `colors` keys and unknown `vars` keys with a message naming the file and listing the union of valid names. Merge (settled by spec): read `PKG_ROOT/themes/pi-council-{dark,light}.json`, apply `theme.overrides.vars` over shipped `vars` (repo wins), then `theme.overrides.colors` over shipped `colors` (repo wins, after vars); var-refs compose (EV-1 round-2 principal verdict: var-refs are THE load-bearing fact). Rejects principal's flat-overrides proposal (a user writing `blue: "#336699"` edits a var without knowing; visual change spans border/borderAccent/thinkingLow/mdLink/bashMode — a feedback surprise the layered structure was built to avoid; Gulf of Evaluation widens). Rejects the spec's working proposal (same flat problem). Falsifiable predictions: (1) token-level override compose — `theme: { variant: "dark", overrides: { colors: { accent: "#ff8800" }, vars: {} } }` → merged `colors.accent === "#ff8800"`, other tokens equal shipped dark. (2) var-level transitive — `overrides.vars.blue = "#336699"`, empty `colors` → `colors.border`, `borderAccent`, `thinkingLow`, `mdLink`, `bashMode` all `"#336699"` in dark; `colors.mdHeading` (literal `#febc38`, not a var-ref) unchanged. (3) Override-wins layering — `vars.blue = "#336699"` AND `colors.border = "#888"` → `border === "#888"`, other four blue consumers `#336699`. (4) Validation fail-fast names the file and lists valid token names for `colors.accemt` typo. (5) Validation var vocabulary — `vars.amber` throws (no `amber` var in either file — spec §1's amber trap). (6) Off switch vocabulary — `theme: false`, `theme: { enabled: false }`, `theme: { variant: "dark" }` distinct states, loader return makes the distinction visible to EV-3. (7) Scaffold invariant — fresh scaffold writes theme section; rerun over user-edited file is byte-for-byte no-op (extend scaffold.test.ts:42-65, don't duplicate). (8) Identity — freshly scaffolded section with both sub-objects empty merges against shipped dark to byte-identical JSON. Preferences (taste): seeds `"variant": "auto"` explicitly; seeds both empty `colors: {}` and `vars: {}` (lean) vs omitting; no strong view on bare `theme: false` acceptance; exact error-message wording is a copy decision for the implementation card.

### Round 2 — bounded exchange (owner, principal, designer)

**owner** (engineering): Per-variant blocks win — the decisive constraint is the settled validation rule interacting with `variant: "auto"`: a declared var is per-variant by construction (dark declares 15 vars incl. `accent`/`cyan`; light declares 14 incl. `teal`, no `accent` var; 11 overlap). Under `auto` (the scaffold default), a single `overrides` block has no statically known variant, so its validator can only be the union of both var sets — accepting `vars.teal` (light-only) that silently no-ops on a dark terminal, the exact Gulf of Evaluation the layered structure exists to prevent. Per-variant blocks give every override key a statically known variant, so `theme.light.vars.accent` and `theme.dark.vars.teal` throw at parse deterministically. Designer's discoverability argument narrows to one decision (which variant block) for the single-token recolor — the scaffold + binding README §7 example close it. Concedes the single-token Execution cost (one extra path key) but answers it: there is no variant-agnostic accent in the TUI; the consumer's perceived accent IS dark's. On `name`: drop it, on the record as an interpretation, not a silent drop — the requirement "name `pi-council`" is satisfied by family identity (shipped JSON names `pi-council-dark`/`pi-council-light`); the §3 working proposal carries no `name` key; §8 says the spec outranks the card's draft wording; §2 (settled) says bare `pi-council` is a "family selector in config prose only — never a theme name." Strict unknown-key rejection inside `theme` makes the drop loud (a consumer carrying the old draft's `name` gets a throw naming file and key, not silence). Scaffold: minimal `{ "enabled": true, "variant": "auto" }` stands; designer's Evaluation concern redirected to the doc layer (binding README §7 work and EV-5) rather than empty JSON shells, which have zero test value (`mergeThemeSection(base, {vars:{}, colors:{}})` is identity) and force both `dark`/`light` shells into every consumer repo forever. Correction to own round-1: "no code change at all" overstates — the top-level sibling needs no change to the existing loop (verified seats.ts:124-125 `council === undefined → {}`), but the settled reserved-key skip for mis-nested `council.theme` is a one-line guard in the loop, and EV-2 should land it. Verified principal's claim: `parseAgentOverride` (seats.ts:80-84) throws `qualifiedOrThrow` on `{"council":{"theme":"dark"}}` — spec §3's "verified" example is stale; the object-form phantom empty seat is real. Designer's layering rule (vars resolve first, colors win last) is fully compatible — it constrains merge order within a variant, not block count. Testable claims: (1) designer's round-1 prediction (2) is falsified by the shipped files — dark `colors` consumers of var `blue` are `["border"]` and nothing else (`borderAccent`/`bashMode` are cyan-refs; `thinkingLow`/`mdLink` literal hexes); the correct dark pin is `dark.vars.cyan = "#123456"` → `merged.colors.borderAccent === "cyan"` and `merged.colors.bashMode === "cyan"` (var-refs survive); transitivity tests must be written per variant against the real graph. (2) Union-vocabulary no-op: `{variant:"auto", overrides:{vars:{teal:"#123456"}}}` (designer's shape) violates "invalid token throws"; per-variant shape: `{"theme":{"dark":{"vars":{"teal":"#123456"}}}}` throws at parse naming the file; `{"theme":{"light":{"colors":{"accent":"cyan"}}}}` throws (cyan undeclared in light). (3) Settled spelling + layering compose: `theme.dark.vars.accent = "#123456"` → `merged.colors.accent === "accent"` (var-ref) and `merged.colors.mdListBullet === "accent"`; `theme.dark.colors.accent = "#ff8800"` → `colors.accent === "#ff8800"` while `mdListBullet` still follows the var. (4) Off-switch vocabulary: `loadThemeConfig` returns `undefined` for absent / `theme: false` / `theme:{enabled:false}`; returns `{variant:"auto"}` for `theme:{}` — spec's "council.theme falsy" phrasing is a typo for "theme falsy". (5) Eager value validation mirrors pi's predicate: `theme.dark.colors.accent = "#fff"` throws at parse (pi's `hexToRgb` theme.js:95-106 rejects non-6-digit); 256-index values must be 0-255. (6) Zero blast radius restated: `loadCouncilConfig` on `{"theme":{...}}` returns `{}`; `{"council":{"theme":{"enabled":true}}}` yields no `"theme"` seat entry after the one-line reserved-key skip. (7) Name-key drop is loud: `{"theme":{"name":"pi-council"}}` throws "unknown key `name`" naming the file.

**principal** (cross-seam): The contract already exists and is asymmetric: both shipped files have identical token names (`colors`, 51 names) but disjoint var names — var name is variant-scoped, token name is not. The common "recolor the accent" case is NOT one var across both variants — it is `vars.accent` in dark and `vars.teal` in light (omp's deliberate intent: accent tracks background temperature); any single `colors.accent` override silently erases it. The settled §3/EV-3 design already committed to the delta model (merge base = `path.join(PKG_ROOT, "themes", "pi-council-{dark,light}.json")`; activation = in-memory nameless; scaffold section = "delta, not palette dump") — under that model the config file is not the place the shape teaches itself. Spec §3's "verified" example is stale twice: (1) `{"council":{"theme":"dark"}}` THROWS `council["theme"] model "dark" must be qualified as provider/id` (qualifiedOrThrow seats.ts:78-81), not phantom override; the object form → phantom empty seat is accurate; the Skeptic's EV-5 Objection 8 repeated the error and it propagated into the settled spec. (2) §3's transitive example "vars.accent → recolor accent/border" is wrong about `border`: `border` follows `vars.blue`, not `vars.accent`. Blind spots: owner cannot see the first-use stumble "which variant do I edit?" is a real discoverability cost absorbed by the README snippet; designer cannot see the vars/colors asymmetry (single `vars` block looks coherent and isn't) nor that the scaffold seed reaches fresh installs only (scaffold.ts:35-57, non-clobbering). Reframe: the three disputes are one decision — is the theme section a **delta over a fixed shipped base** or a **self-describing theme spec**? Designer's three positions (single block, `name` key, empty shells) are all the self-describing-spec answer; owner's three (per-variant, drop `name`, minimal seed) are all the delta answer. The settled design already chose delta. Per-variant blocks win: under `auto`, `overrides.vars.accent="#123456"` has exactly three outcomes — throw (undeclared in light), silent no-op in light, or synthesize `light.vars.accent` + rewrite `light.colors.accent:"teal"→"accent"`; no-op is the silent surprise, synthesize mutates the shipped structure and breaks var-ref survival. **No single-block design preserves transitivity + var-ref-survival + auto simultaneously.** Drop `name` as a recorded ruling-application: NAME-1 satisfied by asset names and merge-base path, not a config key; a key with exactly one legal value read by no consumer path (activation is nameless/in-memory) is dead surface; the card Intent wording should be conformed in the EV-2 PR (same precedent as EV-1's NAME-1 card-text flag). Minimal seed wins; discoverability moves to the README (annotated full shape + worked example, EV-5 §7 mandate). Designer's layering rule composes with per-variant — it IS the per-variant merge order; one pin: "vars first" must mean "merge the vars **map** first", not "resolve var-refs to hex first" — resolution happens at Theme construction after both merges, or transitivity dies. Testable claims: T1 variant-scoping throws (`theme.dark.vars.teal` and `theme.light.vars.accent` both throw); T2 two-layer probe same key name (`vars.accent` → resolved `colors.accent` AND `mdListBullet`; `colors.accent` → only `colors.accent`, `mdListBullet` stays shipped `#febc38`); T3 single-block-under-auto incoherence — every implementation must pick throw/no-op/synthesize, no outcome satisfies the settled invariants; T4 spec-correction no new code (`loadCouncilConfig({council:{theme:"dark"}})` throws the qualified-provider error; `{council:{theme:{enabled:true}}}` returns `{theme:{}}`); T5 name-key dead surface (assets register `pi-council-dark`/`pi-council-light`, `getThemeByName("pi-council") === undefined`, activation installs nameless theme — no consumer reads `theme.name`); T6 scaffold seed is a taste call — minimal and empty-shell seeds both pass delta-identity, no test distinguishes them; settle by minimal-delta + README snippet, don't pretend a test settles taste.

**designer** (human-centered): **Concedes per-variant blocks on the merits.** The disjoint-var-namespaces fact is binding: under single `overrides.vars`, `vars.accent = "#..."` under `variant: "light"` silently no-ops, and the only honest alternatives are throw-at-parse (less forgiving than per-variant validation, which names the *correct* variant) or inventing a runtime active-variant rule the consumer must predict. Per-variant keys make variant scope visible *in the key path itself*; validation vocabulary matches the var vocabulary per variant — the only honest vocabulary. The discoverability win for one flat block survives only if empty shells are seeded (also conceded), so the win evaporates when both halves of the position are pulled. Drop `name` (zero valid states under NAME-1; all three seats agree). Seed minimal `{ "enabled": true, "variant": "auto" }` — README carries the structure; empty `{}` shells teach "this exists and is empty" at 14+ characters of noise. Layering rule (vars first, colors last) is per-merge-order, not per-key-path — per-variant merge implements it without modification. Gulfs: Execution — variant scope in the key path, no need to predict the active variant at write time; Evaluation — a key the variant lacks throws at parse naming the file and the valid var list *for that variant*; no silent no-op. Falsifiable predictions: (1) `theme.dark.vars.teal` throws (teal light-only); `theme.light.vars.cyan` throws (cyan dark-only). (2) Per-variant independence: `theme.dark.vars.accent = "#123456"` → merged light deep-equals shipped light. (3) Inactive-variant overrides inert at activation (contract for EV-3, not implemented here). (4) Var-ref transitive survives: `theme.dark.vars.cyan = "#123456"` → `merged.dark.colors.border === "#123456"` AND `merged.dark.colors.bashMode === "#123456"` AND `merged.dark.colors.mdHeading === "#febc38"` (literal, untouched). (5) Layering survives: `vars.cyan = "#123"` AND `colors.border = "#888"` → `border === "#888"`, `bashMode === "#123"`. (6) Unknown-key discipline for `name`: either throws (strict) or silently accepted (lenient) — implementation card settles; test asserts whichever. (7) Scaffold byte shape: fresh `scaffoldInto` writes `.council.json` whose `theme` field is byte-equal to `{"enabled": true, "variant": "auto"}` — no `dark`/`light` shells, no `name`. (8) Non-clobber extends to theme: re-run over a user-edited `.council.json` lacking `theme` leaves the file byte-identical (does NOT add a `theme` key) — principal's round-1 point (f) deserves to be an assertion, not an accident. Preferences (taste): strict unknown-key throw for `name`-as-unknown-key; empty shells revisited — concedes to minimal, one-line follow-up if observation later shows first-time consumers don't discover the per-variant split.

**Facilitator note — settled shape (recorded for the record):** Round 2 stabilized without a third round (positions converged: designer conceded on the merits, owner/principal aligned). Settled shape: top-level `theme` sibling of `council` = `{ "enabled"?: boolean, "variant": "auto"|"dark"|"light", "dark"?: { "vars"?: Record<string,string>, "colors"?: Record<string,string> }, "light"?: { "vars"?: Record<string,string>, "colors"?: Record<string,string> } }`; no `name` key (family identity via shipped asset names, NAME-1); per-variant validation (vars keys must be declared vars of that variant; colors keys must be ThemeColor ∪ ThemeBg; values hex / 256-index / var-ref / ""); merge at JSON level over `path.join(PKG_ROOT, "themes", ...)`, vars map merged first then colors map, var-refs never resolved to hex before merging; scaffold seeds minimal `{ "enabled": true, "variant": "auto" }` (delta, not palette dump); strict unknown-key throwing inside `theme`; off-switch: `theme: false` or `theme.enabled === false` or absence ⇒ off; `variant` validated but not resolved (auto→dark/light is EV-3's job). Card text conformed per spec §8 one-word fix (`loadThemeConfig`) and the settled shape (Intent), citing spec §8 and the EV-2 shape ruling.

### Round 3 — Skeptic attack (step 4)

**skeptic** — attacked the record and ran probes against current main (no implementation exists yet). VERDICT: blocks — 2 closed-red record errors, 6 open-untested design gaps; the settled shape itself survived.
Probes run (actual output): (P1) stale spec §3 behavior confirmed — `{"council":{"theme":"dark"}}` THROWS `council["theme"] model "dark" must be qualified as provider/id` (not phantom override); `{"council":{"theme":{"enabled":true}}}` → phantom seat `{"theme":{}}`; sibling-only `{"theme":{...}}` → `{}` (invisible to loop today). (P2) var-consumer graphs from the shipped files: dark 15 vars / light 14 / overlap 11; dark-only `cyan, gray, darkGray, accent`; light-only `teal, mediumGray, lightGray`; `blue → [border]` ONLY; `cyan → [borderAccent, bashMode]`; `accent → [accent, mdListBullet]`; `thinkingLow`/`mdLink` are literal hexes, NOT var-refs; 18 literal hex colors in dark, 13 in light; identical 51 color token names both variants. (P3) pi's `hexToRgb` throws `Invalid hex color: #fff` for 3-digit (theme.js:96). (P4) existing suite 148 pass / 2 skip / 0 fail. (P5) scaffold `.council.json` has no theme section today.
Objections:
1. `theme: {}` return value underspecified vs off-switch contract — "absence ⇒ off" ambiguous (absence of the whole key vs of `enabled`). Settling test: `loadThemeConfig({"theme":{}})` → `{variant:"auto"}`; spec must state the absence definition. `open-untested`.
2. Scaffold delta under `variant:"auto"` cannot directly satisfy acceptance "default scaffold section matches the shipped omp palette" — no merge base at EV-2 scope. Settling test: `mergeThemeSection(loadShippedTheme("dark"), {variant:"auto"})` deep-equals shipped dark; same for light. Must be resolved by (a) EV-2 resolving auto for merge-identity testing, or (b) rephrasing the acceptance. `open-untested`.
3. Falsy vocabulary incomplete: `theme: null` (valid JSON, `typeof null === "object"`) and `theme: 0` crash a naive boolean check. Settling test: `loadThemeConfig({"theme":null})` and `{"theme":0}` → off/undefined without crashing. `open-untested`.
4. Designer round-2 prediction (4) factually wrong: `theme.dark.vars.cyan = "#123456"` → `colors.border === "#123456"` is FALSE — `border` references `blue`, not `cyan`. Correct: cyan consumers are `borderAccent` and `bashMode`. Tests must be written against the real graph. `closed-red` (record error; verified by P2).
5. `export` section silently dropped by the described merge shape — merge as `{vars, colors}` loses it; merge of full JSON preserves it. Not specified. Settling test: `mergeThemeSection(loadShippedTheme("dark"), {})` deep-equals full shipped dark INCLUDING `export` (or explicitly asserts the return shape drops it). `open-untested`.
6. Empty-string values in merge underspecified — `""` is the "use pi default" sentinel, distinct from absent key; must be a valid override value. Settling test: `mergeThemeSection(dark, {colors:{text:"blue"}})` → `"blue"` (var-ref); `{colors:{text:""}}` → `""`. `open-untested`.
7. `theme: {}` with no `variant` key — implicit default unspecified. Either default `"auto"` or throw. Settling test pins whichever is chosen. `open-untested`.
8. `colors` validation vocabulary source unspecified — valid token names must be defined (from shipped JSON keys, a static list, or the 55 = 51+4 optional); a hardcoded list must stay in sync. Settling test: `loadThemeConfig({"theme":{"dark":{"colors":{"nonexistent":"#000"}}}})` throws naming the token. `open-untested`.

**Facilitator note — objection disposition:** Objection 4 is a record-correction (the owner's own round-2 already stated the correct graph — `cyan → borderAccent/bashMode`, `blue → [border]` — but designer's round-2 output repeated the wrong pin; the implementation tests must follow the verified graph). Objections 1, 2, 3, 5, 6, 7, 8 are open-untested design clarifications that land in the spec (off-switch falsy handling incl. null/0; `theme:{}` → `{variant:"auto"}` with absence defined as whole-key absence; merge-identity acceptance restated as "delta merges to shipped palette for either variant"; `export` preserved through merge; `""` a valid override value; absent `variant` defaults to `auto`; validation vocabulary = 51 shipped color keys + 4 optional, verifiable set) and each gets a settling test at step 9. None require a ruling seat — all are resolvable within the settled design by the owner.

### Round 4 — Consolidator synthesis (step 5)

**consolidator** — three buckets:
**Agreed design:** top-level `theme` sibling of `council` = `{ "enabled"?: boolean, "variant": "auto"|"dark"|"light", "dark"?: { "vars"?, "colors"? }, "light"?: { "vars"?, "colors"? } }` — no `name` key. Per-variant override blocks (disjoint var namespaces; single variant-agnostic block under `auto` validates only against the union, accepting light-only `teal` that silently no-ops on dark). Drop `name`: family identity via shipped asset names (NAME-1); drop is loud (strict unknown-key throw). Merge: JSON-level over `path.join(PKG_ROOT, "themes", ...)` (never `getPackageDir()`), vars map first then colors map, var-refs never resolved to hex before merging (resolution at Theme construction — transitivity lives on var-ref survival). Loader: sibling `loadThemeConfig(repoRoot)` (spec §8 one-word fix); `theme` reserved key skipped in `loadCouncilConfig`'s loop (one-line guard for mis-nested `council.theme`). Scaffold seed: minimal `{ "enabled": true, "variant": "auto" }` (delta, not palette dump); non-clobbering (re-run over user `.council.json` lacking `theme` leaves it byte-identical — does NOT add a `theme` key). Off-switch: `theme: false` or `theme.enabled === false` or absence ⇒ off. `variant` validated but not resolved (EV-3's job). Validation: `vars` keys must be declared vars of that variant; `colors` keys ThemeColor ∪ ThemeBg; values hex / 256-index / var-ref / `""`; strict unknown-key throwing inside `theme`; malformed JSON throws naming the file.
**Settled disputes:** per-variant over flat (Round 2 convergence); flat-overrides incoherence under `auto`; stale spec §3 example (Skeptic probe P1 — `{"council":{"theme":"dark"}}` THROWS); `hexToRgb` rejects 3-digit (probe P3); existing suite green 148/2/0 + scaffold has no theme section (probes P4/P5); spec §8 one-word fix; closed-red #1 var-consumer graph (probe P2 — `blue→[border]` ONLY, `cyan→[borderAccent,bashMode]`, `accent→[accent,mdListBullet]`, `thinkingLow`/`mdLink` literal hexes — implementation transitivity tests must follow this verified graph); closed-red #2 designer's Round 2 prediction (4) factually wrong (`vars.cyan` does NOT recolor `border`); Phase 1 "name `pi-council`" vs no-`name`-key — settled, not open: the shape ruling delegated the shape and forbade escalation; all three seats converged grounded in NAME-1 + §8; drop is loud; recorded so a steward can see the interpretive chain (if a steward disagrees with the Council's reading of its own mandate, that is steward-level — but the deliberation did not leave it open).
**Open judgment:** none. The Phase 1 ruling pre-empted escalation of the shape question (including the `name` aspect). No values/tradeoff dispute survived Round 2.
**Open objections (7, all open-untested — settling tests written and run at step 9):** (1) `theme:{}` return / absence definition — `loadThemeConfig({"theme":{}})` → `{variant:"auto"}`; absence = whole-key absence. (2) Scaffold delta acceptance under `auto` — acceptance restated as "delta merges to shipped palette for either variant"; settling test `mergeThemeSection(loadShippedTheme("dark"), {variant:"auto"})` deep-equals shipped dark; same for light. (3) Falsy vocabulary incl. `null`/`0` — both → off/undefined without crashing. (4) `export` preservation through merge — merge of full JSON preserves it; settling test `mergeThemeSection(loadShippedTheme("dark"), {})` deep-equals full shipped dark INCLUDING `export`. (5) `""` as valid override value (use-pi-default sentinel). (6) Absent `variant` default — pin `"auto"` or throw. (7) `colors` validation vocabulary source — 51 shipped keys + 4 optional; `theme.dark.colors.nonexistent` throws naming the token.
**Ready to hand off?** Yes — to the owner for implementation. Shape settled within Phase 1-delegated authority; two closed-red graph corrections are fixed test expectations; seven open-untested objections are design clarifications the owner resolves by writing their settling tests at step 9. Card ready to BEGIN implementation but not complete until all seven settling tests pass at step 9 and transitivity tests reflect the verified graph.
