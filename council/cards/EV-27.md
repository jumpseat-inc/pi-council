---
id: EV-27
title: `/`-triggered search input in the model selection modal
state: Done
owner: owner
epic: EPIC-6
goal: Pressing `/` while the model selection modal is at the model level opens a focused search input below the top row, typed characters filter the visible model rows to those whose `qualifiedId` contains the query case-insensitively, pressing Esc with focus in the search input clears its text, and pressing Esc elsewhere at the model level ascends to the provider level unchanged.
---

## Intent

The user-visible surface addressing the stated pain: with providers like
OpenRouter serving hundreds of models, the model-selection level of the
`/council-models` modal (`ModelPicker`, `extensions/model-picker.ts`) is
hard to scan. The surface is the modal's model-selection level (level 2).

Ruled contract for the surface:
- `/` (a printable key on the existing `handleInput` path) opens search
  mode at the model level only; the input renders below the top row — the
  R-1 header `council models — pick a model per seat` stays byte-exact.
- Focus is signified by a `▌` (U+258C) cell at column 0 of the
  search-input row (EV-8's signifier precedent) plus the input row's own
  affordance hint naming the `/` trigger; the four-footer exhaustiveness
  rule stays intact — the search row is never a fifth footer.
- `/` is typeable inside the input: within search mode every printable
  character appends to the query (capture by construction — the trigger
  key and the query character are the same character, handled by
  ordering, and `qualifiedId` values contain `/`).
- The list renders from EV-26's filtered rows; `modelIndex` clamps into
  the filtered set; the render-cache signature at
  `extensions/model-picker.ts:96` includes the query so no keystroke can
  serve a stale frame.
- Esc with focus in the input clears the input's text and keeps it
  focused; Esc elsewhere at the model level ascends to the provider level
  unchanged (`extensions/model-picker.ts:257-262` preserved).
- A query matching no rows renders a no-match empty state distinct from
  the two R-4 ruled empty states; its copy is the ruled literal
  `No models matching "<query>".` (EPIC-6 R-1).
- On Enter at the model level with the input visible, search state clears
  and the picked row advances into the confirm level; backing out of
  confirm (Esc at level 3) returns to the model level with the prior
  search state preserved. This floor is not pinned further.
- Confirm echo and `resolveSelection` are unchanged — a filtered row
  still emits the byte-verbatim `qualifiedId` plus thinking level.

## Acceptance

- Driven tests: `/` opens the input at level 2; typing `claude` narrows
  the rendered rows to `qualifiedId` substring matches; the input renders
  between the header and the first data row with the `▌` cell and an
  affordance hint naming `/`; the footer remains the ruled model-level
  string at every keystroke, including the no-match state.
- `anthropic/claude` (containing `/`) can be typed into the input without
  leaving search mode.
- Render cache: after every keystroke `render(width)` differs (query is
  part of the signature).
- Esc in the input empties the query and keeps focus; Esc at level 2
  outside the input transitions to level 1.
- The no-match state's string is byte-distinct from the two R-4 empty
  states and is asserted byte-exact.
- Selection through a filtered set emits the same `resolveSelection()`
  tuple as the unfiltered path; the confirm echo is byte-equal.

## Phase 1 rulings (features-deliver, binding for this run)

- **R-1 (affordance hint copy)**: while the search input is empty, the
  search row renders `▌ / filter · esc clears` — the `▌` focus signifier
  at column 0, then the hint text `/ filter · esc clears` byte-exact,
  matching the ruled footer's middot idiom. The hint is the empty-input
  rendering; typed query text replaces it.

Recorded human decision — immutable for the run and binding on every seat,
`steward` included.

## Deliberation

### Step 1 gate
Full council, surface-touching (the modal's model level gains a visible
search-input row, `▌` focus signifier, affordance hint, and no-match empty
state — this card changes what a person sees and reads). Per the run's
dispatch direction, `designer` is dispatched alongside `owner` and
`principal` as a third generator (council.md step 1). All surface copy is
Phase-1-ruled (EV-27 R-1 hint copy byte-exact; EPIC-6 R-1 no-match copy
byte-exact) and the interaction contract is a ruled surface contract on
this face; the deliberation confirms the contract is coherent and
unambiguous before the owner builds, and does not reopen ruled copy.

### Step 2 — independent first pass
`owner`, `principal`, and `designer` dispatched in parallel, each with only
the card (jobs 9.1/9.2/9.3). Positions recorded verbatim.

**owner — first pass:** Add level-2-local search state to `ModelPicker`:
`searchActive` + `query`. `currentRows()` at level 2 returns
`filterModelRows(rowsForProvider(group), query)` when active (single source
of truth for windowing, Enter's `picked`, and pushRows — `resolveSelection()`
stays byte-verbatim by construction). Handle order: settled → level 3 →
search-mode interception (level 2 only: printable-append, Esc=clear-and-stay)
→ existing up/down/enter/escape unchanged (incl. the 257-262 ascend) →
fall-through `/` trigger (level 2, not active, `group.models.length > 0`).
Render: input row after `HEADER` in search mode — `"\u258C " + (query ||
"/ filter · esc clears")`, right-truncation never clips the `▌`; no-match
branch renders dim copy + `FOOTER_MODEL` (footer present: Esc-clear is an
active key, unlike the keyless R-4 states). `signature()` gains
`searchActive`/`query`; `modelIndex` re-clamps into the filtered set after
every mutation. Correction to the card: a printable on a kitty-protocol
terminal arrives as a CSI-u sequence (`\x1b[47u`), not bare `/`; pi-tui's
own editor normalizes with `decodePrintableKey` (`editor.js:553`) but the
repo's import surface exposes only `decodeKittyPrintable` (index.d.ts:22),
so implement that recipe minus the unexported `decodeModifyOtherKeysPrintable`
arm. Pinned observables: Enter-at-level-2 does not drop the query —
"search state clears" is the momentary confirm view; "prior search state
preserved" is the observable round-trip. Fresh entry 1→2 resets search
state (mirrors cursor reset); 3→2 preserves it. Trigger gated on non-empty
model list. Crash-bugs to pin: (1) `modelIndex` past a shrunken filtered
set makes Enter set `picked = undefined`, throwing in `resolveSelection` —
clamp after every append/clear; (2) control bytes (`\x7f`, `\r`, `\x1b`
sequences) must never append — printable =
`decodeKittyPrintable(data) ?? (data.length === 1 && data.charCodeAt(0) >= 32
? data : undefined)`. Tradeoffs: no Backspace delete (Esc-clear is the ruled
sole deletion; guard-only, backspace a silent no-op; Backspace-as-delete
flagged as FLLWUP candidate); xterm mode-2 arm unavailable (accepted);
frame clip +1 line at full window is a pre-existing `withModalFrame` seam,
out of card scope; live-terminal routing of `/` unverified by the repo's
gates — unit tests prove the state machine given bytes; a manual
`/council-models` run named as a smoke follow-up, not a gate blocker.
Testable claims: (1) `decodeKittyPrintable("\x1b[47u")` → `"/"` ; driven
`handleInput("\x1b[47u")` opens input, `handleInput("\x1b[99u")` appends
`c`. (2) control-byte guard: `\x7f`/`\r`/`\x1b[A` leave `query` unchanged.
(3) `a` then `b` with equal-length/equal-window/equal-cursor filtered sets:
`render(80)` must differ (query in signature necessary). (4) Esc walk: type
`claude`, ESC → render contains byte-exact `▌ / filter · esc clears`, still
level 2, input still rendered; ESC again → still level 2 (no ascend from
input); separate walk without search → ESC → level 1 (existing 8.4
semantics preserved). (5)
no-match string byte-exact and distinct from both R-4 states; `FOOTER_MODEL`
last line through every keystroke including no-match. (6) `anthropic/claude`
typed incl. the `/` stays in search mode; `alias/claude` narrows to the two
alias rows; `resolveSelection()` after Down+Enter on a filtered row is the
same tuple as unfiltered; confirm echo byte-equal.

**principal — first pass:** Cross-seam reading: `filterModelRows` is merged
(model-picker.ts:92-100) but its consumer's three row-list consumers are
not the same call — `currentRows()` (`:142-148`) returns the full
cross-product, `windowStart()` (`:150-154`) and the Up/Down clamps (`:245`,
`:252`) read `currentRows().length`, and `pushRows` is the only place a
filter could be applied late. The card's "`modelIndex` clamps into the
filtered set" forbids filter-at-render-tail: the filter must be interposed
at `currentRows()`, or cursor, window, and visible rows read three
different lists and `resolveSelection()` reads the wrong array. The
"signature includes the query" clause pins the same seam from the cache
side: `claude` → `claud` (both 2-row results) leaves a query-blind
signature unchanged and serves a stale frame — the query must be a
signature field, not an accident of window math. Blind spots: pipeline view
cannot see that filter output must become the renderer's row list;
renderer view's one `searchActive` boolean cannot route Esc two ways — the
Esc clause is a dual-meaning contract (in-input → clear-and-stay,
elsewhere → ascend) and the acceptance tests both; a second bit
(`inputFocused`) and which key transitions focus out are a handler-state
contract the card does not pin; "search state clears on Enter" read as
`query = ""` passes every listed acceptance while violating the Intent's
own "prior search state preserved". Serving view: `resolveSelection()`
byte-verbatim guarantee only holds if the cursor has been clamped over the
filtered set. Reframe: level 2 is a two-bit state machine (searchActive,
inputFocused) and the filter is the row source: (1) `currentRows()` at
level 2 returns `searchActive ? filterModelRows(rowsForProvider(group),
this.query) : rowsForProvider(group)`; (2) `signature()` adds
`:${searchActive}${inputFocused}:${query}`; (3) `/` with `!searchActive`
opens (query untouched, `inputFocused=true`); `/` and all printables with
`searchActive` append and set `inputFocused=true`; Up/Down move the list
cursor AND set `inputFocused=false` — the unstated key that makes the
"Esc elsewhere" acceptance reachable; Esc routes on `inputFocused`;
(4) B-7 by not mutating on Enter: "search state clears" means the search
row is not part of the level-3 confirm screen; "prior search state
preserved" falls out because the fields are never zeroed. Testable claims:
(1) `/` + query + Down + Esc → level 1, query not cleared (a one-bit state
fails this non-vacuously). (2) `claude` → `claud` → `render(w)` differs
with byte-identical filtered set and cursor. (3) query + cursor movement +
Enter + Esc → same filtered rows, query text, cursor as pre-Enter.
(4) no-match branch (`zzz`) renders `No models matching "zzz".` AND
`FOOTER_MODEL` as the last line with the search row visible — byte-distinct
from R-4#2 which renders no footer (`:182-188`).

**designer — first pass:** Argues the interaction model where focus is a
*sub-state* of the model level: typeables/backspace/Esc act on the query,
arrows/Enter still act on the picker — the single model in which every
ruled byte can be the only thing it claims to be, and a person does not
relearn the keyboard for the duration of a search. Evidenced by: the `▌`
signifier at the focus surface (EV-8 PO ruling 3 precedent, hard-ruled);
knowledge-in-the-world — R-1's empty-input rendering puts the whole
contract on screen (`▌` = where focus is, `/ filter` = how to use it,
`esc clears` = how to leave the text), reusing the ruled footer's middot
idiom; mapping — input row is the predicate, rows below the extension, the
no-match copy taking the rows' place in the same column with dim styling;
feedback immediate — query in the render-cache signature so no keystroke
serves a stale frame; the conceptual model is vim-less-search's `/`
analog. Falsifiable predictions R1–R15: empty-input row byte-exact; typed
text replaces hint with `▌` surviving; `▌` exactly one visible column;
`FOOTER_MODEL` last line at every keystroke incl. no-match; no-match copy
byte-distinct from both R-4 states; Esc-in-input clears and keeps the
input visible; Esc-outside ascends (level 2→1); arrows navigate filtered
rows; `modelIndex` re-clamps into the filtered set on every keystroke;
cache invalidates on query change; selection through a filtered set emits
the same tuple (8.7 continues to pass); `/`-in-query (`openrouter/claude`)
typeable without exiting search; Enter at level 2 advances to confirm
(level 3 renders contain no `▌` row); Esc from confirm restores the prior
search state (`▌ claude`, `modelIndex` preserved); the `▌` appears at no
other level and in no non-search state (existing 8.8 keeps passing).
Design observations: (a) discoverability of `/` before it is pressed is
the largest remaining design gap — the only card-compliant closes are a
first-time hint line (feature surface the card does not authorize) or
accepting the ruled footer as the ceiling; predicts the council accepts the
latter and does not push back; explicitly wants no `/ filter` added to the
model-level footer (would break the four-footer exhaustiveness rule).
(b) the card does not name which keys the input owns — binds the
sub-state model so the test surface has a single contract. Preferences,
ranked last: append-only input with backspace-from-the-end and no visible
cursor marker inside the query text (a second cursor would split the focus
signal; conflicts with EV-8's one-column hard rule); truncate the
empty-input hint from the right (`/ filter` survives — matches existing
`truncateToWidth`); clamp `modelIndex` on every keystroke to the last
surviving row.

### Step 3 — bounded exchange (3 of 3 rounds used)
All three generators re-dispatched with each other's verbatim positions;
rounds 2 and 3 run. Positions recorded verbatim per round.

**Round 2 — owner:** Converges on the structural core with both seats
(filter at `currentRows()` incl. the existing `currentRows().length === 0
→ return` Enter guard; query-in-signature mandated — `claude`→`claud`
serves a stale frame otherwise; B-7 non-mutation; trigger gating;
no-match footer discriminator). Self-corrects: the round-1 printable guard
admits `\x7f` — `decodeKittyPrintable` rejects only codepoints < 32, so
127 sails through as `String.fromCodePoint(127)`, and the bare arm
`charCodeAt(0) >= 32` admits it too; `handleInput("\x7f")` and
`handleInput("\x1b[127u")` would append a DEL. Fix is ordering: check
`matchesKey(data, Key.backspace)` as a guard-only no-op BEFORE the
printable decode (covers `\x7f`, `\x08`, kitty `\x1b[127u`, and
modify-other-keys — strictly better than a DIY codepoint gate). Pushes
back on the two-field model: with `searchActive + query` only, Esc while
searchActive is unconditionally intercepted and the no-match walk
(`/` `z` `z` `z` `z` → Esc clears → Esc again still clears → Down clamps
against an empty set → Enter no-ops) cannot reach level 1 — the user
cannot close the modal. Level 2 needs the second bit; the key that
transfers focus out is Up/Down (move-in-filtered-set AND
`inputFocused=false`; any subsequent printable re-focuses) — the only
transition that also works at zero rows, exactly where an exit is needed.
Kitty claim: confirmed real — `dist/index.d.ts` exports
`decodeKittyPrintable` but not `decodePrintableKey`; EV-27 is the first
surface that needs the decode; the minus-modify-other-keys-arm tradeoff is
acceptable. On the designer: the sub-state model is right but incomplete —
no focus-away state makes R7 unreachable the moment the input is visible;
it needs exactly one binding the designer declined to make (which key
moves focus out), and that binding must be Up/Down. R13's wording invites
a snapshot implementation — pin the observable. +1 frame line: read
`withModalFrame` (`navigator.ts:96-105`: tail-clip); pre-existing seam,
out of card scope; a search-mode window of `maxRows - 1` is the fix if the
council ever wants it. Testable claims: (1) `\x7f` and `\x1b[127u` must
leave `query` unchanged. (2) `/`+`zzzz`+Down+Esc must land at level 1.
(3) `/`+`claude`+Down+Esc → level 1 (ascends, does not clear); re-entering
1→2 shows the unfiltered set and no search row (fresh-entry reset).
(4) stale-index crash: `/` `a` `l` (3 rows), Down Down (index 2), `i` `a`
`s` (2 rows, index 2 out of range) → Enter must not throw and must emit
`{seat, "openrouter/alias/claude-sonnet", thinking:"high"}`. (5) `claude`
vs `claud` → `render(80)` differs. (6) B-7 round-trip: `/`+`claude`+Down
+Enter+Esc → level 2, line 1 byte-equal `▌ claude`, same rows, `modelIndex`
1. Concedes: `inputFocused` deliberately excluded from the signature
(R-1 bytes don't depend on it); backspace stays a guard-only no-op;
xterm arm omitted.

**Round 2 — principal:** Verifies the OWNER's kitty correction against the
installed artifact: `council-models.ts:148-149` hands `d` straight to
`picker.handleInput(d)` with no normalization; under kitty protocol a
printable `/` arrives as `\x1b[47u`; `index.d.ts:21` re-exports
`decodeKittyPrintable` but not `decodePrintableKey`;
`decodeKittyPrintable("\x1b[47u")` returns `"/"` — verified. Filter side:
byte-verbatim-by-construction verified — `filterModelRows` returns
identical `PickRow` references. The `modelIndex`-shrink crash is real,
pointing at the throwing line (Enter does `this.picked =
this.currentRows()[this.modelIndex]`; on a shrunken-but-nonempty filtered
set with stale index, `picked` is `undefined` and `resolveSelection`
throws). Blind spots: OWNER treats "Esc-clear is the sole deletion" as
pinned but the card only rules Esc clears; and the OWNER's own predicate
admits `\x7f` (127 ≥ 32), contradicting the OWNER's own test 2. DESIGNER's
R9 encodes backspace-as-delete as if ruled (it isn't); R7 asserts "Esc
outside the input ascends" while the sub-state model never names the focus
toggle. Reframe: (1) the card smuggles a second focus surface into a
single-cursor machine; collapsing focus into `searchActive` makes the
second Esc clause a dead letter while search is open. The ruling must
state it: either (a) accept search-sticky-until-commit and record the
dead-end as intended, or (b) rule a focus/exit toggle (cheapest closure: a
second Esc on an already-empty query deactivates search, returning to the
plain model level where the 257-262 ascend is reachable — vim/less
behavior). (2) the backspace byte must be ruled, not inferred —
three-way contradictory: DESIGNER's R9 says delete, OWNER's test 2 says
no-op, OWNER's stated predicate actually appends. Testable claims:
(1) `decodeKittyPrintable("\x7f")` is `undefined`, so OWNER's fallback
appends a DEL — exactly one of the OWNER's predicate and the OWNER's test
2 can be true; the predicate needs an upper bound. (2) `handleInput("\x7f")`
with `query === "claude"`: length afterward 5 (no-op) vs 6 (append) vs
5-with-different-rows (delete) — pick one in the ruling. (3) from a
search-active level 2, assert: no sequence reaches level 1 without passing
through level 3 — if true, the elsewhere clause is dead while search is
open. (4) shrink-then-Enter: pin it clamps and `resolveSelection()`
returns the surviving row's byte-verbatim `qualifiedId`. (5) signature
necessity. (6) kitty slash typeability. Converges on: sub-state model,
`filterModelRows` as single source of truth, query in the render
signature, Enter-drops-nothing with round-trip through confirm. The only
places to pull them apart: the `\x7f` byte and the search-mode exit
topology — both ruling questions, not taste.

**Round 2 — designer:** The implementation must be a two-bit state machine
— `(searchActive, inputFocused)` plus `query` — with the filter at
`currentRows()`. One bit cannot route Esc two ways; the focus transition
must be the unstated key the card implies — Up/Down is the natural one.
Enter at level 2 preserves the search state fields (not zeroed) so
backout-from-confirm preserves by construction. Gulfs closed: (1) moving
from "narrow the list" to "pick one" (Down exits input focus); (2) backing
out of confirm (Esc preserves the query); (3) typing `qualifiedId`s
containing `/` (capture by ordering). Evaluation gulf at no-match:
footer-present + dim copy + ruled literal distinguish it from R-4#2's
footer-absent state. Cache key superset: `signature()` includes
`:${searchActive}${inputFocused}:${query}` — not because `inputFocused`
affects rendering today, but so any future focus-driven visual change
lands without a stale frame. Esc routes on focus (`model-picker.ts:257-262`
preserved). "Search state clears" = the level-3 confirm screen does not
render the search row; it does not zero the search state. Predictions:
cache invalidation `claude`→`claud`; focus transition real (`/` `claude`
Down Esc → level 1, `searchActive === false`); backout preservation;
no-match third branch renders `[HEADER, "▌ zqzzzz", "No models matching
\"zqzzzz\".", "↑/↓ move · enter select · esc back"]`; `modelIndex` clamps
after every shrink; kitty-protocol smoke required to verify the
printable-routing claim (prediction, not a gate assertion). Rejects the
OWNER's pi-tui citations — checked `@earendil-works/pi-coding-agent`'s dist
and found no matches (later corrected: wrong package — the citations live
in `@earendil-works/pi-tui`, see round 3). Rejects literal `query = ""` on
Enter; rejects the one-bit state. Endorses the OWNER's trigger gate,
control-byte guard shape, no-match `FOOTER_MODEL`, backout-as-visibility-
rule; endorses the PRINCIPAL's `currentRows()` interposition, two-bit
state, focus-out-via-Up/Down, superset cache key, Enter-preserves-query,
no-match-as-third-branch. Backspace-as-delete retracted to FLLWUP
candidate — the card pins Esc-clear as the sole deletion mechanism.

**Round 3 — owner:** All contested facts settled against the installed
artifact, by probe: `decodeKittyPrintable("\x1b[47u")` → `"/"`,
`("\x1b[99u")` → `"c"`, `("\x7f")` → `undefined`, `("c")` → `undefined`;
`matchesKey("\x7f", Key.backspace)` → true (and `matchesKey("\x7f",
Key.delete)` → false, so DEL is unambiguously backspace in this runtime);
`setKittyProtocolActive` exists but is not the picker's job — the picker
only decodes. The `\x7f` route dissolves by ordering: guard before the
printable decode+fallback; concedes the principal's upper bound
(`charCodeAt(0) >= 32 && <= 126`) as contract documentation — the residual
set after the guard is exactly 32..126. Topology: the card itself already
ruled (b) — EV-27's acceptance line "Esc at level 2 outside the input
transitions to level 1" requires an outside-input state to be reachable,
which a one-bit collapsed machine cannot deliver; the two-bit machine with
Up/Down focus-out is the non-vacuous reading of the card's own acceptance,
not a discretionary closure. Concessions: (1) drops the round-2 exclusion
of `inputFocused` from the signature — the designer's monotonicity
argument wins at zero current-behavior cost; (2) backspace within EV-27 is
pinned as a swallowed no-op — answers principal's "pick one in the
ruling": query length after `\x7f` is 5, rows unchanged; (3) `modelIndex`
re-clamp on every keystroke stands, with the `currentRows().length === 0`
Enter-guard covering shrink-to-zero. Testable claims: (1) principal's
test 3 as written encodes the collapsed one-bit model and must NOT be
adopted; the settling test is the card's own acceptance made two-bit:
`/`, `claude`, `Down`, `Esc` → level 1 with `searchActive === false`;
re-entering 1→2 shows the unfiltered list with no input row — search
state is level-2-scoped and dies with the level on ascent. (2)
`handleInput("\x7f")` and `handleInput("\x1b[127u")` with `query ===
"claude"` → length stays 5, same rows, no cache clear. (3) driven kitty
path end-to-end: raw `d = "\x1b[47u"` handed verbatim to
`picker.handleInput(d)` opens the input; `"\x1b[99u"` appends `c`; while
open, another `"\x1b[47u"` appends `/` — proving `anthropic/claude`
typeability under kitty, which a bare-`/` driven test cannot falsify.
(4) shrink-then-Enter: filter N rows to 1 with `modelIndex > 0`, Enter
directly → `picked` is the surviving row and `resolveSelection()` emits its
byte-verbatim `qualifiedId`; filter to 0, Enter → consumed no-op, no
throw, query preserved. No further objections.

**Round 3 — principal:** The (a)/(b) question is answered by my own
round-1 machine: the two-bit `(searchActive, inputFocused)` state with
Up/Down → `inputFocused=false` and Esc-routes-on-focus IS option (b), and
it reaches level 1 from no-match (`/` `zzzz` `Down` `Esc` → level 1). No
open judgment remains on reachability topology. The one open item the
convergence left: the `▌`'s meaning — an R-1-immutability collision, not a
taste question. R-1 pins `▌` as the focus signifier, and the R-1 hint
`▌ / filter · esc clears` is itself a focus-conditional claim ("esc
clears" is only true while focused). The owner's "byte-identical in both
focus states" renders `▌ / filter · esc clears` while the picker has the
cursor — a focus signifier and an esc-clears promise both shown while
unfocused: a lying signifier, and R-1 is immutable, so the implementation
cannot amend it. The only coherent reading: the search row renders `▌`
iff `inputFocused`, which makes `inputFocused` a render input today and
forces it into `signature()` — present correctness, not future-proofing.
Testable claims: (1) at level 2 with `searchActive=true, query="claude"`,
render(80) must differ between `inputFocused=true` and `inputFocused=false`
at column 0 of the search row (`▌` present iff focused); if the row stays
byte-identical, R-1's "focus signifier" is violated; if it differs,
`inputFocused` must be in `signature()`. Either horn refutes "exclude
`inputFocused`". (2) `\x7f` guard (conceded — the owner's test stands, with
the added assert that nothing is deleted). (3) non-mutation vs
snapshot/restore: the Intent's "search state clears" and "prior search
state preserved" in one bullet is internally contradictory — non-mutation
is the only coherent reading within the "floor not pinned further"
latitude; if the PO wants the ambiguity gone, a one-line pin "confirm is
visibility-only; no state zeroing" suffices. (4) discoverability FLLWUP
(not a gate): at no-match with focus, the ascending exit (Down→Esc) is
invisible — Down has no visible cursor movement at zero rows — but
Esc-clear is the natural footer-advertised recovery; record as FLLWUP
(no-match hint naming the focus-out key), not a card gate.

**Round 3 — designer:** Correction verified against the right package:
`index.d.ts` exports `decodeKittyPrintable`, `Key`, `matchesKey`, `parseKey`
but not `decodeModifyOtherKeysPrintable`/`decodePrintableKey`; `keys.js`
~1155/1171 are module-local. My round-2 check was the wrong package but
the factual conclusion was correct in spirit. Converges (binding for the
owner): filter at `currentRows()` with `modelIndex` clamped on every
keystroke that changes the filtered length; no-match is a third branch
with ruled copy `No models matching "<query>".`; trigger gate `/` opens
only when `level === 2 && group?.models.length > 0`, below which `/` is a
literal (current behavior unchanged at other levels); signature must
include `query` (and may include `inputFocused`) — `level:cursors:top:q:f`;
B-7 non-mutation with round-trip through confirm; backspace/`\x7f`/kitty-
DEL/modify-other-keys backspace: guard-only no-op (no append, no delete) —
Esc is the card's sole deletion mechanism; single-char correction is
FLLWUP, not card scope. Concedes: the export-surface check was mis-aimed
(wrong package), consequence for the card the same; the `inputFocused` bit
is meaningless without a focus-toggling key — the OWNER's read is correct,
without a binding `inputFocused=false` is unreachable from a visible-input
state and no-match becomes an unclosable dead-end; the `\x7f` byte must be
pinned — `matchesKey(data, Key.backspace)` as a guard-only no-op before any
printable decode is the only correct upper bound. Still stands: two-bit
state machine; Up/Down as the focus-toggling key at level 2 (the only
transition that works at zero rows); Esc routes on focus; trigger-gate
shape; no-match footer discriminator; cache key superset (query +
`inputFocused`). REMAINING OBJECTION (explicit escalation): the card text
does not say what Esc does when focus is in the input and the query is
already empty. The candidate bindings differ: first-Esc-deactivates-search
(one transition per Esc) vs double-Esc-deactivates (vim/less; empty-query
Esc is a no-op, the next Esc deactivates). The card text supports either
reading — a ruling question for product-owner, not a design disagreement.
Asserts the card text supports the literal read literally ("Esc with
focus in the input clears its text" — one clause, no second-Esc clause),
so the implementation should default to it unless the PO overrides. No
further design objections.

Exchange closed at the 3-round cap; positions stabilised on the structural
core (filter at `currentRows()`; query + `inputFocused` in the signature;
two-bit focus machine with Up/Down as the focus-out key; Esc routes on
focus; Enter non-mutation with round-trip preservation; `modelIndex`
re-clamp on every keystroke; backspace guard-only no-op; kitty printable
decode via `decodeKittyPrintable` plus legacy fallback with the `\x7f`
guard ordered first; no-match third branch with `FOOTER_MODEL`; trigger
gated on a non-empty model list). Two flagged items carried to the
Skeptic and consolidator: (A) the second-Esc-on-empty binding,
explicitly escalated by designer as a product-owner ruling question;
(B) principal's `▌`-signifier claim, which reads R-1's "focus signifier"
wording as requiring a focus-conditional rendering — in tension with R-1's
byte-exact empty-input rendering `▌ / filter · esc clears` (unconditional
on focus) as the immutable Phase-1 ruling records it.


### Step 5 — synthesis (consolidator, verbatim)

**Agreed design — settled by convergence (all three seats, rounds 2–3):**
1. Filter interposed at the `currentRows()` level-2 branch (`searchActive ? filterModelRows(rowsForProvider(group), query) : rowsForProvider(group)`) — single row source for `windowStart`, clamps, `pushRows`, Enter guard, Enter-pick; `resolveSelection()` byte-verbatim by `PickRow` reference identity.
2. Signature `level:cursors:top:q:f` — `query` and `inputFocused` both included (`inputFocused` as a superset/monotonicity include, conceded by owner at zero present-behavior cost).
3. Two-bit focus machine at level 2: `(searchActive, inputFocused)` + `query`; `/` opens (level 2, `!searchActive`, `group.models.length > 0`); printables append and re-focus; Up/Down move the list cursor AND set `inputFocused=false`; Esc routes on focus (focused → clear-and-stay; unfocused → ascend to level 1, 257–262 preserved); Enter preserves search state (non-mutation); 3→2 restores; 1→2 resets. No-match dead-end closed by the Up/Down focus-out transition.
4. `modelIndex` re-clamps after every keystroke changing filtered length; `currentRows().length === 0` Enter-guard covers shrink-to-zero.
5. Printable decode: `matchesKey(data, Key.backspace)` guard (no-op) BEFORE `decodeKittyPrintable(data) ?? (data.length === 1 && 32 <= charCode <= 126 ? data : undefined)`. Backspace never appends nor deletes; Esc-clear is the sole deletion mechanism.
6. No-match third render branch: dim `No models matching "<query>".` (EPIC-6 R-1 byte-exact) + `FOOTER_MODEL` present, byte-distinct from both R-4 states.
7. R-1 hint row: empty query → `▌ / filter · esc clears` byte-exact, unconditional on focus; typed text replaces the hint text (`▌` stays at column 0); `▌` appears at no other level and in no non-search state.
8. Enter with input visible → confirm without zeroing `query`/`searchActive`; backout restores query, filtered rows, `modelIndex`.

**Settled disputes — each with the test/ruling that settled it:**
- **Objection 1 — `▌` focus-conditional (principal's claim).** Settled **by ruling** (R-1 Phase-1, immutable): R-1's condition is "while the search input is empty", not "while focused"; the `▌` renders unconditionally in the empty-input state regardless of `inputFocused`. The principal's "iff `inputFocused`" reading adds a condition R-1 does not contain. The principal's "lying signifier" concern is recorded but does not survive R-1's immutability.
- **Objection 2 — second-Esc-on-empty binding (designer's flagged item A).** Settled **by ruling** (Intent, ruled surface contract): "Esc with focus in the input clears its text and keeps it focused" is a single binding clause — clears (no-op when already empty) AND keeps focus; no second-Esc-deactivates reading. The card's own Intent answers the designer's PO escalation; no PO judgment required.
- **Objection 3 — `decodePrintableKey` accessibility.** Settled **by Skeptic factual probe** (with correction): `decodePrintableKey` IS deep-importable via `dist/keys.js` (the package has no `exports` field, so deep imports are unrestricted); it is merely not root-exported. The convergence's `decodeKittyPrintable` + legacy fallback approach remains correct and unchanged; a deep import is an available alternative.
- **Objection 4 — shrink-crash.** Settled **by Skeptic**: stale `modelIndex` after filter shrink → `currentRows()[modelIndex]` is `undefined` → `resolveSelection()` throws via `this.picked!` → `pick.model`. Re-clamp after every keystroke is necessary and correct.
- **Objection 5 — query in signature.** Settled **by Skeptic**: `claude` and `claud` both → 2 alias rows, same cursor, identical current signature `2:0:0:0:0` without query. Query must be a signature field.
- **Objection 6 — `\x1b[127u` ordering.** Settled **by Skeptic**: `decodeKittyPrintable("\x1b[127u")` → `"\x7f"` (DEL, codepoint 127). Guard-first (`matchesKey(data, Key.backspace)`) and/or the `<= 126` upper bound independently prevent the append; the convergence prescribes both (belt and suspenders).
- **Objection 7 — `withModalFrame` tail clip.** Settled **by Skeptic** (out of scope): pre-existing seam at `navigator.ts:96-105` (`content.slice(0, Math.max(0, panelHeight - 2))`); out of card scope; a search-mode window of `maxRows - 1` is the future fix.
- **Objection 8 — signature delimiter ambiguity (`q:f` when query may contain `:`).** Settled **by Skeptic**: the signature is a template string compared by full equality, never parsed; query-last placement keeps it unique by construction.

**Open judgment — for `product-owner`, escalating to `steward`:** None. Both items the deliberation carried forward as possible PO escalations — (A) the second-Esc-on-empty binding and (B) the `▌`-signifier focus-conditionality — were closed by applying binding card rulings (the Intent and R-1 Phase-1 respectively), not by arbitrating taste. No values or tradeoff question remains open.

**Open objections:** None. All eight Skeptic objections were tested and closed-green. The converged structural core survives unchallenged.

**Follow-ups recorded (not gates):** backspace-as-single-char-deletion (FLLWUP candidate); no-match hint naming the focus-out key (FLLWUP — discoverability gap at zero rows where Down has no visible cursor movement); live-terminal kitty smoke (named follow-up, not a gate); `withModalFrame` +1 frame-line overrun (pre-existing seam, out of card scope); pre-press `/` discoverability gap accepted — the ruled footer is the ceiling, and no `/ filter` is added to the model-level footer (four-footer exhaustiveness rule preserved).


### Step 7 — In Progress
Spec committed (`docs/superpowers/specs/2026-09-05-EV-27-design.md`,
full-council path — the settled design from steps 2–6, self-reviewed: no
placeholders, consistent with the synthesis, scoped to
`extensions/model-picker.ts`, unambiguous). Handed to the single `owner`
with the spec as the design contract; the owner works in an isolated
worktree, pushes a feature branch, opens a PR against `main`.


### Step 8 — In Review
PR #24 open (base `main`, head `feat/ev-27-search-input` @
`bacbbf85fa1c0743b5961f117ffa674870c27878`) — observed via `gh pr view 24`.
Owner reports all four gates green at that head (frozen-lockfile install exit
0; `bunx tsc --noEmit` exit 0; whole suite 526 pass / 2 skip / 0 fail;
validate clean), plan at
`docs/superpowers/plans/2026-09-05-EV-27-search-input.md`, 13 test-surface
assertions + 2 ruled-copy pins red-first, no scratch files left in `test/`.


### Step 10 — judge PASS
Judge dispatched with the card's `goal` and the Skeptic's step-9 evidence
only (job-9.14). Verdict **PASS**: independently re-ran the full suite and
typecheck at head `bacbbf85` and confirmed every goal clause — `/` at the
model level opens the focused search input below the top row (bare and
kitty `\x1b[47u`); typing narrows the rows to `qualifiedId` case-insensitive
substring matches; Esc with focus in the input clears its text (and stays,
including on a second Esc); Esc elsewhere at the model level ascends to the
provider level with the search state dying with the level.

### Step 11 — deterministic merge check (features-deliver substitution)
All five criteria met, read fresh against head
`bacbbf85fa1c0743b5961f117ffa674870c27878`:
(1) owner gates green in full — Skeptic-run at the head (frozen-lockfile
install 0; tsc 0; suite 526 pass / 2 skip / 0 fail; validate clean) with
gate-integrity injections (tsc turned red by a type change; copy tests
turned red by a one-word hint change; both reverted); (2) `gates` workflow
SUCCESS on the PR head (`gh pr checks 24` →
`[{"name":"gates","state":"SUCCESS","workflow":"gates"}]`, keyed on
`workflow`); (3) no blocking Skeptic objection (17/17 probes closed-green);
(4) judge PASS; (5) no Needs Human / outstanding ruling (both flagged
deliberation items closed by binding rulings on the card face).
Merged with `gh pr merge 24 --squash --match-head-commit bacbbf85…` → PR
#24 MERGED, merge commit `3452abbb4623cd7e9939d81578a555c3cc015779` on
`main` (head re-read immediately before merge: unchanged). Gates workflow
re-ran on the merged SHA: run 33948092436 → success.

### Step 12 — Done
Reconciliation: local `main` had diverged from `origin/main` because the PR
squash folded this card's board commits (made before the branch was cut)
into `3452abb`; reconciled by **union merge** (house pattern,
wiki/card-id-allocation) — merged `origin/main` in, resolved the card
conflict to the superset (In Review + step-8 record), removed the duplicate
board line, kept the designer's raw doc — merge commit `f8f70e4`, pushed
(`3452abb..f8f70e4 main -> main`). Card and board set **Done** — the
observed artifact is: merge landed (`3452abb`), gates green on the merged
SHA (run 33948092436). Validation clean. Follow-up and persistence items
recorded below for the orchestrator; no new FLLWUP card was written to
`council/cards/` (step 13 draft-then-confirm is the orchestrator's gate).


### Step 13/14 — follow-up candidates and persistence (for the orchestrator)
No new FLLWUP card written — step 13's draft-then-confirm gate is the
orchestrator's. Candidates surfaced by this run (highest existing FLLWUP id
is 11; new ids start at 12):
- **FLLWUP-12 candidate** — backspace as single-character deletion in the
  search input (owner + designer flagged; Esc-clear is today's sole
  deletion; a typo costs clear-and-retype).
- **FLLWUP-13 candidate** — no-match hint naming the focus-out key (the
  Down→Esc exit is invisible at zero rows; principal's discoverability
  FLLWUP).
- **FLLWUP-14 candidate** — live-terminal kitty-protocol smoke for
  `/council-models` (unit tests prove the state machine given bytes; the
  CSI-u delivery path needs a manual /council-models run on a
  kitty-protocol terminal; owner-named follow-up, designer prediction 6).
- **FLLWUP-15 candidate** — `withModalFrame` +1 frame-line overrun in
  search mode at full window (pre-existing tail-clip seam; future fix is a
  search-mode window of `maxRows - 1`).
- **FLLWUP-16 candidate** — pre-press `/` discoverability: a first-time
  hint line for the trigger (designer's largest design gap; not authorized
  by this card; ruled footer stays the ceiling — recorded so the idea is
  not lost).
Persistence offer: durable items for wiki-ingest (step 14) —
(1) the union-merge reconciliation pattern under features-deliver when a PR
squash folds board commits (this run's step-12 reconcile); (2) the two-bit
focus-machine pattern (searchActive + inputFocused + Esc-routes-on-focus)
as a reusable surface pattern; (3) the ruling-by-ruling closure precedent
(both flagged deliberation items closed by applying the card's own
Phase-1 rulings, no PO round-trip). The designer's raw round-2 doc is
already at `vault/raw/2026-09-05-design-ev27-round2.md` (committed) as
wiki-ingest source material. No vault edits made by this runner.
