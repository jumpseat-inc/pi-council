---
id: EV-41
title: End-to-end falsifier for provider-error retry on both paths
state: Done
owner: null
epic: EPIC-9
goal: One falsifier forces a Provider finish_reason error through both a parent turn and a seat child on the shipped tree and observes the automatic retry end to end.
---

## Intent

Built and run before the mechanism lands, so it starts red — a Council command
without an end-to-end falsifier is a defect. The card must name both forcing
paths: a stub-child mode (`STUB_MODE=finish_error`) that emits the bare string
`Provider finish_reason error` as its `errorMessage`, and a harness that fails
a parent turn's provider once in a scratch session, in both the TUI and `-p`
branches. The existing `error` stub mode emits `Provider returned 502:
upstream unavailable`, which already matches pi's retryable pattern and so does
not exercise the intake's class.

## Run record (features-deliver / EV-41)

### Step 1 — classification (facilitator)

- **Path: mechanical.** The card ships a falsifier, not a mechanism: it changes
  no engine module under `extensions/`, no `.council.json` surface, no rendered
  council surface, no dispatch path, and no run substrate. Its deliverables are
  test infrastructure (a `test/stub-child.ts` mode, the falsifier itself, and a
  recorded red-at-base run). Both forcing paths are already named by the card's
  `Intent`, and both fixtures exist on the shipped tree (EV-40's
  `test/ev40-harness/`; EV-39's real-Hub retry chain in `test/hub.test.ts`), so
  there is no design left to derive — only reuse, wiring, and evidence. Per
  council.md step 1 a mechanical card skips steps 2–6 and goes directly to
  step 7: no `owner`/`principal`/`designer` deliberation runs and no
  `consolidator` synthesis is written.
- **Surface-touching: no (recorded).** A falsifier asserts on harness and
  telemetry artifacts (session JSONL, stdout, manifests, job-tree rows). It
  adds no user-visible string, surface, empty state, or error state. No
  `designer` is seated; a designer finding would be a step-13 follow-up, never
  a reopening of a mechanical card.
- **Rulings applied (Phase 1, binding): O3 literal hygiene.** The forcing
  literal is the **with-colon** `Provider finish_reason: error`, byte-equal to
  `PROVIDER_FINISH_REASON_ERROR` (`extensions/retry.ts:54`). The recorded ruling
  states the colon-less string is classify-negative on both clauses and tests
  nothing; the shipped constant is the authority. The card's `Intent` spells
  the literal without the colon ("the bare string `Provider finish_reason
  error`"); that spelling is read as naming pi's raw emitted message class —
  i.e. *not* the 502-framed `Provider returned 502: upstream unavailable` that
  the existing `error` stub mode emits — and not as a second literal. The
  card's `goal` phrase "a Provider finish_reason error" resolves to the same
  with-colon message pi emits for a declined finish_reason, and the
  orchestrator's dispatch input states the same explicitly ("with the colon";
  "the with-colon literal is load-bearing"). **No card text is amended:** the
  literal is fixed by the binding ruling and the shipped constant, and the
  `goal` is not changed by reading it correctly. R1 (classifier superset),
  R2/R3 (retry defaults + validation), R4 (hub attempt-label visibility) and
  R5 (retry surface copy) are the mechanism's inputs; they are cited where they
  bind and none is extended to a question it did not answer. No `ESCALATION`
  arises on their account.
- **Steward's standing ruling (carried in the orchestrator's dispatch input;
  applied verbatim, not re-asked).** EV-41 cannot land red through the merge
  gate, so its "starts red" obligation is met by running the authored falsifier
  against the merge-base / pre-mechanism base in a worktree and observing red
  there, then green at the head. The base-commit red run is recorded as
  evidence; **no red test lands**. The pre-mechanism base is named here so the
  red evidence is reproducible: `3a3773f8ed92b816d47aaff288eb87698b1d99e7`,
  the commit immediately preceding the epic's first mechanism merge (EV-37
  `6a375c4`). The owner may additionally record a second, more discriminating
  pre-mechanism base (e.g. post-EV-38 `3e39e66`, before EV-39/EV-40) as extra
  evidence; the required evidence is the base run.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `skeptic`, and `judge`; all three resolve from the packaged seat set
  (`council/agents/*.md`, each file's `name:` matching its filename) and this
  checkout has **no `.pi/agents/`** repo-local override directory, so nothing
  shadows them. No `designer` (not surface-touching); `principal`/
  `consolidator` are not dispatched on the mechanical path;
  `product-owner`/`steward` are ruling seats this container never dispatches.
  No `Unknown seat`.
- **Environment:** step 0 preflight is skipped per the autonomous-run
  substitution (Phase 0 cleared the environment for the epic). Main checkout
  clean at `45ff83054f34316eb59a293ed4165ea9c13c54f7` == `origin/main`; `gh`
  authenticated as `tistaharahap`; no open PRs. All six sibling cards are merged
  and `Done` (EV-37 `6a375c4`, EV-38 `3e39e66`, EV-43 `8853712`, EV-40
  `6ab0e48`, EV-39 `79c0573`, EV-42 `952d5c1`). This card is `Ready`; no
  `Needs Human` state and no outstanding ruling — deterministic merge check
  criterion 5 holds at the start of the card.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md` does
  not exist here):** `bash council/preflight.sh EV-41`, `bunx tsc --noEmit`,
  `bun test`, `python3 council/validate.py` (AGENTS.md §Commands +
  `.github/workflows/gates.yml`, whose `gates` job runs exactly the last three).
  This repo defines no database/import/server gate; `COUNCIL_INTEGRATION=1`
  stays gated and is not run. The `preflight.sh` branch-freshness line
  (FLLWUP-27) fires by construction once a facilitator record commit advances
  `origin/main` past a branch cut — recorded verbatim, never reclassified, and
  never used to weaken a criterion.
- **Grounded environment facts (recorded; the owner re-verifies them):**
  - `test/stub-child.ts` today carries `emit|length|error|fail|hang|slow|flaky`
    and **no** `finish_error` mode. Its `error` mode emits `Provider returned
    502: upstream unavailable`, which matches pi's retryable pattern
    (`extensions/retry.ts:17-51`) and therefore does not exercise the intake's
    class. Its `flaky` mode (EV-39) reads/increments `STUB_STATE` and fails the
    first `STUB_FAIL_TIMES` calls with the 502 string — the
    fail-once-then-succeed shape the falsifier needs.
  - `classifyRetry` (`extensions/retry.ts:80-101`) returns `retry` for a
    `done` report with `stopReason === "error"` and the with-colon literal, and
    returns `undefined` for the colon-less spelling — the load-bearing reason
    for O3.
  - **Seat-child path at head:** `extensions/hub.ts`
    (`spawnJob`/`respawn`/`writeJobManifest`) + `extensions/job-retry.ts`
    (`createRetrySupervisor`). `test/hub.test.ts:491` already drives a real Hub
    + stub child through fail-once-then-succeed under one id / one manifest /
    `attempt: 2` / a fresh attempt-2 session id — the reuse base for the
    seat-child falsifier.
  - **Parent-turn path at head:** `extensions/parent-retry.ts` + the
    `agent_settled` registration in `extensions/index.ts`. E2E evidence already
    exists in `test/ev40-harness/harness-headless.ts`
    (`runHarnessArm({arm: "none", councilExtension: true})` drives the REAL
    installed pi CLI `-p` with the REAL council extension and a faux provider
    that fails with the with-colon literal; `test/ev40-live-gates.test.ts`
    P4/D4 assert the full attempt chain, the countdown lines and the exhausted
    copy on real stdout) and in `test/ev40-harness/tui-retry.py` (pty, 24×80,
    real TUI + real council extension, `EV40_ARM=none`, `EV40_FAILS=1`) for the
    TUI branch.
  - `test/ev43-reachability.test.ts` + `ev43/` are the merged reachability
    probe; the EV-43 colon-less literal is classify-negative and is **not** a
    model for EV-41's fixture.
  - Only real-session evidence counts for the parent path: the harness spawns
    the *installed* pi CLI (`node <pkg>/dist/cli.js`) with an explicitly
    constructed env (PATH + scratch HOME + harness vars, nothing inherited —
    the FLLWUP-21 env-split lesson).

### Step 7 — handoff to one owner

Mechanical path: no design spec is written (council.md step 7 — the card's
`Intent` and `goal` are the handoff). One `owner` dispatch against the card; it
works in a dedicated `git worktree` (never `main`), pushes a branch, opens a PR,
and clears every gate in full. The facilitator remains the single writer of
`council/board.md` and this card file for the duration of the card.

The handoff names four deliverables, all traceable to the card's own `Intent`,
`goal` and the applied rulings above (no new criterion is created here):

- **(a) The forcing stub mode.** `STUB_MODE=finish_error` in
  `test/stub-child.ts` emits an assistant `message_end` with
  `stopReason: "error"` and `errorMessage` byte-equal to
  `PROVIDER_FINISH_REASON_ERROR` (with colon), and exits 0 (the
  `state=done` trap the classifier exists to handle). It carries the
  fail-once-then-succeed shape (a `STUB_STATE` counter, like `flaky`) so the
  retry has a clean attempt to land on, and `test/stub-child.test.ts` pins
  byte-equality to the shipped constant.
- **(b) The seat-child falsifier, on the shipped tree.** A real `Hub` +
  `createRetrySupervisor` dispatch of the `finish_error` stub child that
  observes the automatic retry end to end: attempt 1 settles `done` carrying
  `stopReason error` + the with-colon literal (`classifyRetry === "retry"`),
  attempt 2 is spawned and settles clean under the **same job id and
  manifest**, with `attempt: 2`, one job-tree row, cumulative usage, and a
  final report naming the attempts.
- **(c) The parent-turn falsifier, in both branches, on the shipped tree.**
  The REAL council extension drives the retry (`arm: "none"`,
  `councilExtension: true`) against a faux provider that fails with the
  with-colon literal: the `-p` branch asserts the second assistant message /
  continuation plus the countdown on the real stdout, the TUI branch asserts
  the same through the pty runner. It keeps EV-43's attribution discipline (a
  control arm with no mechanism shows no continuation) and must not credit
  pi's own auto-retry to council — the with-colon literal is what keeps pi's
  `_prepareRetry` out of the picture.
- **(d) The pre-mechanism base red run.** The same authored falsifier run
  against `3a3773f` in a separate worktree, with the raw red output recorded
  verbatim in the owner's report and on this card. No red test lands on the
  branch.

The card's `goal` names **one** falsifier covering both paths: (b) and (c) may
be one test file with a shared verdict or the existing harness components wired
into one EV-41 falsifier — the owner states which, and the Skeptic attacks the
attribution claim either way.

### Step 8 — owner implemented, ran the falsifier, opened a PR

- Owner dispatch `job-26.1` (45-min ceiling; settled `done` at 18.3m / 48 turns,
  tokens in 132442 / out 32603 / cR 4278528 / cW 0 / reason 13759 / total
  4443573, cost≈$0.1097 catalogue).
- **Observed artifacts (read directly, not taken from the seat's report):**
  branch `feat/ev-41-e2e-falsifier` on `origin` at
  `03302744ddf0040bdb7d0df6cf883958aacec557`; PR **#57**
  (`test(ev41): end-to-end falsifier for provider-error retry on both paths`)
  `state: OPEN`, `isDraft: false`, base `main`, head ref
  `feat/ev-41-e2e-falsifier`, `headRefOid` `03302744…`, `mergeable: MERGEABLE`;
  `git diff --stat <merge-base> 0330274` → 5 files, +921,
  **no engine file touched**:
  `docs/superpowers/plans/2026-09-16-EV-41-plan.md` +77,
  `test/ev41-retry-e2e.test.ts` +364 (new), `test/ev41-tui.py` +407 (new),
  `test/stub-child.test.ts` +46, `test/stub-child.ts` +27. Worktree
  `.worktrees/ev-41` clean; main checkout untouched and clean at `12e2962` ==
  `origin/main`.
- **Owner's reported deliverables (claims, independently re-verified at
  step 9, never trusted as-is):** (a) `STUB_MODE=finish_error` emits
  `stopReason:"error"` + `errorMessage` byte-equal to
  `PROVIDER_FINISH_REASON_ERROR` (imported from `extensions/retry.ts`, pinning
  the literal `"Provider finish_reason: error"`), exits 0, with a
  `STUB_STATE`/`STUB_FAIL_TIMES` fail-once-then-clean counter and three TDD
  tests verified red then green; (b) a real `Hub` + `createRetrySupervisor`
  seat-child run observing attempt 1 `done`/`stopReason error`/`retry` verdict,
  the armed backoff, the mid-backoff manifest (`retrying`, `attempt: 2`,
  `sessionId=id`, `attempts=[{1,id}]`), a clean attempt-2 settle under one job
  id / one manifest (`attempt: 2`, `attempts=[{1,id},{2,id-attempt2}]`,
  cumulative `turns=2`), one job-tree row, and the `attempts=2/3` report head;
  (c) one falsifier file (`test/ev41-retry-e2e.test.ts`) whose parent-turn arms
  drive the REAL council extension (`arm:"none"`, `councilExtension: true`) in
  both branches — `-p` (`Retrying in 1s (attempt 2 of 3) — Esc to abort` on the
  real stdout plus the `start → error(literal) → retry re-send →
  EV40-SECOND-RESPONSE` JSONL chain, exit 0) and pty TUI 24×80
  (`EV41-TUI-VERDICT: GREEN`, treatment countdown + second response true,
  control false) — with a disabled-policy control on every branch and a pinned
  check that pi's own `isRetryableAssistantError` is `false` for the with-colon
  literal (attribution closed without copying the colon-less EV-43 string);
  (d) the pre-mechanism base red run at `3a3773f` in a separate worktree,
  `Cannot find module '../extensions/job-retry.ts'` and `'../extensions/retry.ts'`
  → `0 pass / 2 fail / 2 errors`, exit 1, plus a second more discriminating base
  `3e39e66` → `7 fail / 1 error`; both evidence worktrees removed and **no red
  test landed** on the branch.
- **Owner's own gate results — claims, independently re-verified at step 9:**
  `council/preflight.sh EV-41` → `PASS: preflight clean` exit 0 (no FLLWUP-27
  line — `origin/main` had not advanced past the branch cut, so this card is the
  first in EPIC-9 whose preflight did not fire it); `bunx tsc --noEmit` exit 0;
  `bun test` → `847 pass, 2 skip, 0 fail` (849 tests, 75 files, 94.31s), the 2
  skips being the gated `COUNCIL_INTEGRATION` tests; `python3 council/validate.py`
  → `All council artifacts valid`.
- **Criterion 2 observed by the facilitator:** `gh pr checks 57 --json
  name,state,workflow` → `[{name: gates, workflow: gates, state: IN_PROGRESS}]` —
  the `gates` workflow is present and running on head `0330274`; criterion 2 is
  adjudicated at step 11.
- **Transition:** `In Review` set from the observed artifact only — a branch
  exists with an open PR (council.md step 8). No seat's report was used as a
  precondition.

### Step 9 — verify by acting (skeptic, `job-26.2`) — cycle 1 of ≤3

- Skeptic dispatch `job-26.2` (30-min ceiling; settled `done` at 9.3m / 35 turns,
  tokens in 112108 / out 12735 / cR 1401715 / cW 0 / reason 4524 / total
  1526558, cost≈$0.4045 catalogue).
- **Verification subject pinned:** PR head SHA
  `03302744ddf0040bdb7d0df6cf883958aacec557`, head worktree
  `/home/tista/codes/pi-council/.worktrees/ev-41`. **Frame pinned:** step 9
  precedes step 10 judging and step 11's facilitator-executed mechanical merge;
  the merge had not happened.
- **Objections and their actual terms — every one `closed-green`, none open,
  none `open-untested`:**

| # | Claim attacked | Term | Basis (skeptic's real runs at the head) |
|---|---|---|---|
| 1 | Literal hygiene (O3) | `closed-green` | `PROVIDER_FINISH_REASON_ERROR` = `"Provider finish_reason: error"`; live: `classifyRetry` → `retry` on the with-colon literal and `undefined` on the colon-less one; `classifyParentTurnRetry` likewise; pi's `isRetryableAssistantError` is `false` for the with-colon literal and `true` for the 502 string; `grep "Provider finish_reason[^:]"` across the five touched files → no matches; the stub imports the constant and the test pins byte-equality (6/6) |
| 2 | Attribution | `closed-green` | parent arms are `arm:"none"` + `councilExtension:true` (both arms); no `COUNCIL_INTEGRATION`/`skipIf` in the falsifier; the `-p` control (retry disabled) has no countdown, no second message, one user send — it passed, so the treatment's second message is council's alone; TUI `EV41-TUI-VERDICT: GREEN` from a 32s live pty run; no landed disabled-policy arm on the seat path (see 3, where the mutation probe served as its mechanism-off control) |
| 3 | **Gate sensitivity — does the falsifier falsify?** | `closed-green` | Probe A (seat): removal of the with-colon clause from `classifyRetry` → red `Expected: "retry", Received: undefined` at test line 203; Probe B (parent): forcing `classifyParentTurnRetry` to `undefined` → red `Expected to contain: "Retrying in 1s (attempt 2 of 3) — Esc to abort", Received: ""`. Both restored byte-identically (`cmp` identical; sha256 `f320d8e6…`) and re-run green (26 and 12 expects) |
| 4 | End to end, on the shipped tree | `closed-green` | seat: real `Hub` + real `createRetrySupervisor` + real stub subprocess; parent: real installed pi CLI + real council extension + scratch HOME + real 24×80 pty TUI; diff base→head 5 files +921 with **no engine file touched**; the falsifier runs inside `bun test`, unskipped (all 5 EV-41 tests ran in the full suite) |
| 5 | Base-red evidence (steward ruling) | `closed-green` | detached worktree at `3a3773f`: `job-retry.ts`, `retry.ts`, `parent-retry.ts` all absent, `finish_error` count 0; the copied-in authored falsifier reds `Cannot find module '../extensions/job-retry.ts'` / `'../extensions/retry.ts'` → **`0 pass / 2 fail / 2 errors`** — byte-for-byte the owner's recorded red, and the red *is* the mechanism's absence; no red test landed on the branch (full suite at head: 0 fail) |
| 6 | Gates + gate integrity | `closed-green` | see below |

- **Gate results verbatim at head `0330274` (skeptic, independently re-run):**
  `bash council/preflight.sh EV-41` → exit 1, ending
  `FAIL: local history does not descend from origin/main (stale before running a card)`
  — **the FLLWUP-27 by-construction artifact**, which now fires because the
  facilitator's step-8 record commit advanced `origin/main` (`12e2962` →
  `594e5a0`) past this card's branch cut; recorded verbatim, never
  reclassified, never used to weaken a criterion (the owner's earlier `PASS`
  predates the advance). `bunx tsc --noEmit` exit 0;
  `bun test` → `847 pass, 2 skip, 0 fail` (849 tests, 75 files; skips are the
  two network-gated `COUNCIL_INTEGRATION`/`COUNCIL_MCP_INTEGRATION` tests);
  `python3 council/validate.py` → `All council artifacts valid`.
- **Gate-integrity injections (each restored byte-identically, `cmp`-verified):**
  appended `const BROKEN_TSC_PROBE: number = "not a number"` to
  `test/stub-child.ts` → `error TS2322`, exit 2 (restored → exit 0); a temporary
  invalid `council/cards/ZZ-9.md` → four `FAIL:` lines, exit 1 (removed →
  clean; `board.md`/`EV-41.md` never touched); preflight's own red is
  demonstrated live by the FLLWUP-27 line above.
- **Mutations and restoration (facts):** `extensions/retry.ts` mutated twice for
  probes A/B and restored from `/tmp/retry.ts.orig` (`cmp` identical, sha256
  `f320d8e6…`); `parent-retry.ts` (`1a2edca1…`) and `job-retry.ts`
  (`1cb74d7e…`) byte-unchanged throughout; `test/stub-child.ts` restored
  identical; the temporary base worktrees were removed with
  `git worktree remove --force`.
- **Non-blocking measurement discrepancy (recorded plainly).** The optional
  second pre-mechanism base (`3e39e66`) red counts differ between the owner's
  report (`7 fail / 1 error`) and the skeptic's reproduction
  (`1 fail / 1 error` on a bare copy). The difference tracks how much of
  `test/ev40-harness/` was copied into the base worktree (that directory does
  not exist at `3e39e66`), so it is a property of the copy depth, not of the
  falsifier. The **required** base (`3a3773f`) reproduced exactly. Not a defect
  in any claim.
- **Facilitator's own artifact read (not a seat's report):** head worktree
  `.worktrees/ev-41` clean at `HEAD 0330274`; branch on `origin` unchanged at
  `0330274`; main checkout clean at `594e5a0` and never branch-state-mutated;
  `gh pr checks 57 --json name,state,workflow` →
  `[{name: gates, workflow: gates, state: SUCCESS}]` on the head SHA.
- **Verdict: does not block.** No objection is open; no verify→fix cycle was
  consumed (cycle 1 closes green). The card stays `In Review`; step 10 judging
  may proceed at head `0330274`.

### Step 10 — judge (`job-26.3`) — **PASS**

Dispatched with the card's `goal` and the Skeptic's step-9 evidence only (no
spec, no deliberation, no owner report). Subject: PR #57 head
`03302744ddf0040bdb7d0df6cf883958aacec557`, worktree
`/home/tista/codes/pi-council/.worktrees/ev-41`. 0.9m / 11 turns. Verdict
**PASS** — the single falsifier covers both required paths and all five tests
pass (47 expect calls, 0 failures): the seat-child path (real `Hub` +
`createRetrySupervisor`, attempt-1 `done`/`stopReason=error` with the
with-colon literal, backoff armed, attempt 2 respawned under the same job id,
final report `Settled on attempt 2 of 3`, cumulative `usage.turns=2`, one
manifest row), the `-p` parent path (real installed pi CLI + council extension
+ scratch HOME + explicit env; the countdown on stdout and a second assistant
message `EV40-SECOND-RESPONSE` in the session JSONL; control arm shows neither),
and the TUI parent path (real 24×80 pty TUI, `EV41-TUI-VERDICT: GREEN` for
treatment and control). The judge notes no engine file was modified, only test
files and a plan doc. The card proceeds to step 11.

### Step 11 — deterministic merge check (facilitator, mechanical)

Subject: PR **#57**, head `03302744ddf0040bdb7d0df6cf883958aacec557`. The five
criteria, each observed directly:

1. **Owner gates green, in full** — re-run by the facilitator in the worktree at
   this head: `bunx tsc --noEmit` exit 0; `bun test` → `847 pass, 2 skip, 0 fail`
   (849 tests, 75 files, 94.42s; the 2 skips are the gated
   `COUNCIL_INTEGRATION` / `COUNCIL_MCP_INTEGRATION` round-trips);
   `python3 council/validate.py` → `All council artifacts valid`; the only red is
   `council/preflight.sh EV-41`'s recorded FLLWUP-27 branch-freshness line
   (`FAIL: local history does not descend from origin/main`), the by-construction
   artifact of the facilitator's own record commits advancing `origin/main` past
   the branch cut — recorded verbatim at steps 8/9 and never used to weaken a
   criterion. Independently re-run by the step-9 Skeptic at this head.
2. **GitHub Actions green on the PR head SHA** — `gh pr checks 57 --json
   name,state,workflow` → `[{name: gates, workflow: gates, state: SUCCESS}]`,
   keyed on `workflow`.
3. **No blocking Skeptic objection** — step 9, no open objections (all
   `closed-green`, none `open-untested`).
4. **Judge verdict `PASS`** — step 10.
5. **No `Needs Human` state or outstanding ruling** — none; the steward's
   standing red-at-base ruling and the binding O3 literal constraint are applied
   and recorded at step 1, and no ruling is outstanding.

**Merge:** `headRefOid` re-read immediately before merging → still `0330274`;
`gh pr merge 57 --squash --admin --match-head-commit
03302744ddf0040bdb7d0df6cf883958aacec557` exit 0. **Merged SHA:
`653ce014e3a7a6a63a6f453ae6e77407f16ec639`** (squash, merged
2026-09-17T00:15:45Z).

### Step 12 — sync and reconcile; state `Done`

- **Observed artifact (not a seat report):** `gh run list --commit
  653ce014e3a7a6a63a6f453ae6e77407f16ec639` → the `gates` workflow
  `completed success` on the merged SHA.
- **Sync:** local `main` fast-forwarded `9be60a7` → `653ce01`
  (`git merge --ff-only origin/main`) — clean, no reconcile needed, no
  branch-state mutation against the main checkout.
- **State:** `Done` written to `council/cards/EV-41.md` and `council/board.md`
  from the observed "merged with green CI" artifact; `python3
  council/validate.py` clean; record committed and pushed directly to `main`.
- **Step 13 follow-ups (drafts only, returned to the orchestrator — nothing
  written to `council/cards/`):** (1) the live pty/`-p` parent-turn arms run
  inside the default `bun test` suite and cost ~30–95s per full run — a
  measurement/retention question if suite time becomes binding; (2) the optional
  second pre-mechanism base (`3e39e66`) red counts depend on harness copy depth
  (owner `7 fail / 1 error` vs skeptic `1 fail / 1 error`) — a documented
  red-base convention would make base-red evidence comparable across cards;
  (3) `vault/wiki` still has no page for hub retry / the retry policy /
  per-attempt identity (last ingest EPIC-8), which EPIC-9's closure will want;
  (4) EPIC-9's epic-card closure (`EPIC-9` state `Ready` → `Done`) is a
  **separate later decision for the orchestrator**, deliberately not touched by
  this card.
