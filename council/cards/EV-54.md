---
id: EV-54
title: Documentation of the interview, the tier map, and seat-emphasis notes
state: Backlog
owner: null
epic: EPIC-11
goal: README documents the /council-setup interview, its Jev seat-composition decision with the $CONFIG_DIR_NAME/council/gate/setup override path, the tier map with its $CONFIG_DIR_NAME/council/model-tiers.json override path, and seat-emphasis notes each under a ## level heading, and AGENTS.md convention 5 enumerates the seat-emphasis note as a consumer-side-only resource type and the tier map as a merge-override surface in one block, proven by tests asserting each named string appears under a ## heading in README, that the AGENTS.md convention-5 block names both resources, and that the hard-convention numbering is unchanged.
---

## Intent

The two new resource types must be recorded in convention 5's enumeration without renumbering the hard conventions. The README documentation is user-facing: an owner must be able to find the interview, the tier-map override path, and seat-emphasis notes under searchable headings rather than as strings buried in prose. A user-visible resource type ships with documentation a future seat can cite from a `<repository_grounding>` block. The recut's README section names the `Default`/`Redefine` vocabulary and states that `gate.mode` off or a failed Jev call recommends `Default`.

## Acceptance

Each of `/council-setup`, the Jev seat-composition decision, `model-tiers.json`, and `seat-emphasis` appears under a `##` heading in README; the `/council-setup` README section names the `Default`/`Redefine` vocabulary and states that `gate.mode` off or a failed Jev call recommends `Default`; AGENTS.md convention 5 names the tier map and seat-emphasis resources in one block; the hard-convention numbering is unchanged.