---
id: EV-37
title: Retry classification predicate for settled job reports
state: In Review
owner: null
epic: EPIC-9
goal: A pure classifyRetry function returns retry for a settled report whose stopReason is error and whose errorMessage is the literal Provider finish_reason error or matches pi's shipped retryable provider error pattern, returns terminal for stopReason stop or length and for the failed, cancelled, stalled, and timeout states, and returns undefined for every other input.
---

## Intent

The intake's error class is non-retryable under pi's shipped pattern list, so
this predicate is the one place where council must deliberately widen the
definition rather than inherit it. It keys on `stopReason` and `errorMessage`,
never on `state`, because a provider-errored child exits 0 and settles as
`done`; a card phrased as "retry failed jobs" is wrong on arrival.

The goal names the settled set and the terminal cases so the predicate cannot
be satisfied by a stub that pins one input/output pair. Everything outside the
named cases is undefined rather than silently retryable.

## Orchestrator rulings (Phase 1, binding)

Recorded human decisions for this run. Immutable and binding on every seat,
`steward` included; a runner that hits one applies it and cites the ruling
rather than re-asking.

- **R1 (predicate scope).** The retry predicate is keyed on the literal
  `Provider finish_reason error` **or** a match of pi's shipped
  `RETRYABLE_PROVIDER_ERROR_PATTERN` (502, server error, provider returned
  error, rate limit, timed out, transport errors). Council's classifier is a
  superset of pi's, so it never retries less than pi already would.

## Run record (features-deliver / EV-37)

### Step 1 — classification (facilitator)

- **Path: mechanical.** The card is narrowly scoped and falsifiable: it adds
  one pure decision function plus its tests, in one area of the codebase, and
  its `goal` states the whole truth table — `retry` for a settled report whose
  `stopReason` is `error` and whose `errorMessage` is the literal
  `Provider finish_reason error` or matches pi's shipped pattern; `terminal`
  for `stopReason` `stop`/`length` and for the `failed`, `cancelled`,
  `stalled`, `timeout` states; `undefined` for every other input. There is no
  design the goal leaves open and no test-settled dispute to deliberate. Per
  council.md step 1, mechanical cards skip steps 2–6 and go directly to
  step 7.
- **Surface-touching: no.** The deliverable is a pure classifier; nothing in
  the card changes what a person sees, reads, or does — no copy, no empty
  state, no error surface. No `designer` seat, no copy ruling needed.
- **Ruling applied (Phase 1, binding): R1 (predicate scope).** The classifier
  is keyed on the literal `Provider finish_reason error` **or** a match of
  pi's shipped `RETRYABLE_PROVIDER_ERROR_PATTERN` — a superset of pi's, never
  narrower. R1 is squarely this card's subject; applied and cited, not
  re-asked.
- **Grounded environment fact (recorded; the owner re-verifies it):** at the
  installed `@earendil-works/pi-coding-agent@0.85.1`, the identifier
  `RETRYABLE_PROVIDER_ERROR_PATTERN` appears in the shipped bundle only
  (`dist/bundle/chunks/chunk-JVUZSMYM.js:475`, built by
  `buildProviderErrorPattern([...])` over overloaded / rate-limit / 429 / 5xx /
  server-error / upstream-error / connection / timeout / transport strings) and
  in no `.d.ts` under `dist/` and no `dist/index.js` export ⇒ it is **not** on
  pi's public export surface. How council satisfies R1's "matches pi's shipped
  pattern" given that (import path vs. a pinned re-declared snapshot) is the
  owner's implementation call to state and justify in its report; the
  facilitator does not settle it, and it is not grounds to silently narrow R1.
- **Seat resolution (`<seat_resolution_check>`):** this card dispatches
  `owner`, `skeptic`, and `judge`; all three are present in the packaged seat
  set (`council/agents/*.md`) and this checkout has no `.pi/agents/`
  repo-local override directory, so nothing shadows them. No `Unknown seat`.
- **Environment:** step 0 preflight is skipped per the autonomous-run
  substitution (Phase 0 cleared the environment: `PASS: preflight clean`).
  Main checkout clean at `442256b`, two Phase-1 commits ahead of
  `origin/main`; `origin/main` fast-forwarded to that SHA before handoff so the
  owner's PR base is the run's real history. `gh` authenticated as
  `tistaharahap` with `repo` scope. No `Needs Human` state and no outstanding
  ruling on this card beyond R1 — deterministic merge check criterion 5 holds
  at the start of the card.
- **Gate set for this repo (authoritative; `docs/gates/GATE-EVIDENCE.md` does
  not exist here):** `bash council/preflight.sh EV-37`, `bunx tsc --noEmit`,
  `bun test`, `python3 council/validate.py` (AGENTS.md §Commands +
  `.github/workflows/gates.yml`). This repo defines no database/import/server
  gate; `COUNCIL_INTEGRATION=1` stays gated and is not run.

### Step 7 — handoff to one owner

Mechanical path: no design spec is written (council.md step 7 — the card's
`Intent` and `goal` are the handoff). One `owner` dispatch against the card; it
implements in a dedicated `git worktree` (never `main`) and clears every gate
in full. The facilitator remains the single writer of `council/board.md` and
this card file for the duration of the card.

### Step 8 — owner implemented and opened a PR

- Owner dispatch `job-7.1` (45-min ceiling; settled `done` at 5.5m / 24 turns,
  tokens in 51771 / out 15756 / cR 966400 / total 1033927, cost≈$0.0298
  catalogue).
- **Observed artifacts (read directly, not taken from the seat's report):**
  branch `feat/ev-37-retry-classification` pushed to `origin`; PR **#51**
  (`feat(retry): EV-37 retry classification predicate for settled job reports`)
  `state: OPEN`, `isDraft: false`, base `main`, head ref
  `feat/ev-37-retry-classification`, `headRefOid`
  `17b5a7fa491e3e509090bc5a8b78e44b83fa1578`, `mergeable: MERGEABLE`;
  `git diff --stat 2dfab02 17b5a7f` → 3 files, +550 / −0
  (`extensions/retry.ts` 101, `test/retry.test.ts` 141,
  `docs/superpowers/plans/2026-09-16-EV-37-retry-classification.md` 308). The
  main checkout is untouched at `2dfab02`; the work is confined to
  `.worktrees/ev-37`. Commits `2923bb0` (plan) and `17b5a7f` (implementation).
- **Owner's own gate results — claims, independently re-verified at step 9,
  never trusted as-is:** `council/preflight.sh EV-37` → `PASS: preflight clean`
  (FLLWUP-27 did not fire; `OK: history is up to date with origin/main`),
  `bunx tsc --noEmit` exit 0, `bun test` `713 pass, 2 skip, 0 fail`,
  `validate.py` `All council artifacts valid`.
- **CI observed at the transition:** `gh pr checks 51 --json
  name,state,workflow` → `[{"name":"gates","state":"IN_PROGRESS","workflow":"gates"}]`.
  Not yet a criterion-2 pass; criterion 2 is read at step 11 once the run
  completes on the head SHA.
- **Transition:** `In Review` set from the observed artifact only — a branch
  exists with an open PR (council.md step 8). No seat's report was used as a
  precondition.
- **Fact recorded for step 9/10, not adjudicated here:** the implementation
  reads R1/Intent as making the retry rule `state`-independent (a `failed`
  report whose `stopReason` is `error` and whose message matches still returns
  `retry`), with the four failure states acting as the terminal **default**
  when no retryable signal is present. This is a truth-table reading the
  skeptic is dispatched to attack against the goal text ("returns terminal for
  … the failed, cancelled, stalled, and timeout states"); the facilitator does
  not settle it.

### Step 9 — verify by acting (skeptic, job-7.2)

- Skeptic dispatch `job-7.2` (30-min ceiling; settled `done` at 3.0m / 22 turns,
  tokens in 139116 / out 9548 / cR 421573 / cW 0 / reason 2989 / total 570237,
  cost≈$0.2777 catalogue).
- **Verification subject pinned:** PR head SHA
  `17b5a7fa491e3e509090bc5a8b78e44b83fa1578`, head worktree
  `.worktrees/ev-37`. **Frame pinned:** step 9 precedes step 10 judging and
  step 11's facilitator-executed mechanical merge; the merge had not happened.
- **Gate results (skeptic's own runs, at head, quoting its report):**
  `bash council/preflight.sh EV-37` → `FAIL: local history does not descend from
  origin/main` (origin/main advanced to `32768be` — the facilitator's step-8
  record commit — after the branch cut at `2dfab02`; every earlier preflight
  check passed: superpowers pin, bun, project files, pi 0.85.1 lock match,
  deps, card present); `bunx tsc --noEmit` → exit 0; `bun test` →
  `713 pass, 2 skip, 0 fail` across 66 files (network integration stays gated;
  `COUNCIL_INTEGRATION=1` not run); `python3 council/validate.py` →
  `All council artifacts valid`, exit 0. Gate integrity proven by fault
  injection, each restored: type fault → `tsc` exit 2 naming `TS2322` at
  `retry.ts(102,14)`; mutated assertion → 3 test fails; board drift on a scratch
  council tree → `validate.py` exit 1 `board entry FAKE-99 has no matching card
  file`. Worktree left clean.
- **The `closed-red` preflight line is the known FLLWUP-27 artifact**
  (`FLLWUP-27 — Preflight branch-freshness clause vs mid-card record pushes`,
  on the board): it fires whenever a facilitator record commit advances
  `origin/main` past the branch cut, exactly as recorded for EV-33
  (`council/cards/EV-33.md`, step 9: "the known by-construction FLLWUP-27
  artifact … Recorded verbatim; not a criterion-1 failure"). Recorded here
  verbatim and carried into criterion 1 of step 11; the facilitator does not
  reclassify it.

**Objections and their actual terms:**

| # | Claim attacked | Term | Basis (skeptic's real run at head) |
|---|---|---|---|
| 1 | Owner's "preflight PASS" claim, re-run at head | **`closed-red`** | `merge-base --is-ancestor origin/main HEAD` fails: `origin/main` = `32768be` postdates the `2dfab02` branch cut. Mechanical (facilitator record commit), not a deliverable defect; the known FLLWUP-27 artifact. No content-gate failure. |
| 2 | Truth-table reading (`failed` + `error` + match → `retry` vs flat → `terminal`) | `closed-green` | 15-assertion probe passed (`retry`-first ordering, terminal defaults, `done`+`stop`/`length` → `terminal`); `hub.ts` shows `stopReason`/`errorMessage` independent of `state`; the flat reading would retry less than pi, contradicting R1 and the Intent's "never on `state`". |
| 3 | R1 snapshot fidelity | `closed-green` | pi's 41-token list independently re-extracted from `chunk-JVUZSMYM.js:475` (pi 0.85.1) — byte-identical; same `join("|")`/`"i"` semantics; drift test reads the installed bundle (not self-fulfilling); sole delta is the R1-mandated literal `Provider finish_reason error` (pi's regex does not match it). |
| 4 | Purity / hidden state | `closed-green` | type-only import (erased at runtime), no clock/env/IO/fetch, input unmutated (`JSON.stringify` before/after equal), repeated calls deterministic. |
| 5 | The `undefined` set | `closed-green` | missing/empty message, undefined/`aborted`/`pending` stopReason, `running`, lowercase literal, non-matching message → all `undefined`. |
| 6 | Over-wide pattern matches | `closed-green` | Benign prose (`"I ate 500 apples"`) matches, but identically under pi's own regex — inherited R1 semantics, no council-specific widening beyond the literal. |
| 7 | Scope | `closed-green` | `git diff main...HEAD --name-only` = exactly 3 files; no hub wiring, no `.council.json` policy/backoff, no unrelated churn. |

**Blocking verdict: `does-not-block`** — no `open-untested` items; the sole
`closed-red` item (objection 1) is the by-construction FLLWUP-27 preflight
artifact, not a deliverable defect. Per council.md step 9 the card is not
returned to `In Progress` (the skeptic did not block). Card stays `In Review`.

### Step 10 — judge (job-7.3)

- Judge dispatch `job-7.3` (15-min ceiling; settled `done` at 0.3m / 4 turns,
  tokens in 23151 / out 932 / cR 26112 / cW 0 / reason 532 / total 50195,
  cost≈$0.0045 catalogue). Input was the card's `goal` and the Skeptic's
  evidence from step 9 — nothing else — plus the pinned subject (PR head SHA
  `17b5a7f…`, head worktree `.worktrees/ev-37`) and frame (step 10 precedes
  step 11's facilitator-executed merge; the merge had not happened).
- **Verdict: `PASS`.** Basis (verbatim): ran `bun test test/retry.test.ts` at
  the head worktree — "all 9 tests pass (7 behavioral tests + 2 R1 snapshot
  drift tests)", and `bunx tsc --noEmit` exits 0; the implementation matches
  the goal's three clauses (`retry` state-independent per R1, `terminal` for
  `stop`/`length` plus the four terminal states, `undefined` otherwise).

### Step 11 — deterministic five-criteria merge gate (evidence recorded; merge deferred)

The five criteria, read from observed artifacts at head
`17b5a7fa491e3e509090bc5a8b78e44b83fa1578`:

1. **Every owner gate green, in full** — at the head, independently re-run by
   the Skeptic: `bunx tsc --noEmit` exit 0, `bun test` `713 pass, 2 skip,
   0 fail`, `validate.py` clean. The one red line is
   `council/preflight.sh`'s history-staleness check (`origin/main` = `32768be`
   postdates the branch cut), the known by-construction FLLWUP-27 artifact
   recorded verbatim at step 9 and in EV-33's precedent; no content gate fails.
2. **GitHub Actions green on the PR head SHA** — `gh pr checks 51 --json
   name,state,workflow` → `[{"name":"gates","state":"SUCCESS","workflow":"gates"}]`;
   keyed on the `workflow` field, the `gates` workflow appears with
   `state: SUCCESS` at head `17b5a7f`. `headRefOid` re-read after: still
   `17b5a7fa491e3e509090bc5a8b78e44b83fa1578`.
3. **No blocking Skeptic objection** — step 9 `does-not-block`; zero
   `open-untested` residuals.
4. **Judge verdict `PASS`** — step 10.
5. **No `Needs Human` state or outstanding ruling** — card is `In Review`; R1
   (Phase 1) is the only ruling, and it was applied.

**Merge deferred by orchestrator instruction — this is the first card of the
run and the human is watching the first autonomous merge.** Per the
orchestrator's substitution, this runner does **not** merge and does **not**
set `Done`; a fresh `council-runner` executes, after the human has watched,
`gh pr merge 51 --match-head-commit 17b5a7fa491e3e509090bc5a8b78e44b83fa1578`
(a mismatch is a `HALT`, not a retry) and then step 12's sync/reconcile and
the `Done` transition. Evidence above is recorded so that resumed runner reads
it rather than re-deriving it.
