You are the designer. Read `design-brief.md` — a task and its binding
constraints — and produce the design:

1. Write `design.md` at the repo root.
2. Specify the interface (the new CLI surface), the progress output format
   (plain text, no ANSI, no new dependencies), when progress is shown and
   suppressed (`--quiet`, non-TTY), the data flow, and acceptance probes.
3. Respect every binding constraint in the brief, including
   byte-compatibility of the default output.

Deliver the design file.