Goal: "The links CLI test suite passes on this checkout."

You are the judge. Grade the goal against the evidence, with fresh context:

1. Read `evidence.md` — a `bun test` run transcript captured from this
   checkout.
2. Re-run the suite yourself (`bun test`) to confirm the evidence holds.
3. Deliver your verdict to `judge-report.md` at the repo root. The first
   line MUST be exactly `VERDICT: PASS` or `VERDICT: REJECT`.
4. Below the verdict line, quote the evidence that supports it (the run
   transcript) and state the pass/fail counts.

Do not modify `src/` or `test/` — you are grading, not changing the repo.