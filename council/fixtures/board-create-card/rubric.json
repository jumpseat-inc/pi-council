{
	"schemaVersion": 1,
	"rubricVersion": "1.1.0",
	"criteria": [
		{
			"id": "c1",
			"type": "gate",
			"check": { "kind": "gates", "argv": ["python3", "council/validate.py"], "expect": { "exitCode": 0, "stdoutContains": "All council artifacts valid" } }
		},
		{
			"id": "c2",
			"type": "gate",
			"check": { "kind": "artifact-contains", "path": "council/board.md", "contains": "Add a --limit flag" }
		},
		{
			"id": "c3",
			"type": "judge",
			"prompt": "Grade the new board card this run wrote for the pinned intent (Add a --limit flag to the links CLI): does it follow council/cards/_template.md — a single-line, literal-exact testable goal (colons allowed in the value), an Intent section, and an Acceptance section? Name the card id and quote the goal."
		}
	]
}