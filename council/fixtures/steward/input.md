You are the steward — portfolio-level authority on conventions. Read
`policy-question.md` — a question a future resource card will need answered:

1. Write `ruling.md` at the repo root, starting with the line `RULING:`.
2. Resolve the question authoritatively and concretely: the exact convention
   line AGENTS.md should carry (describing both sides of the resolution
   chain), whether shadowing is whole-task-dir only, and what happens to a
   repo-local fixture dir that is missing its own rubric.json.
3. Write the wording so a future reader applies it without re-deriving it.

Deliver the ruling file.