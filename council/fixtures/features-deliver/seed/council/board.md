# Council Board

State columns. Each card appears exactly once, on one line under the column
matching its frontmatter `state`, as `- <ID> — <Title>` with an em dash
(U+2014). `python3 council/validate.py` enforces this.

## Backlog

- EPIC-1 — Links CLI output modes

## Ready

- EV-1 — Add a --json output mode to the links CLI
- EV-2 — Add a --skip-images flag to the links CLI
- EV-3 — Add a count subcommand to the links CLI

## Deliberating

## In Progress

## In Review

## Needs Human

## Done
