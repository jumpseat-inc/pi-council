# EV-82 implementation plan — Record and render the follow-up decision at step 13's pre-write confirm gate

Card: `council/cards/EV-82.md` · Spec: `docs/superpowers/specs/2026-09-22-EV-82-design.md` (authoritative).
Worktree: `/home/tista/codes/pi-council/.worktrees/ev-82`, branch `ev-82-step13-followup-render`.
All work TDD: every test item below is written and observed RED before the implementation that
satisfies it lands. `gate-ledger.ts`, `gate.ts`, `gate-run.ts`, `followup-state.ts` are UNCHANGED.

## TDD order

### Phase A — red tests first (three files, observed red)

**`test/ev82-followup-tool.test.ts`** (ev81 fixture pattern: mkdtemp repoRoot, injected
transport doubles, repo-local `.council.json` + `council/gate/policy.json`):

1. **Off no-op precedes the loaders** — off-policy-omits-budget fixture (`.council.json`
   `gate.mode:"off"` + policy.json WITHOUT `gateStateBudgetTokens`); `runFollowupReview`
   resolves `{mode:"off", recorded:0}` without throwing (a post-packer short-circuit would
   throw the packer's `gateStateBudgetTokens` FAIL); transport never called; zero ledger
   lines; no ledger file.
2. **One ledger line per candidate on both arms** — advisory + active arms: success arm
   records `resolvedMode` = the disposition, `advisory` = the policy mode, `policyVersion` =
   `loadFollowupDecision().version` (via loader, never literal); failure arm (network double)
   records `failure.class` + `advisory`, basis `gate call failed: <reason>`; exactly one line
   per candidate on each arm.
3. **Thrown pre-POST guard** — advisory policy with a wrong model (guard throws pre-POST):
   every candidate yields one mechanical entry with `callId: null`, a GENERIC message that
   never contains `runFollowupGate`'s guard text ("pinned decisions model"), zero ledger lines.
4. **Same-title draft set (skeptic O2)** — two candidates sharing a title: `buildFollowupState`
   throws per candidate → both entries `callId: null`, zero ledger lines, transport never
   called, one entry per candidate (the confirm gate proceeds; the human is the fail-safe).
5. **Mid-run flip** — first invocation under `gate.mode:"off"` → off no-op; rewrite
   `.council.json` to `advisory`; second invocation → advisory shape with recorded candidates.
6. **Mechanical-result opacity + open-only projection** — the result JSON carries no
   disposition/basis/`Mode:` token (key walk + regex over the stringified result); with a
   board fixture holding an open and a Done entry, `boardIds` is the open-only projection
   (the Done id never appears); `siblingTitles` are the other candidates' titles.
7. **FLLWUP-96 composition** — success path reaches `buildFollowupState` + `runFollowupGate`
   (status ok, ledger line exists, `boardIds` populated from the real board fixture); a
   mismatched pair (repo-local questions.json carrying a `score` question — refused in the
   followup domain) fails LOUD from the loader before any candidate work, zero ledger lines.

**`test/ev82-followup-render.test.ts`** (pure: hand-built `GateLedgerRecord` fixtures;
no fs beyond the tool-level qualifier test's tmpdir ledger):

8. **Resolver precedence** — `resolveFollowupTarget`: board-id-exact and sibling-title-exact
   both match → resolves (board id wins by consultation order, same string); unresolvable →
   throws the named failure; a Done-id reference (absent from the open-only lists by
   construction) → throws.
9. **Unresolved Merge, two states that must not collapse** — Merge record with NO target
   supplied → no-arrow no-placeholder line (falls to the human); a SUPPLIED reference that
   resolves to nothing → `renderFollowupLines` throws loud.
10. **callId cell table** — ok → disposition line; `no-api-key` → `credential unresolved`;
    other failure → `gate call failed: <reason>` verbatim exactly once; `http-404` →
    `model-card coming-soon`; callId set + no record → join-miss fallback; `callId: null` →
    unrecorded fallback; no rendered line contains `gate: gate call failed:` (no double colon).
11. **D4 aggregation triple** — all candidates share `credential unresolved` → exactly one
    literal, no per-candidate lines; mixed ok + transport failure → A's `Mode:` line AND B's
    per-card literal; two differing total-failure states → per-card literals only, no
    above-drafts line.
12. **Qualifier from the record** — `advisory: true` vs `false` → byte-differing lines; a
    record under advisory rendered by the TOOL against a repo whose `.council.json` says
    `active` still renders `(advisory)` (the record, never a render-time config re-read).
13. **One line per candidate, verbatim title** — two ok candidates → exactly two lines;
    a 200-char title renders all 200 chars, no ellipsis.
14. **Off-literal input shape (skeptic O4)** — `renderFollowupLines({mode:"off",
    candidates:[]}, [])` → exactly `["off"]`.
15. **Reason flattening (skeptic O5)** — a newline-bearing failure body renders as ONE line,
    newline → space, `gate call failed:` prefix intact.
16. **D3 discriminator** — for every disposition (File/Merge/Drop) × mode (advisory/active) ×
    target-supplied (yes/no) cell: `renderLine.includes(decisionLine(record))` is true.
17. **Render purity canary (skeptic O3)** — source-string scan of
    `extensions/followup-render.ts`: no `node:fs` import, no `fs.` usage, no `gate-run.ts`
    import, no `followup-tool.ts` import.

**`test/prose.test.ts` additions** (packaged `council/` idiom):

18. **Grammar-owner scan** — no `Mode: `-prefixed format-expression string literal outside
    `gate-ledger.ts` (string-literal scope: template/quote-delimited, comment-stripped);
    passes at head without editing shipped files.
19. **Step 13 names the pair, the states, the pass** — council.md step 13 (sliced
    `## 13. Card the follow-ups` → `## 14. Persist`; R0a: the step, never a line range)
    names `council_followup_gate` + `council_followup_render`, the three enablement states
    (off = no decision recorded, unqualified human review, tools still invoked; advisory =
    information, never enforced; active = applied only after confirmation), and the
    unconditional dedup pass in every mode including `off` (board / open cards / siblings).
20. **Pre-write pin** — `confirmed at ledger level` appears in council.md only inside a
    negating sentence; `council/cards/FLLWUP-69.md` stays `state: Backlog`, `epic: EPIC-9`.
21. **Apply clause** — step 13 states the applying arm executes the recorded disposition the
    line names, never the current configuration.
22. **Presented-never-written (skeptic O1)** — anchored multiline scan `^Mode: (File|Merge|Drop) — `
    over `council/cards/*.md` + `council/board.md` yields ZERO hits; step 13 carries the exact
    `## Merged from:` template (heading + trailing colon, blank line, one bullet per source
    title) named as post-confirmation-only.

### Phase B — green, per module (one commit per module, suite green at each)

- `extensions/followup-tool.ts`: `runFollowupReview` (off short-circuit FIRST; then
  `loadFollowupQuestions` + `loadFollowupDecision`; sequential per candidate:
  `buildFollowupState(candidate, repoRoot, siblings, <repoRoot>/council/board.md)` +
  `runFollowupGate`; per-candidate catch → `callId: null` generic, zero ledger lines;
  open-only `boardIds` projection; optional `setWidget` hook) + `renderFollowupInFlight`
  (`followup gate: call in progress · <title>`) + thin parent-only `council_followup_gate`.
- `extensions/followup-render.ts` (pure): `renderFollowupLines` (callId join; byte order
  `decisionLine(record) + " — " + title + (target ? " → " + target : "") + " (" + qualifier +
  ")"`; qualifier from the record's `advisory`; D4 aggregation; literals per R8; reason
  flattening; off literal; fallback literals) + `resolveFollowupTarget` + `followupSuffix` +
  thin parent-only `council_followup_render` (reads the ledger via `readGateLedger` only).
- `extensions/index.ts`: register both tools in the parent path only, after
  `registerGateRenderTool`, never folded into `registerHubTools`.
- `council/procedures/council.md`: step-13 interior insert (the record call; the three
  states; the unconditional dedup pass as the merge-target producer; the pre-write pin with
  `confirmed at ledger level` only inside its negating sentence; the `## Merged from:`
  template, post-confirmation-only; confirmed Drop files nothing). The FLLWUP-41/60 step-12
  pins slice `## 12.` → `## 13. Card the follow-ups`; an interior insert moves nothing.
- `package.json`: version 0.31.0 → 0.32.0 (minor: new tools + behavior change).

## Gates (in the worktree, in order, before opening/updating the PR)

1. `bun install` (if needed) then `bunx tsc --noEmit` — clean.
2. `bun test` — 0 fail (opt-in skips allowed).
3. `python3 council/validate.py` — "All council artifacts valid".
4. `bash council/preflight.sh` — PASS on the branch (branch descends from origin/main,
   verified at worktree creation; merge origin/main first if the record commits move it).

Then push, `gh pr create` to `main`, report PR number + head SHA. No CI polling.
