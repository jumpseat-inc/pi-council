# FLLWUP-115 — Implementation Plan (owner)

Spec: docs/superpowers/specs/2026-09-24-fllwup-115-design.md (authoritative — do not reopen).
Card: council/cards/FLLWUP-115.md. Epic: EPIC-24. Branch: `fllwup-115-frontmatter-epic-parse`.

## Mechanism (one sentence)

Scope `cardEpicKey`'s `^epic:\s*(.*)$` match to the **frontmatter block** (never
strip-then-match — that inverts the parse), sharing one `FRONTMATTER_RE` constant
with `readProcedureBody`'s strip, keeping the three D1 refusal messages
byte-for-byte identical, exporting `cardEpicKey`/`epicKeyFromFace` for the corpus
pin, and bumping the version in the same PR.

## Steps

1. **Worktree** — `git worktree add /tmp/fllwup-115 -b fllwup-115-frontmatter-epic-parse`;
   `bun install --frozen-lockfile` there. Main checkout untouched.
2. **T1 red-at-base (seven-field record)** — write `test/card-epic-key.test.ts`
   with T1 only (compose-path falsifier through `composeRunnerInput` on a face
   with no frontmatter epic + body `epic: EPIC-9`), transplant it into a detached
   worktree at base `03717b04778fd6276348a9737cfe3aefa664fdc2`, run
   `bun test test/card-epic-key.test.ts` → observe red (compose succeeds with
   `EPIC-9` in the dispatch), record the raw output in the test header comment.
   Base role: required (base lacks the frontmatter-scoped mechanism).
3. **T2–T4 + mechanism** — add T2 (precedence pin: frontmatter `EPIC-A` +
   body `EPIC-B` → `EPIC-A`), T3 (byte-exact refusal pins ×3), T4 (203-card
   equivalence sweep, whole-file oracle inline, live `epicKeyFromFace` as the
   new side, iterating `path.join(repoRoot, "council", "cards")` — repo root
   derived from `import.meta.dir`, never `PKG_ROOT`); then the mechanism in
   `extensions/seats.ts` (shared `FRONTMATTER_RE`, pure `epicKeyFromFace`,
   throws stay in `cardEpicKey`, export both) and the version bump
   0.36.1 → 0.36.2 in the same commit. Watch T1 red→green locally before
   committing (TDD: the falsifier must fail on the unmechanized tree in step 2
   and pass after).
4. **Gates, in order** — `council/preflight.sh FLLWUP-115` on the worktree;
   `bunx tsc --noEmit`; full `bun test` (≈101s, no integration probes).
5. **PR** — push branch, `gh pr create` naming FLLWUP-115, mechanism, test set
   (T1–T4), red-base record (seven fields), version bump. End turn; no CI
   polling, no merge.

## Non-goals

No changes to `council/` (board, cards, procedures); `test/ev90-runner-input.test.ts`
untouched; no corpus-hygiene assertion (the nine body occurrences are dispositioned
in T4's header comment only).
