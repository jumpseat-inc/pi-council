# FLLWUP-105 Implementation Plan — stale usages skill remediation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the binding remediation sentence in `council/procedures/usages.md`'s
`**Report.**` section plus a mechanical three-literal pin in `test/`, and record
the delete-and-recopy route in `vault/raw/`.

**Architecture:** Three artifacts on one feature branch, exactly per the settled
spec (`docs/superpowers/specs/2026-09-24-FLLWUP-105-design.md`): the V1 sentence
(product-owner ruling Q1, byte-identical), a new pin test
`test/usages-procedure.test.ts` asserting containment of exactly three literals,
and a `vault/raw/` note naming the package-update-first precondition and the
ingest correction target. No engine change; the pin reads the packaged procedure
via `PKG_ROOT` from `extensions/seats.ts` (hard convention 4).

**Tech Stack:** TypeScript (bun:test), markdown, bun + `python3 council/validate.py`.

**Spec:** `docs/superpowers/specs/2026-09-24-FLLWUP-105-design.md` (settled —
nothing here reopens V1/V2, the pin scope, or the R3 boundary).

## Global Constraints

- Ship-text is the V1 sentence from PO ruling Q1, byte-identical; placed in the
  `**Report.**` section after the existing paragraph; no other sentence altered.
- Pin asserts exactly three literals — `usages: could not write cache:`,
  `skills/usages/`, `/council-init` — containment only: no 4th literal, no
  negative assertions, no section anchor, no ordering assertions, no
  full-sentence match.
- Never `council/validate.py` for the pin (docs-card rule); never an edit to
  `test/scaffold.test.ts`; T-USK1 stays green and unedited.
- No `TOOLING_FILES` widening; no `$CONFIG_DIR_NAME` or `@CONFIG_DIR@` token in
  the procedure file; no hardcoded `.pi` package paths (use `PKG_ROOT`).
- Main repo branch state immutable: all edits and gates in `/tmp/fllwup105-wt`
  on branch `fllwup-105-usages-remediation-pin`; PR opened, not merged (R1
  merge authorization belongs to the orchestrator).

## Review Focus

- Byte-identity of the V1 sentence against the ruling (a single-word drift
  silently changes consumer guidance) — pinned by the three-literal test plus a
  manual diff against the card's Q1 block.
- Pin scoping: a repo-wide scan false-greens on `usages.py`/`index.ts`/
  `scaffold.ts` (skeptic O-scope) — pinned by reading the procedure file path
  only, and by the red-arm run in Task 1.
- Prohibition tokens: `$CONFIG_DIR_NAME`/`@CONFIG_DIR@` must not enter the
  procedure file (`renderProcedure` substitutes neither) — checked by grep in
  Task 2's verification.

---

### Task 1: Pin test (TDD red first)

**Files:**
- Create: `test/usages-procedure.test.ts`

**Interfaces:**
- Consumes: `PKG_ROOT` (exported from `extensions/seats.ts`).
- Produces: `test/usages-procedure.test.ts` — red on the pre-sentence file,
  green after Task 2 adds the sentence.

- [ ] **Step 1: Write the pin test** — new file importing `PKG_ROOT` from
  `../extensions/seats.ts`, reading
  `path.join(PKG_ROOT, "council", "procedures", "usages.md")`, asserting
  containment of exactly the three literals (`bun:test`, relative import).

- [ ] **Step 2: Confirm red** — `bun test test/usages-procedure.test.ts`;
  expected fail (`expect(received).toContain(expected)` on the trigger literal)
  against the sentence-less procedure. Capture output verbatim.

### Task 2: The remediation sentence (green)

**Files:**
- Modify: `council/procedures/usages.md` (`**Report.**` section, after the
  existing paragraph)

**Interfaces:**
- Consumes: the red pin from Task 1.
- Produces: green pin; procedure text carries the V1 sentence byte-identical
  to PO ruling Q1.

- [ ] **Step 1: Append the V1 sentence** to the `**Report.**` section after the
  existing paragraph — exact text from the spec's §1 blockquote; touch nothing
  else.

- [ ] **Step 2: Confirm green** — `bun test test/usages-procedure.test.ts`;
  capture output. Verify byte-identity by diffing the appended sentence against
  the card's Q1 text, and grep the file for `$CONFIG_DIR_NAME`/`@CONFIG_DIR@`
  (expected: no matches).

- [ ] **Step 3: Commit** — `docs(council): FLLWUP-105 step 8 — pin test + V1 remediation sentence in /usages Report section`
  (test + sentence + plan in one coherent commit or plan committed separately
  first; conventional-commit format).

### Task 3: The vault/raw note

**Files:**
- Create: `vault/raw/2026-09-24-fllwup-105-stale-usages-skill-recopy.md`

**Interfaces:**
- Consumes: nothing (prose artifact).
- Produces: the acceptance-required note: delete-and-recopy is the only route
  for an already-initialized consumer; package-update-first precondition with
  the pre-BUG-2 diff evidence; ingest correction target
  `vault/wiki/non-clobbering-scaffold.md:72-76`; `vault/wiki/usages-report.md`
  needs no correction.

- [ ] **Step 1: Write the note** per the spec's §3 shape.
- [ ] **Step 2: Commit** — `docs(vault): FLLWUP-105 raw note — stale copied usages skill reaches the fixed tool only via update-then-delete-recopy`.

### Task 4: Gates (full, in order, in the worktree)

- [ ] **Gate 1:** `council/preflight.sh FLLWUP-105` → all OK, no FAIL line.
- [ ] **Gate 2:** `bunx tsc --noEmit` → exit 0.
- [ ] **Gate 3:** `bun test` (full suite) → 0 fail; T-USK1 green and unedited
  (`git diff main -- test/scaffold.test.ts` empty).
- [ ] **Gate 4:** `python3 council/validate.py` → `All council artifacts valid`.

### Task 5: Push + PR (do not merge)

- [ ] **Step 1:** `git push -u origin fllwup-105-usages-remediation-pin`.
- [ ] **Step 2:** `gh pr create --base main --title "FLLWUP-105: name the remediation route for a stale copied usages skill" --body-file <body>` (summary, acceptance checklist, gate results).
- [ ] **Step 3:** Record branch, PR number/URL, head SHA (`gh pr view --json headRefOid`); end turn; no CI polling.
