---
title: Smoke Test v0.12.0 — Clean Green
type: source
summary: The first clean unattended smoke-test run of the v0.12.0 series — full /council loop + /features-deliver epic green from a fresh container, reconfirming the harness is now a stability gate rather than a bug-hunter.
aliases: [smoke v0.12.0, 2026-08-26 smoke test, clean green]
tags: [pi-council/smoke-test, pi-council/release]
sources: ["[[smoke-test]]", "[[pi-council-overview]]"]
created: 2026-08-26
updated: 2026-08-26
---

# Smoke Test — v0.12.0 (clean green)

Source: the unattended smoke-test run of **v0.12.0** on 2026-08-26. One command
(`bun run smoke` → `bash smoke/run.sh`), fresh `node:24-bookworm` container,
OpenRouter key injected, repo bind-mounted at `/pkg`. Hard-fail semantics —
zero retries, any red is red. Artifacts: `smoke/.artifacts/20260826-010108`.

**Outcome: SMOKE PASS** (`EXIT=0`) — full council loop + epic delivery verified.

## The three phases

- **Phase 0 (scaffold/install/preflight): PASS** — `/council-init` created the
  skeleton non-clobberingly (`.council.json`, `board.md`, `preflight.sh`,
  `.pi/council/mcp.json` all skipped as already-present); `All council artifacts
  valid`; preflight `PASS: preflight clean`.
- **Phase 1 (council loop EV-1, `--count`): PASS** — Judge PASS; harness played
  the human at the merge gate (merged `ev-1-count-flag` into local main);
  kill-shots: `tsc` clean, **5/5** tests pass, `--count` → `3`.
- **Phase 2 (epic EPIC-1, `--json` + `--images`): PASS** — both children merged
  on the deterministic check per the WATCH ruling (no human watcher for the
  first two autonomous merges: EV-2 → `10e585b`, EV-3 → `ea42a7c`); follow-ups
  FLLWUP-1/FLLWUP-2 filed; kill-shots: `tsc` clean, **18/18** tests pass, exact
  output probes, flag-conflict exit 2; council-runner dispatch evidence in
  `runs/`.

## What this run means

The v0.10.0 round caught 3 real bugs in its first pass (see
[[2026-08-25-smoke-test-bugfixes]]) — the test's birth. By contrast this
v0.12.0 run is a **clean full-container green**, the strongest signal yet that
the harness and the engine are stable. The "never trust a claim" philosophy is
reconfirmed: PASS came from the harness re-running reality (`tsc`, the growing
5→18 test suite, exact CLI probes) — not from the Judge's prose.

## Test-suite growth in one round

Phase 1 asserted a 5-test suite; Phase 2 asserted an 18-test suite (both 0
fail). The smoke test now exercises the engine's own growth as it runs.

## Related

- [[smoke-test]] — the concept page; the v0.12.0 clean-green extends its Track record
- [[pi-council-overview]] — the release arc; v0.12.0 is the theme + ingest release
- [[2026-08-25-smoke-test-bugfixes]] — the v0.10.0 bug-catching round that birth the test
- [[council-theme]] — the EPIC-1 subsystem, green under the smoke run
- [[headless-pi]], [[council-runner]] — the operating modes and autonomous merge the run depends on

## Sources

- `smoke/run.sh`, `smoke/driver.sh`, `smoke/assert.sh`
- `smoke/.artifacts/20260826-010108` (run forensics)
- `vault/raw/2026-08-26-smoke-v0.12.0.md` (raw record)
