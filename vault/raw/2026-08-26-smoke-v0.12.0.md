# Smoke Test — v0.12.0 (2026-08-26)

Record of the unattended smoke-test run of pi-council **v0.12.0**. One command
(`bun run smoke` → `bash smoke/run.sh`), fresh `node:24-bookworm` Docker
container, OpenRouter key injected, repo bind-mounted at `/pkg`. Harness
hard-fails on any red (zero retries). Artifacts: `smoke/.artifacts/20260826-010108`.

## Outcome

**SMOKE PASS** — full council loop + epic delivery verified. `EXIT=0`.

## Prerequisites at run time

- `OPENROUTER_API_KEY` set
- Docker server `29.5.3`

## Phase 0 — scaffold / install / preflight: PASS

- Seeded worktree, `pi install -l /pkg`
- `/council-init`: scaffold created (`_template.md`, `validate.py`, vault
  skeleton) with non-clobber skips confirmed for `.council.json`, `board.md`,
  `preflight.sh`, `.pi/council/mcp.json`
- `All council artifacts valid`; preflight `PASS: preflight clean`

## Phase 1 — council loop EV-1 (`--count` flag): PASS

- Council run returned Judge **PASS**; harness played the human at the merge
  gate (merged `ev-1-count-flag` into local main)
- Kill-shot probes: `tsc --noEmit` clean; `bun test` **5 pass / 0 fail**;
  functional `--count` → `3` on `sample.md`

## Phase 2 — epic delivery EPIC-1 (`--json`, `--images`): PASS

- Ledger written to `council/runs/EPIC-1-ledger.md`, committed to main
- Deterministic merge check (no human watcher for first two autonomous merges,
  per WATCH ruling) — EV-2 `--json` merged at 10e585b, EV-3 `--images` merged
  at ea42a7c
- Follow-ups FLLWUP-1 (unknown `--`-prefixed token) and FLLWUP-2 (balanced
  parens/brackets) filed as Backlog cards; cosmetic note ruled DROP
- Kill-shot probes: `tsc` clean; `bun test` **18 pass / 0 fail**; exact
  `--json` and `--images` output probes; flag-conflict exit 2
- council-runner dispatch evidence present under `runs/`

## Test-suite growth

- Phase 1 ran 5 tests; Phase 2 ran 18 tests (both / 0 fail)
